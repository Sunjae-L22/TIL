# 기계학습 기초 (AI & 기계학습 기초 1~4차시)

> 출처: `1_1_기계학습기초.pdf` (총 179페이지, SSAFY · 서울대 오민환 교수) · 정리일: 2026-08-14
> 페이지 표기는 PDF 기준 (슬라이드 인쇄 번호와 동일)

## 한눈에 보기

- AI ⊃ ML ⊃ DL 관계를 설명하고, ML이 아닌 AI(규칙 기반)를 구분할 수 있는가?
- 데이터(Feature/Label) → 가설공간 → 모델 → 학습이라는 틀로 $Y = f^*(X) + \epsilon$의 각 기호를 설명할 수 있는가?
- 회귀/분류 각각에 맞는 평가지표(MSE·R² vs Accuracy·혼동행렬)와 손실(MSE vs Cross Entropy)을 고를 수 있는가?
- 오버피팅이 왜 생기고, 테스트 오류를 검증셋/K-겹 교차검증으로 어떻게 추정하는가?
- K-means와 계층적 군집의 동작 원리 차이(K 지정 여부, 병합 방식, 계산량)를 설명할 수 있는가?

## 목차

1. [1차시 AI와 ML은 무엇인가](#1차시-ai와-ml은-무엇인가) (p.1-50)
2. [2차시 지도학습은 무엇인가](#2차시-지도학습은-무엇인가) (p.51-103)
3. [3차시 교차검증](#3차시-교차검증) (p.104-134)
4. [4차시 비지도 학습은 무엇인가](#4차시-비지도-학습은-무엇인가) (p.135-167)
5. [부록 참고자료와 실습 미리보기](#부록-참고자료와-실습-미리보기) (p.168-179)

---

## 1차시 AI와 ML은 무엇인가

### 머신러닝의 순환 루프 (p.5-7, 13-14)

슬라이드 전체를 관통하는 그림 하나. 이 자료의 1~3차시가 전부 이 루프의 각 칸을 확대한 것이다.

```text
데이터 → 모델 → 학습 → 평가
          ↑______________|   (평가 결과를 바탕으로 반복 개선)
```

- 머신러닝 = 데이터를 이용해 모델을 학습하고, 평가 결과를 바탕으로 데이터·모델·학습 방법을 반복적으로 개선하는 과정.
- 유튜브 추천, 스팸 필터의 공통점: 사람이 모든 규칙을 미리 코딩하지 않아도 **데이터에서 규칙을 학습**해 성능을 향상시킨다 (p.6).

### AI, ML, DL의 정의 (p.8-11)

포함 관계: **AI ⊃ ML ⊃ DL** (벤다이어그램).

| 용어 | 정의 | 예 |
|---|---|---|
| AI | 주어진 환경/데이터를 인지·학습·추론을 통해 목표 달성을 하도록 예측·행동 선택·계획하는 시스템 | — |
| ML | AI 범주 내에서 **데이터로부터 학습**하여 목적을 달성하는 접근 방법론 | 생성형 AI, 언어 모델, 이미지 분류, 추천 시스템 |
| DL | ML 범주 내에서 **신경망(Neural Network) 함수**를 사용한 학습 방법론 | — |
| AI − ML | 데이터 학습 없이 동작하는 AI | 규칙 기반 시스템, 휴리스틱 기반 (최적화)알고리즘 |

> **주의** — "규칙 기반 시스템은 ML의 한 분야"는 틀린 문장(확인문제 1). 규칙 기반은 ML이 아닌 AI의 대표 예시다.

### 데이터의 구성 요소: Feature와 Label (p.12-18)

머신러닝은 규칙을 직접 코딩하지 않고 데이터에서 규칙을 학습하므로, **데이터(Feature, Label)의 분포와 관계가 학습 결과를 결정**한다.

| 구성 요소 | 정의 | 유튜브 추천 예 | 스팸 분류 예 |
|---|---|---|---|
| Feature (피처, 특성) | 모델이 예측에 사용하는 입력정보. 예측·판단의 근거/단서 | 영상 정보(장르, 크리에이터, 조회수, 좋아요 수), 사용자 정보(시청 이력, 구독 채널) | 메일 제목, 발신자, 단어 빈도 |
| Label (라벨, 목표값) | 모델이 예측하려는 정답. 학습의 목표값 | 사용자 피드백(시청 여부, 좋아요 클릭 여부) | 스팸 / 정상 |

### 단일(1D) 피쳐 기반 학습 (p.19-25)

Feature가 하나(1차원)일 때의 가장 단순한 학습 형태. 교육 연수(Years of Education)로 수입(Income)을 예측하는 30명 데이터로 설명한다.

$$\mathrm{Income}_i = f^*(\mathrm{Years\ of\ Education}_i) + \epsilon_i$$

$$D = \{(\mathrm{Years\ of\ Education}_i,\ \mathrm{Income}_i)\}_{i=1}^{30}$$

수식의 각 요소가 이 자료의 핵심 용어다.

- **미지의 참 함수 $f^*$** — Feature와 Label 사이의 실제 평균 관계. 직접 관측할 수 없고, 오차가 포함된 데이터(점)만 관측 가능하다.
- **측정 오차 $\epsilon$** — 데이터에는 측정 기기의 한계, 환경적 요인 등으로 오차가 섞여 있다. 따라서 관측되는 데이터 = 참 함수 + 오차($f^* + \epsilon$).
- 데이터를 설명하는 함수 후보는 여러 개 존재한다(후보모델 1·2·3, p.25) → 어떤 함수가 가장 잘 맞는지 **학습**해야 한다.

### 모델과 가설 공간 (p.26-29)

| 용어 | 정의 |
|---|---|
| 학습 (Learning) | "입력(Feature) → 출력(Label)" 관계를 찾는 과정. 평균 관계를 하나의 함수로 표현. 단, 표현 가능한 함수는 무수히 많음 |
| 가설 공간 (Hypothesis Space) $\mathcal{F}$ | 관계를 표현할 수 있는 **모든 후보 함수들의 모음**. 피쳐 공간과 라벨 공간 위에서 정의된 함수들의 집합 |
| 모델 (Model) | 가설공간 $\mathcal{F}$에 속한 **특정 함수 $f$** |

- 예: "무수히 많은 선형 함수들의 집합"이 가설 공간이라면, 그중 특정 선형 함수 하나가 모델.
- 가설공간은 선형함수로 잡을 수도, 비선형함수로 잡을 수도 있다(p.29). 같은 데이터라도 가설공간 선택에 따라 학습되는 모델이 달라진다 — S자형 데이터에 비선형 가설공간을 쓰면 곡선 모델이 나온다.

### 학습이란 (p.30-33)

학습 = 주어진 데이터에서 정답을 가장 잘 맞출 수 있도록 **모델의 규칙(파라미터)을 조금씩 조정해가는 과정**. 관점: 데이터 $D$ → 가설공간 $\mathcal{F}$ → 선택된 모델 $f$.

학습에 필요한 3가지:

1. **데이터 (Data)** — 입력(특성)과 정답(라벨) 쌍으로 된 정답 모음 (예: 학력-수입 쌍 30개)
2. **가설 공간 (Hypothesis Space)** — 선택할 수 있는 모든 후보 함수들의 집합
3. **선택 기준 (손실 함수)** — 어떤 함수가 더 좋은지 판단하는 척도. 예측값과 실제값의 차이를 측정

학습 과정 5단계:

1. 가설공간에서 하나의 함수를 선택
2. 그 함수로 데이터의 모든 예시를 예측
3. 손실함수로 틀린 정도 계산
4. 더 적게 틀리도록 함수의 파라미터 조정
5. 반복하여 최적의 모델 완성

역할극으로 보면 (p.33): 데이터 $D$가 문제를 제시하고("교육 연수 12년이면 수입 2200만원, 16년이면 3500만원…"), 가설공간 $\mathcal{F}$가 선택지를 제공하고("$y = ax + b$ 형태의 모든 1차 함수"), 학습이 예측 오차가 가장 작은 최적의 $f$를 선택한다.

### 복수(2D+) 피쳐 기반 학습과 일반적 표기 (p.34-42)

피쳐가 2개가 되면 참 함수는 3차원 공간의 곡면(surface)이 된다.

$$\mathrm{Income} = f^*(\mathrm{Years\ of\ Education},\ \mathrm{Seniority}) + \epsilon$$

- 파란색 surface(= 미지의 참 함수 $f^*$)는 관측 불가능, 빨간색 점들(= 데이터)만 관측 가능 (p.36).
- 학습 전: 어떤 가설공간 $\mathcal{F}$을 사용할까? / 학습 후: 데이터를 활용하여 어떤 모델 $f$을 선택해야 할까?

일반적 용어 정리 (p.40) — 이후 모든 차시에서 쓰는 표기:

$$Y = f^*(X) + \epsilon, \qquad X = \begin{bmatrix} X_1 \\ X_2 \\ \vdots \\ X_p \end{bmatrix} \in \mathbb{R}^p, \qquad f^*: \mathbb{R}^p \to \mathbb{R}$$

- $Y$: 예측하려는 라벨(반응/목표) 변수 (Income)
- $X_1, X_2, \dots$: 피쳐(입력/예측) 변수들 (Years of Education, Seniority, …). 총 $p$개의 피쳐를 벡터 $X$로 묶는다
- 측정오차 $\epsilon$: **피쳐 $X$와 독립**이고 $\mathbb{E}[\epsilon] = 0$으로 가정

왜 $f$를 학습하는가 (p.42):

| 목적 | 의미 |
|---|---|
| 예측 | 잘 학습된 $f$로 새로운 입력 $x$에서 목표 $Y$를 예측 |
| 중요 특성 파악 | 피쳐 $X = (X_1, \dots, X_p)$ 중 어떤 특성이 $Y$ 설명에 중요한지 파악 (예: 교육·근속 연수는 소득에 영향 크지만 혼인 여부는 거의 무관) |
| 해석 가능성 | $f$의 복잡도에 따라 각 $X_i$가 $Y$에 어떤 영향(증가/감소 방향, 민감도)을 주는지 이해 |

### 1차시 확인문제 (p.43-48)

1. AI, ML, DL의 관계에 대한 설명으로 옳은 것은? — <details><summary>답</summary>③ ML은 AI의 한 부분으로, 데이터로부터 학습을 수행한다. (AI는 규칙/휴리스틱 기반만으로도 구현 가능, 규칙 기반 시스템은 AI의 예시이지 ML이 아님)</details>
2. Feature와 Label에 대한 설명으로 옳지 않은 것은? — <details><summary>답</summary>④ "Feature는 반드시 수치형 데이터만 될 수 있다"가 틀림. 범주형, 텍스트, 이미지/음성 등도 가능. Label은 분류에서는 카테고리, 회귀에서는 수치값</details>
3. 참함수 $f(X)$를 학습하는 이유로 적절하지 않은 것은? — <details><summary>답</summary>② "모든 데이터의 측정오차($\epsilon$)를 제거하기 위해". 학습을 잘 해도 측정오차는 줄이거나 제거할 수 없다</details>

강의 정리 슬라이드(p.49-50)는 위 내용의 요약이라 생략.

---

## 2차시 지도학습은 무엇인가

### 지도학습의 개념 (p.51-59)

**지도학습(Supervised Learning)** = 정답 라벨이 있는 훈련 데이터를 사용해 예측 모델을 학습하는 방법. "처음 보는 문제도 잘 푸는 AI 만들기."

- 데이터: 입력(feature, $x$)과 정답(label, $y$)이 **항상 쌍으로** 존재, 여러 개 축적되어 있음
- 목표: 훈련 데이터뿐 아니라 **처음 보는(새로운) 데이터에서도** 정확한 예측 (→ 이 "일반화"가 4절과 3차시의 주제)
- 예: 고객 정보 + 탈퇴 여부 → 내일 이탈할 고객 예측 / 기존 사기 거래 데이터 → 새로운 사기 탐지
- 훈련 데이터(train set)에 라벨링 → 학습 → 모델 생성 → 테스트 데이터(test set)로 결과 확인 (p.59, 개·고양이 분류 그림)

### 회귀 vs 분류, 그리고 손실 함수 (p.60-63)

| 구분 | 회귀 (Regression) | 분류 (Classification) |
|---|---|---|
| 예측 대상 | 결과값이 **숫자**(연속값) | 결과값이 **범주**(카테고리) |
| 예시 | 가격, 점수, 온도 | 스팸/정상, 질병 유/무, 고양이/강아지/햄스터 |
| 대표 손실 | 평균제곱오차 $\mathrm{MSE} = \frac{1}{n}\sum(y - \hat{y})^2$ | 교차 엔트로피 $\mathrm{CE} = -\sum y \times \log(\hat{y})$ |
| 손실의 직관 | 정답과 예측의 차이를 제곱해 평균. 많이 틀릴수록 제곱 때문에 손실이 크게 커짐 | 정답 범주에 얼마나 높은 확률을 주었는지 측정. 정답을 확신할수록 0에 가까워짐 |

- **손실 함수(Loss Function)** = 모델의 예측이 정답에서 얼마나 벗어났는지를 하나의 숫자로 나타내는 함수. **학습이란 이 숫자가 작아지는 방향으로 모델을 조금씩 고쳐 나가는 과정**이다.

지도학습 용어 (p.63):

| 용어 | 기호 | 의미 |
|---|---|---|
| 특성 | $x$ | 예측에 사용되는 설명 변수. 예측 함수 $f$의 input (집값 예측: {지역, 평수, 방수, 연식}) |
| 라벨 | $y$ | 맞춰야 하는 정답(실제값) |
| 예측값 | $\hat{y}$ | 학습 과정에서 모델이 내놓은 결과(숫자 또는 범주). $y$의 추정치 |
| 오류 (Error) | $\hat{y} - y$ | 예측값과 라벨의 차이. 손실 함수로 계산. **오류 ≠ 측정 오차** — 오류를 줄이는 것이 지도학습의 목표 |

### 회귀와 평가지표: MSE, RMSE, R² (p.64-74)

회귀 = 입력(feature)으로부터 숫자(output)를 얼마나 정확히 예측할까? 라벨과 출력이 연속적인 수치.

예시 데이터 (p.66, 원문 값):

| 면적($m^2$) | 방 수 | 연식 | 집 값(원) |
|---|---|---|---|
| 85 | 3 | 5 | 4억2천 |
| 102 | 4 | 2 | 5억8천 |
| 70 | 2 | 10 | 3억5천 |
| 90 | 3 | 3 | ? |

(매출액 예측도 동일 구조: TV/라디오/인터넷 광고비 → 매출액)

**평균제곱오차 (Mean Squared Error)** — 각 데이터에서 정답($y_i$)과 예측($\hat{y}_i$)의 평균 제곱 차이값:

$$\mathrm{MSE} = \frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2$$

- 틀린 정도를 **제곱해서 더 큰 오류에 페널티**를 부여하고, 전체 평균 오류 수준을 한눈에 볼 수 있다.
- 단위가 제곱이라 해석이 어려우면 원래 데이터와 같은 단위인 $\mathrm{RMSE} = \sqrt{\mathrm{MSE}}$ 사용.

**결정계수 $R^2$** — Label($y$)의 분산 중에서 Feature로 설명되는 비율:

$$R^2 = 1 - \frac{\sum_{i=1}^{n}(y_i - \hat{y}_i)^2}{\sum_{i=1}^{n}(y_i - \bar{y})^2} \qquad (\bar{y} = y_i\text{들의 평균값})$$

- "평균만 쓰는 단순한 예측"보다 모델이 실제값을 얼마나 더 잘 맞추는지를 0~1 사이 값으로 표현. 1에 가까울수록 설명력이 높다.
- 분자는 모델의 제곱오차, 분모는 평균으로만 예측했을 때의 제곱오차. 즉 **기준선(평균 예측) 대비 상대 성능**이다.

> **질문(강조)** — $R^2$가 음수가 나올 수 있을까? → 나올 수 있음. 예측값 $\hat{y}_i$들이 평균값 $\bar{y}$보다도 못한다면 분수가 1을 넘어 음수가 된다.

MSE vs $R^2$ 비교 (p.71, 원문 표):

| 항목 | MSE | $R^2$ |
|---|---|---|
| 정의 | 예측값과 실제값 차이의 제곱 평균 | 평균 예측 대비 모델의 상대적 성능 |
| 값 범위 | 0 ~ ∞ (무한대) | 0 ~ 1 |
| 단위 | 실제 데이터 단위² | 단위 없음 |
| 해석 | 절댓값 (구체적 오차 크기) | 상대적 비율 (성능 정도) |
| 좋은 값 | 0에 가까울수록 좋음 | 1에 가까울수록 좋음 |
| 장점 | 구체적 오차 크기 알 수 있음 | 직관적 해석, 모델 간 비교 쉬움 |
| 단점 | 직관적 해석 어려움 | 구체적 오차 크기 모름 |

- 예: 집값 예측에서 MSE = (1000만원)²라면 좋은지 나쁜지 애매하지만, $R^2 = 0.8$("평균값만으로 예측할 때 발생하는 제곱오차의 80%를 줄였다")이면 "꽤 좋은 모델"이라 판단 가능 (p.74).
- 사용 시점: MSE는 구체적 오차 크기·실제 손실 비용이 중요할 때, $R^2$는 직관적 평가·여러 모델 비교 시. **상호 보완적으로 함께 사용**.

### 분류와 평가지표: Accuracy, 혼동행렬, Cross Entropy (p.75-86)

분류 = 입력 특성으로부터 출력이 범주값(카테고리)으로 나타나는 예측 문제. 라벨은 미리 정해진 범주 중 하나(이진/다중).

- 이진 예: 스팸/정상, 악성/양성 종양. 다중 예: 학점(A/B/C) 예측.
- **Decision Boundary**: 서로 다른 클래스 영역을 구분하는 경계선. 분류 모델은 이 선을 기준으로 데이터를 구분한다 (p.78-79).

**정확도(Accuracy)** — 분류는 Label이 범주라 MSE나 $R^2$를 쓰기 어렵다. 대신 전체 예측 중 올바르게 맞춘 비율:

$$\mathrm{Accuracy} = \frac{1}{n}\sum_{i=1}^{n} \mathbb{1}(y_i = \hat{y}_i)$$

- $\mathbb{1}$은 지시(indicator) 함수: 조건이 참이면 1, 거짓이면 0. 실제=고양이, 예측=고양이 → 1(맞춤); 예측=강아지 → 0(틀림).

> **정확도의 함정(불균형 데이터)** — 질병 진단 1000명(환자 10명 1%, 건강 990명 99%)에서 무조건 "건강"이라 예측해도 정확도 99%. 하지만 질병 환자를 전부 놓쳐 "질병 발견" 능력은 0이다. **정확도만 보지 말고 혼동행렬 등 다른 지표도 함께 볼 것** (p.82-83).

**혼동행렬(Confusion Matrix)** — 예측과 실제 값 사이의 관계를 행렬로 표현 (p.84-85):

| | 예측 Positive | 예측 Negative |
|---|---|---|
| **실제 Positive** | TP (True Positive) | FN (False Negative, 누락) — Type II Error |
| **실제 Negative** | FP (False Positive, 오탐) — Type I Error | TN (True Negative) |

| 지표 | 수식 | 의미 |
|---|---|---|
| 정밀도 (Precision) | $\frac{TP}{TP+FP}$ | "양성이라 **판정한 것 중**" 진짜 양성의 비율 |
| 재현율 (Sensitivity/Recall) | $\frac{TP}{TP+FN}$ | "**진짜 양성 가운데**" 잡아낸 예측 양성 비율 |
| 특이도 (Specificity) | $\frac{TN}{TN+FP}$ | 실제 음성 중 음성으로 맞춘 비율 |
| 정확도 (Accuracy) | $\frac{TP+TN}{TP+TN+FP+FN}$ | 전체 중 맞춘 비율 |
| F1-score | $2 \times \frac{\text{정밀도} \times \text{재현율}}{\text{정밀도} + \text{재현율}}$ | 정밀도와 재현율의 조화평균 |

**교차 엔트로피(Cross Entropy)** — 정확도·혼동행렬은 학습이 끝난 결과를 "평가"하는 지표다. 맞음/틀림만 세므로 예측 확률이 0.51 → 0.95로 좋아져도 값이 그대로. 학습 중에는 "정답을 얼마나 확신했는지"까지 반영하는 손실이 필요하다:

$$\text{손실} = -\log(\text{정답 범주에 준 확률})$$

| 실제 정답 | 스팸일 확률 예측 | 손실 $-\log(p)$ |
|---|---|---|
| 스팸 | 0.9 | 0.11 (작음) |
| 스팸 | 0.6 | 0.51 |
| 스팸 | 0.1 | 2.30 (큼) |

- 정답에 준 확률이 높을수록 손실이 작아지고, 확률이 낮으면 손실이 급격히 커진다. 회귀의 MSE에 해당하는 분류의 대표 손실 함수.
- 정확도·정밀도·재현율 = 결과를 "평가"하는 지표 / 교차 엔트로피 = 학습에서 "줄여 나가는" 손실.

### 학습의 목적: 일반화와 오버피팅 (p.87-93)

- 학습의 목적 = 훈련 데이터로 학습한 모델이 **새로운(처음 보는) 데이터에서도** 정확한 예측을 하는 것.
- 따라서 성능 평가는 학습에 사용되지 않은 데이터로 한다 → **일반화(generalization) 오류의 최소화** 지향. 훈련 성능이 아무리 좋아도 새 데이터에서 성능이 떨어지면 실전에 못 쓴다.

**오버피팅(overfitting)** (p.90-91):

- 훈련 데이터의 **우연한 패턴/잡음까지 외워버려서** 훈련에서는 잘 맞지만 테스트에서는 성능이 나빠지는 현상.
- 현상: 훈련 오류 급격히 낮음, 테스트 오류 높음/요동.
- 왜 나쁜가: ① **표본 의존·불안정** — 훈련 데이터는 모집단의 일부 표본이라 우연한 잡음이 섞여 있는데, 여기에만 과하게 맞추면 샘플 몇 개만 바뀌어도 예측이 크게 흔들림(분산↑). ② **일반화 실패** — 테스트 오류가 커지고 모집단(population) 성능과 격차가 벌어짐.

> **오해 주의** — 오버피팅 ≠ **분포 변화(distribution shift)**로 인한 에러 증가 (p.92). 훈련 분포와 테스트 분포가 다르면(환경·계절·센서 변경, 실사 고양이 사진으로 훈련하고 일러스트로 테스트 등) 모델이 과적합하지 않아도 성능이 떨어질 수 있다.

| 구분 | 오버피팅 (과적합) | 언더피팅 (과소적합) |
|---|---|---|
| 원인 | 모델이 너무 복잡 | 모델이 단순하거나 학습이 완료되지 않음 |
| 결과 | 잡음까지 학습 → 훈련 오류 작고 테스트 성능 나쁨 | 중요한 패턴을 놓침 → 훈련/테스트 모두 오류 큼 |
| 해결 실마리 | 더 많은 데이터, 테스트 데이터를 활용한 모델 선정, 교차 검증(3차시) | 모델 복잡도를 높이면 개선 가능 |

### 2차시 확인문제 (p.94-99)

1. 지도학습에 대한 설명으로 옳은 것은? — <details><summary>답</summary>② 새로운 입력에 대해 정답을 잘 맞추는 함수를 학습한다. (라벨 없는 학습은 비지도학습, 오버피팅은 피해야 할 현상, 출력은 수치·범주 모두 가능, 목적은 측정오차가 아니라 예측 오류 최소화)</details>
2. 회귀 문제의 특징으로 옳은 것은? — <details><summary>답</summary>② 예측값은 연속적인 숫자(가격, 점수, 온도). (범주 예측은 분류, Accuracy는 분류 지표, 회귀는 MSE/RMSE/R² 사용)</details>
3. 혼동행렬에 대한 설명으로 옳지 않은 것은? — <details><summary>답</summary>⑤ "혼동행렬은 회귀 문제의 성능 평가에서 사용된다"가 틀림. 혼동행렬은 분류용, 회귀는 MSE/RMSE/R²</details>

강의 정리 슬라이드(p.100-103)는 위 표들의 요약이라 생략.

---

## 3차시 교차검증

### 훈련 오류 vs 테스트 오류 (p.104-111)

| 구분 | 정의 | 비유 |
|---|---|---|
| 훈련 오류 | 모델을 학습시킨 **같은 데이터**에 다시 적용해 계산한 오류 | 문제집 점수 |
| 테스트 오류 | 학습에 쓰지 않은 **새 관측치**에 대한 평균 예측오류 | 시험 점수 |

- 문제집만 잘 보면 시험을 잘 본다고 장담할 수 없음 — 보통 훈련 오류는 테스트 오류를 (심하게) **과소평가**한다 (암기 vs 응용 시험).
- 모델 복잡도(Model Complexity)를 높여 가면: **훈련 오류는 계속 감소**하지만 **테스트 오류는 U자형** (p.111).
  - 왼쪽(단순): High Bias·Low Variance = 과소적합 / 오른쪽(복잡): Low Bias·High Variance = 과적합
  - 목적: 테스트 오류 U자형의 **바닥**이 되는 적절한 모델 찾기. "너무 많이 외우지도(과적합), 너무 대충 공부하지도 않은(과소적합) 지점".

### 테스트 오류를 어떻게 추정하나: 재표본화 (p.112-113)

- 이상적으론 충분히 큰 별도 테스트 데이터셋이 있어야 하지만, 현실에선 데이터 자체가 부족해 구하기 어렵다.
- 대안: **재표본화(resampling)** — 데이터를 나눠 여러 번 "훈련→평가"를 반복해 테스트 오류를 추정. "시험용 문제가 따로 없다면, 기존 문제집으로 모의고사를 보자."
- 방법: 검증 홀드아웃(hold-out), K-겹 교차검증(K-fold Cross-Validation).
- 장점: 별도의 테스트 데이터 없이 데이터를 더 효율적으로 사용하여 일반화 오차 추정.

### 검증셋(홀드아웃) 접근 (p.114-119)

절차 (p.115-116):

1. 가용 데이터를 **무작위로** 훈련셋과 검증셋(hold-out)으로 분할 (문제집을 학습용/모의고사용으로 나누고 모의고사는 딱 1번만 보기)
2. 훈련셋으로 모델 적합, 검증셋으로 예측 후 검증 오류 계산 — 정량 반응은 MSE, 범주 반응은 오분류율(또는 F1-score)

자동차 데이터 예시 (p.117): 392개 데이터를 196/196으로 분할해 1차~10차 다항 모델의 검증 MSE 곡선을 그림. 분할을 바꿔 여러 번 반복하면 곡선이 크게 출렁인다.

한계 (p.118-119):

- 어떤 표본이 훈련/검증에 들어가느냐에 따라 **테스트 오류 추정치가 매우 가변적**.
- 훈련셋(=전체의 일부)만으로 적합하므로, 전체 데이터로 학습했을 때보다 성능이 낮게 추정 → **테스트 오류를 과대 추정**하는 경향. (훈련에 데이터의 절반만 쓰면 전체로 학습한 경우보다 성능이 낮을 수밖에 없음)

### K-겹 교차검증 (p.120-126)

테스트 오류 추정의 표준 접근. "문제집을 5등분 해서 서로 다른 모의고사를 5번 보는 것."

절차 (p.121-122):

1. 데이터를 셔플링한 뒤 크기가 같고 **겹치지 않는** K개 폴드로 무작위 분할
2. 폴드 $k \in \{1, \dots, K\}$를 검증셋, 나머지 $K-1$개 폴드를 훈련에 사용
3. $k = 1, \dots, K$ 반복 후 K개의 오류를 평균해 테스트 오류를 추정

$$\mathrm{CV}_{(K)} = \sum_{k=1}^{K} \frac{n_k}{n} \mathrm{MSE}_k, \qquad \mathrm{MSE}_k = \frac{1}{n_k} \sum_{i \in C_k} (y_i - \hat{y}_i)^2$$

- $C_k$는 k번째 폴드의 인덱스 집합, $n_k$는 폴드 크기. $\hat{y}_i$는 **k-폴드를 검증셋으로 두고 나머지로 적합한** 예측.
- 각 폴드 MSE를 폴드 크기 비율 $n_k/n$로 가중 평균하는 구조 — 폴드 크기가 같으면 단순 평균과 같다.
- 추정치는 모델 선택과 최종 모델의 테스트 오류 규모 파악에 사용.

**Leave-One-Out 교차검증 (LOOCV)** (p.123-125):

- $K = n$인 특수 경우. 관측치 하나만 검증셋(크기 1), 나머지 전부로 훈련 — 이걸 n번 반복. "100개 문제로 100개의 서로 다른 모의고사를 만드는 것."
- LOOCV vs 10-겹 비교(자동차 데이터): "99문제 학습 후 1문제 평가 100번" vs "90문제 학습 후 10문제 평가 10번" — 두 방법의 **경향과 최적 차수가 비슷**.
- LOOCV는 모든 데이터를 검증에 쓰지만 **연산량이 많아 학습 시간이 오래 걸림**. 큰 성능 향상이 없으므로 적절한 K를 선택한 K-겹 교차검증을 쓰는 게 좋다.
- 시뮬레이션(p.126): 참(test) MSE 곡선 vs LOOCV 추정(검은 점선) vs 10-겹 CV 추정(주황) — 10-겹 CV 추정은 테스트 성능의 좋은 대안.

### 3차시 확인문제 (p.127-132)

1. 훈련 오류와 테스트 오류에 대한 설명으로 옳은 것은? — <details><summary>답</summary>③ 궁극적으로 테스트 오류를 최소화하는 것이 학습의 목적. (일반적으로 훈련 오류 < 테스트 오류이고, 훈련 오류가 0에 가까우면 오버피팅 가능성이 높음)</details>
2. 언더피팅과 오버피팅의 특징을 올바르게 설명한 것은? — <details><summary>답</summary>① 언더피팅은 훈련 오류와 테스트 오류 모두 크게 나타난다. (오버피팅은 훈련 오류 작고 테스트 오류 큼. 복잡도를 높이면 언더피팅은 개선되지만 오버피팅은 심해짐)</details>
3. K-겹 교차검증에 대한 설명으로 옳지 않은 것은? — <details><summary>답</summary>⑤ "각 폴드에 속한 데이터가 겹쳐도 무방하다"가 틀림. 폴드는 겹치지 않게 명확히 분리해야 한다. (K=n일 경우 LOOCV라는 선지는 옳은 문장)</details>

강의 정리 슬라이드(p.133-134)는 위 내용의 요약이라 생략.

---

## 4차시 비지도 학습은 무엇인가

### 비지도학습이란 (p.135-140)

**비지도학습** = 레이블(정답) 없이 데이터의 구조·패턴·집단(잠재 서브그룹)을 찾아내는 학습.

- 대표 과제: 군집화(clustering), 차원축소(PCA 등), 밀도추정/이상치 탐지
- 출력: "정답 예측"이 아니라 구조/요약/표현(embedding)
- 핵심 질문: ① 무엇을 비슷함/다름으로 볼 것인가(거리·유사도 선택) ② 전처리(스케일 표준화 등)를 어떻게 할 것인가

| 구분 | 지도학습 | 비지도학습 |
|---|---|---|
| 입력 데이터 | 입력 + 라벨(정답) 쌍 | 입력만 |
| 학습 대상 | 예측 모델 | 데이터의 구조 |
| 예 | 가격 예측, 악성 종양 예측 | 고객 세그먼트, 비슷한 뉴스 묶기 |

### 클러스터링 (p.141-144)

**클러스터링** = 데이터 안에서 하위 집단(클러스터)을 찾는 기법들의 총칭. **집단 내부는 서로 유사하게, 집단 간은 상이하게** 데이터를 분할한다.

- 집단 내부 예: 쇼핑몰에서 "비슷한 취향의 고객 그룹" / 집단 사이 예: 고가 제품만 사는 고객 vs 할인 제품만 사는 고객
- 유사/상이의 기준은 문제·도메인에 따라 달라지고, 특성 선택·알고리즘에 따라 결과가 달라질 수 있다.
- 대표 사례: 마케팅 세그먼테이션 — 다수 지표(가구 소득, 직업, 도심 거리 등)로 특정 광고/상품에 더 반응할 하위집단을 식별. 시장 세분화 작업 자체가 클러스터링이다 (p.143).

두 가지 대표 기법: **K-평균(K-means)** — 클러스터 수 K를 미리 정해 분할 / **계층적 군집(Hierarchical)** — K를 사전에 고정하지 않음.

### K-means 클러스터링 (p.145-151)

표기 (p.147-148) — K-means가 만드는 군집 $C_1, \dots, C_K$는 관측치 인덱스의 **비중첩 분할**:

$$C_1 \cup \cdots \cup C_K = \{1, \dots, n\}, \qquad C_k \cap C_{k'} = \varnothing \ (k \neq k')$$

- 모든 데이터를 빠짐없이(어느 한 군집에는 반드시 포함), 겹치지 않게(동시에 여러 그룹 소속 불가), 정확히 하나의 군집에 나눈다. $i$번째 관측치가 $k$번째 군집이면 $i \in C_k$.
- 핵심 아이디어: 좋은 군집화 = **클러스터 내부 변동(Within-Cluster Variation)이 작은 분할**. "그룹 안에서 얼마나 모여 있는지"를 기준으로 K개의 그룹을 만드는 알고리즘.

알고리즘 (p.149-150):

1. **초기화**: 관측치들에 무작위로 $1 \cdots K$ 클러스터를 임시 부여
2. **반복** (할당이 더 이상 바뀌지 않을 때까지):
   - 2a. 각 클러스터의 **중심(centroid)** 계산 (특성 평균 벡터)
   - 2b. 각 관측치를 **가장 가까운 중심**의 클러스터에 재할당 (거리 예 = 유클리드)

특성:

- 중심점과의 평균 거리가 계속 줄어들기 때문에 매 반복마다 결과는 더 좋아짐 (단조 개선).
- 하지만 최적 해를 항상 찾는 건 아님 → 초기값에 따라 **지역 최솟값(local minimum)으로 수렴** 가능.
- 초기값의 영향 (p.151): 서로 다른 초기 레이블에서 최종 분할과 목표값이 달라짐 (예: 목표값 235.8이 최선인데 320.9, 310.9로 수렴하는 경우 존재) → **여러 번 시도 권장**.

### 계층적 군집 (p.152-159)

- K-means는 K를 미리 지정해야 하는 단점이 있다. 계층적 군집은 K를 고정하지 않고 전체 구조를 **덴드로그램(dendrogram)**으로 제공한다.
- 이 강의는 **상향식(agglomerative, 병합)** 방식을 다룸: 잎(leaf, 실제 데이터) → 몸통(root)으로 병합.
- 덴드로그램에서 수평선 높이(거리)를 기준으로 "가위질"하면 원하는 개수의 군집을 얻는다 (p.154).

상향식 알고리즘 (p.155):

1. n개 관측에 대해 쌍별 비유사도 계산, 각 관측치를 하나의 클러스터로 시작
2. $i = n, n-1, \dots, 2$에 대해 반복: $i$개 클러스터 간 쌍별 비유사도를 모두 계산해 **가장 유사한 두 클러스터를 병합**
3. 병합 후 남은 $i-1$개 클러스터 사이의 새 비유사도를 다시 계산 (1개가 될 때까지)

> **계산량 주의** (p.157) — 매 단계에서 모든 클러스터 쌍 간의 거리를 계산해야 하므로, 데이터가 많으면 K-means에 비해 계산량이 많다.

클러스터 간 거리(링크) 유형 (p.158-159):

| 링크 | 군집 간 거리 정의 |
|---|---|
| Single (최소 거리) | 두 클러스터 내 데이터 쌍별 거리 중 **최소값** |
| Complete (최대 거리) | 두 클러스터 내 데이터 쌍별 거리 중 **최대값** |
| Average (평균 거리) | 두 클러스터 내 데이터 쌍별 거리의 **평균** |

- 같은 데이터라도 링크 선택에 따라 클러스터링 결과(덴드로그램)가 달라질 수 있다 → 하나의 링크만 쓰지 말고 다른 종류도 시도 권장.

### 클러스터링 시 주의점 (p.160-161)

- **스케일링**: 표준화(평균 0, 표준편차 1로 입력 변수 변환)가 필요한가? → Yes, 변수 단위 차이의 영향이 크다.
- **몇 개의 클러스터가 적합한지?** → K-means·계층적 모두 어려운 문제, 합의된 정답 없음.
- 단일 시도가 아닌 **여러 번 시도** 권장.

### 4차시 확인문제 (p.162-165)

1. 비지도 학습의 예로 해당하는 것은? — <details><summary>답</summary>C. (라벨이 없는 데이터에서) 구매 성향별 고객 묶음 — 정답 없는 묶음 문제, 전형적인 클러스터링. (스팸 분류·집값 예측·날씨 예측은 지도학습)</details>
2. 각 설명이 K-means의 특징인지, 계층적 군집의 특징인지 분류하시오. (A. 클러스터 수 K 미리 지정 / B. 덴드로그램으로 전체 구조 시각화 / C. 데이터 간 병합(상향식) / D. 클러스터 내부 변동 합 최소화 분할 / E. 매우 큰 데이터셋에도 효율적) — <details><summary>답</summary>K-means: A, D, E / 계층적 군집: B, C. K-means는 빠르고 대규모 데이터에 효율적이지만 K를 미리 정해야 하고 복잡한 모양의 데이터에 한계. 계층적 군집은 K를 미리 안 정해도 되고 구조 파악이 쉽지만 데이터가 많을수록 계산 비용이 커짐</details>

강의 정리 슬라이드(p.166-167)는 위 내용의 요약이라 생략.

---

## 부록 참고자료와 실습 미리보기

- p.168: 마무리 인사 페이지.
- 참고 자료 목록 (p.169-174): 차시별 보충 자료 20개를 표로 제공 — AI/ML/DL 개념, Feature·Label·weight·bias 수식 설명, 파라메트릭/비파라메트릭·Bias-Variance 트레이드오프·KNN, 최소제곱법 vs 최대우도법, adjusted R², 혼동행렬 지표, 과적합 방지법(정규화·데이터 증대), Bias-Variance Tradeoff 실습, 부트스트랩(Bootstrap), Train/Validation/Test 구분, DBSCAN 포함 군집 기법 비교 등. 링크는 QR 코드라 노트로 옮기지 못함 — 원본 슬라이드 참조.
- 실습 미리보기 (p.175-179): 분류모델 학습·검증 및 K-means 클러스터링 실습에서 쓸 개념 소개.

| 실습 개념 | 정의 (원문 요약) |
|---|---|
| EDA (탐색적 데이터 분석) | 데이터 분포, 중앙값·사분위수·상관관계 등을 시각화하여 데이터 특징과 구조를 직관적으로 이해 (기초 통계치, 결측치/이상치 탐지, 상관계수 히트맵) |
| StandardScaler | 피처를 평균 0, 분산 1로 표준화해 특정 피처에 치우치지 않도록 학습을 안정화하는 전처리 |
| Logistic Regression | 선형 방정식 기반의 회귀 계수 해석을 통해 **확률적 분류**를 수행하는 지도 학습 알고리즘 |
| ROC | 임계값(threshold)을 변화시켰을 때 TPR과 FPR의 관계를 나타낸 곡선 (TPR: 진짜를 진짜로 잡는 비율, FPR: 가짜를 진짜로 잡는 비율) |
| AUC | ROC 곡선 아래 면적 |
| PCA (차원축소) | 고차원 데이터를 주성분으로 변환해 정보를 최대한 보존하면서 차원을 축소. 2~3차원 시각화로 데이터 구조 파악 |

---

## 정리 체크리스트

- [ ] AI, ML, DL의 포함 관계를 그리고, ML이 아닌 AI의 예를 2개 들 수 있다
- [ ] $Y = f^*(X) + \epsilon$에서 $f^*$, $\epsilon$, $X \in \mathbb{R}^p$의 의미와 가정($X$와 독립, $\mathbb{E}[\epsilon]=0$)을 설명할 수 있다
- [ ] 데이터 $D$ → 가설공간 $\mathcal{F}$ → 모델 $f$ 관점으로 "학습"을 설명하고, 학습에 필요한 3요소를 말할 수 있다
- [ ] MSE와 $R^2$의 수식·값 범위·용도 차이를 설명하고, $R^2$가 음수가 되는 경우를 말할 수 있다
- [ ] 불균형 데이터에서 Accuracy의 함정을 예시로 설명하고, 혼동행렬에서 Precision/Recall/F1을 계산할 수 있다
- [ ] Cross Entropy가 Accuracy와 달리 "학습용 손실"인 이유를 설명할 수 있다
- [ ] 오버피팅/언더피팅과 훈련·테스트 오류의 U자형 곡선을 연결해 설명하고, 분포 변화와 구분할 수 있다
- [ ] 검증셋 접근의 한계 2가지와, K-겹 교차검증의 $\mathrm{CV}_{(K)}$ 계산 절차를 설명할 수 있다
- [ ] K-means 알고리즘 단계와 지역 최솟값 문제, 계층적 군집의 병합 절차와 링크 3유형을 설명할 수 있다

## 복습 문제

1. 훈련 오류가 0에 가까운 모델이 테스트에서 성능이 나쁘다면 무슨 현상이고, 어떤 해결 실마리가 있는가? — <details><summary>답</summary>오버피팅. 훈련 데이터의 잡음까지 외운 상태. 더 많은 데이터 확보, 테스트(검증) 데이터를 활용한 모델 선정, 교차 검증으로 완화</details>
2. $R^2 = 0.8$의 의미를 "평균 예측" 기준으로 말하면? — <details><summary>답</summary>평균값만으로 예측할 때 발생하는 제곱오차의 80%를 줄였다는 뜻</details>
3. LOOCV는 K-겹 교차검증의 어떤 특수 경우이고, 잘 쓰지 않는 이유는? — <details><summary>답</summary>$K=n$인 경우(검증셋 크기 1). 모든 데이터를 검증에 쓰지만 연산량이 많고, 10-겹 대비 큰 성능 향상이 없어 적절한 K의 K-겹을 권장</details>
4. K-means를 여러 번 실행하라고 권장하는 이유는? — <details><summary>답</summary>초기값에 따라 지역 최솟값(local minimum)으로 수렴해 최종 분할과 목표값이 달라질 수 있기 때문</details>
5. 스팸 확률을 0.6으로 예측했을 때와 0.9로 예측했을 때(실제 스팸) 교차 엔트로피 손실은? — <details><summary>답</summary>각각 0.51, 0.11. 정답에 준 확률이 높을수록 손실이 작아진다</details>

---

> 작성 노트: OCR로 읽은 한글 본문 중 조사·어미가 깨진 부분은 문맥으로 복원했다. 수식·표·퀴즈 정답이 있는 페이지(p.28-29, 32-33, 36, 40, 44, 56, 62-63, 66, 68, 70-71, 74, 81, 83, 85-86, 91-93, 111, 119, 123, 125, 131, 148-151, 155, 159, 165, 178 및 p.1-24 전체)는 원본 이미지로 재확인했다.
> 원본 자료의 확인된 오탈자: p.55-56 "…만드는 방법이 **지시학습**이다"는 문맥상 "지도학습"의 오탈자. 4차시 K-means 섹션 번호는 3-2(알고리즘) 다음이 3-4(단계별 진행)로 3-3이 건너뛰어져 있다(p.149-150).
