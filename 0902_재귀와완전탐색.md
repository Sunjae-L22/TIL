# 재귀와 완전 탐색 — 재귀 함수·부분집합·베이비진·하노이의 탑

> 출처: `3_재귀.pdf` (총 53페이지) · 정리일: 2026-09-02
> 페이지 표기는 PDF 물리 페이지 기준.
> 판독 방식: **전 페이지 이미지 직접 판독** (OCR 미사용). 판독 실패한 페이지는 **없음**.
> 원본 코드 오류는 본문에서 고쳐 쓰고 `> 📌` 로 밝혔다.
> **⚠️ 파일명·표지·목차가 전부 어긋난다.** 파일명은 `3_재귀.pdf` 인데 표지(p.1)와 간지(p.2)에 인쇄된 제목은 「알고리즘 기본 1」이고, 실제 내용은 재귀 + 완전 탐색 + 하노이의 탑이다. 게다가 **p.4-17은 앞 자료(「알고리즘 기본 2」)의 복잡도 파트와 거의 같은 내용**이다.
> 앞 자료: [0901 알고리즘 기본과 리스트](0901_알고리즘기본과리스트.md) · 다음 자료: [0902 순열과 조합](0902_순열과조합.md)
> 관련: [0825 DFS와 BFS 이론](0825_DFS와BFS_이론.md) · [0825 DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md) · [0830 오일러 경로](0830_오일러경로.md)

## 한눈에 보기

- **재귀는 "더 작은 같은 문제"로 문제를 되돌리는 방법**이다. 기본 부분(basis)과 유도 부분(inductive) 두 조각으로 나눠 쓰면 대부분의 재귀 함수가 나온다.
- **재귀 호출은 공짜가 아니다.** 호출마다 스택 프레임이 쌓인다. 파이썬은 기본 재귀 한계가 1000이고, 실제로 `depth(998)` 까지 성공하고 `depth(1000)` 에서 `RecursionError` 가 났다.
- **나이브 피보나치의 호출 횟수는 정확히 `2·F(n+1) − 1`** 이다. n=10에서 177번, n=35에서 2,986만 번. 실측 시간 1.59초. 메모이제이션을 붙이면 `fib(32)` 기준 **88,770배** 차이가 났다.
- **부분집합은 "각 원소를 넣느냐 마느냐"를 N번 반복한 것**이라 $2^N$ 개다. 3중 for 문과 재귀는 완전히 같은 트리를 그리는데, 재귀만 N이 바뀌어도 코드가 안 바뀐다.
- **완전 탐색은 "일단 다 해 본다"** 다. 부끄러운 방법이 아니라 **정답의 기준선**이다. 빠른 풀이를 만든 뒤 완전 탐색과 대조하는 게 검증의 정석이다.
- **완전 탐색이 가능한 경계는 산술로 나온다.** "1억 번 ≈ 1초" 기준선에서 $N!$ 은 **N=11**, $2^N$ 은 **N=26** 까지다. 그 위는 완전 탐색으로 안 된다.
- ⚠️ **강의자료의 베이비진 카운팅 구현(p.37)은 틀렸다.** 자료가 스스로 "베이비진이 아니다"라고 제시한 예제 `101123` 에서 `True` 를 반환한다. 6자리 조합 5,005가지를 전수 대조하니 **3,081건(61.6%)이 오답**이었다. 아래 「베이비진」 절에서 고친 코드와 검증 결과를 적었다.
- **하노이의 탑은 재귀가 반복보다 압도적으로 짧게 나오는 대표 사례**다. 이동 횟수는 $2^n − 1$ 이고, BFS로 상태 공간을 전수 탐색해 보니 n=1..9에서 전부 이 값이 **최소**였다.

## 목차

1. [자료 앞부분은 앞 자료와 중복이다](#자료-앞부분은-앞-자료와-중복이다) (p.4-17)
2. [반복과 재귀](#반복과-재귀) (p.21-22)
3. [팩토리얼과 호출 스택](#팩토리얼과-호출-스택) (p.23-24)
4. [피보나치와 지수 폭발](#피보나치와-지수-폭발) (p.25-27)
5. [파이썬의 재귀 한계](#파이썬의-재귀-한계) (자료에 없음 · 실측 추가)
6. [부분집합](#부분집합) (p.29-33)
7. [베이비진과 완전 탐색](#베이비진과-완전-탐색) (p.37-41)
8. [완전 탐색이 가능한 경계](#완전-탐색이-가능한-경계) (자료에 없음 · 산술 추가)
9. [하노이의 탑](#하노이의-탑) (p.45-52)
10. [정리 체크리스트](#정리-체크리스트)
11. [복습 문제](#복습-문제)

---

## 자료 앞부분은 앞 자료와 중복이다

p.4-17은 「알고리즘 기본」 대단원으로, 다음을 다룬다.

| 페이지 | 내용 | 어디에 정리했나 |
|---|---|---|
| p.6 | 알고리즘의 정의, 1부터 100까지의 합 | [0901 노트 「알고리즘 기본」](0901_알고리즘기본과리스트.md) |
| p.7 | 알고리즘 표현 3방법 (의사코드·순서도·프로그래밍 언어) | 〃 |
| p.8-10 | 좋은 알고리즘의 기준, 성능 분석, 100번 vs 3번 연산 | 〃 |
| p.12-13 | 시간/공간 복잡도, 최선·평균·최악(Ω·Θ·O), 명령문 세기 | [0901 노트 「복잡도」](0901_알고리즘기본과리스트.md) |
| p.14-15 | 빅오 축약 2단계 | 〃 |
| p.16-17 | 복잡도 7종 그래프, 실행 시간 표, "1억 = 약 1초" | 〃 |

**그대로 겹치므로 이 노트에서는 다시 쓰지 않는다.** 다만 두 가지만 짚어 둔다.

- p.12의 표기 대응(최선=Ω, 평균=Θ, 최악=O)은 **관례적인 대응이지 정의가 아니다.** Ω/Θ/O 는 각각 하한·상한하한 동시·상한을 뜻하는 점근 표기이고, "최선/평균/최악"은 입력의 종류를 뜻한다. 서로 독립적인 두 축인데 강의에서 자주 1:1로 붙여 설명한다. 최악의 경우에 대한 Θ 도 성립할 수 있다.
- p.17 표 끝의 물음표 "5천만에 대한 연산을 수행한다면?" 은 답이 슬라이드에 없다. 앞 노트의 기준선을 그대로 쓰면 된다 — **5천만 번은 약 0.5초**, 파이썬이면 한 단계 보수적으로 잡아 **수 초** 를 각오해야 한다.

---

## 반복과 재귀

### 같은 일을 하는 두 방법 (p.21)

- **반복(iteration)** — 작업이 완료될 때까지 되풀이한다. 초기화 → 조건 검사 → 명령문 → 업데이트 → 다시 조건 검사.
- **재귀(recursion)** — 문제의 해를 구하기 위해 **동일하면서 더 작은 문제의 해**를 이용한다.

자료는 `for`/`while` 형(선검사)과 `do~while` 형(후검사) 순서도를 나란히 놓고, 재귀가 이 루프 구조를 대신할 수 있다고 설명한다.

핵심은 방향이다.

```text
반복: 작은 것부터 쌓아 올린다        1 → 2 → 3 → … → n
재귀: 큰 것을 작은 것으로 되돌린다    n → n-1 → … → 1  (그리고 되돌아오며 계산한다)
```

### 재귀 함수의 두 조각 (p.22)

- 함수 내부에서 **직접 혹은 간접적으로 자기 자신을 호출**하는 함수.
- **기본 부분(basis part)** 과 **유도 부분(inductive part)** 으로 구성된다.
  - basis: 더 이상 쪼갤 수 없는 가장 작은 경우. 여기서 재귀가 **멈춘다**.
  - inductive: 자기 자신을 더 작은 인자로 호출한다.
- 반복 구조에 비해 간결하고 이해하기 쉽다. 다만 익숙하지 않으면 어렵게 느껴진다.
- **함수 호출은 메모리의 스택을 쓴다.** 따라서 재귀 호출은 반복적인 스택 사용을 의미하고, **메모리와 속도에서 성능 저하**가 발생한다.

> 📌 재귀 함수를 쓸 때 반드시 확인할 것 두 가지.
> ① **basis 가 반드시 도달하는가** — 도달하지 못하면 `RecursionError` 다.
> ② **인자가 매 호출마다 basis 쪽으로 줄어드는가** — `fact(n)` 이 `fact(n)` 을 부르면 영원히 안 끝난다.

---

## 팩토리얼과 호출 스택

### 재귀적 정의 (p.23)

```text
Basis Rule      : N <= 1 인 경우, N! = 1
Inductive Rule  : N > 1 인 경우, N! = N * (N-1)!
```

```python
def fact(n):
    if n <= 1:          # Basis Rule
        return 1
    else:
        return n * fact(n-1)   # Inductive Rule
```

### n=4 일 때 실제로 일어나는 일 (p.24)

자료는 호출과 반환을 번호로 매겨 보여준다.

```text
fact(4) ─①호출→ fact(3) ─②호출→ fact(2) ─③호출→ fact(1)
                                                    │
        ←⑥return 6      ←⑤return 2      ←④return 1 ┘
   │
   └─⑦ return 24
```

메모리에서는 이렇게 쌓인다.

```text
높은 주소 ↑
          │  fact(1)   return 1;      ← 마지막에 쌓이고 가장 먼저 빠진다
          │  fact(2)   ... fact(1);
          │  fact(3)   ... fact(2);
          │  fact(4)   ... fact(3);
          │  main()    ... fact(4);
낮은 주소 ↓
```

**호출은 아래에서 위로 쌓이고(push), 반환은 위에서 아래로 걷힌다(pop).** 이게 재귀의 실체다 — 재귀 함수는 스택을 쓰는 반복문이고, 그래서 나중에 「스택과 큐」에서 재귀를 명시적 스택으로 바꾸는 이야기가 나온다.

곱셈이 실제로 일어나는 시점을 눈여겨봐야 한다. `n * fact(n-1)` 에서 **곱셈은 `fact(n-1)` 이 값을 돌려준 뒤**다. 즉 내려갈 때는 아무 계산도 안 하고, **올라오면서 계산한다.** 이 성질이 「트리 순회」의 후위 순회와 같은 모양이다.

---

## 피보나치와 지수 폭발

### 정의 (p.25)

- 이전의 두 수 합을 다음 항으로 하는 수열: 0, 1, 1, 2, 3, 5, 8, 13, …
- $F_0 = 0$, $F_1 = 1$, $F_i = F_{i-1} + F_{i-2}$ (i ≥ 2)

### 구현 (p.26-27)

```python
def fibonacci(n):
    if n == 0:            # Basis
        return 0
    elif n == 1:          # Basis
        return 1
    else:                 # Inductive
        return fibonacci(n - 1) + fibonacci(n - 2)
```

정의를 그대로 옮긴 코드라 아름답지만, **이 코드는 실전에서 못 쓴다.** 자료는 여기서 멈추는데, 왜 못 쓰는지가 재귀에서 제일 중요한 부분이다.

### 왜 못 쓰는가 — 같은 값을 몇 번이나 다시 계산한다

`fibonacci(5)` 의 호출 트리를 그려 보면 `fibonacci(2)` 가 세 번, `fibonacci(1)` 이 다섯 번 나온다.

```text
                       F(5)
              ┌─────────┴─────────┐
            F(4)                 F(3)
        ┌─────┴─────┐        ┌─────┴─────┐
      F(3)         F(2)     F(2)        F(1)
   ┌───┴───┐    ┌───┴──┐  ┌──┴───┐
  F(2)    F(1) F(1)  F(0) F(1)  F(0)
┌──┴──┐
F(1) F(0)
```

**팩토리얼의 호출 트리는 일직선(사슬)인데 피보나치는 두 갈래로 갈라진다.** 이 차이가 $O(n)$ 과 $O(\varphi^n)$ 을 가른다.

직접 세어 봤다.

| n | F(n) | 호출 횟수 | 실행 시간 |
|---|---|---|---|
| 10 | 55 | 177 | 0.0000 s |
| 20 | 6,765 | 21,891 | 0.0011 s |
| 25 | 75,025 | 242,785 | 0.0119 s |
| 30 | 832,040 | 2,692,537 | 0.1331 s |
| 32 | 2,178,309 | 7,049,155 | 0.3491 s |
| 35 | 9,227,465 | 29,860,703 | 1.5920 s |

**호출 횟수에는 닫힌 식이 있다.** 위 n 전부에서 정확히 일치했다.

```text
호출 횟수 = 2 * F(n+1) - 1
```

n=35에서 실측 29,860,703 = 2 × 14,930,352 − 1. 피보나치 수 자체가 $\varphi^n/\sqrt5$ 로 커지므로 **호출 횟수도 지수적으로 커진다.** n을 1 늘릴 때마다 약 1.618배다.

### 고치는 법 — 메모이제이션

```python
from functools import lru_cache

@lru_cache(maxsize=None)
def fib(n):
    if n < 2:
        return n
    return fib(n - 1) + fib(n - 2)
```

`fib(32)` 기준 실측: 나이브 **437.5 ms** → 메모 **4.9 μs**. **88,770배**다.

각 값을 한 번씩만 계산하므로 호출 횟수가 $O(n)$ 으로 떨어진다. 이게 DP의 출발점이고, 「재귀 + 메모이제이션 = 하향식 DP」라는 말이 여기서 나온다.

반복문으로 바꾸면 스택도 안 쓴다.

```python
def fib_iter(n):
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a
```

> 📌 **"재귀가 느리다"가 아니라 "겹치는 부분 문제를 캐싱하지 않는 재귀가 느리다"** 가 정확한 진술이다. 팩토리얼 재귀는 겹치는 부분 문제가 없어서 반복문과 시간 복잡도가 같다.

---

## 파이썬의 재귀 한계

자료에는 없지만 반드시 알아야 하는 부분이다. 파이썬은 C 스택 오버플로로 인터프리터가 죽는 것을 막으려고 **재귀 깊이에 인위적 상한**을 둔다.

```python
import sys
sys.getrecursionlimit()   # 1000
```

실측해 보니 이렇다.

```text
depth(900)  성공
depth(995)  성공
depth(998)  성공
depth(1000) RecursionError: maximum recursion depth exceeded
```

**1000이 상한인데 998까지만 되는 이유**는, 이미 모듈 실행·함수 호출로 프레임 몇 개를 쓰고 있어서다. 즉 실질 여유는 1000보다 조금 적다.

코딩 테스트에서 재귀 DFS를 쓸 때 자주 터지는 지점이다. 대처는 셋 중 하나다.

```python
import sys
sys.setrecursionlimit(10 ** 6)      # ① 한계를 올린다 (메모리 여유가 있을 때)
```

```python
# ② 명시적 스택으로 바꾼다 — 한계 자체가 사라진다
stack = [start]
while stack:
    v = stack.pop()
    ...
```

③ 애초에 재귀 깊이가 얕게 나오도록 문제를 다시 본다. 격자 DFS에서 깊이는 최악의 경우 칸 수만큼 간다 — 1000×1000 격자면 깊이 100만이다.

> 📌 `setrecursionlimit` 을 크게 올리면 이번엔 **실제 C 스택이 터져서 세그폴트**가 날 수 있다. 파이썬 예외가 아니라 프로세스가 그냥 죽는다. 한계를 올리는 것과 스택으로 바꾸는 것 중에서는 후자가 안전하다.

---

## 부분집합

### 왜 부분집합인가 (p.29)

- 집합에 포함된 원소들을 **선택하는 것**이다.
- 많은 중요 알고리즘이 원소 그룹에서 **최적의 부분집합**을 찾는 데 사용된다. 예) 배낭 짐싸기(knapsack).
- N개 원소를 가진 집합의 부분집합 개수는 **공집합을 포함해 $2^N$ 개**다.
- 각 원소를 "포함하거나 포함하지 않는" 2가지 경우를 모든 원소에 적용한 것과 같다.

```text
{1, 2, 3, 4}
 ↓  ↓  ↓  ↓
 2 × 2 × 2 × 2 = 16가지
```

이 문장이 부분집합의 전부다. **원소마다 독립적인 O/X 하나**, 그래서 $2^N$. 뒤에 나올 바이너리 카운팅도 결국 이 O/X를 비트 하나에 대응시킨 것이다.

### 반복문으로 (p.30-31)

`{1, 2, 3}` 의 모든 부분집합을 생성한다.

```python
selected = [0] * 3
for i in range(2):
    selected[0] = i
    for j in range(2):
        selected[1] = j
        for m in range(2):
            selected[2] = m
            subset = []
            for n in range(3):
                if selected[n] == 1:
                    subset.append(n+1)
            print(subset)
```

동작은 맞다. 문제는 **원소가 4개가 되면 for 문을 하나 더 써야 한다**는 것이다. N이 바뀔 때마다 코드를 다시 쓰는 구조는 실전에서 못 쓴다.

### 재귀로 (p.32-33)

```python
def create_subset(depth, included):
    if depth == len(input_list):
        cur_subset = [input_list[i] for i in range(len(input_list)) if included[i]]
        subsets.append(cur_subset)
        return

    included[depth] = False          # depth번째 원소를 "빼는" 가지
    create_subset(depth + 1, included)

    included[depth] = True           # depth번째 원소를 "넣는" 가지
    create_subset(depth + 1, included)

input_list = [1, 2, 3]
subsets = []
init_included = [False] * len(input_list)
create_subset(0, init_included)
print(subsets)
```

> 📌 **원본 코드는 그대로 돌리면 죽는다.** 원본 자료에는 리스트 컴프리헨션 결과를 `cnt_subset` 에 담아 놓고 `subsets.append(current_subset)` 으로 정의되지 않은 이름을 append 하도록 되어 있다. 실행하면 `NameError: name 'current_subset' is not defined` 가 난다. 직접 돌려서 확인했다. 같은 코드의 주석판(다음 슬라이드)에는 `subsets.append(cnt_subset)` 으로 고쳐져 있고 함수명도 다르다. 위 코드는 이름을 통일해서 고친 것이다.

### 두 코드는 같은 트리를 그린다

3중 for 문과 재귀는 **완전히 같은 순서로 같은 것을 만든다.** 재귀 트리의 깊이가 원소 인덱스이고, 왼쪽 가지가 "안 넣는다", 오른쪽 가지가 "넣는다"다.

```text
                     depth=0
              F ┌──────┴──────┐ T          ← 원소 1을 넣나?
           depth=1          depth=1
         F ┌──┴──┐ T      F ┌──┴──┐ T      ← 원소 2를 넣나?
        d2      d2       d2      d2
      F ┌┴┐T  F ┌┴┐T   F ┌┴┐T  F ┌┴┐T     ← 원소 3을 넣나?
       {} {3} {2}{2,3} {1}{1,3}{1,2}{1,2,3}
```

리프가 정확히 $2^3 = 8$ 개다. **재귀는 N이 늘어도 코드가 그대로다** — 이게 반복문 대비 유일하고 결정적인 이득이다.

### 성능 — 셋을 재 봤다

N=20 (부분집합 1,048,576개), 3회 중 최소값.

| 방식 | 시간 |
|---|---|
| append/pop 백트래킹 | **666.8 ms** |
| 재귀 · 리프에서 매번 재구성 (자료 p.32 방식) | 1,436.0 ms |
| 바이너리 카운팅 (다음 자료 p.31 방식) | 1,607.4 ms |

**리프에서 리스트 컴프리헨션으로 매번 다시 만드는 게 비싸다.** 리프가 $2^N$ 개인데 매 리프마다 길이 N을 훑으므로 $O(N \cdot 2^N)$ 이다. 백트래킹으로 바꾸면 이렇다.

```python
def subsets_bt(arr):
    out, cur = [], []
    n = len(arr)
    def go(d):
        if d == n:
            out.append(cur[:])       # 리프에서 얕은 복사 한 번
            return
        go(d + 1)                    # 안 넣는다
        cur.append(arr[d])           # 넣는다
        go(d + 1)
        cur.pop()                    # 되돌린다
    go(0)
    return out
```

`cur.append` → 재귀 → `cur.pop()` 이 **백트래킹의 기본형**이다. 「DFS와 BFS 문제 유형」에서 본 그 패턴과 같다. 상태를 만들고, 내려가고, 원상복구한다.

> 📌 **바이너리 카운팅이 가장 느리게 나온 게 의외였다.** 비트 연산이 빠를 것 같지만, 파이썬 레벨에서 $2^N \times N$ 번 `i & (1 << j)` 를 도는 게 리스트 컴프리헨션(C 레벨 루프)보다 느리다. 점근 복잡도가 같으면 **어느 쪽이 C 레벨로 도느냐**가 승부를 가른다.

---

## 베이비진과 완전 탐색

### 문제 (p.37)

- 0~9 사이의 숫자 카드에서 임의의 카드 6장을 뽑는다.
- **run** — 3장의 카드가 연속적인 번호를 갖는 경우.
- **triplet** — 3장의 카드가 같은 번호를 갖는 경우.
- **baby-gin** — 6장이 run과 triplet **으로만** 구성된 경우.
- 6자리 숫자를 입력받아 baby-gin 여부를 판단하라.

| 입력 | 판정 |
|---|---|
| `667767` | baby-gin (666, 777 — triplet 두 개) |
| `054060` | baby-gin (456, 000 — run 하나 + triplet 하나) |
| `101123` | **baby-gin 아님.** triplet(111)은 있으나 남은 023이 run이 아니다. 123을 run으로 써도 011이 run도 triplet도 아니다 |

**"으로만"** 이 핵심이다. run이나 triplet이 하나 있는 걸로는 부족하고, **6장 전부가 두 그룹으로 완전히 나뉘어야** 한다.

### 정렬 접근은 실패한다 (p.38)

숫자를 정렬한 뒤 앞뒤 3자리씩 끊어서 확인하면 될 것 같다.

```text
{6, 4, 4, 5, 4, 4} → 정렬 → {4, 4, 4, 4, 5, 6}   ← 444 / 456. 잘 된다
{1, 2, 3, 1, 2, 3} → 정렬 → {1, 1, 2, 2, 3, 3}   ← 112 / 233. 둘 다 아니다. 실패
```

두 번째는 실제로는 **123 / 123 으로 baby-gin** 이다. 정렬은 이걸 못 찾는다. **정렬은 그룹 경계를 앞3/뒤3 으로 고정해 버리는데, 정답이 되는 분할이 그 경계와 다를 수 있다.**

### 그래서 완전 탐색 (p.39-41)

- 문제의 해법으로 생각할 수 있는 **모든 경우의 수를 나열해 보고 확인**하는 기법.
- **Brute-force**, **generate-and-test** 라고도 한다. "Just-do-it".
- force는 사람의 지능이 아니라 **컴퓨터의 force**를 뜻한다.
- 대부분 문제에 적용 가능하고, 상대적으로 빠른 시간에 알고리즘을 설계할 수 있다.
- 경우의 수가 작다면 유용하다.
- 전형적으로 **순열·조합·부분집합** 같은 조합적 문제와 연관된다.
- **우선 완전 탐색으로 접근해 해답을 도출한 뒤, 성능 개선을 위해 다른 알고리즘을 쓰고 해답을 확인하는 것이 바람직하다.**

마지막 문장이 이 단원에서 제일 중요하다. 완전 탐색은 **버리는 코드가 아니라 정답 검증용 기준선**이다. 「오일러 경로」 노트에서 그리디 사전순 최소를 브루트포스 34,000건으로 대조했던 것과 같은 용법이다.

### 순열 기반 판정 (자료 「순열과 조합」 p.35)

```python
from itertools import permutations

def is_run(cards):
    return cards[0] + 1 == cards[1] and cards[1] + 1 == cards[2]

def is_triplet(cards):
    return cards[0] == cards[1] == cards[2]

def is_babygin_permutations(cards):
    if len(cards) != 6:
        return False
    for perm in permutations(cards):
        first_group, second_group = perm[:3], perm[3:]
        if (is_run(first_group) or is_triplet(first_group)) \
           and (is_run(second_group) or is_triplet(second_group)):
            return True
    return False
```

720가지를 다 만들어 앞3/뒤3으로 잘라 본다. **정렬로 못 찾던 `123123` 도 이건 찾는다.** 정확하다.

낭비는 있다. 그룹 안의 순서는 상관없고 두 그룹의 앞뒤도 상관없으므로, 실제로 필요한 것은 **6개에서 3개를 고르는 조합 20가지**뿐이다.

```python
from itertools import combinations

def ok3(g):
    g = sorted(g)
    return is_run(g) or is_triplet(g)

def is_babygin_comb(cards):
    idx = range(6)
    for a in combinations(idx, 3):
        b = [i for i in idx if i not in a]
        if ok3([cards[i] for i in a]) and ok3([cards[i] for i in b]):
            return True
    return False
```

실측 1회 판정 시간: 순열 720가지 **170.8 μs** → 조합 20가지 **12.5 μs**. 같은 답에 14배 빠르다.

### ⚠️ 자료의 카운팅 구현은 틀렸다 (「순열과 조합」 p.37-38)

자료는 마지막에 카운팅 기반 최적화를 보여준다.

```python
def is_babygin(cards):        # ← 원본. 틀렸다
    counter = [0] * 10
    for card in cards:
        counter[card] += 1

    for count in counter:
        if count >= 3:
            return True                       # triplet 이 하나라도 있으면 True

    for i in range(8):
        if counter[i] >= 1 and counter[i+1] >= 1 and counter[i+2] >= 1:
            return True                       # run 이 하나라도 있으면 True

    return False
```

**이 코드는 "run 또는 triplet이 하나라도 있는가"를 묻는다.** 문제가 요구하는 것은 "6장이 두 그룹으로 **완전히** 나뉘는가"다. 전혀 다른 질문이다.

자료가 스스로 든 예제에서 바로 깨진다.

| 입력 | 정답 | 자료 카운팅 |
|---|---|---|
| `667767` | True | True |
| `054060` | True | True |
| `101123` | **False** | **True** ← 자료 자신이 "아니다"라고 쓴 예제 |

`101123` 은 `counter[1] == 3` 이라 첫 루프에서 바로 `True` 를 반환한다. 남은 `0, 2, 3` 이 run인지는 보지도 않는다.

전수 대조를 했다. 판정이 카드 순서와 무관하므로 오름차순 6장 조합 **5,005가지**만 보면 충분하다.

```text
실제 baby-gin        =   171가지 (3.4%)
자료 카운팅과 불일치 = 3,081건 (61.6%)
  오탐(자료 True·실제 False) = 3,081건
  미탐(자료 False·실제 True) =     0건
```

**절반 이상이 오답이고 전부 오탐**이다. 가장 작은 반례는 `000001` — 0이 세 장이라 triplet 하나는 있지만 남은 `0, 0, 1` 이 아무것도 아니다.

### 고친 카운팅 — 뺀 다음 남은 걸 다시 본다

카운팅으로 제대로 풀려면 **두 그룹을 실제로 빼내야** 한다. 그룹을 하나 빼고, 남은 것에서 또 하나 빼면 된다. 그룹이 2개뿐이라 재귀 깊이도 2다.

```python
def is_babygin(cards):
    c = [0] * 10
    for x in cards:
        c[x] += 1

    def take(need):
        if need == 0:
            return True
        for i in range(10):                    # triplet 을 빼 본다
            if c[i] >= 3:
                c[i] -= 3
                if take(need - 1):
                    c[i] += 3
                    return True
                c[i] += 3
        for i in range(8):                     # run 을 빼 본다
            if c[i] and c[i+1] and c[i+2]:
                c[i] -= 1; c[i+1] -= 1; c[i+2] -= 1
                if take(need - 1):
                    c[i] += 1; c[i+1] += 1; c[i+2] += 1
                    return True
                c[i] += 1; c[i+1] += 1; c[i+2] += 1
        return False

    return take(2)
```

빼고(`c[i] -= 3`) → 내려가고 → 되돌린다(`c[i] += 3`). **또 백트래킹이다.** 부분집합에서 본 append/pop과 정확히 같은 모양이다.

5,005가지 전수 대조에서 **불일치 0건**. 순열 완전 탐색과 완벽히 일치한다.

속도도 이쪽이 압도적이다.

| 방식 | 1회 판정 | 정확성 |
|---|---|---|
| `permutations` 720가지 | 170.8 μs | 정확 |
| `combinations` 20가지 | 12.5 μs | 정확 |
| **고친 카운팅** | **1.5 μs** | **정확** |
| 자료 카운팅 | 0.6 μs | **61.6% 오답** |

**순열 대비 91배 빠르면서 정확하다.** 자료가 하려던 말("완전 탐색으로 답을 확인한 뒤 빠른 방법으로 바꾼다")은 맞는데, 예시로 든 빠른 방법 자체가 틀렸다. 그리고 그걸 잡아낸 방법이 바로 자료가 권한 그 방법 — **완전 탐색과 대조하기** 다.

---

## 완전 탐색이 가능한 경계

자료는 "경우의 수가 작다면 유용하다"고만 하고 넘어간다. [0901 노트](0901_알고리즘기본과리스트.md)의 **"1억 번 ≈ 1초"** 기준선을 쓰면 숫자로 못 박을 수 있다.

| N | $N!$ | $2^N$ |
|---|---|---|
| 10 | 3,628,800 | 1,024 |
| 11 | 39,916,800 | 2,048 |
| 12 | 479,001,600 | 4,096 |
| 13 | 6,227,020,800 | 8,192 |
| 20 | 2.4 × 10¹⁸ | 1,048,576 |
| 25 | 1.6 × 10²⁵ | 33,554,432 |
| 26 | — | 67,108,864 |
| 30 | 2.7 × 10³² | 1,073,741,824 |

$10^8$ 이하인 최대 N은 이렇다.

```text
N!  →  N = 11
2^N →  N = 26
```

즉 **순열 완전 탐색은 N=11까지, 부분집합 완전 탐색은 N=26까지**가 이론상 경계다. 파이썬은 한 단계 보수적으로 잡아야 하므로 실제로는 **순열 N≤10, 부분집합 N≤22** 정도로 본다.

실측으로 확인했다 — `itertools.permutations` 를 그냥 순회만 하는 시간이다.

| n | 가짓수 | 순회 시간 |
|---|---|---|
| 8 | 40,320 | 0.002 s |
| 9 | 362,880 | 0.016 s |
| 10 | 3,628,800 | 0.168 s |
| 11 | 39,916,800 | 1.797 s |
| 12 | 479,001,600 | 약 41 s (추정) |

**n=11에서 이미 1.8초**다. 여기에 각 순열마다 판정 로직이 붙으면 더 늘어난다. 자료(「순열과 조합」 p.9)의 "N > 12 인 경우 시간 복잡도는 폭발적으로 증가한다"가 실측으로 확인된다.

> 📌 이 표를 외울 필요는 없다. **"N! 은 열한 개, 2^N 은 스물여섯 개"** 만 기억하면, 문제에서 N 제한을 보는 순간 완전 탐색이 되는지 안 되는지 3초 안에 판단할 수 있다. N ≤ 20 이면 비트마스크 DP를, N ≤ 10 이면 순열 완전 탐색을 떠올리는 습관이 여기서 나온다.

---

## 하노이의 탑

### 유래 (p.45)

인도 베나레스의 한 사원에 세 개의 다이아몬드 기둥이 있고, 그중 하나에 신이 64개의 순금 원판을 크기 순으로 꽂아 두었다. 승려들이 규칙에 따라 원판을 하나씩 옮기고, 이 일이 끝나면 탑이 무너지고 세상이 종말을 맞는다.

$2^{64} - 1 \approx 1.8 \times 10^{19}$ 번. 1초에 한 장씩 옮겨도 약 5,850억 년이다. 우주 나이의 40배쯤 되니 아직 걱정할 일은 아니다.

### 규칙 (p.46-47)

- 기둥 3개와 서로 다른 크기의 원판 N개.
- 모든 원판을 세 번째 기둥으로 옮긴다.
- **한 번에 한 개씩** 옮긴다. 두 번째 기둥을 보조로 쓸 수 있다.
- **절대로 큰 원판이 작은 원판 위에 놓이면 안 된다.**
- 입력: 원판 개수 N (1 ≤ N ≤ 10). 원판 번호는 맨 위부터 1, 2, 3 순.
- 출력: `옮겨지는 원판 번호: 출발기둥 → 목적기둥`

### 손으로 규칙 찾기 (p.48)

```text
원판 1개                        원판 3개
 1. A → C (원판1)                1. A → C (원판1)
                                 2. A → B (원판2)
원판 2개                         3. C → B (원판1)
 1. A → B (원판1)                4. A → C (원판3)   ← 가장 큰 원판이 한 번에 목적지로
 2. A → C (원판2)                5. B → A (원판1)
 3. B → C (원판1)                6. B → C (원판2)
                                 7. A → C (원판1)
```

**4번째 줄을 기준으로 위아래가 대칭**이다. 위 3개는 "원판 2개를 A에서 B로", 아래 3개는 "원판 2개를 B에서 C로". 가운데 한 줄은 "가장 큰 원판을 A에서 C로". 이게 재귀 구조 그 자체다.

### 구현 (p.51-52)

```python
# n         : 이동할 디스크의 개수
# source    : 디스크가 처음에 위치한 기둥
# target    : 디스크를 이동시킬 목표 기둥
# auxiliary : 보조 기둥

def hanoi(n, source, target, auxiliary):
    if n > 0:
        # 1. n-1개를 source → auxiliary 로 치운다
        hanoi(n - 1, source, auxiliary, target)

        # 2. 가장 큰 원판 하나를 source → target 으로 옮긴다
        print(f"Move disk {n} from {source} to {target}")

        # 3. 치워 뒀던 n-1개를 auxiliary → target 으로 가져온다
        hanoi(n - 1, auxiliary, target, source)

n = 3
hanoi(n, 'A', 'C', 'B')     # A에서 C로, B는 보조
```

**세 줄이 전부다.** 반복문으로 짜면 훨씬 길고 이해하기도 어렵다. 재귀가 압도적으로 유리한 몇 안 되는 사례다.

읽는 요령은 **"n-1개는 이미 옮길 수 있다고 믿는 것"** 이다. 어떻게 옮기는지는 신경 쓰지 않는다. 내가 할 일은 가운데 한 줄, 가장 큰 원판 하나를 옮기는 것뿐이다. 나머지는 재귀가 알아서 한다.

세 인자의 **역할이 자리마다 바뀌는 것**도 눈여겨봐야 한다. 첫 재귀 호출에서는 원래의 target(`C`)이 보조로 들어가고, 두 번째에서는 원래의 source(`A`)가 보조로 들어간다.

### 검증 — 정말 $2^n − 1$ 이고, 정말 최소인가

이동 횟수를 직접 세어 봤다.

| n | 이동 횟수 | $2^n − 1$ |
|---|---|---|
| 1 | 1 | 1 |
| 3 | 7 | 7 |
| 6 | 63 | 63 |
| 10 | 1,023 | 1,023 |
| 15 | 32,767 | 32,767 |
| 20 | 1,048,575 | 1,048,575 |

전부 일치한다. 점화식으로도 바로 나온다.

```text
T(0) = 0
T(n) = T(n-1) + 1 + T(n-1) = 2·T(n-1) + 1
     → T(n) = 2^n - 1
```

**규칙 위반이 없는지**도 시뮬레이션으로 확인했다. n=1..12에서 "큰 원판이 작은 원판 위에 놓이는" 경우 0건, 최종적으로 전부 C 기둥에 크기 순으로 쌓인다.

그리고 **이게 정말 최소인가**를 확인하려고 상태 공간을 BFS로 전수 탐색했다. 상태는 `(A기둥, B기둥, C기둥)` 튜플이고 간선은 합법적인 이동 한 번이다.

| n | BFS 최소 이동 | $2^n − 1$ | |
|---|---|---|---|
| 1~9 | 1, 3, 7, 15, 31, 63, 127, 255, 511 | 동일 | **전부 최적** |

**재귀 해가 곧 최적해다.** 이건 자명하지 않다 — 재귀는 "가능한 방법"을 주지만 "가장 좋은 방법"인지는 별개 문제이고, 실제로 이렇게 확인해 봐야 안다. [0830 오일러 경로](0830_오일러경로.md) 노트에서 그리디 사전순 최소를 브루트포스로 검증했던 것과 같은 습관이다.

### 왜 최소인지 (증명 스케치)

가장 큰 원판 n을 옮기려면 그 위의 n-1개가 **전부 다른 한 기둥에 모여 있어야** 한다. 두 기둥에 나뉘어 있으면 n번 원판이 갈 자리가 없다. 그러므로

1. n-1개를 한 기둥에 모으는 데 최소 T(n-1)
2. n번 원판 이동 최소 1
3. 다시 n-1개를 목적지로 옮기는 데 최소 T(n-1)

이므로 T(n) ≥ 2·T(n-1) + 1 이고, 재귀 해가 정확히 이 값을 달성한다.

---

## 정리 체크리스트

- [ ] 재귀 함수의 basis / inductive 두 조각을 각각 뭐라고 쓸지 말할 수 있다
- [ ] `fact(4)` 의 호출·반환 순서와 스택 상태를 그릴 수 있다
- [ ] 곱셈이 "내려갈 때"가 아니라 "올라올 때" 일어난다는 걸 설명할 수 있다
- [ ] 나이브 피보나치의 호출 횟수가 `2·F(n+1) − 1` 인 이유를 호출 트리로 설명할 수 있다
- [ ] 팩토리얼 재귀는 괜찮은데 피보나치 재귀는 왜 안 되는지 말할 수 있다 (겹치는 부분 문제)
- [ ] 파이썬 기본 재귀 한계가 1000이고 실질 여유는 그보다 적다는 걸 안다
- [ ] `setrecursionlimit` 을 올리는 것보다 스택으로 바꾸는 게 안전한 이유를 안다
- [ ] 부분집합이 $2^N$ 인 이유를 "원소마다 O/X 하나"로 설명할 수 있다
- [ ] 3중 for 문 부분집합과 재귀 부분집합이 같은 트리라는 걸 그릴 수 있다
- [ ] append → 재귀 → pop 백트래킹 형태를 안 보고 쓸 수 있다
- [ ] baby-gin 문제에서 "정렬 후 앞3/뒤3" 이 왜 실패하는지 반례를 들 수 있다
- [ ] "run이나 triplet이 하나라도 있으면 True" 가 왜 틀린 판정인지 반례를 들 수 있다
- [ ] 완전 탐색을 "버리는 코드"가 아니라 "검증 기준선"으로 쓰는 방법을 안다
- [ ] N! ≤ 1억 → N=11, 2^N ≤ 1억 → N=26 을 외우고 있다
- [ ] 하노이의 탑 3줄 재귀를 안 보고 쓸 수 있고, 세 인자의 역할이 어떻게 바뀌는지 말할 수 있다
- [ ] 하노이 이동 횟수 $2^n − 1$ 을 점화식에서 유도할 수 있다

## 복습 문제

1. `fact(0)` 은 뭘 반환하는가? 코드의 basis 조건이 `n <= 1` 인 이유는?
2. 나이브 피보나치에서 `fibonacci(1)` 은 `fibonacci(5)` 를 구할 때 몇 번 호출되는가? 호출 트리를 그려서 세어 보라.
3. `sys.setrecursionlimit(10**7)` 로 올리고 깊이 100만짜리 재귀를 돌리면 어떻게 되는가? 왜 그런가?
4. 부분집합 재귀에서 `included[depth] = True` 를 먼저 쓰고 `False` 를 나중에 쓰면 출력 순서가 어떻게 바뀌는가?
5. 백트래킹에서 `cur.pop()` 을 빠뜨리면 어떤 결과가 나오는가? 직접 돌려 보라.
6. `123123` 이 baby-gin 인 이유를 설명하고, 정렬 접근이 왜 이걸 놓치는지 말하라.
7. 자료의 카운팅 구현이 `False` 를 반환하는데 실제로는 baby-gin 인 입력(미탐)은 존재하는가? 존재하지 않는다면 왜인가?
8. 고친 카운팅에서 triplet 을 먼저 빼고 run 을 나중에 빼는데, 순서를 바꾸면 결과가 달라지는가?
9. 하노이의 탑에서 `hanoi(n-1, source, auxiliary, target)` 의 세 번째 인자가 `target` 인 이유는?
10. 원판 5개일 때 32번째 이동은 존재하는가? 왜인가?
