# DP: 상태와 점화식으로 중복 계산 줄이기

> 강의일: 2026-09-15 · 강의 자료를 바탕으로 개념과 예제를 재구성한 학습 노트
> 확인 범위: `14_DP.pdf` 물리 1–42쪽 전체. 이미지형 PDF를 한국어·영어 OCR로 읽고 핵심 수식과 코드는 원본 이미지와 대조했다. 표지와 구분 페이지는 학습 내용에서 제외했다.

DP(Dynamic Programming, 동적 계획법)의 출발점은 **무엇을 저장할 것인가**다. 같은 의미의 작은 문제를 여러 번 풀고 있다면, 그 답을 상태별로 저장한 뒤 다시 사용한다. 피보나치에서는 이전 두 항, 이항계수에서는 이전 행, 거스름돈에서는 더 작은 금액이 다음 답의 근거가 된다.

## 1. 재귀 호출 트리에서 상태 의존 관계로

`F(n) = F(n-1) + F(n-2)`를 그대로 재귀 호출하면 `F(n-2)` 이하가 여러 경로에서 반복된다. 호출 횟수는 지수적으로 늘지만 서로 다른 상태는 `0`부터 `n`까지뿐이다. 같은 상태의 답을 재사용하면 중복 계산을 제거할 수 있다. [강의: 11–16쪽]

| 구분 | 먼저 하는 일 | 저장·호출 비용 | 주의점 |
|---|---|---|---|
| 단순 재귀 | 필요한 값을 재귀 호출 | 호출 스택 사용 | 같은 부분 문제 반복 |
| 메모이제이션 | 요청한 상태를 계산하고 캐시 | 테이블 + 호출 스택 | 재귀 깊이 제한 |
| 타뷸레이션 | 의존 상태부터 순서대로 계산 | 테이블 | 계산 순서를 설계해야 함 |
| 공간 압축 | 앞으로 필요한 상태만 유지 | 문제별로 다름 | 덮어쓰기 순서가 중요 |

메모이제이션과 DP를 서로 배타적인 기법으로 보면 안 된다. 메모이제이션은 DP를 하향식으로 구현하는 대표적인 방법이다. 재귀 자체가 느린 것이 아니라 **중복 계산과 호출 비용을 따로 봐야 한다**.

최적화 문제에서는 최적 부분 구조가 중요하다. 전체 최적해를 그 상태에 맞는 부분 최적해들로 구성할 수 있어야 한다. 개수 세기나 수열 계산처럼 최소·최대를 구하지 않는 DP도 있다. 따라서 DP를 최적화 문제만의 기법으로 한정하지 않는다. [강의: 17–25쪽, 보완 설명]

## 2. 상태·전이·초기값·순서·답

코드에 앞서 다음 다섯 문장을 쓴다.

1. **상태:** `dp[...]`는 정확히 어떤 문제의 답인가?
2. **전이:** 어떤 더 작은 상태에서 현재 상태를 만들 수 있는가?
3. **초기값:** 더 쪼갤 수 없는 문제의 답과 불가능 상태는 무엇인가?
4. **순서:** 참조할 상태가 먼저 계산되도록 어떤 순서로 순회하는가?
5. **최종 답:** 테이블의 어느 위치가 원래 문제의 답인가?

점화식만 맞아도 초기값과 순회 방향이 틀리면 다른 문제를 풀게 된다. 복잡도는 대체로 **서로 다른 상태 수 × 상태 하나의 전이 비용**으로 계산한다.

## 3. 피보나치: 동일한 점화식, 다른 저장 전략

상태는 `F(i)`: 0번째 항을 0, 1번째 항을 1로 정한 피보나치 수다. 초기값은 `F(0)=0`, `F(1)=1`, 전이는 `F(i)=F(i-1)+F(i-2)`다. [강의: 11–27쪽]

### 하향식 메모이제이션

```python
from functools import cache

@cache
def fib_memo(n):
    if n < 0:
        raise ValueError("n must be nonnegative")
    if n < 2:
        return n
    return fib_memo(n - 1) + fib_memo(n - 2)

assert fib_memo(10) == 55
```

이미 계산한 인수의 답은 재귀 본문을 다시 실행하지 않고 가져온다. 입력이 커지면 메모이제이션을 해도 호출 깊이 때문에 실패할 수 있다. 캐시는 실행 도중 유지되므로 독립 실험에서는 `fib_memo.cache_clear()`로 초기화한다.

### 상향식과 공간 압축

```python
def fib_table(n):
    if n < 0:
        raise ValueError("n must be nonnegative")
    if n < 2:
        return n
    dp = [0] * (n + 1)
    dp[1] = 1
    for i in range(2, n + 1):
        dp[i] = dp[i - 1] + dp[i - 2]
    return dp[n]


def fib_pair(n):
    if n < 0:
        raise ValueError("n must be nonnegative")
    a, b = 0, 1
    for _ in range(n):
        a, b = b, a + b
    return a

assert [fib_pair(i) for i in range(8)] == [0, 1, 1, 2, 3, 5, 8, 13]
assert fib_table(10) == fib_pair(10) == 55
```

`fib_pair`의 반복 시작 시 `a=F(i)`, `b=F(i+1)`이다. 파이썬의 동시 대입은 오른쪽을 먼저 계산하므로 이전 `a`, `b`를 모두 사용한다. `a=b; b=a+b`로 나누면 이미 바뀐 `a`를 사용하게 된다.

단위 비용 산술 모델에서는 세 DP 구현 모두 O(n)번의 연산을 수행한다. 테이블은 O(n)개의 정수, 압축판은 O(1)개의 정수만 보관한다. 다만 Python 큰 정수는 자릿수에 따라 메모리와 덧셈 비용이 증가하므로, 이를 실제 비트 단위 O(1) 메모리라고 해석하지 않는다.

## 4. 이항계수: 한 원소의 포함 여부로 나누기

`C(n,k)`는 서로 다른 n개 중 k개를 고르는 방법의 수다. 특정 원소를 고르면 나머지 n-1개에서 k-1개, 고르지 않으면 n-1개에서 k개를 고른다. 두 경우는 겹치지 않는다. [강의: 29–34쪽]

```text
C(n,k) = C(n-1,k-1) + C(n-1,k)    (0 < k < n)
C(n,0) = C(n,n) = 1
```

| n | C(n,0) | C(n,1) | C(n,2) | C(n,3) | C(n,4) |
|---|---:|---:|---:|---:|---:|
| 0 | 1 | | | | |
| 1 | 1 | 1 | | | |
| 2 | 1 | 2 | 1 | | |
| 3 | 1 | 3 | 3 | 1 | |
| 4 | 1 | 4 | 6 | 4 | 1 |

2차원 테이블은 이전 행만 참조한다. 그래서 한 행으로 압축할 수 있지만, 갱신은 오른쪽에서 왼쪽으로 해야 한다.

```python
def binomial(n, k):
    if n < 0 or not 0 <= k <= n:
        raise ValueError("require 0 <= k <= n")
    k = min(k, n - k)
    row = [1] + [0] * k
    for i in range(1, n + 1):
        for j in range(min(i, k), 0, -1):
            row[j] += row[j - 1]
    return row[k]

assert binomial(5, 2) == 10
assert binomial(0, 0) == 1
```

오른쪽부터 갱신하면 `row[j-1]`가 아직 이전 행의 값이다. 왼쪽부터 갱신하면 현재 행의 값을 섞어 쓰므로 파스칼 점화식이 깨진다. 산술 연산 횟수는 O(nk), 저장하는 정수 개수는 O(k)다. 실제 이항계수만 필요하면 표준 라이브러리 `math.comb`도 사용할 수 있다.

## 5. 동전 거스름돈: 마지막 동전을 모두 비교하기

여기서는 **각 동전을 무제한 사용**하고, 음수가 아닌 정수 금액을 정확히 만드는 최소 동전 수를 구한다. 큰 동전부터 고르는 그리디는 임의의 동전 체계에서 정답을 보장하지 않는다. 예를 들어 동전 `[1,3,4]`로 6을 만들 때 `4+1+1`은 3개지만 `3+3`은 2개다. [강의: 36–41쪽, 예제 재구성]

상태 `dp[x]`는 금액 x를 만드는 최소 개수다. 마지막에 동전 c를 사용했다면 그 앞의 금액은 x-c다.

```text
dp[0] = 0
dp[x] = min(dp[x-c] + 1)    (c <= x인 모든 동전 c)
만들 수 없는 금액은 INF
```

```python
def min_coins(coins, amount):
    if amount < 0 or any(c <= 0 for c in coins):
        raise ValueError("require positive coins and nonnegative amount")
    inf = amount + 1
    dp = [0] + [inf] * amount
    for x in range(1, amount + 1):
        for c in coins:
            if c <= x:
                dp[x] = min(dp[x], dp[x - c] + 1)
    return -1 if dp[amount] == inf else dp[amount]

assert min_coins([1, 3, 4], 6) == 2
assert min_coins([4, 6], 5) == -1
assert min_coins([], 0) == 0
```

양의 정수 동전만 받으므로 `x-c < x`이고 작은 금액부터 계산할 수 있다. 만들 수 있는 해의 동전 수는 최대 amount이므로 `amount+1`을 불가능 표시로 쓸 수 있다. 0원 동전이나 음수 동전은 이 의존 관계를 깨므로 입력 조건에서 제외한다.

동전 종류 수가 m, 목표 금액이 A라면 시간 O(Am), 공간 O(A)다. A의 숫자 크기에 비례하므로 입력 비트 수에 대해서는 **의사 다항 시간**이다. 최소 개수 문제를 조합의 수, 순서 있는 경우의 수, 각 동전 한 번만 쓰는 문제와 혼동하지 않는다.

## 6. 자료에서 보완한 부분

- **21쪽의 최장 경로 설명:** 정점만 상태로 두는 접근으로 일반 그래프의 최장 단순 경로를 해결할 수 없다는 취지로 제한해야 한다. DAG에서는 위상순서 DP가 가능하고, 일반 그래프도 방문 집합을 상태에 포함한 지수 크기 DP를 만들 수 있다. “DP로 해결 불가능”이라고 일반화하지 않는다.
- **17쪽의 DP 범위:** 피보나치와 이항계수처럼 최적화가 아닌 계산·계수 문제에도 적용한다.
- **27쪽의 성능 설명:** 메모이제이션도 중복 계산을 제거한다. 상향식의 장점은 호출 스택 제거와 계산 순서 통제이며, 모든 문제에서 항상 더 빠르다고 단정하지 않는다.
- **15쪽의 초기화:** 길이가 `n+1`인 배열에 `memo[1]`을 쓰는 예제는 n=0 처리에 별도 분기가 필요하다. 본문의 함수는 0을 검사했다.

## 7. 검증과 복습

본문 코드는 자동화 도구로 실행해 확인했다. 피보나치 세 구현은 0부터 30까지 서로 비교했고, 이항계수는 n=0부터 20까지 모든 유효 k를 `math.comb`와 대조했다. 최소 동전 수는 작은 금액에 대해 금액 그래프의 BFS 결과와 비교했다. 이는 지정한 입력의 검증이며 일반적인 정당성은 위의 상태·전이 설명에 근거한다.

- 피보나치를 상수 개수의 변수로 줄일 수 있는 이유는?
- 이항계수의 1차원 갱신을 왼쪽부터 하면 무엇이 달라지는가?
- `dp[0]=0`과 다른 칸의 불가능 표시가 각각 필요한 이유는?
- 방문 여부가 이후 선택에 영향을 주면 정점 번호 하나가 충분한 상태인가?

공개 참고 자료: [MIT 6.006 강의 노트: Dynamic Programming](https://ocw.mit.edu/courses/6-006-introduction-to-algorithms-fall-2011/pages/lecture-notes/), [Python math.comb](https://docs.python.org/3/library/math.html#math.comb). 강의 페이지 표시는 학습 근거이며 원본 슬라이드나 원문 전체는 포함하지 않았다.

검증 환경: Python 3.11.9, NumPy 2.4.6, macOS arm64. 공식 문서 확인일: 2026-09-17.
