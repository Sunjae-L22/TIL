# 기계학습 방법론 (AI & 기계학습 방법론 1~4차시)

> 출처: `1_2_기계학습방법론_선형회귀.pdf` (총 225페이지, SSAFY · 서울대 오민환 교수) · 정리일: 2026-08-15
> 전편: [기계학습 기초](0806_기계학습기초.md) — 지도학습 틀 $Y = f^*(X) + \epsilon$, 가설공간, 오버피팅 개념을 이어받는다
> 페이지 표기는 PDF 기준 (슬라이드 인쇄 번호와 동일)

## 한눈에 보기

- 최소제곱법으로 회귀계수를 어떻게 구하는가 — 단순회귀의 닫힌 해 공식과 다중회귀의 정규방정식 $\hat{\boldsymbol\beta} = (\mathbf X^\top \mathbf X)^{-1}\mathbf X^\top \mathbf y$
- 회귀 결과표(Coefficient·Std. Error·t-statistic·p-value·$R^2$)를 읽고 "이 변수가 유의한가"를 판단할 수 있는가
- 분류 문제에 선형회귀가 부적합한 이유, 그리고 시그모이드·오즈·로짓 변환으로 로지스틱 회귀가 선형 모형과 연결되는 구조
- 신경망이 ReLU로 조각별 선형(piecewise linear) 함수를 만드는 과정 — Shallow와 Deep의 표현력 차이, $\phi,\theta,\psi \to \beta,\Omega$ 표기 정리
- 손실함수 → 경사하강 → SGD → 역전파로 이어지는 "신경망 학습"의 전체 파이프라인

## 목차

1. [1차시 선형회귀](#1차시-선형회귀) (p.1-36)
2. [2차시 로지스틱회귀](#2차시-로지스틱회귀) (p.37-66)
3. [3차시 신경망](#3차시-신경망) (p.67-149)
4. [4차시 신경망 학습](#4차시-신경망-학습) (p.150-214)
5. [부록 참고자료와 실습 미리보기](#부록-참고자료와-실습-미리보기) (p.215-225)

---

## 1차시 선형회귀

### 선형회귀란 (p.6-9)

입력 변수와 출력 변수 사이의 관계를 **직선 형태로 근사**해 예측하는 통계적 방법. 지도학습의 가장 기초가 되는 접근이다. 전편의 틀로 보면 가설공간을 "직선(1차 함수)"으로 제한한 경우다. 단순해 보이지만 개념적으로도 실무적으로도 매우 유용하다(광고비-매출 분석, 신용점수 산출 등).

예시 데이터는 ISLR의 광고 데이터: TV/Radio/Newspaper 광고비(입력)와 매출(출력). 선형회귀로 대답할 수 있는 질문들 (p.9):

- 광고비와 매출 사이에 관계가 있는가?
- 그 관계의 강도는 어느 정도인가?
- 어떤 매체가 매출에 기여하는가?
- 미래 매출을 얼마나 정확히 예측할 수 있는가?
- 매체 간에 상호작용(시너지)가 있는가?

### 단순선형회귀 모형 (p.10-12)

한 개의 설명변수($X$)와 하나의 반응변수($Y$) 사이의 선형 관계를 찾는 방법.

$$Y = \beta_0 + \beta_1 X + \epsilon$$

| 기호 | 의미 |
|---|---|
| $\beta_0$ | 절편 ($X=0$일 때 값) |
| $\beta_1$ | 기울기 ($X$가 1단위 증가할 때 $Y$의 평균 증가량) |
| $\epsilon$ | 관측 오차 |
| hat 표기 ($\hat\beta_0, \hat y$) | 데이터로부터 구한 **추정값** |

예측 예시: TV 광고비가 1억(=100백만원)일 때 매출 추정값은 $\hat y = \hat\beta_0 + 100\hat\beta_1 \approx$ 약 1,200만원 (아래 광고 데이터 계수 기준).

### 최소제곱법 (p.13-14)

실제 관측값과 예측값의 차이(잔차)를 제곱해 합한 값(RSS)을 최소화하는 계수를 찾는 방법.

- 잔차(residual): $e_i = y_i - \hat y_i$, 예측값 $\hat y_i = \hat\beta_0 + \hat\beta_1 x_i$
- RSS(잔차제곱합):

$$\mathrm{RSS} = e_1^2 + e_2^2 + \cdots + e_n^2 = \sum_{i=1}^n (y_i - \hat y_i)^2 = \sum_{i=1}^n \left(y_i - \hat\beta_0 - \hat\beta_1 x_i\right)^2$$

계수는 **closed-form solution**(공식으로 바로 계산할 수 있는 해)이 존재한다:

$$\text{기울기 } \hat\beta_1 = \frac{\sum_{i=1}^n (x_i - \bar x)(y_i - \bar y)}{\sum_{i=1}^n (x_i - \bar x)^2}, \qquad \text{절편 } \hat\beta_0 = \bar y - \hat\beta_1 \bar x$$

여기서 $\bar y = \frac{1}{n}\sum_i y_i,\ \bar x = \frac{1}{n}\sum_i x_i$.

> **예고** — 슬라이드에 "1-2챕터 4차시에서 경사 하강법으로 선형회귀를 푸는 방법을 다룰 예정"이라고 명시되어 있다 (p.14, 24). 닫힌 해로 푸는 이번 방식과 반복 최적화로 푸는 4차시 방식을 대비해서 보라는 뜻.

### 광고 데이터 결과 해석 (p.15-18)

TV 광고비($X$)로 매출($Y$)을 단순선형회귀한 결과 (단위: 1백만원).

| | Coefficient | Std. Error | t-statistic | p-value |
|---|---|---|---|---|
| Intercept | $\hat\beta_0=7.0325$ | 0.4578 | 15.36 | < 0.0001 |
| TV | $\hat\beta_1=0.0475$ | 0.0027 | 17.67 | < 0.0001 |

- **계수 해석**: 절편 7.03 → TV 광고비가 0이어도 평균 판매량 7.03백만원. TV 계수 0.0475 → 광고비 1단위(1백만원) 증가 시 평균 매출 약 4.75만원 증가
- **t-statistic** = Coefficient / Std. Error. 표본 변동성 대비 평균 차이가 얼마나 큰지를 나타내며, 클수록 통계적으로 유의할 가능성이 커짐
- **p-value**: $\beta_1 = 0$일 때 관측값만큼(그 이상) 극단적일 확률. 여기선 < 0.0001 (≪ 0.05)이므로 TV 광고비와 매출 간 관계가 존재한다고 판단
- **모형 적합도** $R^2 = 0.612$ → 판매량 변동의 약 61%를 광고비로 설명 가능 (1에 가까울수록 좋음)

> **원본 오류** — 슬라이드(p.16-18)는 "0.0475×1단위(1백만원) = **4.72만원** 증가"라고 적었지만, $0.0475 \times 100$만원 $= 4.75$만원이 맞다. 계수 표 자체(0.0475)는 ISLR 원문과 일치.

### 다중선형회귀 (p.19-23)

독립 변수가 여러 개일 때 사용하는 회귀. TV만 보던 단순회귀와 달리 TV·Radio·Newspaper를 동시에 고려한다.

$$Y = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \cdots + \beta_p X_p + \epsilon$$

- $\beta_j$ 해석: **다른 변수를 고정한 채** $X_j$가 1단위 증가할 때 $Y$가 평균적으로 $\beta_j$만큼 변화
- $\epsilon$은 관측 오차로, **모델이 설명하지 못하는 부분이지 오류/잔차가 아니다**라고 강조 (모집단 수준의 $\epsilon$ vs 표본에서 계산되는 잔차 $e_i$의 구분)
- 광고 데이터 예: $\text{sales} = \beta_0 + \beta_1\,\text{TV} + \beta_2\,\text{radio} + \beta_3\,\text{newspaper}$
- 추정 방법은 동일하게 RSS 최소화. 기하적으로는 데이터와 가장 가까운 **평면(hyperplane)**을 찾는 과정

$$\mathrm{RSS} = \sum_{i=1}^n e_i^2 = \sum_{i=1}^n (y_i - \hat y_i)^2 = \sum_{i=1}^n \left(y_i - \hat\beta_0 - \hat\beta_1 x_{i1} - \cdots - \hat\beta_p x_{ip}\right)^2$$

### 행렬 표현과 정규방정식 (p.24)

HARD 표시가 붙은 유도. $n$개 데이터를 한 번에 쓰면:

$$\mathbf y = \begin{bmatrix} y_1 \\ y_2 \\ \vdots \\ y_n \end{bmatrix} = \begin{bmatrix} 1 & x_{11} & \cdots & x_{1p} \\ 1 & x_{21} & \cdots & x_{2p} \\ \vdots & \vdots & \vdots & \vdots \\ 1 & x_{n1} & \cdots & x_{np} \end{bmatrix} \begin{bmatrix} \beta_0 \\ \vdots \\ \beta_p \end{bmatrix} + \begin{bmatrix} \epsilon_1 \\ \vdots \\ \epsilon_n \end{bmatrix} = \mathbf X\boldsymbol\beta + \boldsymbol\epsilon$$

RSS를 행렬로 쓰고 미분해 0이 되는 지점을 찾는다:

$$\mathrm{RSS} = (\mathbf y - \mathbf X\hat{\boldsymbol\beta})^\top(\mathbf y - \mathbf X\hat{\boldsymbol\beta}) = \mathbf y^\top\mathbf y - \hat{\boldsymbol\beta}^\top\mathbf X^\top\mathbf y - \mathbf y^\top\mathbf X\hat{\boldsymbol\beta} + \hat{\boldsymbol\beta}^\top\mathbf X^\top\mathbf X\hat{\boldsymbol\beta}$$

$$\frac{\partial\,\mathrm{RSS}}{\partial\hat{\boldsymbol\beta}} = -2\mathbf X^\top\mathbf y + 2\mathbf X^\top\mathbf X\hat{\boldsymbol\beta} = \mathbf 0 \;\;\Longrightarrow\;\; \hat{\boldsymbol\beta} = [\hat\beta_0, \hat\beta_1, \ldots, \hat\beta_p]^\top = (\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top\mathbf y$$

이것이 **정규방정식(normal equations) 해**다. RSS가 가장 작아지는 계수를 "기울기 = 0인 지점"으로 찾는다는 발상은 4차시 경사하강법과 정확히 같은 목적, 다른 수단이다.

### 다중회귀 결과 해석 (p.25-26)

| | Coefficient | Std. Error | t-statistic | p-value |
|---|---|---|---|---|
| Intercept | 2.939 | 0.3119 | 9.42 | < 0.0001 |
| TV | $\hat\beta_1=0.046$ | 0.0014 | 32.81 | < 0.0001 |
| radio | $\hat\beta_2=0.189$ | 0.0086 | 21.89 | < 0.0001 |
| newspaper | $\hat\beta_3=-0.001$ | 0.0059 | -0.18 | 0.8599 |

- TV·라디오는 매출 증가에 유의미 (p-value ≪ 0.05)
- **신문 광고비는 통계적으로 유의하지 않음** (p-value 0.8599 > 0.05) → 매출과 관계가 거의 없음. 단순회귀에서는 유의해 보이던 변수가 다른 변수와 함께 넣으면 사라지는 전형적 사례
- $R^2 = 0.897$ → 단순회귀($R^2=0.612$) 대비 크게 향상된 설명력

### 선형회귀 주의사항 (p.27-29)

- **훈련 성능 ≠ 일반화 성능**: 최소제곱 해는 훈련 데이터만 보고 계산되므로 훈련셋 적합도는 테스트 성능을 과소평가하기 쉽다. 검증/테스트셋으로 일반화 성능을 확인해야 하고, 변수가 많거나 고차항을 쓰면 선형회귀도 **오버피팅**이 발생할 수 있다 (검증/교차검증으로 적절한 적합을 찾음)
- **다중공선성**: 변수들이 서로 상관되어 있으면 계수 추정이 불안정해지고(분산 증가) 해석에 혼동이 생긴다. 변수들이 독립적일 때만 계수 해석이 명확
- **상관 ≠ 인과**: 관찰 데이터의 상관관계로 인과를 주장하면 안 된다. 예: "아이스크림 소비량" vs "상어에 물리는 사고" — 선형 관계가 보여도 인과가 아니다

### 1차시 확인문제 (p.33-36)

서술형 8문항. 정답 요지만 압축:

- 선형회귀의 기본 목적 = 입력·출력 간 **선형 관계를 찾아 예측·해석**
- 잔차 = 실제값 − 예측값. 모델이 설명하지 못한 부분으로 적합도 판단 지표
- 최소제곱법 = **RSS를 최소화**하는 계수를 찾는 방법
- 변수 추가의 양면: 설명력 향상 ↔ 과적합 위험·해석 복잡화
- 다중공선성 발생 시: 계수 분산 증가 → 추정 불안정, 해석·예측력 저하
- 다중회귀 성능 평가 지표 둘: **결정계수 $R^2$**, **잔차표준오차(RSE)**

---

## 2차시 로지스틱회귀

### 분류 문제란 (p.42-45)

**분류(Classification)** = 정해진 범주(카테고리) 중 하나로 지정하는 것. 범주형 변수는 수치의 크고 작음이 아니라 유한한 범주(성별, 혈액형, 지역 등)로 표현되는 변수다. 예: 눈동자 색 {black, brown, blue, green}, 이메일 {spam, normal} — **순서 없는 집합**.

분류 함수의 목표: $f(x)$를 학습하여 입력 $x$가 속할 범주를 예측. 이때 범주를 직접 찍는 것보다 **각 범주에 속할 확률 $P(Y=k \mid X)$를 추정하는 것이 더 유용**할 때가 많다.

예시 데이터 — 신용카드 연체(Default). 산점도·Boxplot(p.44-45): 연체자(+)는 신용카드 사용량(Balance)이 높은 구간에 집중해 그룹 간 분포(중앙값·산포)가 뚜렷이 다르고, **소득(Income)은 분포가 대부분 겹쳐 연체 여부와 큰 상관이 보이지 않음**.

### 분류에 선형회귀를 쓰면 안 되는 이유 (p.46-47)

- **이진 분류**: 선형회귀는 선형함수라 예측값이 제한된 범위를 갖지 못한다 → 예측 확률이 0보다 작거나 1보다 크게 나올 수 있어 확률로 쓰기 부적절 (예: 응급실 환자 {0: 비응급, 1: 응급})
- **다중 분류**: $Y \in \{1: 맑음,\ 2: 눈,\ 3: 비\}$처럼 정수 코딩하면 **인위적 순서**와 1↔2, 2↔3 거리 동일성까지 암묵적으로 가정하게 되어 부적합 (강의 정리 p.60에서 재강조)
- 대안: **시그모이드 함수를 활용해 0~1 범위 확률값을 보장**하는 로지스틱 회귀

### 시그모이드와 로지스틱 모형식 (p.49-50)

기울어진 S자 곡선인 시그모이드(sigmoid) 함수:

$$y = \frac{e^z}{1+e^z} = \frac{1}{1+e^{-z}} \qquad (\text{오일러 수 } e \approx 2.71828)$$

$z \to +\infty$면 $y \to 1$, $z \to -\infty$면 $y \to 0$으로 수렴. 여기의 $z$에 선형회귀 식 $z = \beta_0 + \beta_1 x$를 대입하면 로지스틱 회귀 모형식이 된다. $p(X) = P(Y=1 \mid X)$로 두면:

$$p(X; \boldsymbol\beta) = \frac{e^{\beta_0+\beta_1 x}}{1+e^{\beta_0+\beta_1 x}} = \frac{1}{1+e^{-(\beta_0+\beta_1 x)}}, \qquad \boldsymbol\beta = [\beta_0, \beta_1]^\top$$

> **주의** — 슬라이드 p.49는 출력 범위를 "$0 \le y \le 1$"로 적었지만, 확인문제 정답(p.64)은 **개구간 $(0,1)$**이라고 명확히 한다. 0과 1은 극한으로만 접근하며 실제로 도달하지 않는다. $[0,1]$이라고 답하면 틀린다.

### 오즈와 로짓 변환 (p.51-52)

로지스틱 함수가 선형 함수를 "내포"하는 구조를 해석하는 도구 (HARD).

**오즈(odds)**: 성공($y=1$) 확률이 실패($y=0$) 확률의 몇 배인가.

$$\mathrm{odds} = \frac{p(y=1|x)}{p(y=0|x)} = \frac{p(y=1|x)}{1-p(y=1|x)} = \frac{\text{성공확률}}{\text{실패확률}}$$

**로짓 변환(logit)** = 오즈에 log를 취한 것. 로지스틱 모형식에 로짓 변환을 수행하면 선형식이 튀어나온다:

$$\mathrm{logit}(p) = \log(\mathrm{odds}) = \log\frac{p(x;\boldsymbol\beta)}{1-p(x;\boldsymbol\beta)} = \beta_0 + \beta_1 x$$

정리하면 양방향 관계:

- **로지스틱 모형 = 선형 모형 + 시그모이드 함수**
- **로지스틱 모형의 로짓 변환 = 선형 모형**

### 우도와 MLE (p.53-56)

선형회귀에서 "함수가 데이터와 오차가 작은지"를 MSE로 평가했듯, **확률을 계산하는 함수**는 **우도(Likelihood)** — "현재 확률 함수가 데이터를 얼마나 잘 설명하는지" — 로 평가한다. 우도를 최대화하는 모수를 찾는 것이 **MLE(Maximum Likelihood Estimation)**.

이진 분류에서 우도:

$$\underset{\boldsymbol\beta}{\text{maximize}}\ \mathcal L(\boldsymbol\beta) = \prod_{i:y_i=1} p(x_i;\boldsymbol\beta) \prod_{i:y_i=0} \left(1 - p(x_i;\boldsymbol\beta)\right)$$

곱으로 이뤄진 함수는 $\beta$에 대해 미분이 어렵기 때문에 양변에 log를 취해 **곱셈을 덧셈으로** 바꾼 log-likelihood를 최대화한다:

$$\underset{\boldsymbol\beta}{\text{maximize}}\ \log\mathcal L(\boldsymbol\beta) = \sum_{i=1}^n \left[y_i \log p(x_i;\beta) + (1-y_i)\log\left(1-p(x_i;\beta)\right)\right]$$

- log를 취해도 되는 이유: **log는 단조(monotone) 증가 함수**라서 log-likelihood를 최대화하는 $\beta$와 likelihood를 최대화하는 $\beta$는 같다 (p.55)
- 선형회귀와 달리 닫힌 해가 없어, $\log\mathcal L(\beta)$를 미분해 도함수=0에 근접하도록 **수치적(반복) 최적화**로 $\beta$를 찾는다 (p.56)

### 신용카드 연체 데이터 결과 (p.57-59)

Balance 하나로 Default를 예측한 로지스틱 회귀 결과:

| | Coefficient | Std. error | z-statistic | p-value |
|---|---|---|---|---|
| Intercept | −10.6513 | 0.3612 | −29.5 | <0.0001 |
| balance | 0.0055 | 0.0002 | 24.9 | <0.0001 |

- 해석: Balance가 1단위 증가할 때 연체의 **로짓(log-odds)이 0.0055 증가**. $\log\frac{p(X;\hat\beta)}{1-p(X;\hat\beta)} = -10.6513 + 0.0055 \cdot X$
- 예측 확률 계산 (p.58): $X=1000$이면 $p = \frac{e^{-10.6513+0.0055\cdot1000}}{1+e^{-10.6513+0.0055\cdot1000}} = 0.006$, $X=2000$이면 $0.586$. **입력이 2배 되면 예측 확률은 97배** — 로짓이 선형이라 확률은 비선형으로 뛴다
- 다중 로지스틱 (p.59): $\log\frac{p}{1-p} = \beta_0 + \beta_1X_1 + \cdots + \beta_pX_p$로 확장

| | Coefficient | Std. error | z-statistic | p-value |
|---|---|---|---|---|
| Intercept | −10.8690 | 0.4923 | −22.08 | <0.0001 |
| balance | 0.0057 | 0.0002 | 24.74 | <0.0001 |
| income | 0.0030 | 0.0082 | 0.37 | 0.7115 |
| student[Yes] | −0.6468 | 0.2362 | −2.74 | 0.0062 |

income은 유의하지 않고(EDA에서 본 그대로), 학생 여부는 유의하며 계수가 음수(학생이면 로짓 감소).

### 2차시 확인문제 (p.61-66)

- 틀린 설명 고르기 → "기상 상태 예측에 $Y\in\{1,2,3\}$ 정수 코딩을 해야만 적절하다"가 틀림 (인위적 순서·거리 가정)
- 로지스틱 함수의 출력 범위 → $(0,1)$ (개구간)
- "로지스틱 회귀의 적합도 지표는 MSE다" → X. **우도(likelihood)를 MLE로 최대화**한다

---

## 3차시 신경망

> 3~4차시는 Understanding Deep Learning(Simon Prince) 교재의 표기($\phi, \theta, \psi, \beta, \Omega$)와 그림을 따른다. 3차시에는 강의정리·확인문제 슬라이드가 없다.

### 모수적 함수로서의 선형 모델 (p.72-74)

1D 선형모델 $y = \phi_0 + \phi_1 x$도 "모수(parameter) $\phi$가 정해지면 함수가 결정되는" 모수적 함수다. 손실함수(최소제곱)를 정의하고 손실이 줄도록 $\phi$를 순차적으로 조정하며 찾는 그림(p.73-74)이 4차시 경사하강의 예고편.

$$L[\boldsymbol\phi] = \sum_{i=1}^{I}\left(\mathrm f[x_i, \boldsymbol\phi] - y_i\right)^2 = \sum_{i=1}^{I}(\phi_0 + \phi_1 x_i - y_i)^2$$

### Shallow 네트워크 (p.75-80)

입력층 – 은닉층(hidden layer) 1개 – 출력층으로 구성된 신경망. 복잡한 비선형 패턴까지 학습할 수 있어 선형 모델보다 표현력이 높고, 구조가 간단해 학습이 빠르며 과적합 위험이 비교적 낮다. XOR처럼 비선형 결정경계가 필요한 문제에 사용.

1D 선형회귀와 나란히 두면 (p.77-78):

$$\text{선형회귀: } y = \mathrm f[x, \boldsymbol\phi] = \phi_0 + \phi_1 x$$

$$\text{Shallow 네트워크: } y = \phi_0 + \phi_1\,\mathrm a[\theta_{10} + \theta_{11}x] + \phi_2\,\mathrm a[\theta_{20} + \theta_{21}x] + \phi_3\,\mathrm a[\theta_{30} + \theta_{31}x]$$

$\mathrm a[\cdot]$는 **활성화 함수(activation function)**. 대표 예가 ReLU (p.79):

$$\mathrm a[z] = \mathrm{ReLU}[z] = \begin{cases} 0 & z < 0 \\ z & z \ge 0 \end{cases}$$

위 네트워크의 모수는 10개다 (p.80): $\boldsymbol\phi = \{\phi_0,\phi_1,\phi_2,\phi_3,\theta_{10},\theta_{11},\theta_{20},\theta_{21},\theta_{30},\theta_{31}\}$. 모수가 정해지면 함수가 결정되고 추론(예측)할 수 있으며, 훈련데이터 $\{(x_i,y_i)\}_{i=1}^I$가 주어지면 손실함수를 정의해 손실을 최소화하도록 모수를 조정한다 — 선형회귀와 완전히 같은 틀이다.

### Hidden Unit과 조각별 선형 함수 (p.81-88)

식을 두 부분으로 나눠 생각한다 (p.82):

$$y = \phi_0 + \phi_1 h_1 + \phi_2 h_2 + \phi_3 h_3, \qquad \begin{aligned} h_1 &= \mathrm a[\theta_{10} + \theta_{11}x] \\ h_2 &= \mathrm a[\theta_{20} + \theta_{21}x] \\ h_3 &= \mathrm a[\theta_{30} + \theta_{31}x] \end{aligned}$$

단계별 계산 과정 (p.83-86):

```text
① 선형변환   θ_d0 + θ_d1·x        — 직선 3개
② 활성화     h_d = a[…] (ReLU)    — 각 직선의 음수 부분이 0으로 잘림 (꺾인 점 생성)
③ 상수배     φ_d · h_d            — 기울기·부호 조정
④ 합산       φ_0 + Σ φ_d h_d      — 꺾인 점들이 합쳐져 조각별 선형 함수 완성
```

결과는 입력 구간을 나눈 **조각별 선형(piecewise linear) 함수**다. hidden unit이 3개면 꺾이는 점이 최대 3개. 파라미터 $\phi$가 달라지면 다른 모양의 조각별 선형 함수가 나온다 (p.81).

네트워크 도식(p.87-88)에서는 각 파라미터가 "(출발 노드의 값)×(가중치)를 출력 노드에 더한다"로 읽는다. 절편 $\theta_{d0}, \phi_0$은 값이 1인 바이어스 노드에서 나오는 화살표로 그린다.

### 표현력과 보편적 근사 정리 (p.89-92)

hidden unit을 $D$개로 일반화하면 (p.90):

$$h_d = \mathrm a[\theta_{d0} + \theta_{d1}x], \qquad y = \phi_0 + \sum_{d=1}^{D} \phi_d h_d$$

hidden unit이 많을수록 선형 조각(linear region)이 늘어난다 — 5개 → 10개 → 20개 조각 그림 (p.91). 충분히 많은 hidden unit이 있다면 임의의 1차원 함수를 원하는 정확도로 근사할 수 있다.

> **보편적 근사 정리(universal approximation theorem)** — "Hidden unit을 충분히 많이 갖는다면, 얕은 신경망은 임의의 연속함수를 임의의 정밀도로 근사할 수 있음" (p.92)

### 다중 입력·출력 (p.93-101)

- **출력 2개** (1 input, 4 hidden units, 2 outputs, p.94): hidden unit은 공유하고 출력별 가중치만 따로 둔다. $y_1 = \phi_{10} + \phi_{11}h_1 + \phi_{12}h_2 + \phi_{13}h_3 + \phi_{14}h_4$, $y_2 = \phi_{20} + \phi_{21}h_1 + \cdots + \phi_{24}h_4$
- **입력 2개** (2 inputs, 3 hidden units, p.95-100): $h_d = \mathrm a[\theta_{d0} + \theta_{d1}x_1 + \theta_{d2}x_2]$. 각 hidden unit의 "꺾임"이 2D 입력 평면 위의 **직선**이 되고, 이를 합치면 평면이 볼록 다각형 조각들로 나뉜다
- **일반형** (p.101): $D_i$개 입력, $D$개 hidden units, $D_o$개 출력

$$h_d = \mathrm a\left[\theta_{d0} + \sum_{i=1}^{D_i}\theta_{di}x_i\right], \qquad y_j = \phi_{j0} + \sum_{d=1}^{D}\phi_{jd}h_d$$

### Deep 네트워크: 합성과 접기 (p.102-128)

Deep 네트워크는 **네트워크 2개를 이어붙인 것**으로 이해한다 (p.103-104). 네트워크 1의 출력 $y$가 네트워크 2의 입력이 된다:

$$\text{Net 1: } h_d = \mathrm a[\theta_{d0} + \theta_{d1}x],\ y = \phi_0 + \textstyle\sum_d \phi_d h_d \qquad \text{Net 2: } h'_d = \mathrm a[\theta'_{d0} + \theta'_{d1}y],\ y' = \phi'_0 + \textstyle\sum_d \phi'_d h'_d$$

층별 출력 추적 (p.105-125, 같은 그림의 빌드업 슬라이드 약 20장):

- Net 1이 만드는 $x \to y$는 지그재그(조각별 선형) 함수
- Net 2의 $y \to y'$를 그 출력에 적용하면, $x \to y'$에는 Net 1이 만든 조각마다 Net 2의 패턴이 **복제**되어 훨씬 많은 조각이 생긴다

**"접기(folding)" 비유** (p.126): 첫 네트워크가 입력 공간을 종이처럼 **접고**(여러 $x$가 같은 $y$로 매핑), 두 번째 네트워크가 접힌 종이 위에 함수를 그린 뒤, 펼치면 그 패턴이 접힌 횟수만큼 복제된다.

**Shallow vs Deep 비교** (p.127):

| | 파라미터 수 | 조각적 선형 구역 |
|---|---|---|
| Deep (3 hidden units × 2층) | 20개 | 최대 16개 |
| Shallow (6 hidden units) | 19개 | 최대 7개 |

→ 비슷한 파라미터 수로 Deep 쪽이 만들 수 있는 조각 수가 훨씬 많다. **Deep 네트워크의 표현력이 높음**. 2D 입력에서도 마찬가지 (p.128).

### Deep 네트워크 수식: 새 변수와 행렬 표기 (p.129-149)

합성식을 그대로 전개하면 $h'_d = \mathrm a[\theta'_{d0} + \theta'_{d1}\phi_0 + \theta'_{d1}\phi_1h_1 + \theta'_{d1}\phi_2h_2 + \theta'_{d1}\phi_3h_3]$처럼 복잡해진다 (p.130). 괄호 안 상수 묶음을 **새 파라미터 $\psi$로 치환**하면 단순해진다 (p.131):

$$h'_1 = \mathrm a[\psi_{10} + \psi_{11}h_1 + \psi_{12}h_2 + \psi_{13}h_3], \quad h'_2 = \mathrm a[\psi_{20} + \psi_{21}h_1 + \psi_{22}h_2 + \psi_{23}h_3], \quad h'_3 = \mathrm a[\psi_{30} + \psi_{31}h_1 + \psi_{32}h_2 + \psi_{33}h_3]$$

$$y' = \phi'_0 + \phi'_1h'_1 + \phi'_2h'_2 + \phi'_3h'_3$$

2층 네트워크는 **합성 함수**다 (p.133-135): $y'$를 끝까지 전개하면 $\mathrm a[\,\cdot\,]$ 안에 $\mathrm a[\,\cdot\,]$가 중첩된 식이 된다. 두 번째 hidden unit들의 pre-activation(선형 결합값)만 보면 "출력 3개를 갖는 1층 네트워크"로 볼 수도 있다.

> **원본 오타** — p.133 우상단 식은 $h'_2 = \mathrm a[\psi_{20} + \psi_{21}h_2 + \psi_{22}h_2 + \psi_{23}h_3]$처럼 $h_1$ 자리가 $h_2$로 잘못 인쇄되어 있다 ($h'_3$도 동일). p.131·p.144의 올바른 식($\psi_{21}h_1$, $\psi_{31}h_1$) 기준으로 위에 적었다.

**벡터·행렬 표기** (p.141-148). 층별 식을 묶으면:

$$\mathbf h = \mathrm a[\boldsymbol\theta_0 + \boldsymbol\theta x], \qquad \mathbf h' = \mathrm a[\boldsymbol\psi_0 + \boldsymbol\Psi\mathbf h], \qquad y' = \phi'_0 + \boldsymbol\phi'^\top \mathbf h'$$

일반 표기로 바꾸면 (bias vector $\boldsymbol\beta$, weight matrix $\boldsymbol\Omega$):

$$\mathbf h_1 = \mathrm a[\boldsymbol\beta_0 + \boldsymbol\Omega_0\mathbf x], \qquad \mathbf h_2 = \mathrm a[\boldsymbol\beta_1 + \boldsymbol\Omega_1\mathbf h_1], \qquad \mathbf y = \boldsymbol\beta_2 + \boldsymbol\Omega_2\mathbf h_2$$

$K$개 층으로 일반화 (p.148):

$$\mathbf h_1 = \mathrm a[\boldsymbol\beta_0 + \boldsymbol\Omega_0\mathbf x], \quad \mathbf h_k = \mathrm a[\boldsymbol\beta_{k-1} + \boldsymbol\Omega_{k-1}\mathbf h_{k-1}], \quad \mathbf y = \boldsymbol\beta_K + \boldsymbol\Omega_K\mathbf h_K$$

$$\mathbf y = \boldsymbol\beta_K + \boldsymbol\Omega_K\,\mathrm a\big[\boldsymbol\beta_{K-1} + \boldsymbol\Omega_{K-1}\,\mathrm a[\cdots \boldsymbol\beta_2 + \boldsymbol\Omega_2\,\mathrm a[\boldsymbol\beta_1 + \boldsymbol\Omega_1\,\mathrm a[\boldsymbol\beta_0 + \boldsymbol\Omega_0\mathbf x]]\cdots]\big]$$

차원 읽는 법 (p.149 도식): 입력 $D_i=3$, 은닉층 크기 $D_1=4, D_2=2, D_3=3$, 출력 $D_o=2$이면 $\boldsymbol\Omega_0 \in \mathbb R^{4\times3}$, $\boldsymbol\Omega_1 \in \mathbb R^{2\times4}$, $\boldsymbol\Omega_2 \in \mathbb R^{3\times2}$, $\boldsymbol\Omega_3 \in \mathbb R^{2\times3}$, $\boldsymbol\beta_0 \in \mathbb R^4$, $\boldsymbol\beta_1 \in \mathbb R^2$, $\boldsymbol\beta_2 \in \mathbb R^3$, $\boldsymbol\beta_3 \in \mathbb R^2$. **$\boldsymbol\Omega_k$의 행 = 다음 층 크기, 열 = 이전 층 크기.**

---

## 4차시 신경망 학습

### 손실함수와 학습의 정의 (p.153-157)

- 학습 데이터셋: input/output의 $I$개 쌍 $\{\mathbf x_i, \mathbf y_i\}_{i=1}^I$
- **손실함수(loss function)**: 모델이 얼마나 잘못 예측하는지를 측정하는 함수. 값이 작을수록 정확하게 학습된 것. 정식 표기는 $L[\boldsymbol\phi, \mathrm f[\mathbf x_i, \boldsymbol\phi], \{\mathbf x_i, \mathbf y_i\}_{i=1}^I]$지만 간단히 $L[\boldsymbol\phi]$로 쓴다 — 입력을 출력에 더 잘 매핑할수록 더 작은 값을 반환하는 스칼라
- **학습(training)**: 손실함수를 최소화하는 파라미터를 찾는 것

$$\hat{\boldsymbol\phi} = \underset{\boldsymbol\phi}{\mathrm{argmin}}\left[L[\boldsymbol\phi]\right]$$

1D 선형회귀 예 (p.156-157): 최소제곱 손실 $L[\boldsymbol\phi] = \sum_{i=1}^I(\mathrm f[x_i,\boldsymbol\phi]-y_i)^2 = \sum_{i=1}^I(\phi_0+\phi_1x_i-y_i)^2$. 그림 읽는 법 — 왼쪽 등고선($\phi_0$-$\phi_1$ 평면의 손실 지형, 밝을수록 손실 큼)과 오른쪽 데이터+직선(현재 파라미터의 모델). 예시에선 $\phi_0=0.4, \phi_1=0.2$일 때 $L=7.11$.

### 미분 리뷰 (p.163-169)

$y = x^2 - 4x + 5$ (위로 열린 포물선)에서 $\frac{\partial y}{\partial x} = 2x - 4$. 미분값이 곧 기울기이며, **기울기가 0이 되는 지점이 극값(최소값)**. $x=0,1,3,4$ 각 지점에서 미분의 방향·크기를 화살표로 그려보는 빌드업 — 최소점에서 멀수록 기울기의 절대값이 크고, 부호는 "어느 쪽이 내리막인지"를 알려준다. 이 직관이 그대로 경사하강법이 된다.

### 경사하강법 (p.170-184)

**경사하강(gradient descent)**: 손실 함수 $L[\boldsymbol\phi]$를 최소화하기 위해 파라미터 $\boldsymbol\phi$를 반복적으로 갱신하는 알고리즘.

**Step 1 — 기울기 계산**: $L[\boldsymbol\phi]$를 각 파라미터에 대해 편미분한 벡터를 만든다.

$$\frac{\partial L}{\partial\boldsymbol\phi} = \begin{bmatrix} \dfrac{\partial L}{\partial\phi_0} \\ \dfrac{\partial L}{\partial\phi_1} \\ \vdots \\ \dfrac{\partial L}{\partial\phi_N} \end{bmatrix}$$

**Step 2 — 파라미터 업데이트**: 기울기(미분값)의 **반대 방향**으로 이동해야 손실이 줄어든다.

$$\boldsymbol\phi \longleftarrow \boldsymbol\phi - \alpha\frac{\partial L}{\partial\boldsymbol\phi}$$

$\alpha \ge 0$는 **학습률(learning rate)** — 한 번의 스텝에서 이동하는 크기를 결정. 너무 크면 발산, 너무 작으면 학습이 느리다(확인문제 6 해설).

1D 선형회귀에 적용 (p.172-183, HARD): 전체 손실은 샘플별 손실 $\ell_i$의 합이므로 기울기도 합이다.

$$L[\boldsymbol\phi] = \sum_{i=1}^I \ell_i = \sum_{i=1}^I (\phi_0 + \phi_1 x_i - y_i)^2, \qquad \frac{\partial L}{\partial\boldsymbol\phi} = \sum_{i=1}^I \frac{\partial\ell_i}{\partial\boldsymbol\phi}$$

$$\frac{\partial\ell_i}{\partial\boldsymbol\phi} = \begin{bmatrix} \dfrac{\partial\ell_i}{\partial\phi_0} \\ \dfrac{\partial\ell_i}{\partial\phi_1} \end{bmatrix} = \begin{bmatrix} 2(\phi_0 + \phi_1x_i - y_i) \\ 2x_i(\phi_0 + \phi_1x_i - y_i) \end{bmatrix}$$

"한눈으로 보는 경사하강법" (p.184) — 네 그림이 한 세트: a) 데이터, b) 손실 함수 3D 그래프, c) 손실 함수 등고선(2D), d) 선형함수의 업데이트. 등고선 위의 점 0→1→2→3→4 이동이 오른쪽 그림에서 직선이 데이터에 점점 맞아 들어가는 과정과 대응된다.

### Convex vs Non-convex (p.185-186)

- **Convex**: 아래로 볼록. 그래프 위 임의 두 점을 잇는 선분이 그래프 위(또는 같은 위치)에 있음 → **전역(global) 최소값이 유일**해 최적화가 쉽다
- **Non-convex**: 봉우리·골짜기가 섞임. 두 점을 이은 직선이 그래프 아래로 내려가는 구간 존재 → **여러 지역(local) 최소값과 새들(saddle)점**이 있어 최적화가 어렵다

강의 정리(p.209)의 요약: convex 문제는 2차 미분이 항상 양수(아래로 볼록) → 국소 최소값 = 전역 최소값 → 안정적인 학습 가능.

### 확률적 경사하강법 SGD (p.187-193)

경사하강법의 단점 (p.188-189):

1. Non-convex 문제에서 **지역(local) 최소점에 빠지기 쉬움** — 등고선 그림에서 출발점 2는 local 최소점에 갇힌다
2. 매 스텝마다 **전체 데이터**에 대한 미분값을 계산 → 스텝당 계산량이 많음

대안 = 전체 데이터 대신 **무작위로 선택한 일부 샘플(batch)만 사용**하는 확률적 경사하강법(SGD).

$$\text{GD: } \boldsymbol\phi_{t+1} \longleftarrow \boldsymbol\phi_t - \alpha\sum_{i=1}^{I}\frac{\partial\ell_i[\boldsymbol\phi_t]}{\partial\boldsymbol\phi} \qquad \text{SGD(mini-batch): } \boldsymbol\phi_{t+1} \longleftarrow \boldsymbol\phi_t - \alpha\sum_{i\in\mathcal B_t}\frac{\partial\ell_i[\boldsymbol\phi_t]}{\partial\boldsymbol\phi}$$

SGD의 특성 (p.191-193):

- **경로에 무작위성(노이즈)**: 일부 배치로 기울기를 계산하므로 지그재그(jitter)로 움직임. 그 노이즈가 오히려 **local minima·saddle point에서 빠져나오는 데 도움**이 됨
- **여전히 타당한 업데이트**: 미니배치 기울기는 정확한 전체 기울기는 아니지만 **평균적으로 올바른 방향**을 가리켜 점진적으로 최적점으로 수렴
- **계산 비용 절감**: 반복(스텝)당 연산량이 적어 큰 데이터셋에서 효율적
- **수렴 특성**: full-batch처럼 매끄럽게 수렴하지 않고 더 많이 진동하지만 전역 최저점 근처의 좋은 해에 도달 가능. 단, **convex 문제에서는 full-batch보다 수렴이 늦을 수 있다** — "SGD가 항상 빠르다"고 말하면 안 됨(확인문제 7)

### 역전파 (p.194-208)

신경망은 layer별로 파라미터($\boldsymbol\beta_k, \boldsymbol\Omega_k$)가 있고 서로 영향을 미친다. 이들의 미분값을 구하는 절차가 **역전파(backpropagation)** — 출력 오차를 기준으로 **그래프를 거꾸로** 따라가며 **연쇄법칙**으로 각 노드의 미분값을 계산한다 (HARD).

**준비 — 각 단계별 계산을 분해** (p.195): 3개 은닉층 네트워크의 forward를 pre-activation $\mathbf f_k$와 activation $\mathbf h_k$로 쪼갠다.

$$\mathbf f_0 = \boldsymbol\beta_0 + \boldsymbol\Omega_0\mathbf x_i,\quad \mathbf h_1 = \mathrm a[\mathbf f_0],\quad \mathbf f_1 = \boldsymbol\beta_1 + \boldsymbol\Omega_1\mathbf h_1,\quad \mathbf h_2 = \mathrm a[\mathbf f_1],\quad \mathbf f_2 = \boldsymbol\beta_2 + \boldsymbol\Omega_2\mathbf h_2,\quad \mathbf h_3 = \mathrm a[\mathbf f_2],\quad \mathbf f_3 = \boldsymbol\beta_3 + \boldsymbol\Omega_3\mathbf h_3,\quad \ell_i = \mathrm l[\mathbf f_3, y_i]$$

**연쇄법칙** (p.199): 합성함수 $y = f_1(f_0(x;\boldsymbol\Omega))$의 변화율 = 바깥의 변화율 × 안쪽의 변화율.

$$\frac{\partial y}{\partial\boldsymbol\Omega} = \frac{\partial f_1(f_0(x;\boldsymbol\Omega))}{\partial f_0(x;\boldsymbol\Omega)} \cdot \frac{\partial f_0(x;\boldsymbol\Omega)}{\partial\boldsymbol\Omega}$$

$f_1$의 입력($=f_0$)에 대한 기울기를 구한 뒤, $f_0$의 파라미터 $\Omega$에 대한 기울기와 곱한다. $x$는 파라미터가 아니므로 $\partial y/\partial x$는 필요 없다.

**역전파 절차** (p.200-208):

1. 각 단계별 계산을 분해
2. 각 layer별 값($\mathbf f_k$)을 계산 — forward pass
3. 각 layer별 값($\mathbf f_k$)에 대한 출력 손실($\ell_i$)의 미분을 구함 — 끝(손실)에서 시작해 거꾸로:

$$\frac{\partial\ell_i}{\partial\mathbf f_2} = \frac{\partial\mathbf h_3}{\partial\mathbf f_2}\frac{\partial\mathbf f_3}{\partial\mathbf h_3}\frac{\partial\ell_i}{\partial\mathbf f_3}, \qquad \frac{\partial\ell_i}{\partial\mathbf f_1} = \frac{\partial\mathbf h_2}{\partial\mathbf f_1}\frac{\partial\mathbf f_2}{\partial\mathbf h_2}\frac{\partial\ell_i}{\partial\mathbf f_2}, \qquad \frac{\partial\ell_i}{\partial\mathbf f_0} = \frac{\partial\mathbf h_1}{\partial\mathbf f_0}\frac{\partial\mathbf f_1}{\partial\mathbf h_1}\frac{\partial\ell_i}{\partial\mathbf f_1}$$

앞 단계에서 구한 $\frac{\partial\ell_i}{\partial\mathbf f_{k+1}}$이 다음 계산에 **재사용**되는 것이 역전파의 핵심 효율.

4. 파라미터에 대한 미분을 구함 (p.207): $\mathbf f_k = \boldsymbol\beta_k + \boldsymbol\Omega_k\mathbf h_k$이므로

$$\frac{\partial\ell_i}{\partial\boldsymbol\Omega_k} = \frac{\partial\mathbf f_k}{\partial\boldsymbol\Omega_k}\frac{\partial\ell_i}{\partial\mathbf f_k} = \frac{\partial\ell_i}{\partial\mathbf f_k}\mathbf h_k^\top, \qquad \frac{\partial\ell_i}{\partial\boldsymbol\beta_k} = \frac{\partial\mathbf f_k}{\partial\boldsymbol\beta_k}\frac{\partial\ell_i}{\partial\mathbf f_k} = \frac{\partial\ell_i}{\partial\mathbf f_k}$$

흐름 정리 (p.208):

```text
ℓᵢ → ∂ℓᵢ/∂f₃ → ∂ℓᵢ/∂h₃ → ∂ℓᵢ/∂f₂ → ∂ℓᵢ/∂h₂ → ∂ℓᵢ/∂f₁ → ∂ℓᵢ/∂h₁ → ∂ℓᵢ/∂f₀
        │                │                │                │
        ▼                ▼                ▼                ▼
  ∂ℓᵢ/∂β₃, ∂ℓᵢ/∂Ω₃   ∂ℓᵢ/∂β₂, ∂ℓᵢ/∂Ω₂   ∂ℓᵢ/∂β₁, ∂ℓᵢ/∂Ω₁   ∂ℓᵢ/∂β₀, ∂ℓᵢ/∂Ω₀
```

**손실로부터 각 layer별 파라미터의 미분을 거꾸로 구할 수 있음** — 이 미분값들을 경사하강/SGD의 업데이트 식에 넣으면 신경망 학습이 완성된다.

### 4차시 확인문제 (p.210-213)

객관식 4 + 단답 4. 정답 요지:

1. 손실함수의 역할 = **모델의 예측이 얼마나 잘못되었는지 측정**
2. 경사하강 Step 1 = **파라미터에 대한 기울기(미분값) 계산**
3. SGD의 장점 = **일부 데이터만으로 업데이트해 연산 비용이 작다** (항상 전역 최적해 도달 ×)
4. Convex 문제의 특징 = **2차 미분이 모든 구간에서 양수** (해설: 양수이거나 0 — 어떤 국소 최소점도 전역 최소점과 동일)
5. 학습 과정에서 조정하는 값 = **파라미터(가중치, 편향)**
6. 업데이트 시 사용하는 고정 비율 = **학습률 $\alpha$** (크면 발산, 작으면 느림)
7. SGD가 full-batch GD보다 항상 빠른가 = **아니다.** convex(또는 그에 가까운) 문제에서는 GD가 더 빠를 수 있다
8. SGD가 local minima를 벗어나는 이유 = **미니배치 무작위 선택으로 기울기에 노이즈가 포함**되어 국소 최저점·안장점에 머무르지 않고 더 넓은 영역을 탐색

---

## 부록 참고자료와 실습 미리보기

### 참고 자료 목록 (p.215-219)

차시별 외부 강의·영상 14건의 표 (URL은 슬라이드에 축약 링크로만 제시). 주제만 추리면:

- 1차시: 최소제곱회귀를 선형대수 관점으로(정규방정식), MSE+경사하강 파이썬 구현, 최소제곱 직선 적합·RSS, 사이킷런 PolynomialFeatures 특성 공학과 과적합, 다중선형회귀 Python 구현
- 2차시: 분류 개념·지도학습·평가지표, scikit-learn·StatsModels 로지스틱 실습, 로짓·시그모이드·결정경계(비전공자용), 오즈·최대우도법 이론
- 3차시: 모수적/비모수적 방법 개념, Shallow 네트워크·보편적 근사 정리 영상
- 4차시: 경사하강법 수학 원리, 학습률(step size)·local minima 문제, **Understanding Deep Learning 교재 실습 노트북**(선형 모델에 경사하강 직접 구현)

### 실습 미리보기: MLP 구현 (p.220-225)

다층 퍼셉트론(Multilayer perceptron, MLP)을 PyTorch로 직접 학습시키는 실습 예고.

| 용어 | 정의 (p.222-224) |
|---|---|
| 순전파(forwardpropagation) | 입력값을 각 층의 가중치와 연산하고 활성함수를 통과시킨 후, 실제 정답과 오차를 계산하는 것 |
| 역전파(backpropagation) | 순전파의 역순으로, 오차를 활용해 각 층의 가중치와 편향을 최적화하는 것 |
| 매직 메서드 | 파이썬 객체에 기본 존재하는 함수. `__`로 감싸져 있고 인터프리터가 자동 호출 (`__init__` 등) |
| `__call__` | 객체에 `()`가 붙었을 때 호출되는 매직 메서드 |
| MLP | 입력층, 하나 이상의 은닉층, 출력층으로 구성된 순방향 인공 신경망 |
| PyTorch | Python용 오픈소스 머신러닝 라이브러리 |
| `torch.nn.Module` | 모든 신경망의 기본 클래스. `forward` 메서드를 오버라이드하면 `__call__`에서 호출됨 |

학습/검증 루프의 주요 로직 (p.225):

1. 기울기 초기화
2. 예측 값 만들기
3. 오차 계산
4. 미분 계산 후 역전파 수행
5. 가중치 업데이트

**EarlyStopping** = 학습 반복 도중 적절한 시기에 중단하는 기법. 과적합을 방지하고 새로운 데이터에 좋은 성능을 내기 위함.

---

## 정리 체크리스트

- [ ] 단순선형회귀 모형 $Y=\beta_0+\beta_1X+\epsilon$의 각 기호와 hat 표기의 의미를 설명할 수 있다
- [ ] 잔차와 RSS를 정의하고, $\hat\beta_1, \hat\beta_0$의 닫힌 해 공식을 쓸 수 있다
- [ ] 회귀 결과표에서 t-statistic·p-value로 유의성을, $R^2$로 적합도를 해석할 수 있다
- [ ] 정규방정식 $\hat{\boldsymbol\beta}=(\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top\mathbf y$의 유도 흐름(RSS 미분=0)을 말할 수 있다
- [ ] 다중공선성이 계수 추정에 미치는 영향과 상관≠인과를 예시로 설명할 수 있다
- [ ] 분류에 선형회귀가 부적합한 이유 2가지(확률 범위 이탈, 정수 코딩의 인위적 순서)를 말할 수 있다
- [ ] 시그모이드에 선형식을 대입해 로지스틱 모형식을 만들고, 로짓 변환으로 선형 모형을 복원할 수 있다
- [ ] 우도·log-likelihood·MLE의 관계와 log를 취해도 되는 이유(단조증가)를 설명할 수 있다
- [ ] 로지스틱 회귀 계수를 "로짓이 얼마 증가"로 해석하고 예측 확률을 계산할 수 있다
- [ ] Shallow 네트워크 식에서 hidden unit·활성화 함수·조각별 선형 함수가 만들어지는 과정을 그릴 수 있다
- [ ] 보편적 근사 정리를 한 문장으로 말할 수 있다
- [ ] Deep 네트워크가 "접기"로 표현력을 얻는 직관과 Shallow 대비 조각 수 차이(19개/최대 7 vs 20개/최대 16)를 설명할 수 있다
- [ ] $\mathbf h_k = \mathrm a[\boldsymbol\beta_{k-1}+\boldsymbol\Omega_{k-1}\mathbf h_{k-1}]$ 표기에서 각 $\boldsymbol\Omega_k$의 차원을 결정할 수 있다
- [ ] 손실함수, $\hat{\boldsymbol\phi}=\mathrm{argmin}\,L[\boldsymbol\phi]$, 경사하강 업데이트 식을 쓸 수 있다
- [ ] GD와 SGD의 업데이트 식 차이($\sum_{i=1}^I$ vs $\sum_{i\in\mathcal B_t}$)와 SGD의 장단점을 설명할 수 있다
- [ ] 역전파 4단계와 $\partial\ell_i/\partial\boldsymbol\Omega_k = (\partial\ell_i/\partial\mathbf f_k)\mathbf h_k^\top$를 유도 흐름과 함께 말할 수 있다

## 복습 문제

**1.** 단순선형회귀에서 잔차 $e_i$와 RSS를 정의하고, 최소제곱법의 닫힌 해를 쓰시오.

<details><summary>답</summary>

$e_i = y_i - \hat y_i$ (단, $\hat y_i = \hat\beta_0 + \hat\beta_1 x_i$), $\mathrm{RSS} = \sum_{i=1}^n e_i^2$.

$$\hat\beta_1 = \frac{\sum_i (x_i-\bar x)(y_i-\bar y)}{\sum_i (x_i-\bar x)^2}, \qquad \hat\beta_0 = \bar y - \hat\beta_1\bar x$$

</details>

**2.** 광고 데이터 다중회귀에서 newspaper의 p-value는 0.8599였다. 어떻게 해석하는가?

<details><summary>답</summary>

p-value > 0.05이므로 **통계적으로 유의하지 않다** — TV·radio를 함께 고려하면 신문 광고비는 매출과 관계가 거의 없다. (단순회귀에서 유의해 보여도 다중회귀에서 사라질 수 있다.)

</details>

**3.** 다중선형회귀 계수의 정규방정식 해를 행렬로 쓰고, 유도의 핵심 아이디어를 한 줄로 쓰시오.

<details><summary>답</summary>

$\hat{\boldsymbol\beta} = (\mathbf X^\top\mathbf X)^{-1}\mathbf X^\top\mathbf y$. RSS를 $\hat{\boldsymbol\beta}$로 미분해 $-2\mathbf X^\top\mathbf y + 2\mathbf X^\top\mathbf X\hat{\boldsymbol\beta} = \mathbf 0$ (기울기=0인 지점)을 푼 것.

</details>

**4.** 분류 문제에서 선형회귀를 쓰면 안 되는 이유를 이진/다중 분류 각각에 대해 쓰시오.

<details><summary>답</summary>

- 이진: 예측값이 확률 범위(0~1)를 벗어날 수 있어 확률로 부적절
- 다중: 정수 코딩 시 인위적 순서와 범주 간 거리 동일성(1↔2 = 2↔3)을 암묵적으로 가정하게 됨

</details>

**5.** 로지스틱 회귀 모형식을 쓰고, 로짓 변환하면 무엇이 되는지 쓰시오.

<details><summary>답</summary>

$p(X;\boldsymbol\beta) = \dfrac{e^{\beta_0+\beta_1x}}{1+e^{\beta_0+\beta_1x}}$, 로짓 변환하면 $\log\dfrac{p}{1-p} = \beta_0+\beta_1x$ — 선형 모형이 된다. (로지스틱 모형 = 선형 모형 + 시그모이드)

</details>

**6.** 로지스틱 회귀의 모수 추정에 MSE 대신 무엇을 쓰며, log를 취해도 되는 이유는?

<details><summary>답</summary>

**우도(likelihood)**를 최대화하는 MLE를 쓴다. log는 단조 증가 함수라 log-likelihood를 최대화하는 $\beta$와 likelihood를 최대화하는 $\beta$가 같다. 곱을 합으로 바꿔 미분이 쉬워진다.

</details>

**7.** Balance 계수가 0.0055일 때 "Balance가 1 증가하면"의 올바른 해석은?

<details><summary>답</summary>

연체 **확률**이 0.0055 증가하는 것이 아니라 **로짓(log-odds)이 0.0055 증가**한다. 확률 변화는 비선형이다 ($X$ 1000→2000에서 확률 0.006→0.586, 97배).

</details>

**8.** Shallow 네트워크 $y = \phi_0 + \sum_{d=1}^3\phi_d\,\mathrm a[\theta_{d0}+\theta_{d1}x]$ (ReLU)가 조각별 선형 함수가 되는 과정을 단계로 쓰시오.

<details><summary>답</summary>

① 선형변환으로 직선 3개 생성 → ② ReLU가 각 직선의 음수 부분을 0으로 잘라 꺾임 생성 → ③ $\phi_d$ 상수배로 기울기·부호 조정 → ④ $\phi_0$과 합산해 꺾인 점 최대 3개짜리 조각별 선형 함수 완성.

</details>

**9.** 파라미터 19개 Shallow(6 hidden units)와 20개 Deep(3+3, 2층)의 조각적 선형 구역 최대 개수는 각각 몇 개인가?

<details><summary>답</summary>

Shallow **최대 7개**, Deep **최대 16개**. 비슷한 파라미터 수로 Deep의 표현력이 높다 — 첫 층이 입력을 "접고" 둘째 층의 패턴이 조각마다 복제되기 때문.

</details>

**10.** 경사하강법의 두 단계와 업데이트 식, 그리고 $\alpha$의 역할을 쓰시오.

<details><summary>답</summary>

① 손실함수의 기울기(편미분 벡터) 계산 → ② 기울기 **반대 방향**으로 파라미터 업데이트: $\boldsymbol\phi \leftarrow \boldsymbol\phi - \alpha\,\partial L/\partial\boldsymbol\phi$. $\alpha$는 학습률로 한 스텝의 이동 크기 결정 (크면 발산, 작으면 느림).

</details>

**11.** 역전파에서 $\partial\ell_i/\partial\mathbf f_1$을 연쇄법칙으로 전개하고, $\boldsymbol\Omega_1$과 $\boldsymbol\beta_1$에 대한 미분을 쓰시오.

<details><summary>답</summary>

$$\frac{\partial\ell_i}{\partial\mathbf f_1} = \frac{\partial\mathbf h_2}{\partial\mathbf f_1}\frac{\partial\mathbf f_2}{\partial\mathbf h_2}\frac{\partial\ell_i}{\partial\mathbf f_2}, \qquad \frac{\partial\ell_i}{\partial\boldsymbol\Omega_1} = \frac{\partial\ell_i}{\partial\mathbf f_1}\mathbf h_1^\top, \qquad \frac{\partial\ell_i}{\partial\boldsymbol\beta_1} = \frac{\partial\ell_i}{\partial\mathbf f_1}$$

출력 쪽에서 계산한 $\partial\ell_i/\partial\mathbf f_2$가 재사용된다 — 손실에서 입력 방향으로 거꾸로 전파.

</details>

---

> **읽은 방식 메모** — 전 225페이지를 2200px OCR로 읽고, 수식·표가 있는 51개 페이지(p.13-18, 24-26, 49-52, 54, 57-59, 77-80, 82, 87, 90, 94-95, 101, 116, 126-128, 130-131, 133, 144, 146-149, 154-156, 164, 171-173, 184, 190, 195, 199, 201, 207-208)는 1600px 이미지로 재렌더링해 육안 대조했다. 본문의 모든 수식과 표 값은 이미지 기준. 한국어 설명문은 OCR을 문맥으로 복원했다. 원본 슬라이드의 오류는 각 위치에 인용 블록으로 표시했다: p.16-18 "4.72만원"(계산상 4.75만원), p.49 출력 범위 "$0\le y\le1$"(정답은 개구간), p.133 $\psi_{21}h_2$($h_1$이어야 함). 그 밖의 단순 오타 — p.22 "newpaper", p.53·65 "Mean Squired Error", p.152·155 "최소하하는", p.171 "Gredient descent", p.188 "Stochastic Gradient Decsent" — 는 노트에서 바로잡아 적었다. 빌드업 반복 슬라이드(p.105-125의 층별 출력 약 20장, p.158-162·173-183·200-208의 경사하강·역전파 단계별 반복)는 완성본 기준으로 압축했다.
