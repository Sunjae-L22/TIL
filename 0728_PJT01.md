# Python을 활용한 데이터 수집 — requests, CSV, MySQL 적재

> 출처: `16기_데이터트랙_0728_관통PJT01.pdf` (총 60페이지) · 정리일: 2026-07-28

## 한눈에 보기

- 외부 API에 HTTP 요청을 보내고 응답을 파이썬 자료구조로 받을 수 있다 (`requests`)
- 받은 원본 데이터에서 **필요한 필드만 골라내는 전처리**를 직접 짤 수 있다
- 두 개의 엔드포인트에서 받은 데이터를 키 값으로 **연결(조인)** 할 수 있다
- 전처리 결과를 CSV 파일로 저장하고, 인코딩·줄바꿈 문제를 피할 수 있다
- CSV를 MySQL 테이블에 import해서 **수집 → 가공 → 적재** 파이프라인을 완성할 수 있다

## 목차

1. [관통 프로젝트란](#1-관통-프로젝트란) (p.5-7)
2. [requests로 HTTP 요청 보내기](#2-requests로-http-요청-보내기) (p.8-13)
3. [데이터 전처리](#3-데이터-전처리) (p.14-18)
4. [requests 참고 사항과 pprint](#4-requests-참고-사항과-pprint) (p.19-22)
5. [CSV 파일 형식](#5-csv-파일-형식) (p.25-32)
6. [open 함수와 with 문](#6-open-함수와-with-문) (p.33-37)
7. [csv 모듈로 읽고 쓰기](#7-csv-모듈로-읽고-쓰기) (p.38-48)
8. [MySQL에 CSV import](#8-mysql에-csv-import) (p.49-53)
9. [01 PJT 도전 과제](#9-01-pjt-도전-과제) (p.57-59)

---

## 1. 관통 프로젝트란

### 정의와 목표 (p.6-7)

관통 프로젝트는 **정규 커리큘럼에서 학습한 내용과 추가 기술을 모두 포함한 소규모 프로젝트**다. 즉 배운 것을 확인하는 시험이 아니라, 배운 것 위에 새 기술을 하나씩 얹어 보는 자리다.

목표는 세 가지다.

| 목표 | 의미 |
|---|---|
| 1학기 마지막 월말평가 준비 | 최종 관통 프로젝트로 이어지는 사전 연습 |
| 개인 포트폴리오 | 결과물이 그대로 남는다 |
| 협업 과정 학습 | 코드보다 과정을 익히는 쪽에 무게 |

> **왜 이 흐름을 배우는가** — 이번 자료의 최종 목적지는 p.59의 과제, 즉 "영화 데이터를 수집해서 MySQL에 넣기"다. 앞의 모든 내용(requests → 전처리 → CSV → import)이 그 한 줄을 위한 부품이라고 보면 흐름이 잡힌다.

---

## 2. requests로 HTTP 요청 보내기

### Requests란 (p.9-10)

`Requests`는 **간결한 HTTP 라이브러리**다. 파이썬 표준 라이브러리가 아니므로 따로 설치해야 한다.

```bash
$ pip install requests
```

```python
# 01_requests.py

import requests
```

### 응답 받기 — 3단계로 좁혀가기 (p.11-13)

같은 URL에 대해 코드를 조금씩 바꿔가며 **원하는 형태까지 좁혀가는** 흐름이다. 이 세 단계가 이후 모든 수집 코드의 기본형이 된다.

**① 요청을 보내고 응답 객체를 확인한다**

```python
import requests

response = requests.get('https://jsonplaceholder.typicode.com/todos/')
print(response)
```

```text
$ python 01_requests.py
<Response [200]>
```

`200`은 정상적으로 응답을 받았다는 status code다. 여기서 출력되는 것은 데이터가 아니라 **응답 객체 자체**라는 점이 중요하다.

**② `.json()`으로 응답 데이터만 꺼낸다**

```python
import requests

response = requests.get('https://jsonplaceholder.typicode.com/todos/').json()
print(response)
```

```text
[{'userId': 1, 'id': 1, 'title': 'delectus aut autem', 'completed': False},
 {'userId': 1, 'id': 2, 'title': 'quis ut nam facilis et officia qui', 'completed': False},
 {'userId': 1, 'id': 3, 'title': 'fugiat veniam minus', 'completed': False},
 {'userId': 1, 'id': 4, 'title': 'et porro tempora', 'completed': True},
 …]
```

`.json()`을 붙이는 순간 응답 객체가 **파이썬 리스트/딕셔너리로 변환**된다. 이제부터는 평범한 자료구조 다루기다.

> **주의** — `.json()`은 `requests.get(...)` 뒤에 바로 붙는다. `print(response.json())`처럼 나중에 호출해도 되지만, 이후 코드에서 계속 쓸 거라면 받는 시점에 붙여두는 편이 깔끔하다. 응답이 JSON이 아닐 때 `.json()`을 호출하면 에러가 난다.

**③ 필요한 것만 거른다**

```python
import requests

response = requests.get('https://jsonplaceholder.typicode.com/todos/').json()

for item in response:
    if item['completed']:
        print(item)
```

```text
{'userId': 1, 'id': 4, 'title': 'et porro tempora', 'completed': True}
{'userId': 1, 'id': 8, 'title': 'quo adipisci enim quam ut ab', 'completed': True}
{'userId': 1, 'id': 11, 'title': 'vero rerum temporibus dolor', 'completed': True}
```

`item['completed']`가 `True`인 것만 남긴다. 조건식을 `== True`로 쓰지 않고 값 자체를 조건으로 쓰는 것이 파이썬 관례다.

---

## 3. 데이터 전처리

### 왜 전처리가 필요한가 (p.14)

API가 주는 데이터가 곧 내가 필요한 데이터인 경우는 거의 없다. `jsonplaceholder`가 주는 todo 하나에는 4개 필드가 있지만, 내 서비스에 `id`와 `title`만 필요하다면 나머지는 버려야 한다.

```text
{                                    {
  'userId': 1,                         'id': 4,
  'id': 4,                    →        'title': 'et porro tempora',
  'title': 'et porro tempora',       }
  'completed': True
}
```

### 필요한 값만 추출하기 (p.15)

```python
response = requests.get('https://jsonplaceholder.typicode.com/todos/').json()
completed_items = []
fields = ['id', 'title']
for item in response:
    if item['completed']:
        temp_item = {}
        for key in fields:
            temp_item[key] = item[key]
        completed_items.append(temp_item)

print(completed_items)
```

```text
$ python 01_requests.py
[{'id': 4, 'title': 'et porro tempora'}, {'id': 8, 'title': 'quo adipisci enim quam ut ab'},
 {'id': 10, 'title': 'illo est ratione doloremque quia maiores aut'}, …]
```

구조를 뜯어보면 이중 반복이다.

- 바깥 루프: 전체 todo를 돌면서 `completed`인 것만 통과
- 안쪽 루프: 통과한 항목에서 `fields`에 적힌 키만 새 딕셔너리에 복사

**`fields` 리스트를 따로 빼둔 것이 핵심**이다. 나중에 필요한 필드가 바뀌어도 이 한 줄만 고치면 된다.

> **주의** — `temp_item = {}`은 반드시 `if` 블록 **안**에 있어야 한다. 밖에 두면 모든 항목이 같은 딕셔너리를 공유해서 마지막 값만 남는다. 참조 타입이 복사되지 않는다는 것과 같은 이야기다.

### 다른 리소스와 연결하기 (p.16-18)

`jsonplaceholder`는 6종의 리소스를 제공한다.

| 엔드포인트 | 개수 |
|---|---|
| `/posts` | 100 posts |
| `/comments` | 500 comments |
| `/albums` | 100 albums |
| `/photos` | 5000 photos |
| `/todos` | 200 todos |
| `/users` | 10 users |

todo에 **작성자 정보를 붙이고 싶다**면 두 리소스를 연결해야 한다.

```text
todos 제공 데이터                users 제공 데이터
{                              {
  'userId': 1,   ─────────┐      'id': 1,        ←──┘ 일치
  'id': 4,                └───→   'name': 'Leanne Graham',
  'title': 'et porro tempora',    'username': 'Bret',
  'completed': True               'email': 'Sincere@april.biz', …
}                              }
```

연결 규칙은 두 줄이다.

- **todo의 `userId`와 user의 `id` 값이 일치하는 경우**를 찾는다
- user 정보를 저장할 **새로운 key**에 필요 데이터만 삽입한다

결과는 이런 중첩 구조가 된다.

```python
[
  {'id': 4,
   'title': 'et porro tempora',
   'user': {'id': 1,
            'email': 'Sincere@april.biz',
            'name': 'Leanne Graham',
            'username': 'Bret'}
  },
  …
]
```

> **강사가 던진 질문** — "필요한 user 정보를 얻기 위해 **몇 번의 요청이 필요할까?**"
>
> todo마다 `/users/{id}`를 호출하면 todo 개수만큼 요청이 나간다. 하지만 users는 전부 합쳐 10명뿐이다. **`/users`를 한 번만 호출해 `{id: user}` 형태의 딕셔너리로 만들어 두고 조회**하면 요청은 2번(todos 1 + users 1)으로 끝난다. 이 판단이 과제의 채점 포인트가 될 가능성이 높다.

---

## 4. requests 참고 사항과 pprint

### requests가 제공하는 것 (p.20)

`requests`의 모든 기능은 **7개 메서드**로 접근할 수 있고, 전부 `Response` 객체를 반환한다.

```python
requests.request(method, url, **kwargs)
```

| 인자 | 설명 |
|---|---|
| `method` | `GET`, `OPTIONS`, `HEAD`, `POST`, `PUT`, `PATCH`, `DELETE` |
| `url` | 요청을 보낼 URL |
| `params` | (optional) query string으로 보낼 딕셔너리, 튜플 리스트, 바이트 |
| `data` | (optional) 요청 body에 담을 딕셔너리, 튜플 리스트, 바이트, 파일 객체 |

지금은 `get`만 쓰지만, 데이터를 보내야 하는 순간이 오면 `post`와 `data`를 찾게 된다. **`params`는 URL 뒤 `?key=value`를, `data`는 요청 본문을 담당한다**는 구분만 잡아두면 된다.

### pprint — 읽을 수 있게 출력하기 (p.21-22)

`pprint` 모듈은 파이썬 데이터 구조를 **인터프리터의 입력으로 사용할 수 있는 형태**로 "예쁘게 인쇄"해 준다.

```python
import requests
from pprint import pprint

response = requests.get('https://jsonplaceholder.typicode.com/todos/').json()

completed_items = []
fields = ['id', 'title']
for item in response:
    …

pprint(completed_items)
```

`print`와 `pprint`의 차이는 출력 결과에서 바로 드러난다.

| | `print` | `pprint` |
|---|---|---|
| 줄바꿈 | 없음 — 한 줄로 쭉 이어짐 | 구조에 맞춰 들여쓰기 |
| 딕셔너리 키 순서 | 삽입 순서 그대로 | **키로 정렬되어 출력** |
| 용도 | 로그, 단순 확인 | 중첩 구조 눈으로 검증 |

```text
print  출력 → [{'id': 4, 'title': 'et porro tempora', 'user': {'id': 1, 'name': …}}, {'id': 8, …
pprint 출력 → [
                {'id': 4,
                 'title': 'et porro tempora',
                 'user': {'email': 'Sincere@april.biz',
                          'id': 1,
                          'name': 'Leanne Graham',
                          'username': 'Bret'}
                },
                …
              ]
```

> **주의** — pprint는 **키를 정렬해서 보여줄 뿐, 원본 딕셔너리의 순서를 바꾸지 않는다.** 위 예에서 `email`이 `id`보다 먼저 나오는 것은 출력 형식일 뿐이다. 중첩 구조를 확인할 때만 쓰고, 저장하는 데이터의 순서 판단 근거로 삼지 않는다.

---

## 5. CSV 파일 형식

### 정의 (p.25)

**CSV(comma-separated values)** — 몇 가지 필드를 **쉼표(,)로 구분한 텍스트 데이터 및 텍스트 파일**.

특징은 세 가지다.

1. **단순한 형식** — 사람이 읽기 쉽고 작성하기 쉽다
2. **호환성** — 거의 모든 스프레드시트 프로그램(예: Excel) 및 데이터베이스 시스템에서 지원한다
3. **텍스트 기반** — 텍스트 편집기로 쉽게 편집할 수 있다

2번이 이번 자료에서 CSV를 쓰는 이유다. **파이썬이 만든 파일을 MySQL이 그대로 읽을 수 있는** 공통 포맷이기 때문이다.

### 같은 데이터, 두 가지 모습 (p.27-28)

| 이름 | 나이 | 직업 |
|---|---|---|
| 홍길동 | 30 | 개발자 |
| 김철수 | 25 | 디자이너 |
| 이영희 | 28 | 기획자 |

이 표가 `users.csv` 파일에서는 이렇게 생겼다.

```text
이름,나이,직업
홍길동,30,개발자
김철수,25,디자이너
이영희,28,기획자
```

첫 줄이 **헤더(열 이름)**, 나머지가 데이터 행이다. VSCode에서 열면 하단 상태바에 `UTF-8`, `CRLF`가 표시된다 — 이 두 값이 뒤에서 문제를 일으키는 지점이다.

### Excel에서 열면 한글이 깨진다 (p.29-32)

UTF-8로 저장된 CSV를 Excel에서 더블클릭으로 열면 한글이 `?대짢`, `?샙쎳` 같은 글자로 깨진다. Excel이 파일 인코딩을 UTF-8로 추측하지 못하기 때문이다.

해결은 **더블클릭 대신 가져오기**다.

```text
데이터 → 데이터 가져오기 → 파일에서 → 텍스트/CSV에서
```

가져오기 창에서 세 가지를 지정한다.

| 설정 항목 | 값 |
|---|---|
| 파일 원본 | `65001: 유니코드(UTF-8)` |
| 구분 기호 | `쉼표` |
| 데이터 형식 검색 | `첫 200개 행 기준` |

> **주의** — 데이터가 깨져 보인다고 파일이 잘못 만들어진 게 아니다. **파일은 멀쩡하고 읽는 쪽 설정이 틀린 것**이다. 파이썬으로 다시 읽어보면 정상으로 나온다. 인코딩 문제를 만나면 항상 "누가 어떤 인코딩으로 읽고 있는가"를 먼저 확인한다.

---

## 6. open 함수와 with 문

### open 함수 (p.34)

파일을 열고 **파일 객체를 반환**하는 함수다.

```python
open(filename, mode)
```

| 인자 | 설명 |
|---|---|
| `filename` | 열고자 하는 파일의 이름과 경로 |
| `mode` | 파일을 여는 모드 (읽기 `'r'`, 쓰기 `'w'` 등) |
| `encoding` | 파일을 읽고 쓸 때 사용할 문자 인코딩 |

### with 문 (p.35-37)

**리소스를 자동으로 해제하는 문법**이다. 파일 작업, 네트워크 연결, 쓰레드 락 등에 쓰이고, 코드의 간결성과 가독성을 높인다.

```python
with 표현식 as 변수:   # with 문을 시작하고, 표현식의 결과를 변수에 할당
    코드 블록          # with 블록 내부에서 실행할 코드
# with문 종료 시 리소스 해제
```

파일 읽기에 적용하면 이렇게 된다.

```python
with open('example.txt', 'r') as file:  # 'example.txt' 파일을 읽기 모드로 열기
    content = file.read()               # 파일 내용 읽기
    print(content)                      # 파일 내용 출력
# with 블록이 끝나면 파일이 자동으로 닫힘
```

```text
$ python 02_with_statement.py
예제 샘플
```

`file.close()`를 직접 호출하지 않아도 된다는 게 요점이다. 블록 안에서 에러가 나도 파일은 닫힌다. **파일을 열 때는 `with`를 기본형으로 쓴다**고 외워두면 된다.

---

## 7. csv 모듈로 읽고 쓰기

### 읽기 — csv.reader (p.39)

```python
import csv

# CSV 파일 읽기
with open('users.csv', 'r', encoding='utf-8') as file:
    csv_reader = csv.reader(file)
    for row in csv_reader:
        print(row)
```

```text
$ python 03_csv_read.py
['이름', '나이', '직업']
['홍길동', '30', '개발자']
['김철수', '25', '디자이너']
['이영희', '28', '기획자']
```

각 행이 **리스트**로 나온다. 헤더도 데이터와 똑같이 한 행으로 취급된다.

> **주의** — 나이가 `30`이 아니라 `'30'`이다. **CSV에서 읽은 값은 전부 문자열**이다. 숫자로 계산하려면 `int()`로 직접 변환해야 한다. CSV 파일 자체에 타입 정보가 없기 때문이다.

### 읽기 — csv.DictReader (p.40)

각 행을 **딕셔너리로 변환**해서 읽는다. **첫 번째 행을 열 이름으로 사용**한다.

```python
# CSV 파일 읽기 (DictReader)
with open('users.csv', 'r', encoding='utf-8') as file:
    csv_reader = csv.DictReader(file)
    for row in csv_reader:
        print(row)
```

```text
$ python 03_csv_read.py
{'이름': '홍길동', '나이': '30', '직업': '개발자'}
{'이름': '김철수', '나이': '25', '직업': '디자이너'}
{'이름': '이영희', '나이': '28', '직업': '기획자'}
```

`reader`와 비교하면 차이가 분명하다.

| | `csv.reader` | `csv.DictReader` |
|---|---|---|
| 행의 형태 | 리스트 | 딕셔너리 |
| 접근 방법 | `row[0]` — 인덱스 | `row['이름']` — 열 이름 |
| 헤더 | 데이터 행으로 함께 나옴 | 열 이름으로 소비되어 나오지 않음 |
| 열 순서가 바뀌면 | **코드가 깨진다** | 영향 없음 |

열 순서에 의존하지 않는 `DictReader` 쪽이 안전하다.

### newline 매개변수 (p.42)

`open` 함수의 매개변수로, **파일에서 새로운 줄을 처리하는 방식**을 지정한다.

| 값 | 동작 |
|---|---|
| `None` (기본값) | 모든 줄바꿈 문자(`\n`, `\r\n`, `\r`)를 기본 줄바꿈 문자로 변환 |
| `''` | 줄바꿈 문자를 변환하지 않음 (raw 모드) |
| `'\n'` | 줄바꿈 문자를 LF (Line Feed)로 변환 |
| `'\r\n'` | 줄바꿈 문자를 CRLF (Carriage Return + Line Feed)로 변환 |
| `'\r'` | 줄바꿈 문자를 CR (Carriage Return)로 변환 |

**다양한 운영체제 간의 줄바꿈 문자 차이를 처리**하기 위한 인자다.

### 쓰기 — csv.writer (p.43-44)

**newline을 지정하지 않은 경우:**

```python
with open('data.csv', 'w', encoding='utf-8') as file:
    csv_writer = csv.writer(file)
    csv_writer.writerow(['이름', '나이', '직업'])
    csv_writer.writerow(['홍길동', 30, '개발자'])
    csv_writer.writerow(['김철수', 25, '디자이너'])
    csv_writer.writerow(['이영희', 28, '기획자'])
```

```text
이름,나이,직업
                    ← 빈 줄
홍길동,30,개발자
                    ← 빈 줄
김철수,25,디자이너
                    ← 빈 줄
이영희,28,기획자
```

**`newline=''`을 지정한 경우:**

```python
with open('data.csv', 'w', newline='', encoding='utf-8') as file:
    csv_writer = csv.writer(file)
    csv_writer.writerow(['이름', '나이', '직업'])
    csv_writer.writerow(['홍길동', 30, '개발자'])
    csv_writer.writerow(['김철수', 25, '디자이너'])
    csv_writer.writerow(['이영희', 28, '기획자'])
```

```text
이름,나이,직업
홍길동,30,개발자
김철수,25,디자이너
이영희,28,기획자
```

> **왜 빈 줄이 생기는가** — `csv.writer`는 각 행 끝에 이미 `\r\n`을 붙인다. 그런데 `newline`이 기본값이면 `open`이 그 `\n`을 다시 운영체제 기본 줄바꿈으로 변환하면서 줄바꿈이 한 번 더 들어간다. `newline=''`은 이 재변환을 끄는 스위치다.
>
> **CSV를 쓸 때 `newline=''`은 선택이 아니라 필수**로 외운다. Windows에서만 나타나는 증상이라 macOS/Linux에서 개발하면 모르고 넘어갔다가 나중에 터진다.

### 쓰기 — csv.DictWriter (p.45)

`fieldnames` 인자로 **열 이름을 지정**할 수 있다.

```python
with open('data.csv', 'w', newline='', encoding='utf-8') as file:
    fieldnames = ['이름', '나이', '직업']
    csv_writer = csv.DictWriter(file, fieldnames=fieldnames)

    csv_writer.writeheader()
    csv_writer.writerow({'이름': '홍길동', '나이': 30, '직업': '개발자'})
    csv_writer.writerow({'이름': '김철수', '나이': 25, '직업': '디자이너'})
    csv_writer.writerow({'이름': '이영희', '나이': 28, '직업': '기획자'})
```

```text
이름,나이,직업
홍길동,30,개발자
김철수,25,디자이너
이영희,28,기획자
```

**`writeheader()` 메서드 (p.47)**

- `fieldnames`에 지정된 대로 **CSV 파일의 첫 번째 행에 헤더가 작성**된다
- CSV 파일의 헤더를 정의한다
- 데이터가 어떤 필드에 매핑되는지 명확히 하는 데 도움이 된다

> **주의** — `writerow`에 넘기는 딕셔너리의 키가 `fieldnames`와 다르면 `ValueError`가 난다. 헤더 문자열 하나만 오타 나도 전체가 멈추므로, `fieldnames`를 변수로 빼두고 그 변수를 참조하는 습관이 안전하다.

### 전처리 결과를 CSV로 내보내기 (p.48)

앞의 `01_requests.py`에서 만든 `completed_items`를 그대로 파일로 떨어뜨린다. **수집 → 전처리 → 저장이 하나로 이어지는 지점**이다.

```python
with open('completed_todos.csv', 'w', newline='') as csv_file:
    fieldnames = ['id', 'title']
    writer = csv.DictWriter(csv_file, fieldnames=fieldnames)

    writer.writeheader()
    for item in completed_items:
        writer.writerow({'id': item['id'], 'title': item['title']})
```

```text
id,title
4,et porro tempora
8,quo adipisci enim quam ut ab
10,illo est ratione doloremque quia maiores aut
…
```

> **원본 슬라이드 보완** — 이 코드에는 `encoding='utf-8'`이 빠져 있다. 데이터가 영문뿐이라 동작하지만, **한글이 들어가는 순간 OS 기본 인코딩에 따라 깨진다.** 과제(영화 데이터 = 한글 제목)에서는 반드시 붙인다.
>
> ```python
> with open('completed_todos.csv', 'w', newline='', encoding='utf-8') as csv_file:
> ```

---

## 8. MySQL에 CSV import

### 테이블 먼저 만든다 (p.50)

CSV를 넣을 그릇이 있어야 한다. 순서는 **DB 생성 → 테이블 생성 → import**다.

1. `pjt01` DB 생성
2. `todos` table 생성

| 컬럼 | 타입 |
|---|---|
| `id` | `int` |
| `title` | `varchar(150)` |

```sql
CREATE DATABASE pjt01;

USE pjt01;

CREATE TABLE todos (
    id INT,
    title VARCHAR(150)
);
```

<!-- p.50 슬라이드는 GUI 화면 캡처만 있고 CREATE 구문 자체는 없음. 위 SQL은 캡처된 컬럼 정의(id int, title varchar(150))에 맞춰 재구성한 것 -->

만들고 나면 `SELECT * FROM todos LIMIT 100`의 결과가 `Total 0`이다. 아직 비어 있는 게 정상이다.

### GUI로 import하기 (p.51-53)

DB 클라이언트(슬라이드는 DBeaver 계열)에서 4단계로 끝난다.

1. **file import 버튼 클릭** — 결과 그리드 상단 툴바의 `+` 아이콘
2. **지원 파일 타입 확인 후 Choose 선택** — `Import External Data` 창에 `CSV`, `XLSX`, `JSON`이 표시된다
3. **삽입할 파일 선택 후 삽입** — 앞에서 만든 `completed_todos.csv`
4. **결과 확인**

| id | title |
|---|---|
| 4 | et porro tempora |
| 8 | quo adipisci enim quam ut ab |
| 10 | illo est ratione doloremque quia maiores aut |
| 11 | vero rerum temporibus dolor |
| 12 | ipsa repellendus fugit nisi |

> **주의** — import가 성공하려면 **CSV 헤더와 테이블 컬럼명이 맞아야 한다.** `writeheader()`로 `id,title`을 써둔 것이 여기서 값을 한다. 헤더 없이 저장하면 첫 데이터 행이 헤더로 먹히거나 매핑을 수동으로 잡아야 한다.
>
> `varchar(150)`을 넘는 title이 있으면 잘리거나 에러가 난다. 수집한 데이터의 최대 길이를 먼저 확인하고 컬럼 크기를 정한다.

### 전체 흐름 정리

```text
requests.get()        →  API에서 원본 JSON 수집
      ↓ .json()
파이썬 리스트/딕셔너리 →  필요한 필드만 추출 (전처리)
      ↓ csv.DictWriter
completed_todos.csv   →  중간 산출물 (텍스트 파일)
      ↓ GUI import
MySQL todos 테이블     →  최종 적재
```

---

## 9. 01 PJT 도전 과제

### 프로젝트 생성 및 관리 (p.57)

- 관통 프로젝트 제출 가이드
- 관통 프로젝트 `README.md` 작성 가이드

### 과제 요구사항 (p.59)

**주제: 파이썬으로 영화 데이터를 수집해 MySQL에 적재하기**

- [ ] `pjt01` DB 생성
  - [ ] 요구 조건에 따른 테이블 생성
- [ ] 파이썬으로 영화 데이터 수집 및 가공
  - [ ] 요구 조건에 따른 데이터 전처리
  - [ ] 각 테이블에 삽입 가능한 CSV 파일 export
- [ ] 데이터 삽입

> **"각 테이블에" — 복수형이다.** todos 예제는 테이블 하나였지만 과제는 테이블이 여러 개다. p.17-18의 `todos` + `users` 연결이 여기서 쓰인다. 영화 데이터라면 영화 / 장르 / 출연진처럼 나뉠 것이고, **테이블 개수만큼 CSV를 만들어야 한다**는 뜻으로 읽는 게 맞다.

### 착수 전 확인할 것

- 사용할 API의 응답 구조를 먼저 `pprint`로 찍어본다 — 필드 이름을 모르면 전처리를 못 짠다
- 요청 횟수를 줄일 수 있는지 본다 (p.18의 질문)
- 한글 데이터이므로 `encoding='utf-8'`과 `newline=''`을 처음부터 넣는다
- 테이블 컬럼 타입·길이를 실제 데이터 최대 길이에 맞춰 정한다

---

## 정리 체크리스트

- [ ] `requests.get()`의 반환값과 `.json()`을 붙인 뒤의 반환값 차이를 설명할 수 있다
- [ ] 응답 리스트에서 조건에 맞는 항목의 특정 필드만 추출하는 코드를 짤 수 있다
- [ ] 두 엔드포인트의 데이터를 키 값으로 연결할 때, 요청 횟수를 최소화하는 방법을 말할 수 있다
- [ ] `csv.reader`와 `csv.DictReader`의 차이를 말할 수 있다
- [ ] CSV를 쓸 때 `newline=''`이 필요한 이유를 설명할 수 있다
- [ ] `DictWriter`로 헤더가 있는 CSV를 만들 수 있다
- [ ] `with` 문을 쓰는 이유를 설명할 수 있다
- [ ] Excel에서 한글 CSV가 깨질 때의 해결 순서를 안다
- [ ] 수집 → 전처리 → CSV → DB 적재의 전체 흐름을 그릴 수 있다

## 복습 문제

1. `requests.get(url)`의 결과를 `print`하면 `<Response [200]>`이 나온다. 실제 데이터를 보려면 무엇이 필요한가?
   <details><summary>답</summary><code>.json()</code>을 호출해 응답 본문을 파이썬 리스트/딕셔너리로 변환한다. <code>requests.get(url).json()</code></details>

2. CSV에서 읽은 나이 값 `'30'`으로 바로 덧셈을 하면 어떻게 되는가?
   <details><summary>답</summary>문자열이라 <code>TypeError</code>가 나거나 문자열 연결이 된다. CSV에서 읽은 값은 전부 문자열이므로 <code>int()</code>로 변환해야 한다.</details>

3. `csv.writer`로 파일을 썼더니 행 사이에 빈 줄이 생겼다. 원인과 해결은?
   <details><summary>답</summary>원인 — <code>csv.writer</code>가 붙인 줄바꿈을 <code>open</code>이 한 번 더 변환한다. 해결 — <code>open(..., newline='')</code>으로 재변환을 끈다.</details>

4. `csv.DictWriter`에서 `writeheader()`를 빼먹으면 무슨 일이 생기는가?
   <details><summary>답</summary>헤더 행 없이 데이터만 저장된다. 이 파일을 DB에 import하면 첫 데이터 행이 헤더로 해석되어 한 건이 유실되거나 컬럼 매핑을 수동으로 잡아야 한다.</details>

5. todo 200건 각각에 작성자 정보를 붙여야 한다. users는 10명이다. 요청은 최소 몇 번인가?
   <details><summary>답</summary>2번. <code>/todos</code> 1번, <code>/users</code> 1번을 호출한 뒤 users를 <code>{id: user}</code> 딕셔너리로 만들어 메모리에서 조회한다. todo마다 <code>/users/{id}</code>를 호출하면 201번이 된다.</details>

6. UTF-8 CSV를 Excel에서 열었더니 한글이 깨졌다. 파일을 다시 만들어야 하는가?
   <details><summary>답</summary>아니다. 파일은 정상이고 Excel이 인코딩을 잘못 추측한 것이다. 데이터 → 데이터 가져오기 → 파일에서 → 텍스트/CSV에서로 열고 파일 원본을 <code>65001: 유니코드(UTF-8)</code>로 지정한다.</details>

---

## 원본 자료 메모

정리하면서 확인한 원본 슬라이드의 오탈자와 보완 사항이다.

| 위치 | 내용 |
|---|---|
| p.43, p.44 | 두 장 모두 제목이 "CSV 파일 쓰기 (1/2)" — 뒤쪽은 (2/2)여야 한다 |
| p.51, p.52, p.53 | 세 장 모두 제목이 "csv 파일 import (1/3)" — (2/3), (3/3)이어야 한다 |
| p.48 | `open()`에 `encoding` 인자가 빠져 있다. 한글 데이터에서는 반드시 추가 |
| p.50 | `CREATE` 구문 없이 GUI 캡처만 있어, 캡처된 컬럼 정의에 맞춰 SQL을 재구성했다 |
