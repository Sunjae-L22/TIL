# 적응적 센싱

> 출처: `5-2_AI모델활용.pdf` 중 **p.70-109 (40페이지)** · 정리일: 2026-08-26
> 페이지 표기는 PDF 물리 페이지 기준 (합본 전체 157페이지에서의 위치).
> 판독 방식: **전 페이지 이미지 직접 판독** (OCR 미사용). 판독 실패한 페이지는 **없음**.
> 원본 오류 3건은 본문에서 고쳐 쓰고 `> 📌` 로 밝혔다.
>
> **`5-2_AI모델활용.pdf` 는 강의 4개의 합본이다.** 시리즈: [01 On-device AI와 정수연산 양자화](0826_AI모델활용-01-온디바이스AI와정수연산양자화.md) · [02 테스트 타임 도메인 적응(TTA)](0826_AI모델활용-02-테스트타임도메인적응.md) · **03 적응적 센싱** · [04 도메인 전문지식을 활용한 모델 설계](0826_AI모델활용-04-도메인전문지식모델설계.md)

## 한눈에 보기

- **스케일링 법칙의 지수는 왜 절망적인가** — $-0.050$, $-0.095$, $-0.076$. 자원을 10배 늘려도 손실은 몇 퍼센트만 떨어진다.
- **발상의 전환** — 모델을 데이터에 맞추는 대신, **입력을 모델이 아는 영역으로 옮긴다.** 사람이 안경을 쓰는 것과 같다.
- **ID / C-OOD / OOD** — 분포를 나누는 두 축(공변량 이동 × 의미 이동) 중 센서로 손댈 수 있는 축은 하나뿐이다.
- **Lens** — 전수탐색이 아니라 $K$번 촬영 예산 안의 최선을 고른다.
- **파라미터 50배·데이터 200만 배 차이인데 작은 모델이 이긴다** — 적응적 센싱의 가장 강한 근거.

## 목차

1. [강의 개요와 출발 질문](#강의-개요와-출발-질문) (p.72-74)
2. [초거대 AI와 스케일링 법칙](#초거대-ai와-스케일링-법칙) (p.77-79)
3. [스케일링 법칙의 한계와 피지컬 AI](#스케일링-법칙의-한계와-피지컬-ai) (p.81-82)
4. [발상의 전환: 인간의 인지체계](#발상의-전환-인간의-인지체계) (p.85-85)
5. [적응적 센싱: AI를 위한 안경](#적응적-센싱-ai를-위한-안경) (p.88-92)
6. [적응적 센싱을 위한 데이터셋](#적응적-센싱을-위한-데이터셋) (p.94-95)
7. [적응적 센싱 기법: Lens](#적응적-센싱-기법-lens) (p.97-97)
8. [성능 효과](#성능-효과) (p.99-100)
9. [특징과 함의](#특징과-함의) (p.102-102)
10. [확인문제](#확인문제) (p.103-107)
11. [강의 정리](#강의-정리) (p.109-109)

---

## 강의 개요와 출발 질문

p.70부터 다시 새 강의 덱이다. 원 제목은 「AI 모델 활용: 적응적 센싱」이며, 목차는 다음과 같다.

```text
1. 초거대 AI의 근본 원리와 한계
   1-1. 스케일링의 법칙과 초거대 AI
   1-2. 스케일링 전략의 한계
2. 적응적 센싱을 통한 분포 이동 억제
   2-1. 발상의 전환: 인간의 인지체계
   2-2. 적응적 센싱: AI를 위한 안경
   2-3. 적응적 센싱을 위한 데이터셋
   2-4. 적응적 센싱 기법: Lens
   2-5. 성능 효과
   2-6. 특징과 함의
```

앞 덱(TTA)과 문제의식이 같다. **분포 이동을 어떻게 막을 것인가.** 다만 답이 다르다. TTA는 **모델을 데이터에 맞췄고**, 적응적 센싱은 **데이터(입력 자체)를 모델에 맞춘다**. 강의가 이 기법을 "AI를 위한 안경"이라 부르는 이유다.

### 학습 목표와 오프닝 질문 (p.72, p.74)

**학습 목표**

- 초거대 AI 패러다임의 한계점과 인간인지체계와의 괴리를 이해합니다.
- 적응적 센싱을 통한 분포 이동 억제의 개념을 이해합니다.
- 적응적 센싱의 성능 효과와 특징을 이해합니다.

**오프닝 질문 3개**

1. 초거대 AI의 근본 원리와 한계점은 무엇일까?
2. 현재 AI 학습방식과 인간인지체계의 차이점은 무엇일까?
3. 적응적 센싱과 분포 이동 억제의 개념은 무엇일까?

## 초거대 AI와 스케일링 법칙

### 초거대 AI란 무엇인가 (p.77)

**초거대 AI**

- 파라미터 규모가 매우 큰 인공지능 모델
  - 파라미터 : AI 뇌세포 개수 (파라미터가 많을수록 똑똑함)
- 대량의 데이터를 학습하여 여러 작업을 수행
- 데이터가 많고, 모델이 클수록 성능이 좋아짐 (**스케일링 법칙**)
- **분포 이동의 문제 존재**
- 분포 이동의 리스크를 줄이기 위해 **규모로 보험**을 들었음
  - 웹에서 **방대한 데이터**를 모으고,
  - 이 데이터를 추가 변형까지 시켜서 드문 경우도 흉내낼 수 있게 증강하고,
  - 그 지식을 담을 **초거대 모델**을 설계해서,
  - **고성능 대규모 컴퓨팅**으로 오래 학습

> **주의** "규모로 보험을 들었음"이 이 덱 전체의 논지 출발점이다. 초거대 AI가 커진 이유를 **성능 욕심**이 아니라 **분포 이동에 대한 방어**로 재해석하는 프레임이다. 낯선 입력을 만나지 않으려고 미리 세상의 데이터를 다 삼켜 두는 전략이며, 다음 절(1-2. 스케일링 전략의 한계)에서 이 보험료가 감당 가능한지를 따진다.

**[그림1-1] 초거대 AI 모델 발전 (p.77 차트 전사)** — 가로축 `Number of parameters`(150 M → 1 T), 세로축 연도(2017 → 2024). 점들이 우상향 곡선을 따라 늘어선다.

| 연도 | 모델 | 파라미터 | 분야 | 제공사 |
|---|---|---|---|---|
| 2017 | Transformer | 213 M | NLP | Google |
| 2018 | GPT-1 | 177 M | NLP | OpenAI |
| 2018 | BERT | 340 M | NLP | Google |
| 2019 | GPT-2 | 1.5 B | NLP | OpenAI |
| 2020 | GPT-3 | 175 B | NLP | OpenAI |
| 2021 | ChatGPT | 200 B | NLP | OpenAI |
| 2021 | PanGu-α | 200 B | NLP | HUAWEI |
| 2021 | ERNIE 3.0 Titan | 260 B | NLP | Baidu |
| 2022 | GIT | 700 M | Image-to-Text | Microsoft |
| 2022 | Dalle-2 | 35 B | Vision | OpenAI |
| 2022 | Stable Diffusion | 1.45 B | Text-to-Image | stability ai |
| 2022 | PaLM | 540 B | NLP | Google |
| 2023 | SAM | 636 M | Vision | Meta |
| 2023 | LLaMA | 650 B | NLP | Meta |
| 2023 | PaLM-E | 562 B | Image & Text | Google |
| 2023 | PanGu-Σ | 1.085 T | NLP | HUAWEI |
| 2023 | LLaMA 2 | 700 B | NLP | Meta |
| 2024 | ViT-22B | 22 B | Vision | Google |
| 2024 | Sora | 30 B | Vision | OpenAI |
| 2024 | GroK | 314 B | NLP | xAI |
| 2024 | Claude-3 | (표기 없음) | NLP | Anthropic |
| 2024 | Gemini | 1.6 T | NLP | Google |
| 2024 | GPT-4V | 1.8 T | Vision | OpenAI |

출처 : An overview of large AI models and their applications, December 2024

> 📌 원본 p.77 차트에는 실제와 다른 값이 여럿 있으나, 인용된 외부 그림이므로 위 표에는 **원문 표기 그대로** 옮겼다. 바로잡으면 이렇다. **LLaMA는 650 B가 아니라 최대 65 B**, **LLaMA 2는 700 B가 아니라 최대 70 B**(둘 다 10배 부풀려짐), **GPT-1은 177 M이 아니라 117 M**(자릿수 뒤바뀜), **Dalle-2는 35 B가 아니라 3.5 B**. 또 **ChatGPT는 2021년이 아니라 2022년 11월 공개**이며 파라미터 수는 공개된 적이 없다. 표의 숫자를 그대로 외우지 말 것.

### 분포 이동이 성능 하락의 근본 문제인 이유 (p.78)

**분포 이동(distribution shift)의 문제**

- 분포 이동(Distribution shift) : AI가 학습할 때 경험한 세상과, 실제로 쓰이는 순간의 세상이 달라지는 현상
- 이 때, AI는 낯선 문제에 맞닥뜨리고 성능이 떨어짐
- **AI 성능하락의 근본적 문제**
  - 비유: 밤새 기출문제를 외웠는데, 정작 시험이 범위 밖에서 나온다면?
  - AI는 학습하지 못한 상황 (처음 보는 상황)에 약함

**예시** — 학습 데이터는 **고양이 일러스트**, 실제 데이터는 **실사 고양이 사진**.

- 일러스트 이미지만 학습한 AI 모델은 실제 고양이 이미지를 잘 구분하지 못함

**[그림1-2] 분포 이동 그래프 (p.78 문자 전사)** — 패널 2개.

```text
[왼쪽] 분포 그래프
  train (파랑) : 중심 x≈2, 봉우리 높이 ≈1.6 으로 좁고 뾰족
  test  (빨강) : 중심 x≈1, 봉우리 높이 ≈0.8 로 낮고 넓게 퍼짐
  → 두 봉우리가 거의 겹치지 않는다

[오른쪽] 함수 근사 그래프
  target function  (검정 실선) : 참값 곡선
  learned function (초록 실선) : 모델이 배운 곡선
  train sample (파란 ★) : x≈1.5~2.5 구간에 몰려 있음
  test  sample (빨간 ▲) : x≈0~1.2 구간에 몰려 있음
  → 파란 별이 모인 구간에서는 두 곡선이 붙어 있지만,
    빨간 삼각형이 모인 구간에서는 초록 곡선이 검정 곡선에서 크게 벌어진다
```

오른쪽 패널이 왼쪽 패널의 **결과**다. 학습 샘플이 없던 영역에서 모델의 곡선이 참값에서 멀어지고, 하필 테스트 샘플이 거기에 몰려 있다. 이것이 "성능이 떨어진다"의 기하학적 실체다.

### 스케일링 법칙 (p.79)

**스케일링 법칙 (Scaling Law)**

- AI 모델의 성능이 **모델 크기(Parameters)**, **학습 데이터 양(Dataset Size)**, 그리고 **컴퓨팅 자원(Compute)** 이 증가함에 따라 향상되는 현상
- 스케일링 법칙 덕분에 "데이터 더! 모델 더 크게!" 방식은 **예측가능한 투자전략**이 됨
  - 모델 크기 ↑ + 학습 데이터 양 ↑ + 컴퓨팅 자원 ↑ → Test Loss ↓
  - Test Loss : AI 모델의 오류 정도 (숫자가 낮을수록 성능이 좋음)

**[그림1-3] Scaling Law 그래프 (p.79 전사)** — 로그-로그 축 위에서 세 곡선이 모두 **직선**으로 떨어진다. 세로축은 공통으로 Test Loss이며, 위쪽이 저성능·아래쪽이 고성능이다.

| 패널 | 가로축 | 축 범위 | Test Loss 범위 | 적합식 |
|---|---|---|---|---|
| 1 | Compute (PF-days, non-embedding) | $10^{-9}$ ~ $10^{1}$ | 7 → 2 | $L = (C_{min}/2.3 \cdot 10^{8})^{-0.050}$ |
| 2 | Dataset Size (tokens) | $10^{8}$ ~ $10^{9}$ | 4.2 → 2.7 | $L = (D/5.4 \cdot 10^{13})^{-0.095}$ |
| 3 | Parameters (non-embedding) | $10^{5}$ ~ $10^{9}$ | 5.6 → 2.4 | $L = (N/8.8 \cdot 10^{13})^{-0.076}$ |

$$L = \left(\frac{C_{min}}{2.3 \cdot 10^{8}}\right)^{-0.050} \qquad L = \left(\frac{D}{5.4 \cdot 10^{13}}\right)^{-0.095} \qquad L = \left(\frac{N}{8.8 \cdot 10^{13}}\right)^{-0.076}$$

세 지수 $-0.050$, $-0.095$, $-0.076$ 이 모두 **0에 가까운 음수**라는 점이 중요하다. 자원을 10배 늘려도 손실은 몇 퍼센트밖에 떨어지지 않는다는 뜻이고, 이것이 다음 절에서 다룰 "스케일링 전략의 한계"의 수치적 근거다.

출처 : https://www.forethought.org/research/the-scaling-paradox

**[그림1-4] Scaling Law 예시 (p.79)** — 밈 이미지 한 장. 자막 "그래서 네가 뭘 좋아할지 몰라서 다 가져와 봤어". 무엇이 나올지 모르니 전부 준비해 둔다는 태도가 곧 스케일링 전략이라는 농담이다.

## 스케일링 법칙의 한계와 피지컬 AI

### 던지는 질문: 모든 경우의 수를 미리 학습시킬 수 있는가 (p.081)

강의 전체를 관통하는 질문은 하나다.

> **이 세상에서 일어날 수 있는 모든 경우의 수를 학습 데이터로 미리 준비하는 것이 가능할까?**

이 질문이 지금 중요해진 이유는 AI가 진화하는 방향 때문이다.

- **AI의 진화: 거대 언어모델, 비전언어모델에서 피지컬 AI로**
  - 키보드로 입력받고 모니터로 보여주는 챗봇 수준을 넘어섬
  - 실제 물리 환경에서 (1) 다양한 센서를 통해 세상을 인지하고, (2) 계획하고, (3) 행동해서 세상에 영향을 주기도 하는 **현장을 사는 AI**

화면 안에서만 사는 모델은 입력 분포를 사람이 골라서 넣어줄 수 있지만, 현장을 사는 AI는 입력 분포를 스스로 통제하지 못한다. 여기서 문제가 갈린다.

- 출처: `https://www.cio.com/article/4053096/ai-is-here-physical-ai-is-coming-fast.html`

### 피지컬 AI에서 분포 이동 문제가 심화되는 이유 (p.082)

피지컬 AI로 넘어가면 **경우의 수가 폭발한다.** 슬라이드는 폭발의 축을 두 갈래로 나눈다.

- **환경 변수**: 조도, 날씨, 재질 등
- **로봇의 행동 변수**: 시점, 거리, 모션 블러 등

두 축이 곱해지므로 조합 수는 지수적으로 늘어난다. 그래서 "데이터를 더 모으면 된다"는 답이 성립하지 않는다.

- **보완책의 필요성**
  - 초거대, 초초거대, 초초초거대 AI?
  - 심지어 기술적으로 가능하다고 하더라도, 자본, 개인정보보호, 환경 등 현실의 다양한 부가적 문제 발생

> **주의** 강의가 붉은 글씨로 못박은 결론은 하나다. **→ 적응적 센싱이 필요함!** 이후 모든 내용은 이 한 줄을 푸는 과정이다.

## 발상의 전환: 인간의 인지체계

### 초거대 AI의 발상과 인간 인지체계의 괴리 (p.085-086)

슬라이드는 초거대 AI의 사고방식을 **"모든 것은 공부/노력의 문제야!"** 라는 한 문장으로 희화화한 뒤, 두 가지 예시로 그 부조리를 드러낸다.

- **예시 1)** 수능 시험 날 안경을 두고 와서 문제가 잘 안보였고, 시험을 망쳤다. 난 재수를 하면서 결심했다. 다음 시험 전까지 **흐릿한 글씨로 된 문제를 5000개** 정도 풀어야겠다. 그러면 실수로 안경을 놓고 와도 시험을 잘 볼 수 있을거야!
- **예시 2)** 어젯밤에 헤드라이트를 끄고 운전하다가 앞이 잘 안보여서 사고가 났다. 난 결심했다. 오늘부터 **어두운 사진 100만장으로 물체 식별**하는 연습을 해야지. 그럼 밤 중에도 사고가 나지 않을거야!

- **문제 제기: 이 문제 해결 방식이 상식적인가? 정작 사람은 이렇게 세상을 인지하고 있는가?**

**인간의 인지체계**는 정반대로 동작한다.

- 흐리게 보이면, **안경을 맞춤**
- 눈이 부시면, **선글라스를 맞춤**

즉 사람은 뇌를 재훈련하는 대신 **감각기관 쪽 입력을 고쳐서** 문제를 푼다. 데이터를 늘리는 것이 아니라 데이터가 들어오는 통로를 손본다.

- \[그림2-1\] 인간의 인지체계 예시: 흐린 실내 사진 → (안경 쓴 얼굴) → 선명한 실내 사진 / 먼지 낀 도로 주행 영상 → (선글라스 쓴 얼굴)

> **주의** p.085와 p.086은 같은 슬라이드의 애니메이션 빌드다. p.086에서 "인간의 인지체계"라는 라벨 박스가 붙으며 비유가 개념으로 승격된다. 이 비유가 이후 "AI를 위한 안경"이라는 표현의 근거가 된다.

## 적응적 센싱: AI를 위한 안경

### 적응적 센싱의 정의와 사람 인지체계와의 호환성 (p.088-089)

- **적응적 센싱: 환경에 맞춰서 센서(카메라)를 조절해서 좋은 데이터만 받음**

왜 이 발상이 정당한지를 슬라이드는 사람과의 대응 관계로 설명한다.

- 사람의 인지 체계는 **감각기관과 뇌의 복잡한 상호작용**으로 이루어짐
- 피지컬 AI에서 **뇌는 모델, 센서는 감각기관**: 모델에 제공되는 데이터는 모두 센서가 물리 환경을 해석한 결과물
- **뇌만 거대화하지 말고, 감각기관도 고도화 시키자!** = 주어진 환경에서 센서 제어를 통해 AI 모델이 좋아하는 데이터를 제공하자!

여기서 핵심 논점은 "모델에 들어오는 데이터는 이미 센서가 한 번 해석한 결과물"이라는 사실이다. 센서 단계에서 이미 정보가 결정되므로, 센서를 고정한 채 모델만 키우는 것은 상류를 방치하고 하류만 정화하는 것과 같다.

**\[그림2-2\] 사람의 인지체계와 호환성** (좌우 대비 다이어그램, 문자로 옮김)

```text
[인간의 인지체계]
Scene ──> Sensing ──> Acquired ──> Quality Estimation & Prediction
 (말)      (맨눈)      흐린 말 사진      "Horse? Dog?"  ← 판단 실패
   │
   └─ Low Visibility 감지
        │
        └─> Sensing(Prescriptive Glasses, 처방 안경) ──> 선명한 말 사진 ──> "Horse"  ← 판단 성공

[AI의 인지체계]
Scene ──> Sensing(카메라) ──> Acquired ──> DNN Model ──> Prediction
 (말)                          저품질 이미지            Low Quality:
                                                        Donkey 68% / Horse 25% / Zebra 7%
   │
   └─ Low Visibility 감지 → Camera Control ─(Lens)─> VisiT(Model Confidence 측정)
        │
        └─> Best Parameter Setting으로 재촬영 ──> High Quality:
                                                  Horse 89% / Donkey 7% / Zebra 4%
```

- 사람 쪽 `Prescriptive Glasses`가 AI 쪽 `Camera Control + Lens`에 정확히 대응한다.
- 사람 쪽 "눈으로 보고 흐리다고 느끼는 것"이 AI 쪽 `VisiT`의 **Model Confidence** 측정에 대응한다.
- 출처: `AI Should Sense Better, Not Just Scale Bigger: Adaptive Sensing as a Paradigm Shift`

### ID, C-OOD, OOD: 분포를 나누는 두 축 (p.090)

분포 이동을 억제하는 방법은 **모델 일반화** 방식과 **적응적 센싱** 방식 두 가지가 있다. 두 방식을 비교하려면 먼저 데이터 영역의 정의가 필요하다.

- **빨간 영역**: 모델이 학습해서 맞출 수 있는 영역
- **ID**: 학습 데이터 영역 (선명한 말 사진)
- **C-OOD**: 학습 데이터와 **의미는 같지만 외형이 다름** (선명한 말 사진 VS 흐린 말 사진)
- **OOD**: 완전히 본 적 없는 데이터 (강아지)

두 축의 의미가 이 구분을 만든다.

- 가로축 **Covariate Shift**: 외형(촬영 조건)이 얼마나 달라졌는가
- 세로축 **Semantic Shift**: 의미(클래스 자체)가 얼마나 달라졌는가

따라서 **C-OOD는 Covariate Shift만 큰 영역**이고, **OOD는 Semantic Shift가 큰 영역**이다. 센서로 손댈 수 있는 것은 촬영 조건뿐이므로, 적응적 센싱이 겨냥하는 표적은 **C-OOD 하나**다. 강아지를 말로 바꿔 찍을 수는 없다.

**\[그림2-3\] / \[그림2-4\] 두 방식 대비** (문자로 옮김)

```text
공통 좌표계:  세로축 Semantic Shift  /  가로축 Covariate Shift
  - 좌하단 파란 원      : ID     (학습 데이터)
  - 우측 분홍/보라 원   : C-OOD  (의미 같고 외형 다름)
  - 상단 초록 점선 영역 : OOD    (본 적 없는 의미)
  - 붉은 곡선           : 모델이 맞출 수 있는 영역(빨간 영역)의 경계

(B) Domain Generalization applied  = [그림2-3] 모델 일반화 방식
    붉은 화살표가 "경계선"을 바깥으로 밀어낸다.
    → 빨간 영역을 넓혀서 C-OOD를 품는다.  (모델을 바꾼다)

(C) Sensor Control applied         = [그림2-4] 적응적 센서 방식
    자홍색 화살표가 "데이터 점"을 안쪽으로 밀어넣는다.
    → 경계는 그대로 두고 C-OOD 데이터를 빨간 영역 안으로 옮긴다.  (데이터를 바꾼다)
```

> **주의** 두 그림의 차이는 **화살표가 무엇을 미느냐**다. 모델 일반화는 경계선을 밀고, 적응적 센싱은 데이터 점을 민다. 이 한 가지 차이가 비용 구조 전체를 가른다.

### 방식 1: 모델 일반화 (p.091)

- **모델 일반화: 모델 지식 영역을 확장하여 분포 이동 억제**
  - 빨간 영역(모델이 학습해서 맞출 수 있는 영역)을 **넓히는 전략**
  - 모델 지식을 확장하여 빨간 영역을 넓힘
  - 선명한 말 이미지와 흐린 말 이미지 **모두를 학습** 시킴
  - **추가 학습 비용이 높고 학습에 시간이 소요됨**

즉 앞서 나온 "흐릿한 문제 5000개 풀기"가 바로 이 방식이다. 되기는 되지만 비용이 학습 쪽에 전부 쌓인다.

### 방식 2: 적응적 센싱 (p.092)

- **적응적 센싱: 센서 제어를 통해 다양한 데이터를 모델이 아는 영역 안으로 넣어 줌으로써 분포 이동 억제**
  - 센서를 통해 **C-OOD 데이터를 빨간 영역 안으로 넣어줌**
  - 모델이 맞출 수 있는 데이터가 많아짐
  - 이미지 선명도 조절을 통해 흐린 말 이미지를 선명한 말 이미지로 만듦
  - **기존 모델로도 센서 제어로 인식 가능하여 시간과 비용이 절감됨 → 모델 일반화 방식보다 효율적**

슬라이드는 (C) Sensor Control 쪽에만 별표와 따봉 아이콘을 붙여 이 방식을 선택지로 지목한다.

| 구분 | 모델 일반화 (p.091) | 적응적 센싱 (p.092) |
| --- | --- | --- |
| 손대는 대상 | 모델(빨간 영역 경계) | 데이터(센서 파라미터) |
| 동작 | 빨간 영역을 넓힘 | C-OOD를 빨간 영역 안으로 이동 |
| 재학습 | 필요 (선명+흐린 이미지 모두 학습) | 불필요 (기존 모델 그대로) |
| 비용 | 추가 학습 비용 높음, 시간 소요 | 시간과 비용 절감 |
| 적용 시점 | 학습 단계 | 추론 단계 (실시간) |

## 적응적 센싱을 위한 데이터셋

기존 강건성 벤치마크로는 적응적 센싱을 평가할 수 없다. 센서 파라미터를 바꿔가며 **실제로 다시 촬영한** 데이터가 있어야 하기 때문이다. 그래서 데이터셋 자체를 새로 만든다.

### ImageNet-ES: 이미지 분류 태스크 (p.094)

- **같은 장면을 카메라 센서 파라미터와 조도를 변화시키며 캡쳐**
  - 센서 파라미터: **ISO, Shutter Speed, Aperture** 조절 + **AutoExposure** 옵션
  - 장면: **TinyImageNet의 샘플 1000개**

- **ImageNet-ES (Luminous)**
  - TV 스크린에 띄운 이미지를 카메라로 촬영 (**발광체**)
  - `https://huggingface.co/datasets/edw2n/ImageNet-ES`
- **ImageNet-ES (Diverse)**
  - 종이에 프린트된 이미지를 카메라로 촬영
  - `https://huggingface.co/datasets/edw2n/ImageNet-ES-Diverse`

> 📌 원본 p.94에는 `ImageNet-ES (Luminus)`로 되어 있으나 `ImageNet-ES (Luminous)`가 맞다. 같은 자료의 p.99, p.100 그래프 제목이 모두 `ImageNet-ES (Luminous)`로 표기되어 있다.

발광체(TV)와 반사체(인쇄물)를 나눈 이유는 빛이 눈에 들어오는 경로가 달라 센서 파라미터의 최적점도 달라지기 때문이다.

- **기존 강건성 데이터셋과의 차이**
  - 기존 데이터셋은 **디지털 변환(포토샵), 카메라 줌, 위치 변화**를 사용
  - ImageNet-C, -P, -R, -A, -E 등
  - **아날로그 환경을 카메라 센서가 어떻게 캡쳐해내는 지 구현하지 못함**

**\[그림2-5\] ES-Studio** (촬영 장비 구성, 문자로 옮김)

```text
                    ┌─────────────┐
                    │   PC/모니터  │
                    └──────┬──────┘
   Camera control          │          Screen control
   (sensor parameters,     │          (change displayed image)
    capture command)       │
        ↓                  ↓                    ↓
   ┌─────────┐        Light control        ┌──────────┐
   │ 카메라   │  ←── 100cm ──→   (on/off)  │ TV 스크린 │
   └─────────┘         ☀ × 2 (조명 2개)     └──────────┘

   전체는 Dark room (150 × 150 × 200 cm) 안에 설치
```

암실 안에 넣은 이유는 외부 광원을 배제해야 조도를 통제 변수로 쓸 수 있기 때문이다.

**\[그림2-6\] ImageNet-ES** (샘플 격자, 문자로 옮김)

```text
열 구성:  [AE (Auto Exposure)] │ [Manual parameter settings ......]
행 구성:  Light On  (3행)
          Light Off (3행)
→ 같은 꽃 장면이 조명 On/Off × 수동 파라미터 조합에 따라
  과다노출(백색)부터 완전 암흑(흑색)까지 전 범위로 변한다.
```

- 출처: `https://github.com/Edw2n/ImageNet-ES`

### SenseShift6D: 물체 6D 포즈 추정 태스크 (p.095)

분류가 아닌 태스크에서도 같은 주장이 성립하는지 보이기 위한 두 번째 데이터셋이다.

- **같은 장면을 RGB-D 카메라 센서 파라미터와 조도를 변화시키며 캡쳐**
  - 센서 파라미터: **Exposure, Gain, Depth mode** 조절 + **AutoExposure** 옵션
  - **멀티모달 센서 파라미터 조절이 가능** (RGB와 Depth를 함께 다룸)
  - 장면: **6개 물체의 다양한 6D 포즈**
  - `https://huggingface.co/datasets/Yegyu/SenseShift6D`

**\[그림2-7\] SenseShift6D 촬영 환경 및 물체** (문자로 옮김)

```text
Server ──> HUB ──> Camera (삼각대)
   │                  ↓ 촬영
   PC ──> HUB ──> Light stand × 2 (좌우 조명)
                      ↓
              Turn table & ArUco board 위의 물체
              (ArUco 마커로 포즈 기준 좌표를 잡음)

대상 물체 6개: 스프레이 세제, 캔, 상자, 샌드위치, 회색 마우스형 물체, 노란 오리 인형
```

**\[그림2-8\] SenseShift6D** (샘플 격자, 문자로 옮김)

```text
좌측 블록: [AE] │ [RGB Sensor Control]   행 라벨 B5, B25
우측 블록: [AE] │ [RGB Sensor Control]   행 라벨 B75, B100
  → B5~B100은 밝기(조도) 단계. 낮은 단계에서는 AE 결과가 거의 검게 나온다.
하단: 동일 장면의 Depth 맵 4장 (파랑~빨강 컬러맵으로 깊이 표현)
  → RGB가 어두워도 Depth는 별도로 거동하므로 멀티모달 제어가 필요하다.
```

## 적응적 센싱 기법: Lens

### Lens의 4단계 파이프라인 (p.097)

Lens는 **추론 시점에** 여러 센서 설정으로 찍어보고 모델이 가장 자신 있어 하는 이미지를 골라 쓰는 기법이다. 4단계로 구성된다.

- **센서 파라미터 후보 선택**
  - 옵션이 많아서 현실적으로 모든 파라미터를 테스트할 수 없음
  - **무작위 선택, 등간격 선택, 최단시간 선택**
- **이미지 캡쳐**
  - 후보로 선택된 파라미터들로 이미지 여러 장 캡쳐
- **비전 테스트**
  - 캡쳐한 이미지들을 모델에 입력하여 추론 과정을 거침
  - 점수: 모델이 도출한 이미지의 **confidence score**
- **최적의 파라미터 선택**
  - **점수가 가장 높은 이미지를 선택하여 최종 추론 수행**

**\[그림2-9\] Lens** (파이프라인, 문자로 옮김)

```text
입력 3개
  Scene                        : 촬영 대상 장면 s
  Target Neural Network  M     : 점수를 매길 모델
  # of Options           K (≤ N): 실제로 찍어볼 후보 개수
  Available Sensor Options {p_1, p_2, ..., p_N} : 사용 가능한 전체 파라미터 집합

흐름
  {p_1, ..., p_N} ──> CSA (Candidate Selection) ──> K options {p'_1, p'_2, ..., p'_K}
                                                          │
                                                          ↓
                                                   Camera Control
                                                          │
        ┌────────────┬────────────┬────────────┬──────────┴─────────┐
       p'_1         p'_2         p'_3         p'_4       ...       p'_K
        ↓            ↓            ↓            ↓                    ↓
     x_{s,p'_1}   x_{s,p'_2}   x_{s,p'_3}   x_{s,p'_4}          x_{s,p'_K}   (캡쳐된 이미지)
        ↓            ↓            ↓            ↓                    ↓
      VisiT        VisiT        VisiT        VisiT               VisiT        (품질 점수기)
        ↓            ↓            ↓            ↓                    ↓
       Q_1          Q_2          Q_3          Q_4                 Q_K
        └────────────┴──────── argmax ────────┴────────────────────┘
                                 ↓
                    Best image for model :  x_{s, p̂}
```

기호를 정리하면 다음과 같다.

- $s$: 장면(scene), $p$: 센서 파라미터 설정
- $x_{s,p}$: 장면 $s$를 파라미터 $p$로 촬영한 이미지
- $Q(\cdot)$: VisiT가 매기는 품질 점수 (모델의 confidence score)
- $\mathbf{P}'=\{p'_1, p'_2, \ldots, p'_K\}$: CSA가 고른 후보 집합, $K \le N$

최종 선택식은 다음과 같다.

$$\hat{p} = \operatorname*{argmax}_{p \in \mathbf{P}'} Q(x_{s,p})$$

그리고 최종적으로 모델에 넣는 이미지는

$$x_{s,\hat{p}}$$

이다. **argmax의 정의역이 전체 $\{p_1,\ldots,p_N\}$이 아니라 후보 집합 $\mathbf{P}'$인 점**이 핵심이다. 전수 탐색은 촬영 횟수가 $N$번 필요해 실시간으로 불가능하므로, CSA가 먼저 $K$개로 줄이고 그 안에서만 최댓값을 찾는다. 즉 Lens는 최적해가 아니라 **$K$번의 촬영 예산 안에서의 최선**을 고른다.

> **주의** Lens는 모델을 전혀 건드리지 않는다. 학습된 $M$을 그대로 두고 **입력을 여러 번 만들어 보는 것**만으로 성능을 올린다. 이것이 앞의 (C) Sensor Control 그림에서 "데이터 점을 민다"고 한 동작의 실제 구현이다.

## 성능 효과

### Lens vs. Auto-Exposure (p.099)

먼저 비교 대상인 AE의 성격을 짚는다. 둘은 목적함수가 다르다.

- **Lens**: 센서 제어로 **모델이 보기 좋은** 이미지를 만듦
- **AE**: **사람이 보기 좋은** 이미지를 만들기 위한 제어 기법

- **Lens의 효과**
  - **모델, 학습 데이터 크기에 관계없이 센서 조절만으로 성능 향상**
  - ResNet50에서 AE에 비해 Lens가 **47.56%** 정확도 향상
  - DINOv2-b에서 AE에 비해 Lens가 **8.52%** 정확도 향상
- **초거대 AI 모델도 여전히 분포 이동 제어 기법의 혜택을 받음**

작은 모델에서 이득이 크고 큰 모델에서 이득이 줄어들지만, **0이 되지는 않는다**는 것이 마지막 줄의 요지다.

**\[그림2-10\] 모델 크기별 ImageNet-ES 성능 그래프** (문자로 옮김)

```text
제목: ImageNet-ES (Luminous)
X축: Model Size (Million Parameters)  0 ~ 1000+
Y축: Accuracy (%)  30 ~ 90+

범례
  < Pretrained Dataset >   ○ IN-1K   □ IN-21K   ◇ LAION-2B   △ LVD-142M
  < Sensor Control Method > □ AE(테두리만)  ▨ Random(빗금)  ■ Lens(채움)
  < Model Architectures >
    ResNet50 / ResNet50-Aug / ResNet152 / Swinv2-T / Swinv2-S / Swinv2-B
    Efficient-B0 / Efficient-B3 / OpenCLIP-b / OpenCLIP-h / DINOv2-b / DINOv2-b

읽는 법
  같은 색·같은 모양 마커 3개(테두리/빗금/채움)가 같은 모델의 AE/Random/Lens 결과다.
  왼쪽 붉은 세로 화살표: 소형 모델(ResNet50)에서 AE 약 32% → Lens 약 79%로 수직 상승
  오른쪽 붉은 세로 화살표: 초대형 모델(DINOv2-b, 1000M+)에서도 △(AE) → ▲(Lens)로 상승
  → 화살표가 모두 "수직"이라는 점이 중요하다. 모델 크기(가로축)는 그대로 둔 채
    정확도(세로축)만 올라간다.
```

### Lens vs. 도메인 일반화 (p.100)

같은 그래프를 다른 각도로 읽는다. 이번에는 **작은 모델 + Lens**와 **초거대 모델 + AE**를 직접 맞붙인다.

- **EfficientNet-B3 (Lens) vs. OpenCLIP-h (AE)**
  - OpenCLIP-h는 **초거대 모델의 일종**
  - EfficientNet-B3보다 **50배 많은 모델 파라미터**
  - 그리고 **2,000,000배 많은 학습 데이터**

- **Lens의 효과**
  - **50배 작은 모델과 2,000,000배 적은 학습데이터로 4~10%의 정확도 향상**
  - 작은 모델 + 적응적 센싱이 초거대 모델 + 일반 제어보다 효과가 좋음

> **주의** 붉은 글씨의 결론: **→ 모델만 키우는 게 다가 아니고, 센서도 중요하다.** 이 문장이 이 강의의 최종 주장이다.

**\[그림2-11\] 모델 크기별 ImageNet-ES 성능 그래프** (강조 영역만 문자로 옮김)

```text
좌측 붉은 원 (Model Size ≈ 0~100M, Accuracy ≈ 80~85%)
    라벨: "작은 모델 + 적응적 센싱"
              │
              └── 붉은 화살표로 우측 원을 가리킴 (같은 정확도대 비교)
                          │
우측 붉은 원 (Model Size ≈ 600M, Accuracy ≈ 71~79%)
    라벨: "초거대 모델 + 일반 제어"

→ 좌측 원이 우측 원보다 위에 있다. 즉 6배 작은 지점에서 더 높은 정확도가 나온다.
```

## 특징과 함의

### 사람이 좋아하는 이미지와 모델이 좋아하는 이미지는 다르다 (p.102)

- **AI 모델이 더 잘 맞추는 사진은?**
  - 사람에게 보기 좋은 이미지와 AI 모델이 좋아하는 이미지는 **다를 수 있음**
  - AI 모델을 위한 센서 제어는 사람을 위한 센서 제어와 **방향성이 다를 수 있음**

**\[그림2-12\] AI 모델이 좋아하는 이미지** (문자로 옮김)

```text
좌: 나뭇가지 위 청개구리 — 색이 진하고 대비가 뚜렷한, 사람이 보기 좋은 사진
우: 같은 구도의 청개구리 — 노출이 과해 색이 바래고 흐릿한, 사람이 보기엔 실패한 사진
→ 강의의 질문은 "둘 중 AI 모델이 더 잘 맞추는 쪽은?"이며, 답이 우측일 수 있다는 것이 요지다.
```

- **"그 때 그 때 달라요"**
  - 적응적 센싱의 필요성: 최적의 센서 파라미터는 **모델에 따라, 환경에 따라, 피사체에 따라 달라짐**

이 세 가지 의존성 때문에 "좋은 설정값 하나를 찾아 고정한다"는 접근이 불가능하다. 매 추론마다 다시 찾아야 하므로 Lens 같은 **런타임 탐색**이 필요해진다.

## 확인문제

### 문제 1: 초거대 AI의 스케일링 전략 (p.103-104)

**초거대 AI의 스케일링 전략에 대한 설명으로 가장 옳지 않은 것은?**

- a) AI 모델의 성능은 모델 크기, 학습 데이터 양, 컴퓨팅 자원이 증가함에 따라 향상되는 경향이 있다
- b) 분포 이동 문제를 해결하기 위해 모든 경우의 수를 학습 데이터로 준비하는 것은 현실적으로 가능하다
- c) 초거대 AI 전략은 "데이터 더! 모델 더 크게!"라는 방식으로 규모로 보험을 드는 접근이다
- d) 스케일링 법칙의 한계로는 비용 폭증, 환경 문제, 데이터 고갈 등이 있다

**정답: b)**

**해설:** 분포 이동 문제의 핵심은 세상의 모든 경우의 수를 학습 데이터로 미리 준비하는 것이 불가능하다는 점임. 이것이 바로 초거대 AI가 막대한 비용에도 불구하고 한계를 가지는 이유이며, 적응적 센싱과 같은 새로운 접근법이 필요한 이유

### 문제 2: 적응적 센싱의 정의 (p.105-106)

**적응적 센싱(Adaptive Sensing)에 대한 설명으로 가장 올바른 것은?**

- a) 모델의 파라미터 수를 증가시켜 더 많은 데이터 분포를 학습하는 기법이다
- b) 센서 파라미터를 조절하여 다양한 데이터를 모델이 학습한 영역(ID) 안으로 이동시키는 기법이다
- c) 학습 데이터의 양을 늘려서 분포 이동 문제를 근본적으로 해결하는 기법이다
- d) 모델 일반화와 동일한 개념으로, 학습 단계에서 적용되는 기법이다

**정답: b)**

**해설:** 적응적 센싱은 모델을 키우는 대신 센서(카메라 등)의 파라미터를 똑똑하게 조절하여, 데이터를 모델이 잘 인식할 수 있는 영역으로 이동시키는 기법. 이는 인간이 안경을 쓰는 것과 유사한 접근으로, **추론 단계에서 실시간으로 적용됨.**

> **주의** 오답 d)가 노리는 지점이 시험 포인트다. 모델 일반화는 **학습 단계**, 적응적 센싱은 **추론 단계**다. 적용 시점이 둘을 가르는 결정적 차이다.

### 문제 3: Lens와 초거대 모델의 성능 비교 (p.107-108)

**Lens(적응적 센싱 기법)와 초거대 모델의 성능 비교에 대한 내용으로 옳은 것은?** (2-5 성능 효과 그림 2-10, 2-11 참고)

- a) EfficientNet-B3에 Lens를 적용한 것이 OpenCLIP-h(초거대 모델)보다 50배 많은 파라미터를 사용하지만 더 효율적이다
- b) 적응적 센싱은 초거대 모델보다 항상 많은 학습 데이터가 필요하다
- c) 50배 작은 모델과 2,000,000배 적은 학습 데이터로도 적응적 센싱을 사용하면 초거대 모델보다 4~10% 더 높은 정확도를 달성할 수 있다
- d) 적응적 센싱은 모델 지식 영역(ID)을 확장하는 방식이고, 모델 일반화는 데이터를 ID 영역으로 이동시키는 방식이다

**정답: c)**

**해설:** EfficientNet-B3(Lens 적용)는 OpenCLIP-h보다 50배 작은 모델 파라미터와 2,000,000배 적은 학습 데이터를 사용했지만, ImageNet-ES(Luminous) 벤치마크에서 4~10%의 정확도 향상을 보임. 이는 적응적 센싱이 모델 크기와 데이터 양에 의존하는 기존 방식보다 훨씬 효율적임을 보여줌.

> 📌 원본 p.108 문항 옆 괄호에는 `(2-5 성능 효과 그림 2-8 참고)`로 되어 있으나 `그림 2-10, 2-11`이 맞다. 그림 2-8은 p.95의 SenseShift6D 샘플 격자이고, 2-5절 성능 효과의 그래프는 p.99의 그림 2-10과 p.100의 그림 2-11이다.

- 오답 a)는 파라미터 크기 관계를 뒤집었다. EfficientNet-B3이 **50배 작다.**
- 오답 d)는 두 방식의 정의를 서로 바꿔놓았다. ID 영역을 확장하는 쪽이 모델 일반화다.

## 강의 정리

### 오늘 공부한 내용 요약 및 정리 (p.109)

- **초거대 AI의 한계와 인간 인지체계와의 관계**
  - 초거대 AI는 모델만 키워서 비용이 폭증하지만 인간은 감각기관(센서)를 사용해 효율적으로 해결함
  - AI 모델뿐 아니라 센서를 개선해야함
- **적응적 센싱을 통한 분포 이동 억제**
  - 센서를 조절해 데이터를 모델이 아는 영역으로 이동시키는 기법
  - 모델을 키우지 않고도 분포 이동 문제를 해결
- **적응적 센싱의 성능과 특징**
  - 적응적 센싱은 작은 모델로도 성능이 향상되며 초거대 모델보다 효율적임
  - **실시간으로 환경에 적응할 수 있는 것이 특징**

---

## 정리 체크리스트

- [ ] 스케일링 법칙의 지수가 작다는 것이 왜 한계를 뜻하는지 설명할 수 있다
- [ ] 피지컬 AI에서 분포 이동이 더 심해지는 이유를 말할 수 있다
- [ ] ID / C-OOD / OOD를 두 축 위에 배치할 수 있다
- [ ] 모델 일반화와 적응적 센싱이 각각 무엇을 움직이는지 구분할 수 있다
- [ ] Lens의 argmax 정의역이 전체가 아니라 후보 집합인 이유를 안다
- [ ] "사람이 좋아하는 이미지와 모델이 좋아하는 이미지가 다르다"의 근거를 말할 수 있다
