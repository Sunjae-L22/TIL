# 다양한 신경망 모델과 학습 전략 (딥러닝 및 이미지 모델 2~3차시)

> 출처: `3_1_1_이미지딥러닝모델.pdf` (총 196페이지, SSAFY) · 정리일: 2026-08-15
> 페이지 표기는 PDF 기준 (슬라이드 인쇄 번호와 동일)
> 구성: p.1-90 = 2차시 「다양한 신경망 모델」 · p.91-196 = 3차시 「이미지 모델 학습 전략」

## 한눈에 보기

- CNN이 잘하는 것(지역 특징, 파라미터 공유)과 못하는 것(순서, 긴 거리 의존성, 픽셀 복원)을 구분할 수 있는가?
- RNN의 은닉 상태 순환이 왜 기울기 소실/폭발로 이어지고, LSTM의 셀 상태(덧셈 경로)가 이를 어떻게 막는지 설명할 수 있는가?
- 쿼리/키/값(Q/K/V)으로 어텐션 출력이 계산되는 흐름과, ViT가 이미지를 토큰으로 바꿔 처리하는 파이프라인을 그릴 수 있는가?
- CNN → RNN → LSTM → Attention 순서로 "각 모델이 무엇을 새로 해결했나"를 표로 말할 수 있는가?
- 같은 구조·같은 데이터로도 성능이 갈리는 이유 — 활성화 함수, 초기화, 정규화, 학습률이 각각 어떤 문제를 푸는지 연결할 수 있는가?
- 학습이 안 될 때 손실·정확도 곡선을 보고 원인(초기값, 학습률, 과적합, 모델 용량)을 진단할 수 있는가?

## 목차

1. [CNN의 구조와 한계](#cnn의-구조와-한계) (p.1-12)
2. [RNN 순차 데이터 처리](#rnn-순차-데이터-처리) (p.13-48)
3. [LSTM 대안 모델](#lstm-대안-모델) (p.49-54)
4. [어텐션 메커니즘과 ViT](#어텐션-메커니즘과-vit) (p.55-90)
5. [학습 전략이 왜 중요한가](#학습-전략이-왜-중요한가) (p.91-100)
6. [모델 구성 — 악마는 디테일에](#모델-구성--악마는-디테일에) (p.101-151)
7. [학습 안정성 전략](#학습-안정성-전략) (p.152-185)
8. [부록 — 참고 자료와 실습 미리보기](#부록--참고-자료와-실습-미리보기) (p.186-196)

---

## CNN의 구조와 한계

2차시의 학습 목표는 하나의 질문으로 요약된다(p.3-5). **"왜 CNN만으로는 부족할까?"** — 순서가 중요한 데이터를 처리하지 못하고, 긴 거리 의존성(Long-range Dependency)이 부족하기 때문이다. 이 두 결핍이 각각 RNN과 어텐션/ViT의 등장 배경이 된다.

### CNN 구조 복습 — 다섯 블록 (p.6-8)

```text
입력 → 합성곱 레이어(Convolution Layer) → 풀링(Pooling) → 비선형 함수(Non-linearity)
     → 정규화(Normalization) → 연결층 레이어(Fully Connected Layer)
```

- **합성곱 레이어** — 작은 필터가 이미지를 훑으며 지역 특징을 추출
- **풀링** — 다운샘플링. 예: 224×224×64 → 112×112×64
- **비선형 함수** — ReLU 계열 (3차시에서 상세)
- **정규화** — 채널 통계로 입력을 균일화: $\hat{x}_{i,j} = \dfrac{x_{i,j} - \mu_j}{\sqrt{\sigma_j^2 + \varepsilon}}$
- **연결층** — 특징을 최종 출력(라벨)으로 변환

합성곱 구조의 특성 4가지 (p.8):

1. **지역적인 특징 학습에 특화** — 작은 필터로 학습하여 패턴/경계선 특징 포착 용이
2. **파라미터 효율성 우수** — 이미지 블록별 가중치를 공유하여 연산량 절감
3. **위치 변화와 노이즈에 견고** — Pooling/Stride 합성곱이 세부 차이는 줄이고 이미지 전체 의미에 집중하게 함
4. 이미지 분류/탐지 등 이미지 기반 과업에서 **표준 모델**로 활약

### CNN의 핵심 한계 3가지 (p.10-12)

| 한계 | 내용 | 예시 |
|---|---|---|
| **순서(order) 무시** | 순차(sequential) 데이터의 순서 정보를 반영하기 어려움 | "약을 먹고 잠을 자고" vs "잠을 자고 약을 먹고" / "I am a boy not a girl" vs "I am a girl not a boy" |
| **긴 거리 의존성 부족** | 국소(로컬) 특징 추출에 강하지만 멀리 떨어진 요소 간 관계는 잘 학습 못 함 | 이미지에서 멀리 있지만 반복되는 패턴 |
| **픽셀 단위 복원 한계** | 전체 정보를 라벨로 압축하는 데는 효과적이나 Pooling/Stride로 위치 정보가 손실됨 | 세그멘테이션, 영상 생성/편집에는 새로운 구조 필요 |

> **연결 고리** — 견고함(특성 3)과 위치 정보 손실(한계 3)은 동전의 양면이다. Pooling이 세부 차이를 뭉개주기 때문에 분류에 강하지만, 그래서 픽셀 단위 복원이 안 된다.

---

## RNN 순차 데이터 처리

### 기본 아이디어 (p.13-27)

RNN(Recurrent Neural Nets)은 순차 데이터를 처리하기 위해 **이전 단계의 정보를 은닉 상태(hidden state)로 다음 단계에 전달**하는 구조다. 시간/순서의 흐름을 반영할 수 있어 문장, 음성, 센서 데이터 같은 시계열 처리에 강한 귀납 편향을 제공한다 (p.15).

문자 단위 언어 생성 예시의 다섯 구성 요소 (p.17-27):

| 구성 요소 | 역할 |
|---|---|
| Input Chars | 학습하려는 문장의 문자들. One-hot encoding으로 변환되어 입력층으로 |
| Input Layer | 1과 0으로 된 벡터. 길이는 알파벳 집합 개수와 동일. $W_{xh}$: 입력 → 은닉 변환 |
| Hidden Layer | 현재 입력 + 이전 은닉 상태를 합쳐 만든 내부 메모리. "이전까지의 맥락(Context)"을 담아 전달. $W_{hh}$: 이전 은닉 → 현재 은닉 |
| Output Layer | 은닉 상태를 출력 벡터로 변환. 각 숫자는 다음 문자가 될 점수. $W_{hy}$: 은닉 → 출력 변환 |
| Target Chars | 각 시점의 정답 문자. Output Layer 확률과 비교한 오차로 가중치 업데이트 |

셀(cell)은 이전 값을 기억하는 메모리 역할을 하고, 은닉 상태는 셀이 출력층 방향 또는 다음 시점 $t+1$의 자신에게 보내는 값이다 (p.27). 재귀 표현(자기 자신을 가리키는 화살표)과 시간축으로 펼친 표현(사이클)은 같은 것이다 (p.28).

### 수식 (p.29-31)

시계열 의존성 — 현재 은닉 상태가 이전 은닉 상태에 의존하되, **RNN 상수(가중치) $W$는 모든 시점에 동일하게 적용**된다:

$$h_t = f_W(h_{t-1},\ x_t)$$

순수(vanilla) RNN은 선형변환 + 비선형(tanh)으로 구성된다:

$$h_t = \tanh(W_{hh}h_{t-1} + W_{xh}x_t)$$

$$y_t = W_{hy}h_t$$

- $h_t$: 현재 은닉 상태, $h_{t-1}$: 이전 은닉 상태, $x_t$: 현재 입력
- $\tanh(\cdot)$: 비선형 변환을 통해 더 복잡한 패턴 학습 가능

### 입출력 구조와 계산 그래프 (p.32-44)

```text
Many-to-One (시퀀스 입력 → 단일 출력):  x1 x2 x3 → [h0 →fw→ h1 →fw→ h2 →fw→ h3] → 출력
One-to-Many (단일 입력 → 시퀀스 출력):  ht → [fw' → h1 → y1, → h2 → y2, → h3 → y3, ...]
Many-to-Many (시퀀스 → 시퀀스):        위 둘을 이어 붙인 구조 (인코더-디코더 꼴)
```

- 동일 함수 $f_W$를 모든 시점에 반복 적용하고, 역전파 시 모든 은닉 상태에서 발생한 기울기를 **더해서** $W$에 반영한다 (p.34, 37).
- 언어 생성 예시 (p.41-44): 단위 데이터 `[h, e, l, o]`. "h" → one-hot `[1,0,0,0]` → Hidden `[0.3,-0.1,0.9]` → Output `[1.0,2.2,-3.0,4.1]` → Softmax `[.03,.13,.00,.84]` → Sample "e". 샘플된 문자가 다음 시점 입력이 된다.

### 단순 RNN의 한계 — 기울기 폭발/소실 (p.45-48)

역전파에서 시퀀스를 거슬러 갈 때 선형 가중치 $W$가 **계속 곱셈으로** 반영된다:

- $|W|$의 특이값 > 1 → 곱할수록 무한히 커짐 → **기울기 폭발**
- $|W|$의 특이값 < 1 → 곱할수록 0에 수렴 → **기울기 소실**
- 특이값: 행렬이 벡터를 얼마나 확장시키는지 크기로 나타낸 값. 반복해서 곱할 때 출력이 터지거나 사라지는지를 결정
- 이상적으로 특이값이 1이면 온전한 학습이 가능하지만 현실적으로 유지 불가

> **절삭(clipping)의 한계** — 폭발은 기울기 값을 강제로 절삭해 막을 수 있으나 결국 잘못된 기울기를 양산하고, **소실은 절삭으로 해결 불가** → 다른 모델(LSTM)이 필요하다 (p.47-48).

---

## LSTM 대안 모델

### 셀 상태와 게이트 (p.50-52)

LSTM(Long Short-Term Memory)은 **셀 상태(Cell State)와 게이트 유닛**으로 구성된다. 구동 철학: 긴 시퀀스에서 필요한 정보는 유지, 불필요한 정보는 지워 안정적 학습 유도.

$$\begin{pmatrix} i \\ f \\ o \\ g \end{pmatrix} = \begin{pmatrix} \sigma \\ \sigma \\ \sigma \\ \tanh \end{pmatrix} W \begin{pmatrix} h_{t-1} \\ x_t \end{pmatrix}$$

$$c_t = f \odot c_{t-1} + i \odot g \quad \text{(셀 상태)}$$

$$h_t = o \odot \tanh(c_t) \quad \text{(은닉 상태)}$$

| 게이트 | 역할 | 효과 |
|---|---|---|
| 입력 게이트 $i$ | 새로운 정보의 저장 | 새로운 정보를 적절히 추가해 학습 안정화 |
| 망각 게이트 $f$ | 과거 정보를 제거 | 필요 없는 정보는 줄여 셀 상태가 폭발하지 않도록 제어 |
| 출력 게이트 $o$ | 셀 상태를 현재 은닉 상태로 출력 | 필요한 정보만 은닉 상태로 전달 |
| 셀 후보 $g$ | tanh로 만든 새 후보 정보 | $i$와 곱해져 셀 상태에 더해짐 |

> **원본 슬라이드 주의** — p.51-52는 "망각 게이트 (g)"로 표기하지만, 같은 페이지의 수식 $c_t = f \odot c_{t-1} + i \odot g$에서 과거 셀 상태 $c_{t-1}$을 깎는 것은 $f$다. 표준 표기( $f$ = forget gate, $g$ = cell candidate)에 맞춰 위 표는 $f$를 망각 게이트로 적었다.

### 왜 기울기 소실이 완화되는가 (p.53-54)

셀 상태 경로에서 기울기가 **선형 가중치의 곱 이외에 덧셈 형태로도 전달**된다. $c_t = f \odot c_{t-1} + i \odot g$의 `+`가 기울기의 고속도로 역할을 해서, 각 상태의 기울기를 덧셈 경로와 곱셈 경로로 분리할 수 있다 (소실 방지).

```text
단순 RNN 역전파:  h4 → ×W → h3 → ×W → h2 → ×W → h1   (곱셈만 → 소실/폭발)
LSTM  역전파:    c3 → (+ 경로, ⊙f) → c2 → (+ 경로, ⊙f) → c1  (덧셈 경로로 장기 보존)
```

> **BUT, 정보 희석 문제의 한계** — 망각 게이트가 과거 정보를 조금씩 줄이므로, 누적되면 과거의 중요한 정보가 점차 희석되어 오래된 정보가 약화된다 (p.54). 이것이 어텐션 등장의 복선이다.

---

## 어텐션 메커니즘과 ViT

### 어텐션의 직관 — 패치 간 유사도 (p.55-58)

이미지에는 픽셀 거리와 상관없이 유사한 패치(patch: 이미지를 일정 크기로 잘라낸 조각)가 존재한다. **관련 높은 패치와 낮은 패치의 정보를 구분해서 최종 결정에 반영하자**는 것이 어텐션이다.

### Q/K/V 수식 (p.59)

$x$: 입력, $W$: 쿼리/키/값으로 변환하는 선형 변환, $D$: 입력의 차원, $y$: 출력.

| 요소 | 의미 | 수식 |
|---|---|---|
| 쿼리(Q) | 질의 대상 (집중하려는 입력 토큰/패치) | $q = xW_q$ |
| 키(K) | 특성 (입력 내 기타 토큰) | $k = xW_k$ |
| 값(V) | 키의 실제 정보 | $v = xW_v$ |
| 어텐션(a) | 쿼리와 키의 유사도 (확률 분포 형태) | $a_{ij} = \mathrm{softmax}(q_j \cdot k_i / \sqrt{D})$ |
| 출력(o) | 어텐션으로 가감된 값의 총합 | $y_j = \sum_i a_{ij} v_i$ |

행렬로 쓰면 한 줄이다:

$$\mathrm{Attention}(Q,K,V) = \mathrm{softmax}\!\left(\frac{QK^{\top}}{\sqrt{d_k}}\right)V$$

쿼리와 키가 유사하면 해당 값(V)에 가중치를 높게 준다.

### 셀프 어텐션 vs 교차 어텐션 (p.60-61)

| | 셀프 어텐션 (Self-Attention) | 교차 어텐션 (Cross-Attention) |
|---|---|---|
| Q/K/V 출처 | 하나의 입력 안에서 정의 | 둘 이상의 이종 데이터에서 정의 |
| 효과 | 입력 내부 패치 간 연결망 → 클러스터링 효과 (비슷한 패치가 그룹처럼 묶여 객체 구조 파악) | 서로 다른 입력 간 연결망 구축 |
| 예시 | 같은 이미지 내 유사도 반영 | Text-to-Image: 쿼리는 이미지 패치, 키/값은 텍스트 |

### 모델 별 특성 정리 — 무엇이 새로웠나 (p.62-63)

| 모델 | 새로 해결한 것 | 남은 문제 |
|---|---|---|
| **CNN** | 지역적(작은 범위) 특성에 집중 | 장거리 맥락 이해 떨어짐, 데이터 순서 반영 불가 |
| **RNN** | 데이터 순서를 반영 | 장거리 맥락 학습이 어려움 (기울기 소실/폭발) |
| **LSTM** | 순서 반영 + 장거리 맥락 이해 가능 | 정보 희석 문제 |
| **Attention** | 장거리 맥락에 탁월, 정보 희석 없음 | 데이터 순서? → 위치 인코딩으로 해결 |

### ViT 위치 인코딩 (p.64-66)

어텐션 자체는 순서를 모르므로 **각 입력 토큰에 위치 정보를 담은 벡터를 추가**한다 (이 패치가 이미지의 (2,2)에 위치함을 이해 가능).

| 방식 | 장점 | 단점 |
|---|---|---|
| 학습가능한 위치 인코딩 | 데이터에 알맞게 최적화 | 해상도가 바뀌면 다시 학습 필요 |
| 사인파 인코딩 | 고정 규칙이라 해상도가 바뀌어도 동일한 룰 적용 | 데이터/과업에 특화된 방식이 아님 |
| 상대 위치 인코딩 | 절대 위치가 아닌 상대적 위치를 인코딩 → 해상도 변화에도 적용 가능 | 절대적 위치(예: 좌우 코너) 고려가 어려움 |

> **원본 슬라이드 주의** — p.64(학습가능)에는 "(+) 해상도가 바뀌어도 동일한 룰 적용"과 "(−) 해상도가 바뀌면 다시 학습 필요"가 함께 적혀 있는데 서로 모순이다. (+) 항목은 사인파 인코딩의 장점이 복사된 것으로 보인다. 위 표는 방식별 본래 장단점으로 정리했다.

### ViT 전체 구조 (p.67-68)

```text
이미지 → 패치 분할 → 이미지 패치에 선형가중치 적용(토큰화)
      → [CLS(0*)] + 패치 별 위치 인코딩
      → ViT 인코더 × L개 (일반적으로 12개 이상)
      → 연결층 헤드(MLP head) → 라벨 (새/공/자동차 ...)
```

인코더 1개의 내부 (잔차 연결 포함):

```text
입력 → 정규화 → 다중헤드 어텐션 → ⊕(잔차합) → 정규화 → 연결층(MLP) → ⊕(잔차합) → 출력
```

- **정규화(Normalization)**: 입력을 균일하게 맞춤
- **다중헤드 어텐션(Multi-Head Attention)**: 여러 관점에서 패치 간 관계를 학습 (예: 어떤 헤드는 형태, 다른 헤드는 색, 또 다른 헤드는 위치)
- **연결층(MLP)**: 어텐션 정보를 조합·변환해 더 풍부한 특징 생성
- 전역 관계 학습: CNN은 지역 영역에서 수용영역을 넓혀 전역을 이해하지만, **ViT는 수용영역을 넓히지 않아도 전역 맥락 이해 가능**

### ViT vs ResNet — 데이터 규모가 승부를 가른다 (p.69-70)

ImageNet Top-1 정확도, 사전학습 데이터셋별 (Dosovitskiy et al. 그래프, p.69):

| 사전학습 데이터 | 결과 |
|---|---|
| ImageNet (중간 규모) | **ResNet 성능이 더 우수** |
| ImageNet-21k | ViT도 우수 (ViT-L/16이 큰 ResNet 수준) |
| JFT-300M (300M개 표기 데이터, 거대) | **ViT가 확실한 개선** (ViT-H/14 최고) |

- 모델명 규칙: B/L/H = Base/Large/Huge, `/32` `/16` `/14`는 패치 사이즈 (작을수록 토큰이 많아져 큰 모델)
- ViT는 막대한 상수를 보유하므로 **정규화 기법/데이터 증강이 매우 중요** (p.70)

### DeiT — 증류학습으로 데이터 효율 확보 (p.71-76)

증류학습(Knowledge Distillation, 지식 증류) 방식 (DeiT 2021):

1. **1단계**: "선생님 CNN 모델"을 이미지-레이블로 학습 (Cross Entropy Loss)
2. **2단계**: "학생 ViT 모델"은 선생님 CNN의 예측을 따라가도록 학습 (KL 발산 손실, KL Divergence Loss) + 정답 레이블도 함께 학습 (Cross Entropy Loss)

학생 ViT는 **학습 가능한 증류 토큰을 추가로 입력받아** 선생님의 logit 출력과 동일하게 예측하도록 학습된다 (p.74). ViT-B/16 @ ImageNet 기준 증류 + 길게 학습(300→1000 에폭) + 고해상도 입력(224×224 → 384×384)을 더할수록 성능이 계단식으로 상승한다 (p.75-76).

### ViT 변종과 트렌드 (p.77-80)

- **Swin Transformer** (p.77-78): 이미지 전역에 직접 어텐션은 고비용·비효율 → ① 윈도우 영역을 지정해 내부 어텐션에 집중, ② 다음 블록에서 윈도우를 비껴가게 설정(shifted window). ①②가 중첩되며 전역을 고려하는 효과. CNN처럼 계층적 이해를 활용해 **검출/분할 같은 화소별·공간 이해 과업에서 강함**
- 현존 최고 모델은 기준에 따라 다름 (p.79-80):

| 관점 | 모델 | 특징 |
|---|---|---|
| Vision 백본 (지도학습) | ViT-22B (구글, 220억 상수) | 대규모 비공개 레이블(JFT-3B) 기반, 일반화 우수, 상수 비공개(재현 불가) |
| 자기지도 Vision 백본 | DINOv2 (메타, 최대 10억 상수) | 공개 웹 데이터 14억 장, 자기지도(대조학습/증류), 현재 가장 많이 활용되는 공개 백본 |
| 비전-언어 | EVA-CLIP (상하이 AI) | 이미지-텍스트 대조 학습 + 마스킹 사전학습 결합, 제로샷 분류·멀티모달 검색 강력 |
| 종합 시각 모델 | InternImage-H (상하이 AI Lab) | Conv+Transformer 하이브리드 백본, 탐지/분할 최고 수준, 모델/코드 공개 |

### ViT 장단점 정리 (p.81, 90)

| | 내용 |
|---|---|
| 장점 | 전역 문맥을 한 번에 고려. 위치 인코딩으로 순서 고려 가능 (순서 없는 데이터는 선별 제거도 가능) |
| 단점 | 대규모 학습 자원(데이터·GPU 메모리) 필요. 데이터가 작으면 CNN보다 성능 저하. 한 번에 고려할 수 있는 토큰이 제한적 |
| 활용 | 이미지 분류/탐지/분할/생성에서 SOTA. 멀티모달 모델의 기준 아키텍처 |

> **확인문제 포인트** (p.82-89) — ① CNN 장점이 아닌 것: "순차 데이터의 순서 정보를 효과적으로 반영한다" (그건 RNN/Transformer 몫). ② RNN 특징이 아닌 것: "병렬 연산이 쉬워 속도가 빠르다" (순차 처리라 병렬화 어려움, Transformer가 개선). ③ RNN 대표 한계: 기울기 소실·폭발 (과적합은 모든 ML의 일반 문제, 병렬화 어려움은 대표 한계로는 거리). ④ ViT 설명 중 틀린 것: "데이터가 작을 때도 CNN보다 항상 높은 성능 보장".

---

## 학습 전략이 왜 중요한가

3차시의 문제의식 (p.91-100): **같은 모델 구조, 같은 데이터셋을 사용해도 성능 차이가 크게 난다.** 좋은 구조를 골라도 학습 과정의 불안정성, 과적합, 수렴 속도 문제로 실패할 수 있다.

학습에서 자주 겪는 문제 (p.98):

- **학습 불안정** — 손실 폭발(손실 값이 급격히 커져 수렴 실패), 학습 멈춤(손실 정체), 수렴 실패
- **과적합(오버피팅)** — 훈련 데이터에는 맞지만 검증·실전 성능 저하. 원인: 데이터 부족, 모델 복잡도 과다, 정규화 부족
- **느린 수렴** — 불필요하게 많은 에폭 소모

비유: **학습 레시피 = 재료 손질(전처리), 양념(모델 구성 요소·하이퍼 상수), 손맛(학습 과정 전략)** (p.95). 하이퍼 상수는 학습 전에 사람이 직접 설정하는 값이다. 결론 문장(p.100): *"좋은 구조만으로는 충분하지 않다 → 훈련 전략이 필요하다."*

---

## 모델 구성 — 악마는 디테일에

### 활성화 함수 (p.102-121)

**정의** — 입력 신호의 총합을 출력 신호로 변환하는 함수. 뉴런이 '켜질지/얼마나 반응할지'를 결정하는 스위치 (p.103). **역할** — 신경망에 비선형성 부여 → 복잡한 패턴 학습 가능. 활성화 함수가 없다면 단순 선형모델과 동일하다 (p.104-105).

| 함수 | 정의 | 장점 | 문제점 |
|---|---|---|---|
| Sigmoid | $\sigma(x) = 1/(1+e^{-x})$ | 출력 0~1 → 확률로 해석 가능 (이진 분류) | ① 포화 구간에서 기울기 소실(출력이 0/1 근처면 역전파 신호가 0) ② 출력이 항상 양수 → 편향된 업데이트 ③ 지수 연산 비용 |
| Tanh | $\tanh(x) = \dfrac{e^x - e^{-x}}{e^x + e^{-x}}$ | 출력 (-1, 1), 0 중심 대칭 → Sigmoid보다 안정 | 기울기 소실 여전, $e^x$ 연산 비용 |
| ReLU | $f(x) = \max(0, x)$ | 양의 영역 비포화 → 소실 크게 줄어듦. 단순 max 연산으로 빠름. Sigmoid/tanh 대비 6배 이상 빠른 수렴 보고 | 죽은 뉴런(Dead ReLU): 입력이 계속 음수면 0만 출력, 학습 멈춤. 0 기준 비대칭 |
| Leaky ReLU | $f(x) = \max(0.01x, x)$ | ReLU 장점 + 음수에도 작은 기울기 → 죽지 않음 | 기울기 값(0.01) 설정의 임의성, 0 중심 아님, 항상 ReLU보다 우세하진 않음 |
| ELU | $f(x) = \begin{cases} x & x > 0 \\ \alpha(\exp(x)-1) & x \le 0 \end{cases}$ | ReLU 장점 + 평균 출력 0 근처 + 음수 포화 구간으로 소실·왜곡 해결 | 계산 복잡성 증가, 하이퍼파라미터 $\alpha$ 필요 |
| SELU | $f(x) = \begin{cases} \lambda x & x \ge 0 \\ \lambda\alpha(e^x - 1) & x < 0 \end{cases}$ | 자기 정규화 계열 (모아보기 p.118에 등장) | — |
| Maxout | $\max(w_1^{\top}x + b_1,\ w_2^{\top}x + b_2)$ | 두 선형식 중 최댓값 (모아보기 p.118에 등장) | 파라미터 2배 |

> **원본 슬라이드 주의 2가지** — ① p.118의 SELU 조건이 "$\lambda x$ if $x<0$"로 적혀 있는데, 같은 페이지 그래프(양수 구간 선형)와 모순이며 표준 정의와 부등호가 반대다. 위 표는 표준 정의로 적었다. ② Leaky ReLU 계수가 p.114-115에서는 0.01, p.118 모아보기에서는 0.1로 다르게 등장한다 (통상 기본값은 0.01).

**실무 결론** (p.119-121): 고민 말고 **일단 ReLU부터 시도**. 성능 경쟁 중이라면 ReLU 개선판(Leaky ReLU, ELU, SELU)을 시도. **Sigmoid나 Tanh는 전문가 영역** (은닉층에서는 피하라는 뜻).

### 데이터 전처리 (p.122-126)

이미지 조건을 학습/검증/테스트에 **모두 동일하게** 적용한다: 해상도, 색상(RGB/BGR/Grey), 밝기, 정규화. 일반적 정규화는 **평균 0, 표준편차 1** (p.123).

3-channel 이미지 파이프라인 (p.124):

```python
basic_rgb_pipeline = transforms.Compose([
    transforms.Resize(256, interpolation=InterpolationMode.BICUBIC),  # 크기 통일
    transforms.CenterCrop(224),  # 센터 크롭 (선택)
    transforms.ToTensor(),       # 텐서화
    transforms.Normalize(IMAGENET_MEAN, IMAGENET_STD)  # 정규화
])
```

Grayscale 이미지는 모델 호환 위해 3-channel로 복제 (p.125):

```python
grayscale_pipeline = transforms.Compose([
    transforms.Resize(256, interpolation=InterpolationMode.BICUBIC),
    transforms.CenterCrop(224),
    transforms.Grayscale(num_output_channels=3),  # (H,W) -> (3,H,W)
    transforms.ToTensor(),
    transforms.Normalize(IMAGENET_MEAN, IMAGENET_STD)
])
```

모델별 전처리 방식 예시 (p.126):

| 모델 | 방식 |
|---|---|
| AlexNet | 평균 이미지를 빼기 (학습 데이터 전체의 평균 이미지를 입력에서 뺌) |
| VGG | 채널별 평균 빼기 (R, G, B 각 채널 평균값을 뺌) |
| ResNet | 평균 빼기 + 채널별 표준편차로 나누기 |

> **원본 슬라이드 주의** — p.126은 AlexNet 평균 이미지 크기를 [32, 32, 3]으로 적었는데, 이는 CIFAR-10 예시(CS231n 강의 예제) 크기다. ImageNet 기반 AlexNet이라면 입력 크기(예: 227×227×3)의 평균 이미지여야 맞다.

### 모델 상수(가중치) 초기화 (p.127-140)

**아이디어 1 — 0으로 초기화?** (p.128-129) 모든 출력이 0이 되고 뉴런별 출력·기울기가 동일해져 **학습이 이뤄지지 않는다** (대칭 깨기 실패).

**아이디어 2 — 작은 랜덤 숫자** (p.130-132):

```python
W = 0.01 * np.random.randn(Din, Dout)
```

- 0 중심 Gaussian 분포 → 얕은 모델에서는 동작하는 전략
- 깊은 모델에서는 층을 지날수록 활성값 표준편차가 0.49 → 0.29 → ... → 0.05로 붕괴. $W$가 1보다 작은 수라 역전파에서 계속 곱해지며 **기울기가 0으로 수렴** (상수가 크면 반대로 폭발)

**아이디어 3 — 자비에(Xavier) 초기화** (p.133-134): 분산을 입력 차원으로 맞춤, `std = 1/sqrt(Din)`

```python
W = np.random.randn(Din, Dout) / np.sqrt(Din)
x = np.tanh(x.dot(W))
```

- 모델상수 분산이 입력 차원과 맞으면 **선형연산 이후 출력의 분산 = 입력의 분산**을 보일 수 있음 → 층이 깊어져도 분포 유지 (std 0.63 → 0.30 수준으로 완만)

**아이디어 4 — 허(He) 초기화** (p.135-136): 자비에는 입출력이 대칭 분포라는 가정인데 **ReLU는 양의 분포만 남겨 필연적으로 비대칭** → 자비에 + ReLU 조합은 층이 깊어지며 다시 붕괴. 분산을 2×입력 차원 기준으로: `std = sqrt(2 / Din)`

```python
W = np.random.randn(Din, Dout) * np.sqrt(2) / np.sqrt(Din)
x = np.maximum(0, x.dot(W))
```

- 비대칭이지만 유사 분산이 레이어 후반에도 유지됨 (std 0.83 → 0.81)

**ResNet 주의사항** (p.137-140): 잔차 연결(입력을 출력에 더하는 지름길, 전체를 새로 배우지 않고 변화량만 학습)은 깊은 층에도 기울기를 전달하지만, 초기화가 잘못되면 $x + F(x)$가 층마다 **증폭**될 수 있다. ResNet은 ReLU를 쓰는 구조이므로 He 초기화를 쓰되 $\mathrm{Var}(x + F(x)) = \mathrm{Var}(x)$를 유도한다:

```text
잔차 블록:  x → [conv (He 초기화) → relu → conv (0으로 초기화)] = F(x) → ⊕(x 더하기) → relu
```

두 번째 conv를 0으로 초기화하면 초기엔 $F(x)=0$, 즉 입력 = 출력이라 분포가 안정된 채로 학습이 출발하고, 사실상 초반에는 skip 경로에만 의존해 안정적 학습을 유도한다.

### 모델 정규화 (p.141-151)

훈련 손실은 잘 줄어드는데 **검증 오차가 오히려 오르면 과적합 신호** — 실제 필드에서 가장 많이 겪는 이슈다. 정규화는 모델 복잡도를 제어해 훈련-검증 성능 차이를 줄이는 방법 (p.142).

**아이디어 1 — 가중치 감소 (Weight Decay)** (p.143-146). 가중치가 너무 크면 특정 입력에 과도하게 반응해 훈련 데이터를 외워버린다 → 가중치 크기에 비례한 패널티. 단, 가중치 감소만 완벽히 만족하면 모든 가중치가 0인 "아무것도 학습 안 한 모델"이므로, 원래 손실(CE loss 등)과 **함께** 동작할 때 의미가 있다. 균형이 핵심.

| 방식 | 수식 | 효과 |
|---|---|---|
| L2 (Ridge, Weight Decay) | $R(W) = \sum_k \sum_l W_{k,l}^2$ | 큰 가중치에 제곱 패널티 → 모든 가중치가 골고루 작아짐 |
| L1 (Lasso) | $R(W) = \sum_k \sum_l \lvert W_{k,l} \rvert$ | 중요하지 않은 가중치가 완전히 0 → 희소한 모델, 자동 특성 선택 |
| Elastic net (L1+L2) | $R(W) = \sum_k \sum_l \beta W_{k,l}^2 + \lvert W_{k,l} \rvert$ | $\beta$로 비중 조절 ($\beta$ 크면 L2 효과, 작으면 L1 효과). 변수 많고 상관관계 높을 때 효과적 |

상수의 희소성(가능하면 작은 모델 선호)을 강제하고 싶다면 L1을 선택한다.

**아이디어 2 — 드롭아웃** (p.147-151). 학습 중 일부 뉴런을 확률 $p$로 끔(0으로) → 매 학습 스텝마다 다른 네트워크 구조가 샘플링되는 효과. 특정 뉴런/특징에 대한 과도한 의존을 막는다.

- 장점: 과적합 방지·일반화 향상. 여러 작은 모델을 합친 **앙상블과 유사한 효과** (앙상블: 여러 모델을 학습시켜 예측을 평균/투표). 간단하면서 강력
- 단점: 학습시간 증가, 최적 드롭 비율 탐색 필요. RNN/LSTM 등 시계열에는 변형 기법 필요, **배치 정규화가 있는 모델에서는 비선호** (이미 강력한 정규화가 적용)
- **학습-추론 분포 차이 문제**: 학습 시 출력이 평균적으로 $(1-p)$배로 줄지만 테스트는 뉴런을 전부 사용 → 그대로 쓰면 출력 크기가 달라 성능 저하 (p.150)

| 해결책 | 방법 | 비고 |
|---|---|---|
| Inverted 드롭아웃 | **학습 시** 살아남은 뉴런 출력을 $1/(1-p)$배 스케일링 (예: $p=0.5$면 활성 뉴런에 ×2) | 테스트는 그대로 → 실서비스 표준 |
| 기존 방식 | **테스트 시** 출력에 $(1-p)$를 곱함 (예: $p=0.5$면 ×0.5) | 매 추론마다 곱셈 추가 → 서비스 속도 저하 |

---

## 학습 안정성 전략

### 학습 비율(학습률) 조정 (p.153-160)

**학습률은 가장 중요한 하이퍼파라미터** — 같은 모델·전처리·데이터라도 학습률에 따라 다른 결과가 나오고, 잘못 고르면 학습이 완전히 실패할 수 있다 (p.154). 너무 높으면 손실 폭발, 너무 낮으면 손실이 충분히 줄지 않는다. 기본 전략: **큰 값에서 시작해 에폭이 지날수록 줄인다** (p.155).

| 방식 | 규칙 | 특징 |
|---|---|---|
| 계단식 (Step) | $k$ 에폭마다 감소계수 $\gamma$를 곱함. 예: $\eta_0=0.1,\ \gamma=0.1,\ k=30$이면 0~29에폭 0.1 → 30~59에폭 0.01 → 60~89에폭 0.001 | 직관적이지만 변경 지점을 여러 개 정해야 해 복잡, 학습 곡선이 계단에서 출렁일 수 있음 |
| 코사인 | $\eta_t = \eta_0 \cdot \frac{1}{2}\left(1 + \cos\left(\frac{\pi t}{T}\right)\right)$ | "처음엔 크게, 나중엔 미세하게 조율". 변경 지점 선정이 불필요 |
| 선형 | $\eta_t = \eta_0 \cdot \left(1 - \frac{t}{T}\right)$ | "처음엔 크게 배우고 끝날수록 일정하게 줄여 수렴". 사전학습/미세조정에서 주로 쓰임 |
| 역제곱 꼴 | 처음에 크게, 이후 느리게 감소 | 대규모 학습에는 부적합 |
| 일정값 유지 | 동일 속도로 학습 | 데이터가 많고 과적합 위험이 없을 때 |

**빠른 종료(Early Stopping)** (p.160): 학습 중 검증 데이터셋 성능을 모니터링하다가, 성능이 더 좋아지지 않고 나빠지기 시작하면 조기에 멈춘다. 과적합 방지의 또 다른 방법.

### 하이퍼파라미터 선정 (p.161-165)

- **파라미터**: 학습을 통해 모델 스스로 얻는 값 (가중치, 편향)
- **하이퍼파라미터**: 학습 전에 사용자가 정하는 값. 학습 동안 고정 (p.162)

주요 하이퍼파라미터 (p.163):

| 분류 | 항목 |
|---|---|
| 학습 관련 | **학습률(가장 중요!)** 과 디케이 방식(스텝/코사인/선형), 배치 사이즈(작으면 정규화 효과, 크면 안정성 효과), 에폭 수(조기 종료 여부) |
| 최적화 관련 | 최적화 툴 선택(SGD, Adam, AdamW), 모멘텀·최적화 계수, 가중치 감소(L2, L1) |
| 모델 구조 관련 | 네트워크 깊이, 채널 수/드롭아웃 비율, 정규화(BN, LN, GN 등) |

탐색 방식 (p.164): **그리드 탐색**은 후보를 격자처럼 전부 조합, **랜덤 탐색**은 공간에서 무작위 선택 — 성능에 영향 없는 축의 경우의 수를 낭비하지 않아 효율적. 빅테크도 모든 조합을 실험하지 않는다. 규칙과 직관으로 GPU가 많지 않아도 좋은 성능 도출 가능 (p.165).

### 학습 디버깅 체크리스트 6단계 (p.166-179)

1. **초기 손실값 확인** (p.166) — 분류에서 CE 손실이면 초기 손실은 $\log(C)$ ($C$: 클래스 수)와 유사해야 정상. 크게 벗어나면 데이터 로딩/라벨 문제 확인
2. **작은 샘플 과적합 시켜보기** (p.167-169) — 정규화를 끄고 작은 학습 샘플에 정확도 100%까지 학습. 손실이 안 줄면 학습률/초기화/모델 구조 오류/활성화 함수/코드 버그. 손실이 폭발하면 학습률 줄이고 초기화 변경(가장 흔한 이슈)
3. **학습률 탐색** (p.170) — 전체 데이터로 ~100 iteration만 돌려 손실이 빠르게 줄어드는 학습률을 로그 스케일($10^{-1}\sim10^{-4}$)로 탐색. "손실이 실제로 줄어드는 학습률부터 시작하라"
4. **좋은 조합 고르기** (p.171) — 후보 학습률 × 가중치 변형(decay) 조합을 1~5 에폭만 학습해 비교. 좋은 조합은 초기 몇 에폭에 손실이 줄어든다. 단, **검증 세트 성능으로 관찰할 것**
5. **추가 학습** (p.172) — 좋은 조합들을 10~20 에폭 학습. 이 단계에서는 학습률 감소를 적용하지 않음 (탐색 속도를 늦춰 비효율)
6. **곡선 관찰** (p.173-179) — 손실의 이동평균과 학습/검증 데이터를 함께 본다:

| 곡선 패턴 | 진단 | 처방 |
|---|---|---|
| 초반 손실이 평평하다가 뒤늦게 하락 | 초기값 문제 | 초기화 변경 |
| 손실이 크게 안 떨어지고 유지 | 학습률 과대 | 학습률 줄이기 시도 |
| 학습률 감소 직후 손실 정체 | 정체 오기 전에 너무 일찍 줄임 | 감소 시점을 늦춤 |
| 정확도가 계속 증가 중 | 아직 수렴 전 | 계속 학습 |
| 학습/검증 정확도가 크게 벌어짐 | 과적합 | 정규화 방식 도입 |
| 학습/검증 차이가 매우 적음 | 모델의 힘이 너무 약함 | 큰 모델 도입, 더 오래 학습 |

> **3차시 확인문제 포인트** (p.180-184) — Sigmoid 주요 문제: 출력이 0/1이면 기울기가 0이 되어 역전파가 끊김. Leaky ReLU와 ReLU의 차이: 음수 입력에서 완전히 0이 되지 않고 작은 기울기를 줌. ResNet 전처리: 평균 빼기에서 나아가 스케일(표준편차)까지 맞춤. 드롭아웃 유의점: 학습-추론 간 분포 차이 보정 필요. 계단식 감소 단점: 변경 지점 여러 개 선정으로 복잡, 곡선 불안정.

---

## 부록 — 참고 자료와 실습 미리보기

### 참고 자료 목록 (p.186-190)

1차시(CNN vs FCN, MobileNet)·2차시(RNN 역전파, ViT 증류학습, 백본 개념)·3차시(기울기 소실/폭발, Dying ReLU, ReLU vs Sigmoid 비교) 관련 보충 영상 12개가 QR 코드로 제공된다. 합성곱 연산 과정, 전치(Transpose) 합성곱, MobileNet의 공간별/화소별 합성곱 분리 등 이 자료에 없는 1차시 주제가 포함되어 있다. (URL은 QR 이미지라 노트에는 옮기지 못함)

### 실습 미리보기 — Transfer Learning 기반의 CNN 모델 학습 (p.191-196)

사전 학습된 ResNet을 다양한 방식으로 학습하는 실습 예고편.

- **사전 학습(Pre-trained)**: 대규모 범용 데이터셋으로 모델을 처음부터 학습시키는 단계. 대표: GPT, BERT, ResNet
- **전이 학습(Transfer Learning)**: 사전 학습된 모델을 기반으로 특정 문제에 맞게 재학습. 대표 기법: LP, FT, Adapter, LoRA

| 구분 | LP (Linear Probing) | FT (Fine-Tuning) |
|---|---|---|
| 정의 | 표현을 고정(freeze)하고 마지막 계층(분류기)만 학습 | 가중치를 풀고 전체 파라미터(혹은 일부 레이어)를 학습 |
| 학습 범위 | 최종 선형 레이어만 업데이트 | 전체 모델 파라미터 업데이트 |
| 자원 소모 | 연산량·메모리 적음 → 빠르고 가벼움 | 연산량·메모리 많음 → 시간과 자원 많이 필요 |
| 장점 | 간단하고 빠른 평가 가능, 표현의 질 비교에 좋음, 과적합 위험 낮음 | 높은 성능 확보 가능, 데이터 특화 모델 구축, 복잡한 패턴도 학습 가능 |
| 단점 | 비선형적 구조(non-linear structure)나 복잡한 상호작용을 놓칠 수 있음 | 계산량 크고 자원 소모 큼, 과적합 위험 증가 |
| 대표 활용 | 표현 품질 측정, 레이어별 정보량 분석 | 다운스트림 task 최적화, 실제 서비스 모델 학습 |

- **데이터 증강(Data Augmentation)** (p.195): 기존 학습 데이터를 변형해 새 데이터를 생성 — 다양성 확보, 일반화 향상, 과적합 방지, 수집 비용 없이 확장. 이미지: 회전(Rotation)/뒤집기(Flip)/자르기(Crop)/Random erasing. 텍스트: 무작위 삭제·삽입, Back-translation. 음성: Time Stretch, Noise Injection
- **학습률 스케줄러** (p.196): 학습률을 고정하지 않고 규칙에 따라 동적으로 변화. Step Decay 예시 — 5 에폭마다 0.1배:

```python
# step_size=5: 5 에포크마다
# gamma=0.1: 학습률을 0.1배로 줄임
scheduler = optim.lr_scheduler.StepLR(optimizer, step_size=5, gamma=0.1)
```

1~5 epoch → lr = 0.0005, 6~10 epoch → lr = 0.00005, 11~15 epoch → lr = 0.000005.

---

## 정리 체크리스트

- [ ] CNN 다섯 블록(합성곱→풀링→비선형→정규화→연결층)의 역할을 각각 말할 수 있다
- [ ] CNN의 한계 3가지(순서 무시, 긴 거리 의존성 부족, 픽셀 복원 한계)를 예시와 함께 설명할 수 있다
- [ ] $h_t = \tanh(W_{hh}h_{t-1} + W_{xh}x_t)$에서 $W_{xh}, W_{hh}, W_{hy}$의 역할을 구분할 수 있다
- [ ] RNN 기울기 소실/폭발을 특이값(곱셈 반복)으로 설명하고, 절삭이 소실을 못 막는 이유를 말할 수 있다
- [ ] LSTM의 셀 상태 수식에서 덧셈 경로가 왜 기울기를 장기 보존하는지 설명할 수 있다
- [ ] LSTM의 남은 문제(정보 희석)가 어텐션으로 어떻게 해소되는지 말할 수 있다
- [ ] Q/K/V 각각의 의미와 $\mathrm{softmax}(QK^{\top}/\sqrt{d_k})V$ 계산 순서를 쓸 수 있다
- [ ] 셀프 어텐션과 교차 어텐션의 Q/K/V 출처 차이를 말할 수 있다
- [ ] 위치 인코딩 3방식(학습형/사인파/상대)의 장단점을 구분할 수 있다
- [ ] ViT 파이프라인(패치→선형투영→위치 인코딩→인코더×L→MLP head)을 그릴 수 있다
- [ ] ViT가 ResNet을 이기는 조건(사전학습 데이터 규모)을 말할 수 있다
- [ ] DeiT 증류학습의 2단계(선생님 CNN 학습 → 학생 ViT가 KL 손실 + CE 손실로 학습)를 설명할 수 있다
- [ ] Sigmoid의 문제 3가지와 "일단 ReLU" 결론의 근거를 말할 수 있다
- [ ] 0 초기화가 왜 실패하는지, Xavier와 He의 차이(ReLU 비대칭 보정)를 설명할 수 있다
- [ ] L2/L1/Elastic net 수식과 효과(골고루 축소 vs 희소화)를 구분할 수 있다
- [ ] Inverted 드롭아웃이 기존 방식과 어디서 다른지($1/(1-p)$를 학습 시에 곱함) 말할 수 있다
- [ ] 학습률 스케줄 3종(계단/코사인/선형) 수식을 쓸 수 있다
- [ ] 초기 CE 손실 정상값 $\log(C)$ 체크의 의미를 설명할 수 있다
- [ ] 손실·정확도 곡선 6가지 패턴에서 원인을 진단할 수 있다
- [ ] LP와 FT의 학습 범위·자원·장단점을 비교할 수 있다

## 복습 문제

**1.** 단순 RNN에서 기울기 소실/폭발이 발생하는 이유를 특이값으로 설명하시오. (CNN 쪽 확인문제는 위 「확인문제 포인트」 ① 참고)

<details><summary>답</summary>

역전파 시 선형 가중치 $W$가 시퀀스 길이만큼 반복해서 곱해진다. $|W|$의 특이값이 1보다 크면 곱할수록 무한히 커져 폭발, 1보다 작으면 0에 수렴해 소실된다. 특이값이 정확히 1이어야 온전한 학습이 가능하지만 현실적으로 유지하기 어렵다 (p.46).

</details>

**2.** LSTM의 셀 상태 갱신식을 쓰고, 각 항이 어느 게이트와 연결되는지 설명하시오.

<details><summary>답</summary>

$c_t = f \odot c_{t-1} + i \odot g$, $h_t = o \odot \tanh(c_t)$. $f$(망각 게이트)가 과거 셀 상태를 얼마나 남길지, $i$(입력 게이트)가 새 후보 $g$를 얼마나 반영할지, $o$(출력 게이트)가 셀 상태를 은닉 상태로 얼마나 내보낼지 결정한다. 덧셈 항 덕분에 기울기가 곱셈 경로와 분리되어 장기 보존된다 (p.50-53).

</details>

**3.** 어텐션에서 출력 $y_j$가 계산되기까지의 과정을 수식으로 쓰시오.

<details><summary>답</summary>

$q = xW_q$, $k = xW_k$, $v = xW_v$로 변환한 뒤, 유사도 $a_{ij} = \mathrm{softmax}(q_j \cdot k_i / \sqrt{D})$를 구하고, $y_j = \sum_i a_{ij}v_i$로 값을 가중합한다. 행렬로는 $\mathrm{softmax}(QK^{\top}/\sqrt{d_k})V$ (p.59).

</details>

**4.** ViT 인코더 한 블록의 내부 구성 순서를 쓰시오.

<details><summary>답</summary>

정규화 → 다중헤드 어텐션 → 잔차합 → 정규화 → 연결층(MLP) → 잔차합. 이런 블록을 L개(일반적으로 12개 이상) 통과한다 (p.67).

</details>

**5.** ViT와 ResNet의 성능 역전이 일어나는 조건은?

<details><summary>답</summary>

사전학습 데이터 규모. ImageNet 정도에서는 ResNet이 우수하지만, ImageNet-21k에서는 ViT-L/16이 큰 ResNet 수준이 되고, JFT-300M 사전학습 후 미세조정하면 ViT가 확실히 앞선다 (p.69).

</details>

**6.** Sigmoid의 문제 3가지를 쓰시오.

<details><summary>답</summary>

① 입력이 매우 크거나 작으면 출력이 0/1에 고정되어 기울기가 0 → 역전파 단절(기울기 소실) ② 출력이 항상 양수라 기울기 평균이 한쪽으로 치우쳐 편향된 업데이트 발생 ③ 지수 함수 계산 비용이 ReLU의 max 연산보다 큼 (p.107-109).

</details>

**7.** 깊은 모델에서 `W = 0.01 * np.random.randn(Din, Dout)` 초기화가 실패하는 이유와 해결책 두 가지를 쓰시오.

<details><summary>답</summary>

작은 수(<1)가 역전파에서 반복해 곱해지며 기울기가 0으로 수렴한다(크면 폭발). 해결책: Xavier 초기화 `W = np.random.randn(Din, Dout)/np.sqrt(Din)` (선형연산 후 출력 분산 = 입력 분산 유지), ReLU 모델은 He 초기화 `std = sqrt(2/Din)` (ReLU가 만드는 비대칭 분포 보정) (p.131-136).

</details>

**8.** ResNet 잔차 블록에서 두 conv의 초기화를 다르게 하는 이유는?

<details><summary>답</summary>

첫 번째 conv는 He 초기화, 두 번째 conv는 0으로 초기화한다. 초기에 $F(x)=0$이 되어 입력=출력, $\mathrm{Var}(x+F(x)) = \mathrm{Var}(x)$가 성립한다. 초반에는 skip 경로에만 의존해 분포가 안정된 채로 학습이 출발한다 (p.139-140).

</details>

**9.** L1과 L2 정규화의 결과 차이를 한 줄씩 쓰시오.

<details><summary>답</summary>

L2($R(W)=\sum W^2$): 큰 가중치에 제곱 패널티 → 모든 가중치가 골고루 작아짐. L1($R(W)=\sum|W|$): 절댓값 패널티 → 중요하지 않은 가중치가 완전히 0이 되는 희소 모델(자동 특성 선택). 희소성을 원하면 L1 (p.145).

</details>

**10.** 드롭아웃 $p=0.5$일 때 Inverted 드롭아웃이 학습/테스트 각각에서 하는 일을 쓰시오.

<details><summary>답</summary>

학습 시 뉴런 절반을 끄고 살아남은 뉴런 출력에 $1/(1-p) = 2$배를 곱한다. 테스트 시에는 아무 보정 없이 전체 뉴런을 그대로 사용한다. (기존 방식은 반대로 테스트 출력에 $(1-p)=0.5$를 곱해 추론 연산이 늘어난다) (p.150-151).

</details>

**11.** 학습/검증 정확도 곡선이 (a) 크게 벌어질 때, (b) 차이가 거의 없을 때 각각 어떤 조치를 하는가?

<details><summary>답</summary>

(a) 과적합 → 정규화 방식 도입. (b) 모델의 힘(용량)이 너무 약한 것 → 큰 모델을 도입해 더 오래 학습 (p.178-179).

</details>

---

> **읽은 방식 메모** — 전체 196페이지를 OCR(2200px)로 읽고, 수식·표·코드·다이어그램이 있는 42개 페이지(p.8, 29-31, 40-41, 46, 50-54, 59, 63-69, 73, 77, 91, 105, 116-118, 124-125, 130-136, 139-140, 145-146, 151, 156-158, 163, 166, 174-179, 194, 196)는 1600~2600px 이미지로 재확인했다. 본문의 모든 수식과 코드 블록은 이미지 대조를 거쳤다. 한국어 서술문 일부는 OCR 특성상 조사·어미를 문맥으로 복원했다. p.136의 He 초기화 코드는 슬라이드 원문이 수식 기호(√2)를 코드에 섞어 쓴 것을 `np.sqrt(2)`로 옮겨 적었다. 원본 슬라이드의 오탈자(폭팔→폭발, RestNet→ResNet, p.137·180)는 바로잡아 표기했고, 내용상 오류로 보이는 지점 4곳(LSTM 게이트 라벨, SELU 부등호, 위치 인코딩 장단점 복붙, AlexNet 평균 이미지 크기)은 본문에 "원본 슬라이드 주의"로 남겼다.
