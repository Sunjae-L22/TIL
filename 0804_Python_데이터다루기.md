# 데이터 다루기 (NumPy & Pandas)

> 출처: `0804_AI Python(2)_데이터 다루기.pdf` (총 50페이지) · 정리일: 2026-08-14
> 페이지 표기는 PDF 물리 페이지 기준 (슬라이드 인쇄 번호와 동일)

## 한눈에 보기

- 리스트 대신 NumPy 배열을 쓰면 무엇이 달라지는가 — 벡터화, 연속 메모리, 연산 속도
- `axis=0`과 `axis=1`은 각각 어느 방향의 집계인가
- 크기가 다른 배열끼리의 연산(브로드캐스팅)은 언제, 어떻게 성립하는가
- `df[조건]` 필터에서 `and`/`or` 대신 `&`/`|`와 괄호를 써야 하는 이유
- groupby에서 `['매출']`과 `[['매출', '수량']]`, `count()`와 `size()`는 무엇이 다른가
- groupby와 pivot_table은 무엇이 같고 무엇이 다른가

## 목차

1. [NumPy가 필요한 이유](#numpy가-필요한-이유) (p.4-10)
2. [NumPy 배열 만들기와 연산](#numpy-배열-만들기와-연산) (p.11-18)
3. [NumPy 필터링과 정렬](#numpy-필터링과-정렬) (p.19-20)
4. [Pandas DataFrame 기초](#pandas-dataframe-기초) (p.21-29)
5. [행 선택과 필터링](#행-선택과-필터링) (p.30-34)
6. [groupby와 pivot_table](#groupby와-pivot_table) (p.35-40)
7. [핵심 키워드 정리](#핵심-키워드-정리) (p.48-50)

---

## NumPy가 필요한 이유

### 리스트의 한계 (p.4-7)

부서별 엑셀을 복사·붙여넣기로 합쳐 정렬·합계를 다시 내는 수작업은 데이터가 늘어나는 만큼 느려지지만, 코드는 `df.sort_values('매출', ascending=False)` 한 줄을 재사용하면 된다 (p.4-5). 이때 Pandas(표)는 내부적으로 NumPy(배열) 위에서 동작한다 (p.6).

파이썬 리스트는 산술 연산을 원소별로 적용하지 않는다 (p.7).

- `리스트 + 리스트`는 연결, `리스트 * 2`는 반복 — `[4000, 4500, 5000] * 2`는 원소 6개짜리 리스트가 된다
- 각 원소에 대해 계산하려면 반복문으로 값을 하나씩 처리해야 함

### NumPy 배열의 특징 (p.8-10)

- **배열(ndarray)** : **같은 자료형**의 데이터를 담는 다차원 자료구조 (`.dtype`으로 확인)
- **벡터화(Vectorization)** : 반복문을 직접 작성하지 않고 배열 전체에 연산을 적용 — 일반 반복문보다 빠름
- **집계 함수** : `mean`, `sum`, `max`, `min`, `std` 등 내장

```python
import numpy as np
가격 = np.array([4000, 4500, 5000])
가격 * 0.9          # array([3600., 4050., 4500.]) ← 원소별 10% 할인
print(가격.dtype)   # int64 ← 원소 자료형
```

속도 차이의 근원은 메모리 구조다 (p.9).

| | 저장 방식 | 처리 방식 |
|---|---|---|
| 리스트 | 흩어진 각 객체의 **참조**를 저장 | 반복문으로 값을 하나씩 처리 |
| NumPy 배열 | 같은 자료형 데이터를 **연속된 메모리 공간**에 저장 | 배열 전체를 벡터화 연산으로 처리 |

같은 범위의 숫자를 더하는 벤치마크 (p.10). 데이터가 많아질수록 차이가 커진다 (수치는 실행 환경에 따라 다름).

| 데이터 수 | 파이썬(초) | NumPy(초) | 속도 차이 |
|---|---|---|---|
| 1,000 | 0.0000154 | 0.0000053 | 2.9배 |
| 100,000 | 0.0013191 | 0.0000637 | 20.7배 |
| 1,000,000 | 0.0109592 | 0.0003693 | 29.7배 |

---

## NumPy 배열 만들기와 연산

### 배열 생성 API (p.11)

| API | 역할 |
|---|---|
| `np.array(리스트)` | 리스트를 NumPy 배열로 변환 |
| `np.arange(n)` | 0부터 n-1까지의 정수로 배열을 생성 |
| `np.zeros(n)`, `np.ones(n)` | 0 또는 1로 채운 배열을 생성 |
| `np.linspace(a, b, n)` | a부터 b까지 같은 간격의 값 n개를 생성 |
| `.shape` | 배열의 각 차원 크기를 확인 |

```python
수량 = np.array([2, 1, 3, 5, 1])
print(수량.shape)            # (5,), 원소 5개인 1차원 배열
print(np.zeros(3))           # [0. 0. 0.]
print(np.linspace(0, 1, 5))  # [0.   0.25 0.5  0.75 1.  ]
```

### 벡터 연산 (p.12, p.17-18)

배열과 숫자(스칼라)의 연산은 배열의 **모든 원소**에, 크기가 같은 배열끼리의 연산은 **같은 위치**의 원소끼리 적용된다. 반복문을 쓰지 않는다.

```python
단가 = np.array([4000, 4500, 5000, 5000, 6000])
수량 = np.array([2, 1, 3, 5, 1])

매출 = 단가 * 수량      # 같은 위치의 단가와 수량을 곱해 매출 계산
print(매출)             # [ 8000  4500 15000 25000  6000]
```

> **주의** — `np.array([1, 2, 3]) * 2`는 `[2, 4, 6]`이다 (p.17-18 퀴즈). 리스트였다면 `[1, 2, 3, 1, 2, 3]`으로 길이가 두 배가 된다. 같은 `*`가 자료구조에 따라 전혀 다르게 동작하는 지점.

### 2차원 배열 (p.13)

중첩 리스트 `np.array([[1, 2, 3], [4, 5, 6]])`로 2차원 배열을 만들고, `.shape`는 (행, 열) 튜플, `arr.T`는 행과 열을 전환(전치)한다.

```python
매출표 = np.array([
    [8000, 4500, 15000],   # 서울
    [6000, 5200, 10000]    # 부산
])
print(매출표.shape)   # (2, 3), 2행 3열
print(매출표[0, 1])   # 4500, 0행 1열의 값
print(매출표.T)       # 행과 열을 전환한 3행 2열 배열
```

### reshape와 축 연산 (p.14)

- `reshape(행, 열)` : 원소 수를 유지하면서 형태를 변경. `-1`은 전체 원소 수에 맞춰 해당 차원의 크기를 계산
- `axis=0` : **열별**로 집계 / `axis=1` : **행별**로 집계

```python
arr = np.arange(6)             # [0 1 2 3 4 5]
표 = arr.reshape(2, 3)         # [[0 1 2]
                               #  [3 4 5]]
print(arr.reshape(3, -1))      # 3행 2열, 열 크기를 계산

print(표.sum(axis=0))          # [3 5 7], 열별 합계
print(표.sum(axis=1))          # [3 12], 행별 합계
```

```text
axis=0 ↓ 세로로 접는다            axis=1 → 가로로 접는다
[[0 1 2]                          [[0 1 2] → 3
 [3 4 5]]                          [3 4 5] → 12
 ↓ ↓ ↓
 [3 5 7]  (열별 합)               [3 12]  (행별 합)
```

> **주의** — axis는 "남기는 방향"이 아니라 **접어서 없애는 방향**이다. `axis=0`은 행 방향으로 접으니 열별 결과가 남는다. 헷갈리면 `(2, 3)` 배열의 `sum(axis=0)` 결과 길이가 3(열 수)이라는 것부터 확인.

### 집계 함수 (p.15)

`.mean()` 평균 · `.sum()` 합계 · `.max()` 최댓값 · `.min()` 최솟값 · `.std()` 표준편차(데이터가 평균 기준으로 얼마나 퍼져 있는지). 배열 메서드 `arr.mean()`과 NumPy 함수 `np.mean(arr)` 두 형식 모두 사용 가능.

### 브로드캐스팅(Broadcasting) (p.16)

**크기가 다른 배열을 연산할 수 있도록 작은 배열을 확장하는 방식.** 스칼라는 모든 원소에 적용되고, 1차원 배열과 2차원 배열은 크기가 연산 조건에 맞으면 1차원 배열이 축에 따라 확장된다. `np.newaxis`는 새로운 축을 추가하는 표기로, `(3,)` 배열을 `(3, 1)`처럼 확장할 때 사용.

```python
단가 = np.array([4000, 5000, 6000])          # (3,)
수량 = np.array([[1, 2], [3, 1], [5, 2]])    # (3, 2)

결과 = 단가[:, np.newaxis] * 수량   # (3,1) * (3,2) → (3,2)
print(결과)
# [[ 4000  8000]
#  [15000  5000]
#  [30000 12000]]
```

`(3, 1)`이 열 방향으로 두 번 복제되어 `(3, 2)`끼리의 연산이 된다.

---

## NumPy 필터링과 정렬

### 조건부 필터링 — boolean indexing (p.19)

`배열[조건]`은 조건이 True인 위치의 원소만 추출한다. 조건 결합은 `&`(모두 만족) / `|`(하나 이상 만족), 각 조건은 **괄호**로 구분.

```python
매출 = np.array([8000, 4500, 15000, 25000, 6000])

high = 매출[매출 >= 10000]
print(high)   # [15000 25000]

mid = 매출[(매출 >= 5000) & (매출 <= 15000)]
print(mid)    # [ 8000 15000  6000]
```

### 정렬 (p.20)

- `np.sort(배열)` : **원본을 유지**하고 오름차순으로 정렬한 배열을 반환
- `배열[::-1]` : 순서를 반대로 → `np.sort(배열)[::-1]`이 내림차순
- `np.argsort(배열)` : 오름차순으로 정렬할 **인덱스 순서**를 반환

```python
print(np.sort(매출))        # [ 4500  6000  8000 15000 25000], 오름차순
print(np.sort(매출)[::-1])  # [25000 15000  8000  6000  4500], 내림차순
print(np.argsort(매출))     # [1 4 0 2 3], 오름차순 정렬 기준 인덱스
```

argsort가 돌려주는 것은 값이 아니라 **인덱스**다 — `[1 4 0 2 3]`은 "1번(4500) → 4번(6000) → 0번(8000) → 2번(15000) → 3번(25000) 순으로 뽑으면 오름차순"이라는 뜻. 매출 순위대로 지역 이름을 늘어놓는 것처럼 **다른 배열을 같은 기준으로 정렬**할 때 쓴다.

---

## Pandas DataFrame 기초

### DataFrame과 Series (p.21-23)

- **DataFrame** : 행과 열로 구성된 2차원 표. **같은 인덱스를 공유하는 여러 Series**(1차원 열)의 묶음
- **인덱스(index)** : 각 행을 구분하는 이름(기본 0부터 정수) / **컬럼(columns)** : 열 이름 목록 / `df.values` : 값을 NumPy 배열로 반환
- NumPy 배열과 달리 **열마다 다른 자료형**을 쓸 수 있다 — 지역 열은 문자열, 단가 열은 정수. `df['단가']`처럼 한 열을 꺼내면 타입은 Series

엑셀 개념과의 대응 (p.22):

| 엑셀 | Pandas |
|---|---|
| 시트 | DataFrame |
| 행/열 | DataFrame의 행/열 |
| VLOOKUP | `merge` — 두 표를 공통된 컬럼이나 인덱스를 기준으로 결합 |
| 피벗 테이블 | `groupby` |

### 분석 흐름과 조회 메서드 (p.24-25)

파일 불러오기 → **조회**(`head()`, `info()`) → **필터**(`df[조건]`) → **집계**(`groupby()`) → 시각화 순서로 진행한다.

| 메서드, 속성 | 반환값 | 용도 | 예시 |
|---|---|---|---|
| `head(n)` | DataFrame | 위에서 n개 행을 확인 | `df.head(10)` |
| `tail(n)` | DataFrame | 아래에서 n개 행을 확인 | `df.tail(5)` |
| `info()` | 콘솔 출력 | 컬럼, 자료형, 결측값을 확인 | `df.info()` |
| `describe()` | DataFrame | 수치형 데이터의 요약 통계를 확인 | `df.describe()` |
| `shape` | 튜플 | 행과 열의 수를 확인 | `df.shape` |
| `dtypes` | Series | 각 컬럼의 자료형을 확인 | `df.dtypes` |
| `columns` | Index | 컬럼 이름을 확인 | `df.columns` |

`head(n)`의 n을 생략하면 5개 행을 반환하고, `df['지역'].value_counts()`로 컬럼 값별 행 수(빈도)를 확인한다 (p.29).

### 실습 데이터 만들기 (p.26-27)

이후 예제 전체가 쓰는 카페 판매 데이터. 컬럼은 지역(서울/부산/대전/광주), 메뉴, 단가(4,000~6,000), 수량(1~5), 매출(단가×수량), 총 200행.

```python
import numpy as np
import pandas as pd

np.random.seed(42)
n = 200
지역 = np.random.choice(['서울', '부산', '대전', '광주'], n)
메뉴 = np.random.choice(['아메리카노', '카페라떼', '바닐라라떼'], n)
단가 = np.random.choice([4000, 4500, 5000, 5500, 6000], n)
수량 = np.random.randint(1, 6, n)

df = pd.DataFrame({'지역': 지역, '메뉴': 메뉴, '단가': 단가, '수량': 수량})
df['매출'] = df['단가'] * df['수량']
print(df.shape)   # (200, 5)
```

- `seed(42)`는 난수 고정(같은 데이터 재현), `choice()`는 목록에서 임의 선택. 새 컬럼은 `df['매출'] = ...` 대입으로 생성 — 내부는 NumPy 벡터 연산
- `np.random.randint(1, 6, n)`의 끝값 6은 **포함되지 않는다** (1~5)

### 파일 입출력 (p.28)

```python
df = pd.read_csv('sales.csv')         # 파일 → DataFrame
df.head()                             # 잘 읽혔는지 확인
df.to_csv('result.csv', index=False)  # 저장 (인덱스 제외)
```

- 한글이 깨지면 `encoding='utf-8-sig'`(유니코드 기반) 또는 `'cp949'`(주로 윈도우 한글 환경)를 지정
- 저장할 때 `index=False`를 빼면 인덱스가 열로 함께 저장된다

---

## 행 선택과 필터링

### 컬럼 선택과 조건 필터 (p.30)

- `df["컬럼"]` : 하나의 컬럼을 **Series**로 반환
- `df[조건]` : 조건이 True인 **행**만 반환
- `&` 모두 만족 / `|` 하나 이상 만족 — 각 조건은 괄호로 구분

```python
# 서울 지역만
df[df['지역'] == '서울']

# 매출 1만원 이상이면서 아메리카노만
df[(df['매출'] >= 10000) & (df['메뉴'] == '아메리카노')]
```

### 자주 하는 실수 (p.31)

`and`/`or`를 쓰면 `ValueError: ambiguous` — 반드시 `&`/`|`를 쓰고, 각 조건을 괄호로 감싼다.

```python
# 괄호 누락 (에러)
df[df['매출'] >= 10000 & df['메뉴'] == '아메리카노']   # TypeError

# 올바른 작성
df[(df['매출'] >= 10000) & (df['메뉴'] == '아메리카노')]
```

> **왜 괄호가 필수인가** — 파이썬에서 `&`가 `>=` 같은 비교보다 우선순위가 높아, 괄호가 없으면 `10000 & df['메뉴']`부터 계산하려다 TypeError가 난다.

### 다중 값 필터와 문자열 검색 (p.32)

- `.isin([값1, 값2])` : 목록에 포함된 값을 가진 행을 선택 — 같은 컬럼의 "A 또는 B" 조건은 `|`를 반복하는 것보다 짧고 읽기 쉽다
- `.str.contains("문자")` : 값에 특정 문자가 포함된 행 / `.str.startswith("문자")` : 특정 문자로 시작하는 행

```python
# 아메리카노 또는 카페라떼만
df[df['메뉴'].isin(['아메리카노', '카페라떼'])]

# 메뉴명에 '라떼' 포함
df[df['메뉴'].str.contains('라떼')]
```

### 상위·하위 행 추출 (p.33)

- `.nlargest(n, "컬럼")` / `.nsmallest(n, "컬럼")` : 지정한 컬럼 기준으로 값이 큰/작은 n개 행을 반환
- `.sample(n)` : 무작위로 n개 (`random_state`로 고정)

```python
df.nlargest(5, '매출')    # 매출 상위 5개 주문
df.nsmallest(3, '수량')   # 수량 하위 3개 주문
df.sample(10, random_state=42)
```

### loc 라벨 인덱싱 (p.34)

- `.loc[행 조건, 열 이름]` : 행의 조건과 열 이름(라벨)으로 데이터를 선택, 값 **변경**에도 사용
- `.iloc[행 위치, 열 위치]` : 정수 위치로 행과 열을 선택

```python
# 서울 지역의 매출만 조회
df.loc[df['지역'] == '서울', '매출']

# 조건에 맞는 값 대입 (조건부 대입)
df.loc[df['단가'] < 4000, '단가'] = 4000

df.iloc[0, 1]   # 0행 1열 (위치 기반)
```

> **df[조건]과 loc의 구분** — 행만 거를 때는 `df[조건]`, 행과 열을 **함께** 고르거나 골라낸 자리에 값을 대입할 때는 `.loc`. 조건부 대입이 loc의 대표 사용처다.

---

## groupby와 pivot_table

### groupby 기본 (p.35)

`groupby("컬럼")`은 같은 값을 가진 행을 그룹으로 묶고, 그룹별로 `sum`, `mean`, `count`, `max` 등의 집계 함수를 적용한다. 읽는 순서는 **묶고(groupby) → 고르고(집계할 컬럼) → 집계한다(sum)**.

```python
df.groupby('지역')['매출'].sum()    # 지역별 총매출
df.groupby('메뉴')['단가'].mean()   # 메뉴별 평균 단가
```

### 다중 groupby와 agg (p.36)

- `groupby(["컬럼1", "컬럼2"])` : 두 개 이상의 컬럼을 기준으로 그룹 생성 — 결과에 **계층형 인덱스(MultiIndex)**가 생성됨
- `.agg({...})` : 컬럼마다 서로 다른 집계 함수를 적용
- `.reset_index()` : 그룹 기준으로 사용한 인덱스를 일반 컬럼으로 변환

```python
# 지역별 + 메뉴별 총매출 (MultiIndex)
df.groupby(['지역', '메뉴'])['매출'].sum()

# 지역별로 매출 합, 평균, 수량 합을 동시에
df.groupby('지역').agg(
    매출합=('매출', 'sum'),
    매출평균=('매출', 'mean'),
    수량합=('수량', 'sum')
)
```

### pivot_table (p.37)

행과 열의 **두 기준**으로 데이터를 교차 집계한다.

| 매개변수 | 역할 |
|---|---|
| `index` | 세로축으로 사용할 기준 |
| `columns` | 가로축으로 사용할 기준 |
| `values` | 집계할 값 |
| `aggfunc` | `sum`, `mean`과 같은 집계 함수 |

```python
# 지역 × 메뉴 별 매출 합계, 평균
df.pivot_table(index='지역',
               columns='메뉴',
               values='매출',
               aggfunc=['sum', 'mean'])
```

`groupby(['지역', '메뉴'])`가 세로로 길게 쌓는 결과를, pivot_table은 하나의 기준을 **가로축으로 배치**해 지역×메뉴 격자로 펼친다.

### groupby 주의사항 (p.38)

- `["매출"]` : 하나의 집계 컬럼을 선택해 **Series**로 반환 / `[["매출", "수량"]]` : 여러 컬럼을 선택해 **DataFrame**으로 반환
- `count()` : **NaN을 제외한** 값의 개수 / `size()` : **NaN을 포함한** 전체 행 수. `mean()`도 NaN을 제외하고 계산
- NaN(Not a Number) : 판다스에서 수치형 데이터의 결측값을 나타낼 때 주로 사용하는 특수한 값

```python
df.groupby('지역')['매출'].sum()             # Series 반환
df.groupby('지역')[['매출', '수량']].sum()   # DataFrame 반환

df.groupby('지역')['메뉴'].count()   # NaN 제외 개수
df.groupby('지역').size()            # NaN 포함 전체 행 수
```

> **원문 주의** — 이 페이지에는 "`inplace=True` : 원본을 변경하므로 필요한 경우 결과를 새로운 변수에 저장"이라는 불릿도 있는데, groupby 자체에는 inplace 매개변수가 없다. `sort_values()`, `drop()` 같은 일반 메서드에 해당하는 이야기로, 원본을 유지하려면 inplace를 쓰지 말고 결과를 새 변수에 받으면 된다.

groupby 빈칸 퀴즈(p.39-40)는 복습 문제 4번으로 옮겼다.

---

## 핵심 키워드 정리

확인 문제(p.41-47)는 아래 복습 문제 1~3번으로 옮겼다. 활동 정리 슬라이드의 키워드 표 (p.48):

| 주제 | 키워드 | 핵심 |
|---|---|---|
| NumPy 배열 | `np.array()`, `np.arange()`, `.shape` | 배열 생성과 형태 확인 |
| 배열 연산 | 브로드캐스팅, `axis=0`, `axis=1` | 배열 연산과 축별 계산 |
| 배열 형태와 정렬 | `reshape()`, `.T`, `np.sort()`, `np.argsort()` | 배열 형태 변경, 전치, 정렬 |
| 집계 함수 | `.mean()`, `.sum()`, `.max()`, `.min()`, `.std()` | 배열 값 집계 |
| DataFrame 기초 | DataFrame, Series, index, columns | 행과 열로 구성된 표 형태의 자료구조 |
| 조회와 필터 | `.head()`, `.info()`, `df[조건]`, `&`, `\|` | 데이터 확인과 조건별 선택 |
| 다중 값, 문자열 검색 | `.isin()`, `.str.contains()`, `.str.startswith()` | 목록과 문자열 조건으로 행 선택 |
| 행 추출과 인덱싱 | `.nlargest()`, `.nsmallest()`, `.loc`, `.iloc` | 상위, 하위 행 추출과 레이블, 위치 기반 선택 |
| 그룹 집계 | `groupby()`, `.agg()`, `pivot_table()` | 그룹별 요약과 교차 집계 |

오늘 공부한 내용 요약 슬라이드(p.49)는 위 표와 같은 내용의 문장 버전이라 표로 갈음한다.

---

## 정리 체크리스트

- [ ] 리스트와 NumPy 배열의 `* 2` 결과 차이와, 속도 차이의 원인(참조 저장 vs 연속 메모리)을 설명할 수 있다
- [ ] `np.arange`, `np.zeros`, `np.linspace`, `reshape(3, -1)`의 결과를 정확히 쓸 수 있다
- [ ] `axis=0`(열별)과 `axis=1`(행별)의 집계 방향을 헷갈리지 않는다
- [ ] 브로드캐스팅의 세 가지 케이스와 `np.newaxis`의 용도를 설명할 수 있다
- [ ] `np.sort`와 `np.argsort`의 반환값 차이를 말할 수 있다
- [ ] DataFrame과 Series의 관계, NumPy 배열과의 자료형 정책 차이를 설명할 수 있다
- [ ] 다중 조건 필터에서 `&`/`|`와 괄호가 필요한 이유를 설명할 수 있다
- [ ] `isin`, `str.contains`, `nlargest`를 각각 언제 쓰는지 안다
- [ ] `.loc`로 조건부 대입을 작성할 수 있다
- [ ] groupby에서 `['매출']`/`[['매출', '수량']]`의 반환형 차이와 `count()`/`size()`의 NaN 처리 차이를 설명할 수 있다
- [ ] groupby와 pivot_table의 공통점과 차이를 말할 수 있다

## 복습 문제

**1.** 다음 중 NumPy 배열의 벡터 연산을 올바르게 설명한 것은? (p.42-43)

① `배열 * 2`는 배열을 두 번 이어 붙임
② 각 원소를 계산하려면 반복문을 작성해야 함
③ 반복문 없이 배열의 각 원소에 산술 연산을 적용할 수 있음
④ 하나의 배열에 서로 다른 자료형을 자유롭게 저장할 수 있음

<details><summary>답</summary>

**③.** ①은 파이썬 리스트의 반복 연산에 해당. ②는 벡터 연산을 사용하면 반복문을 작성하지 않아도 되므로 오답. ④는 NumPy 배열이 같은 자료형의 데이터를 저장하므로 오답.

</details>

**2.** Pandas DataFrame에서 부서별 급여의 평균을 구하는 코드는? (p.44-45)

① `df['급여'].sum()`
② `df.groupby('부서')['급여'].mean()`
③ `df.sort_values('급여')`
④ `df['부서'].value_counts()`

<details><summary>답</summary>

**②.** 부서를 기준으로 그룹화한 후 급여의 평균을 계산한다. ①은 전체 급여의 합계, ③은 급여 기준 행 정렬, ④는 부서별 행의 개수.

</details>

**3.** DataFrame에서 나이가 30 이상인 행만 선택하는 코드는? (p.46-47)

① `df[df['나이'] >= 30]`
② `df['나이' >= 30]`
③ `df.filter('나이' >= 30)`
④ `df.loc[나이 >= 30]`

<details><summary>답</summary>

**①.** 나이 컬럼에 조건을 적용해 조건이 True인 행을 선택한다. ②는 문자열 `"나이"`와 숫자 30을 비교하므로 오류 발생, ③은 filter()에 조건식을 전달하는 형식이 아니므로 오답, ④는 나이를 컬럼으로 지정하지 않아 오류 발생.

</details>

**4.** "메뉴별 총 판매 수량"을 구하도록 빈칸을 채우시오 (p.39-40). `df.groupby('___')['___'].___()`

<details><summary>답</summary>

```python
df.groupby('메뉴')['수량'].sum()
```

기준 컬럼(메뉴)으로 묶고, 집계할 컬럼(수량)을 고른 뒤, 집계 함수(sum)를 적용한다.

</details>

---

> **읽은 방식 메모** — 50페이지 전부를 1100px 이미지로 렌더링해 육안으로 판독했다 (OCR 미사용). 코드 블록과 표의 값은 슬라이드 원문을 그대로 옮겼다. 버린 페이지: 표지(p.1), 전체 목차(p.2), 간지(p.3, p.21, p.41), 마무리 인사(p.50). 원본 특이사항 두 곳 — p.29의 예시 스크린샷(df.head, df.info 결과 그림)은 본문 실습 데이터(200행, 지역·메뉴·단가·수량·매출)와 다른 데이터(100행, 음료 컬럼)를 보여주는 장식용 그림이라 코드 주석 기준으로 정리했고, p.38의 inplace 불릿은 groupby가 아닌 일반 pandas 메서드에 해당한다(본문 주의 블록 참고).
