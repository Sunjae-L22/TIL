<!--
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
티스토리 발행 정보  (이 주석 블록은 화면에 보이지 않습니다)

제목    : [VLM] 이미지 파운데이션 모델 완전 정리 — CLIP, LLaVA, 그리고 작아지는 VLM
카테고리 : AI / 딥러닝
태그    : VLM, 파운데이션모델, CLIP, SigLIP, LLaVA, Qwen-VL, InternVL, 멀티모달, 대조학습, 제로샷, sVLM, SmolVLM, 온디바이스AI, SSAFY, TIL
요약    : LLM은 사고를 채웠지만 지각이 비어 있었다. 이 글은 그 빈칸을 메운 과정 —
          CLIP이 이미지와 언어를 하나의 공간에 묶은 방법, 그 인코더를 LLM 앞에 붙여 VLM을 만든 레시피,
          그리고 그 VLM을 스마트폰에 올리기 위해 무엇을 줄였는지 — 를 한 줄기로 이었다.

이미지 업로드 순서 (본문 등장 순)
 1. 01-ideal-vs-real.png        2. 02-cognition-gap.png
 3. 03-clip-matrix.png          4. 04-temperature.png
 5. 05-zeroshot-classifier.png  6. 06-softmax-vs-sigmoid.png
 7. 07-vlm-recipe.png           8. 08-llava-stages.png
 9. 09-mrope.png               10. 10-set-of-mark.png
11. 11-image-token-memory.png  12. 12-korean-tokenizer.png
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-->

이미지 모델을 하나 학습시켜 서비스에 올려 본 사람은 같은 벽에 부딪힌다. **"학습시킨 것과 조금만 달라도 왜 이렇게 못 맞히지?"**

기존 이미지 모델은 학습한 도메인과 비슷한 데이터에서"만" 잘 작동했다. 그런데 실제 서비스에는 예측하지 못한 사진이 들어온다. 스케치가 들어오고, 그림체가 이상한 렌더링이 들어오고, 조명이 무너진 사진이 들어온다. 학습 데이터가 넉넉하지도 GPU가 많지도 않은 팀이라면 도메인마다 모델을 새로 만드는 선택지는 애초에 없다.

이 벽을 넘는 답이 **파운데이션 모델**이고, 이미지 쪽의 대표 주자가 CLIP이다. 그리고 CLIP이 만든 "이미지와 언어를 하나의 공간에 묶는" 기술은 곧바로 다음 질문으로 이어진다. **LLM은 사고는 하는데 볼 줄을 모른다. 여기에 눈을 달 수는 없을까?** 이 글은 그 눈을 다는 과정 — 파운데이션 모델이라는 개념에서 출발해, CLIP으로 시각과 언어를 정렬하고, 그 인코더를 LLM 앞에 붙여 VLM을 만들고, 마지막으로 그 VLM을 스마트폰에 올리기까지 — 를 한 줄기로 정리한 글이다.

> 이 글은 [RNN부터 ViT까지 완전 정리]에서 이어진다.

ViT, 셀프 어텐션, 쿼리·키·값, 트랜스포머 인코더 블록은 앞 글에서 다뤘으므로 여기서는 다시 설명하지 않는다. 이 글에서 필요한 사전 지식은 딱 한 문장으로 압축된다 — **ViT는 이미지를 16×16 패치로 잘라 토큰처럼 만든 뒤 트랜스포머 인코더에 넣는 모델이고, 트랜스포머는 토큰 배열을 받아 토큰 사이의 관계를 어텐션으로 계산하는 구조다.** 이 문장만 들고 오면 아래 내용은 전부 따라온다.

<br>

## 📖 이 글에서 다루는 내용

1. 파운데이션 모델 — 검색엔진과 압축 사이의 절충
2. 적응 활용 — 제로샷, 퓨샷, 파인튜닝의 구분
3. 인지 파이프라인의 빈칸 — LLM에 눈을 달다
4. CLIP — 자연어를 출력 인터페이스로 삼은 모델
5. 대조 학습의 실제 — N×N 행렬, 대칭 손실, temperature
6. 제로샷 이미지 인식기 — CLIP을 분류기로 쓰는 법
7. SigLIP — softmax를 sigmoid로 바꾸면 무엇이 달라지나
8. 멀티모달 정합 — 여러 모달리티를 한 공간에
9. 심화 — CLIP을 손실함수로 뒤집어 쓰기
10. VLM — LLM에 눈을 다는 레시피와 LLaVA
11. Qwen-VL 계열 — 해상도와 위치를 다시 설계하다
12. InternVL — 눈도 같이 키운다
13. Set of Mark — 번호를 붙이면 갑자기 잘 본다
14. 도메인 특화 파운데이션 모델들
15. sVLM — 손바닥 위의 VLM
16. 한국어 sVLM — 토크나이저부터 손해를 본다
17. 실습 — LMDeploy로 VLM 배포하기
18. 핵심 요약

<br>

---

## 1. 파운데이션 모델 — 검색엔진과 압축 사이의 절충

정의부터 외우면 이 개념은 잘 안 붙는다. 대신 **"이상적인 AI 모델은 무엇일까"** 라는 사고 실험에서 출발하면 파운데이션 모델이 왜 그 모양인지가 한 번에 보인다.

AI 모델은 결국 함수다. 그것도 세 가지 조건이 붙은 함수다 — 입출력을 연결하고, 그 형태가 데이터로부터 결정되며, **학습 때 보지 못한 데이터에 대해서도 작동해야 하는** 함수. 마지막 조건이 어렵다.

### 이상적인 모델 — 모든 것을 기억하는 검색엔진

만약 모델이 이 세상에서 발생 가능한 모든 데이터와 그 설명을 전부 기억하고 있다면 어떨까? 내가 원하는 답과 비슷한 답이 이미 DB에 들어 있을 확률이 높다. 이 모델의 정체는 사실 **AI가 아니라 검색 엔진**이다. 검색 포털이나 최근접 이웃 탐색(Nearest Neighbor Search)이 그 구조다.

문제는 비용이다. 데이터 확보·저장·탐색 비용이 전부 감당이 안 된다. 세상의 모든 이미지를 저장할 수도, 매 질의마다 전부 뒤질 수도 없다.

### 현실적인 모델 — 압축과 표현 학습

그래서 현실은 반대편을 택했다. **학습이란 데이터를 패턴화해서 모델 안에 압축(compress)하는 것**이다. 수많은 이미지를 몇억 개의 파라미터로 욱여넣는 과정에서 모델은 "무엇이 비슷하고 무엇이 다른가"를 파악하게 되고, 그 패턴 덕분에 처음 보는 데이터에도 일반화가 된다. 이것이 **표현 학습(representation learning)** 이다.

![모든 데이터를 저장해 찾아 쓰는 이상적 검색엔진 모델과 데이터를 파라미터로 압축하는 현실적 뉴럴넷 모델을 양 끝에 두고 그 사이 절충점으로서 대규모 사전학습을 하는 파운데이션 모델의 위치를 보여주는 다이어그램](01-ideal-vs-real.png)

파운데이션 모델의 아이디어는 이 두 극단 사이의 절충이다. 전부 기억하는 건 불가능하지만, **"압축하되 세상의 수많은 데이터를 최대한 많이 담아 보자"** 는 것. 그래서 대규모 사전학습이라는 답이 나온다.

> **파운데이션 모델 (foundation model)** — 대규모 데이터로 사전학습한 후, 다양한 문제·작업에 범용적으로 빠르게 적응시켜 활용할 수 있는 기반 AI 모델. 스탠포드 HAI가 2021년 보고서(Bommasani et al., "On the Opportunities and Risks of Foundation Models")에서 새로운 범주로 구분하기 시작했다.

### 세 가지 특징

| 특징 | 내용 |
|---|---|
| **대규모 사전학습** | 트랜스포머 + 대규모 데이터. 태스크와 무관하게 비슷한 패턴이 재활용된다. 상당수가 비지도 학습이라 라벨링 없이 데이터를 긁어모을 수 있다 |
| **적응성(adaptability)** | 미세조정 성능이 높다. 믿고 쓸 수 있는 모델 하나를 여러 목적에 적응시킨다 |
| **범용성** | 다양한 작업과 **한정되지 않는 출력**을 지원한다. 기존 분류기가 20여 개 클래스에 묶여 있었다면, 파운데이션 모델은 만 개 이상 또는 자연어로 정의되는 무제한 대상을 다룬다 |

> 💡 **"한정되지 않는 출력"이 결정적 차이다**
> 기존 분류기는 softmax 출력 차원이 클래스 수로 고정되어 있다. 클래스를 하나 추가하려면 헤드를 새로 만들고 다시 학습해야 한다. 파운데이션 모델은 **자연어를 출력 인터페이스로 삼아** 클래스 집합을 런타임에 바꾼다. 이 차이가 이 글 전체의 통주저음이다.

개발 프로세스도 이 관점에서 5단계로 정리된다.

```text
Data Creation → Data Curation → Training → Adaptation → Deployment
(뉴스·이미지·    (수집·정제해     (파운데이션   (태스크별     (사용자에게
 문서·코드 등)    데이터셋 구축)    모델 학습)    적응·튜닝)    서비스 제공)
```

여기서 앞의 세 단계(Training까지)는 대규모 리소스가 드는 일이라 소수 기관만 할 수 있다. **대부분의 개발자가 실제로 하는 일은 Adaptation부터**다. 그래서 다음 절이 필요하다.

<br>

---

## 2. 적응 활용 — 제로샷, 퓨샷, 파인튜닝의 구분

과거에는 태스크마다 모델을 새로 **학습**했지만, 이제는 잘 학습된 모델을 얼마나 잘 **활용**하느냐가 핵심이다. 활용 기법은 프롬프트 엔지니어링·프롬프트 튜닝, 전이학습, 적응 학습, 파인튜닝 등으로 다양한데, 시험이든 실무든 먼저 갈라야 하는 축은 하나다. **모델 가중치가 바뀌는가?**

| 기법 | 정의 | 가중치 변경 |
|---|---|---|
| **Zero-shot** | 처음 보는 문제를 추가 학습 없이 바로 적용. 모델이 이미 가진 배경 지식만 쓴다 | 없음 |
| **Few-shot** | 예제 몇 개만 보여주면 바로 적용 | 없음 |
| **Fine-tuning** | 처음부터 배우지 않고 조금만 알려주면 금방 적응 | **있음** |

```text
대규모 데이터
     ↓
Pre-training (사전 학습) — 폭넓은 기본 지식 습득
     ↓
Zero-shot / Few-shot (추가 학습 없이)  |  Fine-tuning (특정 태스크에 추가 학습)
```

> ⚠️ **"퓨샷이면 예제로 학습하는 것 아닌가"는 흔한 착각이다**
> 퓨샷의 예제는 **프롬프트 안에 들어가는 입력**이지 학습 데이터가 아니다. 대화가 끝나면 사라진다. 반면 파인튜닝은 가중치 파일 자체가 바뀌므로 결과물이 남는다. "학습(learning)"이라는 단어가 두 자리에 다 쓰여서 헷갈리는데, 갈림길은 항상 가중치가 바뀌었는지 하나다.

<br>

---

## 3. 인지 파이프라인의 빈칸 — LLM에 눈을 달다

여기서 관점을 한 번 크게 벌려야 한다. 왜 하필 "이미지와 언어"를 붙이는 연구가 이렇게 폭발했는가.

강의는 AGI를 향한 로드맵으로 인간 지능의 구조를 빌려온다. 인간의 지능(cognition)은 **지각(perception)과 고차 인지 과정(higher cognitive processes)의 합집합**이라는 것이다. 실제 세계를 인지하는 과정은 네 블록으로 나뉜다.

![실제 세계에서 지각과 인지를 거쳐 해석에 이르는 인지 파이프라인에서 LLM이 인지 블록만 채웠고 지각 블록이 비어 있으며 그 자리에 이미지 인코더를 꽂는 것이 VLM임을 보여주는 다이어그램](02-cognition-gap.png)

```text
World          →  Perception  →  Cognition  →  Interpretation
(실제 3D 세계)     (눈: 망막에      (뇌: 사고·      "A man standing next to
                    맺힌 상)         추론)          the pavilion with a gray roof!"
```

2022년 11월 이전까지 AI는 각 분야가 이 파이프라인의 매우 부분적인 능력만 따로 모델링하고 있었다. ChatGPT 이후 LLM이 여러 인지 능력 벤치마크에서 인간 수준에 근접하면서 **Cognition 블록이 채워졌다.** 그런데 결론은 이랬다.

> ➡️ **사고 능력과 언어 능력만으로는 현실 세계를 이해하기에 충분하지 않다.** Perception 블록이 비어 있기 때문이다.

여기서 "LLM에 눈을 달자"는 발상이 나온다. 그 결과물이 **VLM(Vision-Language Model)** 이고, 최소 구조는 놀랄 만큼 단순하다.

```text
이미지  → [이미지 인코더] → 토큰(Tokens) ┐
                                        ├→ [언어모델] → "빨간 스카프를 두르고 있는
텍스트 "이 이미지를 설명해줘" → 토큰 ────┘              고양이 사진이에요"
```

핵심 아이디어는 **이미지를 언어모델이 먹을 수 있는 토큰 시퀀스로 바꾸어 텍스트 토큰과 나란히 이어 붙이는 것**이다. 언어모델 입장에서는 입력이 어차피 전부 토큰 배열이므로 구조를 거의 바꿀 필요가 없다.

### 이게 왜 지금 중요한가 — 두 가지 사례

**(1) GPT-4의 멀티모달 입력.** 자연어에 국한되던 LLM이 이미지·문서·음성을 함께 처리하게 되면서 응용이 튀었다. 제조 결함 검출이 대표적이다. "왼쪽 이미지가 결함을 가지고 있는지 판단하고, 있다면 구체적 이유를 알려줘"라는 질문에 GPT-4V는 "원형 물체 가장자리에 이물질이 보이고, 긁힌 자국이 있으며, 줄에 마모된 듯한 불규칙한 부분이 있다"고 답한다. **결함 유형을 미리 학습시키지 않고도** 자연어 지시만으로 이상 탐지가 되는 것 — 앞서 말한 "한정되지 않는 출력"의 실물이다. 손으로 그린 목업 사진을 주고 "이걸 동작하는 웹사이트로 만들어줘"라고 하면 HTML/CSS/JS가 나오는 sketch-to-webpage도 같은 계열이다.

**(2) Claude의 Computer use.** AI 에이전트가 스스로 여러 프로그램을 조작해 태스크를 푸는 방향인데, 여기에는 근본적인 문제가 있다. **에이전트와 기존 응용 프로그램을 잇는 표준 인터페이스가 없다.**

> 💡 **Computer use가 왜 VLM 문제인가**
> 앱마다 API를 붙이는 대신 "화면을 스크린샷으로 찍어 읽는다"를 택하면, 모든 프로그램에 대해 **픽셀이라는 하나의 인터페이스**로 통일된다. 호환성이 압도적으로 좋아지는 대신, 모델이 화면을 정확히 읽어낼 수 있어야 한다는 조건이 붙는다. 즉 시각 이해가 선택이 아니라 필수가 된다.

<br>

---

## 4. CLIP — 자연어를 출력 인터페이스로 삼은 모델

**CLIP(Contrastive Language-Image Pre-training, OpenAI, 2021)** 은 AI가 언어와 시각을 통합해 이해하는 방식을 보여준 패러다임 전환이다. 파운데이션 모델로서의 특징은 입력과 출력 양쪽에 걸쳐 있다.

- **입력** — 학습하지 않은 새로운 도메인의 데이터에도 좋은 성능을 낸다 (제로샷 전이)
- **출력** — 자연어를 이용해 한 번도 본 적 없는 카테고리도 **텍스트 설명만으로 출력 정의가 가능**하다

학습 신호는 한 문장이다. **언어와 이미지의 유사도를 배운다.**

```text
Anchor(강아지 사진)
   ├─ 가깝게 ──→  "Dog"                    (양성 / positive)
   └─ 멀게  ──→  "Bicycle", "Computer"     (음성 / negatives)
```

### 제로샷 성능표는 이렇게 읽는다

CLIP 논문의 대표 표인데, 잘못 읽기 쉽다.

| 데이터셋 | ImageNet ResNet101 | CLIP ViT-L |
|---|---|---|
| ImageNet (ResNet101의 학습 데이터) | 76.2% | 76.2% |
| ImageNet V2 | 64.3% | 70.1% |
| ImageNet Rendition | 37.7% | 88.9% |
| ObjectNet | 32.6% | 72.3% |
| ImageNet Sketch | 25.2% | 60.2% |
| ImageNet Adversarial | 2.7% | 77.1% |

첫 줄에서 두 모델은 **76.2%로 동점**이다. 그런데 ImageNet은 ResNet101이 직접 학습한 데이터다. 분포가 달라진 나머지 다섯 개 데이터셋에서 ResNet101은 무너지고(최악 2.7%), CLIP은 60~89%를 유지한다.

> ➡️ **CLIP의 강점은 "더 높은 정확도"가 아니라 분포 변화(distribution shift)에 대한 강건성이다.** 서두에서 말한 "학습한 것과 조금만 달라도 못 맞히는" 문제에 대한 답이 이 표 한 장에 있다.

### 학습 데이터 — 4억 쌍을 어떻게 모았나

CLIP은 인터넷에서 수집한 **4억(400M) 개의 이미지-텍스트 쌍**으로 학습했다. 사람이 라벨을 붙인 게 아니라 웹페이지의 `alt` 속성, 이미지 캡션, 제목을 긁어왔다.

```html
<img src="/imgs/cat-sleeping.jpg" alt="A Cat sleeping on a blanket">
```

여기에 중복 이미지, 저해상도·저품질 이미지, 너무 짧은 텍스트를 걷어내는 정제 과정을 거친다. 데이터셋 규모의 변화를 보면 이 접근이 왜 판을 바꿨는지가 보인다.

| 데이터셋 | 규모 | 종류 | 대략 연도 |
|---|---|---|---|
| CIFAR | 60K | 사람이 붙인 클래스 라벨 | ~2009 |
| ImageNet | 14M | 사람이 붙인 클래스 라벨 | ~2009 |
| CLIP | 400M | 이미지-텍스트 쌍 | 2021 |
| ALIGN | 1.8B | 이미지-텍스트 쌍 | ~2021 |
| CoCa | 4.8B | 이미지-텍스트 쌍 | ~2022 |
| LAION | 5.8B | 이미지-텍스트 쌍 | ~2022 |

사람이 라벨링한 데이터셋(6만~1,400만)과 alt-text를 긁어온 쌍(4억~58억) 사이에는 **1,000배 이상**의 차이가 있다. **라벨링 비용을 안 들이는 것이 곧 스케일이다.**

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> 원본 표기 `LIAON`은 `LAION`(Large-scale Artificial Intelligence Open Network), `Coca`는 `CoCa`(Contrastive Captioner)의 표기 오류라 바로잡았다.

### 인코더 두 개

| 역할 | 모델 | 입력 단위 |
|---|---|---|
| 텍스트 인코더 | Transformer (**Encoder only**) | 서브워드(sub-word) 임베딩 |
| 이미지 인코더 | ViT-B (또는 ResNet50) | 패치(patch) 임베딩 |

두 인코더 모두 앞 글에서 다룬 구조 그대로다. 텍스트 쪽은 GPT 계열과 같은 서브워드 룩업 테이블(어휘 5만 257개 × 임베딩 차원 768~1600)을 쓰고, 이미지 쪽은 16×16 패치 분할 → 평탄화 → 선형 투영 → [class] 토큰 추가 + 위치 임베딩 → 트랜스포머 인코더 순서다. **ViT와 다른 점은 마지막 MLP 분류 헤드 자리에 대조 학습 목적함수가 붙는다는 것 하나뿐이다.**

<br>

---

## 5. 대조 학습의 실제 — N×N 행렬, 대칭 손실, temperature

대조 학습(contrastive learning)의 기준은 간단하다.

- 앵커(anchor, 목표 이미지)를 대응하는 텍스트(**양성**)와 **가깝게**
- 일치하지 않는 여러 텍스트(**음성**)와는 **멀게**

이렇게 하면 모델은 **의미에 따른 거리 감각**을 배운다. 문제는 "양성 하나에 음성을 몇 개나 붙일 것인가"인데, CLIP의 답이 아름답다. **미니배치 자체를 음성 공급원으로 쓴다.**

![배치 크기 N의 이미지 임베딩과 텍스트 임베딩이 만드는 N×N 코사인 유사도 행렬에서 대각선 N개가 양성 쌍이고 나머지가 음성 쌍이며 행 방향은 이미지에서 텍스트로 열 방향은 텍스트에서 이미지로 향하는 두 손실이 됨을 보여주는 다이어그램](03-clip-matrix.png)

배치 크기가 N이면 이미지 임베딩 N개와 텍스트 임베딩 N개가 나오고, 이들의 코사인 유사도를 전부 계산하면 N×N 행렬이 만들어진다.

```text
        T_1     T_2     T_3   …   T_N
 I_1  [I_1·T_1] I_1·T_2 I_1·T_3 … I_1·T_N
 I_2   I_2·T_1 [I_2·T_2] I_2·T_3 … I_2·T_N
 I_3   I_3·T_1  I_3·T_2 [I_3·T_3] … I_3·T_N
  ⋮
 I_N   I_N·T_1  I_N·T_2  I_N·T_3 … [I_N·T_N]

→ 대각선 N개가 양성 쌍, 나머지 N²−N개가 전부 음성 쌍
```

배치가 커질수록 음성이 제곱으로 늘어난다. 음성 데이터를 따로 모을 필요가 없다는 것이 이 설계의 핵심이다.

### 의사코드

논문에 실린 numpy 스타일 의사코드가 구조를 가장 정확하게 보여준다.

```python
# image_encoder - ResNet or Vision Transformer
# text_encoder  - CBOW or Text Transformer
# I[n, h, w, c] - 정렬된 이미지 미니배치
# T[n, l]       - 정렬된 텍스트 미니배치
# W_i, W_t      - 각 모달리티를 공통 임베딩 공간으로 보내는 학습 가능한 투영
# t             - 학습되는 temperature 파라미터

# 각 모달리티의 특징 추출
I_f = image_encoder(I)  # [n, d_i]
T_f = text_encoder(T)   # [n, d_t]

# 공통 멀티모달 임베딩으로 투영 후 L2 정규화 [n, d_e]
I_e = l2_normalize(np.dot(I_f, W_i), axis=1)
T_e = l2_normalize(np.dot(T_f, W_t), axis=1)

# 스케일이 적용된 쌍별 코사인 유사도 [n, n]
logits = np.dot(I_e, T_e.T) * np.exp(t)

# 대칭 손실 — 정답은 항상 대각선이므로 라벨이 곧 0,1,2,…,n-1
labels = np.arange(n)
loss_i = cross_entropy_loss(logits, labels, axis=0)   # 열 방향
loss_t = cross_entropy_loss(logits, labels, axis=1)   # 행 방향
loss   = (loss_i + loss_t) / 2
```

L2 정규화를 먼저 하기 때문에 내적이 곧 코사인 유사도가 된다. 그리고 **정답 라벨이 `np.arange(n)`, 즉 "i번째 이미지의 정답은 i번째 텍스트"** 라는 점이 요령이다. 사람이 라벨을 붙이지 않아도 배치를 구성하는 순간 정답이 공짜로 생긴다.

### 손실 함수 — 두 항의 차이는 인덱스 순서 하나

`s_ij`를 i번째 이미지와 j번째 텍스트 임베딩의 코사인 유사도, τ를 학습되는 temperature라 하면 손실은 이렇게 쓴다.

```text
              1 (                  exp(s_ii / τ)                          exp(s_ii / τ)          )
L_CLIP  =  ── ( − (1/N) Σ_i log ───────────────────  − (1/N) Σ_i log ─────────────────── )
              2 (                Σ_j exp(s_ij / τ)                    Σ_j exp(s_ji / τ)          )
                     └────── Image → Text Loss ──────┘      └────── Text → Image Loss ──────┘
```

> ⚠️ **두 항의 차이는 분모의 인덱스 순서 하나뿐이다**
> 앞 항의 분모는 `s_ij` — **행 방향**이다. 이미지 i 하나를 두고 배치 안의 모든 텍스트가 경쟁한다. 뒤 항의 분모는 `s_ji` — **열 방향**이다. 텍스트 i 하나를 두고 모든 이미지가 경쟁한다. 의사코드의 `axis=0` / `axis=1`이 정확히 이 두 방향이다. 이것이 **대칭 손실(symmetric loss)** 이고, 헷갈리기 딱 좋은 지점이다.

### Temperature — 분포의 뾰족함을 정하는 손잡이

softmax는 로짓을 확률 분포로 바꾸는데, 여기에 T로 나눈 값을 넣으면 분포의 모양이 달라진다.

```text
softmax(x)_i   = exp(x_i) / Σ_n exp(x_n)
softmax_T(x)_i = exp(x_i / T) / Σ_n exp(x_n / T)
```

같은 로짓 `[+6.0, −5.0, +4.0, +1.5, +9.9, −2.3, +4.0]`에 T만 바꿔 보면 이렇다.

| T | 결과 확률 | 성격 |
|---|---|---|
| **T = 7.00** | 0.19, 0.04, 0.14, 0.10, **0.33**, 0.06, 0.14 | 평평함 (soft) |
| **T = 1.00** | 0.02, 0.00, 0.00, 0.00, **0.97**, 0.00, 0.00 | 뾰족함 (sharp) |

![같은 로짓 벡터에 temperature가 클 때는 확률이 고르게 퍼져 평평해지고 작을 때는 최댓값 하나에 확률이 몰려 뾰족해지는 대비를 두 개의 막대그래프로 보여주는 다이어그램](04-temperature.png)

> ⚠️ **방향을 반대로 외우기 쉽다**
> **T가 클수록 평평**해지고(선택이 무작위에 가까워짐), **작을수록 한 후보에 몰린다.** CLIP 의사코드의 `logits = ... * np.exp(t)`는 1/τ를 곱하는 것과 같으므로, 학습되는 t가 커지면 분포가 날카로워진다. 나누기와 곱하기가 한 번 뒤집혀 있어서 헷갈린다. **"온도가 높으면 분자가 활발히 돌아다녀 고르게 퍼진다"** 로 물리 비유를 걸어 두면 잘 안 잊는다.

<br>

---

## 6. 제로샷 이미지 인식기 — CLIP을 분류기로 쓰는 법

CLIP은 분류기로 학습된 적이 없다. 그런데 분류를 시킬 수 있다. 이 절차가 파운데이션 모델의 "한정되지 않는 출력"을 가장 잘 보여주는 사례다.

![원하는 카테고리 텍스트 목록을 프롬프트 템플릿에 넣어 텍스트 인코더로 미리 임베딩해 벡터 DB를 만들고 쿼리 이미지의 임베딩과 유사도를 계산해 가장 높은 카테고리를 반환하는 제로샷 이미지 인식기의 추론 흐름 다이어그램](05-zeroshot-classifier.png)

1. 원하는 물체 카테고리 리스트를 **텍스트로** 준비한다 (`plane`, `car`, `dog`, …, `bird`)
2. 각 카테고리를 프롬프트 템플릿 `A photo of a {object}.` 에 넣어 텍스트 임베딩으로 바꾸고 **Vector DB로 미리 계산해 둔다**
3. 쿼리 이미지를 이미지 인코더에 넣어 임베딩을 얻고, DB의 모든 텍스트 임베딩과 유사도를 계산해 **argmax**를 반환한다

```text
[label text] → "A photo of a {object}." → [Text Encoder] → T_1 T_2 T_3 … T_N  (미리 계산, Vector DB)

[query image] → [Image Encoder] → I_1
                                   ↓
                I_1·T_1  I_1·T_2  [I_1·T_3]  …  I_1·T_N   ← argmax
                                   ↓
                         "A photo of a dog."
```

여기서 나오는 질문 세 개가 이 구조의 성질을 드러낸다.

- **검색 시스템과 무엇이 같은가?** — 텍스트 임베딩을 미리 계산해 두고 쿼리 벡터와 유사도로 찾는 구조는 벡터 검색 그 자체다. 1절의 "이상적인 모델 = 검색엔진"이 여기서 되돌아온다. 다만 저장하는 것이 원본 데이터가 아니라 **압축된 의미 벡터**라는 점이 다르다.
- **카테고리 이외에 무엇이 가능한가?** — 텍스트 자리에 무엇이든 넣을 수 있다. "a blurry photo", "a photo taken at night", "a person wearing a helmet" 처럼 속성·상황·안전 규칙을 넣으면 그 자체가 분류 축이 된다.
- **카테고리가 아주 많으면?** — 텍스트 임베딩은 한 번만 계산하면 되므로, 카테고리가 늘어나도 추가 비용은 벡터 검색 비용뿐이다. 실제로 ANN 인덱스(FAISS 등)를 붙이는 것이 정석이다.

> 💡 **프롬프트 템플릿이 성능을 바꾼다**
> `dog` 하나만 넣는 것보다 `A photo of a dog.`가 잘 맞는다. 학습 데이터인 alt-text가 대부분 문장 형태였기 때문에, 템플릿을 씌워야 학습 분포와 비슷해진다. 원 논문은 여러 템플릿의 임베딩을 평균 내는 **프롬프트 앙상블**로 정확도를 더 끌어올렸다. 제로샷이라고 해서 튜닝할 게 없는 건 아니다.

<br>

---

## 7. SigLIP — softmax를 sigmoid로 바꾸면 무엇이 달라지나

CLIP의 대조 학습에는 구조적인 낭비가 있다. softmax의 분모 `Σ_j exp(s_ij/τ)`가 배치 안의 **모든** 음성 쌍을 포함하기 때문에, 이미 충분히 멀어진 쌍(강아지 이미지 ↔ "컴퓨터")에도 계속 "더 멀어져라"는 기울기가 흐른다. 게다가 배치 전체에 대한 정규화가 필요해서 배치를 키우는 것 자체가 부담스럽다.

**SigLIP**(Zhai et al., "Sigmoid Loss for Language Image Pre-Training", ICCV 2023)의 답은 손실함수를 갈아 끼우는 것이다. 각 (이미지, 텍스트) 쌍을 배치 전체와 비교하는 대신 **독립적인 이진 분류 문제**로 다룬다. "이 쌍은 맞는 쌍인가, 아닌가?" 그것뿐이다.

```text
                1   N   N              1
L_SigLIP  =  − ─── Σ   Σ   log ──────────────────────────
                N  i=1 j=1        1 + exp( −z_ij · (s_ij/τ + b) )
```

| 기호 | 의미 |
|---|---|
| `s_ij` | i번째 이미지 임베딩과 j번째 텍스트 임베딩의 코사인 유사도 |
| τ | 학습 가능한 온도. 구현에서는 t = exp(t')의 곱셈 스케일로 다룬다 |
| b | 학습 가능한 편향. sigmoid 기반이라 편향 항이 추가로 필요하다 |
| `z_ij` | 정답 레이블 — 양성 쌍이면 +1(s가 커지면 좋음), 음성 쌍이면 −1(s가 작아지면 좋음) |

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> 원본은 모델 이름을 한 곳에서 `SigCLIP`으로 적었는데, 슬라이드 제목·소제목·인용 논문 모두 **SigLIP**이므로 그쪽으로 통일했다. 또 손실 함수의 앞 계수가 `1/N²`으로 되어 있으나, 같은 슬라이드의 의사코드가 `/ n`으로 나누고 원 논문도 배치 크기로 나누므로 **1/N**이 맞다.

### 6줄짜리 의사코드

```python
# img_emb    : 이미지 모델 임베딩 [n, dim]
# txt_emb    : 텍스트 모델 임베딩 [n, dim]
# t_prime, b : 학습 가능한 temperature와 bias
# n          : 미니배치 크기

t = exp(t_prime)
zimg = l2_normalize(img_emb)
ztxt = l2_normalize(txt_emb)
logits = dot(zimg, ztxt.T) * t + b
labels = 2 * eye(n) - ones(n)          # 대각선만 +1, 나머지는 -1
l = -sum(log_sigmoid(labels * logits)) / n
```

`labels = 2 * eye(n) - ones(n)` 한 줄이 대각선(양성)은 +1, 나머지(음성)는 −1인 레이블 행렬을 만든다. 이 트릭 덕분에 양성/음성을 if 분기 없이 한 번에 처리한다.

### 왜 이게 더 나은가 — 포화(saturation)의 효용

![시그모이드 곡선의 포화 구간에서는 이미 잘 분리된 음성 쌍의 기울기가 0에 가까워져 학습에서 빠지지만 소프트맥스는 배치 전체가 분모에 들어가 이미 멀어진 쌍도 계속 밀어내는 차이를 나란히 보여주는 다이어그램](06-softmax-vs-sigmoid.png)

sigmoid 함수 σ(s) = 1/(1+exp(−s))는 s가 커지면 1에 **포화**한다. 포화하면 기울기가 0에 가까워지고, 그러면 **이미 잘 분리된 음성 쌍은 학습에서 사실상 빠진다.** "어느 정도 벌어지면 더 이상 신경 쓰지 않는다"가 sigmoid 손실의 핵심 성질이다. CLIP의 softmax는 배치 전체 정규화 때문에 이 "포화 후 무시"가 일어나지 않는다.

실험 결과도 명확하다. 학습 데이터에 인위적으로 노이즈(이미지 / 텍스트 / 배치 / 이미지·텍스트 / 셋 다)를 가하고 ImageNet 제로샷 정확도를 비교했을 때, 노이즈 비율 전 구간에서 **sigmoid가 softmax보다 위에 있다.** 이 강건성 덕분에 SigLIP은 지금 최신 VLM들의 비전 인코더로 널리 쓰이고 있고, **SigLIP 2**도 공개되었다.

> ➡️ **CLIP은 "배치 안에서 줄 세우기", SigLIP은 "쌍마다 O/X 채점".** 줄 세우기는 배치 전체를 봐야 하지만 채점은 쌍만 보면 되므로, 배치를 키우기도 분산 학습을 붙이기도 쉬워진다.

<br>

---

## 8. 멀티모달 정합 — 여러 모달리티를 한 공간에

지금까지 한 일에 이름을 붙이면 **멀티모달 정합(multi-modal alignment)** 이다.

> **멀티모달 정합 (multi-modal alignment)** — 서로 다른 두 가지 이상의 모달리티(예: 이미지와 텍스트) 사이에 **공통된 임베딩 벡터 공간을 구성하는 것.** 공통 공간이 있으면 서로 다른 모달리티의 임베딩끼리 유사도를 직접 비교할 수 있다.

```text
Spoken Word   ──Speech──┐
  사자 이미지 ──Vision──┤
Roaring Sound ──Audio───┼──▶ 하나의 MULTIMODAL SPACE 안 같은 지점
  "LION"      ──Text ───┘
```

### ImageBind — 이미지를 허브로 삼아 여섯을 묶다

CLIP이 이미지↔텍스트 둘을 묶었다면, **ImageBind**(Meta, CVPR 2023)는 **이미지, 비디오, 텍스트, 오디오, 깊이(depth), 열화상(heat map), IMU**를 하나의 공간에 묶는다. 이름 그대로 **이미지를 허브(hub)로 두고 나머지를 이미지에 묶는(bind)** 구조다. 모든 모달리티 쌍에 대해 데이터를 모으는 대신 "무엇이든 이미지와는 짝지을 수 있다"는 점을 이용한 설계다.

논문이 보여준 응용 네 가지가 정합의 쓸모를 정확히 보여준다.

| 응용 | 내용 |
|---|---|
| **Cross-Modal Retrieval** | "불 타닥거리는 소리" 오디오를 넣으면 대응하는 이미지·비디오·깊이맵·텍스트를 전부 검색해 온다 |
| **Embedding-Space Arithmetic** | 새 이미지 임베딩 ⊕ 파도 소리 임베딩 ⇒ 바닷가에 있는 새 이미지. 임베딩 공간의 덧셈이 의미의 합성이 된다 |
| **Audio to Image Generation** | 개 짖는 소리, 엔진 소리, 빗소리를 넣으면 각각에 해당하는 이미지를 생성한다 |
| **Audio to Segmentation** | "Dog barking", "Sea waves", "Keyboard typing" 같은 오디오 질의로 이미지 안 해당 영역을 분할해 낸다 |

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> ImageBind 저자 표기가 원본에서는 `Gidhar` / `Girdhard`로 두 번 다르게 적혀 있는데 **Girdhar**가 맞고, 모델명도 `ImageBIND`가 아니라 **ImageBind**다.

### CLIP·SigLIP을 눈으로 재사용하는 VLM 계보

여기가 이 글의 접합부다. **대부분의 VLM은 인코더를 새로 만들지 않고 사전학습된 CLIP 계열을 "눈"으로 가져다 쓴다.**

| 비전 인코더 | 이를 채택한 VLM |
|---|---|
| **CLIP 기반** | BLIP-2 (CLIP + OPT/FlanT5), InstructBLIP (BLIP-2의 instruction tuning 버전), LLaVA (CLIP + Vicuna), MiniGPT-4, mPLUG-Owl |
| **SigLIP 기반** | PaLI-X (SigLIP + PaLM), SmolVLM |

최근에는 CLIP·SigLIP의 성공 레시피를 기반으로 특화된 비전 인코더를 따로 개발하는 흐름이다 (뒤에 나올 InternViT가 그 예다).

<br>

---

## 9. 심화 — CLIP을 손실함수로 뒤집어 쓰기

모달리티 사이를 오가는 설계는 크게 두 갈래다.

- **변환(Translating)** — A를 네트워크에 통과시켜 B를 직접 만든다. `모달리티 A → [인코더] → [중간층] → [디코더] → 모달리티 B`
- **정합(Matching)** — A와 B를 각각 인코딩해 **정렬된 피쳐 공간**에 올리고, 그 공간의 거리로 둘을 연결한다

CLIP은 정합 계열이다. 여기서 이 절 전체의 출발점이 되는 질문이 나온다. **"이 연관성을 거꾸로 활용할 수는 없을까?"** 즉 "정합 정도를 재는 도구"였던 CLIP을 **"정합 정도를 최대화하도록 입력을 깎는 손실함수"** 로 뒤집는 것이다.

```text
텍스트 ──▶ [Text Encoder] ──▶ T₁ ──┐
                                    ├──▶ I₃·T₁  (정렬 정도 = 손실함수)
이미지 ──▶ [Image Encoder] ──▶ I₃ ──┘
                                          │ 유사도 최대화
                ◀── backpropagation ──────┘
```

인코더 두 개는 **얼려 둔다.** 대신 입력 쪽 일부를 학습 가능한 파라미터로 두고, 유사도에서 입력 쪽으로 그래디언트를 흘려보내 유사도가 커지도록 입력을 깎는다.

> ⚠️ **여기서 말하는 "학습"은 모델 학습이 아니다**
> 파라미터를 갱신해 모델을 개선하는 training이 아니라, **단일 입력 하나에 대해서만 도는 최적화(optimization)** 다. 사전학습된 CLIP은 계속 고정이고, 새 입력이 들어올 때마다 최적화를 처음부터 다시 돌린다. 그래서 학습 데이터가 0개여도 동작하지만, 대신 **추론 한 번이 비싸다.** 원본 슬라이드도 "단일 데이터에 대해서만 학습 = 최적화 (학습X)"라고 반복해 강조한 지점이다.

무엇을 학습 가능한 파라미터로 두느냐에 따라 응용이 갈린다.

| 학습 가능하게 둔 대상 | 얻는 결과 |
|---|---|
| 텍스트(캡션) 쪽 | **Image-to-text** — 이미지 캡셔닝 |
| 미분 가능한 이미지 구성 (예: 미분 가능 3D-to-2D 렌더링) | **Text-to-X** — Text-to-3D 등 |

예를 들어 "정면을 향해 걷고 있는 앨런 튜링" 같은 텍스트를 주고, 미분 가능한 렌더러로 뽑은 2D 이미지가 그 텍스트와의 CLIP 유사도를 최대화하도록 3D 메시를 최적화하면 Text-to-3D가 된다. 이 아이디어로 뻗어 나간 응용이 이미지 캡셔닝, 텍스트 기반 이미지 스타일 변환, Text-to-image, Text-to-motion, Text-to-3D, 텍스트 기반 이미지·비디오 검색까지다. CLIP 논문이 4년 동안 4만 회 넘게 인용된 이유가 여기 있다.

| 예제 | 입력 → 출력 | 논문 |
|---|---|---|
| **ZeroCap** | 이미지 → 캡션 | Tewel et al., CVPR 2022 |
| **StyleCLIP** | 텍스트 명령 + 원본 이미지 → 편집된 이미지 | Patashnik et al., ICCV 2021 |
| **CLIP-Actor** | 텍스트 설명 → 3D 애니메이팅 아바타 | Youwang et al., ECCV 2022 |

**ZeroCap**은 학습 데이터 없이 "The President Trump smiles.", "Manhattan on the iconic island.", "Mario driving." 처럼 고유명사까지 짚어 낸다. 다만 **OCR 능력은 제한적**이어서 간판의 "CENTRAL PARK"를 "Central PARK."로 대략만 읽는다. **StyleCLIP**은 StyleGAN의 잠재 공간을 CLIP loss로 최적화해 "Mohawk hairstyle", "Without makeup" 같은 명령대로 얼굴을 편집한다. **CLIP-Actor**는 "Captain America is jump kicking" 텍스트만으로 3D 인간 메시에 스타일과 모션을 입힌다.

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> 원본 슬라이드 제목은 `ZeroCLIP`인데, 같은 페이지 하단 인용이 가리키는 논문·기법 이름은 **ZeroCap**이다.

같은 계열의 손실함수로 **SDS(Score Distillation Sampling) loss**도 있다. CLIP 대신 얼려 둔 text-to-image 확산 모델을 채점자로 쓰는 방식으로, DreamFusion(Poole et al., ICLR 2023)의 Text-to-3D가 대표 사례다. **확산 모델 자체는 2편에서 다룬다.**

<br>

---

## 10. VLM — LLM에 눈을 다는 레시피와 LLaVA

이제 3절에서 비워 뒀던 Perception 블록을 실제로 채운다. 구조는 이미 4절에서 예고편으로 나왔다.

```text
World          Perception        Cognition        Interpretation
(실제 장면)  ──▶  [ CLIP ]  ──▶  [ LLM ]   ──▶  "A man standing next to
                  (눈 = 지각)     (뇌 = 사고)      the pavilion with a gray roof!"
```

**멀티모달 언어 모델**은 이미지·소리·비디오 등 여러 모달리티를 함께 이해하고 처리하는 언어 모델이다. ChatGPT, Claude, LLaVA, InstructBLIP, Qwen-VL, InternVL, LLaMA-Vision, SmolVLM, Phi, HyperCLOVA X SEED Vision 등이 여기 속한다.

### 가장 저렴한 레시피 — 가운데만 학습한다

![이미지 인코더는 동결하고 그 뒤의 선형 투영만 학습하며 텍스트 디코더도 동결하는 VLM 구축의 기본 레시피와 프로젝션 출력이 언어모델에게 소프트 프롬프트로 작동함을 보여주는 다이어그램](07-vlm-recipe.png)

```text
[Image]
  │
  ▼
Image Encoder  (동결)      ──▶ Feature map
  │
  ▼
Linear Projection  (학습)  ──▶ Soft prompts (Image projection)
  │
  ▼
Text Decoder  (동결)       ──▶ "A picture of a dog on a skateboard"

└──── Image Space ────┴──────── Text Space ────────┘
```

> ➡️ **가운데 Linear Projection만 학습하고 앞뒤 인코더·디코더는 얼린다.** 이 패턴이 이후 거의 모든 VLM에서 반복된다. 프로젝션의 출력은 텍스트 디코더 입장에서 **소프트 프롬프트(soft prompt)** 로 작동한다. 즉 이미지를 "텍스트 토큰인 척하는 벡터"로 번역해 LLM 앞에 붙여 주는 셈이다.

학습해야 할 파라미터가 선형 레이어 하나뿐이므로, 이미 있는 CLIP과 이미 있는 LLM만 있으면 개인 GPU로도 VLM을 만들 수 있다. 이미지 캡셔닝("My desk as it looked after a few days of working")과 Visual QA(Q: "What is the person holding?" → A: "A tennis racket")가 바로 나온다.

### LLaVA — Large Language and Vision Assistant

**LLaVA**(Liu et al., "Visual Instruction Tuning", NeurIPS 2023)는 이 레시피를 제대로 밀어붙여 이름을 남긴 모델이다. 기호로 옮기면 이렇다.

```text
X_v (Image) ──▶ [Vision Encoder] ──▶ Z_v ──▶ [Projection W] ──▶ H_v ┐
                                                                     ├──▶ [Language Model f_φ] ──▶ X_a
X_q (Language Instruction) ────────────────────────────────▶ H_q ┘        (Language Response)
```

- `X_v` : 입력 이미지 / `Z_v` : 비전 인코더 출력 피쳐 / `H_v` : 프로젝션 후 언어 토큰 공간의 이미지 표현
- `X_q` : 언어 명령 / `H_q` : 명령 토큰 임베딩 / `X_a` : 최종 언어 응답

특징은 **이미지 / 명령(instruction) / 답변이 짝지어진 데이터셋을 만들어 instruction tuning으로 학습**했다는 점이다. 덕분에 단순 캡셔닝이 아니라 "이 사진에서 사람들이 겪을 어려움이 뭘까?" 같은 지시에 답한다.

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> 원본은 LLaVA 논문을 어떤 페이지에서는 `NeurIPS 2024`, 다른 페이지에서는 `NeurIPS 2023`으로 적고 있다. Visual Instruction Tuning은 **NeurIPS 2023**이 맞다.

### 2단계 학습 — 무엇을 언제 녹이는가

![LLaVA의 2단계 학습에서 비전 인코더는 계속 동결이고 1단계는 프로젝션만 2단계는 프로젝션과 언어모델을 학습하는 반면 Qwen-VL의 3단계 학습은 마지막 단계에서 반대로 비전 인코더를 얼리는 대비를 보여주는 동결 해동 맵 다이어그램](08-llava-stages.png)

| 구성 요소 | Step 1: 사전 학습 | Step 2: Fine-tuning |
|---|---|---|
| Vision Encoder | 동결 | 동결 |
| Projection W | **학습** | **학습** |
| Language Model `f_φ` | 동결 | **학습** |
| 목적 | 표현 공유 — 이미지를 텍스트 표현으로 바꾸는 선형 레이어를 학습해 텍스트와 이미지를 **공통된 토큰 표현**으로 처리 | 특정 작업에 맞춰 선형 레이어와 언어 모델 등 필요한 부분만 강화 |
| 효율 | 전체 모델을 다시 훈련하지 않아 자원·시간 절감. **프로젝션 레이어만 학습** | FP16 같은 정밀도 최적화로 적은 메모리에서 큰 모델 학습 |

> ➡️ **두 단계 모두 비전 인코더는 계속 얼려 둔다.** 바뀌는 것은 언어 모델을 여는지 여부뿐이다. Step 1은 "프로젝션만", Step 2는 "프로젝션 + 언어 모델".

### 학습 데이터는 ChatGPT가 만들었다

LLaVA의 시각 명령 데이터는 사람이 만든 게 아니라 **ChatGPT로 자동 생성한 합성 데이터**다. 절차가 영리하다.

1. **COCO 데이터셋**은 이미지에 대응하는 캡션과 물체 바운딩 박스를 라벨로 제공한다
2. 그 캡션과 박스 좌표를 **텍스트로** ChatGPT에 넣는다 — 즉 이미지를 못 보는 LLM에게 장면을 텍스트로 대신 보여 준다
3. ChatGPT에게 **역(逆) 질의를 만들게 한다.** 답(라벨)을 먼저 주고 질문을 만들게 하는 방식이다

| 질의 타입 | 설명 | 예시 |
|---|---|---|
| 대화(conversation) | 짧은 사실 확인 QA | Q: "What type of vehicle is featured in the image?" / A: "The image features a black sport utility vehicle (SUV)" |
| 자세한 설명 | 장면 전체를 문단으로 서술 | "The image is an underground parking area with a black SUV parked. There are three people in the scene…" |
| 복잡한 추론 | 장면에서 유추해야 답이 나오는 질문 | Q: "What challenges do these people face?" / A: "…the challenge of fitting all their luggage into the black SUV…" |

컨텍스트로 넣는 것은 두 가지다. 캡션 여러 줄("A group of people standing outside of a black vehicle with various luggage")과, 정규화된 바운딩 박스 좌표(`person: [0.681, 0.242, 0.774, 0.694]`, `backpack: [0.384, 0.696, 0.485, 0.914]`).

> 💡 **파운데이션 모델을 데이터 공장으로 쓴다**
> 이미지를 볼 수 없는 텍스트 LLM에게 "캡션 + 박스 좌표"라는 형태로 장면을 대신 보여 주고, 그로부터 시각 명령 데이터를 뽑아낸다. 라벨링 비용을 거의 0으로 만드는 이 발상은 **합성 데이터(synthetic data)** 라는 큰 주제로 이어지는데, 이건 2편에서 본격적으로 다룬다.

<br>

---

## 11. Qwen-VL 계열 — 해상도와 위치를 다시 설계하다

**Qwen-VL**(Alibaba, 2023)은 상용 VLM에 맞서는 오픈소스 VLM이다. 텍스트 LLM인 Qwen-LM에 시각 처리 능력을 부여해 여러 이미지 입력, 번역, 텍스트 읽기, 위치 찾기(물체 탐지), 인식, 이해를 한 모델로 처리한다. VQAv2, Flickr30K, GQA, TextVQA, ChartQA, AI2D, OCR-VQA, RefCOCO 계열 등 11개 벤치마크에서 당시 오픈소스 모델들을 넓게 앞섰다.

### 3단계 학습 — 마지막에 ViT를 얼린다

| 구성 요소 | Stage 1: Pretraining | Stage 2: Multi-task Pretraining | Stage 3: Supervised Finetuning |
|---|---|---|---|
| ViT | **학습** | **학습** | 동결 |
| CrossAttn (Learnable Query Embs) | **학습** | **학습** | **학습** |
| QwenLM | 동결 | **학습** | **학습** |
| 입력 해상도 | Low Resolution | High Resolution | High Resolution |
| 데이터 | Image-Text Pairs | Multi-task and Interleaved VL Data | Chat Interleaved VL Data |

> ⚠️ **LLaVA와 정반대 지점이 있다**
> LLaVA는 처음부터 끝까지 비전 인코더를 얼렸는데, Qwen-VL은 Stage 1·2에서 ViT를 **학습시키고** Stage 3에서 **얼린다.** 시각-언어 대응을 먼저 충분히 잡아 놓은 뒤에는 비전 인코더를 건드리지 않고 대화 능력만 끌어올린다는 의도다. "무엇을 얼릴 것인가"에 정답이 하나 있는 게 아니라, **어느 단계에서 무엇이 이미 충분히 학습됐는지**가 판단 기준이다.

### Qwen2-VL — Naive Dynamic Resolution

Qwen2-VL은 다국어 텍스트와 이미지 내 텍스트 이해, **임의의 해상도 처리**, 에이전트 기능을 붙였다. 여기서 중요한 건 두 번째다. 고정 해상도(224×224 등)로 리사이즈하는 대신 **원본 해상도 그대로 넣고 크기에 비례하는 가변 개수의 토큰**을 만든다.

| 입력 | 해상도 | 생성 토큰 수 |
|---|---|---|
| Picture 1 (긴 문서 스크린샷) | 1092 × 8204 | **11,427 tokens** |
| Picture 2 (수식 한 줄) | 224 × 28 | **8 tokens** |
| Picture 3 (풍경 사진) | 1260 × 700 | **1,125 tokens** |
| Video 1 (16초) | 644 × 336 | **2,208 tokens** |

토큰 수가 8개에서 11,427개까지 **세 자릿수 차이**로 변한다. 이것이 "임의의 해상도 처리 가능"의 실체다. 수식 한 줄짜리 이미지를 224×224로 늘려 억지로 196토큰을 만드는 낭비도, 긴 문서를 224×224로 뭉개 글자를 못 읽게 만드는 손실도 없다.

### M-RoPE — 위치 id를 3차원으로

토큰 수가 가변이 되면 위치 정보도 다시 설계해야 한다. 먼저 기존 방식을 정리하면,

- **기존 PE**: `Positioned Token = Token + PE` — 입력 맨 앞에서 **한 번만** 더한다
- **기존 RoPE**: 쿼리 `q_i`와 키 `k_j`에 회전 행렬 R을 곱한다

```text
q'_i = R(i) q_i ,  k'_j = R(j) k_j   ⇒   q'_i · k'_j = q_iᵀ R(j−i) k_j
```

회전 행렬을 곱하면 내적이 **상대 위치 (j−i)에만 의존**하게 된다는 것이 RoPE의 요령이다.

**M-RoPE(Multimodal Rotary Position Embedding)** 는 여기서 위치 id를 스칼라가 아니라 **(Time, Height, Width) 3-튜플**로 확장한다.

![비디오 프레임 격자의 각 패치에 시간 높이 너비 세 축의 위치 id가 붙고 다음 프레임은 시간 축만 증가하며 이어지는 텍스트 토큰은 세 축이 모두 같은 값을 가져 기존 1차원 RoPE와 동일하게 동작함을 보여주는 M-RoPE 다이어그램](09-mrope.png)

```text
프레임 0 좌상단 패치  (0, 0, 0) → 오른쪽으로 (0, 0, 1), (0, 0, 2), (0, 0, 3)
                              → 아래로     (0, 1, 0), (0, 2, 0) …
프레임 1, 2 …         (1, 0, 0), (2, 0, 0) …   ← 시간 축만 증가

이어지는 텍스트 토큰:
  This(4,4,4)  video(5,5,5)  features(6,6,6)  a(7,7,7)  dog,(8,8,8)  …
```

> 💡 **텍스트 토큰의 세 축 값이 같다는 게 요령이다**
> `(t, t, t)` 형태로 맞춰 두면 M-RoPE가 텍스트 구간에서는 **기존 1D RoPE와 정확히 동일하게 동작**한다. 사전학습된 LLM이 이미 가진 위치 감각을 전혀 깨뜨리지 않고 이미지·비디오 축만 얹을 수 있다. 새 기능을 붙일 때 기존 동작을 보존하는 설계 — 좋은 확장의 전형이다.

### Qwen2.5-VL, Qwen2.5-Omni, Qwen3-VL

| 버전 | 새로 들어온 것 |
|---|---|
| **Qwen2.5-VL** | 강력한 문서 파싱(다국어 OCR, 표·차트·수식·악보·필기체), 정밀한 객체 그라운딩과 카운팅, 장시간 비디오에서 초 단위 이벤트 구간 추출 |
| **Qwen2.5-Omni** | 읽고·쓰고·보고·듣고·말하는 Omni 모델. **Thinker–Talker** 2단 구조로 Thinker가 텍스트 토큰을 생성하면 Talker가 코덱 토큰으로 바꾸고 스트리밍 디코더가 음성 파형을 낸다 |
| **Qwen3-VL** | 백본이 Qwen3 Dense/MoE 디코더로 교체되고 **DeepStack** 추가 |

Qwen2.5-VL은 비디오에서 **Dynamic FPS sampling**을 쓴다. 같은 8초 비디오라도 0.5FPS / 1FPS / 2FPS로 샘플링하면 토큰이 644 / 1288 / 2576개로 달라지고, M-RoPE의 시간 id를 절대 시간(0.0s, 0.5s, 1.0s …)에 정렬해 둔다. 비전 인코더 쪽은 대부분의 층에 **윈도우 어텐션**을 쓰고 소수의 층에만 **전체 어텐션**을 둬서, 고해상도 입력에서도 연산량이 폭발하지 않게 만들었다.

```text
입력 프레임 → Window partition (112 단위 격자)
          → Conv3D (2×14×14)
          → ×M 블록: RMSNorm → Window Attention → RMSNorm → FFN with SwiGLU  (각 블록 잔차 연결)
          → ×1 블록: RMSNorm → Full Attention   → RMSNorm → FFN with SwiGLU
          → Qwen2.5 LM Decoder
```

> 💡 **DeepStack이 Qwen3-VL의 구조적 차이다**
> 기존에는 비전 토큰을 입력 시퀀스 맨 앞에 **한 번만** 붙였다. DeepStack은 비전 토큰을 **LLM의 여러 블록에 나누어 주입**한다. 층을 지나며 시각 정보가 희석되는 것을 막는 설계다.

<br>

---

## 12. InternVL — 눈도 같이 키운다

**InternVL**(OpenGVLab, CVPR 2024)은 다른 각도의 문제를 짚는다. 기존 VLM은 LLM이 수십 B인데 비전 인코더는 수백 M에 그쳐 **균형이 맞지 않았다.** 뇌는 거대한데 눈이 작은 셈이다.

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> 개발 조직 표기가 원본에서는 `OpenGBLab`인데, 같은 슬라이드의 깃허브 주소가 가리키듯 **OpenGVLab**이 맞다.

핵심 아이디어는 **LLM의 대규모 용량에 맞춰 이미지 모델의 용량도 키우는 것**이다. 비전 인코더를 **6B 파라미터**까지 키우고, 대조 학습과 생성이라는 두 목적을 **가중치를 공유하는 두 LLM**으로 함께 학습한다.

```text
image ──▶ [비전 인코더를 6B까지 스케일업] ──contrastive──┐
text  ──▶ [large language model] ───────────────────────┘
                    ▲ shared weights ▼
prompt ─▶ [large language model] ──generative──▶
```

### 라인업과 구조

| 모델 | Vision Part | Language Part |
|---|---|---|
| `InternVL2_5-1B` | `InternViT-300M-448px-V2_5` | Qwen2.5-0.5B-Instruct |
| `InternVL2_5-2B` | `InternViT-300M-448px-V2_5` | `internlm2_5-1_8b-chat` |
| `InternVL2_5-4B` | `InternViT-300M-448px-V2_5` | Qwen2.5-3B-Instruct |
| `InternVL2_5-8B` | `InternViT-300M-448px-V2_5` | `internlm2_5-7b-chat` |
| `InternVL2_5-26B` | `InternViT-6B-448px-V2_5` | `internlm2_5-20b-chat` |
| `InternVL2_5-38B` | `InternViT-6B-448px-V2_5` | Qwen2.5-32B-Instruct |
| `InternVL2_5-78B` | `InternViT-6B-448px-V2_5` | Qwen2.5-72B-Instruct |

비전 파트는 InternViT-300M과 InternViT-6B 두 가지, 입력 해상도는 448px로 통일되어 있다. 비전과 언어를 레고 블록처럼 조합해 크기별 라인업을 만든 것이 특징이다.

```text
[Single-Image / Multi-Image / Video]
        ▼
  Dynamic High Resolution   ← 입력 이미지를 448px 타일 여러 장으로 쪼갠다
        ▼
  InternViT-300M / InternViT-6B  (학습)
        ▼
  Pixel Unshuffle           ← 공간 해상도를 채널로 접어 비전 토큰 수를 줄인다
        ▼
  MLP Projector (학습)              Chat Message ──▶ Text Tokenizer
        │                                                  │
        └──────────────▶ InternLM2.5 / Qwen2.5 (학습) ◀────┘
```

LLaVA의 단일 선형 레이어 대신 **MLP Projector**를 쓰고, 그 앞에 **Pixel Unshuffle**을 두어 토큰 수를 압축하는 것이 차이다. 고해상도 타일링으로 토큰이 늘어나는 만큼 어딘가에서 줄여야 하는데, 그 자리가 여기다.

### 학습 전략 — 점진적 스케일링

| 구성 요소 | Stage 1: MLP Warmup | Stage 1.5 (선택): ViT Incremental | Stage 2: Full Model Instruction Tuning |
|---|---|---|---|
| InternViT | 동결 | **학습** | **학습** |
| MLP Projector | **학습** | **학습** | **학습** |
| LLM | 동결 | 동결 | **학습** |
| 손실 | NTP loss | NTP loss | NTP loss |

손실은 처음부터 끝까지 **NTP loss(Next Token Prediction loss)** 하나다. 여기에 더해 **점진적 스케일링 전략(Progressive Scaling Strategy)** 이라는 요령이 있다.

> 💡 **작은 LLM으로 길들인 눈을 큰 LLM에 재사용한다**
> 작은 LLM과 붙여 학습해 둔 InternViT를 더 큰 LLM 파이프라인의 Stage 1에 **그대로 가져다 쓴다.** 비전 인코더를 매번 처음부터 학습하지 않으므로, LLM 크기를 키울 때 드는 비용이 크게 준다. 비싼 부품을 재활용하는 이 발상이 78B급 모델까지 라인업을 뽑을 수 있게 만든 실질적 이유다.

<br>

---

## 13. Set of Mark — 번호를 붙이면 갑자기 잘 본다

VLM을 실제로 써 보면 금방 알게 되는 약점이 있다. **"어디에 있는지"를 자연어로 지칭하는 걸 유난히 못한다.** "오른쪽 노트북 왼쪽에 뭐가 있어?" 같은 질문에서 엉뚱한 답을 낸다.

**Set of Mark(SoM)** 는 이 문제를 학습이 아니라 **입력 가공**으로 푸는 비주얼 프롬프팅 기법이다(Yang et al., "Set-of-Mark Prompting Unleashes Extraordinary Visual Grounding in GPT-4V", 2023).

```text
원본 이미지 ──▶ [세그멘테이션 파운데이션 모델] ──▶ 영역마다 번호 태그 부착
                                                     │
                                    Image + SoM ─────┴──▶ [VLM] ──▶ 번호를 근거로 답변
```

![원본 이미지를 그대로 넣으면 컵인지 머그인지 애매한 답이 나오지만 세그멘테이션 모델로 영역마다 번호 마크를 붙여 넣으면 12번 램프라고 정확히 지칭하는 답이 나오는 Set of Mark 전후 비교 다이어그램](10-set-of-mark.png)

| 질문 | 원본 이미지만 | 이미지 + SoM |
|---|---|---|
| "What is on the left side of the right laptop?" | "there is a **cup or mug**." ❌ | "On the left side of the right laptop (numbered as 9), there is **a lamp, which is numbered as 12**." ✅ |
| "I want to find a seat close to windows, where can I sit?" | "You can sit on **either of the two black chairs** in front of the white desks…" ❌ | "You can sit on the **chair numbered as 7**. It's the closest seat to the windows (represented by the area numbered as 1)." ✅ |

> ➡️ **SoM이 통하는 이유는 문제의 종류를 바꿔 주기 때문이다.** "이 물체의 위치를 자연어로 묘사하라"는 어려운 생성 문제가, 번호를 붙이는 순간 **"1~20 중 번호를 고르라"는 이산 선택 문제**로 바뀐다. 언어모델이 애초에 잘하는 종류의 일이 된다. 게다가 답이 번호로 나오므로 후처리로 좌표를 되찾기도 쉽다.

실제 효과가 가장 큰 곳은 **컴퓨터 조작 에이전트**다. 3절에서 봤듯 에이전트는 화면을 스크린샷으로 읽는데, "로그인 버튼을 누르라"고 지시하려면 그 버튼의 좌표가 필요하다. SoM은 화면 요소마다 번호를 붙여 "12번을 클릭"이라는 실행 가능한 출력을 만들어 준다.

<br>

---

## 14. 도메인 특화 파운데이션 모델들

CLIP의 레시피는 도메인을 갈아 끼우면 그대로 재사용된다. 데이터만 바꿔 대조 학습을 돌리면 그 분야의 파운데이션 모델이 나온다.

### 의료 — BiomedCLIP, MedCLIP, LLaVA-Med

**BiomedCLIP**(Zhang et al., 2023)은 **1,500만 개의 과학 논문 이미지-텍스트 쌍**으로 사전학습했다. 학습 방식은 CLIP과 완전히 동일한 대조 학습이고, 데이터만 논문 그림과 그 캡션으로 바뀌었다. 캡션이 이미 전문가가 쓴 정밀한 설명이라는 점이 핵심이다.

```text
의료 이미지 ──▶ [Image encoder] ──▶ I₁ I₂ I₃ … Iₙ ┐
                                                   ├──▶ n×n 유사도 행렬 ──▶ CLIP contrastive loss
캡션        ──▶ [Text encoder]  ──▶ T₁ T₂ T₃ … Tₙ ┘
```

병리 진단 QA에서 기존 모델들을 넓게 앞선다.

| 질문 | 정답 | MEVF | QCR | PubMedCLIP | BiomedCLIP |
|---|---|---|---|---|---|
| What are the hyperdensities on the periphery of the image? | ribs | storage of urine ❌ | intestine ❌ | spinal cord ❌ | **ribs ✅** |
| What is the biological sex of the patient? | female | inflammation … ❌ | treat brain diseases … ❌ | nodule ❌ | **female ✅** |

| 모델 | 연도 | 설명 |
|---|---|---|
| **MedCLIP** | 2022 | 의료 텍스트와 이미지 임베딩을 정합시킨 의료용 CLIP. 텍스트로 이미지 상의 질병을 탐지하거나 특정 종류의 의료 이미지를 검색 |
| **LLaVA-Med** | 2023 | LLaVA를 의료 데이터에 파인튜닝. 의료 이미지 기반 챗봇 대화가 가능 (Li et al., NeurIPS 2023 Datasets and Benchmarks, Spotlight) |

LLaVA-Med는 흉부 X-ray를 주면 "양측에 불규칙한 침윤이 보이는 흉부 X-레이입니다. 이러한 침윤은 폐의 불투명도가 증가하는 영역으로, 감염·염증 또는 기타 폐 질환을 나타낼 수 있습니다" 수준으로 답한다.

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> BiomedCLIP 설명 페이지의 대조 행렬 그림은 3번째 행 셀이 전부 `T₁I_j`로, 마지막 행 셀이 전부 `T₂I_j`로 적혀 있다. 각각 `T₃I_j`, `TₙI_j`가 맞다 — 대각 성분이 `T_iI_i`여야 CLIP 손실이 성립한다. 5절의 행렬 그림과 대조해 보면 바로 보이는 오류다.

### 제조 — AnomalyGPT

**AnomalyGPT**(AAAI 2024)는 제조 결함·불량 탐지 모델로, 챗봇 형식으로 결함에 대해 질의응답을 주고받는다. "이미지 상에 결함이 존재하나요?" → "네, 2번 이미지를 참고하시면 오른쪽 하단에 결함이 존재합니다" → "어떤 결함인가요?" → "이미지 오른쪽 아래에 발생한 갈라짐(crack)입니다" 식이다.

구성은 **ImageBind의 이미지 인코더 + Vicuna 언어 모델**을 제조업 데이터에 파인튜닝한 것이다.

> 💡 **왜 CLIP이 아니라 ImageBind를 썼을까**
> 제조 검사는 RGB 이미지만 쓰지 않는다. 열화상·깊이 같은 이종 센서를 함께 보는 경우가 많은데, ImageBind는 그 모달리티들을 이미 하나의 공간에 묶어 두었다. 모델 선택이 도메인의 센서 구성을 따라간 사례다.

### 3D와 로봇 — 3D-LLM, VLA

**3D-LLM**(Hong et al., 2023)은 point cloud 같은 3차원 표현과 자연어의 관계를 학습한다. "Please tell me where is the chair?" → "The chair is located at `<loc3><loc56><loc34>`."

> ➡️ **핵심 트릭은 통합 어휘(Unified Vocab.)** 다. 일반 단어 토큰 옆에 `<loc1>`~`<loc64>` 같은 **위치 토큰을 어휘에 추가**해서, 3차원 좌표를 "특별한 단어"로 취급해 그냥 언어 모델처럼 생성하게 만든다. 좌표 회귀 헤드를 따로 붙이지 않아도 되는 설계다.

**VLA(Vision-Language-Action) 모델**은 한 걸음 더 나간다. 입력은 사람의 텍스트 명령 + 로봇 시점 영상, 출력은 **로봇 행동(위치 변화, 관절 움직임)** 이다. 구성은 **VLM + Action expert**이고, Physical AI와 직결된다. 대표적으로 **PaLM-E(562B)** 는 네 가지 태스크를 하나의 모델로 처리한다.

| 태스크 | 예시 |
|---|---|
| Robot Mobile Manipulation | "Task: give me the chips from the drawer / Next step: …" |
| Robot Tabletop Manipulation | "Task: sort blocks by colors into corners / Next step: …" |
| Task and Motion Planning | "Q: How to put yellow block on blue plate? / A: …" |
| Visual Question Answering | "Q: Tell me a joke about the image. / A: …" |

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> PaLM-E 그림의 `Task and Motion Panning`은 **Task and Motion Planning**의 오타다.

<br>

---

## 15. sVLM — 손바닥 위의 VLM

여기까지 오면 다음 질문은 정해져 있다. **"좋은 건 알겠는데, 이거 내 컴퓨터에서 돌아가나?"**

OpenVLM Leaderboard(2025년 4월 기준) 상위권을 보면 답이 나온다.

| Rank | Method | Param(B) | Language Model | Vision Model | Avg Score |
|---|---|---|---|---|---|
| 1 | InternVL3-78B | 78.4 | Qwen2.5-72B | InternViT-6B-v2.5 | 79.1 |
| 2 | InternVL3-38B | 38.4 | Qwen2.5-32B | InternViT-6B-v2.5 | 77.8 |
| 3 | InternVL2.5-78B-MPO | 78 | Qwen2.5-72B | InternViT-6B-v2.5 | 77 |
| 4 | Ovis2-34B | 34.9 | Qwen2.5-32B | AIMv2-1B | 76.5 |
| 5 | Qwen2.5-VL-72B | 73.4 | Qwen2.5-72B | QwenViT | 76.1 |
| 6 | InternVL2.5-38B-MPO | 38 | Qwen2.5-32B | InternViT-6B-v2.5 | 75.3 |

읽어야 할 열은 성능이 아니라 `Param(B)`다. 상위권이 전부 **34B~78B**다. 개인 GPU나 모바일에 올릴 수 있는 크기가 아니다.

경량화의 흐름은 3단 계단으로 정리된다.

```text
1) VLM   : 멀티모달 데이터를 처리하기 위한 비전-언어 모델 개발
   ↓
2) sLLM  : 대규모 언어모델을 경량화하려는 시도
   ↓
3) sVLM  : (1) + (2) → 효율적인 소형 VLM
```

핵심은 **sLLM에서 이미 검증된 경량화 기법이 VLM으로 그대로 옮겨온다**는 것이다.

### SmolVLM — 무엇을 줄였나

Hugging Face의 SmolVLM은 LLaVA 계열 표준 파이프라인을 그대로 쓰되 **각 블록을 작은 모델로 갈아 끼운다.**

```text
이미지 → [Vision encoder = SigLIP] → hidden states
       → [Modality projection + Pooling] → 이미지 토큰
                                             ↘
텍스트 ─────────────────────────────────────→ [LLM] → 출력 토큰
```

- 비전 인코더: **SigLIP**
- LLM: **Llama 3.1 8B → SmolLM2 1.7B로 교체** — 이것이 가장 큰 경량화 포인트
- `Modality projection` 뒤에 **Pooling**을 붙여 **이미지 토큰 수 자체를 줄인다**

마지막 항목이 이 절의 핵심이다. 배치 크기 1, 같은 입력에서의 추론 GPU 메모리(GB)를 보자.

![SmolVLM의 파이프라인에서 풀링이 이미지 토큰 수를 줄이는 지점을 표시하고 이미지 한 장과 두 장 입력 시 네 모델의 GPU 메모리 사용량 막대를 나란히 놓아 파라미터 수가 아니라 이미지 토큰 수가 메모리를 지배함을 보여주는 다이어그램](11-image-token-memory.png)

| 모델 | 이미지 1장 | 이미지 2장 | 증가량 |
|---|---|---|---|
| **SmolVLM** | **5.02** | **5.70** | **+0.68** |
| PaliGemma 3B | 6.72 | 7.76 | +1.04 |
| InternVL2 2B | 10.52 | 16.51 | +5.99 |
| Qwen2-VL-2B | 13.70 | 23.12 | +9.42 |

> ⚠️ **파라미터 수가 작다고 메모리가 작은 게 아니다**
> InternVL2 2B와 Qwen2-VL-2B는 이름상 2B인데도 SmolVLM보다 2~4배 많은 메모리를 쓴다. 이미지를 **몇 개의 토큰으로 바꾸느냐**(비전 인코더의 해상도 처리 + 풀링 여부)가 실제 메모리를 지배한다. 이미지가 1장에서 2장으로 늘 때 SmolVLM은 +0.68GB인데 Qwen2-VL-2B는 +9.42GB로 벌어지는 것이 결정적 증거다. 앞 절에서 본 Naive Dynamic Resolution은 정확도에는 좋지만 메모리에는 비싼 선택이라는 뜻이기도 하다.

RAM 사용량 대비 승률 산점도에서도 SmolVLM은 약 5GB에 약 62%로, 곡선 위로 확연히 튀어나온 지점에 있다(moondream2 약 4GB/20%, PaliGemma 3B 약 6.7GB/30%, MiniCPM-V-2 약 7.5GB/50%, InternVL2 2B 약 10GB/65%, Qwen2-VL 2B 약 13GB/87%, Idefics3 8B 약 17.5GB/93%).

### Moondream 0.5B — 모바일 실시간 실행

모바일·엣지에서의 **실시간 실행**을 염두에 두고 만든 모델이다.

| 양자화 | 다운로드 크기 | 실행 메모리 |
|---|---|---|
| 8비트 | 479MB | 996MB |
| 4비트 | 375MB | 816MB |

> 📌 **원본 슬라이드와 다르게 쓴 부분**
> 원본은 이 모델을 "2억 개의 파라미터"라고 적었는데, 모델명이 Moondream **0.5B**이고 8비트 양자화 크기가 479MB인 점을 보면 **약 5억 개(0.5B)** 가 맞다. 8비트는 파라미터 1개당 1바이트이므로 479MB ≒ 4.79억 파라미터로 계산이 맞아떨어진다. 같은 페이지의 Phi-3.5 설명 "비디오 용약"도 **비디오 요약**의 오타다.

**양자화(quantization)** 는 모델 파라미터의 정밀도(비트 수)를 낮춰 크기와 메모리를 줄이는 기술이다. 모델 파일 크기와 메모리가 줄고 실행 속도가 오르는 대신, 정밀도가 낮아져 정확도·추론 품질이 소폭 감소할 수 있다.

기능은 캡셔닝에 그치지 않는다. Image Captioning, Visual QA, Object Detection, **Pointing (x, y)** — 물체 좌표를 직접 출력, **Gaze Detection** — 시선 방향 추정, OCR & 문서 이해까지 제공한다.

```bash
pip install moondream==0.0.5
```

```python
import moondream as md
from PIL import Image

# 로컬 모델 파일 경로로 초기화한다
model = md.vl(model="path/to/moondream-0_5b-int4.mf")

# 이미지를 한 번만 인코딩해 둔다
image = Image.open("path/to/image.jpg")
encoded_image = model.encode_image(image)

# 캡션 생성
caption = model.caption(encoded_image)["caption"]
print("Caption:", caption)

# 같은 인코딩 결과로 질문만 바꿔 여러 번 물어본다
answer = model.query(encoded_image, "What's in this image?")["answer"]
print("Answer:", answer)
```

> ✅ **`encode_image()`를 분리해 둔 API 설계에 주목할 것**
> 같은 이미지에 질문을 여러 개 던질 때 비전 인코더를 다시 돌리지 않는다. 온디바이스에서는 이 한 번의 절약이 체감 응답 속도를 좌우한다. VLM을 서비스에 붙일 때 가장 먼저 확인할 최적화 지점이기도 하다.

### 온디바이스 사례와 그 밖의 sVLM

**Gemini Nano**는 18억(Nano-1) / 32.5억(Nano-2) 파라미터의 두 변형으로, 스마트폰 안에서 직접 실행되도록 설계됐다. 2024년 픽셀 9 시리즈에 탑재되어, 녹음 중 카메라로 비친 자료를 함께 인식하고 요약하는 기능으로 쓰였다. **갤럭시 온디바이스 AI**는 모바일 NPU로 텍스트 기반 이미지 생성, 인페인팅·아웃페인팅("Change the hat", "Extend the background"), 어조 변환과 문법 교정, 자연어 기반 사진 촬영을 기기 안에서 처리한다.

| 모델 | 크기 | 특징 |
|---|---|---|
| Qwen 2.5-VL | 3B | 다양한 크기의 이미지와 장시간 영상 처리. 표·악보·화학식 등과 JSON 형식 출력 지원 |
| Phi-3.5 vision instruct | 4.2B | OCR, 차트 분석, 비디오 요약에 특화된 경량 멀티모달 모델 |
| DeepSeek-VL2 | 1B | 저비용 오픈소스 LLM·VLM |
| Gemma 3 | 1B | Google의 오픈소스 멀티모달 모델. 1B~27B 라인업 |

<br>

---

## 16. 한국어 sVLM — 토크나이저부터 손해를 본다

"영어 잘하는 모델을 그대로 쓰면 되지 않나"에 대한 답이다. **한국어는 토크나이저 단계에서부터 손해를 본다.** 여기서 한국어 전용 모델을 따로 만들 이유가 생긴다.

같은 뜻의 문장을 언어별로 옮겨 같은 토크나이저에 넣은 결과다.

| 언어 | 문장 | Tokens | Characters |
|---|---|---|---|
| 영어 | I love machine learning | 4 | 23 |
| 한국어 | 나는 머신러닝을 사랑한다 | 7 | 13 |
| 중국어 | 我喜欢机器学习 | 4 | 7 |
| 일본어 | 私は機械学習が大好きです | 9 | 12 |

영어는 23글자를 4토큰에 담는데, 한국어는 13글자에 7토큰이 든다. **글자 수는 절반인데 토큰은 거의 두 배다.** 영어는 일부 언어보다 최대 2.5배 높은 토큰 정보 밀도를 보이고, 비영어권은 컨텍스트 활용 효율이 낮고 토큰이 낭비되는 **"구조적" 불이익**을 진다. 모델별 비교를 봐도 영어 쪽 토큰 길이는 15~24로 일정한데 비영어 쪽만 32~79까지 벌어진다(BloomZ 16→41, Smaug-72B 17→52, Phi3 17→60, Mistral-7B 17→62).

> ⚠️ **이건 언어의 한계가 아니라 토큰화 방법의 효율성 차이다**
> 한국어가 "정보를 덜 담는 언어"라는 뜻이 결코 아니다. 토크나이저는 학습 코퍼스에서 **빈도가 높은 표현**에 맞춰 어휘를 구성하는데, 그 코퍼스가 영어 중심이었을 뿐이다. 형태소가 복잡한 핀란드어·독일어도 같은 이유로 불이익을 진다.

### 해법은 어휘 확장

방법은 의외로 단순하다. **한국어 서브워드를 토크나이저 어휘에 추가(vocabulary extension)** 하면 된다.

![같은 한국어 문장이 기본 토크나이저에서는 어휘에 없는 글자들이 UTF-8 바이트 세 개씩으로 쪼개져 19토큰이 되지만 한국어 서브워드를 어휘에 추가한 확장 토크나이저에서는 8토큰으로 줄어드는 대비를 보여주는 다이어그램](12-korean-tokenizer.png)

```text
입력: 이번 방학 때 뭐해?          (13 Characters)

Base 토크나이저 : 19 Tokens
  <s> _ 이 [EB][B2][88] _ 방 학 _ [EB][95][8C] _ [EB][AD][90] 해 ?
  → "번", "때", "뭐"가 어휘에 없어 UTF-8 바이트 3개로 쪼개진다 (byte fallback)

Extended 토크나이저 : 8 Tokens
  <s> _이번 _방 학 _때 _뭐 해 ?
```

- Base 19토큰 → Extended **8토큰.** 같은 문장이 절반 이하로 줄어든다
- 줄어드는 지점이 정확히 **바이트 폴백(byte fallback)이 일어나던 글자들**이다. 한 글자가 3토큰으로 터지던 걸 1토큰으로 되돌린 것
- 토큰이 줄면 컨텍스트 여유 증가, 추론 속도 증가, API 비용 감소로 곧장 이어진다

### 한국어 특화 sVLM

| 모델 | 개발 | Hugging Face |
|---|---|---|
| **HyperCLOVAX-SEED-Vision-Instruct-3B** | NAVER | `naver-hyperclovax/HyperCLOVAX-SEED-Vision-Instruct-3B` |
| **Kanana-1.5-v-3b-instruct** | Kakao | `kakaocorp/kanana-1.5-v-3b-instruct` |

비슷한 크기(3.6~4.2B)의 다섯 모델을 비교한 벤치마크가 한국어 모델의 존재 이유를 정확히 보여준다.

| 항목 | kanana-1.5-v-3b (3.62B) | HCX-SEED-Vision-3B (3.72B) | Phi-3-Vision (4.15B) | Qwen2.5-VL-3B (3.75B) | InternVL2.5-4B (3.94B) |
|---|---|---|---|---|---|
| 전체 평균 | ~73 | ~59 | ~48.5 | ~63.5 | ~61 |
| 영어 이미지 이해 | ~74 | ~65 | ~65.5 | ~74 | ~74.5 |
| 한국어 이미지 이해 | ~68 | ~52 | ~36.5 | ~60.5 | ~54.5 |
| 영어/한국어 멀티모달 지시 이해 | ~77 | ~60 | ~44.5 | ~56 | ~54.5 |

> ➡️ **영어 이미지 이해에서는 다섯 모델이 거의 비슷하다(~65~75).** 격차는 한국어 열에서 벌어진다. Phi-3-Vision은 영어 ~65.5에서 한국어 ~36.5로 반토막이 난다. 한국어 모델을 따로 만드는 이유가 이 두 열에 다 들어 있다.

<br>

---

## 17. 실습 — LMDeploy로 VLM 배포하기

**LMDeploy**는 LLM의 효율적 압축·배포·서빙을 지원하는 오픈소스 툴킷이다. VLM도 같은 인터페이스로 다룬다.

| 기능 | 내용 |
|---|---|
| 효율적인 추론 | 지속적 배치(continuous batching), 병렬화, 고성능 CUDA 커널로 **최대 1.8배 처리량** |
| 효과적인 양자화 | 4비트 양자화 등으로 **2.4배 빠른 추론** |
| 분산 서빙 | 여러 머신·GPU에서 다중 모델 서비스 배포 |
| 대화형 추론 | 대화 이력 재처리를 줄이는 **캐싱** |
| 높은 호환성 | 위 기능들을 동시에 사용 가능 |

지원 VLM 목록은 이 글에서 다룬 모델이 거의 전부 들어 있다 — LLaVA(1.5, 1.6) 7B~34B, InternLM-XComposer2/2.5, Qwen-VL 7B, Qwen2-VL 2B/7B/72B, Qwen2.5-VL 3B/7B/72B, DeepSeek-VL·VL2, InternVL-Chat/InternVL2/2.5/3 (1B~78B), Phi-3-vision·Phi-3.5-vision, GLM-4V 9B, Llama3.2-vision 11B/90B, Molmo, Gemma3 1B~27B, Llama4.

**오프라인 배포** — 파이썬 파이프라인으로 바로 추론한다.

```python
from lmdeploy import pipeline
from lmdeploy.vl import load_image

# 모델 이름만 넘기면 가중치를 받아 파이프라인을 구성한다
pipe = pipeline('OpenGVLab/InternVL2-8B')

# 원격 URL 이미지를 그대로 불러온다
image = load_image('https://raw.githubusercontent.com/open-mmlab/mmdeploy/main/tests/data/tiger.jpeg')

# (프롬프트, 이미지) 튜플 하나를 인자로 넘긴다
response = pipe(('describe this image', image))
print(response)
```

**온라인 배포** — 한 줄로 API 서버를 띄운다.

```bash
lmdeploy serve api_server OpenGVLab/InternVL2-8B
```

> ⚠️ **`pipe((...))` 의 이중 괄호는 오타가 아니다**
> `(프롬프트, 이미지)` **튜플 하나**를 인자로 넘기는 형태라서 괄호가 두 겹이다. 원본 예제 코드에는 `f'describe this image'`처럼 f-string이 쓰여 있는데, 치환할 변수가 없으므로 `f` 접두사는 불필요하다. 위 코드에서는 빼고 썼다.

모델 이름 문자열만 바꾸면 같은 코드가 다른 VLM에서도 그대로 돈다. `Qwen/Qwen2.5-VL-3B-Instruct`로 바꿔 돌려 보면 15절에서 본 크기와 메모리의 차이를 직접 체감할 수 있다.

<br>

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **파운데이션 모델** | "다 기억하는 검색엔진"과 "압축한 뉴럴넷" 사이의 절충. 대규모·적응성·범용성이 세 특징이고, 결정적 차이는 **한정되지 않는 출력** |
| **적응 활용** | 갈림길은 하나 — 가중치가 바뀌는가. 제로샷·퓨샷은 안 바뀌고 파인튜닝은 바뀐다 |
| **VLM이 필요한 이유** | 인지 파이프라인 World → Perception → Cognition → Interpretation에서 LLM은 Cognition만 채웠다. 비어 있는 Perception에 이미지 인코더를 꽂는 것이 VLM |
| **CLIP** | 4억 개 alt-text 쌍으로 이미지·텍스트를 한 공간에 정합. 강점은 정확도가 아니라 **분포 변화에 대한 강건성** |
| **대조 학습** | 배치가 곧 음성 공급원 — N×N 행렬의 대각선 N개가 양성, 나머지 N²−N개가 음성. 라벨은 `np.arange(n)` |
| **대칭 손실** | 두 항의 차이는 분모 인덱스 순서뿐. `s_ij`는 행 방향(Image→Text), `s_ji`는 열 방향(Text→Image) |
| **Temperature** | T가 크면 평평, 작으면 뾰족. 의사코드의 `* np.exp(t)`는 1/τ를 곱하는 것이라 방향이 뒤집혀 보인다 |
| **제로샷 인식기** | 카테고리 → `A photo of a {}` 템플릿 → 텍스트 임베딩 미리 계산 → 쿼리 이미지와 argmax. 사실상 벡터 검색 |
| **SigLIP** | softmax를 sigmoid로. 이미 멀어진 음성 쌍은 **포화해서 학습에서 빠진다**. 손실 계수는 1/N, 배치 확장에 유리 |
| **멀티모달 정합** | 공통 임베딩 공간 구성. ImageBind는 이미지를 허브로 6개 모달리티를 묶는다 |
| **CLIP 뒤집어 쓰기** | 인코더를 얼리고 입력을 깎아 유사도를 최대화. 모델 학습이 아니라 **입력 하나에 대한 최적화** (ZeroCap, StyleCLIP) |
| **VLM 기본 레시피** | 이미지 인코더(동결) → 선형 투영(**학습**) → LLM(동결). 프로젝션 출력이 **소프트 프롬프트**로 작동 |
| **LLaVA** | 2단계 학습 — 비전 인코더는 계속 동결, Step 1은 프로젝션만, Step 2는 프로젝션 + LLM. 학습 데이터는 ChatGPT로 합성 |
| **Qwen-VL** | 3단계 학습으로 마지막에 **ViT를 얼린다**(LLaVA와 반대). Naive Dynamic Resolution은 8~11,427토큰, M-RoPE는 (t,h,w) 3튜플이고 텍스트에서는 (t,t,t) |
| **InternVL** | 비전 인코더를 6B까지 키워 LLM과 균형을 맞춘다. MLP Projector + Pixel Unshuffle, 손실은 NTP loss 하나 |
| **Set of Mark** | 번호 마크를 미리 그려 넣으면 "위치 묘사"가 "번호 고르기"로 바뀐다. 컴퓨터 조작 에이전트의 기본기 |
| **sVLM** | 메모리를 지배하는 것은 파라미터 수가 아니라 **이미지 토큰 수**. SmolVLM은 SigLIP + SmolLM2 1.7B + Pooling |
| **한국어 sVLM** | 한국어는 토크나이저에서 손해(13자 7토큰). 어휘 확장으로 19토큰 → 8토큰. 영어 성능은 비슷하고 **한국어 열에서 격차가 벌어진다** |

<br>

다음 글에서는 **"보는 모델" 다음의 "만드는 모델과 쓰는 법"** 을 다룬다. Segment Anything과 Grounding DINO 같은 분할·탐지 파운데이션 모델, 확산 모델 기반 이미지·비디오 생성, 3D 복원, 그리고 이 거대한 모델들을 적은 비용으로 내 도메인에 적응시키는 PEFT와 개인화(DreamBooth), 마지막으로 파운데이션 모델을 데이터 공장으로 쓰는 합성 데이터 전략까지다.

<br>

## 🔗 참고 자료

- [Radford et al., 2021 — Learning Transferable Visual Models From Natural Language Supervision (CLIP)](https://arxiv.org/abs/2103.00020)
- [Zhai et al., 2023 — Sigmoid Loss for Language Image Pre-Training (SigLIP)](https://arxiv.org/abs/2303.15343)
- [Girdhar et al., 2023 — ImageBind: One Embedding Space To Bind Them All](https://arxiv.org/abs/2305.05665)
- [Liu et al., 2023 — Visual Instruction Tuning (LLaVA)](https://arxiv.org/abs/2304.08485)
- [Bai et al., 2023 — Qwen-VL: A Versatile Vision-Language Model](https://arxiv.org/abs/2308.12966)
- [Wang et al., 2024 — Qwen2-VL: Enhancing Vision-Language Model's Perception of the World at Any Resolution](https://arxiv.org/abs/2409.12191)
- [Chen et al., 2024 — InternVL: Scaling up Vision Foundation Models](https://arxiv.org/abs/2312.14238)
- [Yang et al., 2023 — Set-of-Mark Prompting Unleashes Extraordinary Visual Grounding in GPT-4V](https://arxiv.org/abs/2310.11441)
- [Bommasani et al., 2021 — On the Opportunities and Risks of Foundation Models](https://arxiv.org/abs/2108.07258)
- [Hugging Face Blog — SmolVLM: Redefining Multimodal AI for Everyone](https://huggingface.co/blog/smolvlm)
- [OpenVLM Leaderboard](https://huggingface.co/spaces/opencompass/open_vlm_leaderboard)
- [LMDeploy 공식 문서](https://lmdeploy.readthedocs.io/)
- [moondream — 온디바이스 VLM](https://moondream.ai/)
