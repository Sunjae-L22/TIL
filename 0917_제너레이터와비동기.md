# 제너레이터와 비동기: 값을 만드는 시점과 기다리는 방식

> 강의일: 2026-09-17 · 강의 자료를 바탕으로 개념과 예제를 재구성한 학습 노트
> 확인 범위: `제너레이터_비동기.pdf` 물리 1–61쪽 전체. 한국어·영어 OCR로 읽고 주요 코드와 비교 표를 이미지로 대조했다.

제너레이터는 **필요한 시점에 다음 값을 만든다**. 비동기는 **기다리는 동안 다른 작업을 진행한다**. 둘 다 실행을 중단했다가 이어갈 수 있지만, 제너레이터를 썼다고 I/O가 자동으로 비동기가 되는 것은 아니다.

## 1. iterable과 iterator

반복 가능한 객체(iterable)는 `iter(obj)`로 반복자를 얻을 수 있다. 반복자(iterator)는 `next(it)`로 다음 값을 내놓고, 끝나면 `StopIteration`을 발생시킨다. 리스트는 반복 가능하지만 그 자체가 반복자는 아니다. [강의: 6–13쪽]

```python
values = [10, 20]
it = iter(values)
assert next(it) == 10
assert list(it) == [20]
assert list(it) == []
assert list(values) == [10, 20]
```

`for`는 내부적으로 반복자를 얻고 다음 값을 요청하다가 종료 신호를 처리한다. 반복자는 보통 `__iter__()`에서 자신을 반환하고 `__next__()`에서 상태를 갱신한다.

```python
class Countdown:
    def __init__(self, start):
        self.current = start

    def __iter__(self):
        return self

    def __next__(self):
        if self.current <= 0:
            raise StopIteration
        result = self.current
        self.current -= 1
        return result

assert list(Countdown(3)) == [3, 2, 1]
```

직접 클래스를 만들면 상태와 종료 조건을 관리해야 한다. `yield`는 이 상태 저장을 함수 문법으로 표현하게 해 준다. 엄밀히는 `iter()`가 인덱스 접근 기반의 기존 시퀀스 프로토콜도 지원하므로 “모든 iterable에 반드시 `__iter__` 메서드가 있다”는 설명은 범위를 좁혀 읽는다.

## 2. yield: 반환 후 종료가 아니라 반환 후 중단

```python
import inspect

def countdown(start):
    while start > 0:
        yield start
        start -= 1

g = countdown(3)
assert inspect.getgeneratorstate(g) == "GEN_CREATED"
assert next(g) == 3
assert inspect.getgeneratorstate(g) == "GEN_SUSPENDED"
assert list(g) == [2, 1]
assert inspect.getgeneratorstate(g) == "GEN_CLOSED"
assert list(g) == []
```

제너레이터 **함수 호출**은 제너레이터 객체를 만든다. 본문 실행은 `next()` 또는 반복문이 값을 요구할 때 시작한다. `yield`를 만나면 값을 내보내고 지역 변수와 실행 위치를 유지한다. 다음 요청은 그 다음 줄부터 이어진다. [강의: 15–20, 35–37쪽; Python 제너레이터 문서]

| 상태 | 의미 | 다음 단계 |
|---|---|---|
| CREATED | 객체만 생성 | 첫 `next()` |
| RUNNING | 본문 실행 중 | `yield` 또는 종료 |
| SUSPENDED | `yield`에서 대기 | 다음 요청으로 재개 |
| CLOSED | 소진·종료 | 같은 객체를 처음부터 다시 읽을 수 없음 |

위 코드는 외부에서 관측 가능한 CREATED, SUSPENDED, CLOSED를 확인한다. RUNNING은 제너레이터가 실제 실행 중일 때의 상태다.

## 3. 리스트와 제너레이터의 선택

```python
squares_list = [x * x for x in range(5)]
squares_gen = (x * x for x in range(5))
assert squares_list == list(squares_gen) == [0, 1, 4, 9, 16]
```

리스트 컴프리헨션은 원소를 즉시 모은다. 제너레이터 표현식은 결과를 요청할 때 순차 계산한다. 다만 제너레이터 표현식의 가장 바깥 iterable은 객체 생성 시 평가되므로 “관련된 모든 계산이 항상 지연된다”는 뜻은 아니다.

| 필요한 작업 | 적합한 출발점 |
|---|---|
| 큰 입력을 한 번 읽어 합계·필터링 | 제너레이터·반복자 |
| 앞의 일부만 소비하고 종료 | 제너레이터 |
| 여러 번 반복, 인덱싱, 길이 확인 | 리스트 |
| 전체 정렬 | 전체 데이터를 모을 저장 공간 고려 |

제너레이터라도 내부에 큰 리스트를 만들거나 `list(g)`로 결과를 전부 모으면 메모리 절약 효과가 줄어든다. 보관하는 지역 변수와 참조 대상도 메모리를 사용한다. 무한 제너레이터는 `itertools.islice`처럼 소비 개수를 제한해야 한다. [강의: 19–32, 36–37쪽]

### 무한 수열과 중계

```python
from itertools import islice

def fibonacci_stream():
    a, b = 0, 1
    while True:
        yield a
        a, b = b, a + b


def flatten(groups):
    for group in groups:
        yield from group

assert list(islice(fibonacci_stream(), 7)) == [0, 1, 1, 2, 3, 5, 8]
assert list(flatten([[1, 2], [], [3]])) == [1, 2, 3]
```

`yield from`은 다른 iterable의 값을 소비자에게 전달한다. 단순 값 전달에서는 반복문 안의 `yield`와 같은 결과를 내며, 전체 제너레이터 프로토콜에서는 `send`, 예외, 반환값의 위임까지 관련된다. [강의: 27–33쪽, 프로토콜 범위 보완]

파일도 `for line in file`로 순회할 수 있다. 파일을 연 채 `yield`하는 함수는 소비가 중간에 끝났을 때도 자원을 닫을 수 있도록 `with`의 수명과 제너레이터 종료를 함께 설계한다. 줄별 처리라도 한 줄 자체가 매우 크면 그만큼의 메모리는 필요하다.

## 4. 메모리 측정은 무엇을 비교했는가

강의는 `tracemalloc`으로 현재 할당량과 피크를 비교한다. 이는 Python이 추적하는 할당량이며 프로세스 전체 RSS가 아니다. 생성만 비교하면 리스트는 모든 원소를 만들었지만 제너레이터는 아직 계산하지 않았다는 차이가 있다. [강의: 21–24쪽]

아래 코드는 양쪽 모두 같은 정수 합계를 계산하고, 각각 독립적으로 추적을 시작한다. 원소 개수는 100,000이다. 이는 강의의 큰 입력을 그대로 재현한 벤치마크가 아니라 작은 독립 확인이다.

```python
import gc
import tracemalloc

def peak_for_sum(materialize, n=100_000):
    gc.collect()
    tracemalloc.start()
    seq = [i for i in range(n)] if materialize else (i for i in range(n))
    total = sum(seq)
    _, peak = tracemalloc.get_traced_memory()
    tracemalloc.stop()
    return total, peak

list_total, list_peak = peak_for_sum(True)
gen_total, gen_peak = peak_for_sum(False)
assert list_total == gen_total == 4_999_950_000
assert gen_peak < list_peak
```

측정값은 Python 버전·구현·할당 상태에 따라 달라진다. 여기서 확인하는 것은 동일 입력의 합계와 이 실행에서의 피크 대소 관계다. 제너레이터가 모든 연산에서 더 빠르다는 결론은 내리지 않는다.

## 5. 동시성과 병렬성

동시성은 여러 작업이 겹쳐 진행되는 구조이고 병렬성은 실제로 여러 실행 자원에서 동시에 계산하는 것이다. I/O 대기 구간을 겹치면 CPU 계산을 병렬화하지 않아도 전체 완료 시간이 줄어들 수 있다. [강의: 40–49쪽]

| 방식 | 실행 단위 | 활용 | 비용·제약 |
|---|---|---|---|
| threading | 같은 프로세스의 스레드 | 블로킹 I/O 라이브러리 | 공유 상태 동기화 |
| multiprocessing | 별도 프로세스 | CPU 계산 병렬화 | 생성·직렬화·메모리 |
| asyncio | 이벤트 루프의 Task | 비동기 I/O | 블로킹 코드를 직접 실행하면 루프 정지 |

기본 GIL 활성 CPython에서는 순수 Python CPU 계산이 스레드 수만큼 빨라진다고 기대할 수 없다. GIL을 해제하는 확장 모듈이나 free-threaded 빌드는 조건이 다르다. 버전 번호만으로 현재 프로세스의 GIL 상태를 단정하지 않는다.

## 6. async def, await, Task

`async def`는 코루틴 함수를 정의한다. 호출하면 코루틴 객체가 만들어지고, `await`하거나 Task로 스케줄링해야 실행된다. `await`는 현재 코루틴에서 결과를 기다리는 지점이다. **실제로 중단이 필요한 경우** 이벤트 루프가 다른 준비된 작업을 진행할 수 있다. 이미 완료된 awaitable을 기다리는 등 모든 `await`가 반드시 작업 전환을 일으키는 것은 아니다. [강의: 51–55쪽, 표현 보완]

```python
import asyncio

async def make_value(value, delay):
    await asyncio.sleep(delay)
    return value

async def collect():
    return await asyncio.gather(
        make_value("first", 0.02),
        make_value("second", 0),
    )

assert asyncio.run(collect()) == ["first", "second"]
```

`gather`의 결과 순서는 완료 순서가 아니라 **입력 순서**다. 반대로 다음 코드는 순차적으로 기다린다.

```python
async def sequential():
    first = await make_value("first", 0.02)
    second = await make_value("second", 0.02)
    return [first, second]
```

코루틴이라는 이유만으로 자동 동시 실행되지는 않는다. 동시에 진행할 작업을 `gather`나 Task로 등록해야 한다. `asyncio.run()`은 일반 Python 스크립트의 진입점용이며 이미 이벤트 루프가 실행 중인 노트북 환경에서는 셀에서 `await collect()`처럼 사용한다.

## 7. 블로킹 호출과 작업 수명

`async def` 안에 넣은 `time.sleep()`은 여전히 블로킹 함수다. 네트워크를 동기 라이브러리로 요청하거나 긴 CPU 계산을 직접 수행해도 이벤트 루프를 점유한다. [강의: 54–61쪽]

```python
import time

def blocking_value():
    time.sleep(0.01)
    return 42

async def call_blocking():
    return await asyncio.to_thread(blocking_value)

assert asyncio.run(call_blocking()) == 42
```

`to_thread`는 기존 블로킹 I/O 함수를 스레드로 넘길 때 유용하다. 기본 GIL 빌드의 순수 Python CPU 병렬화 해결책으로 일반화하지 않는다. CPU 작업은 `ProcessPoolExecutor` 등을 검토하고, 프로세스 실행 진입점과 직렬화 가능한 함수를 준비해야 한다.

| API | 사용할 때 | 확인할 점 |
|---|---|---|
| `create_task(coro)` | 작업을 예약하고 나중에 기다림 | Task 참조·결과·예외를 관리 |
| `gather(*aws)` | 입력 순서로 결과 수집 | 기본 예외 전파가 다른 작업의 자동 취소를 뜻하지 않음 |
| `TaskGroup` | 관련 작업의 수명과 실패를 함께 관리 | Python 3.11 이상, 실패 시 나머지 작업 취소·정리 |
| `to_thread(func)` | 블로킹 I/O 위임 | 외부 스레드의 작업 종료와 Task 취소는 별개 |

수많은 작업을 한꺼번에 만들면 메모리나 서버의 동시 요청 한도를 넘을 수 있다. 세마포어나 작업 큐로 동시 실행 수를 제한하는 설계도 필요하다. 이 글의 예제는 외부 요청 없이 로컬 대기만 사용했다.

## 8. 확인한 것과 복습 질문

본문의 반복자 소진, 제너레이터 상태, 피보나치 스트림, 같은 합계의 메모리 비교, `gather` 결과 순서, `to_thread` 반환값을 자동화 도구로 실행해 확인했다. 별도 이벤트 기록으로 두 비동기 작업이 모두 시작된 뒤 완료되는지도 확인했다. 실제 API 처리량이나 보편적인 속도 향상 배수는 측정하지 않았다.

- 제너레이터 객체를 두 번 순회하면 왜 두 번째가 비어 있는가?
- `list(generator)`는 메모리 특성을 어떻게 바꾸는가?
- `await f(); await g()`와 `await gather(f(), g())`의 차이는?
- 비동기 함수 안의 동기 I/O는 누가 기다리는가?

공개 참고 자료: [Python 반복자·제너레이터](https://docs.python.org/3/tutorial/classes.html#generators), [코루틴과 Task](https://docs.python.org/3/library/asyncio-task.html), [tracemalloc](https://docs.python.org/3/library/tracemalloc.html), [free-threaded Python](https://docs.python.org/3/howto/free-threading-python.html).

검증 환경: Python 3.11.9, NumPy 2.4.6, macOS arm64. 공식 문서 확인일: 2026-09-17.
