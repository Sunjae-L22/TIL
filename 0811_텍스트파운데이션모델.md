# 텍스트 파운데이션 모델 (거대 언어 모델)

> 출처: `2_2_텍스트파운데이션모델.pdf` (총 180페이지) · 정리일: 2026-08-14
> 페이지 표기는 PDF 기준 (슬라이드 인쇄 번호와 동일)

## 한눈에 보기

- BERT·GPT-1도 빅데이터·자가 학습·트랜스포머를 다 갖췄는데, 왜 GPT-3부터 "거대 언어 모델"이라 부르는가 → 규모의 법칙과 **창발성**
- 사전 학습만 된 LLM은 지시를 못 따른다. 쓸 만한 모델을 만드는 **정렬(Alignment)** 2단계: 지시 학습(SFT) → 선호 학습(RLHF)
- 같은 모델이 매번 다른 답을 내는 이유: **디코딩 알고리즘** 6종 (Greedy → Top-P)과 Temperature의 의미
- 프롬프트 = 지시(시스템 프롬프트 + 유저 쿼리) + 예시. CoT와 예시 선택이 성능을 바꾸는 메커니즘
- 정답이 없는 생성 태스크를 평가하는 4가지 방법과 **LLM-as-judge**의 3가지 편향
- 환각·탈옥·AI 텍스트 검출 — 한계는 학습 방식(다음 토큰 예측) 자체에서 나온다

## 목차

1. [파운데이션 모델 개관](#파운데이션-모델-개관) (p.5-17)
2. [텍스트 파운데이션 모델이란](#텍스트-파운데이션-모델이란) (p.18-29)
3. [거대 언어 모델의 학습 — 정렬](#거대-언어-모델의-학습--정렬) (p.30-56)
4. [거대 언어 모델의 추론](#거대-언어-모델의-추론) (p.57-101)
5. [거대 언어 모델의 평가](#거대-언어-모델의-평가) (p.102-139)
6. [거대 언어 모델의 응용과 한계](#거대-언어-모델의-응용과-한계) (p.140-162)
7. [복습 문제](#복습-문제) (p.163-171)
8. [실습 예고: 합성 데이터 제작](#실습-예고-합성-데이터-제작) (p.176-180)

---

## 파운데이션 모델 개관

### 정의와 이전 방식의 차이 (p.5-12)

**파운데이션 모델**: 대량의 데이터로 사전 학습된 대규모 AI 모델. 다양한 작업에 범용적으로 활용할 수 있는 기초(foundation) 역할을 한다.

| | 파운데이션 모델 이전 | 파운데이션 모델 |
|---|---|---|
| 새 태스크 해결 | 태스크마다 **별도의 학습** 필요 | **자세한 설명(프롬프트)** 입력만으로 충분 |
| 예시 | AlexNet(2012, 이미지→분류), BERT(2018, 문장→긍/부정) | ChatGPT(텍스트), SORA(비디오), GPT-4o(멀티모달) |

### 3가지 구성요소 (p.13-17)

1. **빅데이터** — 인터넷 데이터가 기하급수적으로 증가(2025년 추정 175 ZB). 딥러닝 모델은 데이터가 늘수록 성능이 오른다.
2. **자가 학습(Self-supervised Learning)** — 사람이 정답을 만들 필요가 없다. 대표: **다음 토큰 예측(Next token prediction)**. 웹에서 긁은 문장 "아까 밥 먹고 왔어"를 그대로 `입력: 아까 밥 먹고 / 정답: 왔어`로 변환해 학습 데이터를 만든다. 모델은 다음 토큰의 확률 분포(왔어 0.5, 왔는데 0.2, ...)를 출력한다.
3. **어텐션 기반 트랜스포머** — 어텐션(Attention)은 입력에서 중요한 부분에 주의를 집중하는 메커니즘, 트랜스포머는 이를 기반으로 한 신경망 구조.

트랜스포머(디코더) 전체 흐름 (p.17):

```text
Input Tokens → Input Embedding → (+ Positional Encoding)
  → [Transformer Block] × N     ← 블록 내부: LayerNorm → Linear → Multi-head Attention → Linear
  → LayerNorm → Linear → Softmax                (+ 잔차 연결) → LayerNorm → Feed Forward (+ 잔차 연결)
  → Output Probabilities
```

---

## 텍스트 파운데이션 모델이란

### 언어 모델에는 이미 3요소가 있었다 (p.20-22)

- GPT-1, BERT도 빅데이터·자가 학습·트랜스포머를 모두 사용(BERT: 사전 학습 + 태스크별 파인튜닝으로 SQuAD +2.1%p, GLUE +7.6%p SOTA 달성). 그러나 **태스크마다 추가 학습이 필요**했다 — 파운데이션 모델이 아니다.
- **GPT-2**: 추가 학습 없이 텍스트 지시만으로 새로운 태스크를 "어느 정도" 수행할 수 있음을 확인. 가장 큰 GPT-2(1542M)조차 **underfitting** 상태 → 모델을 더 키우면 성능이 오를 여지 발견.
- 언어 모델 품질 지표 Perplexity (낮을수록 자연스러움):

$$\text{Perplexity} = \sqrt[N]{\frac{1}{P(w_1, w_2, \dots, w_N)}} = \sqrt[N]{\frac{1}{\prod_{i=1}^{N} P(w_i \mid w_1, \dots, w_{i-1})}}$$

### LLM의 특이점: 규모의 법칙과 창발성 (p.23-24)

**(1) 규모의 법칙(Scaling Law)** — 손실 $L$은 컴퓨트 $C$, 데이터 $D$, 파라미터 수 $N$ 각각에 대해 거듭제곱 법칙으로 감소한다 (Kaplan et al. 2020):

$$L = \left(\frac{C_{\min}}{2.3 \times 10^{8}}\right)^{-0.050}, \quad L = \left(\frac{D}{5.4 \times 10^{13}}\right)^{-0.095}, \quad L = \left(\frac{N}{8.8 \times 10^{13}}\right)^{-0.076}$$

**(2) 창발성(Emergent Property)** — 특정 규모를 넘어서면 갑자기 발현되는 성질.

- **인-컨텍스트 학습(In-context Learning)**: 프롬프트 안의 설명·예시만 보고(그래디언트 업데이트 없이) 새 태스크 수행. GPT-3 175B는 few-shot 예시가 늘수록 성능이 급상승하지만 13B·1.3B는 미미 → 규모가 만든 능력.
- **추론(reasoning) 능력** 역시 창발성의 예.

### 거대 언어 모델의 정의와 예시 (p.25-29)

- **거대 언어 모델(LLM)**: 기존 대비 (1) 더 큰 모델(>7B)이 (2) 더 많은 데이터(>1T 토큰)에서 학습되어 **창발성이 나타나기 시작한 언어 모델**. 일반적으로 다음 토큰 예측으로 사전 학습된 트랜스포머 기반 모델을 의미.
  - 주의: 기준은 시간에 따라 바뀌므로 7B, 1T가 절대적 기준은 아니다.

| | 폐쇄형(Closed) | 개방형(Open sourced) |
|---|---|---|
| 예시 | ChatGPT(OpenAI), Claude(Anthropic), Gemini(Google) | LLaMA(Meta), Gemma(Google), Qwen(Alibaba), DeepSeek, Mistral |
| 장점 | 일반적으로 더 우수한 성능·최신 기능, 사용 쉬움 | 무료 다운로드·사용, 모델 구조·소스 코드 공개 |
| 단점 | API 호출마다 비용, 모델·출력 정보 제한적 | (상대적으로 성능 열세) |

- 폐쇄형 3사 특징: ChatGPT(최다 활성 유저·전반적 성능), Claude(안전 지향, 코딩 강점), Gemini(최장 1M 토큰 컨텍스트, 멀티모달 강점). GPT-5 계열처럼 플래그십/mini/nano로 가격·성능을 계층화해 제공.
- 개방형 모델은 Hugging Face에서 다운로드해 활용 (200만+ 모델).

---

## 거대 언어 모델의 학습 — 정렬

### GPT-3: 거대 언어 모델의 시초 (p.31-32)

| 항목 | 값 |
|---|---|
| 크기 | 175B 파라미터 (96 layers, $d_{model}$=12288) — 이전 언어 모델 대비 최소 10배 |
| 학습 방법 | 다음 토큰 예측 |
| 학습 데이터 | 3000억 토큰 (4TB 텍스트 = 인터넷 + 양질의 텍스트북) — Common Crawl 60%, WebText2 22%, Books1·2 각 8%, Wikipedia 3% |
| 학습 비용 | 약 150억 원으로 추산 |

인-컨텍스트 학습 능력이 본격적으로 나타난 첫 모델.

### 왜 정렬이 필요한가 (p.33-34)

다음 토큰 예측만 한 모델의 한계:

- "달 착륙을 6살에게 설명해줘" → 설명 대신 비슷한 지시문("중력을 6살에게 설명해줘...")을 이어서 생성 — **지시를 따르지 않음**
- "표지판 자르는 도구 알려줘" → 그대로 알려줌 — **유해 응답**

**정렬(Alignment) 학습**: LLM의 출력이 사용자의 의도와 가치를 반영하도록 하는 것.

1. **지시 학습(Instruction tuning)** — 주어진 지시에 어떤 응답이 생성되어야 하는지
2. **선호 학습(Preference learning)** — 상대적으로 어떤 응답이 더 선호되어야 하는지

### 지시 학습 (p.35-42)

- 방법 자체는 기존 지도 추가 학습(Supervised Fine-Tuning, SFT)과 동일. 차이는 **모든 NLP 태스크를 자연어 지시로 표현**해 한 모델에 학습시키는 것 (FLAN).
  - 감정 분석 → "주어진 리뷰 속 유저의 감정이 긍정적이야, 부정적이야?"
  - 같은 태스크도 여러 지시 템플릿으로 다양화
- 평가 설계: 일반화 성능을 재기 위해 **관련 없는 태스크 클러스터를 학습에서 통째로 제외**하고 테스트에만 사용 (예: 요약은 학습 제외, 테스트만).
- 결과: 예시 없이도(0-shot) 새 지시에 올바르게 응답하는 능력 급증. LaMDA-PT 137B를 지시 학습한 FLAN이 GPT-3 175B zero-shot을 능가.
- 성능의 3가지 핵심 요소 (p.42):
  1. **학습 태스크 개수** — 다양할수록 일반화↑
  2. **모델 크기** — 특정 규모 이하에서는 지시 학습이 오히려 역효과 → 지시를 이해·응답하는 것도 창발성
  3. **지시 방식** — 자연어로 대화하듯 지시하는 것이 가장 효과적

### 선호 학습 (p.43-52)

지시 학습의 한계: **입력마다 적절한 정답이 하나라고 가정**한다.

- 수학처럼 정답이 정해진 태스크는 자연스러움 (GSM8K 예: 양의 정수 m, n의 최대공약수 6, 최소공배수 126일 때 m+n의 최솟값 = 60)
- 번역·시 같은 개방형(open-ended) 태스크는 좋은 답이 여러 개 → 복수 정답을 허용하는 대신 **더 선호되는 응답을 생성**하도록 학습하고 싶다.

**선호 학습**: 다양한 응답은 모델이 생성하고, 응답 간 선호도는 사람이 제공. ChatGPT를 만든 핵심 알고리즘(공식 블로그에서 InstructGPT와 같은 방법의 RLHF 사용을 언급).

**InstructGPT의 RLHF 3단계** (p.48-52):

```text
Step 1. 지시 학습 (SFT)
  유저 지시 수집 → 사람 주석자가 시범 답변 작성 → GPT-3를 지도 학습
Step 2. 보상 모델 (Reward Model, RM) 학습
  지시 학습된 모델이 응답 후보 여러 개 생성 → 사람이 순위 매김 (D > C > A = B)
  → 사람과 일치한 선호도를 출력하도록 RM을 지도 학습 (선호 응답 입력 → 높은 보상)
Step 3. 강화 학습 (PPO)
  새 프롬프트 → 정책(모델)이 응답 생성 → RM이 보상 계산 → 보상이 높아지도록 정책 업데이트
  ※ Step 3에는 사람 개입 없음 — 학습된 RM이 사람을 대신
```

- 결과 (p.53-54): 사람 평가에서 단순 프롬프팅·SFT보다 우위, 모델 크기(1.3B → 6B → 175B) 전 구간에서 일관된 향상. 유해 응답(RealToxicity)·거짓 응답(TruthfulQA, Hallucinations)도 감소.
- **LLaMA2** (p.55-56): 같은 철학으로 학습하되 보상 모델을 **Safety RM / Helpful RM 2개로 분리**. 당시 대화형 개방형 모델 중 최고 성능.

---

## 거대 언어 모델의 추론

### 자동회귀 생성 (p.59-63)

- 학습된 LLM은 **순차적 추론으로 토큰을 하나씩** 생성한다(Auto-regressive Generation). EOS 토큰(예: BERT의 [SEP]) 생성 또는 사전 정의된 토큰 수 도달 시 종료.
- 형식화: 입력 $x = [x_1, \dots, x_L]$에 대해 모델은 다음 토큰 확률 분포 $\hat{p}(x)$를 제공. **디코딩 알고리즘 = $\hat{p}(x)$로부터 $x_{L+1}$을 선택하는 방법**.

```python
from transformers import AutoTokenizer, AutoModelForCausalLM
tokenizer = AutoTokenizer.from_pretrained("gpt2")
model = AutoModelForCausalLM.from_pretrained("gpt2")
prompt = "Today I believe we can finally"
input_ids = tokenizer(prompt, return_tensors="pt").input_ids
outputs = model.generate(input_ids, do_sample=False, max_length=30)  # 자동회귀 생성
tokenizer.batch_decode(outputs, skip_special_tokens=True)
```

`generate()` 인자 조합이 곧 디코딩 알고리즘 선택이다: `num_beams=1, do_sample=False` → greedy / `do_sample=True` → sampling / `num_beams>1` → beam search.

### 디코딩 알고리즘 6종 (p.64-77)

1. **Greedy Decoding** — 매 시점 확률이 가장 높은 토큰 선택. 직후만 보므로 최종 문장이 최선이 아닐 수 있다.
2. **Beam Search** — 확률 상위 $k$개(beam size) 후보 문장을 **누적 생성 확률**로 동시에 유지.
   - 예: greedy는 "The nice(0.5) woman(0.4)" = 0.20 경로를 택하지만, beam은 "The dog(0.4) has(0.9)" = 0.36을 찾아낸다.
   - 대가: 후보마다 LLM 추론을 수행하므로 계산 비용 증가.
3. **Sampling** — 확률 분포에서 랜덤 추출. 다양성↑, 품질 불안정.
4. **Sampling with Temperature** — 하이퍼파라미터 $T$로 분포를 조작.
   - $T > 1$: 분포가 평평(smooth)해져 다양한 응답 (창의적이나 품질 저하 가능)
   - $T < 1$: 분포가 뾰족(sharp)해져 최고 확률 응답에 집중 (안정적이나 다양성 부족)
5. **Top-K Sampling** — 확률 상위 $K$개 중에서만 샘플링. 잡음 단어를 배제하지만 $K$가 고정이라 문맥에 따라 불균형 — 다음 단어 후보가 넓은 문맥(누적 0.68)과 좁은 문맥(누적 0.99)에 같은 $K$를 쓴다.
6. **Top-P Sampling (Nucleus)** — $K$를 고정하는 대신 **누적 확률이 $P$를 처음 넘는 상위 후보들**에서 샘플링 → 후보 수가 문맥에 따라 자동 조절.
   - Holtzman et al. 2020: 사람 텍스트 Perplexity 12.38에 Nucleus(p=0.95)가 13.13으로 가장 근접. Greedy는 PPL 1.50으로 오히려 지나치게 확신하며 반복률 73.66%로 퇴화.

| 알고리즘 | 장점 | 단점 |
|---|---|---|
| Greedy Decoding | 쉬운 사용법 | 최적해 보장 X |
| Beam Search | 좋은 응답 생성 확률 ↑ | 큰 계산 비용 |
| Sampling | 다양한 응답 생성 가능 | 품질 불안정 |
| Sampling with Temperature | 창의성/안정성 조절 가능 | T↑: 품질 저하, T↓: 다양성 부족 |
| Top-K Sampling | 잡음 단어 배제, 품질 향상 | K값 고정 → 문맥 따라 불균형 |
| Top-P Sampling (Nucleus) | 확률 누적 기준, 품질·다양성 균형 | P값 설정 필요 (경우에 따라 랜덤성 여전) |

> 실전 연결 — OpenAI Playground의 Temperature, Top P 슬라이더가 바로 이 값들. 같은 질문("SSAFY에 대해 알려줘")도 설정에 따라 다른 응답이 나온다 (p.78-79).

### 프롬프트 엔지니어링 (p.80-101)

**입력 프롬프트 = (1) 지시(instruction) + (2) 예시(few-shot examples)** — 무엇을 어떻게 지시하고 어떤 예시를 주는지가 성능을 크게 좌우한다. 원하는 답을 얻기 위해 입력을 설계·조정하는 기법이 프롬프트 엔지니어링.

**지시 = 시스템 프롬프트 + 유저 쿼리** (p.84-88)

- 시스템 프롬프트: 유저 쿼리와 무관하게 "기본값"으로 주어지는 LLM 입력. 서비스에 따라 유저 수정 권한이 있을 수도, 없을 수도.
- 활용: 응답 규칙 강제(경쟁사 언급 금지), 개인화(메모리 기반), 페르소나(캐릭터 대화 서비스). Anthropic은 Claude의 시스템 프롬프트를 공개.
- 주의: 시스템 프롬프트의 제약이 답변에 항상 반영되지는 않는다.

**Chain-of-Thought(CoT) 프롬프팅** (p.91-95)

- 질문-정답만 예시로 주지 말고 **추론 과정을 예시에 포함** → 모델이 추론을 생성한 뒤 답하도록 유도 → 수학 등 복잡한 문제의 정답률 상승.
- CoT 효과는 모델이 클수록 커진다(PaLM 540B에서 GSM8K 대폭 향상) → 추론 능력도 창발성.
- **0-shot CoT**: 예시 없이 `Let's think step by step` 한 문장만 추가해도 추론 성능이 크게 향상. 역시 임계 규모를 넘어야 효과.

**Few-shot 예시 선택** (p.96-98)

- 어떤 예시를 주느냐에 따라 성능이 크게 흔들림 (SST-2 감정 분석, 예시 5회 무작위 선택 → 정확도 86.9~95.8).
- 해결: **풀려는 쿼리와 유사한 예시를 거리 기반으로 선택** (KATE) — 문장 인코더로 임베딩 후 kNN. 가까운 예시 10개(EM 46.0)가 먼 예시(31.0)보다 훨씬 낫다.

**프롬프트 엔지니어링 in 2026 = SKILL.md** (p.99-101)

- 특정 태스크를 위해 엔지니어링된 프롬프트를 파일(SKILL.md: name + description + 지침·예제)로 공유·재사용. 에이전트가 필요할 때 스킬 파일을 읽어 컨텍스트 윈도우에 로드해 정해진 절차를 수행 — "반복적인 지시를 한 번에".

---

## 거대 언어 모델의 평가

### 평가의 3요소와 LLM 평가의 특징 (p.104-110)

평가(Evaluation) 설계의 3요소: **① 목표 ② 평가 방법 ③ 평가 지표** (예: 배달 앱 — 배달하는 것 / 배달 시간 측정 / 평균 배달 시간).

LLM 평가가 기존 AI 모델 평가와 다른 점:

- 다양한 태스크를 동시에 학습했으므로 **많은 태스크 성능을 종합적으로** 판단해야 함 (GPT-4 리포트: MMLU 86.4% 5-shot, HumanEval 67.0% 0-shot, GSM-8K 92.0% 5-shot CoT 등 벤치마크 묶음으로 비교)
- 디코딩 알고리즘·프롬프트(shot 수 등)에 따라 같은 질문도 예측이 바뀜 → **공평한 비교를 위해 조건을 명시·통제**해야 함

### 평가 방법의 분류 (p.111-118)

| 상황 | 방법 | 예시 |
|---|---|---|
| 정답이 정해진 경우 | 예측-정답 일치도 = **정확도(Accuracy)** | MMLU (57개 전문 분야 객관식) |
| 정답이 없는 경우 #1 | 사람이 임의 정답 작성 후 **유사도 비교** | ROUGE (단어 겹침), 임베딩 코사인 유사도 (Sentence-BERT) |
| 정답이 없는 경우 #2 | 정답 무관, **텍스트 자체 품질** 측정 | Perplexity |
| 정답이 없는 경우 #3 | 생성 텍스트 간 **상대적 선호** 평가 | 사람 선호(LMArena), LLM-as-judge |

- ROUGE-1 예: 정답 "The cat sat on the mat." vs 출력 "The cat lay on the mat." → 겹친 unigram 5/6 = 0.83
- Perplexity (언어 모델 $p_\theta$로 계산, 예: GPT-2):

$$\mathrm{PPL}(X) = \exp\left\{-\frac{1}{t}\sum_{i}^{t} \log p_\theta(x_i \mid x_{<i})\right\}$$

- 창의성·유창성·가독성처럼 지표를 따로 설계하기 어려운 측면은? 기존에는 사람을 고용 → 이 역할을 LLM으로 대체하자는 것이 다음 주제.

### LLM-as-judge (p.119-139)

- **LLM-as-judge (G-Eval)**: 풀려는 태스크 + 평가할 텍스트 + 평가 기준을 주면 LLM이 점수·피드백을 생성.
- GPT-4 기반 평가는 ROUGE·BERTScore 등 기존 지표보다 **사람 평가와의 상관이 높다**.
- 이미 실무에 쓰임: LLaMA3 학습 데이터 품질 필터링, FineWeb의 교육적 가치 점수(0-5 additive rubric) 매기기.
- 상대 선호 평가에도 사용: 사람 선호 기반 **LMArena**(두 모델 응답 블라인드 비교, 가장 신뢰받는 방법 중 하나)는 비용·시간이 크다 → 심사위원 역할을 LLM에게 (MT-Bench: "공정한 심사위원으로서 A/B 중 더 나은 답 선택, [[A]]/[[B]]/[[C]] 형식 출력").

**LLM-as-judge의 3가지 편향과 보완** (p.134-139):

| 편향 | 내용 | 보완 |
|---|---|---|
| 위치 편향 | 특정 위치(첫 번째) 응답을 선호 — A/B 순서를 바꾸면 판정이 뒤집힘 | 순서를 바꿔 두 번 평가 후 평균 |
| 길이 편향 | 품질과 무관하게 긴 응답 선호 (중복 내용으로 늘려도 선호) | 길이 영향을 통계적으로 제거 (AlpacaEval의 Length-Controlled win rate) |
| 자기 선호 편향 | 생성 모델과 평가 모델이 같으면 자기 응답 선호 | 인지하고 다른 모델로 교차 평가 |

---

## 거대 언어 모델의 응용과 한계

### 응용 1: 멀티모달 파운데이션 모델 (p.141-144)

**핵심 아이디어: 다른 모달리티 데이터를 LLM이 이해할 수 있도록 토큰화 + 추가 학습.**

LLaVA(이미지+텍스트) 구조 (p.142):

```text
X_v(이미지)     → Vision Encoder → Z_v(특징 벡터) → Projection W → H_v(이미지 토큰 표현) ┐
X_q(텍스트 질문) → Tokenization  → H_q(텍스트 토큰 표현) ──────────────────────────────┴→ f_φ(Language Model) → X_a(응답)
```

- GPT-4o: 멀티모달 입력(이미지·비디오·오디오) → 멀티모달 출력(오디오·텍스트) 실시간 질의응답
- VideoPoet: 비디오·오디오까지 토큰화해 LLM으로 zero-shot 비디오 생성
- HELIX(Figure AI): 비전-언어 모델이 의미 이해 + 제어 신호 변환 → 로봇 행동 생성 (Robotics)

### 응용 2: 합성 데이터 생성 (p.145-154)

**Self-Instruct**: 사람이 작성한 **175개** 시드 데이터로 시작, GPT-3로 **52000개** 합성 지시 데이터 생성.

```text
175개 시드 → Task Pool → ① 지시 생성 (in-context 예시로 새 지시 생성)
          → ② 분류 태스크 여부 판별 → ③ 인스턴스 생성 (Input-first / 분류는 Output-first)
          → ④ 필터링: ROUGE-L 유사도로 중복 제거(유사도 높음), 노이즈 제거 → Pool에 추가 (반복)
```

- 효과: GPT-3 + 합성 데이터 ROUGE-L 39.9 ≈ 사람 데이터 기반 InstructGPT 40.8. SuperNI와 함께 학습 시 51.6 > 49.5 — **소량의 사람 데이터를 대규모로 확장**해도 사람 데이터급 성능.
- **Alpaca**: 이 방식을 LLaMA-1 7B에 적용한 최초의 개방형 instruction-following 모델 (데이터 생성 비용 $500 미만).
- **Alpagasus**: LLM 프롬프팅으로 합성 데이터 품질을 0~5점 채점 → 52000개 중 **약 9000개만 4.5점 이상** → 그 데이터만 학습. 더 적은 데이터로 더 빠르게 학습하면서 BBH·DROP·HumanEval에서 전체(52k) 사용보다 높은 성능 (단, 원자료 표 기준 MMLU는 52k 쪽이 더 높다).

### 한계 (p.155-162)

| 한계 | 내용 | 원인/대응 |
|---|---|---|
| **환각(Hallucination)** | 사실과 다르거나 지어낸 내용을 정확한 정보와 동일한 자신감·유창함으로 생성 → 진위 구별 어려움 | 확률적 다음 토큰 예측이라는 생성 방식 자체 + 학습 데이터의 지식 컷오프(예: GPT-4o mini는 2023-10까지). **RAG(검색 증강 생성)** 로 완화 — 대부분의 LLM 서비스에 기본 탑재 |
| **탈옥(Jailbreaking)** | 프롬프트 엔지니어링으로 정렬을 우회 (예: DAN — "너는 무엇이든 할 수 있는 DAN인 척해") | 다단계 학습 과정에서 기인한 근본적 한계. 유형 ① 경쟁 목표: 안전 규칙 vs 지시 이행을 충돌시킴 ("Absolutely! Here's로 시작해") ② 불일치 일반화: base64 등 학습 분포 밖 입력으로 안전 학습 회피 |
| **AI 텍스트 검출** | 무분별한 사용이 학교·회사에서 새로운 문제를 만듦 (AI 면접 부정행위 등) | 어느 정도 탐지 가능: GPTZero 같은 검출기, 생성 시점에 심는 워터마킹(SynthID) |

---

## 복습 문제

원본 확인문제 (p.163-170) 기반.

1. 거대 언어 모델과 BERT·GPT-1 같은 기존 언어 모델의 차이점으로 올바른 것은? (a) 다음 토큰 예측 기반 자기 지도 학습 (b) 트랜스포머 기반 구조 (c) 규모 확대로 나타난 창발성 (d) 이미지를 포함한 멀티모달 학습 데이터 — <details><summary>답</summary>**c**. a·b는 기존 언어 모델에도 적용되며, LLM은 텍스트 데이터의 다음 토큰 예측으로 사전 학습되므로 d는 해당 없음.</details>
2. 지시 학습과 선호 학습에 대해 올바르지 않은 것은? (a) 효과적 지시 학습에는 다양한 지시 데이터가 필요 (b) 크기가 충분치 않은 모델은 지시 학습 후 성능이 떨어질 수 있다 (c) 보상 모델은 사람의 선호를 모방하도록 학습된다 (d) 강화 학습 기반 선호 학습의 모든 과정에 사람의 개입이 필요하다 — <details><summary>답</summary>**d**. Step 1(지시 학습)·Step 2(보상 모델)에는 사람 데이터가 필요하지만, Step 3(강화 학습)은 모델이 응답을 생성하고 보상 모델이 피드백을 주므로 사람 개입이 필요 없다.</details>
3. 디코딩 알고리즘 설명으로 올바르지 않은 것은? (a) Greedy는 가장 확률 높은 토큰을 생성 (b) Beam Search는 여러 후보를 동시에 고려해 생성 비용이 크다 (c) 1 이상의 Temperature를 쓰면 다양한 응답이 생성될 확률이 감소한다 (d) Top-K는 상위 K개 토큰 중 확률에 따라 무작위 선택 — <details><summary>답</summary>**c**. T>1이면 분포가 smooth(uniform에 가깝게)해져 다양한 토큰·응답이 생성될 확률이 **증가**한다.</details>
4. LLM의 평가·한계에 대해 올바르지 않은 것은? (a) 정답을 몰라도 LLM-as-judge로 평가 가능 (b) LLM-as-judge 선호 판단에는 짧은 응답을 선호하는 길이 편향이 있다 (c) 최신 LLM은 안전성 훈련이 잘 되어 탈옥이 일어나지 않는다 (d) LLM 생성 응답은 탐지될 수 있으므로 무분별하게 사용하면 안 된다 — <details><summary>답</summary>**b와 c 중 출제 의도상 b** (원본 해설 기준). 길이 편향은 **긴** 응답을 선호하는 것. 참고로 c도 사실과 다르다 — 원본 문제·해설 모두 b를 정답으로 제시하나, c 역시 틀린 서술(탈옥은 여전히 발생)이므로 시험에서는 문항 맥락을 확인할 것.</details>

### 강의 정리 요약 (p.171)

- LLM = **1B 이상**의 큰 트랜스포머 모델이 대규모 텍스트에서 **다음 토큰 예측**으로 사전 학습된 모델. 실 세계 적용을 위해 **지시 학습 + 선호 학습**으로 추가 학습.
- 추론: 다양한 **디코딩 알고리즘**이 있고, **프롬프트**(예: CoT)에 따라 성능이 크게 변한다.
- 평가: 정답이 있으면 기존 방식, 없으면 **LLM-as-judge**. 환각·탈옥·AI 텍스트 감지 등 한계는 여전.

---

## 실습 예고: 합성 데이터 제작

실습 주제 (p.176-180): 합성 데이터(Synthetic Data) 제작 과정을 직접 경험.

- [ ] 환경 설정: API 통신 개념 (프로그램 간 정해진 규칙으로 대화), `httpx`(비동기 HTTP 클라이언트) / `json`(응답 파싱) 라이브러리
- [ ] 프롬프트 엔지니어링으로 합성 데이터 생성 — 생성 품질은 프롬프트에 크게 의존 (3-2절 참고)
- [ ] 생성 결과를 구조화해 학습용 데이터셋으로 변환 (Self-Instruct, Alpagasus 방식 참고)
- [ ] **LLM-as-judge 시스템**으로 합성 데이터 품질 자동 평가 — 벤치마크 생성 → 답안 생성 → 채점(점수·정확성·가독성) 파이프라인 (4절 참고)

---

**정리 체크리스트**

- [ ] 파운데이션 모델의 3가지 구성요소를 말할 수 있다
- [ ] BERT와 GPT-3의 차이를 창발성·스케일링 관점에서 설명할 수 있다
- [ ] RLHF 3단계에서 사람이 개입하는 단계와 아닌 단계를 구분할 수 있다
- [ ] Temperature, Top-K, Top-P가 확률 분포를 각각 어떻게 바꾸는지 설명할 수 있다
- [ ] 0-shot CoT가 무엇이고 왜 큰 모델에서만 통하는지 말할 수 있다
- [ ] LLM-as-judge의 3가지 편향과 각 보완책을 짝지을 수 있다
- [ ] 환각의 원인을 학습 방식과 연결해 설명하고 RAG의 역할을 말할 수 있다

> 정리 노트 범위 밖: 표지·목차·학습목표(p.1-4), 마무리 인사·참고 자료 목록(p.172-175)은 생략했다. OCR 기반으로 읽은 구간(p.25-180) 중 한글 조사·어미가 깨진 부분은 문맥으로 복원했고, 수식·표·수치가 있는 페이지(p.25, 32, 40, 44, 61, 66, 76, 77, 118, 142, 150, 153, 154, 171)는 고해상도 이미지로 재확인했다.
