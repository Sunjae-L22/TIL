# 토큰과 컨텍스트 윈도우 — 토크나이저, 과금 구조, 그리고 대화 기억 3전략

> 출처: `1-2-development-environment-and-llm-api-mechanism.pdf`, `1-3-langchain-framework-deep-dive.pdf` 등 LLM 추가자료 · 정리일: 2026-08-24
> 블로그 발행본: https://it-study-2002.tistory.com/entry/%ED%86%A0%ED%81%B0%EA%B3%BC-%EC%BB%A8%ED%85%8D%EC%8A%A4%ED%8A%B8-%EC%9C%88%EB%8F%84%EC%9A%B0
> 이어지는 편: [0824 RAG 설계 결정](0824_RAG설계결정.md)

LLM API를 처음 붙이고 나면 예외 없이 같은 순서로 놀란다. 먼저 **잘 된다**에 놀라고, 그다음 **생각보다 비싸다**에 놀라고, 마지막으로 **대화가 길어지니까 갑자기 느려지고 더 비싸진다**에 놀란다. 세 번째 놀람이 특히 당황스럽다. 코드를 바꾼 것도 없고 질문 길이도 그대로인데 열 번째 턴의 요금이 첫 번째 턴의 몇 배가 되어 있다.

세 가지 모두 하나의 사실에서 나온다. **LLM은 글자를 세지 않고 토큰을 센다.** 그리고 한 번의 요청에 실을 수 있는 토큰의 총량 — 컨텍스트 윈도우 — 은 정해져 있다. 이 두 가지를 모르면 LLM 앱의 비용도 응답 속도도 품질도 전부 통제할 수 없는 변수가 된다.

> 이 글은 [LangChain 체인과 RAG](https://it-study-2002.tistory.com/entry/LangChain-%EC%B2%B4%EC%9D%B8%EA%B3%BC-RAG)에서 이어진다. 그 글이 LLM을 **어떻게 호출하는가**를 다뤘다면, 이 글은 그 호출이 **무엇으로 계산되는가**를 다룬다.

이 글은 그 계산 단위를 기준점으로 만들기 위한 정리다. 토큰의 정체에서 출발해 한국어가 실제로 몇 배나 비싼지 직접 재 보고, 요금이 붙는 자리를 해부한 뒤, 컨텍스트 윈도우라는 예산을 어떻게 배분할지까지 간다. 마지막 절의 대화 기억 3전략은 그 예산 관리의 가장 구체적인 사례다.

<br>

## 📖 이 글에서 다루는 내용

1. 토큰은 단어가 아니다 — 쪼개지는 단위의 정체
2. 한국어는 정말 더 비싼가 — 직접 재 본 토큰 밀도
3. 토크나이저는 세대마다 바뀐다 — 같은 문장, 다른 값
4. 과금 해부 — 입력 토큰, 출력 토큰, 그리고 보이지 않는 추론 토큰
5. 컨텍스트 윈도우 — 예산의 총액
6. 예산 안에서 경쟁하는 네 부류
7. 대화가 길어질 때 벌어지는 일 — 누적 비용의 함정
8. 대화 기억 3전략 — Buffer, Summary, VectorStoreRetriever
9. 입력 품질이 출력 품질을 정한다
10. 새는 곳 막기 — 키와 실행 환경

<br>

---

## 1. 토큰은 단어가 아니다 — 쪼개지는 단위의 정체

LLM은 문자열을 그대로 먹지 않는다. 먼저 **토크나이제이션(tokenization)** 을 거쳐 정수 ID의 배열로 바꾸고, 그 ID를 벡터로 바꿔 신경망에 넣는다. 이때 나뉜 조각 하나가 **토큰(token)** 이다.

> **토큰 (token)** — 모델이 처리하는 텍스트의 최소 단위. 단어일 수도, 단어의 일부일 수도, 공백이나 기호 하나일 수도 있다.

"단어 단위 아닌가?"라는 짐작이 가장 흔한 오해다. 실제로 돌려보면 이렇다.

```python
import tiktoken

enc = tiktoken.get_encoding('o200k_base')   # 최신 GPT 계열이 쓰는 인코딩
ids = enc.encode('unbelievable')

print(ids)                                   # [373, 9880, 45794]
print([enc.decode([i]) for i in ids])        # ['un', 'bel', 'ievable']
```

`unbelievable`은 영어 사전에 있는 한 단어지만 **토큰 3개**다. 반대로 `안녕하세요`는 다섯 글자인데 `['안', '녕하세요']` 두 토큰으로 끝난다.

> **[그림]** unbelievable이 un/bel/ievable 세 조각으로, 토크나이제이션은이 일곱 조각으로 나뉘는 모습을 나란히 놓고 토큰이 단어도 글자도 아님을 보여주는 해부도

규칙이 없어 보이는 이유는 실제로 언어학적 규칙이 아니기 때문이다. 대부분의 LLM은 **BPE(Byte Pair Encoding)** 계열 알고리즘을 쓰는데, 원리는 단순하다 — 학습 말뭉치에서 **자주 붙어 나오는 바이트 쌍부터 차례로 하나의 토큰으로 합친다.** 그래서 결과는 이렇게 정리된다.

| 성질 | 이유 |
|---|---|
| 흔한 단어일수록 통째로 한 토큰 | 자주 등장해서 병합 단계 초반에 합쳐졌다 |
| 드문 단어일수록 잘게 쪼개짐 | 병합될 만큼 자주 나오지 않았다 |
| 앞의 공백이 토큰에 붙는다 | `계산`과 ` 계산`은 서로 다른 토큰이다 |
| 어떤 문자열도 인코딩 실패가 없다 | 최악의 경우 바이트 단위까지 내려간다 |

세 번째 항목이 특히 실무에서 자주 문제가 된다. 프롬프트를 다듬다 공백 하나를 넣고 빼는 것만으로 토큰 경계가 통째로 밀릴 수 있다.

> 💡 **글자 수로 토큰을 어림하는 관행이 있지만 언어마다 배율이 다르다**
> 영어는 "4글자 ≈ 1토큰"이 그럭저럭 맞는다. 한국어에 그 비율을 쓰면 크게 어긋난다 — 다음 절에서 그 배율을 직접 잰다. 어림값이 필요하면 언어별로 자기 데이터에 대고 한 번 재서 자기 배율을 갖는 편이 낫다.

<br>

---

## 2. 한국어는 정말 더 비싼가 — 직접 재 본 토큰 밀도

"한국어는 토큰을 많이 먹는다"는 말은 자주 들리는데 몇 배인지는 잘 안 나온다. 재 보면 된다. 뜻이 같은 두 문장을 같은 인코딩에 넣는다.

```python
import tiktoken
enc = tiktoken.get_encoding('o200k_base')

en = 'Tokenization is the process of splitting text into smaller units.'
ko = '토크나이제이션은 텍스트를 더 작은 단위로 나누는 과정이다.'

for name, s in [('영어', en), ('한국어', ko)]:
    print(name, '글자', len(s), '토큰', len(enc.encode(s)))

# 영어   글자 65  토큰 12
# 한국어 글자 32  토큰 22
```

**글자 수는 한국어가 절반인데 토큰 수는 거의 두 배다.** 한 문장으로는 표본이 적으니 문체가 다른 문장으로 한 번 더 확인한다.

```python
ko2 = '이 약관에서 정하는 보험금의 지급사유가 발생한 경우 회사는 보험수익자에게 약정한 보험금을 지급한다.'
en2 = ('The company shall pay the agreed insurance benefit to the beneficiary '
       'when the payment event stipulated in these terms occurs.')

# 한국어 글자 55  토큰 31
# 영어   글자 126 토큰 21
```

| 문장 | 글자 수 | 토큰 수 | 글자당 토큰 |
|---|---|---|---|
| 영어 (일반문) | 65 | 12 | 0.18 |
| 한국어 (일반문) | 32 | 22 | 0.69 |
| 영어 (약관문) | 126 | 21 | 0.17 |
| 한국어 (약관문) | 55 | 31 | 0.56 |

> **[그림]** 같은 뜻의 영어 문장과 한국어 문장을 글자 수와 토큰 수 두 축의 막대로 비교해 한국어가 글자당 토큰을 3~4배 소비함을 보여주는 그래프

**글자당 토큰이 3~4배다.** 같은 내용을 담는 데 필요한 글자 수가 한국어 쪽이 절반이라 최종 배율은 1.5~1.8배로 줄지만, 그래도 같은 문서를 넣으면 한국어 쪽이 눈에 띄게 비싸다.

왜 이렇게 되는지는 토큰을 하나씩 꺼내 보면 바로 보인다.

```python
enc.encode('보험료는 어떻게 계산되나요?')
# ['보험', '료', '는', ' 어떻게', ' 계산', '되', '나요', '?']   → 8토큰
```

`보험`과 ` 계산`처럼 흔한 명사는 통째로 한 토큰이 됐지만, `료`·`는`·`되`·`나요` 같은 **조사와 어미는 개별 토큰으로 떨어져 나간다.** 한국어는 교착어라 어간 뒤에 조사와 어미가 계속 붙는데, 그 조합이 워낙 많아 통째로 병합될 만큼 자주 등장하기 어렵다. 게다가 학습 말뭉치의 대부분이 영어라 한글 조각은 애초에 병합 우선순위가 낮다.

> ⚠️ **토큰 한도를 글자 수로 방어하면 한국어에서 뚫린다**
> "입력을 2,000자로 제한했으니 안전하다"는 식의 방어는 언어에 따라 배율이 3배 넘게 달라져서 의미가 없다. 한도 검사는 반드시 **토크나이저로 실제 토큰 수를 세서** 한다. 요약 길이 지정도 마찬가지다 — "500자 이내"보다 "300토큰 이내"가 비용과 직결되는 단위다.

<br>

---

## 3. 토크나이저는 세대마다 바뀐다 — 같은 문장, 다른 값

토큰 수는 **모델 고유의 값이 아니라 토크나이저에 딸린 값**이다. 같은 문장을 이전 세대 인코딩(`cl100k_base`, GPT-4 계열)에 넣으면 결과가 달라진다.

```python
c = tiktoken.get_encoding('cl100k_base')   # 이전 세대
o = tiktoken.get_encoding('o200k_base')    # 현재 세대

# '토크나이제이션은 텍스트를 더 작은 단위로 나누는 과정이다.'
#   cl100k_base → 35토큰
#   o200k_base  → 22토큰       (영어 문장은 둘 다 12토큰으로 동일)
```

**영어는 그대로인데 한국어만 35 → 22로 줄었다.** 이전 세대에서 무슨 일이 벌어지고 있었는지는 조각을 꺼내 보면 드러난다.

```python
[c.decode([i]) for i in c.encode('보험료는 어떻게 계산되나요?')]
# ['보', '�', '�', '�', '료', '는', ' �', ... ]   → 18토큰
# 같은 문장을 o200k_base로 넣으면 8토큰
```

`�`는 유니코드의 **대체 문자(replacement character)** 다. 디코딩했을 때 온전한 글자가 되지 못한 조각, 즉 **한 글자를 이루지 못한 UTF-8 바이트 조각**이라는 뜻이다. 한글 한 글자는 UTF-8로 3바이트인데, 어휘집에 그 글자가 없으면 바이트 단위까지 내려가 **글자 하나가 토큰 3개**가 된다. 같은 문장이 이전 세대에서 18토큰, 현재 세대에서 8토큰인 이유가 이것이다. `험`·`떻`처럼 상대적으로 덜 흔한 글자만 골라 3배가 됐다는 점도 눈여겨볼 만하다.

> **[그림]** 같은 한국어 문장이 이전 세대 인코딩에서는 바이트 단위로 쪼개져 35토큰, 현재 세대에서는 22토큰으로 처리되는 대비를 보여주는 다이어그램

| | cl100k_base (이전 세대) | o200k_base (현재 세대) |
|---|---|---|
| 어휘집 크기 | 약 10만 | 약 20만 |
| 영어 문장 (예시) | 12토큰 | 12토큰 |
| 한국어 문장 (예시) | 35토큰 | 22토큰 |
| 한글 처리 | 상당수가 바이트 단위로 분해 | 흔한 어절이 통째로 병합 |

> 💡 **모델을 바꾸면 비용 추정도 다시 해야 한다**
> 토큰당 단가만 비교하고 모델을 고르면 틀린다. 어휘집이 다르면 **같은 프롬프트의 토큰 수 자체가 달라지기** 때문이다. 단가가 20% 싼 모델이 한국어 토큰을 50% 더 먹으면 오히려 손해다. 비교는 반드시 `단가 × 실제 토큰 수`로 한다. 그리고 토큰 수를 세는 코드는 모델명과 함께 관리해야 한다 — `tiktoken.encoding_for_model(model)`처럼 모델에서 인코딩을 끌어오는 방식이 안전하다.

<br>

---

## 4. 과금 해부 — 입력 토큰, 출력 토큰, 그리고 보이지 않는 추론 토큰

이제 요금이 어디에 붙는지 본다. LLM API의 과금은 두 축이다.

| 축 | 무엇이 들어가는가 | 특징 |
|---|---|---|
| **입력 토큰** | 시스템 메시지, 이전 대화 전체, 도구 실행 결과, 검색된 문서, 이번 질문 | 단가가 싸다. 대신 **양이 폭발한다** |
| **출력 토큰** | 모델이 새로 생성한 답변 | 단가가 몇 배 비싸다. 대신 양은 대체로 통제 가능하다 |

여기서 놓치기 쉬운 게 **입력 토큰의 범위**다. "내가 방금 친 질문"만 입력이 아니다. LLM API는 무상태(stateless)라서 이전 대화를 기억하지 못하고, 그래서 **매 요청마다 대화 전체를 다시 보낸다.** 열 번째 턴의 입력에는 첫 번째 턴부터 아홉 번째 턴까지가 전부 들어 있다.

그리고 최근 모델에는 세 번째 항목이 있다. **추론 토큰(reasoning token)** 이다.

```text
요청 1건의 토큰 구성

[입력 토큰]
  시스템 메시지          ~200
  이전 대화 9턴          ~4,800
  검색된 문서 3청크      ~1,500
  이번 질문              ~40
                        ────────
                        6,540 토큰

[출력 토큰]
  추론 토큰 (화면에 안 보임)   ~800   ← 과금된다
  실제 답변                    ~300
                              ────────
                              1,100 토큰
```

추론 모델은 답을 내기 전에 내부적으로 사고 과정을 생성한다. 그 과정은 응답 본문에 나오지 않지만 **출력 토큰으로 계산되어 과금된다.** 답변은 세 줄인데 출력 토큰이 수천인 상황이 여기서 나온다.

> **[그림]** 요청 한 건의 토큰이 시스템 메시지·대화 기록·검색 문서·질문으로 이루어진 입력과 추론 토큰·답변으로 이루어진 출력으로 나뉘는 구조를 비율 막대로 보여주는 해부도

실제 사용량은 추정하지 말고 응답 객체에서 읽는다.

```python
response = client.responses.create(model=model, input='안녕')

print(response.usage.input_tokens)    # 실제 입력 토큰
print(response.usage.output_tokens)   # 추론 토큰 포함
print(response.usage.total_tokens)
```

> ⚠️ **추론 모델은 샘플링 파라미터를 받지 않는다**
> `temperature`와 `top_p`는 다음 토큰의 확률 분포를 직접 건드리는 손잡이인데, 추론 계열 모델은 확률 분포를 조절하는 대신 **내부 사고 과정을 거쳐 답을 정하는 구조**라 이 파라미터를 지원하지 않는다. 넘기면 무시되거나 에러가 난다. 답변의 다양성을 조절하고 싶으면 파라미터가 아니라 프롬프트로 지시해야 한다. 디코딩 파라미터의 이론은 [텍스트 파운데이션 모델](https://it-study-2002.tistory.com/entry/%ED%85%8D%EC%8A%A4%ED%8A%B8%ED%8C%8C%EC%9A%B4%EB%8D%B0%EC%9D%B4%EC%85%98%EB%AA%A8%EB%8D%B8) 편의 디코딩 절에 정리해 뒀다.

<br>

---

## 5. 컨텍스트 윈도우 — 예산의 총액

토큰이 비용의 단위라면, **컨텍스트 윈도우(context window)** 는 그 비용을 쓸 수 있는 한도다.

> **컨텍스트 윈도우** — 모델이 한 번의 요청에서 읽고 고려할 수 있는 토큰의 최대 범위.

여기 두 가지 오해가 붙는다.

**오해 1 — "입력 길이 제한이다."** 아니다. 시스템 메시지, 이전 대화, 도구 실행 결과, 검색된 문서, 이번 질문, 그리고 **생성될 답변까지 전부 한 윈도우 안에 들어간다.** 입력으로 윈도우를 꽉 채우면 답변을 쓸 자리가 없다.

**오해 2 — "윈도우 안에 넣기만 하면 모델이 다 본다."** 넣을 수는 있지만 균등하게 활용되지는 않는다. 긴 컨텍스트의 중간에 있는 정보를 모델이 잘 놓친다는 것은 반복해서 관찰되는 현상이다. **한도는 물리적 상한이지 품질 보증이 아니다.**

가장 중요한 성질은 이것이다 — **윈도우 밖의 정보는 존재하지 않는 것과 같다.** 모델은 이번 요청에 실리지 않은 것을 참조할 방법이 없다. 어제 나눈 대화도, 저장소에 있는 문서도, 도구가 반환했지만 잘라낸 결과도 마찬가지다. LLM 앱 설계의 상당 부분이 결국 **"이번 요청의 윈도우에 무엇을 실을 것인가"** 하나로 수렴하는 이유다.

<br>

---

## 6. 예산 안에서 경쟁하는 네 부류

윈도우를 예산으로 보면, 그 예산을 나눠 쓰는 항목은 대체로 네 부류다. 그리고 이들은 **서로 경쟁한다** — 하나가 늘면 다른 하나가 줄어야 한다.

> **[그림]** 컨텍스트 윈도우를 하나의 가로 막대로 놓고 시스템 메시지·대화 기록·도구 및 검색 결과·답변 여유가 서로 자리를 다투는 구조를 보여주는 예산 다이어그램

| 항목 | 성질 | 통제 방법 |
|---|---|---|
| **시스템 메시지** | 고정 비용. 매 요청에 그대로 실린다 | 짧게 쓴다. 길어지면 매 턴 그 비용을 낸다 |
| **대화 기록** | 턴마다 단조 증가 | 잘라내기 / 요약 / 검색 — 8절의 3전략 |
| **도구·검색 결과** | 예측 불가. 한 번에 폭발할 수 있다 | 개수 제한(`k`), 길이 절단, 관련도 필터 |
| **답변 여유** | 남은 자리. 부족하면 답이 잘린다 | `max_output_tokens`로 미리 확보 |

가장 사고가 잦은 게 세 번째다. 도구 하나가 JSON을 만 줄쯤 뱉거나 검색 `k`를 넉넉하게 잡아 두면, 다른 세 항목이 아무리 알뜰해도 한 번에 예산을 넘긴다.

```python
# 검색 결과는 개수와 길이를 둘 다 막는다
retriever = vectorstore.as_retriever(search_kwargs={'k': 3})

def format_docs(docs, limit=1200):
    joined = '\n\n'.join(d.page_content for d in docs)
    return joined[:limit]          # 길이 상한도 함께 건다

# 도구 결과도 마찬가지 — 원본을 그대로 메시지에 붙이지 않는다
def summarize_tool_result(raw: str, limit: int = 800) -> str:
    return raw if len(raw) <= limit else raw[:limit] + '\n...(생략)'
```

> ✅ **예산은 미리 배분하고 시작한다**
> 윈도우가 128k라고 128k를 다 쓰겠다는 뜻이 아니다. 실무에서는 "시스템 500 / 대화 기록 4,000 / 검색 결과 2,000 / 답변 1,000"처럼 **항목별 상한을 먼저 정하고 그 안에서 운영**한다. 상한이 없으면 초과는 어느 날 갑자기, 하필 가장 긴 대화에서 터진다.

<br>

---

## 7. 대화가 길어질 때 벌어지는 일 — 누적 비용의 함정

이제 서두의 세 번째 놀람으로 돌아온다. 대화가 길어지면 왜 비용이 튀는가.

LLM API가 무상태이므로 매 턴 대화 전체를 다시 보낸다. 한 턴에 오가는 양이 일정하다고 해도, **N번째 턴의 입력은 1번째부터 N−1번째까지의 누적**이다. 그래서 총 입력 토큰은 턴 수에 비례하지 않고 **턴 수의 제곱에 비례해** 늘어난다.

```python
system, per_turn, turns = 200, 500, 20

total = 0
for n in range(1, turns + 1):
    total += system + per_turn * n     # n번째 요청의 입력 토큰
print(total)      # 109,000
```

한 턴에 500토큰씩 20턴이면 오간 대화 자체는 1만 토큰이다. 그런데 **누적 입력 토큰은 10만을 넘는다.** 열 배 넘게 차이가 난다.

> **[그림]** 턴이 늘어날수록 요청당 입력 토큰이 선형으로, 누적 입력 토큰이 제곱으로 증가하는 모습을 두 곡선으로 비교한 그래프

| 턴 | 그 요청의 입력 토큰 | 여기까지 누적 |
|---|---|---|
| 1 | 700 | 700 |
| 5 | 2,700 | 8,500 |
| 10 | 5,200 | 29,500 |
| 20 | 10,200 | 109,000 |

체감되는 증상 세 가지가 전부 이 표에서 나온다.

- **비용이 뒤로 갈수록 가파르게 오른다** — 20번째 턴 한 번이 1번째 턴의 15배다.
- **응답이 점점 느려진다** — 입력이 길수록 첫 토큰까지의 시간이 늘어난다.
- **어느 순간 에러가 난다** — 대화 기록이 윈도우를 밀어낸다.

그러니까 "대화를 기억하게 만들기"는 기능 구현이 아니라 **예산 관리 문제**다. 다음 절의 세 전략은 그 문제에 대한 세 가지 답이다.

<br>

---

## 8. 대화 기억 3전략 — Buffer, Summary, VectorStoreRetriever

앞서 [LangChain 체인과 RAG](https://it-study-2002.tistory.com/entry/LangChain-%EC%B2%B4%EC%9D%B8%EA%B3%BC-RAG) 편에서 기억을 **어디에 둘 것인가**로 갈랐다면(체인 외부 세션 저장소 vs LangGraph State), 여기서는 다른 축으로 가른다 — **무엇을 남길 것인가.** 축이 다르므로 둘은 조합된다. 어디에 두든, 남길 것을 고르는 문제는 그대로 남는다.

> **[그림]** 대화 기록을 그대로 쌓는 Buffer, 오래된 대화를 요약으로 접는 Summary, 임베딩해 두고 관련된 것만 꺼내는 VectorStoreRetriever 세 전략을 토큰 사용량 곡선과 함께 비교한 카드

### 전략 1 — Buffer Memory: 그대로 쌓는다

대화를 가공 없이 전부 보관하고 전부 보낸다.

```python
from langchain_core.chat_history import InMemoryChatMessageHistory

store = {}

def get_session_history(session_id: str):
    if session_id not in store:
        store[session_id] = InMemoryChatMessageHistory()
    return store[session_id]
```

- **장점** — 정보 손실이 0이다. 구현이 가장 단순하고, 짧은 대화에서는 정확도도 가장 높다.
- **단점** — 7절의 제곱 증가를 그대로 맞는다.

실무에서는 여기에 **윈도우 잘라내기**를 얹어 쓰는 경우가 많다. 최근 K턴만 남기는 방식이다.

```python
def trim(messages, keep=10):
    return messages[-keep:]      # 최근 10개만 남긴다
```

단순하지만 **오래된 것부터 통째로 잃는다.** 사용자가 첫 턴에 말한 이름이나 제약 조건이 스무 번째 턴에서 사라지는 게 이 방식의 전형적인 사고다.

### 전략 2 — Summary Memory: 접어서 쌓는다

오래된 대화를 LLM으로 요약해 한 덩어리로 접고, 최근 몇 턴만 원문으로 남긴다.

```text
[요약] 사용자는 홍길동이고 반려묘 보험을 알아보는 중이다.
       3세 코리안숏헤어, 월 3만원대 예산을 언급했다.
[최근] user: 그럼 슬개골은 보장돼?
       assistant: ...
```

- **장점** — 토큰 사용량이 턴 수와 무관하게 거의 일정해진다. 오래된 맥락도 압축된 형태로 살아남는다.
- **단점** — **요약 자체가 LLM 호출이라 비용과 지연이 붙는다.** 그리고 요약은 손실 압축이다. 접히는 과정에서 나중에 필요해질 세부 사항이 사라질 수 있고, 요약이 요약을 다시 요약하면 왜곡이 누적된다.

### 전략 3 — VectorStoreRetriever Memory: 저장해 두고 필요한 것만 꺼낸다

대화 기록을 임베딩해 벡터 저장소에 넣고, 매 턴 **현재 질문과 관련도가 높은 과거 조각만** 검색해 넣는다. RAG를 대화 기록에 적용하는 셈이다.

- **장점** — 기록이 아무리 길어져도 매 요청에 실리는 양은 일정하다. 백 턴 전의 발언도 관련만 있으면 정확히 돌아온다.
- **단점** — 인프라가 하나 늘고, **검색이 빗나가면 바로 앞 턴의 맥락도 놓친다.** "방금 말한 그거"처럼 시간적으로 인접한 참조가 의미 유사도로는 안 잡히는 경우가 있어, 보통 최근 N턴은 무조건 넣고 그 위에 검색을 얹는다.

### 무엇으로 고르는가

세 전략은 우열이 아니라 **정확도 · 비용 · 지연시간의 삼각형에서 어느 꼭짓점을 포기하는가**의 문제다.

| | Buffer | Summary | VectorStoreRetriever |
|---|---|---|---|
| 토큰 증가 | 턴 수의 제곱 | 거의 일정 | 거의 일정 |
| 정보 손실 | 없음 (잘라내면 발생) | 압축 손실 | 검색 실패 시 누락 |
| 추가 비용 | 없음 | 요약용 LLM 호출 | 임베딩 + 벡터 저장소 |
| 추가 지연 | 없음 | 요약 대기 | 검색 대기 |
| 어울리는 자리 | 짧은 Q&A, 10턴 이내 | 긴 상담형 대화 | 장기 사용자 기억, 누적 지식 |

> ✅ **자연어 기억과 구조화된 상태를 구분한다**
> 사용자 이름, 선택한 상품, 예산 같은 **꼭 유지돼야 하는 값**은 대화 요약에 맡기지 말고 별도 필드로 빼서 저장하는 편이 안정적이다. 요약은 손실 압축이고 검색은 확률적이지만, 딕셔너리 값은 사라지지 않는다. 실무에서 잘 도는 구성은 대체로 **"구조화된 상태 + 최근 N턴 원문 + (요약 또는 검색)"** 의 조합이다.

> 💡 **에이전트에서는 도구 결과가 더 큰 문제다**
> 도구를 여러 번 호출하는 에이전트는 관찰 결과(observation)를 메시지에 계속 누적하는데, 이게 대화 기록보다 빨리 부푼다. 원칙은 하나다 — **다음 판단에 필요한 것만 남긴다.** 이미 결론에 반영된 중간 관찰은 요약하거나 버려도 된다.

<br>

---

## 9. 입력 품질이 출력 품질을 정한다

지금까지가 "예산을 어떻게 아낄 것인가"였다면, 마지막은 반대쪽 질문이다 — **같은 예산으로 어떻게 더 좋은 답을 받을 것인가.**

출력 품질은 모델 성능만으로 정해지지 않는다. 같은 모델에 같은 질문을 던져도 컨텍스트에 무엇이 들어갔느냐에 따라 답이 갈린다.

> **[그림]** 명확한 목표·충분한 배경·충돌하는 정보·불필요한 정보가 각각 답변 품질에 미치는 영향을 사분면으로 정리한 매트릭스

| 컨텍스트의 상태 | 답변에 나타나는 결과 |
|---|---|
| 목표와 출력 형식이 명확하다 | 답변 방향이 안정된다. 재시도 횟수가 준다 |
| 필요한 배경 정보가 들어 있다 | 추측과 환각이 준다 |
| **서로 충돌하는 정보가 섞여 있다** | 모델이 어느 쪽을 따를지 흔들린다 |
| **무관한 정보가 많다** | 핵심이 묻힌다. 게다가 그 자리는 유료다 |

아래 두 줄이 중요하다. **컨텍스트는 많이 넣을수록 좋은 게 아니다.** 무관한 문서를 검색해 넣으면 비용만 오르는 게 아니라 답이 나빠진다. 검색을 붙였다는 사실 자체는 정확성을 보장하지 않는다.

> ⚠️ **`k`를 키워서 검색 품질을 메우려는 시도가 가장 흔한 함정이다**
> "관련 문서를 못 찾는 것 같으니 `k`를 3에서 10으로 올리자"는 대응은 대개 역효과다. 정답 청크가 들어올 확률이 조금 오르는 대신 노이즈 7개가 확실하게 들어오고, 비용도 3배가 된다. 검색이 빗나가면 손댈 곳은 `k`가 아니라 **청킹 전략, 임베딩 모델, 질의 재작성** 쪽이다. 이 이야기는 다음 글에서 이어간다.

<br>

---

## 10. 새는 곳 막기 — 키와 실행 환경

마지막으로 짧게, 비용이 예상 밖으로 튀는 또 하나의 경로를 짚는다. **키 관리**다.

API 키는 인증 수단인 동시에 **요금 청구의 기준**이다. 유출되면 제3자가 내 계정으로 과금을 발생시킨다. 그래서 코드에 직접 적지 않고 환경 변수로 분리하는 것이 표준이다.

```python
# .env  (반드시 .gitignore에 등록)
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-5-nano

import os
from dotenv import load_dotenv

load_dotenv()
api_key = os.getenv('OPENAI_API_KEY')
```

```text
.env
.env.*
!.env.sample        # 키 없는 예시 파일만 저장소에 올린다
```

몇 가지 실무 규칙이 붙는다.

| 상황 | 대응 |
|---|---|
| 키가 저장소에 올라갔다 | **즉시 폐기하고 재발급한다.** 커밋을 되돌려도 이미 노출된 것으로 간주 |
| 용도가 여러 개다 | 프로젝트·환경별로 키를 분리한다. 사고 시 폐기 범위가 좁아진다 |
| OpenAI 호환 프록시를 쓴다 | `base_url`만 바꾸면 SDK와 LangChain이 그대로 동작한다 |
| 팀과 환경을 맞춰야 한다 | 가상환경 + `requirements.txt` / `pyproject.toml`로 버전 고정 |

```python
from openai import OpenAI

client = OpenAI(
    api_key=os.getenv('OPENAI_API_KEY'),
    base_url='https://<프록시-주소>/v1/',   # 표준 엔드포인트면 이 줄 자체가 불필요
)
```

> ⚠️ **커밋 되돌리기로는 키를 되돌릴 수 없다**
> 공개 저장소에 올라간 키는 자동 스캐너가 몇 분 안에 수집한다. `git revert`나 히스토리 정리는 이미 수집된 키에 아무 영향이 없다. 유일하게 유효한 조치는 **폐기 후 재발급**이다. 순서도 중요하다 — 먼저 폐기하고, 그다음 히스토리를 정리한다.

<br>

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **토큰** | 단어도 글자도 아니다. BPE로 **자주 붙어 나오는 조각**이 하나의 토큰. 앞 공백도 토큰의 일부 |
| **한국어 밀도** | 글자당 토큰이 영어의 3~4배. 같은 뜻 기준으로는 1.5~1.8배. **한도 검사는 글자 수가 아니라 토크나이저로** |
| **토크나이저 세대** | 토큰 수는 모델이 아니라 인코딩에 딸린 값. 어휘집이 커지면 한국어가 크게 줄어든다. 비용 비교는 `단가 × 실제 토큰 수` |
| **과금 2축** | 입력(시스템+대화 전체+도구 결과+질문)과 출력. **추론 토큰은 화면에 안 보여도 과금된다** |
| **컨텍스트 윈도우** | 입력 한도가 아니라 **입력과 출력의 총액**. 윈도우 밖 정보는 존재하지 않는 것과 같다 |
| **예산 경쟁 4항목** | 시스템 메시지 / 대화 기록 / 도구·검색 결과 / 답변 여유. **항목별 상한을 미리 정한다** |
| **누적의 함정** | API는 무상태라 매 턴 전체 재전송. 총 입력 토큰은 **턴 수의 제곱**으로 는다 (500토큰×20턴 → 누적 10.9만) |
| **Buffer** | 손실 0, 구현 단순. 대신 제곱 증가를 그대로 맞는다. 잘라내기를 얹으면 오래된 것부터 잃는다 |
| **Summary** | 토큰이 거의 일정. 대신 요약용 LLM 호출 비용 + 손실 압축 + 왜곡 누적 |
| **VectorStoreRetriever** | 기록이 길어도 실리는 양은 일정. 대신 검색이 빗나가면 바로 앞 맥락도 놓친다 — 최근 N턴은 무조건 넣는다 |
| **꼭 지킬 값** | 이름·선택 상품·예산 같은 값은 요약이나 검색에 맡기지 말고 **구조화된 상태로 별도 저장** |
| **입력 품질** | 컨텍스트는 많을수록 좋은 게 아니다. **무관하거나 충돌하는 정보는 비용도 올리고 답도 나쁘게 만든다** |
| **키 관리** | `.env` + `.gitignore`. 유출 시 **폐기 후 재발급**이 유일한 조치. 커밋 되돌리기는 의미 없다 |

<br>

## 다음 글 예고

9절 마지막에서 미뤄 둔 질문이 있다 — 검색이 빗나갈 때 `k`가 아니라 어디를 손대야 하는가. 답은 **검색 파이프라인의 설계 결정** 쪽에 있다. 문서를 어디서 자를 것인가, 어떤 임베딩 모델을 쓸 것인가, 검색 앞뒤에 무엇을 덧댈 것인가.

다음 글 [RAG 설계 결정 — Naive에서 Modular까지, 청킹 3전략, 그리고 임베딩 고르기](0824_RAG설계결정.md)에서 그 결정들을 하나씩 짚는다.

<br>

## 🔗 참고 자료

- [OpenAI Tokenizer — 문장을 넣으면 토큰 분할을 보여준다](https://platform.openai.com/tokenizer)
- [tiktoken (GitHub)](https://github.com/openai/tiktoken)
- [OpenAI API 문서 — Responses API, Usage](https://platform.openai.com/docs)
- [LangChain 파이썬 레퍼런스 — Memory](https://python.langchain.com)
- [Lost in the Middle: How Language Models Use Long Contexts (Liu et al., 2023)](https://arxiv.org/abs/2307.03172)
