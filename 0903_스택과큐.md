# 스택과 큐, 그리고 연결 리스트 — 삽입·삭제 위치를 제한하면 무엇이 생기는가

> 출처: 알고리즘 과정 4일차 강의자료 (97p) · 정리일: 2026-09-06
> 이어지는 노트: [0902 재귀와 완전 탐색](0902_재귀와완전탐색.md) · [0904 트리와 그래프](0904_트리와그래프.md)

앞 노트에서 **재귀는 스택을 쓰는 반복문**이라고 정리했다. 그때 "호출 스택"이라는 이름만 쓰고 넘어갔는데, 이번 자료가 그 스택 자체를 정면으로 다룬다. 스택 → 큐 → 원형 큐 → 연결 리스트 순으로 가고, 마지막 연결 리스트는 다음 자료의 트리 표현으로 그대로 이어진다.

이 자료를 관통하는 한 문장은 이것이다. **삽입과 삭제를 아무 데서나 하지 못하게 막으면, 그 대가로 특정 문제가 공짜로 풀린다.**

---

## 1. 스택 — 삽입과 삭제를 한쪽 끝으로 몰기

### 1.1 정의와 성질

- 물건을 쌓아 올리듯 자료를 쌓아 올린 형태의 자료구조
- 스택에 저장된 자료는 **선형 구조**를 갖는다
  - 선형 구조: 데이터 요소들 사이에 순서가 존재
  - 비선형 구조: 데이터 요소가 순차적으로 나열되지 않음 (다음 자료의 트리·그래프가 여기 해당)
- **후입선출(LIFO, Last-In-First-Out)** — 마지막에 넣은 것을 가장 먼저 꺼낸다
  - 1, 2, 3 순으로 넣으면 3, 2, 1 순으로 꺼낸다

### 1.2 주요 연산 4가지

| 연산 | 하는 일 |
|---|---|
| `push` | 저장소에 자료를 저장한다 (삽입) |
| `pop` | 저장소에서 자료를 꺼낸다 (삭제). 넣은 순서의 역순으로 나온다 |
| `is_empty` | 스택이 공백인지 확인 |
| `peek` | 스택의 `top` 에 있는 자료를 **꺼내지 않고 반환** |

`top` 은 "저장된 원소 중 마지막 원소"를 가리킨다. 이 하나의 변수가 스택의 전부라고 해도 된다 — push는 `top` 을 올리고 쓰고, pop은 읽고 `top` 을 내린다.

### 1.3 구현

```python
class Stack:
    def __init__(self, capacity=10):
        self.capacity = capacity          # 스택의 최대 용량
        self.items = [None] * capacity    # 스택을 저장할 리스트
        self.top = -1                     # top 위치 (초기값 -1 = 공백)

    def push(self, item):
        if self.is_full():
            raise IndexError("Stack is full")
        self.top += 1                     # top 을 먼저 올리고
        self.items[self.top] = item       # 그 자리에 쓴다

    def pop(self):
        if self.is_empty():
            raise IndexError("Stack is empty")
        item = self.items[self.top]       # 먼저 읽고
        self.items[self.top] = None
        self.top -= 1                     # top 을 내린다
        return item

    def is_empty(self):
        return self.top == -1

    def is_full(self):
        return self.top == self.capacity - 1

    def peek(self):
        if self.is_empty():
            raise IndexError("Stack is empty")
        return self.items[self.top]

    def get_size(self):
        return self.top + 1
```

**`top = -1` 이 초기값인 이유.** 0으로 두면 "빈 스택"과 "원소 하나가 들어 있는 스택"을 구분할 수 없다. `-1` 은 리스트에 존재하지 않는 인덱스라서 "아직 아무것도 없다"는 뜻으로 안전하게 쓸 수 있다.

**push 와 pop 의 순서가 대칭이다.** push는 `top += 1` 다음에 쓰고, pop은 읽은 다음에 `top -= 1` 한다. 이 순서를 뒤집으면 첫 원소를 못 넣거나 마지막 원소를 두 번 꺼낸다.

> 실행해서 확인했다. `capacity=3` 으로 A, B, C 를 push 하면 `items = ['A','B','C']`, `top = 2`, `is_full()` 이 True. pop 하면 `'C'` 가 나오고 `items = ['A','B',None]`, `get_size() == 2`. 네 번째 push 는 `IndexError: Stack is full`. **원본 코드에 문제 없다.**

파이썬에서는 실전에서 그냥 `list` 를 쓴다. `append` 가 push, `pop()` 이 pop이고 둘 다 상수 시간이다. 위 클래스는 스택이 무엇인지 보기 위한 것이지 실제로 쓸 물건은 아니다.

---

## 2. 스택 응용 1 — 괄호 검사

### 2.1 조건

괄호는 대괄호 `[]`, 중괄호 `{}`, 소괄호 `()` 세 종류다. 올바른 괄호식의 조건은 셋이다.

1. 왼쪽 괄호의 개수와 오른쪽 괄호의 개수가 같아야 한다
2. 같은 괄호에서 왼쪽 괄호는 오른쪽 괄호보다 먼저 나와야 한다
3. 괄호는 서로 짝이 맞아야 한다

잘못된 예: `(a(b)` / `a(b)c)` / `a{b(c[d]e}f)`

### 2.2 알고리즘

문자열을 왼쪽부터 차례로 조사하면서,

- **왼쪽 괄호**를 만나면 스택에 push
- **오른쪽 괄호**를 만나면 스택에서 pop 하고 짝이 맞는지 검사
  - 스택이 비어 있으면 → 조건 1 또는 2 위배
  - 꺼낸 것과 짝이 안 맞으면 → 조건 3 위배
- 문자열이 끝났는데 스택에 남아 있으면 → 조건 1 위배

```python
def check_paren(s):
    pair = {')': '(', ']': '[', '}': '{'}
    st = []
    for ch in s:
        if ch in '([{':
            st.append(ch)
        elif ch in pair:
            if not st:           # 조건 1·2 위배
                return False
            if st.pop() != pair[ch]:   # 조건 3 위배
                return False
    return not st                # 남아 있으면 조건 1 위배
```

**왜 스택인가.** 괄호는 **가장 최근에 열린 것이 가장 먼저 닫혀야** 한다. 이게 정확히 LIFO다. 만약 이 규칙이 없다면(예: `([)]` 를 허용한다면) 스택으로 못 푼다.

자료의 예제 `if( ( i==0 ) && ( j==0 )` 는 `(` 가 3개, `)` 가 2개다. 끝까지 훑고 나면 스택에 `(` 하나가 남는다 → 오류.

> 실행 결과. `(a(b)` False / `a(b)c)` False / `a{b(c[d]e}f)` False / `if( ( i==0 ) && ( j==0 )` False / 괄호를 하나 닫아 준 `if( ( i==0 ) && ( j==0 ) )` True / `a{b(c[d]e)f}` True. 자료의 예시 판정과 전부 일치한다.

---

## 3. 스택 응용 2 — 함수 호출과 시스템 스택

프로그램의 함수 호출·복귀 순서를 관리하는 게 스택이다. **가장 마지막에 호출된 함수가 가장 먼저 끝나고 돌아오므로** 후입선출이다.

```text
main()  ─① F_1() 호출 ─② ─▶ F_1()  ─③ F_2() 호출 ─④ ─▶ F_2()
        ◀─────────── ⑧ 복귀 ───────         ◀──── ⑥ 복귀 ────  ⑤ end F_2()
   ⑨ end main()                    ⑦ end F_1()
```

함수 호출이 발생하면 **스택 프레임(stack frame)** 이 시스템 스택에 push된다. 프레임에 담기는 것:

- 지역 변수
- 매개변수
- 수행 후 **복귀할 주소**

```text
    ┌────────────────────────┐
    │ stack_Frame(F_2)       │ ← top (현재 실행 중인 함수)
    │ F_2() 실행 관련 정보    │
    ├────────────────────────┤
    │ stack_Frame(F_1)       │
    ├────────────────────────┤
    │ stack_Frame(main)      │
    └────────────────────────┘
```

함수가 끝나면 top 프레임을 pop 하면서 저장된 복귀 주소로 돌아간다. 전체 프로그램이 끝나면 시스템 스택은 공백이 된다.

**이게 0902 노트에서 본 `RecursionError` 의 정체다.** 파이썬의 재귀 한계 1000은 "이 스택에 프레임을 1000개까지만 쌓겠다"는 뜻이다. 재귀 함수가 자기를 부를 때마다 프레임이 하나씩 쌓이고, 리턴할 때 하나씩 걷힌다. 곱셈이 "올라오면서" 일어나는 것도 프레임이 걷히는 순서가 LIFO이기 때문이다.

---

## 4. 스택 응용 3 — 계산기(후위 표기법)

### 4.1 왜 표기법을 바꾸는가

우리가 쓰는 `A + B` 는 **중위 표기법**이다. 연산자가 피연산자 사이에 있다. 사람에게는 편하지만 기계가 계산하려면 괄호와 우선순위를 먼저 해석해야 한다.

**후위 표기법** `AB+` 는 연산자를 피연산자 뒤에 둔다. 이 표기에는 **괄호가 아예 없고**, 왼쪽부터 한 번 훑으면서 계산할 수 있다.

문자열 수식 계산의 일반적 방법은 두 단계다.

- **Step 1.** 중위 → 후위 변환 (스택 이용)
- **Step 2.** 후위 표기식을 스택으로 계산

### 4.2 Step 1 — 손으로 하는 방법

자료가 제시하는 손 절차는 이렇다.

1. 수식의 각 연산자에 대해 **우선순위에 따라 괄호를 씌운다**
2. 각 연산자를 **그에 대응하는 오른쪽 괄호의 뒤로** 옮긴다
3. 괄호를 제거한다

```text
예) A*B-C/D
  1단계 : ( (A * B) - (C / D) )
  2단계 : ( (A B)*  (C D)/  )-
  3단계 : AB*CD/-
```

`(6+((5*(2-8))/2))` 는 같은 절차로 `6 5 2 8 - * 2 / +` 가 된다.

### 4.3 Step 1 — 스택으로 하는 방법

자료는 손 절차만 보여 주고 코드는 주지 않는다. 실제로 쓰는 건 스택 알고리즘이다.

```python
PREC = {'+': 1, '-': 1, '*': 2, '/': 2}

def to_postfix(expr):
    out, st = [], []
    i = 0
    while i < len(expr):
        ch = expr[i]
        if ch == ' ':
            i += 1; continue
        if ch.isdigit():                       # 여러 자리 숫자 처리
            j = i
            while j < len(expr) and expr[j].isdigit():
                j += 1
            out.append(expr[i:j]); i = j; continue
        if ch == '(':
            st.append(ch)
        elif ch == ')':
            while st and st[-1] != '(':        # 여는 괄호까지 전부 쏟아낸다
                out.append(st.pop())
            st.pop()                           # 여는 괄호 버리기
        else:                                  # 연산자
            while st and st[-1] != '(' and PREC[st[-1]] >= PREC[ch]:
                out.append(st.pop())           # 나보다 세거나 같은 건 먼저 내보낸다
            st.append(ch)
        i += 1
    while st:
        out.append(st.pop())
    return out
```

핵심은 `PREC[st[-1]] >= PREC[ch]` 한 줄이다. **"스택 꼭대기 연산자가 나보다 우선순위가 높거나 같으면 그것부터 내보낸다."** 같을 때도 내보내는 이유는 왼쪽 결합(`a-b-c` 는 `(a-b)-c`)을 맞추기 위해서다.

### 4.4 Step 2 — 후위 표기식 계산

- 피연산자를 만나면 스택에 push
- 연산자를 만나면 필요한 만큼 pop 해서 계산하고 결과를 다시 push
- 수식이 끝나면 마지막으로 pop 해서 출력

```python
import operator
OPS = {'+': operator.add, '-': operator.sub,
       '*': operator.mul, '/': operator.truediv}

def eval_postfix(tokens):
    st = []
    for t in tokens:
        if t in OPS:
            b = st.pop(); a = st.pop()   # 순서 주의! 나중에 꺼낸 게 왼쪽 피연산자
            st.append(OPS[t](a, b))
        else:
            st.append(int(t))
    return st.pop()
```

**`b = st.pop(); a = st.pop()` 의 순서가 함정이다.** 스택은 LIFO라 나중에 넣은 것이 먼저 나온다. `5 - 3` 을 후위로 쓰면 `5 3 -` 이고, 스택에는 아래부터 `5`, `3` 이 쌓인다. 먼저 pop 되는 `3` 이 오른쪽 피연산자다. `-` 와 `/` 처럼 교환법칙이 성립하지 않는 연산에서 이걸 뒤집으면 부호와 값이 통째로 틀린다.

자료의 계산 과정을 그대로 따라가면:

| 토큰 | 스택 (아래→위) | 하는 일 |
|---|---|---|
| `6` | `6` | push |
| `5` | `6 5` | push |
| `2` | `6 5 2` | push |
| `8` | `6 5 2 8` | push |
| `-` | `6 5 -6` | 8, 2 pop → `2 - 8 = -6` push |
| `*` | `6 -30` | -6, 5 pop → `5 * -6 = -30` push |
| `2` | `6 -30 2` | push |
| `/` | `6 -15` | 2, -30 pop → `-30 / 2 = -15` push |
| `+` | `-9` | -15, 6 pop → `6 + -15 = -9` push |
| 끝 | | pop → **-9** |

> 검증했다. 내가 만든 `to_postfix("(6+((5*(2-8))/2))")` 의 결과가 `6 5 2 8 - * 2 / +` 로 **자료의 답과 문자 단위로 일치**했고, `eval_postfix` 결과 `-9.0` 이 파이썬 `eval()` 과 같았다.
>
> 더 나아가 **무작위 수식 200,000건**을 만들어(괄호 깊이 3, 연산자 4종, 피연산자 1~9) `eval_postfix(to_postfix(e))` 와 파이썬 `eval(e)` 를 대조했다. **불일치 0건.** 우선순위 처리와 pop 순서가 모두 맞다는 뜻이다.

---

## 5. 큐 — 삽입과 삭제를 반대쪽 끝으로 갈라놓기

### 5.1 정의

- 스택처럼 삽입·삭제 위치가 제한적인데, **방향이 다르다**
  - 큐의 **뒤(rear)** 에서는 삽입만, **앞(front)** 에서는 삭제만
- **선입선출(FIFO, First-In-First-Out)** — 가장 먼저 넣은 것이 가장 먼저 나온다
- 실생활 비유: 서비스 대기 행렬(줄 서기)

| 연산 | 하는 일 |
|---|---|
| `enqueue` | 큐의 뒤쪽에 원소를 삽입 |
| `dequeue` | 큐의 앞쪽에서 원소를 삭제하고 반환 |
| `is_empty` | 공백 상태 확인 |
| `peek` | 앞쪽 원소를 삭제 없이 반환 |

`front` 는 "마지막으로 삭제된 원소"를, `rear` 는 "저장된 원소 중 마지막 원소"를 가리킨다. **`front` 가 가리키는 자리는 이미 나간 자리**라는 게 포인트다 — 그래서 `peek` 이 `items[front + 1]` 을 본다.

### 5.2 구현 (선형 큐)

```python
class Queue:
    def __init__(self, capacity=10):
        self.capacity = capacity
        self.items = [None] * capacity
        self.front = -1        # 맨 앞 요소 "바로 앞"의 인덱스
        self.rear = -1         # 맨 뒤 요소의 인덱스

    def enqueue(self, item):
        if self.is_full():
            raise IndexError("Queue is full")
        self.rear += 1
        self.items[self.rear] = item

    def dequeue(self):
        if self.is_empty():
            raise IndexError("Queue is empty")
        self.front += 1
        item = self.items[self.front]
        self.items[self.front] = None
        return item

    def is_empty(self):
        return self.front == self.rear        # 둘이 만나면 공백

    def is_full(self):
        return self.rear == self.capacity - 1  # ← 문제의 한 줄

    def peek(self):
        if self.is_empty():
            raise IndexError("Queue is empty")
        return self.items[self.front + 1]

    def get_size(self):
        return self.rear - self.front
```

**`is_empty` 가 `front == rear` 인 이유.** 둘 다 -1 에서 시작한다. enqueue 하면 rear만, dequeue 하면 front만 올라간다. 그러니 두 값의 차이가 정확히 "들어 있는 개수"고, 차이가 0이면 비어 있다. `get_size` 가 `rear - front` 인 것도 같은 이유다.

---

## 6. 큐 응용

### 6.1 버퍼

- 데이터를 한 곳에서 다른 곳으로 전송하는 동안 **일시적으로 보관하는 메모리 영역**
- 입출력과 네트워크에서 주로 쓰인다
- **순서대로 들어오고 순서대로 나가야 하므로** FIFO — 즉 큐가 맞는다

키보드 입력 버퍼가 대표적이다. 타자를 치면 문자가 버퍼에 차례로 쌓이고, Enter 키가 들어오면 그때 프로그램 실행 영역으로 넘어간다. 이때 순서가 뒤집히면 `APS` 가 `SPA` 가 된다.

### 6.2 마이쮸 나눠주기

자료가 낸 문제다.

```text
1번이 줄을 선다 → 1번이 1개 받는다 → 1번이 다시 줄을 선다 → 2번이 새로 들어와 줄을 선다
→ 1번이 2개 받는다 → 1번 다시 줄, 3번 새로 줄
→ 2번이 1개 받는다 → 2번 다시 줄, 4번 새로 줄
→ 1번이 3개 받는다 → ...
```

규칙을 정리하면: 큐에서 한 명 꺼내 **자기 차례 수만큼** 받고, 받은 사람은 **다음 개수 +1 로 다시 뒤에 서고**, **새 번호 한 명이 뒤에 더 선다.**

> **20개의 마이쮸가 있을 때 마지막 것을 누가 가져갈까?**

```python
from collections import deque

def myzzu(total):
    q = deque([[1, 1]])      # [번호, 받을 개수]
    nxt, left, last = 2, total, None
    while left > 0:
        num, cnt = q.popleft()
        take = min(cnt, left)
        left -= take
        last = num
        if left > 0:
            q.append([num, cnt + 1])   # 받은 사람은 +1 개로 다시 뒤에
            q.append([nxt, 1])         # 새 사람 한 명 추가
            nxt += 1
    return last
```

> 돌려서 확인했다. **답은 2번.** 진행 과정도 자료의 그림과 정확히 맞는다 — 남은 개수가 20 → 19(1번) → 17(1번 2개) → 16(2번) → 13(1번 3개) → 12(3번) 으로 줄어드는 것까지 같다.
>
> 개수를 바꿔 보면 답이 규칙 없이 튄다: 1개→1번, 5개→1번, 10개→2번, 15개→1번, **20개→2번**, 30개→1번, 50개→14번, 100개→7번. **눈으로 예측할 수 없다.** 자료가 "출력해 보자"고 하는 이유다.

---

## 7. 원형 큐 — 선형 큐의 치명적 결함

### 7.1 잘못된 포화 상태 인식

선형 큐를 쓰면서 삽입·삭제를 계속하면, **리스트 앞부분에 쓸 수 있는 공간이 남아 있는데도** `rear == n-1` 이라는 이유로 포화로 판정해 더 이상 삽입하지 않는다.

```text
Q  [0]  [1]  [2]  [3]
   [  ] [  ] [  ] [██]
              ↑    ↑
            front  rear      ← 3칸이 비어 있는데 "Full"
```

> **직접 재현했다.** `capacity=5` 인 원본 `Queue` 에 `enqueue` 와 `dequeue` 를 번갈아 반복하면 **정확히 5회 만에** `IndexError: Queue is full` 이 난다. 그 순간의 상태는
>
> ```text
> items = [None, None, None, None, None]   front = 4   rear = 4
> ```
>
> **리스트가 통째로 비어 있는데 "가득 찼다"고 한다.** 이게 자료가 말하는 "잘못된 포화 상태 인식"이고, 이 큐는 평생 `capacity` 번만 쓸 수 있는 일회용이라는 뜻이다.

### 7.2 해결 방법 1 — 원소를 앞으로 밀기 (기각)

매 연산마다 저장된 원소를 배열 앞부분으로 전부 이동시키는 방법. **원소 이동에 시간이 많이 들어 큐의 효율성이 급격히 떨어진다.** dequeue 한 번이 O(N)이 되므로 큐를 쓰는 의미가 없어진다.

### 7.3 해결 방법 2 — 논리적으로 원형이라고 치기

1차원 배열을 그대로 쓰되, **배열의 처음과 끝이 연결되어 원형을 이룬다고 가정**한다. `[n-1]` 다음이 `[0]` 이다. 구현은 **나머지 연산자 `%`** 한 글자로 끝난다.

```text
        [3]   [4]
    [2]           …
    [1]         [n-2]
        [0]   [n-1]      ← [n-1] 다음이 [0]
```

### 7.4 front 자리는 비워 둔다

원형으로 만들면 새 문제가 생긴다. **`front == rear` 가 공백인지 포화인지 구분되지 않는다.** 한 바퀴 돌아 rear가 front를 따라잡으면 둘이 같아지는데, 이건 가득 찬 상태다.

해결책은 **front가 있는 자리를 절대 쓰지 않는 것**이다. 칸 하나를 희생하는 대신,

- `front == rear` → 공백
- `(rear + 1) % capacity == front` → 포화

로 두 상태가 갈린다. 그래서 생성자가 `capacity + 1` 로 배열을 잡는다 — **요청한 만큼 담으려면 한 칸이 더 필요하다.**

```python
class CircularQueue:
    def __init__(self, capacity=10):
        self.capacity = capacity + 1          # 한 칸은 항상 비워 둔다
        self.items = [None] * self.capacity
        self.front = 0
        self.rear = 0

    def enqueue(self, item):
        if self.is_full():
            raise IndexError("Queue is full")
        self.rear = (self.rear + 1) % self.capacity
        self.items[self.rear] = item

    def dequeue(self):
        if self.is_empty():
            raise IndexError("Queue is empty")
        self.front = (self.front + 1) % self.capacity
        item = self.items[self.front]
        self.items[self.front] = None
        return item

    def is_empty(self):
        return self.front == self.rear

    def is_full(self):
        return (self.rear + 1) % self.capacity == self.front

    def peek(self):
        if self.is_empty():
            raise IndexError("Queue is empty")
        return self.items[(self.front + 1) % self.capacity]

    def get_size(self):
        return (self.rear - self.front + self.capacity) % self.capacity
```

**`get_size` 에 `+ self.capacity` 가 붙은 이유.** rear가 한 바퀴 돌아 front보다 앞 인덱스에 있으면 `rear - front` 가 음수가 된다. capacity를 더한 뒤 `%` 를 씌우면 항상 0 이상이 나온다.

> 같은 실험을 원형 큐로 했다. `capacity=5` 에 **enqueue+dequeue 를 1,000,000회** 반복해도 예외가 나지 않았다. 5개를 채우면 `is_full()` 이 True, `get_size()` 가 5, `items = [None, 0, 1, 2, 3, 4]` — **인덱스 0 이 비어 있는 게 눈으로 보인다.** 6번째 enqueue 는 정상적으로 거부된다.

### 7.5 실전에서는

파이썬에는 `collections.deque` 가 있고, 이게 내부적으로 블록 연결 리스트라 양쪽 끝 삽입·삭제가 모두 상수 시간이다.

> **직접 구현한 원형 큐 vs `deque`** — 300,000회의 enqueue/dequeue 혼합 연산에서 원형 큐 40.7 ms, `deque` 14.8 ms 로 **`deque` 가 2.8배 빠르다.** 파이썬 레벨 메서드 호출과 `%` 연산이 C 레벨 구현을 이길 수 없다. 원형 큐는 원리를 이해하는 용도고, 코딩 테스트에서는 `deque` 를 쓴다.

### 7.6 큐를 `list` 로 쓰면 안 되는 이유

`list.pop(0)` 은 뒤의 모든 원소를 한 칸씩 당긴다 — O(N)이다. N번 dequeue 하면 O(N²)이 된다.

> **실측.** 원소를 전부 꺼내는 데 걸린 시간.
>
> | N | `list.pop(0)` | `deque.popleft()` | 배수 |
> |---|---|---|---|
> | 10,000 | 7.5 ms | 0.4 ms | 20배 |
> | 50,000 | 222.5 ms | 2.3 ms | 97배 |
> | 100,000 | 883.4 ms | 3.5 ms | 249배 |
> | 200,000 | 3,539.1 ms | 7.5 ms | **470배** |
>
> N이 2배가 될 때 `list` 쪽 시간이 4배씩 뛴다 — 정확히 O(N²)의 모습이다. `deque` 쪽은 선형으로 는다. **BFS에서 큐를 `list` 로 쓰면 이 표의 왼쪽 열을 그대로 겪는다.**

---

## 8. 연결 리스트

### 8.1 리스트(배열)의 문제점

- 삽입/삭제 시 **연속된 메모리 배치를 유지하려고 원소들을 이동**시켜야 한다
- 원소가 많고 삽입/삭제가 잦을수록 이 비용이 크게 는다

```text
삽입: [4][9][ ][11][23]       삭제: [4][9][11][23]
          ↑ 18 삽입                     ↑ 9 삭제
      뒤쪽을 전부 한 칸씩 밀어야         뒤쪽을 전부 한 칸씩 당겨야
```

### 8.2 연결 리스트의 정의

- **논리적 순서와 물리적(메모리상) 순서가 일치하지 않는다.** 개별적으로 흩어져 있는 원소를 **연결**해 하나의 자료구조를 이룬다
- **링크를 통해 원소에 접근**하므로 물리적 순서를 맞추는 작업이 필요 없다
- 크기를 **동적으로 조정**할 수 있어 메모리를 효율적으로 쓴다

### 8.3 기본 구조

- **노드(Node)** — 연결 리스트에서 하나의 원소를 표현하는 기본 구성 요소
  - **데이터 필드** — 원소의 값
  - **링크 필드** — 다음 노드의 참조 값
- **헤드(Head)** — 첫 노드에 대한 참조 값

```text
Head ─▶ [ Data | Link ] ─▶ [ Data | Link ] ─▶ …
        └─── 노드(Node) ───┘
```

### 8.4 단순 연결 리스트

- 노드가 **하나의 링크 필드**로 다음 노드와 연결
- 헤드가 가장 앞 노드를 가리키고, 링크가 연속적으로 다음을 가리킨다
- **링크가 `Null` 인 노드가 마지막 노드**

```text
Head[0x1000] ─▶ 0x1000:[ A | 0x1008 ] ─▶ 0x1008:[ B | 0x1010 ]
             ─▶ 0x1010:[ C | 0x1018 ] ─▶ 0x1018:[ D | Null ]
```

### 8.5 삽입 연산 — 세 가지 위치

**① 첫 번째 노드로 삽입** (공백 리스트에 'A' 넣기)

1. 새 노드 `new` 생성
2. `new` 의 데이터 필드에 'A' 저장
3. **Head에 저장된 참조값을 `new` 의 링크 필드에 저장** (기존 Head가 Null이면 Null이 들어간다)
4. **Head에 `new` 의 참조값 저장**

**순서가 절대적이다.** 3번과 4번을 뒤집으면 Head를 먼저 덮어써서 기존 리스트 전체를 잃는다.

**② 마지막 노드로 삽입** ('C','A' 리스트 끝에 'D')

1. 새 노드 `new` 생성
2. 데이터에 'D', **링크에 `Null`** 저장
3. **리스트 마지막 노드의 링크 필드에 `new` 의 참조값 저장**

마지막 노드를 찾으려면 처음부터 걸어가야 한다 — 그래서 O(N)이다.

**③ 가운데 노드로 삽입** ('C','A','D' 의 두 번째 자리에 'B')

1. 새 노드 `new` 생성
2. 데이터에 'B' 저장
3. **삽입 위치 바로 앞 노드의 링크 필드를 `new` 의 링크 필드에 복사**
4. **`new` 의 참조값을 앞 노드의 링크 필드에 저장**

역시 3 → 4 순서다. 4를 먼저 하면 원래 다음 노드로 가는 유일한 참조를 잃는다.

### 8.6 삭제 연산

**가운데 노드 삭제** ('A','B','C','D' 에서 'B')

1. 삭제할 노드의 **선행 노드 탐색**
2. 삭제할 노드의 링크 필드를 **선행 노드의 링크 필드에 복사** (건너뛰게 만든다)
3. 삭제할 노드의 링크 필드에 `Null` 저장

**첫 노드 삭제** ('A','C','D' 에서 'A')

1. 선행 노드 탐색 → **없음**
2. 삭제할 노드의 링크 필드를 **Head에 복사**
3. 삭제할 노드의 링크 필드에 `Null` 저장

**선행 노드가 있느냐 없느냐가 코드의 분기를 만든다.** 그래서 구현에서 `position == 0` 을 따로 처리한다.

### 8.7 최종 구현

```python
class Node:
    def __init__(self, data):
        self.data = data
        self.next = None


class SinglyLinkedList:
    def __init__(self):
        self.head = None

    def insert(self, data, position):
        new_node = Node(data)
        if position == 0:
            new_node.next = self.head
            self.head = new_node
        else:
            current = self.head
            for _ in range(position - 1):
                if current is None:
                    print("범위를 벗어난 삽입입니다.")
                    return
                current = current.next
            new_node.next = current.next
            current.next = new_node

    def append(self, data):
        new_node = Node(data)
        if self.is_empty():
            self.head = new_node
        else:
            current = self.head
            while current.next:
                current = current.next
            current.next = new_node

    def is_empty(self):
        return self.head is None

    def delete(self, position):
        if self.is_empty():
            print("싱글 링크드 리스트가 비었습니다.")
            return
        if position == 0:
            deleted_data = self.head.data
            self.head = self.head.next
        else:
            current = self.head
            for _ in range(position - 1):
                if current is None or current.next is None:
                    print("범위를 벗어났습니다.")
                    return
                current = current.next
            deleted_node = current.next
            deleted_data = deleted_node.data
            current.next = current.next.next
        return deleted_data

    def search(self, data):
        current = self.head
        position = 0
        while current:
            if current.data == data:
                return position
            current = current.next
            position += 1
        return -1

    def __str__(self):
        result = []
        current = self.head
        while current:
            result.append(current.data)
            current = current.next
        return str(result)
```

---

## 9. 🔥 원본 코드의 버그 — 범위 검사가 정확히 한 칸씩 어긋난다

정상 입력으로는 잘 돌아간다. `append('A'), append('C'), append('D')` 후 `insert('B', 1)` 하면 `['A','B','C','D']`, `delete(1)` 하면 `'B'` 를 반환하고 `['A','C','D']` 가 된다. `search` 도 맞는다.

**문제는 범위를 벗어난 입력이다.** 이 코드에는 `print("범위를 벗어난 삽입입니다.")` / `print("범위를 벗어났습니다.")` 라는 방어 코드가 분명히 있는데, **가장 흔한 실수인 '한 칸 넘긴 경우'에는 그 메시지가 안 뜨고 `AttributeError` 로 죽는다.**

### 9.1 재현

```python
L = SinglyLinkedList()          # 빈 리스트
L.insert('X', 1)
# AttributeError: 'NoneType' object has no attribute 'next'

M = SinglyLinkedList()
M.append('A'); M.append('B')    # 길이 2
M.insert('X', 3)                # 한 칸 밖
# AttributeError: 'NoneType' object has no attribute 'next'
M.insert('X', 5)                # 세 칸 밖
# "범위를 벗어난 삽입입니다."  ← 이건 정상 동작

M.delete(2)                     # 마지막 원소 바로 다음 자리
# AttributeError: 'NoneType' object has no attribute 'data'
M.delete(5)
# "범위를 벗어났습니다."  ← 이건 정상 동작
```

### 9.2 전수 대조

길이 0~3 × position 0~길이+2 를 전부 돌렸다.

| 길이 | position | insert 결과 | delete 결과 |
|---|---|---|---|
| 0 | 0 | `['X']` | 비었습니다 |
| 0 | **1** | **AttributeError** | 비었습니다 |
| 0 | 2 | 범위를 벗어난 삽입입니다 | 비었습니다 |
| 1 | 0 | `['X','A']` | `'A'` |
| 1 | **1** | `['A','X']` | **AttributeError** |
| 1 | **2** | **AttributeError** | 범위를 벗어났습니다 |
| 2 | 2 | `['A','B','X']` | **AttributeError** |
| 2 | **3** | **AttributeError** | 범위를 벗어났습니다 |
| 3 | 3 | `['A','B','C','X']` | **AttributeError** |
| 3 | **4** | **AttributeError** | 범위를 벗어났습니다 |

패턴이 뚜렷하다.

- `insert` 는 `position == 길이 + 1` 에서만 터진다. `길이 + 2` 부터는 메시지가 뜬다
- `delete` 는 `position == 길이` 에서만 터진다. `길이 + 1` 부터는 메시지가 뜬다

### 9.3 원인

```python
current = self.head
for _ in range(position - 1):
    if current is None:            # ← 전진하기 "전에" 검사한다
        print("범위를 벗어난 삽입입니다.")
        return
    current = current.next         # ← 이 줄이 current 를 None 으로 만들 수 있다
new_node.next = current.next       # ← 그 None 을 검사 없이 쓴다
```

**검사가 루프 안에만 있고, 루프를 빠져나온 뒤에는 없다.** 마지막 `current = current.next` 가 `current` 를 `None` 으로 만들면 그대로 루프가 끝나고 `current.next` 를 건드린다. 한 칸 더 벗어나면 루프가 한 번 더 돌면서 검사에 걸리므로, **딱 경계 한 칸에서만 구멍이 난다.**

이런 종류의 버그가 위험한 건 **가장 자주 들어오는 잘못된 입력이 정확히 그 한 칸**이기 때문이다. 리스트 길이를 하나 잘못 센 경우가 대부분이지, 세 칸씩 넘겨 부르는 일은 드물다.

### 9.4 고친 코드

루프를 빠져나온 뒤에 검사를 한 번 더 하면 된다.

```python
def insert(self, data, position):
    new_node = Node(data)
    if position == 0:
        new_node.next = self.head
        self.head = new_node
        return True
    current = self.head
    for _ in range(position - 1):
        if current is None:
            break
        current = current.next
    if current is None:                       # ← 추가
        print("범위를 벗어난 삽입입니다.")
        return False
    new_node.next = current.next
    current.next = new_node
    return True

def delete(self, position):
    if self.is_empty():
        print("싱글 링크드 리스트가 비었습니다.")
        return None
    if position == 0:
        d = self.head.data
        self.head = self.head.next
        return d
    current = self.head
    for _ in range(position - 1):
        if current is None:
            break
        current = current.next
    if current is None or current.next is None:   # ← 여기로 옮긴다
        print("범위를 벗어났습니다.")
        return None
    d = current.next.data
    current.next = current.next.next
    return d
```

> **검증.** 고친 코드로 길이 0~5 × position 0~길이+2 의 모든 조합에 대해 삽입 후 리스트 / 삭제 반환값 / 삭제 후 리스트를 파이썬 `list` 의 동작과 대조했다. **99건 전부 일치, 불일치 0건.**

---

## 10. 연결 리스트는 정말 빠른가 — 실측

자료는 단순 연결 리스트를 이렇게 정리한다.

- **장점** — 필요한 만큼만 메모리를 사용
- **단점** — 특정 요소 접근이 순차 탐색이라 O(N), 역방향 탐색 불가

"삽입/삭제가 O(1)이라 배열보다 빠르다"는 통념이 흔한데, 파이썬에서는 조건이 붙는다.

### 10.1 맨 앞 삽입 — 연결 리스트가 압승

| N | 연결 리스트 head 삽입 | `list.insert(0, x)` | 배수 |
|---|---|---|---|
| 1,000 | 0.2 ms | 0.2 ms | 1.3배 |
| 10,000 | 1.7 ms | 18.0 ms | 10.5배 |
| 50,000 | 10.8 ms | 447.0 ms | 41.3배 |
| 100,000 | 27.4 ms | 1,777.4 ms | 64.8배 |
| 300,000 | 91.3 ms | 16,110.3 ms | **176.5배** |

O(1) N번 = O(N) 과 O(N) N번 = O(N²) 의 차이가 그대로 나온다. **여기까지는 통념대로다.**

### 10.2 가운데 삽입 — 뒤집힌다

N=50,000 리스트의 한가운데에 1,000번 삽입했다.

| 방법 | 시간 |
|---|---|
| 연결 리스트 · 매번 head 부터 중간까지 **걸어가서** 삽입 | 349.53 ms |
| 파이썬 `list` · `insert(N//2, -1)` | 9.19 ms |
| 연결 리스트 · **노드 참조를 이미 들고 있을 때** | **0.156 ms** |

**같은 "연결 리스트 가운데 삽입"인데 두 경우의 차이가 2,237배다.**

- 걸어가야 하면 파이썬 `list` 보다 **38배 느리다**. 25,000번의 `cur = cur.next` 가 전부 파이썬 레벨 루프인데, `list.insert` 의 이동은 C 레벨 `memmove` 한 번이다
- 참조를 들고 있으면 `list` 보다 **59배 빠르다**. 이때가 진짜 O(1)이다

**결론.** 연결 리스트의 "삽입·삭제 O(1)"은 **위치를 이미 알고 있을 때만** 성립한다. 위치를 찾는 O(N)이 앞에 붙으면 배열의 O(N) 이동보다 오히려 느리다 — 파이썬 반복문이 C 레벨 메모리 복사보다 훨씬 무겁기 때문이다.

그래서 실전에서 연결 리스트가 이기는 자리는 **"이미 손에 쥐고 있는 노드를 떼거나 붙이는" 패턴**이다. LRU 캐시(해시로 노드를 O(1)에 찾고 → 연결 리스트에서 O(1)에 떼어 앞으로 옮김)가 정확히 그 구조다.

---

## 11. 이중 연결 리스트 (참고)

단순 연결 리스트의 단점 "역방향 탐색 불가"를 없앤다.

- 양쪽 방향으로 순회할 수 있도록 노드를 연결
- **두 개의 링크 필드**(`prev`, `next`)와 한 개의 데이터 필드

```text
Head ─▶ [Null| A |0x100C] ⇄ [0x1000| B |0x1018] ⇄ [0x100C| C |Null]
```

### 11.1 삽입 (cur 다음에 D 넣기) — 5단계

1. 새 노드 `new` 생성, 데이터에 'D'
2. **cur의 next 를 new의 next 에 저장** (new가 cur의 다음을 가리키게)
3. **cur의 참조값을 new의 prev 에 저장** (new가 cur을 가리키게)
4. **new의 참조값을 cur(= new의 이전 노드)의 next 에 저장**
5. **new의 참조값을 new의 다음 노드의 prev 에 저장**

**2·3 을 먼저, 4·5 를 나중에 하는 게 핵심이다.** 새 노드의 양쪽 링크를 먼저 채우고 나서 기존 노드들의 링크를 바꾼다. 순서를 뒤집으면 기존 연결을 잃는다.

### 11.2 삭제 (cur 제거) — 4단계

1. 현재 상태
2. **cur의 next 를 cur의 이전 노드의 next 에 저장**
3. **cur의 prev 를 cur의 다음 노드의 prev 에 저장**
4. **cur의 prev, next 에 Null 저장**

이중 연결 리스트에서는 **선행 노드를 찾을 필요가 없다.** `cur.prev` 가 이미 그것이다. 단순 연결 리스트의 삭제가 "선행 노드 탐색"부터 시작해야 했던 것과 대비된다 — 링크 하나를 더 두는 값이 여기서 나온다.

> 자료의 이중 연결 리스트 구간에는 **코드 슬라이드가 하나도 없다.** 그림만 10장이다. 파이썬 `collections.deque` 가 내부적으로 이 구조(블록 단위 이중 연결 리스트)라는 점을 알아 두면 충분하다.

---

## 12. 정리

| | 스택 | 큐 | 연결 리스트 |
|---|---|---|---|
| 제한 | 한쪽 끝에서만 삽입·삭제 | 뒤에서 삽입, 앞에서 삭제 | 제한 없음(대신 순차 접근) |
| 순서 | LIFO | FIFO | — |
| 대표 응용 | 괄호 검사, 함수 호출, 후위 표기 계산, DFS | 버퍼, 대기 행렬, BFS | 동적 크기, 잦은 중간 삽입·삭제 |
| 파이썬 실전 | `list` (`append`/`pop`) | `collections.deque` | 직접 구현 (표준 자료형 없음) |

이번 자료에서 얻은 것 세 가지.

1. **제한이 기능이다.** 스택은 삽입·삭제를 한쪽으로 몰았기 때문에 "가장 최근 것"을 공짜로 안다 → 괄호 검사와 함수 복귀가 풀린다. 큐는 양쪽으로 갈랐기 때문에 "가장 오래된 것"을 공짜로 안다 → 순서 보존과 최단 거리가 풀린다
2. **원본 코드는 반드시 실행해 본다.** 연결 리스트의 범위 검사 버그는 눈으로 봐서는 못 잡았다. 방어 코드가 "있어 보이는" 게 오히려 함정이었다
3. **점근 복잡도가 파이썬에서 그대로 나오지 않는다.** 연결 리스트 가운데 삽입은 O(1)로 적히지만, 위치를 찾는 비용을 포함하면 배열보다 38배 느렸다. 0902 노트의 바이너리 카운팅·`used` 배열 사례와 원인이 같다 — **파이썬 레벨 루프 vs C 레벨 루프**

다음 자료는 트리와 그래프다. 여기서 만든 두 도구가 바로 쓰인다 — **스택으로 깊이 우선, 큐로 너비 우선**, 그리고 **연결 리스트 노드로 트리 노드**를 만든다.
