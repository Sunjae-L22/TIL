# RAG 설계 결정 — Naive에서 Modular까지, 청킹 3전략, 그리고 임베딩 고르기

> 출처: `1-4-rag-theory-and-core-modules.pdf`, `1-3-langchain-framework-deep-dive.pdf` 등 LLM 추가자료 · 정리일: 2026-08-24
> 블로그 발행본: https://it-study-2002.tistory.com/entry/RAG-%EC%84%A4%EA%B3%84-%EA%B2%B0%EC%A0%95
> 앞선 편: [0824 토큰과 컨텍스트 윈도우](0824_토큰과컨텍스트윈도우.md)

RAG 파이프라인을 처음 붙이고 나면 대개 잘 돈다. 문제는 잘 돌지 **않을 때** 어디를 손대야 할지 알 수 없다는 것이다. 답이 이상하면 프롬프트를 고쳐 보고, 그래도 안 되면 `k`를 3에서 10으로 올려 보고, 그래도 안 되면 모델을 바꿔 본다. 셋 다 대체로 헛수고다.

RAG는 하나의 기법이 아니라 **여섯 단계짜리 파이프라인**이고, 각 단계마다 결정해야 할 것이 따로 있다. 문서를 어디서 자를 것인가, 어떤 임베딩 모델을 쓸 것인가, 검색 전에 질문을 다시 쓸 것인가, 검색 후에 다시 정렬할 것인가. 이 결정들을 구분하지 못하면 디버깅이 통째로 감이 된다.

> 이 글은 [토큰과 컨텍스트 윈도우](0824_토큰과컨텍스트윈도우.md)에서 이어진다. 그 글의 마지막에서 "검색이 빗나가면 `k`가 아니라 다른 곳을 손대야 한다"고 미뤄 뒀던 이야기다.

RAG를 코드로 구현하는 과정은 [LangChain 체인과 RAG](https://it-study-2002.tistory.com/entry/LangChain-%EC%B2%B4%EC%9D%B8%EA%B3%BC-RAG) 편에서 다뤘고, 검색이 왜 어려운가라는 이론적 배경은 [RAG 이론과 Deep Research](https://it-study-2002.tistory.com/entry/RAG%EC%9D%B4%EB%A1%A0-Deepresearch) 편에 있다. 이 글은 그 사이를 메운다 — **돌아가는 파이프라인을 놓고 무엇을 고를 것인가.**

<br>

## 📖 이 글에서 다루는 내용

1. 파인튜닝과 RAG — 같은 문제를 푸는 게 아니다
2. Naive RAG — 일직선 파이프라인과 그 천장
3. Advanced RAG — 검색 앞뒤에 무엇을 덧대는가
4. Modular RAG — 단계를 부품으로 쪼개면
5. 계보 정리 — 무엇을 해결했고 무엇이 남았나
6. 청킹 3전략 — 어디서 자를 것인가
7. 크기와 겹침 — 두 개의 손잡이
8. 임베딩 모델 선택 — 벤치마크로는 못 고른다
9. 최소 평가 세트 — 고르려면 재야 한다
10. 검색을 도구로 노출할 때 — 스키마가 라우팅을 정한다

<br>

---

## 1. 파인튜닝과 RAG — 같은 문제를 푸는 게 아니다

"모델이 우리 문서를 모른다"는 문제 앞에서 선택지는 둘로 보인다. 파인튜닝으로 가르치거나, RAG로 찾아서 넣어주거나. 그런데 이 둘은 **경쟁하는 대안이 아니라 서로 다른 문제를 푸는 도구**다.

> **[그림]** 파인튜닝은 모델의 가중치를 바꾸고 RAG는 입력에 근거를 덧붙인다는 차이를 두 개의 파이프라인으로 나란히 비교한 다이어그램

| | Fine-Tuning | RAG |
|---|---|---|
| **무엇을 바꾸나** | 모델의 가중치 | 모델에 들어가는 입력 |
| **잘하는 것** | 말투, 출력 형식, 도메인 특유의 업무 방식 | 최신 정보, 사내 문서, 자주 바뀌는 지식 |
| **지식 갱신** | 재학습이 필요하다 | 저장소만 다시 색인하면 된다 |
| **근거 제시** | 어렵다. 왜 그렇게 답했는지 추적 불가 | 검색된 문서를 출처로 붙일 수 있다 |
| **보안 문서** | 학습 데이터에 넣어야 해 부담이 크다 | 저장소에 두고 접근 제어를 걸 수 있다 |

가르는 기준을 한 줄로 적으면 이렇다 — **"어떻게 말하는가"를 바꾸려면 파인튜닝, "무엇을 아는가"를 바꾸려면 RAG.**

그래서 실서비스에서는 둘을 같이 쓰는 경우가 흔하다. 상담 어투와 응답 형식은 파인튜닝으로 굳히고, 상품 정보와 약관은 RAG로 붙이는 식이다. 반대로 **파인튜닝으로 지식을 주입하려는 시도는 대체로 비싸고 잘 안 된다.** 학습된 지식은 어느 파라미터에 들어갔는지 알 수 없어 수정도 삭제도 어렵고, 문서가 한 줄 바뀔 때마다 재학습할 수는 없다.

> 💡 **"우리 회사 문서로 파인튜닝하자"는 요청이 오면 먼저 물을 것**
> 원하는 게 지식인지 형식인지 확인한다. 대개는 지식이고, 그러면 RAG가 답이다. 형식이라도 프롬프트와 구조화 출력으로 상당 부분 해결되므로, 파인튜닝은 그 둘로 안 되는 것이 명확해진 다음에 꺼내는 카드다.

<br>

---

## 2. Naive RAG — 일직선 파이프라인과 그 천장

가장 기본적인 형태부터 본다. **Naive RAG**는 다섯 단계가 한 줄로 이어진 구조다.

> **[그림]** 문서 로드에서 청크 분할, 임베딩, 저장, 유사 검색, 답변 생성까지 한 방향으로 이어지는 Naive RAG 파이프라인과 그 위에 표시된 네 가지 구조적 한계

```text
문서 로드 → 청크 분할 → 임베딩·저장  |  질문 임베딩 → 유사 검색 → 답변 생성
└──────── 색인 (문서가 바뀔 때) ─────┘  └────── 질의 (질문마다) ──────┘
```

구현 자체는 몇 줄이면 끝난다.

```python
loader = PDFMinerLoader(str(PDF_PATH))
documents = loader.load()

splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=75)
chunks = splitter.split_documents(documents)

vectorstore = Chroma.from_documents(chunks, embedding=embeddings)
retriever = vectorstore.as_retriever(search_kwargs={'k': 3})

rag_chain = (
    {'context': retriever | format_docs, 'question': RunnablePassthrough()}
    | prompt | llm | StrOutputParser()
)
```

이 구조가 잘 도는 조건은 꽤 좁다. **질문이 문서의 표현과 비슷하고, 답이 한 청크 안에 온전히 들어 있고, 저장소가 하나일 때.** 그 조건을 벗어나는 순간 네 가지 지점에서 막힌다.

| 한계 | 구체적으로 무슨 일이 일어나나 |
|---|---|
| **질문을 그대로 검색어로 쓴다** | "그거 얼마야?" 같은 구어체 질문은 문서의 어떤 문장과도 안 닮았다 |
| **검색이 한 번뿐이다 (one-shot)** | 첫 검색이 빗나가면 그걸로 끝. 결과를 검토하는 단계가 없다 |
| **키워드를 못 잡는다** | 벡터 검색은 의미는 잡지만 상품 코드·조항 번호 같은 **정확한 문자열**에 약하다 |
| **답을 검토하지 않는다** | 근거에 없는 내용을 지어내도 걸러지지 않는다 |

네 항목을 한 문장으로 줄이면 — **일직선이라 되돌아갈 수가 없다.** Advanced RAG는 이 직선 위에 되돌아갈 지점을 만드는 작업이다.

<br>

---

## 3. Advanced RAG — 검색 앞뒤에 무엇을 덧대는가

Advanced RAG는 새로운 구조가 아니라, 앞 절의 네 한계에 각각 대응하는 **보완 단계를 검색 앞뒤에 끼워 넣은 것**이다.

> **[그림]** Naive RAG의 일직선 위에 질의 재작성, 하이브리드 검색, 리랭킹, 자가 검토 네 단계가 검색 앞뒤로 덧붙는 구조를 보여주는 다이어그램

### 검색 전 — 질의 재작성 (Query Rewrite)

사용자의 질문을 **검색하기 좋은 형태로 다시 쓴다.** 대화 맥락을 풀어 넣고, 구어체를 문서의 표현으로 바꾸고, 필요하면 여러 개의 검색어로 쪼갠다.

```text
원 질문   : "그거 슬개골도 되나?"
대화 맥락 : (앞 턴에서 반려묘 실손보험을 이야기했다)
재작성    : "반려묘 실손보험의 슬개골 탈구 보장 여부와 면책 조건"
```

앞 글에서 본 [토큰과 컨텍스트 윈도우](0824_토큰과컨텍스트윈도우.md)의 관점에서 보면 이 단계는 **LLM 호출이 한 번 더 들어가는 비용**이다. 그 비용을 낼 만한 자리는 대체로 멀티턴 대화형 RAG다. 단발성 질문만 받는다면 효과가 크지 않다.

### 검색 시 — 하이브리드 검색 (Hybrid Search)

벡터 검색과 키워드 검색(BM25 등)을 **함께 돌리고 결과를 합친다.** 서로의 약점이 정확히 반대라서 잘 맞는다.

| | 잘 잡는 것 | 놓치는 것 |
|---|---|---|
| **벡터 검색** | 표현이 달라도 의미가 같은 문장 | 상품 코드, 조항 번호, 고유명사, 오타 |
| **키워드 검색** | 정확한 문자열, 희귀 용어 | 동의어, 다른 표현으로 쓴 같은 뜻 |

`제3조 제2항`이나 `KB-2024-A`처럼 **의미가 아니라 표기 자체가 중요한 것**을 다루는 도메인에서는 하이브리드가 사실상 필수다.

### 검색 후 — 리랭킹 (Re-ranking)

검색으로 넉넉하게(예: 20개) 가져온 뒤, **더 정확한 모델로 다시 순위를 매겨** 상위 3개만 남긴다.

```text
1단계 (Bi-encoder)   질문·문서를 따로 인코딩 → 코사인 유사도 → 빠르다, 덜 정확하다 → 20개
2단계 (Cross-encoder) 질문+문서를 함께 인코딩 → 직접 관련도 산출 → 느리다, 정확하다 → 3개
```

이 2단계 구조가 성립하는 이유는 비용 구조가 다르기 때문이다. Bi-encoder는 문서 벡터를 **미리 계산해 둘 수 있어** 수만 건을 훑어도 싸다. Cross-encoder는 질문이 들어와야 계산이 시작되므로 후보 20개 정도에만 쓸 수 있다. 자세한 원리는 [RAG 이론과 Deep Research](https://it-study-2002.tistory.com/entry/RAG%EC%9D%B4%EB%A1%A0-Deepresearch) 편의 Bi/Cross 절에 있다.

> ✅ **`k`를 키우고 싶을 때가 리랭킹을 붙일 때다**
> 앞 글에서 "`k`를 3에서 10으로 올리는 것은 대개 역효과"라고 했는데, 정확히는 **`k`를 올리되 그 뒤에 리랭킹을 붙이면 이야기가 달라진다.** 검색 단계에서 20개를 가져오는 것은 재현율(recall)을 위한 것이고, 리랭킹이 그중에서 3개로 줄여 정밀도(precision)를 회복한다. 리랭킹 없이 `k`만 올리면 노이즈 17개가 그대로 프롬프트에 들어간다.

### 생성 후 — 자가 검토 (Self-check)

답변이 검색된 근거 안에 있는 내용인지 다시 확인한다. 근거에 없으면 재검색하거나 "답할 수 없다"로 돌린다. 구현은 대체로 구조화 출력을 쓴다.

```python
class Grounding(BaseModel):
    supported: bool = Field(description='답변이 주어진 근거만으로 뒷받침되는가')
    missing: str = Field(description='근거에서 확인되지 않는 주장. 없으면 빈 문자열')

checker = llm.with_structured_output(Grounding)
```

<br>

---

## 4. Modular RAG — 단계를 부품으로 쪼개면

Advanced RAG까지는 여전히 **하나의 정해진 순서**가 있다. Modular RAG는 그 순서를 고정하지 않는다. 각 단계를 독립적인 모듈로 만들어 놓고, **질문에 따라 다르게 조립한다.**

> **[그림]** 같은 모듈 집합을 질문 유형에 따라 다르게 배열한 세 가지 경로를 나란히 놓아 Modular RAG의 조립 가능성을 보여주는 다이어그램

```text
[부품]  질의 재작성 · 벡터 검색 · 키워드 검색 · 리랭킹 · 생성 · 자가 검토 · 재검색

간단한 질문   :  벡터 검색 → 생성
정확한 코드   :  키워드 검색 → 생성
복잡한 질문   :  질의 재작성 → 벡터+키워드 → 리랭킹 → 생성 → 자가 검토 → (필요시 재검색)
문서에 없는 것 :  검색 생략 → 바로 생성  또는  거절
```

마지막 줄이 중요하다. **검색을 아예 하지 않는 것도 하나의 경로다.** 모델이 이미 잘 아는 일반 상식에 검색을 붙이면 정확도가 오르기는커녕 떨어질 수 있고, 비용과 지연만 는다. "검색할지 말지"부터가 판단 대상이라는 이야기다.

여기서 자연스럽게 다음 단계가 나온다. 경로를 사람이 미리 정해 두는 대신 **LLM에게 고르게 하면** 그게 Agentic RAG다. 그 이야기 — 에이전트의 네 가지 역할, Router 구조와 계층형 구조 — 는 [RAG 이론과 Deep Research](https://it-study-2002.tistory.com/entry/RAG%EC%9D%B4%EB%A1%A0-Deepresearch) 편에서 다뤘으므로 여기서는 넘어간다.

> ⚠️ **처음부터 Modular로 짓지 않는다**
> 모듈이 늘수록 조합이 늘고, 조합이 늘수록 **어느 조합에서 문제가 생겼는지 추적하기 어려워진다.** 실전 순서는 Naive로 시작해 실패 사례를 모으고, 그 실패가 어느 단계에서 발생하는지 확인한 뒤 그 단계에만 보완을 붙이는 쪽이다. 9절의 평가 세트가 그 확인을 가능하게 만든다.

<br>

---

## 5. 계보 정리 — 무엇을 해결했고 무엇이 남았나

세 단계를 한 장으로 정리하면 이렇다.

> **[그림]** Naive RAG에서 Advanced RAG를 거쳐 Modular RAG로 이어지는 발전 과정을 각 단계가 해결한 것과 남긴 문제로 나누어 표시한 계보 다이어그램

| 단계 | 해결한 것 | 남은 문제 |
|---|---|---|
| **Naive** | 모델이 모르는 외부 지식을 붙였다 | 한 번 검색하고 끝. 검토도 되돌아감도 없다 |
| **Advanced** | 질의를 다듬고, 재정렬하고, 검토한다 | 순서가 고정이라 질문 종류에 맞출 수 없다 |
| **Modular** | 질문에 따라 경로를 다르게 조립한다 | 경로를 **누가** 고르는가 → Agentic RAG |

> ➡️ **각 단계는 앞 단계를 대체하지 않는다.** Modular RAG의 부품은 Advanced RAG에서 만든 것이고, Advanced RAG의 뼈대는 Naive RAG다. 최신 구조를 쓴다고 앞 단계를 건너뛸 수 있는 게 아니라, **앞 단계를 제대로 만들어 놓아야 부품으로 쓸 수 있다.**

<br>

---

## 6. 청킹 3전략 — 어디서 자를 것인가

파이프라인 구조에서 개별 단계의 결정으로 내려온다. 첫 번째는 **청킹**이다. 검색의 최소 단위를 만드는 단계라, 여기서 잘못 자르면 뒤의 어떤 보완도 회복시키지 못한다.

> **[그림]** 같은 문단을 고정 크기·재귀·의미 단위 세 가지 방식으로 자른 결과를 나란히 놓아 경계가 어디에 생기는지 비교한 다이어그램

### Fixed-size Chunking — 글자 수로 자른다

정해진 길이마다 기계적으로 자른다.

- **장점** — 구현이 가장 단순하고, 청크 크기가 균일해 토큰 예산을 정확히 계산할 수 있다.
- **단점** — **문장이나 표 중간에서 잘린다.** 잘린 반쪽은 그 자체로는 의미가 통하지 않아 검색에도 안 걸리고, 걸려도 근거 구실을 못 한다.
- **쓸 자리** — 구조가 거의 없는 로그나 자막처럼 애초에 경계가 없는 텍스트, 그리고 빠른 실험.

### Recursive Chunking — 자연스러운 경계부터 찾는다

**문단 → 줄 → 단어 → 글자** 순으로 내려가며 자를 지점을 찾는다. 목표 크기 안에 들어오는 상위 경계가 있으면 거기서 자르고, 없으면 한 단계 잘게 내려간다.

```python
splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=75,
    separators=['\n\n', '\n', '. ', ' ', ''],   # 위에서부터 시도한다
)
```

- **장점** — 의미 단위가 어느 정도 보존되면서 크기도 대체로 균일하다. 추가 비용이 없다.
- **단점** — 문서에 문단 구조가 없으면 결국 Fixed-size와 같아진다.
- **쓸 자리** — **대부분의 문서에서 기본 선택지.** 조·항으로 끊기는 약관, 소제목이 있는 매뉴얼처럼 문단 경계가 곧 의미 경계인 문서에서 특히 잘 맞는다.

`separators` 목록을 문서에 맞게 바꾸면 효과가 꽤 크다. 마크다운이면 `['\n## ', '\n### ', '\n\n', ...]`처럼 헤딩을 최우선 경계로 올리는 식이다.

### Semantic Chunking — 의미가 바뀌는 지점에서 자른다

문장 단위로 임베딩한 뒤, **인접 문장 사이의 유사도가 급격히 떨어지는 지점**을 주제 전환으로 보고 자른다.

- **장점** — 한 청크가 하나의 주제를 담는다. 주제가 문단과 무관하게 이어지거나 끊기는 문서에서 강하다.
- **단점** — **색인 단계에서 임베딩을 두 번 한다**(경계 탐색용 + 최종 청크용). 청크 크기가 들쭉날쭉해 토큰 예산 계산이 어렵고, 유사도 임계값이라는 튜닝 대상이 하나 더 생긴다.
- **쓸 자리** — 보고서, 정책 문서, 논문처럼 **의미 흐름이 문서 구조와 어긋나는** 자료.

| | Fixed-size | Recursive | Semantic |
|---|---|---|---|
| 자르는 기준 | 글자·토큰 수 | 구분자 우선순위 | 문장 간 의미 유사도 |
| 의미 보존 | 낮음 | 중간 | 높음 |
| 크기 균일성 | 높음 | 중간 | 낮음 |
| 색인 비용 | 없음 | 없음 | 임베딩 추가 |
| 기본값으로 | ✗ | **✓** | 필요할 때 |

> 💡 **표와 코드 블록은 어떤 전략으로도 잘 안 잘린다**
> 세 전략 모두 텍스트가 줄글이라고 가정한다. 표는 헤더와 데이터 행이 갈라지면 양쪽 다 쓸모가 없어지고, 코드는 함수 중간에서 끊기면 근거가 되지 못한다. 이런 요소는 **자르기 전에 따로 뽑아내 통째로 한 청크**로 만들고, 앞뒤 문맥 한두 줄을 붙여 두는 편이 낫다. 로딩 단계에서 표를 별도 Document로 분리하는 로더를 쓰는 것도 방법이다.

<br>

---

## 7. 크기와 겹침 — 두 개의 손잡이

전략을 골랐으면 숫자를 정해야 한다. `chunk_size`와 `chunk_overlap`은 **정밀도와 문맥 사이의 트레이드오프 손잡이**다.

> **[그림]** chunk_size를 줄이면 검색 정밀도가 오르고 문맥이 줄어드는 반대 방향의 변화를 두 축으로 표시하고 chunk_overlap이 경계 문장을 어떻게 보호하는지 함께 보여주는 다이어그램

| 손잡이 | 작게 하면 | 크게 하면 |
|---|---|---|
| `chunk_size` | 검색 단위가 정밀해진다 / 문맥이 부족해진다 | 문맥이 풍부해진다 / 무관한 내용이 딸려 온다 |
| `chunk_overlap` | 중복 저장·비용이 준다 / 경계 문맥이 끊긴다 | 경계가 보호된다 / 중복 저장·임베딩 비용이 는다 |

출발점으로는 `chunk_size` 300~800자, `chunk_overlap`은 그 **10~20%** 가 무난하다. 다만 이 숫자는 문서 종류와 질문 유형에 따라 달라지므로, 정답을 찾으려 하기보다 **9절의 평가 세트로 두세 개 값을 비교해 보는 편**이 빠르다.

여기에 두 가지 실무 규칙을 덧붙인다.

**첫째, 겹침을 0으로 두지 않는다.** 겹침이 없으면 경계에 걸친 문장이 앞뒤로 반 토막씩 나뉜다. 하필 그 문장이 답의 근거라면 어느 청크를 가져와도 반쪽짜리 근거만 들어간다. 겹침은 낭비가 아니라 **경계에 걸친 정보의 보험**이다.

**둘째, 검색 단위와 전달 단위를 분리할 수 있다.** 작은 청크로 검색해 정밀도를 챙기고, 실제 프롬프트에는 그 청크가 속한 **더 큰 단위(부모 문서)를 넣어** 문맥을 채우는 방식이다. `chunk_size` 손잡이의 트레이드오프를 정면으로 우회한다.

```python
# 검색은 작은 조각으로, 전달은 큰 조각으로
from langchain.retrievers import ParentDocumentRetriever
```

> ⚠️ **청킹을 바꾸면 색인을 전부 다시 만들어야 한다**
> `chunk_size`를 한 번 바꾸면 기존 벡터는 전부 무효다. 임베딩 비용이 색인 쪽에 몰려 있다는 걸 감안하면, 문서가 많을수록 이 재작업이 무겁다. 그래서 **청킹 실험은 전체 문서가 아니라 대표 문서 몇 건으로 먼저** 하고, 값을 확정한 뒤 전체 색인을 돌리는 순서가 맞다.

<br>

---

## 8. 임베딩 모델 선택 — 벤치마크로는 못 고른다

검색 품질의 상한을 정하는 것은 결국 임베딩 모델이다. 그런데 이 선택은 리더보드 점수로 결정할 수 없다.

> **[그림]** 임베딩 모델 선택 시 검토해야 할 언어 적합성, 도메인 용어, 벡터 차원과 비용, 최대 입력 길이 네 축을 사분면으로 정리한 다이어그램

| 기준 | 왜 중요한가 |
|---|---|
| **언어 적합성** | 다국어 모델이라고 한국어를 잘하는 게 아니다. 한국어 의미 유사도를 실제로 재 봐야 한다 |
| **도메인 용어** | 일반 코퍼스로 학습한 모델은 업계 전문 용어의 의미 관계를 모른다. 사내 문서에서 가장 자주 걸리는 지점 |
| **벡터 차원** | 차원이 높으면 대체로 정확하지만 저장 용량과 검색 시간이 는다. 문서가 많을수록 부담이 커진다 |
| **최대 입력 길이** | 모델이 받는 토큰 한도보다 청크가 길면 **뒤가 잘린 채 임베딩된다.** 조용히 실패하므로 특히 위험하다 |
| **비용과 지연** | 색인은 일회성이지만 질의 임베딩은 질문마다 발생한다. 두 비용을 나눠서 본다 |
| **운영 형태** | API형은 시작이 빠르고, 로컬 오픈소스형은 문서를 외부로 내보내지 않는다 |

벤치마크 점수를 신뢰하기 어려운 이유는 단순하다. **벤치마크의 문서와 질문이 내 문서와 내 사용자의 질문이 아니기 때문이다.** 일반 문서 검색에서 1위인 모델이 특정 도메인 문서에서 3위 모델보다 나쁠 수 있고, 그 사실은 재 보기 전에는 알 수 없다.

> ⚠️ **색인과 질의는 반드시 같은 모델로 한다**
> 서로 다른 임베딩 모델이 만든 벡터는 같은 공간에 있지 않으므로 유사도가 아무 의미가 없다. 그런데 **에러가 나지 않는다** — 차원만 맞으면 계산은 되고, 그냥 엉뚱한 결과가 나온다. 모델을 바꾸면 색인 전체를 다시 만들어야 하고, 저장소 메타데이터에 어떤 모델로 만든 색인인지 기록해 두면 이 사고를 막을 수 있다.

<br>

---

## 9. 최소 평가 세트 — 고르려면 재야 한다

앞의 세 절이 전부 "재 보고 정하라"로 끝났다. 그럼 무엇을 어떻게 재는가. **평가 세트 없이는 어떤 결정도 감으로 하게 된다.**

거창할 필요는 없다. 최소 구성은 이렇다.

> **[그림]** 실제 사용자 질문, 기대되는 정답 문서, 설정별 검색 결과, 비용과 응답 시간 네 가지로 구성된 최소 평가 세트의 구조를 보여주는 다이어그램

| 구성 요소 | 어떻게 만드나 |
|---|---|
| **실제 질문 20~30개** | 상상해서 쓰지 말고 실제 사용자 질문 로그에서 가져온다. 어려운 질문·모호한 질문·문서에 답이 없는 질문을 반드시 섞는다 |
| **기대 정답 문서** | 각 질문에 대해 "이 청크(또는 이 페이지)가 나와야 한다"를 사람이 지정한다. 가장 손이 많이 가지만 여기가 핵심이다 |
| **설정별 검색 결과** | 청킹·임베딩·`k` 조합을 바꿔가며 검색만 돌린다. 생성까지 갈 필요 없다 |
| **비용과 응답 시간** | 색인 비용, 질의당 비용, 응답 지연을 함께 기록한다 |

검색 단계만 먼저 재는 게 요령이다. **생성 품질은 검색 품질의 상한을 넘지 못하므로**, 검색이 정답 문서를 못 가져오는 상태에서 프롬프트를 아무리 다듬어도 소용이 없다.

```python
def recall_at_k(cases, retriever, k=3):
    hit = 0
    for question, expected_ids in cases:
        got = {d.metadata['id'] for d in retriever.invoke(question)[:k]}
        if got & set(expected_ids):
            hit += 1
    return hit / len(cases)

# 설정을 바꿔가며 같은 함수를 돌린다
# chunk_size=300 → 0.72   chunk_size=500 → 0.85   chunk_size=1000 → 0.79
```

두 가지 지표면 충분히 시작할 수 있다.

- **Recall@k** — 정답 문서가 상위 k개 안에 들어왔는가. 검색이 놓치고 있는지 본다.
- **거절률** — 문서에 답이 없는 질문에 대해 "답할 수 없다"고 했는가. 지어내는지 본다.

> ✅ **평가 세트는 한 번 만들면 계속 쓴다**
> 이 세트가 있으면 청킹 값 변경, 임베딩 모델 교체, 리랭킹 추가가 전부 **숫자로 비교되는 실험**이 된다. 없으면 "이게 더 나은 것 같다"는 인상으로 결정하게 되고, 그 인상은 대체로 마지막에 시험해 본 질문 하나에 좌우된다. 만드는 데 반나절, 아끼는 시간은 그 몇 배다.

<br>

---

## 10. 검색을 도구로 노출할 때 — 스키마가 라우팅을 정한다

마지막으로 4절에서 미뤄 둔 지점을 짚는다. 경로를 LLM이 고르게 만들면, **LLM이 무엇을 보고 고르는가**가 문제가 된다. 답은 도구 스키마다.

[LangChain 체인과 RAG](https://it-study-2002.tistory.com/entry/LangChain-%EC%B2%B4%EC%9D%B8%EA%B3%BC-RAG) 편에서 `@tool` 데코레이터의 docstring과 타입 힌트가 그대로 도구 명세가 된다는 것을 봤다. 검색기를 도구로 노출할 때는 그 명세의 품질이 곧 **라우팅 정확도**가 된다.

```python
@tool
def search_policy(query: str) -> str:
    """보험 약관 문서에서 보장 범위, 면책 조항, 지급 절차를 검색한다.

    상품별 보장 내용이나 약관 조항을 확인해야 할 때 사용한다.
    가입 절차나 요금 문의에는 사용하지 않는다.

    Args:
        query: 검색할 내용. 대화의 맥락을 풀어 쓴 완전한 문장으로 넣는다.
               예: "반려묘 실손보험의 슬개골 탈구 보장 여부"
    """
```

세 가지 규칙으로 정리된다.

| 규칙 | 이유 |
|---|---|
| **무엇을 하는지가 아니라 언제 쓰는지를 쓴다** | 모델은 도구를 고르는 중이지 이해하는 중이 아니다 |
| **쓰지 말아야 할 경우를 명시한다** | 도구가 여러 개일 때 경계를 그어 주지 않으면 가장 먼저 나온 것을 고른다 |
| **인자에 예시를 넣는다** | 좋은 검색어의 모양을 보여주면 질의 재작성이 인자 생성 단계에서 함께 일어난다 |

마지막 항목이 특히 유용하다. **인자 설명에 "대화 맥락을 풀어 쓴 완전한 문장"이라고 적어 두면**, 3절의 질의 재작성을 위한 별도 LLM 호출 없이도 상당 부분이 해결된다. 모델이 도구 인자를 만들면서 이미 그 일을 하기 때문이다.

> 💡 **도구가 여러 개면 설명은 서로를 의식해야 한다**
> `search_policy`와 `search_faq`가 둘 다 "문서를 검색한다"고만 적혀 있으면 모델은 사실상 동전을 던진다. 도구 설명은 개별로 쓰는 게 아니라 **묶어 놓고 경계를 정하는 작업**이다. 도구 목록을 나란히 놓고 읽었을 때 "이 질문은 이쪽"이 분명해지는지 확인한다.

<br>

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **파인튜닝 vs RAG** | "어떻게 말하는가"는 파인튜닝, **"무엇을 아는가"는 RAG.** 지식 주입 목적의 파인튜닝은 비싸고 잘 안 된다 |
| **Naive RAG의 천장** | 질문을 그대로 검색 / 검색 한 번 / 키워드에 약함 / 답 검토 없음 — **일직선이라 되돌아갈 수 없다** |
| **질의 재작성** | 구어체·대화 맥락을 문서의 표현으로. LLM 호출이 하나 늘므로 멀티턴 대화형에서 값어치가 있다 |
| **하이브리드 검색** | 벡터는 의미, 키워드는 정확한 문자열. **상품 코드·조항 번호를 다루면 사실상 필수** |
| **리랭킹** | Bi-encoder로 넉넉히(20) → Cross-encoder로 추려서(3). **`k`를 올리려면 리랭킹과 함께 올린다** |
| **Modular RAG** | 질문에 따라 경로를 다르게 조립. **검색을 생략하는 것도 하나의 경로다** |
| **발전 계보** | 각 단계는 앞 단계를 대체하지 않는다. 앞 단계를 제대로 만들어야 부품으로 쓸 수 있다 |
| **청킹 3전략** | Fixed(단순·잘림) / **Recursive(기본값)** / Semantic(의미 보존, 색인 비용↑·크기 불균일) |
| **표·코드** | 세 전략 모두 줄글을 가정한다. 잘리면 양쪽 다 쓸모없어지므로 **따로 뽑아 통째로 한 청크** |
| **두 손잡이** | `chunk_size`는 정밀도↔문맥, `chunk_overlap`은 비용↔경계 보호. 출발점 300~800자에 겹침 10~20% |
| **검색≠전달** | 작은 청크로 검색하고 큰 부모 문서를 전달하면 트레이드오프를 우회할 수 있다 |
| **임베딩 선택** | 벤치마크로 못 고른다. 언어·도메인·차원·**최대 입력 길이**(초과분은 조용히 잘린다)·비용·운영 형태 |
| **모델 일치** | 색인과 질의는 같은 모델로. 다르면 **에러 없이 엉뚱한 결과**가 나온다. 색인에 모델명을 기록해 둔다 |
| **평가 세트** | 실제 질문 20~30 + 기대 정답 문서. **검색 단계만 먼저 잰다** — 생성은 검색의 상한을 못 넘는다 |
| **도구 스키마** | 무엇을 하는지가 아니라 **언제 쓰고 언제 쓰지 않는지.** 인자 예시가 질의 재작성을 대신하기도 한다 |

<br>

## 🔗 참고 자료

- [LangChain 파이썬 레퍼런스 — Text Splitters, Retrievers](https://python.langchain.com)
- [LangChain 공식 문서 — Retrieval](https://docs.langchain.com)
- [Chroma 공식 문서](https://docs.trychroma.com)
- [MTEB: Massive Text Embedding Benchmark](https://huggingface.co/spaces/mteb/leaderboard)
- [Retrieval-Augmented Generation for Large Language Models: A Survey (Gao et al., 2023)](https://arxiv.org/abs/2312.10997)
