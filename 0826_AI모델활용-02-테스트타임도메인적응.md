# 테스트 타임 도메인 적응 (TTA)

> 출처: `5-2_AI모델활용.pdf` 중 **p.42-69 (28페이지)** · 정리일: 2026-08-26
> 페이지 표기는 PDF 물리 페이지 기준 (합본 전체 157페이지에서의 위치).
> 판독 방식: **전 페이지 이미지 직접 판독** (OCR 미사용). 판독 실패한 페이지는 **없음**.
> 원본 오류 9건은 본문에서 고쳐 쓰고 `> 📌` 로 밝혔다.
>
> **`5-2_AI모델활용.pdf` 는 강의 4개의 합본이다.** 시리즈: [01 On-device AI와 정수연산 양자화](0826_AI모델활용-01-온디바이스AI와정수연산양자화.md) · **02 테스트 타임 도메인 적응(TTA)** · [03 적응적 센싱](0826_AI모델활용-03-적응적센싱.md) · [04 도메인 전문지식을 활용한 모델 설계](0826_AI모델활용-04-도메인전문지식모델설계.md)

## 한눈에 보기

- **학습은 잘 됐는데 현장에서 무너지는 이유** — 검증 97%가 테스트 68%로 떨어지는 분포 이동(distribution shift).
- **통계 하나만 바꿔서 68% → 95%** — TTA의 존재 이유가 한 장에 들어 있다.
- **무엇을 갱신하는가로 기법이 갈린다** — TENT는 BN 통계와 파라미터, SAR은 신뢰할 샘플만 골라서, TPT는 텍스트 프롬프트, PromptAlign은 텍스트와 이미지 프롬프트 둘 다.
- **테스트 시점에 라벨 없이 어떻게 학습하는가** — 엔트로피 최소화(확신도를 높이는 방향)라는 대리 목적함수.

## 목차

1. [분포 이동과 테스트 타임 도메인 적응(TTA)](#분포-이동과-테스트-타임-도메인-적응tta) (p.44-50)
2. [CNN 기반 TTA 기법](#cnn-기반-tta-기법) (p.53-54)
3. [비전언어모델 기반 TTA 기법](#비전언어모델-기반-tta-기법) (p.56-57)
4. [TTA 확인문제](#tta-확인문제) (p.59-65)
5. [TTA 강의 정리](#tta-강의-정리) (p.67-69)

---

## 분포 이동과 테스트 타임 도메인 적응(TTA)

여기부터 새 강의 덱이다. 원 제목은 「AI 모델 활용: 테스트 타임 도메인 적응」이며, 목차는 다음과 같다.

```text
1. 분포 이동과 테스트 타임 도메인 적응 (TTA)
2. TTA 기법
   2-1. CNN 기반 TTA 기법
   2-2. 비전언어모델 기반 TTA 기법
```

### 학습 목표와 오프닝 질문 (p.44, p.46)

**학습 목표**

- 분포 이동 문제를 이해하고 모델 성능 하락과 연계합니다.
- 테스트 타임 도메인 적응의 기본 개념을 이해합니다.
- 테스트 타임 도메인 적응의 대표적 기법들의 동작 원리를 이해합니다.

**강의가 먼저 던지는 질문 3개** — 이 셋이 그대로 뒤의 기법 순서가 된다.

1. 테스트 데이터 분포에 적응하는 것은 어떤 어려움이 있을까?
2. 테스트 데이터 레이블이 없는 상황에서 어떤 로스를 통해 도메인 적응을 할 수 있을까?
3. 로스 없이도 테스트 데이터 분포에 적응할 수 있을까?

두 번째 질문의 답이 **엔트로피 손실**(TENT·SAR·TPT), 세 번째 질문의 답이 **BatchNorm 통계 재계산**과 **분포 정렬**(PromptAlign)이다.

### 분포 이동(Distribution Shift)이란 무엇인가 (p.48-49)

**정의**

- AI 모델이 현실에서 동작할 때, 학습 데이터에서 경험하지 못했던 **낯선 데이터**에 맞닥뜨리는 현상
- AI 성능 하락의 주요 원인
- 학습할 때는 깨끗한 데이터로 훈련되었지만, 실제 환경에서는 노이즈 등으로 데이터 분포가 달라져 정확도가 떨어짐

**p.48 그래프 (문자 전사)** — 가로축 하나 위에 봉우리 두 개가 서로 어긋난 채 겹쳐 있다.

```text
        학습 도메인 분포              테스트 도메인 분포
      (선명한 새 이미지)             (흐릿한 새 이미지)
             ╭─╮                        ╭─╮
            ╱   ╲                      ╱   ╲
           ╱     ╲╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╱     ╲
      ────┴───────┴──────────────────┴───────┴────
                  ↑ 두 분포가 어긋난 만큼이 "분포 이동"
```

두 봉우리의 모양은 비슷하지만 **중심이 옆으로 밀려 있다**. 이 어긋남의 폭이 곧 성능 하락 폭이 된다.

**p.49 학습 시점 vs 테스트 시점 (문자 전사)**

```text
[ 학습 시점 ]                          [ 테스트 시점 ]
 선명한 새 이미지 ──val──▶ 모델 → 97%    흐릿한 새 이미지 ──test──▶ 모델 → 68% ↓

 학습 모델                              학습 모델
  ... ─▶ [Batch Norm] ─▶ ...             ... ─▶ [Batch Norm] ─▶ ...
            ▲                                       ▲
   [Source stats/params]                   [Source stats/params]   ← 그대로!
```

핵심은 오른쪽 그림의 화살표다. 입력 데이터는 바뀌었는데 **BatchNorm이 참조하는 통계는 여전히 학습 데이터의 것(Source stats)** 이다. 정규화의 기준점이 틀어졌으니 그 뒤 계층이 아무리 정확해도 답이 어긋난다.

### TTA: 테스트 시점에 모델을 재조정한다 (p.50)

**테스트타임 도메인 적응 (Test-Time Adaption: TTA)**

- AI가 실제 쓰이는 순간, **테스트 데이터의 분포를 파악**하여 모델을 유연하게 적응시킴
- 즉, 테스트 데이터를 입력받을 때 모델의 일부를 새 데이터의 통계에 맞게 재조정하여 성능을 유지하는 기법

> 📌 원본 p.50에는 "Test-Time Adaption"으로 되어 있으나 "Test-Time Adaptation"이 맞다. 같은 덱의 p.59·p.60·p.67에는 "Test-Time Adaptation"으로 올바르게 표기되어 있다.

**p.50 3단 비교 (문자 전사)** — 이 강의 전체에서 가장 중요한 그림이다.

```text
[ 학습 시점 ]              [ 테스트 시점 ]             [ 테스트 시점 + TTA ]
 선명 이미지                흐릿 이미지                 흐릿 이미지
   │ val                      │ test                      │ test after TTA
   ▼                          ▼                           ▼
 97%                        68% ↓                       95% ↑

 학습 모델                   학습 모델                   학습 + 적응된 모델
 ...─▶[Batch Norm]─▶...      ...─▶[Batch Norm]─▶...      ...─▶[Batch Norm]─▶...
          ▲                           ▲                           ▲
 [Source stats/params]       [Source stats/params]       [Target stats/params]
```

바뀐 것은 **BatchNorm이 참조하는 통계 하나뿐**이다. 가중치를 다시 학습하지도, 데이터를 다시 모으지도 않았는데 68% → 95%로 회복한다. TTA가 매력적인 이유가 이 한 장에 다 들어 있다.

> **주의** TTA는 "재학습"이 아니다. 학습을 처음부터 다시 하는 것이 아니라, **테스트 중에 일부 계층(주로 BatchNorm)만** 들어오는 데이터에 맞게 조정한다. 이 구분이 확인문제 1번의 함정이다.

## CNN 기반 TTA 기법

레이블 없는 테스트 데이터로 어떻게 모델을 고치는가. CNN 계열의 답은 **BatchNorm 계층만 건드린다**로 요약된다.

### TENT: 배치 정규화 계층을 테스트 데이터에 최적화 (p.53)

TENT는 BatchNorm 계층 하나에서 **두 가지**를 동시에 바꾼다. 둘을 섞어서 이해하면 안 된다.

**1) 평균 분산 업데이트** — 순전파만으로 즉시

- 테스트 데이터를 입력하면서 BatchNorm의 **평균($\mu$)과 분산($\sigma^2$)을 테스트 데이터 기준으로 재계산**
- 모델이 새로운 데이터 분포에 즉각 적응

**2) 어파인 파라미터 업데이트** — 역전파가 필요

- 테스트 데이터의 **엔트로피(불확실성)를 손실 함수로 설정**
- **역전파**를 통해 $\gamma$(스케일), $\beta$(이동) 값을 미세 조정
- 예측의 확신도를 높이는 방향으로 적응

**BatchNorm 계층 내부 흐름 (p.53 도식 문자 전사)**

```text
IN ──▶ (−) ──▶ (÷) ──▶ (×) ──▶ (+) ──▶ OUT
        ▲       ▲       ▲       ▲
        μ       σ       γ       β
     └── 통계(순전파로 재계산) ──┘ └── 파라미터(역전파로 학습) ──┘
```

앞의 두 연산($-\mu$, $\div\sigma$)은 **데이터에서 바로 얻는 통계**라 레이블이 필요 없고, 뒤의 두 연산($\times\gamma$, $+\beta$)은 **학습되는 파라미터**라 손실 함수가 필요하다. TENT가 엔트로피를 손실로 끌어온 이유가 여기 있다.

**p.53 수식 (원문 그대로)**

$$\text{normalization}\quad \mu \leftarrow \mathbb{E}[x_t],\ \ \sigma^2 \leftarrow \mathbb{E}\big[(\mu - x_t)^2\big]$$

$$\text{transformation}\quad \gamma \leftarrow \gamma + \partial H/\partial\gamma,\ \ \beta \leftarrow \beta + \partial H/\partial\beta$$

> 📌 원본 p.53의 transformation 식은 부호가 슬라이드 본문과 어긋난다. 본문은 "엔트로피를 손실 함수로 설정"하고 "예측의 확신도를 높이는 방향으로 적응"한다고 하므로 엔트로피 $H$를 **최소화**해야 하고, 그렇다면 경사하강 방향인 $\gamma \leftarrow \gamma - \eta\,\partial H/\partial\gamma$, $\beta \leftarrow \beta - \eta\,\partial H/\partial\beta$ 가 맞다. 원논문 그림은 $\gamma \leftarrow \gamma + \nabla\gamma$ 처럼 "갱신량 $\nabla\gamma$"를 통째로 쓰는 표기라 부호 문제가 없는데, 슬라이드가 그 자리에 $\partial H/\partial\gamma$를 그대로 대입하면서 부호가 뒤집혔다.

**결과**

- **학습 재진행 없이**, 테스트 데이터 분포에 맞게 모델을 **즉시 최적화**

### SAR: 최적화를 더 안정적으로 (p.54)

TENT를 그대로 쓰면 무너지는 경우가 있다. SAR은 그 실패 모드를 정면으로 겨냥한다.

- **관찰**: 일부 테스트 데이터가 노이즈가 많거나 이상치라서 큰 gradient를 만들어 모델을 불안정하게 만듦
- **테스트 샘플 선택**: 엔트로피(불확실성)가 **낮은** 테스트 샘플만 선택해 모델 업데이트에 반영
- **안정적 엔트로피 최소화**: 엔트로피 손실이 너무 요동치지 않도록 **평탄화(loss smoothing)**
- **결과**: TENT보다 학습이 안정적이고, 테스트 환경에서도 성능 유지

> **주의** 방향을 헷갈리기 쉽다. SAR이 고르는 것은 **엔트로피가 낮은(= 모델이 이미 확신하는) 샘플**이다. 불확실한 샘플일수록 정보가 많아 보이지만, 레이블이 없는 상황에서 그건 그냥 **틀린 방향으로 크게 움직이는 gradient**다. 확인문제 2번의 오답 B가 정확히 이 지점을 노린다.

TENT와 SAR의 관계는 한 줄로 정리된다. **TENT는 적응의 기본형, SAR은 안정성 강화형.**

## 비전언어모델 기반 TTA 기법

CLIP 같은 비전언어모델은 BatchNorm이 아니라 **프롬프트**를 갖고 있다. 그래서 건드리는 대상이 바뀐다.

### TPT: 증강본의 일관성으로 프롬프트를 튜닝 (p.56)

**TPT (Test-Time Prompt Tuning): 일관성 있는 예측을 돕자!**

- 테스트 이미지마다 여러 **증강**(회전, 밝기 변화 등)을 생성
- 엔트로피가 가장 낮은 증강 이미지를 **6개** 선택
- 6개의 평균 **엔트로피를 로스**로 세팅하고 이를 최소화하도록 **텍스트 프롬프트** 업데이트

**p.56 파이프라인 (문자 전사)**

```text
 prompt p (Learnable Parameters) ─┐
   class: [Cat] [Dog] ... [Bird] ─┴─▶ text encoder ──┐
                                                      ├─▶ 클래스 확률 분포
 a single test image                                  │
   └─▶ augmented views: A₁, A₂, …, A_N ─▶ image encoder ─┘
                    │
                    ▼
        confidence selection
          p_p(y|A₁(X))  ✓ accept
          p_p(y|A_N(X)) ✓ accept
          p_p(y|A₂(X))  ✗ reject   ← 엔트로피 높은 증강본은 버림
                    │
                    ▼  average
              p̃_p(y|X)
                    │
                    ▼
              min_p  H(p̃_p)
                    │
        ┈┈┈┈ Back Propagation ┈┈┈▶ prompt p 로 되돌아감
```

증강본 전부를 쓰지 않고 **골라 쓴다**는 점이 SAR과 같은 발상이다. 다만 SAR은 "샘플을 고르고", TPT는 "같은 샘플의 증강본을 고른다".

여기서 갱신되는 것은 **텍스트 프롬프트뿐**이다. 이미지 인코더도 텍스트 인코더도 전부 얼어 있다.

$$\min_{p} \ H\big(\tilde{p}_{p}\big)$$

### PromptAlign: 학습 통계와 테스트 통계를 직접 정렬 (p.57-58)

TPT의 엔트로피 로스는 "예측이 뾰족해지도록" 밀 뿐, **학습 때 보던 분포로 되돌리라는 신호는 없다**. PromptAlign이 채우는 것이 그 빈칸이다.

**PromptAlign: 학습 데이터와 테스트 데이터를 직접 정렬 시키자!**

- PromptAlign은 학습 데이터 통계까지 활용해 **분포 정렬(distribution alignment)** 을 수행
- 학습 데이터와 테스트 데이터의 **통계적 차이(평균, 분산)** 를 최소화하도록 설계
- 두 데이터의 인코딩 결과를 **계층별(feature layer)로 정렬**
  - 학습 시 얻은 feature 분포와 테스트 feature 분포를 정렬시키는 방식
- 추가적으로 엔트로피 로스를 함께 사용하여 안정적인 예측 유지
- 최종적으로 **텍스트 프롬프트 + 이미지 프롬프트를 모두 업데이트**

**p.57-58 아키텍처 (문자 전사)**

```text
 Labels ─▶ [Word Embed]❄ ─▶ [텍스트 프롬프트🔥 + 토큰] ─▶ F_t ❄ ─┐
                                                                  ├─▶ ⊙ ─▶ p(y|X_test) ─▶ L_entropy
 Test Sample ─▶ 증강본들 ─▶ [Patch Embed]❄ ─▶ [비주얼 프롬프트🔥] ─▶ F_v ❄ ─┘
                                                             │
                                                             ├──▶ L_align
                                                             │        ▲
 Source Data ─────────────────────────────▶ F_v ❄ (오프라인) ─┘

 범례: 🔥 Trainable  /  ❄ Frozen  /  □ Text Prompts  /  ■ Visual Prompts
```

**Distribution Alignment 내부 (p.57 우측 확대 전사)**

```text
 [테스트 경로]  Encoder Layer 1 ─ Encoder Layer 2 ─ … ─ Encoder Layer L
                     │                  │                    │
                μ₁(T,p), σ₁²(T,p)   μ₂(T,p), σ₂²(T,p)   μ_l(T,p), σ_l²(T,p)
                     └──────────────────┴────────────────────┘
                                        ▼
                                   ┃  L_align  ┃
                                        ▲
                     ┌──────────────────┬────────────────────┐
                   μ̂₁, σ̂₁²           μ̂₂, σ̂₂²             μ̂_l, σ̂_l²
                     │                  │                    │
 [소스 경로]   Encoder Layer 1 ─ Encoder Layer 2 ─ … ─ Encoder Layer L
               ( Offline Computed Source Data Statistics )
```

핵심 구조는 **계층마다 (평균, 분산) 한 쌍을 뽑아 위아래로 맞대는 것**이다. 소스 쪽 통계 $\hat{\mu}_l, \hat{\sigma}_l^2$ 는 **오프라인에서 미리 계산해 저장**해 두므로, 테스트 시점에 학습 데이터를 다시 들고 다닐 필요가 없다.

$$\mathcal{L}_{\text{align}} = \sum_{l} \Big( \big\| \mu_l(\mathcal{T}, p) - \hat{\mu}_l \big\| + \big\| \sigma_l^2(\mathcal{T}, p) - \hat{\sigma}_l^2 \big\| \Big)$$

> 📌 위 $\mathcal{L}_{\text{align}}$ 전개식은 슬라이드에 수식으로 적혀 있지 않다. p.57 도식이 각 계층의 $\mu_l, \sigma_l^2$ 를 $\mathcal{L}_{\text{align}}$ 로 모으는 구조와 "통계적 차이(평균, 분산)를 최소화"라는 본문 설명을 그대로 식으로 옮긴 것이므로, 원문 표기가 아님을 밝혀 둔다.

**세 기법의 갱신 대상 비교**

| 기법 | 무엇을 바꾸나 | 손실 | 학습 데이터 필요? |
|---|---|---|---|
| TENT | BatchNorm의 $\mu, \sigma^2, \gamma, \beta$ | 엔트로피 | 불필요 |
| SAR | BatchNorm 파라미터 (샘플 선별 + 평탄화) | 엔트로피 (선별·평탄화) | 불필요 |
| TPT | 텍스트 프롬프트 | 증강본 6개의 평균 엔트로피 | 불필요 |
| PromptAlign | 텍스트 프롬프트 + 이미지 프롬프트 | 정렬 로스 + 엔트로피 로스 | **통계만** 오프라인으로 필요 |

## TTA 확인문제

네 문제 모두 5지선다이며, 문제 슬라이드와 정답 해설 슬라이드가 쌍으로 붙어 있다. 답을 보기 전에 먼저 풀어 볼 것.

> 📌 원본에서는 네 문제가 모두 슬라이드마다 "1"로 번호가 매겨져 있으나(한 슬라이드에 한 문제씩), 노트에서는 구분을 위해 1~4로 다시 번호를 붙였다.

### 문제 1: TTA의 주요 목적 (p.59-60)

**TTA(Test-Time Adaptation)의 주요 목적은 무엇인가?**

- A. 테스트 데이터의 라벨을 예측한 후 모델을 재학습하기 위함이다.
- B. 테스트 데이터의 분포를 파악하여, 모델이 새로운 환경에 적응하도록 하기 위함이다.
- C. 학습 데이터의 평균과 분산을 고정하여 과적합을 방지하기 위함이다.
- D. 테스트 데이터를 이용해 모델의 모든 가중치를 다시 초기화하기 위함이다.
- E. 학습 단계에서 모델의 복잡도를 줄이기 위해 사용된다.

**정답: B**

**해설**

- TTA는 Test-Time Adaptation, 즉 "테스트 시점 적응" 기법이다.
- 모델이 학습 단계에서 본 데이터와 실제 환경의 데이터 분포가 다를 때, 테스트 데이터의 통계(분포)를 반영해 모델을 조정함으로써 성능 저하를 막는 것이 목적이다.
- 학습을 새로 하는 것이 아니라, 테스트 중에 일부 계층(주로 BatchNorm)을 테스트 데이터에 맞게 조정한다.

→ 즉, "테스트 환경 변화에 모델이 유연하게 적응하도록 만드는 기법"이다.

### 문제 2: TENT와 SAR의 차이 (p.61-62)

**다음 중 TENT와 SAR의 주요 차이점으로 옳은 것은?**

- A. TENT는 학습 데이터를 이용해 BatchNorm을 업데이트하고, SAR은 테스트 데이터를 이용한다.
- B. SAR은 불확실성이 높은 샘플을 더 적극적으로 업데이트에 반영한다.
- C. TENT는 테스트 데이터의 엔트로피를 최소화하며, SAR은 그 과정을 더 안정적으로 수행하도록 개선한다.
- D. TENT는 파라미터 업데이트를 하지 않지만, SAR은 전체 네트워크를 다시 학습한다.
- E. 두 기법 모두 테스트 시점에 모델을 변경하지 않는다.

**정답: C**

**해설**

- **TENT(Test-time Entropy Minimization)** 는 테스트 데이터를 입력받을 때, BatchNorm의 평균·분산(통계)과 $\gamma$, $\beta$(파라미터)를 업데이트하여 엔트로피를 최소화하는 방식으로 모델을 적응시킨다. 하지만 이 과정에서 노이즈가 큰 테스트 샘플이 들어오면 모델이 불안정해질 수 있다.
- **SAR(Stable Adaptation Refinement)** 은 엔트로피가 낮은(확실한) 샘플만 선택해 업데이트하고, 엔트로피 변화가 너무 크지 않게 평탄화(loss smoothing)를 적용하여 적응 과정을 더 안정적으로 만드는 기법이다.

→ 따라서 TENT는 적응의 기본형, SAR은 안정성 강화형이라고 할 수 있다.

> 📌 원본 p.62에는 SAR을 "Stable Adaptation Refinement"로 풀어 썼으나, TTA 문헌에서 SAR은 **Sharpness-Aware and Reliable entropy minimization**(Niu et al., ICLR 2023)의 약자다. 슬라이드가 설명하는 "엔트로피 낮은 샘플만 선별(Reliable)"과 "손실 평탄화(Sharpness-Aware)"가 정확히 그 두 단어에 대응한다.

### 문제 3: TPT의 핵심 아이디어 (p.63-64)

**다음 중 TPT(Test-time Prompt Tuning) 기법의 핵심 아이디어로 가장 적절한 것은?**

- A. 테스트 이미지 대신 학습 이미지를 사용해 프롬프트를 미리 튜닝한다.
- B. 테스트 이미지의 여러 증강본을 활용해 예측의 일관성을 높인다.
- C. 이미지 인코더의 통계값을 고정하여 모델을 안정화한다.
- D. 테스트 시점에서 모델 전체 파라미터를 다시 학습한다.
- E. 프롬프트 대신 모델의 가중치를 직접 업데이트한다.

**정답: B**

**해설**

- TPT는 테스트 이미지의 증강본 여러 개를 생성하고, 그중 엔트로피가 가장 낮은 샘플들을 이용해 텍스트 프롬프트를 조정하는 방식으로 예측 일관성을 향상시키는 기법이다.

→ 즉, "증강을 통해 모델이 다양한 시점에서도 같은 답을 하도록 유도"하는 것이 핵심이다.

### 문제 4: PromptAlign의 주요 목표 (p.65-66)

**PromptAlign 기법의 주요 목표는 무엇인가?**

- A. 테스트 이미지에 맞는 새로운 언어 모델을 학습한다.
- B. 프롬프트를 무작위로 변경해 테스트 데이터를 다양화한다.
- C. 학습 데이터와 테스트 데이터의 분포 차이를 줄이기 위해 통계적 정렬을 수행한다.
- D. 테스트 데이터의 레이블을 예측한 뒤 프롬프트를 재훈련한다.
- E. 학습 데이터만을 이용해 테스트 적응 없이 프롬프트를 미리 고정한다.

**정답: C**

**해설**

- PromptAlign은 학습 데이터와 테스트 데이터의 분포 차이를 줄이기 위해 각 계층별 통계(평균, 분산)를 정렬하는 방식으로 프롬프트를 정렬한다.
- 학습 단계에서 저장한 통계와 테스트 단계에서 계산한 통계의 차이를 최소화하여 모델이 두 분포를 자연스럽게 일치시키도록 적응하게 만든다.

→ 라벨이 없어도 학습-테스트 간 분포를 정렬할 수 있는 비지도 TTA 기법이다.

## TTA 강의 정리

덱2가 스스로 요약한 세 장이다. 앞 내용과 겹치지만, 강사가 무엇을 시험에 낼 만한 문장으로 골랐는지가 드러나므로 그대로 남긴다.

### 분포 이동과 TTA 요약 (p.67)

**분포 이동**

- 학습(Training) 데이터와 실제 테스트(Test) 데이터의 분포가 달라지는 현상.
- 예: 밝은 조명에서 학습한 모델이 어두운 조명(테스트 환경)에서는 성능이 급락하는 경우.
- 현실의 데이터는 환경, 장비, 시간대 등에 따라 통계적 특성이 달라지기 때문.
- 문제점: 학습 데이터 기준으로 정규화된 통계가 테스트 환경에서는 맞지 않아, 모델이 잘못된 예측을 하게 됨

**TTA (Test-Time Adaptation)**

- 모델을 다시 학습하지 않고, 테스트 시점에 들어오는 데이터의 분포를 활용해 모델 일부를 즉시 조정하는 기법.
- 평균($\mu$), 분산($\sigma^2$)과 어파인 파라미터 $\gamma$, $\beta$를 테스트 데이터 통계에 맞게 업데이트하여 적응.

### CNN 기반 TTA 요약 (p.68)

**TENT 기법**

- 평균·분산 업데이트 : 테스트 데이터를 입력할 때마다 평균($\mu$)과 분산($\sigma^2$)을 새 데이터 기준으로 계산.
- 어파인 파라미터 업데이트 : 테스트 데이터의 예측 결과 엔트로피를 손실로 설정. 역전파를 통해 $\gamma$, $\beta$를 업데이트 → 모델의 예측이 정확해짐.
- 결과 : 추가 학습 없이, 테스트 환경에 모델이 즉시 적응.

**SAR 기법**

- 신뢰도 있는 샘플만 선택 (Entropy Filtering) : 엔트로피 값이 일정 기준 이하인 '확실한 데이터'만 업데이트에 사용.
- 엔트로피 평탄화 : 엔트로피 손실이 급격히 변하지 않도록 평탄화(loss smoothing) 적용 → 안정적 적응 유지.

### 비전언어모델 기반 TTA 요약 (p.69)

**TPT (Test-time Prompt Tuning)**

- 테스트 이미지를 여러 방식으로 증강 (augmentation)
- 각 증강본을 모델에 통과시켜 예측 수행
- 엔트로피가 낮은 이미지 **6개**만 선택
- 평균 엔트로피를 로스로 두고 텍스트 프롬프트 업데이트

**PromptAlign**

- 학습 단계에서 이미지 인코더 계층별 평균/분산 통계 저장
- 테스트 단계에서 증강본을 통해 동일한 통계 계산
- 두 통계 간 차이를 줄이는 정렬 로스(Alignment Loss) 계산
- 엔트로피 로스와 함께 텍스트·이미지 프롬프트를 업데이트

---

## 정리 체크리스트

- [ ] 분포 이동이 무엇이고, 왜 검증 성능이 테스트에서 안 나오는지 설명할 수 있다
- [ ] TTA가 파인튜닝·도메인 적응과 무엇이 다른지 말할 수 있다
- [ ] TENT가 갱신하는 것이 BN의 어느 부분인지 구분할 수 있다 (통계 vs 파라미터)
- [ ] 엔트로피 최소화가 라벨 없이 학습할 수 있게 해주는 원리를 안다
- [ ] SAR이 TENT에 무엇을 더했는지 두 가지로 말할 수 있다
- [ ] TPT가 증강본을 전부 쓰지 않고 골라 쓰는 이유를 안다
- [ ] PromptAlign이 정렬하는 두 통계가 각각 어디서 오는지 안다
