# 트라이 — 접두사 질의를 O(L)에 답하는 자료구조

> 자기주도 학습 정리 · 정리일: 2026-08-26
> 블로그 발행본: https://it-study-2002.tistory.com/entry/트라이-완전정리

검색창에 `pyth`까지만 쳤는데 `python`, `pythonic`, `pytorch`가 주르륵 떨어지는 화면을 매일 본다. 이걸 딕셔너리로 만들어 보려고 하면 곧바로 막힌다. `"pyth" in words`는 `False`고, 그렇다고 등록된 단어를 전부 훑으면서 `startswith`를 부르면 단어가 많아질수록 느려진다.

문제는 자료구조 선택이다. **해시는 "이 값이 있느냐"에 답하도록 만들어진 물건이라, "이 글자들로 시작하는 값이 있느냐"에는 원리상 답할 수 없다.** 이 질문을 O(글자 수)에 답하려고 만든 것이 트라이다.

이 글은 트라이를 처음 쓰는 사람이 코딩 테스트에서 바로 꺼낼 수 있을 만큼만 정리한 것이다. 구조와 세 가지 기본 연산, 복잡도와 메모리, 그리고 노드에 값을 얹어 응용하는 지점까지 다룬다.

## 📖 이 글에서 다루는 내용

1. 왜 트라이인가 — 해시가 못 하는 한 가지
2. 구조 — 글자는 노드가 아니라 간선에 있다
3. 만들기 — 딕셔너리 중첩과 배열, 두 가지 구현
4. 세 가지 기본 연산
5. 복잡도 — N이 아니라 L에 비례한다
6. 메모리 — 트라이의 진짜 약점
7. 노드에 값을 얹는다
8. 이진 트라이 — 숫자도 문자열이다
9. 문제 유형 판별
10. 흔한 실수 다섯 가지
11. 트라이가 답이 아닐 때
12. 핵심 요약

---

## 1. 왜 트라이인가 — 해시가 못 하는 한 가지

해시 테이블은 키를 정수로 바꿔 배열 인덱스로 삼는다. 이 방식이 빠른 이유는 **키 전체를 한 덩어리로 뭉쳐서** 위치를 계산하기 때문이다. 그런데 바로 그 이유 때문에 접두사를 물을 수 없다.

`hash("python")`과 `hash("pyth")`는 아무 관계가 없는 두 정수다. 앞 네 글자가 같다고 해시값이 비슷해지지 않는다. 오히려 **한 글자만 달라도 완전히 다른 값이 나오도록** 설계된 것이 좋은 해시 함수다.

> **[그림]** 해시는 단어 전체를 하나의 정수로 뭉쳐 저장하므로 pyth로 시작하는 단어를 찾으려면 전체를 훑어야 하지만 트라이는 p-y-t-h 경로를 따라가면 그 아래가 전부 답이 되는 구조 비교

그래서 접두사 질의를 해시로 처리하면 이렇게 된다.

```python
words = {"python", "pythonic", "pytorch", "java", "javascript"}

# "pyth" 로 시작하는 단어가 있는가?
any(w.startswith("pyth") for w in words)   # 등록된 단어 전체를 훑는다
```

단어가 N개고 평균 길이가 L이면 **O(N·L)** 이다. 단어가 10만 개면 질의 한 번에 10만 번을 훑는다.

트라이는 질문을 뒤집는다. 단어를 통째로 저장하는 대신 **글자 하나씩 갈라지는 길**로 저장해 두면, `pyth`를 찾는 일은 `p → y → t → h` 네 칸을 걸어가는 것이 된다. 등록된 단어가 10만 개든 100만 개든 네 걸음이다.

| | 완전 일치 검색 | 접두사 검색 | 사전순 순회 |
|---|---|---|---|
| **해시 (`set`, `dict`)** | 평균 O(L) | **불가 — 전체 스캔 O(N·L)** | 불가 |
| **트라이** | O(L) | **O(L)** | 가능 (DFS로) |

> ➡️ **트라이를 꺼낼 신호는 하나다 — 문제가 "~로 시작하는"을 묻는가.**
> 완전 일치만 물으면 `set`이 더 빠르고 더 가볍다. 트라이를 쓸 이유는 접두사다.

---

## 2. 구조 — 글자는 노드가 아니라 간선에 있다

여기서 처음 배우는 사람이 가장 많이 헷갈린다. 그림으로 그릴 때는 노드 안에 글자를 써 넣지만, **개념상 글자는 간선(edge)에 붙어 있다.** 노드는 "여기까지 온 상태"를 뜻할 뿐이다.

> **[그림]** 루트에서 시작해 간선마다 글자가 하나씩 붙고 루트부터 어떤 노드까지의 경로가 곧 하나의 접두사가 되는 트라이 구조와 단어의 끝을 표시하는 is_end 플래그

정리하면 세 가지다.

- **루트는 빈 문자열이다.** 아무 글자도 읽지 않은 상태.
- **간선 하나가 글자 하나다.** 루트에서 어떤 노드까지의 경로를 이어 붙이면 그게 그 노드가 나타내는 문자열이다.
- **공통 접두사는 경로를 공유한다.** `cat`과 `car`는 `c → a`까지 같은 길을 쓰고 마지막에서만 갈라진다. 트라이가 메모리를 아끼는 유일한 지점이자, 접두사 질의가 빨라지는 이유다.

### 단어의 끝을 따로 표시해야 하는 이유

`app`과 `apple`을 둘 다 등록했다고 하자. 경로만 있으면 **`app`이 등록된 단어인지, 아니면 그냥 `apple`로 가는 길목인지 구분할 수가 없다.**

> **[그림]** app과 apple을 모두 넣었을 때 경로만으로는 app이 등록된 단어인지 지나가는 길목인지 알 수 없으므로 단어가 끝나는 노드에 is_end 플래그를 세워야 한다는 것을 보여주는 다이어그램

그래서 모든 트라이 구현에는 **단어가 끝나는 노드를 표시하는 플래그**가 있다. 보통 `is_end`, `is_word`, `end` 같은 이름을 쓴다.

```python
trie = {}
for word in ["app", "apple"]:
    node = trie
    for ch in word:
        node = node.setdefault(ch, {})

# is_end 가 없으면
"app" 이 등록됐는지 알 수 없다   # 경로는 있지만 그게 단어의 끝인지 모른다
```

> ⚠️ **`is_end`를 빼먹으면 예제는 통과하고 채점에서 틀린다**
> 등록된 단어가 서로 접두사 관계가 아니면 (`cat`, `dog`, `bird`) 없어도 답이 맞다. `app`과 `apple`처럼 한쪽이 다른 쪽의 접두사일 때만 틀린다. 예제에 그런 쌍이 없으면 통과하고 넘어가게 된다.

---

## 3. 만들기 — 딕셔너리 중첩과 배열, 두 가지 구현

트라이 구현은 크게 두 갈래다. 코딩 테스트에서는 첫 번째로 충분한 경우가 대부분이다.

### 방식 A — 중첩 딕셔너리 (짧고 빠르게)

노드를 딕셔너리 하나로 본다. 키가 글자, 값이 자식 노드다.

```python
END = "$"          # 단어의 끝을 나타내는 특별한 키

def build(words):
    root = {}
    for word in words:
        node = root
        for ch in word:
            node = node.setdefault(ch, {})   # 없으면 {} 를 만들고, 있으면 그걸 반환
        node[END] = True                     # 여기서 단어가 끝난다
    return root

root = build(["cat", "car", "card", "dog"])
```

`setdefault`가 이 구현의 핵심이다. "키가 없으면 기본값을 넣고, 어느 쪽이든 그 값을 돌려준다"를 한 줄로 처리한다.

**장점**은 짧다는 것과, 클래스를 안 만들어도 된다는 것이다. **단점**은 `END` 키가 글자와 같은 공간에 섞여 있다는 점인데, 실제로 나올 수 없는 문자(`$`, `*`, `#`)를 고르면 문제가 없다.

> ⚠️ **`END` 로 쓸 문자를 입력에 나올 수 있는 것으로 고르면 안 된다**
> 입력이 소문자 알파벳뿐이면 `$`든 `*`든 상관없다. 하지만 임의의 문자열이 들어올 수 있다면 문자열 대신 `None`이나 정수 `0` 같은 **타입이 다른 키**를 쓰는 편이 안전하다. 파이썬 딕셔너리는 키 타입을 안 가린다.

### 방식 B — 클래스 + 배열 (알파벳이 고정일 때)

알파벳이 소문자 26개로 고정이라면 자식을 길이 26짜리 리스트로 잡을 수 있다.

```python
class Node:
    __slots__ = ("child", "is_end")
    def __init__(self):
        self.child = [None] * 26
        self.is_end = False

class Trie:
    def __init__(self):
        self.root = Node()

    def insert(self, word):
        node = self.root
        for ch in word:
            i = ord(ch) - ord("a")
            if node.child[i] is None:
                node.child[i] = Node()
            node = node.child[i]
        node.is_end = True
```

배열 방식은 **자식을 사전순으로 순회하기 쉽다**는 장점이 있다. `for i in range(26)` 이 곧 사전순이다. 딕셔너리 방식으로 사전순 순회를 하려면 매번 `sorted(node)`를 불러야 한다.

대신 **글자 하나짜리 노드도 26칸을 차지한다.** 6절에서 다시 본다.

| | 중첩 딕셔너리 | 클래스 + 배열 |
|---|---|---|
| 코드 길이 | 짧다 | 길다 |
| 메모리 | 있는 자식만 저장 | 노드마다 26칸 고정 |
| 사전순 순회 | `sorted()` 필요 | 인덱스 순서가 곧 사전순 |
| 문자 종류 | 아무거나 | 미리 정해진 집합만 |
| 코딩 테스트 | **기본값** | 사전순이 필요하거나 노드가 매우 많을 때 |

---

## 4. 세 가지 기본 연산

트라이가 하는 일은 결국 셋이다. 셋 다 **루트에서 글자를 따라 내려간다**는 점이 같고, 마지막에 무엇을 확인하느냐만 다르다.

> **[그림]** insert는 없는 길을 만들며 내려가고 search는 길을 따라간 뒤 is_end를 확인하며 startswith는 길이 있는지만 보고 끝난다는 세 연산의 차이

### insert — 없으면 만들며 내려간다

```python
def insert(root, word):
    node = root
    for ch in word:
        node = node.setdefault(ch, {})
    node[END] = True
```

`cat`을 넣은 뒤 `car`를 넣으면 `c → a`까지는 이미 있는 길을 그대로 쓰고 `r`만 새로 만든다. **공통 접두사가 길수록 새로 만드는 노드가 적다.**

### search — 끝까지 갔는데 그게 단어인가

```python
def search(root, word):
    node = root
    for ch in word:
        if ch not in node:
            return False
        node = node[ch]
    return END in node          # 길은 있었다. 그게 단어의 끝이었나?
```

### startswith — 길이 있기만 하면 된다

```python
def starts_with(root, prefix):
    node = root
    for ch in prefix:
        if ch not in node:
            return False
        node = node[ch]
    return True                 # 여기까지 왔으면 그걸로 끝
```

> **[그림]** search와 startswith는 내려가는 과정이 완전히 같고 마지막 한 줄만 다르다는 비교

두 함수의 차이는 **마지막 `return` 한 줄뿐이다.** `search`는 `END`를 확인하고, `starts_with`는 확인하지 않는다. 이 한 줄이 "등록된 단어인가"와 "이걸로 시작하는 단어가 있는가"를 가른다.

### 삭제는 왜 잘 안 나오는가

트라이 삭제는 **다른 단어가 그 길을 쓰고 있는지 확인하며 거슬러 올라가야** 해서 구현이 번거롭다. 코딩 테스트에서 삭제를 요구하는 트라이 문제는 드물고, 나오더라도 `is_end`를 `False`로 바꾸는 **논리적 삭제**로 충분한 경우가 대부분이다. 노드까지 실제로 지우려면 자식이 하나도 없고 `is_end`도 꺼진 노드만 지우면서 위로 올라간다.

---

## 5. 복잡도 — N이 아니라 L에 비례한다

트라이의 세 연산은 전부 **O(L)** 이다. L은 다루는 문자열의 길이다. **등록된 단어 수 N은 복잡도에 등장하지 않는다.**

> **[그림]** 등록된 단어가 늘어나도 검색 비용은 그대로이고 오직 문자열 길이에만 비례한다는 것을 해시 및 전체 스캔과 비교한 그래프

| 연산 | 트라이 | 해시(`set`) | 정렬 리스트 + 이분탐색 |
|---|---|---|---|
| 삽입 | O(L) | 평균 O(L) | O(N) — 중간 삽입이 밀린다 |
| 완전 일치 검색 | O(L) | 평균 O(L) | O(L·log N) |
| **접두사 존재 확인** | **O(L)** | O(N·L) | O(L·log N) |
| 접두사로 시작하는 단어 전부 | O(L + 답의 총 길이) | O(N·L) | O(L·log N + 답) |
| 사전순 k번째 | O(L·문자종류) | 불가 | O(1) |

여기서 두 가지를 짚어야 한다.

**첫째, 해시의 O(L)과 트라이의 O(L)은 상수가 다르다.** 해시는 문자열 전체를 한 번 훑어 해시값을 만들고(O(L)) 배열 한 칸에 접근한다. 트라이는 L번 반복하면서 매번 딕셔너리를 조회한다. **파이썬에서는 반복문 자체가 비싸서 완전 일치만 놓고 보면 `set`이 트라이보다 대체로 빠르다.**

**둘째, 그래서 트라이의 값어치는 속도가 아니라 "할 수 있느냐"에 있다.** 해시로는 아예 못 하는 질의를 O(L)에 처리한다는 것이 요점이다.

> 💡 **파이썬 문자열은 해시값을 캐싱한다**
> 같은 문자열 객체를 반복해서 키로 쓰면 두 번째 조회부터는 해시 계산이 사라진다. 완전 일치 질의만 있는 문제에서 `set`이 유리한 이유가 하나 더 있는 셈이다.

---

## 6. 메모리 — 트라이의 진짜 약점

시간은 좋은데 공간이 문제다. **노드 수는 최악의 경우 등록된 모든 문자열의 길이 합만큼 생긴다.**

> **[그림]** 공통 접두사가 없는 단어를 넣으면 노드 수가 문자열 길이 합만큼 늘어나고 배열 방식은 노드마다 26칸을 차지해 실제 쓰는 칸보다 훨씬 많은 공간을 잡는다는 비교

길이 6짜리 단어 7만 개를 공통 접두사 없이 넣으면 노드가 최대 42만 개다. 여기에 구현 방식이 곱해진다.

| 구현 | 노드 하나의 크기 | 42만 노드 |
|---|---|---|
| 배열 26칸 (파이썬 `list`) | 리스트 객체 + 포인터 26개 ≈ 250바이트 | 100MB를 넘긴다 |
| 배열 26칸 (`array('i')` 평탄화) | 정수 26개 ≈ 104바이트 | 40MB대 |
| 중첩 딕셔너리 | 빈 dict ≈ 64바이트 + 자식마다 항목 | 자식 수에 비례 |

**공통 접두사가 많으면 이 숫자가 확 줄어든다.** 사전에서 뽑은 단어처럼 앞부분이 겹치는 데이터는 노드 수가 길이 합의 절반 이하로 떨어지는 경우가 흔하다. 반대로 무작위 문자열은 거의 안 겹쳐서 최악에 가깝다.

> ⚠️ **메모리 제한이 빡빡하면 배열 26칸 방식을 먼저 의심한다**
> 파이썬에서 `[None] * 26` 짜리 리스트를 노드마다 만들면 실제로 쓰는 자식이 한둘이어도 26칸 값을 다 지불한다. 자식이 적은 트라이라면 딕셔너리가 훨씬 가볍다. **자식이 적은지 많은지는 데이터에 달렸으니, 애매하면 둘 다 만들어서 재보는 편이 확실하다.**

### 노드를 줄이는 방법

자식이 하나뿐인 노드가 길게 이어지면 그 구간을 한 노드에 문자열로 눌러 담을 수 있다. 이걸 **압축 트라이(Radix Tree, Patricia Trie)** 라고 한다. 노드 수가 크게 줄지만 삽입·분할 구현이 훨씬 복잡해져서, 코딩 테스트에서 직접 짤 일은 거의 없다. 이름만 알아 두면 된다.

---

## 7. 노드에 값을 얹는다

여기서부터가 트라이의 응용이다. **트라이 노드는 곧 접두사 하나다.** 그러니 노드에 값을 하나 얹어 두면 "이 접두사에 대한 무언가"를 O(L)에 조회할 수 있게 된다.

> **[그림]** 트라이 노드에 카운트를 얹으면 접두사로 시작하는 단어 개수를 즉시 알 수 있고 대표값을 얹으면 그 접두사 아래 최적 후보를 즉시 알 수 있다는 구조

### 카운트 — 이 접두사로 시작하는 단어가 몇 개인가

삽입할 때 지나가는 노드마다 1씩 더한다.

```python
def insert(root, word):
    node = root
    for ch in word:
        node = node.setdefault(ch, {})
        node["#cnt"] = node.get("#cnt", 0) + 1   # 지나가는 길목마다 센다
    node[END] = True

def count_prefix(root, prefix):
    node = root
    for ch in prefix:
        if ch not in node:
            return 0
        node = node[ch]
    return node.get("#cnt", 0)
```

전화번호부 접두어 문제, 자동완성 후보 개수 같은 유형이 이 한 줄로 끝난다.

### 대표값 — 이 접두사 아래에서 가장 좋은 것

노드마다 "그 아래 단어들 중 우선순위가 가장 높은 것"을 들고 있게 하면, 접두사를 입력받아 후보를 즉시 꺼낼 수 있다. **삽입할 때 지나가는 노드마다 비교해서 갱신**하면 되므로 추가 비용은 없다.

```python
# 노드마다 (-중요도, 등록순서, 단어) 튜플의 최솟값을 유지한다
cand = (-importance, order, word)
node = root
for ch in word:
    node = node.setdefault(ch, {})
    cur = node.get("#best")
    if cur is None or cand < cur:
        node["#best"] = cand
```

중요도를 음수로 뒤집어 둔 것이 요령이다. **"중요도 최대, 같으면 먼저 등록된 것"이라는 2단계 우선순위가 튜플 최솟값 하나로 정리된다.** 비교 함수를 따로 짤 필요가 없다.

> 💡 **문자열 길이에 상한이 있으면 이 갱신 비용이 상수가 된다**
> 단어 길이가 6 이하라고 문제가 보장하면, 단어 하나를 넣을 때 갱신되는 노드는 최대 6개다. 등록이 아무리 많아도 삽입 한 번에 6번이다. **제약 조건에 길이 상한이 적혀 있으면 이 기법을 의심해 볼 만하다.**

### 서브트리 집계 — 사전순 k번째 찾기

노드마다 "그 아래에 있는 단어 수"를 들고 있으면, 루트에서 자식을 사전순으로 훑으며 카운트를 빼는 방식으로 **사전순 k번째 단어**를 O(L·문자종류)에 찾을 수 있다. 정렬된 리스트에서 인덱스로 꺼내는 것과 같은 일을, 삽입이 O(L)인 구조에서 해내는 것이다.

---

## 8. 이진 트라이 — 숫자도 문자열이다

트라이는 문자열 전용이 아니다. **정수를 이진수로 펼치면 0과 1 두 글자로 된 고정 길이 문자열이다.** 자식이 둘뿐인 트라이가 되고, 이걸 이진 트라이라고 부른다.

> **[그림]** 정수를 상위 비트부터 이진수로 펼쳐 자식이 둘뿐인 트라이에 넣고 XOR 최댓값을 구할 때 각 비트에서 반대쪽 자식을 우선 선택해 내려가는 과정

대표 유형은 **XOR 최댓값**이다. 배열에서 두 수를 골라 XOR한 값의 최댓값을 구하는 문제인데, 전부 짝지어 보면 O(N²)이라 터진다.

핵심 관찰은 이렇다. XOR은 **비트가 서로 다를 때 1**이 된다. 그리고 상위 비트 하나가 하위 비트 전부보다 값이 크다. 그러니 **상위 비트부터 내려가면서 매번 "나와 반대인 쪽"으로 갈 수 있으면 가는 것**이 최선이다.

```python
BITS = 30                       # 값의 상한에 맞춰 정한다 (10억이면 30비트)

def insert(root, x):
    node = root
    for b in range(BITS - 1, -1, -1):      # 상위 비트부터
        bit = (x >> b) & 1
        node = node.setdefault(bit, {})

def max_xor(root, x):
    node, res = root, 0
    for b in range(BITS - 1, -1, -1):
        bit = (x >> b) & 1
        want = 1 - bit                     # 반대쪽으로 가야 그 자리가 1이 된다
        if want in node:
            res |= (1 << b)
            node = node[want]
        else:
            node = node[bit]               # 반대쪽이 없으면 어쩔 수 없이 같은 쪽
    return res
```

N개를 전부 넣고 각각에 대해 `max_xor`를 부르면 **O(N·30)** 이다. O(N²)이 O(N)으로 내려온다.

> ⚠️ **`BITS`를 값의 상한보다 작게 잡으면 조용히 틀린다**
> 상위 비트가 잘려나가도 예외가 나지 않고 그냥 답이 작아진다. 값의 최댓값이 10⁹이면 2³⁰ ≈ 1.07×10⁹ 이므로 30비트가 필요하다. **문제의 값 범위를 보고 정하되, 애매하면 한 비트 넉넉하게 잡는다.**

---

## 9. 문제 유형 판별

지문에서 아래 표현이 보이면 트라이를 후보에 올린다.

| 지문의 신호 | 트라이에서 하는 일 |
|---|---|
| "~로 시작하는", "접두어", "prefix" | 경로를 따라 내려가면 끝 |
| "자동완성", "검색어 추천" | 접두사 노드 아래를 DFS로 수집 |
| "어떤 단어가 다른 단어의 접두어인가" | 삽입 중에 `is_end`를 만나거나, 끝난 노드에 자식이 있으면 참 |
| "사전순으로 k번째" | 노드마다 서브트리 단어 수를 유지 |
| "두 수의 XOR 최댓값" | 이진 트라이 |
| "문자열 집합에서 여러 패턴을 한 번에 찾기" | 아호-코라식 (트라이 + 실패 링크) |

### 전화번호 목록 유형 — 한쪽이 다른 쪽의 접두어인가

이 유형은 트라이 입문 문제로 가장 자주 나온다. 두 가지 경우를 모두 잡아야 한다.

```python
def has_prefix_pair(words):
    root = {}
    for word in words:
        node = root
        for ch in word:
            if END in node:          # ① 이미 끝난 단어 위를 지나간다
                return True
            node = node.setdefault(ch, {})
        if node:                     # ② 내가 끝난 자리에 이미 자식이 있다
            return True
        node[END] = True
    return False
```

①은 짧은 단어를 먼저 넣은 경우, ②는 긴 단어를 먼저 넣은 경우다. **한쪽만 검사하면 입력 순서에 따라 틀린다.** 정렬해서 넣으면 ①만으로 되지만, 그러면 정렬 비용이 붙는다.

> 💡 **트라이를 안 쓰고 푸는 방법도 있다**
> 정렬한 뒤 이웃한 두 문자열만 `startswith`로 비교하면 된다. 사전순으로 붙어 있는 두 문자열이 접두사 관계가 아니면 다른 어떤 쌍도 아니기 때문이다. **N log N에 끝나고 코드가 세 줄이다.** 트라이가 유일한 답인 문제와 트라이로도 되는 문제는 구분해서 보는 편이 좋다.

---

## 10. 흔한 실수 다섯 가지

**하나. `is_end`를 빼먹는다.** 3절에서 본 그대로다. 등록된 단어끼리 접두사 관계가 없으면 통과하고, 있으면 틀린다.

**둘. `END` 키가 글자와 충돌한다.** 중첩 딕셔너리 방식에서 `END`를 `"e"` 같은 실제 글자로 두면 `"e"`로 시작하는 단어가 들어오는 순간 뒤엉킨다. 입력에 나올 수 없는 문자를 쓰거나 타입이 다른 키를 쓴다.

**셋. `#cnt` 같은 부가 키를 자식 순회에 섞는다.** 딕셔너리 방식에서 노드를 순회할 때 `for ch in node:` 하면 `END`와 `#cnt`까지 글자로 딸려 나온다.

```python
for ch in node:                              # END, #cnt 까지 나온다
for ch in node:
    if ch in (END, "#cnt"): continue         # 매번 걸러야 한다
```

부가 정보가 늘어날수록 이 필터가 지저분해진다. 세 개 이상 얹을 거면 클래스 방식으로 넘어가는 편이 낫다.

**넷. 재귀로 DFS를 돌다 깊이 제한에 걸린다.** 파이썬 기본 재귀 한도는 1000이다. 문자열이 그보다 길면 접두사 수집 DFS에서 터진다. 길이 상한을 확인하고, 넘으면 스택으로 바꾸거나 `sys.setrecursionlimit`을 올린다.

**다섯. 루트를 재사용한다.** 테스트 케이스가 여러 개인 문제에서 트라이를 새로 만들지 않으면 앞 케이스의 단어가 그대로 남아 있다. **케이스마다 `root = {}` 로 다시 시작한다.**

---

## 11. 트라이가 답이 아닐 때

트라이가 잘 맞지 않는 상황도 분명하다. 셋 중 무엇을 꺼낼지는 **문제가 무엇을 묻느냐**로 갈린다.

> **[그림]** 완전 일치만 물으면 해시, 무엇으로 시작하는지를 물으면 트라이, 사전순 k번째를 물으면 정렬 리스트와 이분탐색을 고르는 판단 기준 비교

| 상황 | 왜 안 맞나 | 대신 |
|---|---|---|
| 완전 일치만 묻는다 | 해시가 더 빠르고 더 가볍다 | `set`, `dict` |
| 문자열이 아주 길고 개수는 적다 | 노드 수가 길이 합만큼 생긴다 | 해시, 이분탐색 |
| 접미사나 중간 부분 문자열을 찾는다 | 트라이는 앞에서만 갈라진다 | 접미사 배열, KMP, Z 알고리즘 |
| 메모리 제한이 빡빡하다 | 노드 하나당 오버헤드가 크다 | 정렬 + 이분탐색 |
| 문자 종류가 매우 많다 (유니코드 전체) | 배열 방식이 불가능하고 딕셔너리도 무겁다 | 해시 |

**"접미사"가 나오면 트라이가 아니다.** 트라이는 앞에서부터 갈라지는 구조라 뒤에서 시작하는 질의에는 쓸 수 없다. 문자열을 뒤집어 넣으면 접미사 질의가 접두사 질의가 되지만, 그건 문제가 접미사 하나만 물을 때 통하는 우회다.

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **꺼낼 신호** | 지문에 "~로 시작하는"이 있으면 트라이. 완전 일치만이면 `set` |
| **구조** | 글자는 간선에, 노드는 "여기까지 온 상태". 루트는 빈 문자열 |
| **`is_end`** | 필수다. 없으면 `app`과 `apple` 같은 접두사 쌍에서만 틀린다 |
| **구현 선택** | 기본은 중첩 딕셔너리. 사전순 순회가 필요하면 배열 26칸 |
| **`setdefault`** | "없으면 만들고 어느 쪽이든 반환" — 삽입 한 줄의 핵심 |
| **복잡도** | 세 연산 전부 O(L). 등록된 단어 수 N은 안 나온다 |
| **search vs startswith** | 내려가는 과정은 같고 마지막 `return` 한 줄만 다르다 |
| **속도의 진실** | 완전 일치만 보면 `set`이 더 빠르다. 트라이의 값어치는 "할 수 있느냐" |
| **메모리** | 노드 수 ≈ 문자열 길이 합. 배열 26칸은 안 쓰는 칸까지 지불한다 |
| **노드에 얹기** | 카운트 → 접두사 개수, 대표값 → 최적 후보, 서브트리 수 → 사전순 k번째 |
| **길이 상한** | 문제가 길이 상한을 주면 노드 갱신이 상수가 된다. 기법을 의심할 신호 |
| **이진 트라이** | 정수를 상위 비트부터 펼치면 자식 둘짜리 트라이. XOR 최댓값이 O(N·비트) |
| **`BITS`** | 값 상한보다 작게 잡으면 예외 없이 답만 작아진다 |
| **접두사 쌍 판정** | 짧은 게 먼저인 경우와 긴 게 먼저인 경우 **둘 다** 검사한다 |
| **케이스 초기화** | 테스트 케이스마다 루트를 새로 만든다 |
| **안 쓸 때** | 완전 일치만 · 접미사 질의 · 메모리 제한 · 문자 종류가 매우 많을 때 |

## 🔗 참고 자료

- [파이썬 공식 문서 — dict.setdefault](https://docs.python.org/ko/3/library/stdtypes.html#dict.setdefault)
- [파이썬 공식 문서 — sys.setrecursionlimit](https://docs.python.org/ko/3/library/sys.html#sys.setrecursionlimit)
- [Wikipedia — Trie](https://en.wikipedia.org/wiki/Trie)
- [Wikipedia — Radix tree (압축 트라이)](https://en.wikipedia.org/wiki/Radix_tree)
- [Wikipedia — Aho-Corasick algorithm](https://en.wikipedia.org/wiki/Aho%E2%80%93Corasick_algorithm)
