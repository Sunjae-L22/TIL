# 정렬: 불변식, 안정성, 파이썬 구현 비용

> 강의일: 2026-09-16 · 강의 자료를 바탕으로 개념과 예제를 재구성한 학습 노트
> 확인 범위: `15_정렬.pdf` 물리 1–65쪽 전체. 한국어·영어 OCR로 읽고 주요 코드와 표를 이미지 대조했다.

정렬 알고리즘을 구분하는 좋은 질문은 **한 단계가 끝나면 어느 부분이 확정되는가**다. 버블은 뒤쪽 최댓값, 선택은 앞쪽 최솟값을 확정한다. 삽입은 정렬된 접두 구간을 넓힌다. 병합은 정렬된 두 구간을 합치고, 퀵은 피벗을 기준으로 원소를 분할한다.

## 1. 시간만큼 중요한 비교 기준

안정 정렬은 정렬 키가 같은 원소의 **기존 상대 순서**를 보존한다. 제자리 정렬은 보조 저장 공간을 적게 쓰는 성질이다. 둘은 별개다. 비교 횟수뿐 아니라 이동, 슬라이싱, 리스트 앞쪽 삭제, 재귀 스택까지 구현 비용에 포함해야 한다.

| 알고리즘 | 최선 | 평균 | 최악 | 추가 공간 | 안정성: 아래 구현 기준 |
|---|---|---|---|---|---|
| 버블, 조기 종료 있음 | O(n) | O(n²) | O(n²) | O(1) | 안정 |
| 삽입 | O(n) | O(n²) | O(n²) | O(1) | 안정 |
| 선택 | O(n²) | O(n²) | O(n²) | O(1) | 불안정 |
| 카운팅 | O(n+K) | O(n+K) | O(n+K) | O(n+K) | 안정 배치 가능 |
| 병합 | O(n log n) | O(n log n) | O(n log n) | O(n) | 동률이면 왼쪽 우선 |
| 퀵, 첫 원소 피벗 | O(n log n) | O(n log n) | O(n²) | 평균 O(log n), 최악 O(n) 스택 | 불안정 |

K는 키 범위의 크기다. 퀵의 평균은 입력 순서 등에 대한 가정을 전제로 한다. 강의의 조기 종료 없는 버블 구현은 이미 정렬된 입력에서도 O(n²)다. 표의 버블은 아래처럼 종료 조건을 추가한 버전이다. [강의: 6–64쪽, 구현 비용 보완]

## 2. 버블: 인접 교환으로 최댓값을 뒤로

왼쪽부터 이웃한 두 원소를 비교한다. 큰 값을 오른쪽으로 보내며 한 번 순회하면 미정렬 구간의 최댓값이 끝에 도착한다. 다음 순회는 그 마지막 칸을 제외한다. [강의: 6–11쪽]

```python
def bubble_sort(a):
    for end in range(len(a) - 1, 0, -1):
        changed = False
        for j in range(end):
            if a[j] > a[j + 1]:
                a[j], a[j + 1] = a[j + 1], a[j]
                changed = True
        if not changed:
            break
```

같은 값끼리는 교환하지 않아 안정적이다. 교환이 한 번도 없으면 모든 인접 쌍이 순서대로이므로 종료할 수 있다. 이 최적화가 이미 정렬된 입력의 최선을 O(n)으로 만든다.

## 3. 삽입: 정렬된 앞부분에 한 값을 끼워 넣기

반복 시작 시 `a[:i]`가 정렬되어 있다는 불변식을 유지한다. `a[i]`를 잠시 보관하고 더 큰 값들을 오른쪽으로 밀어 빈자리에 넣는다. [강의: 14–23쪽]

```python
def insertion_sort(a):
    for i in range(1, len(a)):
        value = a[i]
        j = i - 1
        while j >= 0 and a[j] > value:
            a[j + 1] = a[j]
            j -= 1
        a[j + 1] = value
```

정렬된 입력에서는 각 원소가 바로 제자리를 찾는다. 역순이면 앞부분을 거의 전부 밀어 O(n²)이다. 비교를 `>=`로 바꾸면 같은 키의 기존 원소까지 밀어 안정성이 깨질 수 있다.

## 4. 선택: 최소 원소의 위치를 찾은 뒤 한 번 교환

미정렬 구간 전체에서 최솟값을 찾아 그 구간의 첫 칸으로 옮긴다. 교환 횟수는 O(n)이지만 최솟값을 찾는 비교는 총 O(n²)이다. [강의: 39–45쪽]

```python
def selection_sort(a):
    for i in range(len(a) - 1):
        smallest = i
        for j in range(i + 1, len(a)):
            if a[j] < a[smallest]:
                smallest = j
        a[i], a[smallest] = a[smallest], a[i]
```

키만 비교한다고 할 때 `[2A, 2B, 1C]`에서 첫 교환 결과는 `[1C, 2B, 2A]`다. 같은 키 2의 순서가 뒤집히므로 일반적인 교환 기반 선택 정렬은 불안정하다.

## 5. 카운팅: 값의 범위를 인덱스로 사용

키가 0부터 max_value 사이의 정수일 때 각 값의 빈도를 센다. 누적합 `count[v]`는 v 이하 원소 수이므로, v를 둘 마지막 위치의 다음 칸을 알려 준다. 뒤에서부터 입력을 읽어 `count[v]`를 하나 줄이고 그 위치에 둔다. [강의: 25–36쪽]

```python
def counting_sort(values, max_value):
    if max_value < 0 or any(v < 0 or v > max_value for v in values):
        raise ValueError("keys must be within 0..max_value")
    counts = [0] * (max_value + 1)
    for v in values:
        counts[v] += 1
    for v in range(1, len(counts)):
        counts[v] += counts[v - 1]
    result = [0] * len(values)
    for v in reversed(values):
        counts[v] -= 1
        result[counts[v]] = v
    return result

assert counting_sort([2, 0, 2, 1], 2) == [0, 1, 2, 2]
```

| 단계 | 0 | 1 | 2 |
|---|---:|---:|---:|
| 빈도 | 1 | 1 | 2 |
| 누적 개수 | 1 | 2 | 4 |

뒤에서 먼저 본 같은 키를 뒤쪽 칸에 넣으므로, 레코드 전체를 함께 배치하는 형태로 확장하면 안정성을 보존한다. 정수 값만 반환하는 위 예제에서는 동일 값의 신원을 볼 수 없다는 점도 구분한다.

정수가 몇 개 없더라도 최댓값이 수십억이면 카운트 배열이 비현실적으로 커진다. 음수까지 받으려면 최솟값만큼 오프셋을 두고, K를 `최댓값-최솟값+1`로 계산한다. 비교 정렬의 하한과 모순되지 않는 이유는 키 범위에 대한 추가 조건을 이용하기 때문이다.

## 6. 병합: 정렬된 두 구간의 앞을 비교

분할은 가운데에서 하고, 병합에서 정렬을 수행한다. 두 구간이 각각 정렬되어 있다면 맨 앞 후보 중 작은 것이 전체에서 다음 원소다. 포인터를 앞으로만 이동하면 한 병합에 O(n)이 걸린다. [강의: 48–51쪽]

```python
def merge_sort(values):
    if len(values) <= 1:
        return list(values)
    mid = len(values) // 2
    left = merge_sort(values[:mid])
    right = merge_sort(values[mid:])
    i = j = 0
    result = []
    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            result.append(left[i])
            i += 1
        else:
            result.append(right[j])
            j += 1
    result.extend(left[i:])
    result.extend(right[j:])
    return result
```

동률일 때 왼쪽을 먼저 선택해야 같은 키의 순서를 보존한다. 분할 깊이가 O(log n), 각 깊이의 병합 총량이 O(n)이므로 시간 O(n log n)이다. 슬라이싱도 복사 비용이 있지만 이 구현의 전체 차수는 유지된다. 동시에 살아 있는 보조 배열 크기의 합은 O(n)이다.

**자료 코드 교정:** 51쪽은 Python 리스트의 `pop(0)`을 반복한다. 한 번 삭제할 때 뒤 원소를 당기므로 상수 시간이 아니다. 최상위 병합만으로도 최악 O(n²) 이동이 생긴다. 또한 `<`로 비교하고 동률을 오른쪽에서 먼저 꺼내므로 그 구현은 안정적이지 않다. 본문은 인덱스 이동과 `<=`로 두 문제를 수정했다.

## 7. 퀵: 분할할 때 피벗 위치를 확정

첫 원소를 피벗으로 정하고, 왼쪽에서는 피벗 이상, 오른쪽에서는 피벗 미만인 값을 찾는다. 두 포인터가 교차하기 전이면 교환한다. 교차하면 피벗을 오른쪽 포인터 위치로 옮긴다. [강의: 54–64쪽]

```python
def partition(a, lo, hi):
    pivot = a[lo]
    left, right = lo + 1, hi
    while True:
        while left <= hi and a[left] < pivot:
            left += 1
        while right > lo and a[right] >= pivot:
            right -= 1
        if left >= right:
            break
        a[left], a[right] = a[right], a[left]
    a[lo], a[right] = a[right], a[lo]
    return right


def quick_sort(a, lo=0, hi=None):
    if hi is None:
        hi = len(a) - 1
    if lo < hi:
        p = partition(a, lo, hi)
        quick_sort(a, lo, p - 1)
        quick_sort(a, p + 1, hi)
```

분할 뒤 피벗 왼쪽은 피벗보다 작고 오른쪽은 크거나 같다. 피벗은 재귀 범위에서 제외한다. 같은 값만 있는 배열도 오른쪽 포인터가 줄어 종료하지만, 매번 크기 n-1인 문제가 남아 최악 O(n²), 스택 O(n)이 된다. 정렬된 입력에서도 첫 원소 피벗은 같은 문제를 일으킨다.

무작위 피벗은 기대 성능을 개선하고, 같은 키가 많을 때는 `<`, `=`, `>`로 나누는 3방향 분할이 도움이 된다. 둘 다 이 강의 구현에 자동으로 포함된 성질은 아니다.

## 8. 실무에서는 내장 정렬부터

`sorted(iterable)`은 새 리스트를 반환하고 `list.sort()`는 원본을 바꾸며 `None`을 반환한다. 둘 다 안정성을 보장하고 `key`로 비교 기준을 지정한다. [Python 정렬 HOWTO](https://docs.python.org/3/howto/sorting.html)

```python
records = [("가", 80), ("나", 90), ("다", 80)]
assert sorted(records, key=lambda x: x[1]) == [("가", 80), ("다", 80), ("나", 90)]
assert sorted(records, key=lambda x: (-x[1], x[0]))[0] == ("나", 90)
```

내림차순 점수, 오름차순 이름처럼 방향이 다르면 각 키의 방향을 따로 표현한다. `reverse=True`는 전체 정렬 순서를 뒤집는 옵션이지 두 번째 키에만 적용하는 옵션이 아니다.

## 9. 교정과 검증 기록

- 36쪽 `counting_sort(arr, max_value)`의 누적합 반복에 미정의 `k`가 등장한다. 본문은 `len(counts)`를 사용했다.
- 51쪽의 `pop(0)` 병합과 동률 처리 문제를 위에서 수정했다.
- 54쪽의 O(n log n) 설명에는 최악 O(n²) 조건을 추가했다.
- 22쪽 삽입 과정의 마지막 설명은 22를 16과 30 사이에 넣는 것으로 읽어야 한다. 그림의 최종 배열 순서는 맞다.

본문 여섯 정렬 구현은 빈 배열, 단일 원소, 정렬·역정렬, 중복 원소와 고정 시드 무작위 배열을 Python `sorted`와 대조했다. 카운팅 정렬에는 비음수 정수만 전달했다. 결과의 정렬 정확성을 확인한 것이며 실행 시간 벤치마크는 수행하지 않았다.

복습할 때는 버블의 뒤쪽, 삽입의 앞쪽, 선택의 확정 위치를 직접 표시해 보자. 병합의 동률 처리와 퀵의 재귀 범위도 손으로 추적하면 안정성과 종료 조건을 함께 확인할 수 있다.

검증 환경: Python 3.11.9, NumPy 2.4.6, macOS arm64. 공식 문서 확인일: 2026-09-17.
