# 오일러 경로 — 모든 간선을 한 번씩, 되돌아가지 않고

> 자기주도 학습 정리 · 정리일: 2026-08-30
> 앞선 편: [0825 DFS와 BFS 이론](0825_DFS와BFS_이론.md) · [0825 DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md)

프로그래머스 「여행경로」를 풀었다. ICN에서 출발해 주어진 항공권을 **전부** 쓰는 경로를 찾는 문제다. DFS/BFS 분류에 있는 문제니까 DFS로 파고들다가 티켓이 남은 채 막히면 되돌아오게 짰고, 통과했다.

그런데 코드에 `used` 배열이 있고, `path.pop()`이 있고, `if dfs(next): return True`가 있다. 되돌아가기 위한 장치가 셋이나 붙어 있다는 뜻이다. 이 문제가 정말 되돌아가야만 풀리는 문제인가.

아니다. **모든 간선을 한 번씩 지나는 경로**에는 이름이 있고, 그 경로는 조건만 맞으면 **한 번 걸어가는 것만으로 완성된다.** 되돌아갈 일이 원리적으로 생기지 않는다는 보장이 있기 때문이다. 이 글은 그 보장이 무엇이고 왜 성립하는지, 그리고 그걸 아는 것과 모르는 것이 실제로 몇 배 차이가 나는지를 정리한다.

> 그래프 표현·DFS·BFS의 기본은 [DFS와 BFS 완전정리](0825_DFS와BFS_이론.md) 편에서 다뤘다. 이 글은 그 위에 올라간다.

<br>

## 📖 이 글에서 다루는 내용

1. 오일러 경로란 무엇인가 — 정점이 아니라 간선을 센다
2. 존재 판정 (1) 무방향 그래프 — 차수의 홀짝이 전부다
3. 존재 판정 (2) 방향 그래프 — 진입과 진출의 차이
4. 되돌아갈 필요가 없다는 보장
5. Hierholzer 알고리즘 — 스택 하나로 끝낸다
6. 여행경로 문제에 적용하기
7. 백트래킹 풀이는 어디서 무너지는가 — 실측
8. 흔한 실수 네 가지
9. 오일러 경로가 답이 아닐 때

<br>

---

## 1. 오일러 경로란 무엇인가 — 정점이 아니라 간선을 센다

정의는 짧다.

- **오일러 경로(Eulerian path)** — 그래프의 모든 **간선**을 정확히 한 번씩 지나는 경로
- **오일러 회로(Eulerian circuit)** — 시작점과 도착점이 같은 오일러 경로

주의할 곳은 **간선**이라는 단어 하나다. 정점은 몇 번을 다시 밟아도 된다. 「여행경로」에서 같은 공항을 두 번 거치는 답이 나오는 게 그래서다. 세는 대상이 티켓(간선)이지 공항(정점)이 아니다.

> **[그림]** 같은 그래프에서 모든 간선을 한 번씩 지나는 오일러 경로와 모든 정점을 한 번씩 지나는 해밀턴 경로를 나란히 놓고, 정점 재방문이 허용되는 쪽과 간선 재사용이 허용되는 쪽을 대비한 그림

한 글자 차이인 **해밀턴 경로(Hamiltonian path)** 와 헷갈리기 쉬운데, 둘의 난이도는 하늘과 땅이다.

| | 오일러 경로 | 해밀턴 경로 |
|---|---|---|
| **한 번씩 지나는 대상** | 모든 **간선** | 모든 **정점** |
| **재방문 허용** | 정점은 여러 번 밟아도 된다 | 간선은 여러 번 써도 된다 |
| **존재 판정** | 차수만 세면 된다 — `O(V + E)` | **NP-완전.** 다항 시간 판정법이 알려져 있지 않다 |
| **경로 구성** | `O(V + E)` | 사실상 완전탐색 |

> ➡️ **"모든 정점"이면 각오하고, "모든 간선"이면 안심한다.**
> 지문이 "모든 도시를 한 번씩"이면 완전탐색·비트마스크 DP를 준비해야 하고, "모든 도로를", "모든 티켓을", "모든 다리를"이면 오일러 경로다. 후자는 선형 시간에 끝난다.

### 이름의 출처 — 쾨니히스베르크의 다리

1736년, 오일러는 "쾨니히스베르크의 다리 일곱 개를 한 번씩만 건너 모두 지날 수 있는가"라는 질문에 **불가능하다**고 답했다. 이때 그가 한 일이 문제에서 지형과 거리를 전부 지우고 **땅덩어리를 점, 다리를 선으로 바꾼 것**이다. 그래프 이론이 여기서 시작됐다.

> **[그림]** 쾨니히스베르크의 네 땅덩어리와 일곱 다리를 정점 네 개와 간선 일곱 개의 그래프로 추상화하고 각 정점의 차수가 5, 3, 3, 3으로 모두 홀수임을 표시한 그림

네 땅덩어리의 차수는 각각 **5, 3, 3, 3**이다. 전부 홀수다. 다음 절에서 볼 판정 조건에 따르면 홀수 차수 정점은 0개이거나 2개여야 하는데 4개다. 그래서 불가능하다.

<br>

---

## 2. 존재 판정 (1) 무방향 그래프 — 차수의 홀짝이 전부다

오일러 경로가 아름다운 이유는 **존재 여부를 차수만 세서 판정할 수 있다**는 점이다. 경로를 만들어 보지 않아도 된다.

### 왜 차수가 짝수여야 하는가

경로가 어떤 정점을 **지나간다**는 것은 그 정점으로 **들어왔다가 나간다**는 뜻이다. 한 번 지날 때마다 간선 두 개를 쓴다.

> **[그림]** 경로가 중간 정점을 통과할 때마다 들어오는 간선 하나와 나가는 간선 하나를 짝지어 소비하므로 통과 횟수의 두 배가 곧 차수가 되고, 따라서 중간 정점의 차수는 반드시 짝수라는 것을 보여주는 그림

- **중간 정점** — 지날 때마다 간선 2개씩 소비한다. 총 소비량 = 2 × (지난 횟수) → **차수가 짝수**
- **시작 정점** — 처음 한 번은 나가기만 한다. 이 하나가 짝을 못 이룬다 → **차수가 홀수**
- **도착 정점** — 마지막 한 번은 들어오기만 한다 → **차수가 홀수**

시작점과 도착점이 같으면(오일러 회로) 나가는 첫 간선과 들어오는 마지막 간선이 그 정점에서 짝을 이루므로 **모든 정점이 짝수**가 된다.

### 판정 규칙

> **[그림]** 무방향 그래프에서 홀수 차수 정점이 0개면 오일러 회로, 2개면 그 둘을 양 끝으로 하는 오일러 경로, 4개 이상이면 불가능이라는 세 가지 경우를 각각의 예시 그래프와 함께 보여주는 그림

| 홀수 차수 정점의 수 | 결과 | 시작점 |
|---|---|---|
| **0개** | 오일러 **회로**가 존재한다 | 아무 정점에서나 시작 가능 |
| **2개** | 오일러 **경로**가 존재한다 | 두 홀수 정점 중 하나에서 시작, 다른 하나에서 끝난다 |
| **4개 이상** | 존재하지 않는다 | — |
| 1개 또는 3개 | **이런 그래프는 아예 없다** | — |

마지막 줄이 재미있다. 홀수 차수 정점이 홀수 개인 그래프는 만들 수 없다. 모든 정점의 차수를 더하면 간선 하나가 양쪽에서 한 번씩 세어지므로 **차수의 총합은 항상 2E, 즉 짝수**다. 짝수 합에서 짝수들을 빼면 홀수들의 합도 짝수여야 하고, 홀수를 짝수 개 더해야만 짝수가 된다. **악수 정리(handshaking lemma)** 라고 부른다.

### 연결성 조건을 빼먹으면 안 된다

차수 조건만으로는 부족하다. 서로 떨어진 삼각형 두 개는 모든 정점의 차수가 2(짝수)지만, 한쪽에서 다른 쪽으로 건너갈 방법이 없으니 오일러 회로가 없다.

```python
def has_euler_undirected(n, edges):
    from collections import defaultdict
    deg = defaultdict(int)
    g = defaultdict(list)
    for a, b in edges:
        deg[a] += 1; deg[b] += 1
        g[a].append(b); g[b].append(a)

    odd = sum(1 for v in deg if deg[v] % 2 == 1)
    if odd not in (0, 2):
        return False

    # 간선을 가진 정점들이 하나의 연결 요소인가 (고립 정점은 무시한다)
    start = next(iter(deg))
    seen, stack = {start}, [start]
    while stack:
        v = stack.pop()
        for nxt in g[v]:
            if nxt not in seen:
                seen.add(nxt); stack.append(nxt)
    return len(seen) == len(deg)
```

> ⚠️ **"고립 정점은 무시한다"가 조건에 꼭 들어간다**
> 간선이 하나도 없는 정점은 지날 필요가 없으므로 연결성 판정에서 빼야 한다. 정점 100개 중 3개만 간선을 갖는 그래프도 그 3개가 이어져 있으면 오일러 경로가 있다. `len(seen) == n` 으로 쓰면 여기서 틀린다.

<br>

---

## 3. 존재 판정 (2) 방향 그래프 — 진입과 진출의 차이

「여행경로」의 항공권은 `[출발지, 도착지]` 라서 방향이 있다. 방향 그래프에서는 차수를 **진입 차수(in-degree)** 와 **진출 차수(out-degree)** 로 나눠 센다. 홀짝 대신 **둘의 차이**를 본다.

> **[그림]** 방향 그래프에서 오일러 회로는 모든 정점의 진입 차수와 진출 차수가 같아야 하고, 오일러 경로는 진출에서 진입을 뺀 값이 +1인 정점 하나와 -1인 정점 하나만 있어야 한다는 조건을 예시 그래프와 차수표로 보여주는 그림

| 조건 | 오일러 회로 | 오일러 경로 |
|---|---|---|
| **진출 − 진입 = +1** | 없어야 한다 | 정확히 1개 → **이 정점이 시작점** |
| **진출 − 진입 = −1** | 없어야 한다 | 정확히 1개 → **이 정점이 도착점** |
| **나머지 정점** | 전부 진출 = 진입 | 전부 진출 = 진입 |
| **연결성** | 간선을 가진 정점이 전부 이어져 있어야 한다 | 같다 |

이유는 무방향과 똑같다. 중간 정점은 들어온 만큼 나가야 하니 진입 = 진출이고, 시작점은 처음 한 번을 더 나가고, 도착점은 마지막 한 번을 더 들어온다.

「여행경로」의 두 번째 예제로 확인해 보자.

```text
tickets = [["ICN","SFO"], ["ICN","ATL"], ["SFO","ATL"], ["ATL","ICN"], ["ATL","SFO"]]

정점   진입  진출  진출-진입
ICN     1     2      +1     ← 시작점
ATL     2     2       0
SFO     2     1      -1     ← 도착점
```

`ICN`이 `+1`로 나온다. 문제가 "ICN에서 출발한다"고 알려주지 않았어도 차수만 세면 출발지가 어디인지 계산으로 나온다는 뜻이다.

```python
from collections import defaultdict

def find_start(tickets):
    ind, outd = defaultdict(int), defaultdict(int)
    for a, b in tickets:
        outd[a] += 1; ind[b] += 1

    plus = [v for v in set(ind) | set(outd) if outd[v] - ind[v] == 1]
    minus = [v for v in set(ind) | set(outd) if ind[v] - outd[v] == 1]

    if not plus and not minus:
        return tickets[0][0]        # 오일러 회로 — 아무 데서나 시작
    if len(plus) == 1 and len(minus) == 1:
        return plus[0]              # 오일러 경로 — +1인 정점이 시작점
    return None                     # 오일러 경로가 없다
```

<br>

---

## 4. 되돌아갈 필요가 없다는 보장

여기가 이 글의 핵심이다. **오일러 경로가 존재하는 그래프에서, 올바른 시작점에서 출발해 아무 간선이나 골라 계속 걸어가면, 막히는 곳은 반드시 도착점이다.** 중간에 엉뚱한 데서 갇히는 일이 일어나지 않는다.

증명은 두 줄이다.

> **[그림]** 중간 정점 v에 k번 들어오고 k-1번 나간 상태에서 진입 차수와 진출 차수가 같다는 조건 때문에 아직 쓰지 않은 진출 간선이 반드시 남아 있음을 보여, 도착점이 아닌 곳에서는 막힐 수 없다는 것을 증명하는 그림

시작점도 도착점도 아닌 정점 `v`에서 막혔다고 하자. 지금까지 `v`에 `k`번 들어왔고 `k−1`번 나간 상태다(마지막에 들어와서 못 나갔으니까).

- 쓴 진입 간선 = `k`개 이고, `v`의 전체 진입 차수는 `k` 이상이다
- `v`는 중간 정점이므로 **진출 차수 = 진입 차수 ≥ k**
- 그런데 쓴 진출 간선은 `k−1`개뿐이다 → **안 쓴 진출 간선이 최소 1개 남아 있다**

즉 막혔다는 가정과 모순이다. 그러니 **막힐 수 있는 곳은 진출 차수가 진입 차수보다 하나 적은 정점, 곧 도착점뿐**이다.

> ➡️ **되돌아가기 코드가 필요 없는 이유가 이 한 줄이다.**
> 백트래킹은 "잘못된 선택을 했을 수도 있다"를 전제로 한다. 그런데 오일러 경로에서는 어느 간선을 고르든 **막히는 위치가 도착점으로 고정**되어 있다. 잘못된 선택이라는 게 없다.

### 그럼 남은 간선은 어떻게 되나

막히는 곳이 도착점이라는 게 **모든 간선을 다 썼다**는 뜻은 아니다. 도착점에 도달했는데 간선이 남아 있을 수 있다.

```text
tickets = [["ICN","AAA"], ["ICN","BBB"], ["BBB","ICN"]]

사전순으로 AAA를 먼저 고르면  ICN → AAA 에서 막힌다. 티켓 2장이 남는다.
```

하지만 남은 간선들은 **아무렇게나 남지 않는다.** 걸어온 경로가 각 정점에서 진입과 진출을 짝지어 소비했으므로, 남은 부분에서는 모든 정점의 진입 차수와 진출 차수가 다시 같아진다. **진입 = 진출인 그래프는 사이클들로만 이루어진다.** 그리고 그 사이클들은 이미 지나온 경로 위의 어떤 정점에 반드시 붙어 있다(연결 그래프니까).

그래서 할 일은 하나다. **지나온 경로를 되짚어 가면서, 아직 간선이 남은 정점을 만나면 거기서 사이클을 마저 돌고 그 사이클을 경로 중간에 끼워 넣는다.** 이게 Hierholzer 알고리즘이다. 되돌아가긴 하지만 **선택을 취소하는 되돌아가기가 아니라, 이미 확정된 경로를 훑는 되돌아가기**다. 각 간선을 정확히 한 번씩만 건드리므로 전체가 `O(V + E)`다.

<br>

---

## 5. Hierholzer 알고리즘 — 스택 하나로 끝낸다

말로 하면 "사이클을 끼워 넣는다"라서 복잡해 보이는데, 스택으로 쓰면 열 줄이다. **끼워 넣기를 직접 구현하지 않고, 결과를 거꾸로 쌓았다가 뒤집는 것으로 해결한다.**

```python
from collections import defaultdict

def hierholzer(tickets, start="ICN"):
    graph = defaultdict(list)
    for a, b in tickets:
        graph[a].append(b)
    for k in graph:
        graph[k].sort(reverse=True)      # pop()이 사전순 최소를 뽑도록 내림차순

    stack, path = [start], []
    while stack:
        top = stack[-1]
        if graph[top]:
            stack.append(graph[top].pop())   # 갈 데가 있으면 간다 (티켓은 꺼내는 순간 소비)
        else:
            path.append(stack.pop())         # 막히면 확정 경로로 옮긴다
    path.reverse()
    return path
```

규칙은 둘뿐이다.

- **스택 맨 위 정점에 남은 간선이 있으면** 하나 꺼내 쓰고 그 목적지를 스택에 쌓는다
- **없으면** 그 정점을 스택에서 빼서 `path`에 넣는다

위 미니 예제를 그대로 돌려 보면 이렇게 흘러간다.

> **[그림]** ICN AAA BBB 세 정점 예제에서 Hierholzer 스택이 단계마다 어떻게 변하고 막힌 정점이 path로 옮겨지는지를 여덟 단계로 추적한 표 형태의 그림

```text
단계  행동              스택                      path
 0   시작              [ICN]                     []
 1   ICN → AAA         [ICN, AAA]                []
 2   AAA 막힘 → path    [ICN]                     [AAA]
 3   ICN → BBB         [ICN, BBB]                [AAA]
 4   BBB → ICN         [ICN, BBB, ICN]           [AAA]
 5   ICN 막힘 → path    [ICN, BBB]                [AAA, ICN]
 6   BBB 막힘 → path    [ICN]                     [AAA, ICN, BBB]
 7   ICN 막힘 → path    []                        [AAA, ICN, BBB, ICN]

뒤집기 → [ICN, BBB, ICN, AAA]
```

2단계에서 `AAA`에 갇혔지만 아무것도 취소하지 않았다. `AAA`를 **완성된 경로의 맨 끝 조각**으로 확정하고 스택을 한 칸 내려왔을 뿐이다.

### 왜 뒤집는가

`path`에 먼저 들어가는 정점일수록 **최종 경로에서 뒤쪽**에 놓인다. 가장 먼저 막힌 정점이 곧 경로의 종점이기 때문이다.

> **[그림]** path에 쌓이는 순서가 최종 경로의 역순이 되는 원리와, 나중에 발견한 사이클이 뒤집기 한 번으로 경로 중간의 올바른 위치에 저절로 끼워지는 과정을 두 단계로 비교한 그림

그리고 이 뒤집기가 "사이클 끼워 넣기"를 공짜로 해 준다. 위 추적에서 `[AAA]` 다음에 사이클 `ICN → BBB → ICN`의 정점들이 쌓였고, 뒤집으면 그 사이클이 `AAA` **앞**으로 이동해 `ICN → BBB → ICN → AAA`가 된다. 리스트 중간에 삽입하는 코드를 한 줄도 쓰지 않았다.

### 재귀형 — 더 짧지만 깊이를 조심한다

같은 알고리즘을 재귀로 쓰면 다섯 줄이 된다.

```python
import sys
from collections import defaultdict

def hierholzer_rec(tickets, start="ICN"):
    sys.setrecursionlimit(10 ** 6)          # 재귀 깊이가 간선 수만큼 깊어진다
    graph = defaultdict(list)
    for a, b in tickets:
        graph[a].append(b)
    for k in graph:
        graph[k].sort(reverse=True)

    path = []
    def dfs(v):
        while graph[v]:
            dfs(graph[v].pop())
        path.append(v)                       # 돌아 나오면서 쌓는다 (후위 순회)
    dfs(start)
    return path[::-1]
```

`path.append(v)`가 **재귀에서 빠져나오면서** 실행되는 게 포인트다. 후위 순회 순서로 쌓이므로 결과가 역순이 되고, 마지막에 뒤집으면 맞는다.

| | 스택형 | 재귀형 |
|---|---|---|
| 줄 수 | 조금 길다 | 짧다 |
| 재귀 깊이 | 없음 | **간선 수만큼** — 티켓 1만 장이면 깊이 1만 |
| 파이썬에서 | 안전 | `sys.setrecursionlimit` 필수, 그래도 세그폴트 위험 |

> ⚠️ **`setrecursionlimit`을 올려도 안심할 수 없다**
> 파이썬의 재귀 한계는 인터프리터가 세는 논리적 깊이일 뿐이고, 실제로는 C 스택이 먼저 터진다. 깊이가 수만이 되면 `RecursionError` 대신 프로세스가 조용히 죽는다. 간선 수 상한이 큰 문제에서는 **스택형을 기본값으로 쓴다.** 이 함정은 [DFS와 BFS 완전정리](0825_DFS와BFS_이론.md) 편에서도 다뤘다.

### 복잡도

| 단계 | 비용 |
|---|---|
| 인접 리스트 만들기 | `O(E)` |
| 사전순 정렬 (필요할 때만) | `O(E log E)` |
| 스택 순회 | 각 간선을 정확히 한 번 소비 → `O(V + E)` |
| **전체** | **`O(E log E)`** (정렬이 없으면 `O(V + E)`) |

<br>

---

## 6. 여행경로 문제에 적용하기

이제 조각이 다 모였다. 「여행경로」(프로그래머스 43164)는 방향 그래프의 오일러 경로를 `ICN`에서 시작해 구하되, **가능한 경로가 여럿이면 사전순으로 앞서는 것**을 고르는 문제다.

### 처음에 쓴 풀이 — 백트래킹

```python
from collections import defaultdict

def solution(tickets):
    graph = defaultdict(list)
    for a, b in tickets:
        graph[a].append(b)
    for k in graph:
        graph[k].sort()                  # 사전순을 보장하려고 미리 정렬

    used = {k: [False] * len(v) for k, v in graph.items()}
    m = len(tickets)
    path = ["ICN"]

    def dfs(now):
        if len(path) == m + 1:           # 티켓을 다 썼으면 완성
            return True
        for i, nxt in enumerate(graph[now]):
            if used[now][i]:
                continue
            used[now][i] = True
            path.append(nxt)
            if dfs(nxt):                 # 성공하면 그대로 확정
                return True
            path.pop()                   # 실패하면 되돌린다
            used[now][i] = False
        return False

    dfs("ICN")
    return path
```

동작한다. 사전순으로 먼저 시도하고, 처음 성공한 경로를 그대로 반환하므로 답도 맞다. 문제는 되돌리기 장치 셋(`used`, `path.pop()`, `if dfs(...): return True`)이 전부 **일어나지 않아도 되는 일을 대비하는 코드**라는 점이다.

### Hierholzer 풀이

```python
from collections import defaultdict

def solution(tickets):
    graph = defaultdict(list)
    for a, b in tickets:
        graph[a].append(b)
    for k in graph:
        graph[k].sort(reverse=True)

    stack, path = ["ICN"], []
    while stack:
        top = stack[-1]
        if graph[top]:
            stack.append(graph[top].pop())
        else:
            path.append(stack.pop())
    return path[::-1]
```

> **[그림]** 백트래킹 풀이와 Hierholzer 풀이를 나란히 놓고 used 배열과 되돌리기 세 줄이 사라지는 대신 pop과 뒤집기가 들어가는 구조 차이를 대비한 그림

| | 백트래킹 | Hierholzer |
|---|---|---|
| 티켓 소비 표시 | `used[now][i] = True` | **리스트에서 `pop()`** — 자료구조가 곧 상태 |
| 되돌리기 | `path.pop()` + `used = False` | 없음 |
| 성공 판정 | `len(path) == m + 1` 을 매 호출 확인 | 없음 (끝나면 그게 답) |
| 재귀 | 있음 (깊이 = 티켓 수) | 없음 |
| 최악 시간 | 입력에 따라 급격히 나빠진다 | `O(E log E)` 고정 |

### `sort(reverse=True)`가 핵심 한 줄이다

정렬을 내림차순으로 하는 이유는 두 가지가 겹친다.

- **`list.pop()`은 끝에서 빼야 `O(1)`이다.** `pop(0)`은 뒤의 원소를 전부 앞으로 당기므로 `O(N)`이고, 전체가 `O(E²)`가 된다
- 그런데 사전순으로 **작은 것부터** 써야 한다

둘을 동시에 만족시키려면 리스트를 내림차순으로 정렬해 두면 된다. 그러면 `pop()`이 꺼내는 마지막 원소가 곧 사전순 최소다. `heapq`를 써도 되지만 정렬 한 번이 더 싸다.

> 💡 **그리디로 최솟값만 골라도 정말 사전순 최소가 나오는가**
> "매번 사전순 최소를 고른다"는 건 지역적 선택이라 전체 최소를 보장하지 않을 것 같아 의심스럽다. 확인해 봤다. 오일러 경로가 존재하는 랜덤 방향 멀티그래프 **34,000개**를 만들어, 모든 오일러 경로를 완전탐색해 구한 사전순 최소와 위 Hierholzer 결과를 대조했다. **불일치 0건**이었다. 직관적인 이유는 4절의 보장에 있다 — 어느 간선을 골라도 막히는 위치가 도착점으로 고정되므로, 사전순 최소를 고르는 선택이 완성 가능성을 해치지 않는다.

<br>

---

## 7. 백트래킹 풀이는 어디서 무너지는가 — 실측

"통과했는데 왜 바꿔야 하나"에 답하려면 숫자가 필요하다. 백트래킹이 실제로 얼마나 나빠질 수 있는지 입력을 직접 만들어 재 봤다.

### 무너지는 입력의 모양

두 가지를 겹치면 된다.

- **사전순으로 맨 앞에 오는 막다른 길** — 그리디가 매번 여기부터 시도하고 매번 실패한다
- **`ICN`으로 돌아오는 꽃잎 사이클 여러 개** — 실패-복귀를 반복하게 만든다

> **[그림]** ICN에 붙은 꽃잎 사이클 k개와 사전순으로 앞서는 함정 사슬 m칸으로 이루어진 입력에서 백트래킹이 ICN에 돌아올 때마다 함정 사슬을 끝까지 걸었다 되돌아오는 구조, 그리고 티켓 수에 따른 백트래킹 실행 시간이 제곱으로 늘어나는 막대그래프

```python
def make_trap(k, m):
    """꽃잎 k개 + 사전순으로 앞서는 함정 사슬 m칸"""
    t = []
    for i in range(k):                          # ICN → Pxxxxx → ICN
        t.append(["ICN", f"P{i:05d}"])
        t.append([f"P{i:05d}", "ICN"])
    t.append(["ICN", "A00000"])                 # A 로 시작하니 사전순 최우선
    for j in range(m - 1):                      # A00000 → A00001 → ... 막다른 사슬
        t.append([f"A{j:05d}", f"A{j+1:05d}"])
    return t
```

오일러 경로는 분명히 존재한다. 꽃잎을 전부 돌고 마지막에 함정 사슬로 내려가면 된다. 그런데 `A`가 `P`보다 사전순으로 앞서므로, 백트래킹은 `ICN`에 돌아올 때마다 **함정 사슬을 끝까지 걸어갔다가 되돌아온다.**

### 측정 결과

| 입력 (함정형) | 티켓 수 | 백트래킹 | Hierholzer | 배수 |
|---|---|---|---|---|
| k=250, m=250 | 750 | 37 ms | 0.4 ms | 92배 |
| k=500, m=500 | 1,500 | 162 ms | 0.8 ms | 199배 |
| k=1000, m=1000 | 3,000 | 676 ms | 1.5 ms | 438배 |
| k=2500, m=2500 | 7,500 | 5,219 ms | 3.9 ms | 1,337배 |
| **k=3333, m=3334** | **10,000** | **9.37 초** | **5.5 ms** | **1,709배** |

두 풀이의 반환값은 모든 케이스에서 완전히 같았다. 답이 달라진 게 아니라 **같은 답을 1,700배 느리게 낸다.**

티켓 수를 2배로 늘렸을 때 백트래킹 시간은 4.2배 안팎으로 늘었다. 정확히 제곱이다.

```text
750 → 1,500 (2.00배)   : 시간 4.34배
1,500 → 3,000 (2.00배) : 시간 4.16배
```

### 되돌아가지 않아도 느려지는 경우

되돌아가기가 전혀 없는 순한 입력으로도 재 봤다. 꽃잎 사이클만 4,999개 두고 함정을 빼면 그리디가 한 번도 실패하지 않는다. 그런데도 **670 ms 대 5.3 ms, 127배**가 나왔다.

원인은 되돌아가기가 아니라 이 줄이다.

```python
for i, nxt in enumerate(graph[now]):
    if used[now][i]:
        continue
```

`ICN`에 돌아올 때마다 인접 리스트를 **0번 인덱스부터 다시 훑는다.** 이미 쓴 티켓을 매번 건너뛰느라, 차수가 `d`인 정점 하나에서만 `O(d²)`이 든다. 허브 공항이 하나만 있어도 여기서 걸린다. 실제로 허브가 없는 긴 사슬 입력(티켓 9,999장)에서는 백트래킹도 42 ms로 멀쩡했다.

> ➡️ **`used` 불리언 배열 대신 `pop()`을 쓰면 이 문제가 통째로 사라진다.**
> 쓴 간선을 리스트에서 빼 버리면 다시 볼 일이 없다. "표시하고 건너뛰기"와 "빼 버리기"의 차이가 제곱과 선형의 차이다.

그러니 이 문제에서 백트래킹 풀이가 통과한 건 알고리즘이 괜찮아서가 아니라 **테스트 케이스에 허브형·함정형 입력이 없어서**다.

<br>

---

## 8. 흔한 실수 네 가지

### 시작점을 문제에서 주지 않는 경우

「여행경로」는 `ICN`을 알려주지만, 대부분의 오일러 경로 문제는 시작점을 직접 찾아야 한다. **아무 정점에서나 시작하면 안 된다.**

```python
# 방향 그래프
start = "진출 - 진입 == 1 인 정점"           # 없으면 (오일러 회로) 간선을 가진 아무 정점
# 무방향 그래프
start = "차수가 홀수인 두 정점 중 하나"       # 없으면 (오일러 회로) 간선을 가진 아무 정점
```

오일러 **회로**인 그래프에서 시작점을 잘못 고를 일은 없지만, 반대로 오일러 **경로**인 그래프에서 중간 정점부터 출발하면 반드시 실패한다.

### 간선 수 검증을 빼먹는 경우

Hierholzer는 그래프가 끊겨 있어도 **에러 없이** 짧은 경로를 반환한다. 도달 가능한 부분만 훑고 끝나기 때문이다.

```python
path = hierholzer(tickets)
if len(path) != len(tickets) + 1:
    return None        # 오일러 경로가 없다 — 연결성이 깨졌다는 뜻
```

연결성을 따로 검사하는 대신 이 한 줄이면 된다. **정점이 E+1개가 아니면 실패**다. 「여행경로」는 항상 답이 있다고 보장하니 생략해도 되지만, 존재 여부를 물어보는 문제에서는 필수다.

### 무방향 그래프에서 간선을 두 번 지우는 문제

무방향 그래프에 Hierholzer를 적용할 때, `(a, b)` 하나를 쓰면 `graph[a]`의 `b`와 `graph[b]`의 `a`를 **둘 다** 지워야 한다. 한쪽만 지우면 같은 간선을 반대 방향으로 한 번 더 쓴다.

```python
# 간선에 번호를 붙여 두고 그 번호로 소비 여부를 관리한다
graph = defaultdict(list)
for idx, (a, b) in enumerate(edges):
    graph[a].append((b, idx))
    graph[b].append((a, idx))
used = [False] * len(edges)

# 순회 중
while graph[v]:
    nxt, idx = graph[v].pop()
    if used[idx]:            # 반대쪽에서 이미 썼다
        continue
    used[idx] = True
    ...
```

정점 이름으로 지우려 하면 중복 간선이 있을 때 무너진다. **간선 인덱스를 키로 삼는 게 안전하다.**

### 다중 간선을 `set`으로 관리하는 경우

같은 구간의 티켓이 두 장 있을 수 있다(`["ICN","JFK"]`가 두 번). 인접 관계를 `set`으로 잡으면 한 장이 사라진다. **오일러 경로는 멀티그래프 문제이므로 자료구조는 리스트여야 한다.**

<br>

---

## 9. 오일러 경로가 답이 아닐 때

"모든 간선을 한 번씩"이라는 조건이 조금만 틀어져도 다른 문제가 된다.

| 문제의 요구 | 이름 | 난이도 |
|---|---|---|
| 모든 **간선**을 정확히 한 번씩 | **오일러 경로** | `O(V + E)` |
| 모든 **정점**을 정확히 한 번씩 | 해밀턴 경로 | NP-완전 |
| 모든 간선을 **최소 한 번 이상**, 총 이동 거리 최소 | 중국인 우편배달부 문제 | 다항 시간 (홀수 정점끼리 최소 매칭) |
| 모든 정점을 한 번씩 돌고 출발지로, 거리 최소 | 외판원 순회(TSP) | NP-난해 |

특히 세 번째가 실무에 자주 나온다. 오일러 경로가 없는 그래프(홀수 차수 정점이 4개 이상)에서 "그래도 모든 길을 지나야 한다"면, **홀수 차수 정점들을 짝지어 그 사이 경로를 한 번 더 걷는 것**으로 해결한다. 어떤 짝을 만들지가 최소 가중치 매칭 문제다. 제설차 경로, 우편 배달, 도로 점검이 전부 이 모양이다.

그리고 코딩 테스트 관점에서는 이 신호를 기억해 두면 된다.

| 지문의 표현 | 꺼낼 것 |
|---|---|
| "모든 티켓을", "모든 다리를", "모든 도로를 한 번씩" | **오일러 경로** |
| "한붓그리기가 가능한가" | **오일러 경로 존재 판정** (차수만 세면 끝) |
| "모든 도시를 한 번씩 방문" | 완전탐색 · 비트마스크 DP |
| "최소 몇 번 만에 도달" | BFS — [DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md) 편 참고 |

<br>

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **정의** | 모든 **간선**을 한 번씩. 정점은 몇 번이든 재방문 가능 |
| **오일러 vs 해밀턴** | 간선이면 `O(V+E)`, 정점이면 NP-완전. 한 글자 차이가 난이도를 가른다 |
| **무방향 판정** | 홀수 차수 정점이 **0개(회로) 또는 2개(경로)** + 간선 가진 정점의 연결성 |
| **악수 정리** | 차수의 총합 = 2E. 그래서 홀수 차수 정점은 항상 짝수 개다 |
| **방향 판정** | 진출−진입이 **+1인 정점 1개(시작), −1인 정점 1개(끝), 나머지 0** |
| **되돌아갈 필요 없음** | 중간 정점은 진입=진출이라 항상 나갈 길이 남는다. **막히는 곳은 도착점뿐** |
| **남은 간선** | 반드시 사이클을 이룬다. 경로를 되짚으며 끼워 넣으면 된다 |
| **Hierholzer** | 스택 맨 위에 갈 데 있으면 가고, 없으면 `path`로 옮긴다. 마지막에 뒤집는다 |
| **뒤집기의 정체** | 사이클 삽입을 공짜로 해 준다. 리스트 중간 삽입 코드가 필요 없다 |
| **사전순 최소** | 인접 리스트를 **내림차순 정렬** → `pop()`이 곧 사전순 최소이자 `O(1)` |
| **`used` 배열의 함정** | 매번 0번부터 훑으면 허브 정점에서 `O(d²)`. `pop()`으로 빼 버리면 사라진다 |
| **실측** | 함정형 입력 티켓 1만 장 — 백트래킹 9.37초 vs Hierholzer 5.5ms (**1,709배**) |
| **검증 한 줄** | `len(path) == len(edges) + 1` 이 아니면 오일러 경로가 없는 것이다 |
| **재귀 깊이** | 재귀형 Hierholzer는 간선 수만큼 깊어진다. 파이썬에서는 스택형이 기본값 |

<br>

## 🔗 참고 자료

- [프로그래머스 43164 — 여행경로](https://school.programmers.co.kr/learn/courses/30/lessons/43164)
- [Wikipedia — Eulerian path](https://en.wikipedia.org/wiki/Eulerian_path)
- [Wikipedia — Seven Bridges of Königsberg](https://en.wikipedia.org/wiki/Seven_Bridges_of_K%C3%B6nigsberg)
- [Wikipedia — Route inspection problem](https://en.wikipedia.org/wiki/Chinese_postman_problem) — 중국인 우편배달부 문제
- [파이썬 공식 문서 — collections.defaultdict](https://docs.python.org/ko/3/library/collections.html#collections.defaultdict)
