# 최단 경로 — 다익스트라, 벨만-포드, 플로이드-워셜: 완화 한 줄을 어떤 순서로 반복하는가

> 출처: 알고리즘 과정 강의자료 · 정리일: 2026-09-12

---

## 0. 한 줄 요약

세 알고리즘은 전부 **`dist[u] + w < dist[v]` 한 줄(완화, relax)** 을 반복한다. 다른 것은 **어떤 간선을 어떤 순서로 완화하느냐** 뿐이다.

| 알고리즘 | 완화 순서 | 푸는 문제 | 음수 간선 | 복잡도 |
|---|---|---|---|---|
| **다익스트라** | 시작점에서 **가장 가까운 정점부터** 하나씩 확정하고, 그 정점의 진출 간선만 완화 | 한 정점 → 모든 정점 | 불가 | `O((V+E) log V)` |
| **벨만-포드** | **모든 간선을 V-1 번** 되풀이해 완화 | 한 정점 → 모든 정점 | 가능 (음수 사이클 검출) | `O(VE)` |
| **플로이드-워셜** | **경유 정점 k 를 하나씩 늘리며** 모든 쌍 (i, j) 를 완화 | 모든 정점 → 모든 정점 | 가능 (음수 사이클은 불가) | `O(V^3)` |

그 순서의 차이가 음수 가중치를 견디는지, 모든 쌍을 구하는지, 파이썬에서 어디서 무너지는지를 결정한다.

**이번에 원본 자료를 직접 돌려 보면서 확인한 것들** (전체는 15절)

1. ⭐ "다익스트라는 음수 간선에서 깨진다"는 말은 구현에 따라 **오답**(꺼낸 정점을 확정하는 판: 정답 -4 를 2 로 낸다)이거나 **지수 시간**(자료의 판: 정점 25개 그래프에서 pop 8,388,632회, 정점 하나에 2배씩)이라는 뜻이다. 자료의 판은 음수 간선에서도 **정답을 내지만 보장이 없고**, 음수 사이클이 있으면 끝나지 않는다
2. ⭐ 벨만-포드의 순회 수는 **간선 검사 순서가 정한다.** 자료 그래프의 간선 10개를 무작위로 섞어 10,000번 돌리면 갱신 순회가 1회(0.9%)부터 5회(1.0%)까지 퍼진다. 경로 그래프 V=2,000 은 정방향이면 1회, 역방향이면 1,999회
3. ⭐ 자료의 "플로이드-워셜은 다익스트라와 시간 복잡도가 동일하다"는 **모든 쌍 + 밀집 그래프**에서만 맞는 말이다. V=400 완전 그래프에서 플로이드 2,953.1 ms 대 다익스트라 400번 2,180.4 ms(1.4배)로 같은 자릿수지만, 희소 그래프(E=4V)에서는 4,125.7 ms 대 110.4 ms 로 **37.4배** 차이다
4. ⭐ 자료의 플로이드-워셜 코드 예시 그래프에는 **음수 사이클 2→5→4→2 (합 -1)** 가 들어 있다. 실행하면 대각선이 `[0, 0, -1, 0, -1, -2]` 로 나온다 — 이것이 그대로 "대각선이 음수면 음수 사이클" 검출법의 예시가 된다
5. 플로이드-워셜의 삼중 루프에서 k 를 안쪽에 두면 무작위 그래프 1,000건 중 **730건(73.0%)** 이 틀린다. k 가 바깥인 판은 정점마다 다익스트라를 돌린 결과와 1,000건 전부 일치
6. 자료의 다익스트라 예제 큐에 적힌 `d = 6` 은 **간선 가중치**이고, 실제 큐에 들어가는 값은 **누적 거리 9** 다. 자료 코드의 `graph` 딕셔너리에는 그림의 간선 11개 중 **b→d(6), e→a(3) 두 개가 빠져 있다** (최종 거리는 같다)
7. 자료 주석의 다익스트라 출력 `{a:0, b:3, c:5, d:9, e:11, f:12}`, 연습 그래프 답 `0,4,6,10,8,9,11,13,17,21`, 벨만-포드 출력 `A:0 B:3 C:2 D:5 E:2 F:4`, 플로이드-워셜 K=1~5 행렬 — **전부 실행 결과와 일치했다**

---

## 1. 최단 경로 문제

### 1.1 정의

원본 자료의 정의는 이렇다.

> 간선의 가중치가 있는 그래프에서 두 정점 사이의 경로들 중에 간선의 가중치의 합이 최소인 경로

두 가지 문제로 갈린다.

| 문제 | 뜻 | 알고리즘 |
|---|---|---|
| **단일 출발 최단 경로** | 하나의 시작 정점에서 다른 모든 정점까지 | 다익스트라 (음의 가중치 허용 안 함), 벨만-포드 (음의 가중치 허용, 음의 사이클 존재 여부 감지) |
| **모든 쌍 최단 경로** | 모든 정점에서 모든 정점까지 | 플로이드-워셜 |

원본 자료는 목차에서 "플로이드-워셜", 본문에서 "플로이드-워샬"로 표기가 섞여 있다. 이 노트는 **플로이드-워셜**로 통일한다.

### 1.2 관통선 — 완화(relax) 한 줄

세 알고리즘의 안쪽에는 똑같은 한 줄이 있다.

```python
if dist[u] + w < dist[v]:      # u 까지 온 거리에 간선 (u, v, w) 를 붙이면 v 가 더 가까워지는가
    dist[v] = dist[u] + w      # 그렇다면 v 의 거리를 줄인다 — 이것이 '완화'
```

`dist[v]` 는 **"지금까지 찾은 v 까지의 가장 짧은 거리"** 이고, 완화는 그 값을 한 간선만큼 **더 낮출 수 있는지** 묻는 것이다. 거리는 완화될 때마다 내려가기만 하고 절대 올라가지 않는다. 그래서 문제는 "어떤 간선을 어떤 순서로 완화하면 더 이상 내려갈 데가 없는 상태(= 진짜 최단 거리)에 가장 싸게 도달하는가"가 된다.

| 알고리즘 | 완화 순서 | 이 순서가 통하는 조건 |
|---|---|---|
| 다익스트라 | dist 가 가장 작은 정점을 꺼내 **그 정점의 진출 간선**만 완화. 한 번 꺼낸 정점은 다시 안 본다 | 가중치가 음수가 아니어야 "가장 가까운 정점의 거리는 이미 최종값"이 보장된다 |
| 벨만-포드 | **모든 간선을 한 바퀴** 완화하는 것을 V-1 번 | 조건 없음. 음수 사이클만 없으면 된다 (있으면 검출) |
| 플로이드-워셜 | **경유 정점 k 를 고정**하고 모든 (i, j) 를 `D[i][k] + D[k][j]` 로 완화. k 를 1부터 n 까지 | 조건 없음. 음수 사이클만 없으면 된다 (있으면 대각선이 음수가 된다) |

### 1.3 BFS 와의 관계

가중치가 전부 같은 그래프에서는 BFS 가 곧 최단 경로다. 큐에서 나오는 순서가 거리 순이기 때문이다. 가중치가 달라지면 그 보장이 깨진다 — "간선 하나 건너"가 "거리 1"이 아니게 되므로 큐의 앞쪽이 가장 가까운 정점이라는 성질이 사라진다. 다익스트라는 **그 큐를 우선순위 큐로 바꾼 BFS** 다. 가중치가 전부 같으면 다익스트라의 우선순위 큐는 그냥 큐처럼 동작한다.

- 근거와 반례는 [0825 DFS와 BFS 이론](0825_DFS와BFS_이론.md) 에 정리해 두었다
- 가중치가 0 과 1 뿐이면 덱 양끝 삽입(0-1 BFS)으로 우선순위 큐 없이 된다 — [0825 DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md)

---

## 2. 다익스트라 — 가장 가까운 정점부터 확정한다

### 2.1 개요 (자료 원문)

- 시작 정점에서 다른 모든 정점으로의 최단 경로를 구하는 알고리즘
- 시작 정점에서의 거리가 최소인 정점을 선택해 나가면서 최단 경로를 구하는 방식이다
- **탐욕 기법**을 사용한 알고리즘으로 MST 의 프림 알고리즘과 유사하다
- **우선순위 큐**를 활용한다

"프림과 유사하다"는 **정점을 하나씩 확정해 넓혀 가는 구조**가 같다는 뜻이다. 프림은 "트리에서 가장 가까운 정점"(간선 하나의 가중치)을, 다익스트라는 "시작점에서 가장 가까운 정점"(누적 거리)을 고른다. 고르는 기준만 다르고 뼈대는 같다. 프림은 [0910 최소 신장 트리](0910_최소신장트리.md) 에 있다.

우선순위 큐로 파이썬의 `heapq` 를 쓴다. 최소 힙이고, 리스트나 튜플을 넣으면 **첫 원소** 기준으로 정렬되므로 `[거리, 정점]` 순서로 넣어야 거리가 작은 것이 먼저 나온다 — [0908 힙과 백트래킹](0908_힙과백트래킹.md).

### 2.2 동작 순서 (자료 원문)

> 현재 상황에서 가장 좋은 선택을 반복해서 최종 해답을 구하는 방식

1. 시작 정점에서 각 정점까지의 최단 거리를 저장할 리스트를 생성 (모든 거리를 무한대로 초기화하고, 시작 정점의 거리는 0으로 설정)
2. 시작 정점과 거리를 우선순위 큐에 삽입
3. 가장 짧은 거리를 가진 정점을 추출하고, 추출한 정점과 인접한 정점을 모두 확인 (기존 거리보다 멀면 무시)
4. 인접한 정점까지 거리가 기존에 저장된 거리보다 작으면, 거리를 갱신하고 우선순위 큐에 삽입
5. 우선순위 큐가 모두 빌 때까지 3~4 과정 반복

3번이 "가장 가까운 정점부터", 4번이 완화다.

### 2.3 예제 — 12단계를 표 하나로

자료의 예제 그래프. 방향 그래프, 정점 6개, 간선 11개, 시작 정점 a.

```text
간선: a→b 3   a→c 5   b→c 2   c→b 1   b→d 6   c→d 4
      c→e 6   e→a 3   d→e 2   d→f 3   e→f 6

배치:        b ───6──→ d
           ↗ │↑        │ ↘3
        3 /  2│1       2   f
         a    │        ↓ ↗6
        5 \   ↓↓       e
           ↘  c ───6──→ (e→a 3 은 되돌아가는 긴 대각선)
              └────4───→ d
```

원본 자료는 12장에 걸쳐 진행하는데, 자료의 코드를 그대로 돌리며 큐와 dist 를 찍은 것이 아래 표다. (실행: `bench/s1_reproduce.py`)

| # | 사건 | 우선순위 큐 (정점:누적 거리) | a | b | c | d | e | f |
|---|---|---|---|---|---|---|---|---|
| 1 | (a, 0) 삽입 | a:0 | 0 | ∞ | ∞ | ∞ | ∞ | ∞ |
| 2 | **a 꺼냄** (0) | 비어 있음 | 0 | ∞ | ∞ | ∞ | ∞ | ∞ |
| 3 | a→b: 0+3 < ∞ → 갱신, (b, 3) 삽입 | b:3 | 0 | **3** | ∞ | ∞ | ∞ | ∞ |
| 4 | a→c: 0+5 < ∞ → 갱신, (c, 5) 삽입 | b:3 c:5 | 0 | 3 | **5** | ∞ | ∞ | ∞ |
| 5 | **b 꺼냄** (3) | c:5 | 0 | 3 | 5 | ∞ | ∞ | ∞ |
| 6 | b→c: 3+2 == 5 → 갱신 X | c:5 | 0 | 3 | 5 | ∞ | ∞ | ∞ |
| 7 | b→d: 3+6 = 9 < ∞ → 갱신, (d, 9) 삽입 | c:5 **d:9** | 0 | 3 | 5 | **9** | ∞ | ∞ |
| 8 | **c 꺼냄** (5) | d:9 | 0 | 3 | 5 | 9 | ∞ | ∞ |
| 9 | c→b: 5+1 = 6 > 3 → 갱신 X | d:9 | 0 | 3 | 5 | 9 | ∞ | ∞ |
| 10 | c→d: 5+4 == 9 → 갱신 X | d:9 | 0 | 3 | 5 | 9 | ∞ | ∞ |
| 11 | c→e: 5+6 = 11 < ∞ → 갱신, (e, 11) 삽입 | d:9 e:11 | 0 | 3 | 5 | 9 | **11** | ∞ |
| 12 | **d 꺼냄** (9) | e:11 | 0 | 3 | 5 | 9 | 11 | ∞ |
| 13 | d→e: 9+2 == 11 → 갱신 X | e:11 | 0 | 3 | 5 | 9 | 11 | ∞ |
| 14 | d→f: 9+3 = 12 < ∞ → 갱신, (f, 12) 삽입 | e:11 f:12 | 0 | 3 | 5 | 9 | 11 | **12** |
| 15 | **e 꺼냄** (11) | f:12 | 0 | 3 | 5 | 9 | 11 | 12 |
| 16 | e→a: 11+3 = 14 > 0 → 갱신 X | f:12 | 0 | 3 | 5 | 9 | 11 | 12 |
| 17 | e→f: 11+6 = 17 > 12 → 갱신 X | f:12 | 0 | 3 | 5 | 9 | 11 | 12 |
| 18 | **f 꺼냄** (12), 진출 간선 없음, 큐 비어 종료 | 비어 있음 | 0 | 3 | 5 | 9 | 11 | 12 |

최종 `dist = [0, 3, 5, 9, 11, 12]` — 자료의 마지막 장과 같다.

읽는 법:

- 꺼내는 순서 a(0) → b(3) → c(5) → d(9) → e(11) → f(12) 가 **거리 오름차순**이다. 이것이 "가장 가까운 정점부터"의 실체다
- 이 그래프에서는 큐에 정점이 **한 번씩만** 들어간다. 낡은 항목이 생기지 않아 pop 6회로 끝난다. 갱신이 여러 번 일어나는 그래프에서는 같은 정점이 큐에 여러 번 들어가고, 뒤에 꺼낸 것은 `continue` 로 버린다 (2.7절)
- 꺼낸 정점의 거리는 그 순간 **최종값**이다. 뒤에 꺼낸 정점의 거리가 더 크므로, 거기서 출발해 돌아와 봐야 더 줄일 수 없다 — 가중치가 음수가 아니라는 가정 위에서만 성립한다 (3절)

### 2.4 📌 큐에 들어가는 값은 간선 가중치가 아니라 누적 거리다

원본 자료의 6·7·8 단계 큐에는 `d` 의 값이 **6** 으로 적혀 있다. 그런데 `dist[d]` 는 `3 + 6 = 9` 이고, 코드는 `heapq.heappush(min_heap, [distance, adjacent])` 로 **누적 거리 `distance`** 를 넣는다. 같은 큐에서 `e` 는 11(누적)로 맞게 적혀 있어 자료 안에서도 일관되지 않는다. 6 은 간선 b→d 의 가중치다. 최종 dist 는 자료가 맞다.

이 오타가 다익스트라를 설명하기에 오히려 좋은 소재다. 큐에 간선 가중치를 넣으면 **"시작점에서 가장 가까운 정점"이 아니라 "직전 정점에서 가장 가까운 정점"** 을 꺼내게 되고, 그건 프림 알고리즘이다. 두 알고리즘의 차이가 정확히 "큐에 무엇을 넣는가"에 있다.

### 2.5 연습 그래프 (A~J)

자료의 연습 문제. 방향 그래프, 정점 10개, 간선 17개, 시작 A.

```text
A→B 4   A→C 6   C→B 3   B→D 9   B→E 8   C→E 2   C→F 3   E→D 2   E→F 1
E→G 3   E→H 7   D→G 6   F→H 4   F→I 8   G→J 13  H→J 9   I→J 4
```

| D_min | A | B | C | D | E | F | G | H | I | J |
|---|---|---|---|---|---|---|---|---|---|---|
| 자료 정답 | 0 | 4 | 6 | 10 | 8 | 9 | 11 | 13 | 17 | 21 |
| 코드 실행 | 0 | 4 | 6 | 10 | 8 | 9 | 11 | 13 | 17 | 21 |

전부 일치한다. 최단 경로 트리는 A→B, A→C, C→E, C→F, E→D, E→G, F→H, F→I, I→J 다. J 는 I 경유 17+4 = 21 이 H 경유 13+9 = 22, G 경유 11+13 = 24 보다 짧다. F 는 C 경유(6+3)와 E 경유(8+1)가 9 로 동점인데, 코드는 `<` 비교라 먼저 도달한 C 경유가 남는다.

### 2.6 자료 코드 (원문)

```python
import heapq, math

def dijkstra(graph, start):
    distances = {v: math.inf for v in graph}
    distances[start] = 0
    min_heap = []
    heapq.heappush(min_heap, [0, start])

    while min_heap:
        current_distance, current_vertex = heapq.heappop(min_heap)

        if distances[current_vertex] < current_distance: continue

        for adjacent, weight in graph[current_vertex].items():
            distance = current_distance + weight
            if distance < distances[adjacent]:
                distances[adjacent] = distance
                heapq.heappush(min_heap, [distance, adjacent])

    return distances
```

```python
graph = {
    'a': {'b': 3, 'c': 5}, 'b': {'c': 2},
    'c': {'b': 1, 'd': 4, 'e': 6}, 'd': {'e': 2, 'f': 3},
    'e': {'f': 6},'f': {}
}
start_v = 'a'
res = dijkstra(graph, start_v)
# {'a': 0, 'b': 3, 'c': 5, 'd': 9, 'e': 11, 'f': 12}
```

실행하면 주석 그대로 `{'a': 0, 'b': 3, 'c': 5, 'd': 9, 'e': 11, 'f': 12}` 가 나온다. **자료 주석의 출력값이 실제와 일치했다.**

> 📌 **코드의 `graph` 딕셔너리는 그림과 다르다.** 그림의 간선 11개 중 **b→d (6)** 과 **e→a (3)** 이 딕셔너리에 없다 (9개). b→d 로도 d 는 3+6 = 9 라 최종 거리는 같고, e→a 는 시작점으로 돌아가는 간선이라 거리에 영향이 없다. 그래서 주석의 결과는 맞다. 하지만 **(d, 9) 가 큐에 들어가는 시점**(그림: b 를 꺼낼 때, 코드: c 를 꺼낼 때)과 **경로 복원 결과**(그림: a→b→d→f, 코드: a→c→d→f)가 달라진다 (2.9절). 이 노트의 12단계 표는 그림의 11개 간선으로 돌린 것이다.

그림대로 쓰면 이렇다.

```python
graph = {
    'a': {'b': 3, 'c': 5}, 'b': {'c': 2, 'd': 6},
    'c': {'b': 1, 'd': 4, 'e': 6}, 'd': {'e': 2, 'f': 3},
    'e': {'a': 3, 'f': 6}, 'f': {}
}
```

### 2.7 코드 읽기

**`[distance, adjacent]` — 리스트를 힙에 넣는다.** `heapq` 는 원소끼리 `<` 로 비교하고, 리스트와 튜플은 첫 원소부터 사전순으로 비교된다. 첫 원소가 거리라 거리가 작은 항목이 먼저 나온다. 거리가 같으면 두 번째 원소(정점 이름)를 비교하는데, 정점 이름이 비교 불가능한 객체면 여기서 `TypeError` 가 난다. 문자열·정수면 문제없다. 튜플 `(distance, adjacent)` 를 쓰는 것이 더 흔하고 조금 가볍지만 동작은 같다.

**`if distances[current_vertex] < current_distance: continue` — 낡은 항목 버리기.** 같은 정점이 큐에 여러 번 들어갈 수 있다. dist 가 9 였다가 7 로 줄면 큐에는 `[9, v]` 와 `[7, v]` 가 둘 다 있다. `[7, v]` 를 먼저 꺼내 처리하고, 나중에 `[9, v]` 가 나오면 `distances[v] = 7 < 9` 라 건너뛴다. 큐에서 항목을 직접 지우는 대신 **꺼낼 때 낡았는지 보는** 방식이고, 이를 lazy deletion 이라 부른다.

이 줄에는 두 번째 의미가 있다. **"확정"이라는 개념이 없다.** 꺼낸 정점을 visited 집합에 넣어 다시 안 보는 것이 아니라, 나중에 더 짧은 거리로 다시 들어오면 **다시 처리한다.** 양수 가중치에서는 그런 일이 일어나지 않아 두 방식이 같지만, 음수 간선이 있으면 결과가 갈린다 (3절).

**`distances[adjacent]` 가 `math.inf` 인 정점** — `distance < inf` 는 항상 참이라 처음 닿는 정점은 무조건 갱신된다. 도달 불가능한 정점은 끝까지 `inf` 로 남는다.

### 2.8 복잡도

정점 V, 간선 E. 각 간선은 그 출발 정점을 꺼낼 때 한 번씩 검사되고, 갱신이 성공할 때만 힙에 push 한다. push 는 최대 E 번, pop 도 최대 E 번, 각각 `O(log E) = O(log V)`.

- 우선순위 큐(힙) 판: `O((V + E) log V)`
- 큐 없이 매번 리스트를 훑어 최소를 찾는 판: `O(V^2)` — 밀집 그래프(E ≈ V²)에서는 이쪽이 이론상 같거나 낫다. 실측은 11절

### 2.9 경로 복원 — `prev` 딕셔너리 (자료에 없는 것)

거리만이 아니라 경로도 필요하면 갱신할 때 "어디서 왔는가"를 같이 적는다.

```python
def dijkstra_path(graph, start):
    distances = {v: math.inf for v in graph}
    prev = {v: None for v in graph}                 # 추가
    distances[start] = 0
    min_heap = [(0, start)]
    while min_heap:
        current_distance, current_vertex = heapq.heappop(min_heap)
        if distances[current_vertex] < current_distance:
            continue
        for adjacent, weight in graph[current_vertex].items():
            distance = current_distance + weight
            if distance < distances[adjacent]:
                distances[adjacent] = distance
                prev[adjacent] = current_vertex     # 추가: 갱신될 때 함께 기록
                heapq.heappush(min_heap, (distance, adjacent))
    return distances, prev

def build_path(prev, target):
    path = []
    while target is not None:
        path.append(target)
        target = prev[target]
    return path[::-1]
```

그림 그래프(간선 11개)에서 돌리면 `prev = {a: None, b: a, c: a, d: b, e: c, f: d}`, a→f 는 **a → b → d → f** (거리 12). d 는 b 를 꺼낼 때 3+6 = 9 로 먼저 도달하고, 뒤에 c→d 가 5+4 = 9 로 같은 값을 내지만 `<` 비교에 걸리지 않아 `prev[d]` 는 b 로 남는다. 자료 코드의 딕셔너리(b→d 없음)로 돌리면 `prev[d] = c`, a→f 는 **a → c → d → f** 다. 거리는 같아도 경로가 다르다. **같은 거리의 경로가 둘이면 먼저 도달한 쪽이 남는다** — 문제가 "사전순으로 가장 앞선 경로" 같은 조건을 걸면 동점 규칙을 따로 넣어야 한다.

---

## 3. ⭐ 검증 — 다익스트라와 음수 간선: "깨진다"의 정확한 뜻

자료는 다익스트라를 "음의 가중치를 허용하지 않음"으로 소개한다. 정확히 무엇이 어떻게 깨지는지를 두 가지 구현으로 대조했다. (실행: `bench/s2_negative.py`)

### 3.1 확정 판(visited 집합)은 오답을 낸다

교과서식 구현 — 꺼낸 정점을 `done` 에 넣고 두 번 다시 보지 않는다.

```python
def dijkstra_visited(graph, start):
    distances = {v: math.inf for v in graph}
    distances[start] = 0
    min_heap = [(0, start)]
    done = set()
    while min_heap:
        d, u = heapq.heappop(min_heap)
        if u in done:
            continue
        done.add(u)                       # 여기서 '확정'
        for v, w in graph[u].items():
            if v in done:
                continue                  # 확정된 정점은 다시 보지 않는다
            if d + w < distances[v]:
                distances[v] = d + w
                heapq.heappush(min_heap, (d + w, v))
    return distances
```

반례 그래프: `A→B 1, A→C 5, C→B -10, B→D 1`. 정답은 A→C→B→D = 5 - 10 + 1 = **-4** 다.

```text
A 확정 (0)      → dist[B] = 1, dist[C] = 5
B 확정 (1)      → dist[D] = 2
D 확정 (2)
C 확정 (5)      → C→B: 5-10 = -5 < 1 인데 B 는 이미 확정 → 무시
```

결과 `{A: 0, B: 1, C: 5, D: 2}` — **D = 2, 오답.** B 를 1 로 확정한 뒤 C(5)를 거쳐 B 가 -5 가 되는 길을 발견하지만, 확정된 B 의 진출 간선 B→D 를 다시 보지 않는다. "지금 꺼낸 정점의 거리는 최종값"이라는 다익스트라의 전제가 음수 간선에서 무너진 것이다. 벨만-포드로 돌리면 `{A: 0, B: -5, C: 5, D: -4}` 다.

### 3.2 자료의 판은 정답을 낸다 — 그러나

자료의 코드(2.6절)는 `done` 이 없다. 낡은 항목만 버리고, **더 짧은 거리로 다시 들어온 정점은 다시 처리한다.** 같은 반례에서:

```text
A(0) 꺼냄 → B=1, C=5
B(1) 꺼냄 → D=2
C(5) 꺼냄 → B: -5 < 1 갱신, (B, -5) 삽입
B(-5) 꺼냄 → D: -4 < 2 갱신, (D, -4) 삽입      ← B 를 다시 처리한다
D(2) 꺼냄 → dist[D] = -4 < 2, 낡은 값 → 건너뜀
D(-4) 꺼냄 → 끝
```

결과 `{A: 0, B: -5, C: 5, D: -4}` — **정답.** 자료의 벨만-포드 예제 그래프(D→E = -3 포함)에 넣어도 `{A: 0, B: 3, C: 2, D: 5, E: 2, F: 4}` 로 벨만-포드와 같다 (pop 10회). 즉 자료의 코드는 음수 간선에서 **종종 맞는다.** 확정 판도 이 그래프에서는 우연히 맞았다.

그런데 **보장이 없다.** 정답을 내는 대신 시간을 잃는다.

### 3.3 지수 폭발 — 정점 하나에 pop 이 2배

그래프 가족: 정점 0..n, 간선 `0→i` 가중치 `2^i` (i = 1..n), 간선 `i→j` (1 ≤ j < i) 가중치 `-2^i`. DAG 라 음수 사이클은 없고 최단 거리는 잘 정의된다 (정점 j 의 최단 거리는 0→n→n-1→…→j 로 `2^(j+1) - 2^n`).

| n | 정점 | 간선 | 자료 판 pop | `2^(n-1) + n` | 시간 | 벨만-포드 순회 | 시간 | 일치 |
|---|---|---|---|---|---|---|---|---|
| 8 | 9 | 36 | 136 | 136 | 0.1 ms | 8 | 0.1 ms | O |
| 12 | 13 | 78 | 2,060 | 2,060 | 1.0 ms | 12 | 0.2 ms | O |
| 16 | 17 | 136 | 32,784 | 32,784 | 13.3 ms | 16 | 0.4 ms | O |
| 20 | 21 | 210 | 524,308 | 524,308 | 216.9 ms | 20 | 0.7 ms | O |
| 22 | 23 | 253 | 2,097,174 | 2,097,174 | 889.1 ms | 22 | 0.9 ms | O |
| 24 | 25 | 300 | **8,388,632** | 8,388,632 | **3,659.2 ms** | 24 | 1.2 ms | O |

pop 횟수가 정확히 `2^(n-1) + n` 이다. **정점 하나가 늘 때마다 2배.** dist 는 벨만-포드와 전부 일치했다 — 틀리는 것이 아니라 **느려지는** 것이다.

왜 그런가. n = 4 의 pop 순서를 보면 보인다.

```text
0:0 → 1:2 → 2:4 → 1:0 → 3:8 → 2:0 → 1:-4 → 4:16 → 3:0 → 1:-8 → 2:-8 → 1:-12
```

정점 k 를 (처음으로) 꺼낼 때마다 `-2^k` 간선이 그보다 작은 정점 1..k-1 의 거리를 **전부 다시 줄인다.** 그러면 1..k-1 이 통째로 다시 큐에 들어가고, 그 안에서 앞서 했던 일이 그대로 반복된다. `T(k) = T(k-1) + T(k-1) + …` 꼴이라 2배씩 는다. 양수 가중치에서는 "꺼낸 정점보다 뒤에 꺼낸 정점은 거리가 더 크므로 앞 정점을 다시 줄일 수 없다"가 보장되어 이 되먹임이 원천 차단된다. 음수 간선은 그 보장을 없앤다.

벨만-포드는 같은 그래프를 정점 순서 0, 1, …, n 으로 검사하면 순회 n 번(경로가 n→n-1→…→1 로 검사 순서와 반대라 순회마다 한 칸씩만 전진)이 들지만, 순회당 E 번 검사라 **다항식**이다. 정점 순서를 n, n-1, …, 0 으로 뒤집으면 **순회 3회**로 끝난다 (5절).

### 3.4 음수 사이클이 있으면 자료의 판은 끝나지 않는다

`A→B 1, B→C -2, C→A -1, C→D 5` (A→B→C→A 한 바퀴 = -2). 자료의 판은 A, B, C 의 거리가 한 바퀴마다 2 씩 줄어 영원히 돈다. pop 10,000회에서 강제로 끊었을 때 `dist = {A: -6666, B: -6665, C: -6665, D: -6660}` 이었다. 벨만-포드는 "음수 사이클 발생"을 돌려주고, 확정 판은 끝나기는 하지만 `{A: 0, B: 1, C: -1, D: 4}` 처럼 의미 없는 값을 낸다.

### 3.5 결론

| 구현 | 음수 간선 (사이클 없음) | 음수 사이클 |
|---|---|---|
| 확정 판 (visited) | **오답** 가능 (D = 2) | 끝나지만 값이 의미 없음 |
| 자료 판 (낡은 값만 버림) | **정답**, 그러나 **지수 시간** 가능 | **무한 루프** |
| 벨만-포드 | 정답, `O(VE)` | 검출 |

"음수 간선에서 다익스트라가 틀린다"는 말은 구현에 따라 **오답**이거나 **지수 시간**이라는 뜻이다. 둘 다 벨만-포드가 해결한다.

---

## 4. 벨만-포드 — 모든 간선을 V-1 번

### 4.1 개요 (자료 원문)

- 시작 정점에서 다른 모든 정점으로의 최단 경로를 구하는 알고리즘
- **음수 가중치**를 갖는 간선이 있는 그래프에서도 동작
- 다익스트라 알고리즘과 달리, 탐욕 기법 대신 **동적 프로그래밍(DP) 접근**을 사용한다
- 우선순위 큐를 사용하지 않는다

"DP" 는 부분 문제가 **"간선을 최대 k 개 써서 갈 수 있는 최단 거리"** 라는 뜻이다. k 번째 순회가 끝나면 그 답이 나와 있고, k+1 번째 순회는 그 위에 간선 하나를 더 얹는다.

### 4.2 동작 순서 (자료 원문)

1. 시작 정점에서 각 정점까지의 최단 거리를 저장할 리스트 생성 (모든 거리를 무한대로 초기화하고, 시작 정점의 거리는 0으로 설정)
2. 모든 간선을 반복해서 검사하고, 각 간선을 통해 더 짧은 경로가 발견되면 거리를 업데이트
   - **1번째 반복:** 시작 정점에서 한 개의 간선만으로 갈 수 있는 모든 정점들의 최단 거리가 갱신
   - **2번째 반복:** 시작 정점에서 최대 두 개의 간선만으로 갈 수 있는 모든 정점들의 최단 거리가 갱신
   - **k번째 반복:** 시작 정점에서 최대 k개의 간선만으로 갈 수 있는 모든 정점들의 최단 거리가 갱신
3. 마지막 정점을 제외한 모든 정점에 대해서 2번 과정 반복 (v - 1번 탐색)
4. 마지막으로 한 번 더 모든 간선을 검사하여 거리가 갱신되면, 음수 사이클이 존재한다는 것을 의미

### 4.3 예제 — 17장을 표 하나로

자료의 예제 그래프. 방향 그래프, 정점 6개(A~F), 간선 10개, **D→E = -3** 이 음수, 시작 A.

```text
간선: A→B 4   A→C 2   B→C 3   C→B 1   B→D 2
      B→E 3   C→D 4   C→E 5   D→E -3  E→F 2

배치:   B ──2──→ D
      ↗ │↑  ↘3    │-3
    4/  3│1   ↘   ↓
    A    │↓     ↘ E ──2──→ F
    2\   C ──5──→ ↑
      ↘  └───4───→ D
```

자료의 코드는 간선을 `for u in graph: for v, weight in graph[u].items()` 순서, 즉 **A, B, C, D, E 의 진출 간선 순**으로 검사한다. 순회마다 간선 10개를 전부 보는데, 아래 표는 갱신이 일어난 검사만 적었다. (실행: `bench/s1_reproduce.py`)

| 순회 | 검사 (갱신된 것만) | A | B | C | D | E | F |
|---|---|---|---|---|---|---|---|
| 시작 | | 0 | ∞ | ∞ | ∞ | ∞ | ∞ |
| 1 | A→B 0+4 < ∞ · A→C 0+2 < ∞ | 0 | **4** | **2** | ∞ | ∞ | ∞ |
| 1 | B→D 4+2 < ∞ · B→E 4+3 < ∞ (B→C 7 > 2 갱신 X) | 0 | 4 | 2 | **6** | **7** | ∞ |
| 1 | C→B 2+1 < 4 (C→D 6 == 6, C→E 7 == 7 갱신 X) | 0 | **3** | 2 | 6 | 7 | ∞ |
| 1 | D→E 6-3 < 7 | 0 | 3 | 2 | 6 | **3** | ∞ |
| 1 | E→F 3+2 < ∞ | 0 | 3 | 2 | 6 | 3 | **5** |
| 2 | A→B 4 > 3, A→C 2 == 2 갱신 X | 0 | 3 | 2 | 6 | 3 | 5 |
| 2 | B→D 3+2 < 6 (B→C 6 > 2, B→E 6 > 3 갱신 X) | 0 | 3 | 2 | **5** | 3 | 5 |
| 2 | C→B 3 == 3, C→D 6 > 5, C→E 7 > 3 갱신 X | 0 | 3 | 2 | 5 | 3 | 5 |
| 2 | D→E 5-3 < 3 | 0 | 3 | 2 | 5 | **2** | 5 |
| 2 | E→F 2+2 < 5 | 0 | 3 | 2 | 5 | 2 | **4** |
| 3 | 간선 10개 전부 갱신 X → **변화 없음, 중단** | 0 | 3 | 2 | 5 | 2 | 4 |

최종 `A=0 B=3 C=2 D=5 E=2 F=4`. 자료의 마지막 장(순회 3 에서 변화 없음 → 중단)과 같다.

읽는 법:

- 순회 1 안에서 B 가 4 → 3 으로, E 가 7 → 3 으로 **같은 순회 안에서 두 번** 줄었다. "k 번째 순회 = 간선 k 개까지"는 **최소 보장**이지 상한이 아니다. 검사 순서가 경로 방향과 맞으면 한 순회에 여러 간선이 연쇄로 반영된다 (5절)
- D 는 순회 1 에서 B 경유 6, 순회 2 에서 갱신된 B(3) 경유 5. E 는 D 가 줄자 D→E(-3) 로 따라 줄고, F 는 E 를 따라 준다. 음수 간선 하나가 **뒤에 있는 정점을 순회 하나씩 늦게** 끌어내린다
- 순회 3 은 아무것도 바꾸지 않았다. 코드는 이 순회에서 `updated` 가 False 라 `break` 한다 — **실행된 순회 3회 = 갱신 순회 2회 + 확인 순회 1회**

### 4.4 📌 순회 2 표의 오타

원본 자료의 순회 2 표에는 두 군데 계산식이 어긋나 있다. 결과(갱신 없음)는 맞다.

| 위치 | 원본 | 맞는 식 |
|---|---|---|
| B 검사, c 셀 | `3+3<2` | `3+3 = 6 > 2` (6 < 2 는 거짓) |
| C 검사, b·d·e 셀 | `3+1>3`, `3+4>5`, `3+5>3` | dist[C] = 2 이므로 `2+1 == 3`, `2+4 = 6 > 5`, `2+5 = 7 > 3` |

순회 3 의 같은 자리는 `3+3>2`, `2+1==3 / 2+4>5 / 2+5>2` 로 바르게 적혀 있다. 실행해서 확인했다.

### 4.5 자료 코드 (원문)

```python
def bellman_ford(graph, start):
    v_count = len(graph)
    distances = {v: float('inf') for v in graph}
    distances[start] = 0

    for i in range(v_count - 1):
        updated = False
        for u in graph:
            for v, weight in graph[u].items():
                if distances[u] != float('inf') and distances[u] + weight < distances[v]:
                    distances[v] = distances[u] + weight
                    updated = True
        if not updated:
            break

    for u in graph:
        for v, weight in graph[u].items():
            if distances[u] != float('inf') and distances[u] + weight < distances[v]:
                return "음수 사이클 발생"

    return distances
```

```python
# 예시 그래프
graph = {
    'A': {'B': 4, 'C': 2},
    'B': {'C': 3, 'D': 2, 'E': 3},
    'C': {'B': 1, 'D': 4, 'E': 5},
    'D': {'E': -3},
    'E': {'F': 2},
    'F': {}
}

# 시작 정점 설정
start_vertex = 'A'

# 벨만-포드 알고리즘 실행
result = bellman_ford(graph, start_vertex)

print(f"'{start_vertex}': {result}")
```

실행 결과 `'A': {'A': 0, 'B': 3, 'C': 2, 'D': 5, 'E': 2, 'F': 4}` — **자료의 최종 표와 일치했다.** 이번 코드의 `graph` 는 그림의 간선 10개와 전부 같다.

코드 읽기:

- `distances[u] != float('inf')` — 아직 닿지 않은 정점에서 나가는 간선은 건너뛴다. 이게 없어도 `inf + w < inf` 는 거짓이라 결과는 같지만, `inf + (-3)` 같은 계산을 피하고 "닿지 않은 정점은 검사하지 않는다"는 뜻을 분명히 한다
- `updated` 플래그와 `break` — **조기 종료.** 한 순회에서 아무것도 안 바뀌면 다음 순회도 안 바뀐다 (입력이 같으니 출력도 같다). 이 한 줄이 실제 성능을 정한다 (5절: 경로 그래프에서 500배)
- 마지막 블록 — V-1 순회가 끝난 뒤 **한 번 더** 검사해 줄어드는 간선이 있으면 음수 사이클이다 (7절)
- 변수 `i` 는 쓰이지 않는다. `for _ in range(v_count - 1)` 로 써도 된다

### 4.6 왜 V-1 번인가

정점이 V 개면 단순 경로(정점을 두 번 지나지 않는 경로)의 간선 수는 **최대 V-1** 이다. 음수 사이클이 없으면 최단 경로는 단순 경로다 (사이클을 돌아 봐야 합이 0 이상이라 빼는 게 낫다). 그러므로 "간선 k 개까지"를 V-1 까지 밀면 모든 최단 경로가 반영된다.

순회 k 가 끝났을 때 **"간선 k 개 이하로 갈 수 있는 최단 거리"가 전부 반영되어 있다**는 것이 귀납으로 증명된다. 순회 k 에서 최단 경로의 k 번째 간선 (u, v) 를 검사할 때, dist[u] 에는 이미 앞 k-1 개 간선의 합(순회 k-1 까지의 결과)이 들어 있으므로 dist[v] 가 그 합 + w 이하가 된다. 검사 순서가 어떻든 성립한다 — 순서는 **더 빨리 끝나느냐**만 정한다.

### 4.7 복잡도

순회 하나가 `O(E)`, 최대 V-1 순회 → `O(VE)`. 밀집 그래프(E ≈ V²)면 `O(V^3)`. 하지만 조기 종료 덕에 실제 순회 수는 "최단 경로의 최대 간선 수 + 1" 근처에서 끝난다. 6절에서 V = 2,000 / E = 10,000 무작위 그래프의 실제 순회는 9회였다.

---

## 5. ⭐ 검증 — 벨만-포드의 순회 수는 간선 검사 순서가 정한다

(실행: `bench/s3_bf_order.py`)

### 5.1 자료 그래프의 간선 10개를 무작위로 섞어 10,000번

`random.seed(2026)`. 어떤 순서든 dist 는 같았다 (10,000건 전부 자료 결과와 일치). 달라지는 것은 순회 수다.

| 갱신이 있었던 순회 수 | 건수 | 비율 |
|---|---|---|
| 1회 | 85 | 0.9% |
| 2회 | 2,148 | 21.5% |
| **3회** | **5,549** | **55.5%** |
| 4회 | 2,121 | 21.2% |
| 5회 | 97 | 1.0% |

최소 1, 최대 5, 평균 3.00. 자료 순서(A~E 정점 순)는 2회에 해당한다.

왜 1 에서 5 까지 퍼지는가. 최단 경로 트리의 가장 긴 경로가 A→C→B→D→E→F 로 **간선 5개**다. 이 다섯 간선이 검사 순서상 **앞에서 뒤로** 놓이면 한 순회 안에서 A 의 값이 F 까지 연쇄로 흘러 1회에 끝나고, **뒤에서 앞으로** 놓이면 순회마다 한 간선씩만 전진해 5회가 든다.

- 최선 순서 예: `BC CE AC CB AB BD DE EF BE CD` — AC, CB, BD, DE, EF 가 이 순서로 들어 있다
- 최악 순서 예: `BE EF DE CE CD BD CB AB AC BC` — EF, DE, BD, CB, AC 가 거꾸로다

### 5.2 경로 그래프 V = 2,000 — 정방향 1회, 역방향 1,999회

간선 `0→1→2→…→1999` (가중치 전부 1). 간선 리스트를 정방향으로 주면 순회 1 안에서 0 의 값이 1999 까지 한 번에 흐르고, 역방향으로 주면 순회마다 한 칸씩만 전진한다. 3회 중앙값.

| 조건 | 갱신 순회 | 실행 순회 | 시간 |
|---|---|---|---|
| 정방향 + 조기 종료 | 1 | 2 | **0.6 ms** |
| 정방향 + 조기 종료 없음 | 1 | 1,999 | 314.7 ms |
| 역방향 + 조기 종료 | 1,999 | 1,999 | 232.2 ms |
| 역방향 + 조기 종료 없음 | 1,999 | 1,999 | 239.4 ms |

- 정방향은 **1회**, 역방향은 정확히 **V-1 = 1,999회**. 이론상 최악(V-1)이 실제로 나오는 입력이다
- `updated` 플래그 한 줄이 정방향에서 **500배** 차이(0.6 ms 대 314.7 ms)를 만든다. 플래그가 없으면 이미 끝난 답을 1,998번 다시 확인한다
- 역방향에서는 플래그가 있어도 소용없다 — 매 순회 갱신이 있으니까
- 정방향·조기 종료 없음(314.7 ms)이 역방향(232.2 ms)보다 느린 것은 조건식 `distances[u] != inf and …` 때문이다. 역방향에서는 대부분의 정점이 오래 ∞ 로 남아 `and` 뒤의 덧셈이 생략되고, 정방향에서는 첫 순회 뒤 전부 유한값이라 덧셈과 비교를 매번 한다

정점 번호를 알고 있다면 (예: DAG 의 위상 순서) 그 순서로 간선을 정렬해 주는 것만으로 벨만-포드가 몇 배씩 빨라진다. 3.3절의 지수 폭발 그래프에서도 정점 순서를 뒤집자 순회가 20회에서 3회로 줄었다.

---

## 6. ⭐ 검증 — 벨만-포드 vs 다익스트라 (양수 가중치, 같은 그래프)

(실행: `bench/s4_bf_vs_dij.py`) 무작위 방향 그래프, 가중치 1~100, `random.seed(2026)`, 시작 정점에서 전부 도달 가능하게 트리 간선을 먼저 깔았다. 3회 중앙값.

| V | E | 다익스트라 | 벨만-포드 (조기 종료) | 배수 | BF 실제 순회 | 벨만-포드 (조기 종료 없음) |
|---|---|---|---|---|---|---|
| 500 | 2,500 | 0.8 ms | 3.9 ms | 5.1배 | 8 | 202.3 ms |
| 1,000 | 5,000 | 1.3 ms | 8.5 ms | 6.6배 | 9 | 831.6 ms |
| **2,000** | **10,000** | **3.1 ms** | **17.6 ms** | **5.7배** | **9** | **3,357.3 ms** |

- 세 조건 모두 dist 가 전부 일치했다
- 벨만-포드의 실제 순회 수는 V-1 = 1,999 가 아니라 **9** 다. 무작위 그래프에서 최단 경로의 간선 수가 10개를 넘지 않았다. `O(VE)` 라는 최악 복잡도와 실제 비용의 차이가 크다
- 그래도 순회마다 E = 10,000 개 간선을 전부 다시 보므로 다익스트라(간선 하나당 한 번)보다 **5~7배** 느리다
- 조기 종료를 빼면 1,999 × 10,000 번 검사 → 3,357.3 ms. 다익스트라의 **1,083배**

같은 양수 그래프면 다익스트라가 답이고, 벨만-포드는 **음수 간선이 있을 때** 꺼내는 도구다. 다만 그 벨만-포드도 조기 종료 없이는 쓸 물건이 아니다.

---

## 7. 음수 사이클 검출

### 7.1 벨만-포드 — V 번째 검사에서 또 줄어들면

V-1 순회 뒤에는 간선 V-1 개 이하인 모든 경로가 반영되어 있다 (4.6절). V 번째 검사에서 **또** 줄어드는 간선이 있다면 간선 V 개 이상인 경로가 더 짧다는 뜻이고, 정점이 V 개인 그래프에서 간선 V 개짜리 경로는 반드시 어떤 정점을 두 번 지난다 — 사이클을 돌았는데 더 짧아졌으니 **음수 사이클**이다.

### 7.2 플로이드-워셜 — 대각선이 음수면

`dist[i][i]` 는 0 으로 시작한다 (i 에서 i 로 아무 데도 안 가는 경로). 이것이 음수가 됐다는 것은 i 를 떠나 i 로 돌아오는 길의 합이 음수라는 뜻이다. 알고리즘이 끝난 뒤 `dist[i][i] < 0` 인 i 가 있으면 i 는 음수 사이클 위에 있다.

### 7.3 📌 자료의 플로이드-워셜 코드 예시 그래프에 음수 사이클이 있다

자료의 플로이드-워셜 코드(8.5절)에 딸린 예시 그래프(6정점, 0~5):

```text
0→1 3   1→2 1   2→3 7   2→5 2   3→5 3   4→2 -4   4→3 2   5→4 1
```

**2→5→4→2 = 2 + 1 + (-4) = -1.** 자료 자신이 개요에서 "음수 가중치가 있어도 정상적으로 동작 (음수 사이클은 X)" 라고 전제해 놓고, 코드 예시로는 음수 사이클이 든 입력을 줬다. 자료에 기대 출력은 적혀 있지 않다. 실행하면 이렇게 나온다. (실행: `bench/s5_negcycle.py`)

```text
[0, 3, 3, 9, 7, 5]
[inf, 0, 0, 6, 4, 2]
[inf, inf, -1, 5, 3, 1]
[inf, inf, 0, 0, 4, 2]
[inf, inf, -5, 1, -1, -3]
[inf, inf, -4, 2, 0, -2]
```

대각선 `[0, 0, -1, 0, -1, -2]` — 정점 2, 4, 5 가 음수이고, 정확히 사이클 위의 정점이다. 이 값들은 "최단 거리"가 아니다 (사이클을 더 돌면 더 작아지므로 최단 거리가 정의되지 않는다). `dist[5][5]` 가 -1 이 아니라 -2 인 것도 그 증거다. k=4 단계에서 5→4→2→5 로 -1 이 됐고, k=5 단계에서 `dist[5][5] = dist[5][5] + dist[5][5] = -1 + -1` 로 **사이클을 두 바퀴 돈 값**이 됐다.

이 예시를 고칠 필요는 없다. 뒤집으면 **검출법의 시연**이 된다.

같은 그래프를 딕셔너리로 바꿔 벨만-포드에 넣으면 (시작 0):

```text
5회 순회 뒤 dist = {0: 0, 1: 3, 2: 2, 3: 8, 4: 5, 5: 4}
6번째 검사에서 또 줄어드는 간선: 4→2 (5-4 = 1 < 2), 4→3 (5+2 = 7 < 8)
→ "음수 사이클 발생"
```

순회를 더 돌리면 값이 계속 내려간다: 순회 6 에서 dist[2] = 1, 순회 8 에서 0, 순회 12 에서 -2.

사이클을 없앤 판 — `4→2` 를 -4 에서 **-2** 로 바꾸면 (2→5→4→2 = +1):

```text
벨만-포드: 6번째 검사에서 줄어드는 간선 없음 → {0: 0, 1: 3, 2: 4, 3: 9, 4: 7, 5: 6}
플로이드-워셜:
[0, 3, 4, 9, 7, 6]
[inf, 0, 1, 6, 4, 3]
[inf, inf, 0, 5, 3, 2]
[inf, inf, 2, 0, 4, 3]
[inf, inf, -2, 2, 0, 0]
[inf, inf, -1, 3, 1, 0]
대각선 전부 0 → 정상
```

음수 간선(4→2 = -2)이 있어도 사이클 합이 양수면 두 알고리즘 모두 정상이다. 벨만-포드의 0행(`0, 3, 4, 9, 7, 6`)과 플로이드의 첫 행이 같다.

---

## 8. 플로이드-워셜 — 경유 정점을 하나씩 늘리며 모든 쌍

### 8.1 개요 (자료 원문)

- 모든 정점 쌍 간의 최단 경로를 구하는 알고리즘
  - 다익스트라, 벨만포드는 정점과 정점 간의 최단 경로를 구하는 알고리즘
- 동적 프로그래밍을 사용하여 최단 경로를 점진적으로 갱신
- 음수 가중치가 있어도 정상적으로 동작 (음수 사이클은 X)
- 다익스트라 알고리즘과 시간 복잡도가 동일하지만 구현이 간단하다

마지막 항목은 근거 없이 적혀 있다. "구현이 간단하다"는 맞다 (삼중 루프 네 줄). "시간 복잡도가 동일하다"는 **어떤 조건에서 같은 말이 되는지**를 10절에서 실측으로 판정한다.

### 8.2 동작 원리 — 부분 문제 정의 (자료 6장)

> 동적 계획 알고리즘으로 모든 쌍 최단 경로 문제를 해결하려면 먼저 부분문제들을 찾아야 한다. 이를 위해 일단 그래프의 정점의 수가 적을 때를 생각해보자.

**정점이 3개일 때.** 정점 i 에서 j 까지의 최단 경로는 두 가지 중 짧은 것이다 — i 에서 j 로 **직접** 가는 경로와 **정점 1 을 경유**하는 경로.

```text
        1
      ↗   ↘
    i ───→ j        D[i][j] = min( D[i][j],  D[i][1] + D[1][j] )
```

**핵심 아이디어.** 경유 가능한 정점들을 정점 1 로부터 시작하여, 정점 1 과 2, 그 다음엔 정점 1, 2, 3 으로 **하나씩 추가**하여, 마지막에는 정점 1~n 까지의 모든 정점을 경유 가능한 정점으로 고려하면서 모든 쌍의 최단 경로의 거리를 계산한다. (DP)

**부분 문제 정의.** 입력 그래프의 정점을 각각 1, 2, 3, ···, n 이라 하자.

$$D^{k}_{ij} = \text{정점 } \{1, 2, \cdots, k\} \text{ 만을 경유 가능한 정점으로 고려하여, 정점 } i \text{ 로부터 정점 } j \text{ 까지의 모든 경로 중 가장 짧은 경로의 거리}$$

- $k = 0$: 정점 0 은 그래프에 없으므로 **어떤 정점도 경유하지 않는다**는 뜻. 따라서 $D^{0}_{ij}$ 는 입력으로 주어지는 간선 (i, j) 의 가중치다
- $D^{1}_{ij}$ 는 i 에서 정점 1 을 경유하여 j 로 가는 경로와 i 에서 j 로 직접 가는 경로 중 짧은 거리. 모든 쌍 i, j 에 대하여 $D^{1}_{ij}$ 를 계산하는 것이 **가장 작은 부분 문제들**이다 (단 i ≠ 1, j ≠ 1)
- 그 다음엔 i 에서 정점 2 를 경유하여 j 로 가는 경로의 거리와 $D^{1}_{ij}$ 중 짧은 거리를 $D^{2}_{ij}$ 로 정한다. 정점 2 를 경유하는 경로의 거리는 $D^{1}_{i2} + D^{1}_{2j}$ (단 i ≠ 2, j ≠ 2)
- k 를 계속 늘려, 정점 i 에서 정점 k 를 경유하여 j 로 가는 경로의 거리와 $D^{k-1}_{ij}$ 중 짧은 거리를 $D^{k}_{ij}$ 로 정한다. 정점 k 를 경유하는 경로의 거리는 $D^{k-1}_{ik} + D^{k-1}_{kj}$ (i ≠ k, j ≠ k)

$$D^{k}_{ij} = \min\left(D^{k-1}_{ij},\; D^{k-1}_{ik} + D^{k-1}_{kj}\right)$$

이런 방식으로 k 가 1 에서 n 이 될 때까지 계산해서 $D^{n}_{ij}$, 즉 **모든 정점을 경유 가능한 정점으로 고려한** 모든 쌍 i, j 의 최단 경로의 거리를 찾는 것이 플로이드-워셜 알고리즘이다.

자료의 마지막 원리 그림은 첨자 표기를 배열 표기로 바꾼다.

```text
new D[i][j] = min( old D[i][j],  D[i][k] + D[k][j] )
```

**왜 위첨자를 떼고 행렬 하나로 되는가.** 점화식은 $D^{k-1}$ 에서 $D^{k}$ 를 만든다. 그런데 k 단계에서 쓰는 값 $D^{k-1}_{ik}$, $D^{k-1}_{kj}$ 는 **k 행과 k 열**이고, 이 값들은 k 단계에서 바뀌지 않는다 ($D^{k}_{ik} = \min(D^{k-1}_{ik}, D^{k-1}_{ik} + D^{k-1}_{kk})$ 인데 $D_{kk} = 0$ 이라 그대로). 그래서 k 단계를 진행하는 도중에 "이미 갱신된 k 행·k 열 값"을 읽어도 "갱신 전 값"과 같다. 행렬 하나를 제자리에서 덮어써도 된다.

**왜 "경유 정점 k 를 지나는 경로는 (i→k 부분) + (k→j 부분)" 으로 쪼개지는가.** 최단 경로가 k 를 지난다면, 그 앞부분 i→k 와 뒷부분 k→j 도 각각 최단이어야 한다 (아니면 그 부분을 바꿔 더 짧게 만들 수 있다). 그리고 두 부분 모두 k 를 **중간에** 지나지 않는다 (음수 사이클이 없으면 같은 정점을 두 번 지나는 게 이득일 수 없다). 그래서 두 부분은 {1..k-1} 만 경유하는 경로, 즉 $D^{k-1}$ 이다. 최적 부분 구조.

### 8.3 동작 과정 — K = 0 ~ 5 행렬

자료의 예제. 방향 그래프, 정점 5개(v1~v5), 간선 14개, 음수 간선 v4→v1 = -2, v5→v2 = -3.

```text
v1→v2 4   v1→v3 2   v3→v1 1   v1→v4 5   v4→v1 -2   v3→v2 3   v2→v3 1
v2→v5 4   v5→v2 -3  v3→v4 1   v3→v5 2   v5→v3 3    v5→v4 1   v4→v5 2

배치:  v1 ───4───→ v2
        │↑  ↘2 ↗3   │↑
       5│-2  v3   4│-3
        ↓│ ↙1  ↘2  ↓│
       v4 ←──1──── v5     (v4→v5 2 는 아래쪽 평행선, v3→v4 1, v5→v3 3 은 대각선)
```

`bench/s1_reproduce.py` 로 K 단계마다 행렬을 찍어 자료의 여섯 표와 셀 단위로 대조했다. **K=1 부터 K=5 까지 전부 일치.** 굵은 셀이 그 단계에서 갱신된 것이다.

**K=0 (입력 인접 행렬)**

| D | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 1 | 0 | 4 | 2 | 5 | ∞ |
| 2 | ∞ | 0 | 1 | ∞ | 4 |
| 3 | 1 | 3 | 0 | 1 | 2 |
| 4 | -2 | ∞ | ∞ | 0 | 2 |
| 5 | ∞ | -3 | 3 | 1 | 0 |

**K=1** — v1 을 경유. 갱신 2개: D[4,2] = min{∞, -2+4} = **2**, D[4,3] = min{∞, -2+2} = **0**. v1 로 들어가는 간선은 v3→v1, v4→v1 뿐이고 v3 은 이미 v2, v4, v5 로 직접 가는 간선이 더 짧아 갱신이 v4 행에서만 일어난다.

| D | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 1 | 0 | 4 | 2 | 5 | ∞ |
| 2 | ∞ | 0 | 1 | ∞ | 4 |
| 3 | 1 | 3 | 0 | 1 | 2 |
| 4 | -2 | **2** | **0** | 0 | 2 |
| 5 | ∞ | -3 | 3 | 1 | 0 |

**K=2** — v2 를 경유. D[1,5] = 1→2→5 = 4+4 = **8**, D[5,3] = 5→2→3 = -3+1 = **-2**.

| D | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 1 | 0 | 4 | 2 | 5 | **8** |
| 2 | ∞ | 0 | 1 | ∞ | 4 |
| 3 | 1 | 3 | 0 | 1 | 2 |
| 4 | -2 | 2 | 0 | 0 | 2 |
| 5 | ∞ | -3 | **-2** | 1 | 0 |

**K=3** — v3 을 경유. 갱신 7개: D[1,4] 5→**3**, D[1,5] 8→**4**, D[2,1] ∞→**2**, D[2,4] ∞→**2**, D[2,5] 4→**3**, D[5,1] ∞→**-1**, D[5,4] 1→**-1**. v3 이 그래프의 중심(모든 정점과 연결)이라 가장 많이 바뀐다. D[1,5] 는 K=2 에서 8 이 됐다가 다시 4 로 — **한 셀이 여러 단계에서 거듭 갱신**될 수 있다.

| D | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 1 | 0 | 4 | 2 | **3** | **4** |
| 2 | **2** | 0 | 1 | **2** | **3** |
| 3 | 1 | 3 | 0 | 1 | 2 |
| 4 | -2 | 2 | 0 | 0 | 2 |
| 5 | **-1** | -3 | -2 | **-1** | 0 |

**K=4** — v4 를 경유. 갱신 3개, 전부 **1열**: D[2,1] 2→**0**, D[3,1] 1→**-1**, D[5,1] -1→**-3**. v4→v1 = -2 라는 음수 간선이 "v4 까지 갈 수 있으면 v1 은 그보다 2 싸다"를 모든 행에 퍼뜨린다.

| D | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 1 | 0 | 4 | 2 | 3 | 4 |
| 2 | **0** | 0 | 1 | 2 | 3 |
| 3 | **-1** | 3 | 0 | 1 | 2 |
| 4 | -2 | 2 | 0 | 0 | 2 |
| 5 | **-3** | -3 | -2 | -1 | 0 |

**K=5 (최종)** — v5 를 경유. 갱신 3개, 전부 **2열**: D[1,2] 4→**1**, D[3,2] 3→**-1**, D[4,2] 2→**-1**. 이번엔 v5→v2 = -3 이 2열을 끌어내린다. 자료의 "총 3개의 원소가 갱신되고, 이것이 주어진 입력에 대한 최종해"와 같다.

| D | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 1 | 0 | **1** | 2 | 3 | 4 |
| 2 | 0 | 0 | 1 | 2 | 3 |
| 3 | -1 | **-1** | 0 | 1 | 2 |
| 4 | -2 | **-1** | 0 | 0 | 2 |
| 5 | -3 | -3 | -2 | -1 | 0 |

D[1,2] = 1 은 직접 간선 4 가 아니라 **v1→v3→v5→v2 = 2 + 2 - 3** 이다. 대각선은 끝까지 전부 0 — 음수 사이클이 없다는 뜻이다.

### 8.4 📌 K=1 갱신 목록의 D[5,2]

원본 자료의 K=1 갱신 목록에 `D[5,2] = min{D[5,2], D[5,1]+D[1,2]} = min{3, ∞+4} = 3` 이라고 적혀 있다. D[5,2] 의 기존 값은 **-3** 이므로 `min{-3, ∞+4} = -3` 이 맞다. 표(K=0, K=1 모두)에는 -3 으로 바르게 적혀 있고 수식 줄에서만 부호가 빠졌다. 실행해서 확인했다.

같은 목록은 i=1 행, j=1 열, i=j 대각선을 뺀 12개 셀만 다룬다. k=1 단계에서 1행·1열은 `D[1][1] = 0` 을 거치는 셈이라 바뀔 수 없고, 대각선은 (음수 사이클이 없는 한) 0 이라 바뀌지 않는다. 코드는 그런 구분 없이 25개 셀을 전부 검사하지만 결과는 같다.

### 8.5 자료 코드 (원문)

```python
INF = float('inf')
def floyd_warshall(graph):
    v_len = len(graph)
    dist = [[INF] * v_len for _ in range(v_len)]

    for i in range(v_len):
        dist[i][i] = 0

    for u in range(v_len):
        for v in range(v_len):
            if graph[u][v] != 0:
                dist[u][v] = graph[u][v]

    for k in range(v_len):
        for i in range(v_len):
            for j in range(v_len):
                if dist[i][k] + dist[k][j] < dist[i][j]:
                    dist[i][j] = dist[i][k] + dist[k][j]

    return dist
```

```python
# 예시 그래프 (인접 행렬)
graph = [
    [0, 3, INF, INF, INF, INF],
    [INF, 0, 1, INF, INF, INF],
    [INF, INF, 0, 7, INF, 2],
    [INF, INF, INF, 0, INF, 3],
    [INF, INF, -4, 2, 0, INF],
    [INF, INF, INF, INF, 1, 0]
]

result = floyd_warshall(graph)
for row in result:
    print(row)
```

이 코드에 8.3절의 5정점 행렬을 넣으면 K=5 최종 표와 셀 단위로 일치한다. 코드 자체는 맞다. 딸린 예시 그래프는 7.3절에서 다뤘다 — 음수 사이클이 있어 출력의 대각선이 `[0, 0, -1, 0, -1, -2]` 가 된다.

코드 읽기:

- `if graph[u][v] != 0:` — 0 이 아닌 값을 복사한다는 뜻인데 `INF != 0` 도 참이라 INF 도 그대로 복사된다. `dist` 초기값이 INF 라 결과는 같다. 다만 이 조건은 **가중치 0 인 간선을 "없는 간선"으로 취급**한다. 가중치 0 이 유효한 문제에서는 `if u != v and graph[u][v] != INF` 로 써야 한다
- `dist[i][k] + dist[k][j]` — `INF + INF`, `INF + (-4)` 모두 `inf` 라 비교에서 자연히 걸러진다. 파이썬의 `float('inf')` 덕이다. 정수 큰 값(예: `10**9`)을 INF 로 쓰면 `INF + 음수 < INF` 가 참이 되어 **없는 경로가 생긴다** — 그 경우엔 `if dist[i][k] == INF or dist[k][j] == INF: continue` 가 필요하다
- 루프 순서 `k → i → j`. **k 가 가장 바깥**이어야 한다. 9절

### 8.6 경로 복원 — `next` 행렬 (자료에 없는 것)

`next[i][j]` 에 "i 에서 j 로 가는 최단 경로의 **첫 걸음**"을 둔다. 초기값은 직접 간선이 있으면 j, 없으면 None. 갱신될 때 `next[i][j] = next[i][k]` — i 에서 j 로 가려면 일단 i→k 경로의 첫 걸음을 밟는다.

```python
def floyd_path(D0):
    n = len(D0)
    dist = [row[:] for row in D0]
    nxt = [[(j if D0[i][j] != INF and i != j else None) for j in range(n)] for i in range(n)]
    for k in range(n):
        for i in range(n):
            for j in range(n):
                if dist[i][k] + dist[k][j] < dist[i][j]:
                    dist[i][j] = dist[i][k] + dist[k][j]
                    nxt[i][j] = nxt[i][k]
    return dist, nxt

def floyd_build(nxt, i, j):
    if nxt[i][j] is None:
        return []
    path = [i]
    while i != j:
        i = nxt[i][j]
        path.append(i)
    return path
```

5정점 예제에서 (실행: `bench/s8_path.py`):

```text
v1→v2: v1 → v3 → v5 → v2   (거리 1)
v1→v5: v1 → v3 → v5        (거리 4)
v2→v1: v2 → v3 → v4 → v1   (거리 0)
v5→v1: v5 → v2 → v3 → v4 → v1   (거리 -3)
v4→v2: v4 → v5 → v2        (거리 -1)
```

---

## 9. ⭐ 검증 — 루프 순서: k 는 가장 바깥이어야 한다

(실행: `bench/s6_floyd_order.py`) 무작위 방향 그래프 1,000건 (V = 6~8, 가중치 1~20, 간선 확률 0.4, `random.seed(2026)`). 정점마다 다익스트라를 돌려 만든 모든 쌍 행렬을 정답으로 삼았다.

| 루프 순서 | 정답과 불일치 | 비율 |
|---|---|---|
| **k, i, j** (k 바깥 — 정상) | **0건** / 1,000건 | 0% |
| i, k, j (k 가운데) | 817건 | 81.7% |
| i, j, k (k 안쪽) | 730건 | 73.0% |

k 안쪽 판이 틀린 셀은 49,743개 중 2,834개(5.7%). 틀린 셀은 **전부 정답보다 크거나 ∞** 였다 — 작아지는 방향으로 틀리지는 않는다. 완화는 실제로 존재하는 경로의 길이만 쓰기 때문에 정답 아래로 내려갈 수는 없고, 다만 **찾아야 할 경로를 못 찾는다.**

가장 작은 반례: 사슬 `0→3→2→1` (가중치 1). 정답 D[0][1] = 3.

```text
k 바깥 : D[0] = [0, 3, 2, 1]
k 안쪽 : D[0] = [0, ∞, 2, 1]     ← D[0][1] 이 ∞ 로 남는다
```

k 안쪽 판은 i=0 행을 **맨 먼저 끝낸다.** 그때 D[0][1] 은 D[0][k] + D[k][1] 을 보는데,

- k=3: D[0][3] = 1 이지만 D[3][1] 은 아직 간선 그대로 ∞ (3→2→1 은 **3 행을 처리할 때** 비로소 생긴다)
- k=2: D[2][1] = 1 이지만 D[0][2] 는 아직 ∞ (같은 행이라도 j=2 는 j=1 보다 **뒤에** 처리된다)

그래서 D[0][1] = ∞ 로 확정되고 다시 오지 않는다. 다른 행이 나중에 좋아져도 0 행은 이미 지나갔다. k 바깥 판은 k=2 단계에서 D[3][1] = D[3][2] + D[2][1] = 2 를 먼저 만들고, k=3 단계에서 D[0][1] = D[0][3] + D[3][1] = 3 을 만든다.

**"경유 정점 집합 {1..k} 를 한 단계씩 넓힌다"는 부분 문제 정의는 모든 (i, j) 쌍이 같은 k 단계를 함께 지날 때만 성립한다.** k 를 안쪽에 두면 어떤 쌍은 k=n 까지 다 본 뒤인데 다른 쌍은 아직 k=0 이라, 부분 문제 $D^{k-1}_{kj}$ 가 아직 계산되지 않은 채로 읽힌다. 순서가 곧 정의다.

---

## 10. ⭐ 검증 — 플로이드 O(V³) vs 다익스트라 V번: "시간 복잡도가 동일하다"는 언제 맞는가

(실행: `bench/s7_floyd_vs_dij.py`) `random.seed(2026)`, 가중치 1~100, 3회 중앙값.

### 10.1 순수 파이썬 플로이드-워셜의 한계 V

자료 코드 원문을 완전 그래프에 돌렸다.

| V | V³ | 자료 코드 | 행 캐시 판 | 초당 완화 검사 (자료 코드) |
|---|---|---|---|---|
| 100 | 1,000,000 | 48.9 ms | 35.4 ms | 20,435,747 |
| 200 | 8,000,000 | 367.2 ms | 262.9 ms | 21,788,346 |
| 300 | 27,000,000 | 1,266.0 ms | 881.6 ms | 21,327,274 |
| 400 | 64,000,000 | **3,090.0 ms** | 2,087.9 ms | 20,711,721 |

초당 약 **2,100만 번** 완화 검사. V³ 이 이 값 이하여야 1초 안에 끝나므로 **1초에 V ≈ 280, 10초에 V ≈ 590** 이 순수 파이썬의 한계다. 코딩 테스트에서 흔히 말하는 "1억 번 ≈ 1초"는 C/C++ 기준이고, 이 코드의 파이썬은 그 1/5 근처다. V = 400 이면 이미 3초다.

"행 캐시 판"은 같은 알고리즘에서 `dist[i]`, `dist[k]`, `dist[i][k]` 를 루프 밖으로 빼고 `dist[i][k] == INF` 면 j 루프를 통째로 건너뛰는 흔한 최적화다. 30% 정도 빨라진다.

```python
for k in range(n):
    dk = dist[k]
    for i in range(n):
        di = dist[i]
        dik = di[k]
        if dik == INF:
            continue
        for j in range(n):
            nd = dik + dk[j]
            if nd < di[j]:
                di[j] = nd
```

### 10.2 모든 쌍 — 플로이드 vs 다익스트라 V번

같은 V 에서 희소 그래프(정점당 진출 간선 4개, E = 4V)와 밀집 그래프(완전 그래프, E = V(V-1)) 각각. 결과 행렬은 전부 일치.

| V | 희소 E | 플로이드 | 다익스트라 × V | 배수 | 밀집 E | 플로이드 | 다익스트라 × V | 배수 |
|---|---|---|---|---|---|---|---|---|
| 100 | 400 | 64.5 ms | 5.9 ms | 10.9배 | 9,900 | 49.4 ms | 42.6 ms | 1.2배 |
| 200 | 800 | 480.9 ms | 24.9 ms | 19.3배 | 39,800 | 370.4 ms | 294.4 ms | 1.3배 |
| 300 | 1,200 | 1,714.0 ms | 61.1 ms | 28.1배 | 89,700 | 1,275.8 ms | 933.1 ms | 1.4배 |
| **400** | 1,600 | **4,125.7 ms** | **110.4 ms** | **37.4배** | 159,600 | **2,953.1 ms** | **2,180.4 ms** | **1.4배** |

(배수 = 플로이드 시간 / 다익스트라 V번 시간)

- **희소 그래프에서는 다익스트라 V번이 압도적이다.** V = 400 에서 37.4배. 플로이드는 간선이 있든 없든 V³ 번 검사하고, 다익스트라는 있는 간선만 본다
- **밀집 그래프에서는 같은 자릿수다.** 1.2~1.4배로 다익스트라 V번이 근소하게 빠르다
- 희소 그래프에서 플로이드가 밀집보다 오히려 느린 것(4,125.7 대 2,953.1)은 셀 대부분이 `inf`(float) 라 float 덧셈·비교가 int 보다 느리기 때문이다

### 10.3 판정 — 자료의 문장은 어떤 조건에서 맞는가

모든 쌍을 구하려면 다익스트라를 V번 돌려야 하고 그 비용은 `O(V · (V + E) log V)` 다.

- 밀집 그래프 (E ≈ V²): `O(V^3 log V)` — 플로이드의 `O(V^3)` 보다 **log V 만큼 크다**
- 희소 그래프 (E ≈ V): `O(V^2 log V)` — 플로이드보다 **V / log V 배 작다**

그러니 "시간 복잡도가 동일하다"는 **모든 쌍 + 밀집 그래프**라는 조건에서만 (log 인자를 무시하고) 맞는 말이고, 희소 그래프에서는 틀린 말이다. 자료가 그 조건을 적지 않았을 뿐, 다익스트라·벨만-포드가 "정점과 정점 간"이고 플로이드가 "모든 쌍"이라는 문맥이 앞에 있으니 모든 쌍 기준으로 읽는 것이 맞다.

**밀집 그래프에서 log V 가 안 보이는 이유** — 힙 연산은 갱신이 성공할 때만 일어난다. 완전 그래프 V = 400 에서 다익스트라 400번의 완화 검사는 63,840,000회(= V·E)인데 힙 push 는 707,803회로 1/90 이다. 무작위 가중치에서 성공 갱신은 정점당 V·ln V 근처(1,769.5 회, V·lnV ≈ 2,396.6)에 그친다. 시간은 플로이드와 마찬가지로 **V³ 번의 검사**가 지배하고, log 가 붙는 힙 연산은 곁가지다.

| 조건 | 무엇을 쓰나 |
|---|---|
| 모든 쌍 · V ≤ 300 정도 · 음수 간선 있어도 됨 | **플로이드-워셜** — 네 줄, 인접 행렬 그대로 |
| 모든 쌍 · 희소 그래프 · 양수 가중치 | 다익스트라 V번 — 수십 배 빠르다 |
| 모든 쌍 · 희소 · 음수 간선 | 벨만-포드 한 번으로 가중치를 재조정한 뒤 다익스트라 V번 (존슨 알고리즘, 이 자료 밖) |
| 한 정점에서만 | 플로이드를 쓸 이유가 없다. 다익스트라 또는 벨만-포드 |

---

## 11. (선택) 다익스트라 — 리스트 스캔 O(V²) vs heapq

(실행: `bench/s9_list_vs_heap.py`) 우선순위 큐 없이 매번 미확정 정점을 훑어 최소를 고르는 판과 `heapq` 판. 시작 정점 하나, 3회 중앙값.

| V | 희소 E | 리스트 스캔 | heapq | 배수 | 밀집 E | 리스트 스캔 | heapq | 배수 |
|---|---|---|---|---|---|---|---|---|
| 500 | 2,000 | 6.5 ms | 0.3 ms | 19.0배 | 249,500 | 13.5 ms | 8.8 ms | 1.5배 |
| 1,000 | 4,000 | 27.9 ms | 0.8 ms | 34.0배 | 999,000 | 53.3 ms | 32.0 ms | 1.7배 |
| 2,000 | 8,000 | 113.1 ms | 1.7 ms | 67.5배 | 3,998,000 | 209.6 ms | 129.2 ms | 1.6배 |

이론상 밀집 그래프에서는 `O(V^2)` 인 리스트 판이 `O(V^2 log V)` 인 힙 판을 이겨야 하는데 **역전은 없었다.** 리스트 판의 "최소 찾기" 루프가 파이썬 레벨(V번의 비교를 파이썬 바이트코드로)이고, 힙 판은 힙 연산이 C 로 구현된 `heapq` 이며 push 도 드물다. 파이썬에서는 항상 `heapq` 판을 쓰면 된다.

---

## 12. 세 알고리즘 선택 기준

| 질문 | 예 | 아니오 |
|---|---|---|
| 모든 쌍이 필요한가? | V 가 수백 이하면 **플로이드-워셜**. 크고 희소하면 다익스트라 V번 | ↓ |
| 음수 간선이 있는가? | **벨만-포드** (음수 사이클도 검출) | ↓ |
| 가중치가 전부 같은가? | **BFS** | ↓ |
| 가중치가 0/1 뿐인가? | 0-1 BFS (덱) | **다익스트라** (heapq) |

| | 다익스트라 | 벨만-포드 | 플로이드-워셜 |
|---|---|---|---|
| 문제 | 단일 출발 | 단일 출발 | 모든 쌍 |
| 접근 | 탐욕 + 우선순위 큐 | DP (간선 수 k) + 전체 간선 반복 | DP (경유 정점 k) + 삼중 루프 |
| 음수 간선 | 불가 (오답 또는 지수 시간) | 가능 | 가능 |
| 음수 사이클 | 무한 루프 (자료 판) | 검출 (V번째 검사) | 검출 (대각선 < 0) |
| 복잡도 | `O((V+E) log V)` | `O(VE)`, 조기 종료로 실제는 훨씬 적음 | `O(V^3)` |
| 파이썬 실측 | V=2,000 / E=10,000 : 3.1 ms | 같은 그래프 17.6 ms (조기 종료 없이 3,357.3 ms) | V=400 완전 그래프 3,090.0 ms |
| 자료구조 | 인접 리스트(딕셔너리) + heapq | 간선 리스트면 충분 | 인접 행렬 |
| 경로 복원 | `prev[v]` | `prev[v]` | `next[i][j]` |

---

## 13. 체크리스트

- [ ] 세 알고리즘의 안쪽은 전부 `dist[u] + w < dist[v]` 완화 한 줄이다. **어떤 간선을 어떤 순서로** 완화하는지가 알고리즘의 이름이다
- [ ] 다익스트라의 우선순위 큐에는 **간선 가중치가 아니라 시작점부터의 누적 거리**를 넣는다. 가중치를 넣으면 프림이다
- [ ] `heapq` 는 리스트·튜플의 **첫 원소**로 정렬한다. `[거리, 정점]` 순서
- [ ] `if distances[u] < current_distance: continue` 는 낡은 큐 항목을 버리는 줄이다. 이 판에는 "확정"이 없어 음수 간선에서 정답을 내기도 하지만 **보장이 없다** (정점 하나에 pop 2배)
- [ ] 꺼낸 정점을 visited 로 확정하는 판은 음수 간선에서 **오답**이다 (반례: D 를 2 로, 정답 -4)
- [ ] 음수 간선이 보이면 벨만-포드. 음수 사이클이 있으면 다익스트라(자료 판)는 **끝나지 않는다**
- [ ] 벨만-포드는 V-1 순회가 상한이고, **조기 종료(`updated`)** 를 반드시 넣는다. 경로 그래프에서 500배
- [ ] 벨만-포드의 순회 수는 **간선 검사 순서**가 정한다. 경로 방향과 같으면 1회, 반대면 V-1 회
- [ ] V번째 검사에서 또 줄어들면 음수 사이클. 플로이드에서는 **대각선 < 0**
- [ ] 플로이드의 삼중 루프는 **k 가 가장 바깥**. 안쪽에 두면 73% 가 틀린다
- [ ] `if graph[u][v] != 0` 은 가중치 0 인 간선을 없는 간선으로 취급한다. INF 를 정수로 잡으면 `INF + 음수 < INF` 함정
- [ ] 순수 파이썬 플로이드는 **V ≈ 300** 이 한계다 (V=400 에 3초). 희소 그래프면 다익스트라 V번이 수십 배 빠르다
- [ ] "플로이드 = 다익스트라와 같은 복잡도"는 **모든 쌍 + 밀집 그래프**에서만 맞는 말이다
- [ ] 경로가 필요하면 다익스트라·벨만-포드는 `prev`, 플로이드는 `next[i][j] = next[i][k]`. 동점이면 **먼저 도달한 경로**가 남는다

---

## 14. 실행으로 확인한 것 (요약)

| # | 확인한 것 | 결과 |
|---|---|---|
| 1 | 자료 다익스트라 코드 + 주석 출력 | `{a:0, b:3, c:5, d:9, e:11, f:12}` **일치**. 단 코드의 graph 에 그림의 b→d(6), e→a(3) 누락 |
| 2 | 다익스트라 12단계 큐 | 6·7·8 단계의 큐에 들어가는 값은 자료의 6 이 아니라 **누적 거리 9** |
| 3 | 연습 그래프 A~J | `0,4,6,10,8,9,11,13,17,21` **일치** |
| 4 | 자료 벨만-포드 코드 + 예제 | `A:0 B:3 C:2 D:5 E:2 F:4` **일치**, 실행 순회 3회 (갱신 2 + 확인 1) |
| 5 | 벨만-포드 순회 2 표 | `3+3<2` → `3+3>2`, C 검사 첫 항 3 → **2**. 결과(갱신 없음)는 같음 |
| 6 | 플로이드 K=1~5 행렬 | 자료의 여섯 표와 **셀 단위 전부 일치**. K=1 목록의 D[5,2] 는 3 이 아니라 **-3** |
| 7 | 플로이드 코드 예시 그래프(6정점) | 음수 사이클 2→5→4→2 = -1, 출력 대각선 `[0, 0, -1, 0, -1, -2]` |
| 8 | 확정 판 다익스트라 · 음수 간선 반례 | D = **2** (정답 -4) — 오답 |
| 9 | 자료 판 다익스트라 · 같은 반례 / 자료 BF 그래프 | **-4** / `A:0 B:3 … F:4` — 정답 |
| 10 | 자료 판 · 지수 폭발 가족 n=20 / 24 | pop **524,308 / 8,388,632** (`2^(n-1)+n`), 216.9 / 3,659.2 ms. 벨만-포드 0.7 / 1.2 ms |
| 11 | 자료 판 · 음수 사이클 | pop 10,000 에서 강제 중단, dist 계속 감소. 벨만-포드는 "음수 사이클 발생" |
| 12 | 벨만-포드 간선 순서 10,000 셔플 | 갱신 순회 1회 0.9% · 2회 21.5% · **3회 55.5%** · 4회 21.2% · 5회 1.0%, dist 전부 일치 |
| 13 | 경로 그래프 V=2,000 | 정방향 **0.6 ms**(1회) / 정방향·조기 종료 없음 314.7 ms / 역방향 232.2 ms(**1,999회**) |
| 14 | BF vs 다익스트라 V=2,000 / E=10,000 | 3.1 ms 대 17.6 ms (**5.7배**), BF 실제 순회 9, 조기 종료 없으면 3,357.3 ms |
| 15 | 음수 사이클 검출 (6정점 예시) | BF 6번째 검사에서 4→2, 4→3 또 감소 / 플로이드 대각선 음수. 4→2 = -2 로 고치면 둘 다 정상 |
| 16 | 플로이드 루프 순서 1,000건 | k 바깥 불일치 **0건** / (i,k,j) 817건 / (i,j,k) **730건 (73.0%)** |
| 17 | 순수 파이썬 플로이드 V=100~400 | 48.9 / 367.2 / 1,266.0 / **3,090.0 ms**, 초당 약 2,100만 검사 → 1초 한계 V ≈ 280 |
| 18 | 플로이드 vs 다익스트라 V번, V=400 | 희소 4,125.7 대 110.4 ms (**37.4배**) / 밀집 2,953.1 대 2,180.4 ms (1.4배) |
| 19 | 밀집 V=400 다익스트라 V번의 힙 push | 완화 검사 63,840,000 회 대 push 707,803 회 (1/90) |
| 20 | 경로 복원 | 그림 그래프 a→f = **a→b→d→f**, 코드 딕셔너리 = a→c→d→f. 플로이드 v1→v2 = v1→v3→v5→v2 |
| 21 | 리스트 스캔 vs heapq, V=2,000 | 희소 67.5배, 밀집 1.6배 — 역전 없음 |
