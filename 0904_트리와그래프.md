# 트리와 그래프 — 비선형 자료구조와 이진 탐색 트리

> 출처: 알고리즘 과정 5일차 강의자료 (85p) · 정리일: 2026-09-06
> 앞 노트: [0903 스택과 큐, 그리고 연결 리스트](0903_스택과큐.md) · [0902 재귀와 완전 탐색](0902_재귀와완전탐색.md)
> 관련: [0825 DFS와 BFS 이론](0825_DFS와BFS_이론.md) · [0825 DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md) · [0830 오일러 경로](0830_오일러경로.md)

앞 노트에서 스택과 큐를 만들었다. 그 두 개는 **비선형 자료구조를 훑기 위한 도구**였고, 이번 자료가 그 대상을 다룬다. 트리 → 이진 트리 → 순회 → 그래프 → 이진 탐색 트리(BST) 순이다.

DFS/BFS 노트에서 그래프 표현 3방식을 이미 다뤘으므로, 여기서는 **자료가 새로 주는 것**에 무게를 둔다 — 트리 용어의 정확한 정의, 이진 트리의 배열 표현, 순회 3종, 간선 리스트, 그리고 BST 전체.

---

## 1. 트리

### 1.1 성질

- **비선형 구조**
- 원소들 간에 **1:N 관계**를 가지는 자료구조
- 원소들 간에 **계층 관계**를 가지는 계층형 자료구조
- 상위 원소에서 하위 원소로 내려가면서 확장되는 나무 모양

### 1.2 정의 — 재귀적이다

한 개 이상의 노드로 이루어진 유한 집합이며 다음을 만족한다.

- 노드 중 최상위 노드를 **루트(root)** 라 한다
- 나머지 노드들은 n(≥0)개의 분리 집합 T₁, …, T_N 으로 분리될 수 있다
- 이들 T₁, …, T_N 은 **각각 하나의 트리가 되며**(재귀적 정의) 루트의 **부 트리(subtree)** 라 한다

**"트리는 루트 하나와 부 트리들이다"** — 정의 안에 자기 자신이 들어 있다. 0902 노트에서 본 basis / inductive 구조 그대로다. 그래서 트리를 다루는 코드는 거의 전부 재귀로 짜이고, 순회 3종도 3줄짜리 재귀로 끝난다.

### 1.3 용어

예제 트리 T (루트 A):

```text
            A
        ┌───┼───┐
        B   C   D
       ┌┴┐  │  ┌─┼─┐
       E F  G  H I J
          │
          K
```

| 용어 | 정의 | 예 |
|---|---|---|
| 노드(node) | 트리의 원소 | A ~ K |
| 간선(edge) | 노드와 노드를 연결하는 선. 부모와 자식을 연결 | A—B |
| 루트 노드 | 트리의 시작 노드인 최상위 노드 | A |
| 형제 노드(sibling) | 같은 부모의 자식들 | B, C, D |
| 조상 노드(ancestor) | 간선을 따라 루트까지 이르는 경로의 모든 노드 | K의 조상 = F, B, A |
| 자손 노드(descendant) | 서브 트리에 있는 하위 레벨 노드들 | B의 자손 = E, F, K |
| 서브 트리 | 부모와 연결된 간선을 끊었을 때 생기는 트리 | |
| **노드의 차수** | 노드에 연결된 **자식 노드 수** | B=2, C=1 |
| **트리의 차수** | 트리에 있는 노드의 차수 중 **가장 큰 값** | T의 차수 = 3 |
| 단말 노드(leaf) | 차수가 0인 노드 | E, G, H, I, J, K |
| **레벨** | 루트에서 노드까지의 거리. **루트가 0**, 자식은 부모+1 | K의 레벨 = 3 |
| **높이** | 루트에서 가장 먼 리프까지의 **간선 수** = 최대 레벨 | T의 높이 = 3 |

**주의할 두 가지.**

- **그래프의 차수와 트리의 차수가 다르다.** 그래프에서 차수는 "정점에 연결된 간선 수"(부모로 가는 간선 포함)인데, 트리에서 노드의 차수는 **자식 수만** 센다
- **레벨이 0부터 시작한다.** 자료마다 1부터 세는 경우가 있어 문제 지문을 확인해야 한다. 여기서는 0 기준이고, 그래서 "높이 = 최대 레벨"이 성립한다

---

## 2. 이진 트리

### 2.1 정의

- **차수가 2인 트리** — 각 노드가 자식을 최대 2개까지만 가진다
- 왼쪽 자식 / 오른쪽 자식이 **구분된다** (자식이 하나여도 왼쪽인지 오른쪽인지가 다른 트리다)
- 모든 노드가 최대 2개의 서브 트리를 갖는다

### 2.2 특성

- 레벨 i 에서의 노드 최대 개수 = **2^i**
- 높이 h인 이진 트리의 노드 개수
  - **최소 = h + 1** (한 줄로 늘어진 편향 트리)
  - **최대 = 2^(h+1) − 1** (모든 자리가 찬 포화 트리)

높이 3이면 최소 4개, 최대 15개다.

### 2.3 종류 3가지

| 종류 | 정의 |
|---|---|
| **포화 이진 트리** (Perfect) | 모든 레벨이 포화 상태. 높이 h일 때 노드가 정확히 2^(h+1)−1 개. 루트를 1번으로 하여 2^(h+1)−1 까지 번호가 정해진다 |
| **완전 이진 트리** (Complete) | 높이 h, 노드 수 n일 때 (2^h ≤ n ≤ 2^(h+1)−1), **포화 이진 트리의 1번부터 n번까지 빈자리 없이** 채워진 트리 |
| **편향 이진 트리** (Skewed) | 높이 h에 대해 최소 개수의 노드를 가지면서 **한쪽 방향 자식만** 갖는 트리. 왼쪽 편향 / 오른쪽 편향 |

**완전 이진 트리의 정의를 "마지막 레벨만 왼쪽부터 채워진 트리"로 외우면 헷갈린다.** 정확한 정의는 "포화 트리 번호 1~n 이 빈 자리 없이 차 있다"이고, 이 정의가 곧 **배열 표현에서 구멍이 없다**는 뜻이다. 힙(heap)이 완전 이진 트리인 이유도 이것이다 — 배열에 빈틈없이 담기니까.

### 2.4 리스트(배열)를 이용한 표현

노드에 번호를 부여한다.

- 루트의 번호를 **1**로 한다
- 레벨 n의 노드에 왼쪽부터 오른쪽으로 **2^n 부터 2^(n+1)−1 까지** 차례로 부여

그러면 **번호 하나로 부모·자식을 계산할 수 있다.**

```text
노드 번호가 i일 때
  부모 노드 번호     : i // 2
  왼쪽 자식 노드 번호 : 2 * i
  오른쪽 자식 노드 번호: 2 * i + 1
  레벨 n의 시작 번호  : 2^n
```

> 📌 **자료에는 부모가 `i / 2` 로 적혀 있다.** 파이썬에서 `/` 는 실수 나눗셈이라 `tree[i / 2]` 를 그대로 쓰면 `TypeError: list indices must be integers or slices, not float` 이 난다. 실행해서 확인했다. **`i // 2` 여야 한다.** (자료의 실제 코드는 `index // 2` 로 맞게 되어 있다 — 설명 슬라이드와 코드가 어긋난 것이다.)

배열 크기는 이렇게 나온다.

```text
레벨 i의 최대 노드 수 = 2^i
모든 노드 수         = 1 + 2 + 4 + … + 2^h = 2^(h+1) − 1
리스트 크기          = 2^(h+1)          (인덱스 0을 비워 두므로 한 칸 더)
```

**인덱스 0을 비우는 이유.** 루트를 1번으로 두면 `2*i`, `2*i+1`, `i//2` 공식이 전부 맞아떨어진다. 0번을 루트로 하면 `2*0 = 0` 이라 자기 자신을 가리켜 공식이 깨진다.

```python
class BinaryTree:
    def __init__(self):
        self.tree = [None, 'A', 'B', 'C', 'D', 'E',
                     'F', 'G', 'H', 'I', 'J', 'K', 'L']

    def insert(self, value):
        self.tree.append(value)

    def get_node(self, index):
        if index < len(self.tree):
            return self.tree[index]
        return None

    def set_node(self, index, value):
        while len(self.tree) <= index:      # 필요한 만큼 늘린다
            self.tree.append(None)
        self.tree[index] = value

    def get_left_child(self, index):
        left_index = 2 * index
        if left_index < len(self.tree):
            return self.tree[left_index]
        return None

    def get_right_child(self, index):
        right_index = 2 * index + 1
        if right_index < len(self.tree):
            return self.tree[right_index]
        return None

    def get_parent(self, index):
        if index <= 1:                      # 루트에는 부모가 없다
            return None
        parent_index = index // 2
        return self.tree[parent_index]
```

### 2.5 배열 표현의 단점

- **편향 이진 트리에서 쓰지 않는 원소의 메모리 낭비가 심하다**
- 중간에 노드를 삽입·삭제하면 리스트 크기 변경이 어려워 비효율적

> **낭비를 실측했다.** 높이 h인 **편향** 트리는 노드가 h+1 개뿐인데 배열은 2^(h+1) 칸이 필요하다.
>
> | 높이 h | 노드 수 | 배열 크기 2^(h+1) | 사용률 | 배열 메모리 |
> |---|---|---|---|---|
> | 3 | 4 | 16 | 25% | 0.2 KB |
> | 5 | 6 | 64 | 9.4% | 0.6 KB |
> | 10 | 11 | 2,048 | 0.54% | 16.1 KB |
> | 15 | 16 | 65,536 | 0.024% | 512.1 KB |
> | 20 | 21 | 2,097,152 | **0.001%** | **16.0 MB** |
>
> **노드 21개를 담으려고 16MB를 잡는다.** 같은 높이의 포화 트리라면 그 배열이 꽉 찬다. 배열 표현은 **완전 이진 트리에 최적화된 표현**이고, 편향될수록 무너진다.

### 2.6 연결 리스트를 이용한 표현

배열 표현의 단점을 보완하려면 연결 리스트를 쓴다. 이진 트리의 노드는 자식이 최대 2개이므로, **앞 노트의 이중 연결 리스트 노드와 같은 3칸 구조**를 그대로 쓸 수 있다.

```text
[ left | 데이터 | right ]
   ↓              ↓
왼쪽 자식      오른쪽 자식
```

이중 연결 리스트에서는 `prev`/`next` 가 **앞뒤 이웃**을 가리켰는데, 트리에서는 같은 3칸이 **두 자식**을 가리킨다. 구조는 같고 의미만 다르다. 리프 노드는 양쪽이 `null` 이다.

---

## 3. 이진 트리 순회

### 3.1 세 가지 순회

**순회(traversal)** 는 트리의 노드들을 체계적으로 방문하는 것이다. V(루트) / L(왼쪽 서브트리) / R(오른쪽 서브트리) 를 어떤 순서로 처리하느냐로 갈린다.

| 순회 | 순서 | 설명 |
|---|---|---|
| **전위**(preorder) | **VLR** | 부모를 방문한 후 자식을 좌·우 순서로 방문 |
| **중위**(inorder) | **LVR** | 왼쪽 자식 → 부모 → 오른쪽 자식 |
| **후위**(postorder) | **LRV** | 자식을 좌우 순서로 방문한 후 부모 |

**세 코드가 한 줄만 다르다.**

```python
class TreeNode:
    def __init__(self, key):
        self.left = None
        self.right = None
        self.val = key

def preorder_traversal(root):
    if root:
        print(root.val)               # ← 방문이 앞
        preorder_traversal(root.left)
        preorder_traversal(root.right)

def inorder_traversal(root):
    if root:
        inorder_traversal(root.left)
        print(root.val)               # ← 방문이 가운데
        inorder_traversal(root.right)

def postorder_traversal(root):
    if root:
        postorder_traversal(root.left)
        postorder_traversal(root.right)
        print(root.val)               # ← 방문이 뒤
```

**`print` 한 줄의 위치만 바뀐다.** 재귀 호출 두 개는 그대로다. 이것이 트리의 재귀적 정의가 코드로 드러난 모습이다 — "왼쪽 서브트리를 처리한다"가 곧 "같은 함수를 왼쪽 자식에 대해 부른다"이다.

`if root:` 가 basis(종료 조건)다. `None` 을 만나면 아무것도 하지 않고 돌아온다.

### 3.2 수행 순서 추적

예제 트리:

```text
        A
      ┌─┴─┐
      B   C
    ┌─┴┐ ┌┴─┐
    D  E F  G
   ┌┴┐
   H I
```

| 순회 | 단계별 전개 | 최종 |
|---|---|---|
| 전위 | A → T1 → T2 / A → B (T3) E → C F G | **A B D H I E C F G** |
| 중위 | T1 → A → T2 / (T3) B E → A → F C G | **H D I B E A F C G** |
| 후위 | T1 → T2 → A / (T3) E B → F G C → A | **H I D E B F G C A** |

> 세 코드를 실제로 돌려 자료의 답과 대조했다. **전위·중위·후위 셋 다 문자 단위로 일치.**

### 3.3 수식 트리

- 수식을 표현하는 이진 트리
- **연산자는 루트이거나 가지 노드, 피연산자는 모두 잎 노드**

```text
        +
      ┌─┴─┐
      *    E
    ┌─┴┐
    *   D
  ┌─┴┐
  /   C
┌┴┐
A  B
```

이 트리를 세 가지로 순회하면 **세 가지 표기법이 그대로 나온다.**

| 순회 | 결과 | 표기법 |
|---|---|---|
| 전위 | `+ * * / A B C D E` | **전위(prefix)** 표기 |
| 중위 | `A / B * C * D + E` | **중위(infix)** 표기 |
| 후위 | `A B / C * D * E +` | **후위(postfix)** 표기 |

**앞 노트의 계산기가 여기서 만난다.** 스택으로 중위→후위 변환을 했는데, 그건 사실 **중위 표기로 트리를 복원한 뒤 후위 순회한 것과 같다.** 후위 표기식을 스택으로 계산한 것도 후위 순회 순서대로 계산한 것이다.

> 수식 트리를 직접 만들어 세 순회를 돌렸다. **세 결과 모두 자료의 답과 일치.**

### 3.4 재귀 순회의 한계와 스택 순회

순회는 재귀가 가장 짧지만, **깊이가 깊으면 터진다.**

```python
def pre_stack(root):                # 전위 순회를 명시적 스택으로
    res, st = [], [root]
    while st:
        n = st.pop()
        if n is None:
            continue
        res.append(n.val)
        st.append(n.right)          # 오른쪽을 먼저 넣어야
        st.append(n.left)           # 왼쪽이 먼저 나온다 (LIFO)
    return res
```

**`right` 를 먼저 push 하는 게 핵심이다.** 스택은 LIFO라 나중에 넣은 `left` 가 먼저 pop 된다. 순서를 바꾸면 VRL 순회가 된다.

> **실측.** 깊이 50,000인 오른쪽 편향 트리에서
>
> - 재귀 전위 순회 → **RecursionError**
> - 스택 전위 순회 → 성공, 노드 50,000개, **4.8 ms**
>
> 같은 순회인데 재귀는 못 하고 스택은 한다. 0903 노트에서 본 "재귀 = 시스템 스택을 쓰는 것"의 반대 방향이다 — **시스템 스택 대신 내 스택을 쓰면 한계가 사라진다.** DFS를 스택으로 짜는 이유와 정확히 같다.

---

## 4. 그래프

> 이 절은 [0825 DFS와 BFS 이론](0825_DFS와BFS_이론.md) 노트와 크게 겹친다. 거기서 다룬 그래프 표현 3방식과 DFS/BFS 구현은 반복하지 않고, **이 자료가 새로 주는 것**(간선 리스트, 세 표현의 정량 비교)만 적는다.

### 4.1 정의

- 아이템(사물 또는 추상적 개념)들과 **이들 사이의 연결 관계**를 표현한다
- **정점(Vertex)** — 그래프의 구성요소, 하나의 연결점
- **간선(Edge)** — 두 정점을 연결하는 선
- **차수(Degree)** — 정점에 연결된 간선의 수
- V개의 정점을 가지는 그래프는 **최대 V(V−1)/2 간선**이 가능
  - 5개 정점이면 최대 5×4/2 = 10개
- 선형 자료구조나 트리로 표현하기 어려운 **N:M 관계**를 표현하기에 좋다

> 📌 `V(V−1)/2` 는 **무방향 단순 그래프** 기준이다. 방향 그래프는 (u,v)와 (v,u)가 다르므로 **V(V−1)** 개까지 가능하다. 자료가 유형을 밝히지 않고 이 식만 제시한다.

**트리와의 관계.** 트리도 그래프다. 다만 조건이 붙는다.

- 각 노드는 최대 하나의 부모를 갖는다
- 각 노드는 자식이 없거나 하나 이상 존재한다
- **두 노드 사이에 유일한 경로가 존재한다**

정점 V개인 트리는 간선이 정확히 V−1개고 사이클이 없다. 이 두 조건이 "유일한 경로"를 만든다.

### 4.2 유형

| 유형 | 설명 |
|---|---|
| 무방향 그래프 | 간선에 방향이 없다 |
| 방향 그래프 | 간선에 방향이 있다 |
| 가중치 그래프 | 간선에 비용이 붙는다 |
| **DAG** (Directed Acyclic Graph) | 사이클 없는 방향 그래프 |
| 트리 | 위 조건을 만족하는 그래프 |
| **완전 그래프** | 가능한 모든 간선을 가진 그래프 |
| **부분 그래프** | 원래 그래프에서 일부 정점이나 간선을 제외한 그래프 |

### 4.3 인접 · 경로 · 사이클

- **인접(Adjacency)** — 두 정점 사이에 간선이 존재하면 서로 인접해 있다. 완전 그래프의 임의의 두 정점은 항상 인접
- **경로(Path)** — 정점 A에서 시작해 B로 끝나는, 두 정점 사이를 잇는 간선들을 순서대로 나열한 것
  - **같은 정점을 거치지 않는** 간선들의 sequence
  - 어떤 정점에서 다른 정점으로 가는 경로는 여러 가지일 수 있다
- **사이클(Cycle)** — 경로의 시작 정점과 끝 정점이 같은 것

### 4.4 표현 3방식

간선 정보를 저장하는 방식이고, **메모리와 성능을 고려해 고른다.**

- **인접 행렬(Adjacency Matrix)** — V×V 크기의 2차원 배열
- **인접 리스트(Adjacency List)** — 각 정점마다 나가는 간선 정보를 저장
- **간선 리스트(Edge List)** — 간선(시작 정점, 끝 정점)을 객체로 표현해 리스트에 저장

#### 인접 행렬

- V×V **정방 행렬**. 행 번호와 열 번호가 정점에 대응
- 인접하면 1, 아니면 0
- **무향 그래프** — i번째 행의 합 = i번째 열의 합 = Vᵢ 의 차수
- **유향 그래프** — 행 i의 합 = Vᵢ 의 **진출 차수**, 열 i의 합 = Vᵢ 의 **진입 차수**

| 항목 | 내용 |
|---|---|
| 장점 | 두 정점 사이 간선 확인이 **O(1)**, 구현이 단순, **정적 그래프**에 적합 |
| 단점 | 공간 복잡도 **O(V²)**, 간선 수 확인이나 인접 정점 나열이 느림 |
| 적합 | **Dense Graph**(밀집 그래프), 간선 존재 여부를 빠르게 확인해야 할 때 |

#### 인접 리스트

각 정점에 대한 인접 정점들을 순차적으로 표현한다. 원래는 연결 리스트로 저장하지만 파이썬에서는 그냥 리스트의 리스트를 쓴다.

```text
무향 그래프: 노드 수 = 간선 수 × 2,  각 정점의 노드 수 = 정점의 차수
방향 그래프: 노드 수 = 간선 수,      각 정점의 노드 수 = 정점의 진출 차수
```

| 항목 | 내용 |
|---|---|
| 장점 | 공간 복잡도 **O(V + E)**, 인접 정점 나열이 빠름 |
| 단점 | 간선 존재 확인이 느림(**O(V)**), 연결 리스트 구현이 복잡 |
| 적합 | **Sparse Graph**(희소 그래프), 그래프가 동적으로 변할 때, 인접 정점을 자주 순회할 때 |

#### 간선 리스트

간선 그 자체를 객체로 만들어 리스트에 담는다.

| 인덱스 | 시작 정점 | 끝 정점 |
|---|---|---|
| 0 | 0 | 1 |
| 1 | 0 | 2 |
| 2 | 0 | 5 |
| 3 | 0 | 6 |
| 4 | 4 | 3 |
| 5 | 5 | 3 |
| 6 | 5 | 4 |
| 7 | 6 | 4 |

| 항목 | 내용 |
|---|---|
| 장점 | 공간 복잡도 **O(E)**, 간선을 직접 다루는 연산에 효율적 |
| 단점 | 간선 존재 확인 **O(E)**, 특정 정점의 인접 정점 찾기 **O(E)** |
| 적합 | **간선 중심 연산**(예: MST — 크루스칼은 간선을 가중치로 정렬해서 훑는다) |

### 4.5 실측 — 세 표현의 차이

V=2,000 / E=6,000 인 **희소** 그래프(밀도 0.30%, 평균 차수 6)에서 재 봤다.

**메모리**

| 표현 | 메모리 |
|---|---|
| 인접 행렬 | **30.640 MB** |
| 인접 리스트 | 0.530 MB |
| 간선 리스트 | 0.651 MB |

**58배 차이.** V²=400만 칸에 실제 1은 12,000개뿐이니 당연하다.

**"두 정점이 인접한가" 20,000회**

| 표현 | 시간 |
|---|---|
| 인접 행렬 | 1.11 ms |
| 인접 리스트 | 1.67 ms |
| 간선 리스트 | **2,664.13 ms** |

여기가 반직관이다. **희소 그래프에서는 인접 행렬(O(1))과 인접 리스트(O(V))의 차이가 1.5배밖에 안 난다.** 이론상 O(V)=2,000이지만 실제로 훑는 건 그 정점의 차수만큼, 즉 평균 6개다. **O(V)는 최악값이고 실제 비용은 O(deg(v))** 라는 걸 숫자가 보여 준다. 반면 간선 리스트는 진짜로 E개를 훑어서 2,400배 느리다.

**밀도를 올리면 뒤집힌다** (V=500 고정).

| 밀도 | 평균 차수 | 인접 행렬 | 인접 리스트 | 리스트가 몇 배 느린가 |
|---|---|---|---|---|
| 1.2% | 6.0 | 0.79 ms | 1.45 ms | 2.0배 |
| 24.0% | 120.0 | 0.79 ms | 12.01 ms | 13.8배 |
| 88.2% | 440.0 | 0.89 ms | 40.52 ms | **44.0배** |

인접 행렬은 밀도와 무관하게 일정하고, 인접 리스트는 평균 차수에 비례해 느려진다. **"Dense 면 행렬, Sparse 면 리스트"의 실체가 이 표다.**

**"모든 정점의 이웃을 전부 나열" 1회**

| 표현 | 시간 |
|---|---|
| 인접 행렬 | 122.09 ms (V² = 4,000,000칸을 훑는다) |
| 인접 리스트 | **0.07 ms** (V+E = 8,000) |

**1,744배.** DFS/BFS는 정확히 이 연산을 한다 — 그래서 그래프 탐색에는 인접 리스트를 쓴다.

---

## 5. 이진 탐색 트리 (BST)

### 5.1 왜 필요한가

대량의 데이터를 효율적으로 저장·검색·수정해야 하는데, 선형 자료구조에는 한계가 있다.

- **배열** — 데이터 삽입/삭제가 비효율적 (O(n))
- **연결 리스트** — 검색이 비효율적 (O(n))

둘 다 한쪽은 되고 한쪽은 안 된다. 양쪽을 동시에 O(log N)으로 만들려는 게 BST다.

### 5.2 정의와 순서 속성

이진 트리의 특성을 가지면서 추가로 **순서 속성**을 갖는다.

- **왼쪽 자식 노드의 키 값이 부모 노드의 키 값보다 작다**
- **오른쪽 자식 노드의 키 값이 부모 노드의 키 값보다 크다**

이 속성은 **모든 노드에 대해 재귀적으로** 성립해야 한다. "왼쪽 자식"만이 아니라 **왼쪽 서브트리 전체**가 부모보다 작아야 한다. 이걸 놓치면 삽입·삭제에서 트리가 깨진다.

**중요한 따름정리 하나.** BST를 **중위 순회하면 정렬된 순서로 나온다.** LVR = (나보다 작은 것들) → (나) → (나보다 큰 것들) 이기 때문이다. BST가 "정렬된 상태를 유지한다"는 말의 정확한 의미가 이것이다.

### 5.3 장단점

| | 내용 |
|---|---|
| **장점** | 삽입/삭제 후에도 정렬 상태를 유지 / 데이터가 균형 있게 분포하면 탐색·삽입·삭제가 평균 **O(log N)** / 동적으로 크기 조정 가능 |
| **단점** | 트리가 한쪽으로 치우치면 최악 **O(N)** / 노드마다 자식 포인터 2개를 저장해야 하므로 메모리 오버헤드 |

### 5.4 높이와 깊이

- **높이** — 특정 노드에서 가장 깊은 리프까지의 **간선 수**. 트리의 높이는 루트의 높이. **리프의 높이는 0**
- **깊이** — 루트에서 해당 노드까지의 간선 수. **루트의 깊이는 0**
- 균형 잡힌 트리의 높이는 **O(log N)**, 불균형한 트리의 높이는 **O(N)**

### 5.5 탐색

순서 속성을 이용해 루트에서 시작해 키를 비교하며 내려간다.

```text
트리:          10
           ┌───┴───┐
           5       15
          ┌┴┐     ┌┴─┐
          3 7    12  17

찾는 값: 12
  1) 현재 10, 10 < 12 → 오른쪽으로
  2) 현재 15, 15 > 12 → 왼쪽으로
  3) 현재 12, 12 == 12 → 종료
```

```python
class Node:
    def __init__(self, key):
        self.key = key
        self.left = None
        self.right = None

class BST:
    def __init__(self):
        self.root = None

    def search(self, key):
        return self._search(self.root, key)

    def _search(self, node, key):
        if node is None or node.key == key:
            return node
        if key < node.key:
            return self._search(node.left, key)
        return self._search(node.right, key)
```

- 균형: **O(log N)** / 치우침: **O(N)**

### 5.6 삽입

- BST의 특징을 유지하며, 삽입할 위치를 찾기 위해 루트에서 **적절한 위치까지 내려간다**
- 순서 속성을 유지하기 위해 새 노드는 **반드시 리프 노드로 삽입**한다

```text
삽입 노드: 16
  1) 현재 10, 10 < 16 → 오른쪽
  2) 현재 15, 15 < 16 → 오른쪽
  3) 현재 17, 17 > 16 이고 리프이므로 → 17의 왼쪽 자식으로 삽입
```

```python
    def insert(self, key):
        if self.root is None:
            self.root = Node(key)
        else:
            self._insert(self.root, key)

    def _insert(self, node, key):
        if key < node.key:
            if node.left is None:
                node.left = Node(key)
            else:
                self._insert(node.left, key)
        else:
            if node.right is None:
                node.right = Node(key)
            else:
                self._insert(node.right, key)
```

**중간에 끼워 넣지 않는 이유.** 탐색 경로를 따라 내려가다 `None` 을 만난 자리가 **그 값이 있어야 할 유일한 자리**다. 그 자리 말고 다른 데 넣으면 순서 속성이 깨진다.

> 📌 **정의와 코드가 어긋나는 곳 — 중복 키.** 자료는 "오른쪽 자식의 키 값이 부모보다 **크다**"고 정의하는데, `_insert` 의 `else` 는 `key >= node.key` 이므로 **같은 값도 오른쪽에 넣는다.**
>
> 실행해서 확인했다. `10, 5, 15, 10, 10` 을 삽입하면 중위 순회가 `[5, 10, 10, 10, 15]` 로 10이 세 개 들어간다. 게다가 `delete(10)` 을 한 번 부르면 `[5, 10, 10, 15]` 가 되어 **하나만 지워진다.** 중복을 허용할지 말지를 정하고 코드를 맞춰야 한다 — 보통은 삽입 시 같은 키를 무시하거나 카운트를 노드에 얹는다.

### 5.7 삭제 — 세 가지 경우

삭제할 노드의 **자식 노드 유무**에 따라 나뉜다.

**① 리프 노드인 경우** — 단순히 제거

**② 자식이 하나인 경우** — 삭제할 노드의 자식을 부모에 연결한다. 각 서브트리는 독립적으로 존재하므로 BST 속성이 유지된다

**③ 자식이 둘인 경우** — **중위 후속자** 또는 중위 전임자를 찾는다

- **중위 후속자(inorder successor)** — 삭제할 노드의 **오른쪽 서브트리에서 가장 작은 값**. 일반적으로 이걸 쓴다
- **중위 전임자(inorder predecessor)** — 왼쪽 서브트리에서 가장 큰 값
- 목적은 **삭제할 노드와 값이 가장 가까운 노드**를 찾아 그 자리에 앉히는 것

절차:

1. 중위 후속자의 값을 삭제할 노드에 **복사**한다
2. 중위 후속자를 **삭제하고**, 그 노드의 상태에 따라 **재귀적으로 반복**한다
3. 중위 후속자는 **무조건 리프이거나 자식이 하나뿐**이므로 ①이나 ②로 귀결된다 — 무한히 재귀하지 않는다

**3번이 이 알고리즘이 끝나는 이유다.** 중위 후속자는 오른쪽 서브트리의 최솟값이므로 **왼쪽 자식이 있을 수 없다**(있다면 그게 더 작으니까). 그래서 자식이 0개나 1개다.

> **전수 확인했다.** 무작위 BST 3,000개를 만들어, 자식이 둘인 노드 **19,787개**의 중위 후속자를 전부 조사했다. **왼쪽 자식을 가진 경우 0건.** 자료의 주장이 맞다.

```python
    def delete(self, key):
        self.root = self._delete(self.root, key)

    def _delete(self, node, key):
        if node is None:
            return node

        if key < node.key:
            node.left = self._delete(node.left, key)
        elif key > node.key:
            node.right = self._delete(node.right, key)
        else:
            if node.left is None:            # 자식 0개 또는 오른쪽만
                return node.right
            elif node.right is None:         # 왼쪽만
                return node.left

            temp = self._minValueNode(node.right)   # 중위 후속자
            node.key = temp.key                     # 값만 복사
            node.right = self._delete(node.right, temp.key)  # 후속자를 재귀 삭제

        return node

    def _minValueNode(self, node):
        current = node
        while current.left is not None:
            current = current.left
        return current
```

**`self.root = self._delete(self.root, key)` 패턴을 눈여겨볼 것.** `_delete` 가 "이 서브트리를 처리한 뒤의 새 루트"를 **반환**하고, 호출한 쪽이 그걸 자기 링크에 다시 꽂는다. 이렇게 하면 부모 노드를 따로 들고 다닐 필요가 없다. 자식이 0~1개인 경우가 `return node.right` / `return node.left` 한 줄로 끝나는 것도 이 구조 덕분이다.

- 균형: **O(log N)** / 치우침: **O(N)**

---

## 6. 🔥 BST의 균형 문제 — 실측

BST의 구조는 **삽입되는 데이터의 순서에 따라 결정된다.** 특정 패턴으로 들어오면 불균형이 생긴다.

불균형한 BST의 문제점:

- 검색/삽입/삭제가 **O(N)** 이 됨 (균형이면 O(log N))
- 트리 높이가 늘어나면서 **메모리 공간**을 더 씀
- 트리 높이가 늘어나면서 **깊은 재귀 호출로 스택 오버플로우**가 날 수 있음

### 6.1 정렬된 데이터를 넣으면

N = 10,000 을 두 가지 순서로 넣어 봤다.

| 삽입 순서 | 높이 | 삽입 시간 | 탐색 1,000회 |
|---|---|---|---|
| **정렬된 순서** (0,1,2,…) | **9,999** | 5,900.7 ms | 610.50 ms |
| 무작위 순서 | **29** | 10.5 ms | 0.99 ms |

`log₂(10,000) ≈ 13.3` 이다. 무작위 삽입은 높이 29로 그 근처에 머물지만, **정렬된 입력은 높이 9,999** — 완전히 한 줄로 늘어진 연결 리스트가 된다.

- 삽입이 **562배** 느리다
- 탐색이 **617배** 느리다

**"정렬된 데이터를 BST에 넣으면 연결 리스트가 된다"** 는 문장을 숫자로 보면 이렇다. 그리고 정렬된 입력은 실무에서 **가장 흔한 입력**이다 — DB에서 뽑은 id, 시간순 로그, 정렬해서 넘긴 배열.

### 6.2 재귀 깊이가 먼저 터진다

기본 재귀 한계에서 같은 실험을 했다.

```text
sys.getrecursionlimit() = 1000

정렬된 정수를 차례로 삽입 →  997개에서 RecursionError
무작위 순서로 삽입        →  100,000개 전부 성공 (예외 없음)
```

**정렬된 입력은 1,000개도 못 넣는다.** `_insert` 가 리프까지 재귀로 내려가는데 높이가 곧 재귀 깊이라서, 997번째 삽입에서 시스템 스택이 찬다. 0902 노트에서 본 "파이썬 재귀 한계 1000인데 998까지"와 정확히 같은 지점이다.

성능이 문제가 아니라 **아예 동작하지 않는다.** 이게 자료가 말하는 "스택 오버플로우 문제"의 실제 모습이다.

### 6.3 해결 — 자가 균형 트리

불균형 문제를 해결하는 것이 **자가 균형 트리(Self-Balancing Tree)** 다.

- **AVL 트리** — 모든 노드에서 좌우 서브트리 높이 차를 1 이하로 유지. 회전(rotation)으로 맞춘다
- **레드-블랙 트리** — 색 규칙으로 "가장 긴 경로가 가장 짧은 경로의 2배를 넘지 않게" 유지

자료는 이름만 언급하고 넘어간다. **파이썬 표준 라이브러리에는 BST가 없다** — `dict` 와 `set` 이 해시 테이블이기 때문이다. 정렬 순서가 필요하면 `sorted` + `bisect`(정렬된 리스트에 이진 탐색) 또는 `heapq` 를 쓰고, 진짜 균형 트리가 필요하면 `sortedcontainers` 같은 외부 패키지를 쓴다.

---

## 7. 정리

### 트리

- 트리의 **정의 자체가 재귀적**이고, 그래서 순회 코드가 3줄 재귀로 끝난다
- 노드의 차수는 **자식 수**(그래프의 차수와 다르다), 레벨은 **0부터**, 높이는 **최대 레벨**
- 배열 표현은 **`i//2`, `2i`, `2i+1`** 세 공식으로 굴러가고 인덱스 0을 비운다. **완전 이진 트리에만 효율적** — 편향되면 높이 20에 노드 21개로 16MB를 쓴다
- 순회 3종은 `print` **한 줄의 위치**만 다르다. 수식 트리에서 세 순회가 세 표기법(prefix/infix/postfix)으로 그대로 대응된다
- **깊은 트리에서는 재귀 순회가 터지고 스택 순회는 된다** (깊이 5만에서 RecursionError vs 4.8ms)

### 그래프

- 표현 3방식의 선택은 **밀도**가 결정한다. 희소면 인접 리스트, 밀집이면 인접 행렬, 간선 중심 연산(MST)이면 간선 리스트
- 희소 그래프에서 인접 행렬은 인접 리스트보다 **메모리를 58배** 쓰고, 이웃 나열이 **1,744배** 느리다
- **O(V)로 적히는 인접 리스트의 간선 확인이 실제로는 O(deg(v))** 라 희소 그래프에서는 인접 행렬과 1.5배 차이밖에 안 난다. 밀도가 88%가 되면 44배로 벌어진다

### BST

- 순서 속성은 **자식이 아니라 서브트리 전체**에 대해 성립한다. 중위 순회하면 정렬 순서가 나온다
- 삭제의 세 경우 중 ③이 ①·②로 귀결되는 이유는 **중위 후속자가 왼쪽 자식을 가질 수 없기 때문**이다 (19,787건 전수 확인)
- **삽입 순서가 성능을 결정한다.** 정렬된 10,000개를 넣으면 높이 9,999에 탐색이 617배 느려지고, 기본 재귀 한계에서는 **997개에서 아예 죽는다**
- 그래서 실무에서는 자가 균형 트리를 쓴다. 파이썬 표준에는 없다

### 이번 자료를 관통하는 것

앞 노트의 **스택·큐·연결 리스트가 여기서 전부 재료로 쓰인다.** 연결 리스트 노드 3칸이 이진 트리 노드가 되고, 스택이 재귀 순회를 대신하고, 큐가 레벨 순회(BFS)를 만든다. **선형 자료구조는 비선형 자료구조를 다루기 위한 도구였다.**
