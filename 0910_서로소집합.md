# 서로소 집합과 유니온 파인드 — 대표자 하나로 "같은 집합인가"를 묻는다

> 출처: 알고리즘 과정 강의자료 · 정리일: 2026-09-10

---

## 0. 한 줄 요약

서로소 집합(Disjoint-set)은 **"이 두 원소가 같은 그룹에 속하는가"** 한 가지 질문에 특화된 자료구조다. 각 집합에서 **대표자(representative)** 한 명을 뽑아 두고, 질문을 **"두 원소의 대표자가 같은가"** 로 바꾼다. 그러면 집합 전체를 비교할 일이 없어진다.

구현은 배열 하나다. `parent[i]` 에 i의 부모를 넣고, 부모를 따라 올라가서 만나는 루트가 대표자다. 이 구조 위에서 연산은 셋뿐이다.

| 연산 | 하는 일 |
|---|---|
| `Make-Set(x)` | x 하나만 든 새 집합을 만든다 |
| `Find-Set(x)` | x가 속한 집합의 대표자를 돌려준다 |
| `Union(x, y)` | x가 속한 집합과 y가 속한 집합을 하나로 합친다 |

`Find-Set` 과 `Union` 이 이름을 합쳐 **유니온 파인드(Union-Find)** 라고도 부른다.

**이번에 원본 자료를 직접 돌려 보면서 확인한 것들** (전체는 16절)

1. ⭐ 자료의 「연산의 문제점」 그림(사슬)이 **바로 앞 슬라이드의 union 코드로는 재현되지 않는다.** 같은 코드에 **인자 순서를 뒤집어야** 그 그림이 나온다
2. ⭐ **무작위 입력이 최악 입력보다 더 나빴다.** 최적화 없는 유니온 파인드는 원소 100,000개 · 무작위 union 200,000회에서 **489,594.6 ms(8분 9.6초)** 가 걸렸다
3. 자료의 재귀형 `find_set` 은 기본 재귀 한계에서 **깊이 998** 까지만 성공하고, 「최종 완성 코드」는 그대로 붙여 쓰면 `NameError` 다

---

## 1. 왜 이 자료구조가 필요한가

### 1.1 "같은 집합인가"라는 질문의 비용

원소 N개를 그룹으로 나눠 파이썬 `set` 여러 개로 관리한다고 하자. "a와 b가 같은 그룹인가"를 물으면 그룹 목록을 훑어야 하고, 두 그룹을 합치려면 한쪽 원소를 다른 쪽으로 전부 옮긴 뒤 목록에서 하나를 지워야 한다. 질의와 병합이 각각 수십만 번 들어오는 문제에서는 이 방식이 통하지 않는다.

### 1.2 대표자라는 아이디어

원본 자료의 문장은 이렇다.

> 집합에 속한 하나의 특정 멤버를 통해 각 집합들을 구분한다. 이를 대표자(representative)라 한다.

이 한 줄이 자료구조 전체를 설계한다.

- 집합마다 대표자를 **한 명** 정해 둔다
- 원소는 자기 집합 전체를 모를 필요가 없다. **대표자로 가는 길만** 알면 된다
- "a와 b가 같은 집합인가" → `Find-Set(a) == Find-Set(b)`

집합의 **내용**을 비교하던 문제가 **대표자 하나를 비교**하는 문제로 바뀐다. 집합의 크기가 아무리 커져도 비교는 값 하나짜리다.

> **[그림]** Make-Set 을 4번 해서 홑원소 집합 {x} {y} {a} {b} 를 만들고, Union(x,y)·Union(a,b) 로 {x,y}, {a,b} 를 만들고, Union(x,a) 로 {x,y,a,b} 하나가 되는 과정. Find-Set(y) 는 x 를, Find-Set(b) 는 a 를 돌려준다.

### 1.3 정의

- **서로소(disjoint) 집합** 또는 **상호 배타 집합**은 서로 중복 포함된 원소가 없는 집합들이다. 다시 말해 **교집합이 없다**
- 두 집합 간에 공통 원소가 하나도 없을 때 이 두 집합을 서로소 집합이라 한다

여기서 중요한 것은 **분할(partition)** 이라는 성질이다. 전체 원소가 겹치지 않는 그룹들로 완전히 나뉜다. 그래서 "원소 x가 속한 집합"이 **유일하게 결정된다.** `Find-Set(x)` 이 값 하나를 돌려줄 수 있는 이유가 이것이다.

### 1.4 표현 방법 두 가지

원본 자료는 두 가지를 제시한다.

| 표현 | 방식 |
|---|---|
| **연결 리스트** | 같은 집합의 원소들을 하나의 연결리스트로 묶고, 맨 앞 원소를 대표자로 삼는다 |
| **트리** | 같은 집합의 원소들을 하나의 트리로 묶고, 루트를 대표자로 삼는다 |

실무에서 쓰는 건 **트리 표현**이다. 연결리스트 표현은 왜 밀렸는지를 보여 주는 비교 대상으로 뒤에서 따로 다룬다 (11절).

---

## 2. 트리 표현 — 자식이 부모를 가리킨다

### 2.1 방향이 거꾸로다

보통 트리는 부모가 자식을 가리킨다(`node.left`, `node.right`). 서로소 집합의 트리는 **반대**다.

- **자식 노드가 부모 노드를 가리킨다**
- **루트 노드가 대표자**가 된다

왜 거꾸로인가. 이 자료구조에서 필요한 연산이 **"위로 올라가기"** 뿐이기 때문이다. `Find-Set(x)` 은 x에서 루트까지 올라간다. 아래로 내려갈 일이 없다. 자식 목록을 들고 있을 필요가 없으니 **노드 하나당 포인터 한 개**면 끝난다.

그래서 트리 전체가 **정수 배열 하나**로 저장된다.

### 2.2 배열로 저장한 모습

자료의 예를 그대로 옮기면 이렇다. 집합은 {a}, {b}, {c, d, e, f} 세 개.

| 인덱스 | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 노드 | a | b | c | d | e | f |
| 부모 인덱스 | 0 | 1 | 2 | 2 | 2 | 4 |

읽는 법:

- `parent[0] = 0` → a의 부모가 a 자신 → **a는 루트**
- `parent[3] = 2` → d의 부모가 c
- `parent[5] = 4` → f의 부모가 e, `parent[4] = 2` → e의 부모가 c

`Find-Set(f)` 은 5 → 4 → 2 에서 `parent[2] == 2` 이므로 멈추고 **2(c)** 를 돌려준다.

> **[그림]** 자식이 부모를 가리키는 트리(a, b는 홀로 루트, c 아래 d와 e, e 아래 f)와 부모 인덱스 배열 `[0, 1, 2, 2, 2, 4]` 의 대응. 부모가 자기 자신인 칸이 루트다.

### 2.3 "부모가 자기 자신"이 루트 표시인 이유

`parent[root] = -1` 이나 `None` 로 표시할 수도 있지만, 그러면 값 하나를 특별 취급해야 한다. `parent[root] = root` 로 두면 종료 조건이 `while parent[x] != x` 한 줄로 끝나고, 초기화도 `list(range(n))` 한 줄이다. 초기 상태(모두 홑원소 집합)가 그대로 "모두가 자기 자신의 루트"라는 뜻이 된다.

### 2.4 자료의 트리 구성 과정

자료는 union을 하나씩 적용하며 트리가 자라는 과정을 보여 준다.

```text
Make-Set(a) ~ Make-Set(f)      →  a b c d e f 가 각각 셀프 루프 (집합 6개)
Union(c, d)                    →  d → c            {a} {b} {c,d} {e} {f}
Union(e, f)                    →  f → e            {a} {b} {c,d} {e,f}
Union(d, f)                    →  e → c            {a} {b} {c,d,e,f}
```

마지막 `Union(d, f)` 를 눈여겨볼 것. 인자는 **d와 f** 인데 실제로 부모가 바뀌는 건 **e** 다. union은 인자로 받은 원소가 아니라 **그 원소들의 루트**를 붙인다.

`find_set(d) = c`, `find_set(f) = e` 이므로 `parent[e] = c` 가 되고, 인자 d와 f는 손대지 않는다.

**두 트리의 루트를 잇는 것** — 이게 "합치기"의 정확한 의미다. 그래서 union의 비용은 사실상 **find 두 번의 비용**이고, 이 사실이 뒤(9절)에서 "무작위 입력이 왜 그렇게 느린가"의 답이 된다.

---

## 3. 연산 3종 — 자료의 원본 코드

자료의 코드를 한 글자도 고치지 않고 옮긴다. 각각에 걸리는 점을 따로 적었다.

### 3.1 make_set

```python
def make_set(n):
    """n개의 원소를 가진 초기 집합 생성"""
    return [i for i in range(n + 1)]

# 6개의 원소로 테스트
parent = make_set(6)
```

`[i for i in range(n + 1)]` 은 `list(range(n + 1))` 과 같다. `parent[i] = i`, 즉 **모두가 자기 자신의 루트**인 상태로 시작한다.

**걸리는 점 — 길이가 n이 아니라 n+1 이다.**

`make_set(6)` 이 돌려주는 리스트는 `[0, 1, 2, 3, 4, 5, 6]` 으로 **길이 7**이다. 그런데 바로 앞 절의 배열 표현 그림은 인덱스 0을 a에 배정한 **0-based** 였다. 같은 자료 안에서 인덱싱 규약이 어긋난다.

이건 오타라기보다 **관용**에 가깝다. 문제 풀이에서 정점 번호가 1부터 주어지는 경우가 압도적으로 많아서, 인덱스 0을 버리고 1..n 을 그대로 쓰는 게 편하다. `parent[3]` 이 곧 "3번 정점의 부모"가 되어 `-1` 오프셋 실수를 없앤다.

다만 규약을 밝히지 않으면 `for i in range(6)` 같은 루프에서 마지막 원소가 조용히 빠진다. 주석 한 줄로 못박아야 한다.

```python
def make_set(n):
    """정점 번호를 1..n 으로 쓴다 — 인덱스 0은 버린다"""
    return list(range(n + 1))
```

### 3.2 find_set

```python
def find_set(x):
    # 노드 x가 자기 자신을 부모 노드로 가지는 경우 그대로 반환
    if x == parent[x]:
        return x
    return find_set(parent[x])
```

부모를 따라 올라가서 자기 자신이 부모인 노드(루트)를 만나면 그걸 돌려준다.

**걸리는 점 세 가지**

1. **`parent` 를 인자로 받지 않고 전역에서 읽는다.** 전역 이름 하나에 묶여 있어서 같은 프로그램에서 서로소 집합을 두 개 쓸 수 없다.
2. **재귀형이다.** 트리가 깊어지면 파이썬 재귀 한계에 걸린다 (10절에서 측정).
3. **경로 압축이 없다.** 같은 x를 백 번 물어도 백 번 똑같이 올라간다.

전역 참조가 왜 위험한지는 [파이썬 스코프와 global](https://it-study-2002.tistory.com/entry/파이썬-스코프와-global) 편에서 정리한 규칙 그대로다. 읽기만 하면 `global` 선언 없이도 동작하는데, 그래서 "동작하니까 괜찮은 코드"로 착각하기 쉽다.

### 3.3 union

```python
def union(x, y):
    """두 집합을 합치기"""
    root_x = find_set(x)
    root_y = find_set(y)
    if root_x != root_y:
        parent[root_y] = root_x
```

- 두 원소의 루트를 각각 찾는다
- 이미 같은 루트면 아무것도 하지 않는다 (이미 같은 집합)
- 다르면 **`root_y` 를 `root_x` 밑에 붙인다**

마지막 줄이 이 글의 중심이다. `parent[root_y] = root_x` 는 **"y 쪽 트리를 x 쪽 트리에 붙인다"** 는 뜻이고, 이건 **대칭이 아니다.**

`union(x, y)` 와 `union(y, x)` 는 **같은 집합을 만들지만 다른 트리를 만든다.** 집합으로서의 결과는 같고, 자료구조의 모양은 다르다. 그리고 뒤의 성능은 집합이 아니라 **모양**이 결정한다.

---

## 4. ⭐ 검증 1 — 자료의 그림과 자료의 코드가 어긋난다

### 4.1 자료의 「연산의 문제점」 슬라이드

자료는 위 세 연산의 문제점을 이렇게 보여 준다.

```text
1. Union(f, e)
2. Union(e, d)
3. Union(d, c)
4. Union(c, b)
5. Union(b, a)
```

그림은 **a → b → c → d → e → f** 로 한 줄로 늘어진 사슬이다. 루트는 f. b가 노랑으로 강조되고 `Find-Set(b)` 라벨이 붙어, b에서 f까지 네 칸을 전부 타고 올라가는 굵은 화살표가 그려져 있다. 트리 높이가 원소 수에 비례해 탐색이 O(N)이 된다는 이야기다.

**내용 자체는 맞다.** 최적화가 없으면 편향 트리가 생길 수 있고 find가 O(N)이 된다.

문제는 **바로 앞 슬라이드의 코드로 저 다섯 줄을 실행하면 저 그림이 안 나온다**는 것이다.

### 4.2 직접 돌려 본 결과

```text
자료 코드 그대로 · Union(f,e) Union(e,d) Union(d,c) Union(c,b) Union(b,a)
  parent = a→f  b→f  c→f  d→f  e→f  f→f
  루트 = f, 트리 높이 = 1
  Find-Set(b) 가 타고 올라간 간선 수 = 1
```

**사슬이 아니라 높이 1짜리 별 모양이 나온다.**

손으로 따라가 보면 당연하다. `union(x, y)` 는 `parent[find(y)] = find(x)` 다.

```text
Union(f, e)  →  find(f)=f, find(e)=e  →  parent[e] = f
Union(e, d)  →  find(e)=f, find(d)=d  →  parent[d] = f     ← e가 아니라 f 밑에!
Union(d, c)  →  find(d)=f, find(c)=c  →  parent[c] = f
Union(c, b)  →  find(c)=f, find(b)=b  →  parent[b] = f
Union(b, a)  →  find(b)=f, find(a)=a  →  parent[a] = f
```

첫 인자가 항상 **이미 f에 병합된 원소**라서, 매번 `find` 가 f를 돌려주고 새 원소는 전부 f 바로 밑에 달린다. 사슬이 자랄 수가 없다.

### 4.3 그러면 어떻게 해야 그 그림이 나오는가

두 가지 길이 있다. 둘 다 돌려 봤다.

**(가) 대입 방향을 뒤집는다 — `parent[root_x] = root_y`.** 결과는 `a→a b→a c→b d→c e→d f→e`, 루트 a, 높이 5. 사슬은 나오지만 방향이 반대다. 루트가 **f가 아니라 a** 이고 `Find-Set(b)` 은 b → a 한 칸이면 끝나서, 자료 그림의 요점("b가 루트까지 다 훑는다")이 사라진다.

**(나) 코드는 그대로 두고 인자 순서를 뒤집는다.**

```text
자료 순서   Union(f,e)…Union(b,a)     높이 1   a→f b→f c→f d→f e→f f→f
뒤집은 순서 Union(a,b)…Union(e,f)     높이 1   a→a b→a c→a d→a e→a f→a
Union(b,a) Union(c,b) …               높이 5   a→b b→c c→d d→e e→f f→f
```

세 번째가 정확히 자료의 그림이다.

```text
Union(b, a)  →  parent[a] = b
Union(c, b)  →  parent[b] = c
Union(d, c)  →  parent[c] = d
Union(e, d)  →  parent[d] = e
Union(f, e)  →  parent[e] = f
```

**a → b → c → d → e → f, 루트 f, 높이 5.** `Find-Set(b)` 은 b → c → d → e → f 로 **간선 4개**를 탄다. 그림 그대로다.

> **[그림]** 같은 다섯 쌍을 자료의 호출 순서로 돌렸을 때(f를 중심으로 한 별, 높이 1, Find-Set(b)가 간선 1개)와 인자 순서만 뒤집었을 때(a→b→c→d→e→f 사슬, 높이 5, Find-Set(b)가 간선 4개)를 나란히 놓은 그림.

### 4.4 세 경우를 표로

| 호출 | 결과 parent | 높이 | 루트 | Find-Set(b) 간선 수 |
|---|---|---|---|---|
| `Union(f,e) … Union(b,a)` (자료 순서) | a→f b→f c→f d→f e→f f→f | 1 | f | 1 |
| `Union(a,b) … Union(e,f)` | a→a b→a c→a d→a e→a f→a | 1 | a | 1 |
| `Union(b,a) … Union(f,e)` | a→b b→c c→d d→e e→f f→f | **5** | **f** | **4** |

**자료 코드에서 사슬이 나오려면 "항상 새 원소를 첫 번째 인자 자리에" 놓아야 한다.**

### 4.5 그래서 무엇을 배우는가

이걸 "자료가 엉터리다"로 읽으면 얻는 게 없다. **그림도 맞고 코드도 맞다.** 둘을 잇는 **호출 순서가 뒤집혀 있을 뿐**이다.

진짜 교훈은 이쪽이다.

> **union 은 방향이 있는 연산이다.** 집합으로서의 결과는 대칭이지만, 만들어지는 트리는 대칭이 아니다. 그래서 union 을 쓸 때는 **어느 쪽이 어디에 붙는지** 반드시 못박고 써야 한다.

랭크나 크기 기준 없이 union을 쓰면 **입력이 트리 모양을 결정한다.** 그래서 최악 입력을 만들기가 쉽고, 문서에 "Union(x, y)" 라고만 적으면 읽는 사람이 방향을 알 수 없다. 코드 주석에 `parent[find(y)] = find(x)` 라고 한 줄 적는 편이 낫다.

랭크 최적화(7절)의 진짜 의미도 여기 있다. **인자 순서가 트리 모양을 결정하지 못하게 막는 것**이다.

---

## 5. 연산의 문제점 — 편향 트리

### 5.1 무엇이 문제인가

사슬이 만들어지면 이렇게 된다.

- `Find-Set(x)` 이 루트까지 전 경로를 타고 올라간다 → **O(N)**
- `Union(x, y)` 은 내부에서 `find` 를 두 번 부른다 → **역시 O(N)**
- 즉 **모든 연산이 O(N)** 이 되어, 그냥 배열을 선형 탐색하는 것과 다를 게 없어진다

> **[그림]** 사슬 a→b→c→d→e→f 에서 Find-Set(b)가 b→c→d→e→f 네 칸을 타고 올라가는 경로와, 원소 20,000개 사슬에서 전원 find 를 돌린 실측 (최적화 없음 3,461.4 ms / 경로 압축 2.8 ms).

### 5.2 원소 20,000개로 재 본 것

항상 사슬이 되는 순서로 union을 20,000번 하고, 그 다음 **전 원소에 대해 find** 를 한 번씩 돌렸다.

| 조합 | union(ms) | union 뒤 높이 | 전원 find(ms) | find 뒤 높이 |
|---|---|---|---|---|
| 최적화 없음 | 3.9 | 19,999 | **3,461.4** | 19,999 |
| 경로 압축만 | 3.8 | 19,999 | 2.8 | **1** |
| 랭크만 | 4.6 | **1** | 1.8 | 1 |
| 둘 다 | 5.0 | **1** | 1.9 | 1 |

읽을 게 많은 표다.

- **union 자체는 다 빠르다 (3.9 ~ 5.0 ms).** 사슬을 만드는 이 순서에서는 매번 새 원소가 첫 번째 인자라 `find` 가 즉시 끝난다. **비용은 나중에 온다**
- **최적화 없이 전원 find 는 3,461.4 ms.** 높이 19,999 짜리 사슬에서 20,000개 원소를 각각 훑으니 대략 2억 번의 포인터 추적이다
- **경로 압축만 켜면 union 직후 높이는 19,999 그대로다.** 경로 압축은 **find 를 할 때만** 일어나는데, 이 입력에서는 union 이 부르는 find 가 전부 한 칸짜리라 압축할 경로가 없다. **전원 find 를 한 번 돌리고 나서야** 높이가 1로 접힌다 — 그 한 번이 2.8 ms 다
- **랭크만 켜면 애초에 사슬이 안 생긴다.** union 뒤 높이가 이미 1이다. 랭크는 **사후 정리가 아니라 예방**이다
- **둘 다 켠 게 랭크만 켠 것보다 아주 살짝 느리다 (1.9 vs 1.8 ms).** 랭크가 이미 높이를 1로 만들어 놨으니 압축 로직의 분기 비용만 더해진 것이다. 오차 범위의 차이이고, 입력이 달라지면 뒤집힌다 (9절)

---

## 6. 최적화 1 — 경로 압축 (Path Compression)

### 6.1 아이디어

> Find-Set을 행하는 과정에서 만나는 모든 노드들이 직접 root를 가리키도록 포인터를 바꾸어 준다.

어차피 루트까지 올라갔다. **올라간 김에** 그 경로 위의 노드들 부모를 전부 루트로 바꿔 버린다. 다음번에 같은 노드를 물으면 한 칸이면 끝난다.

**"조사한 김에 정리한다"** 는 발상이다. 추가 순회 비용이 0이다.

### 6.2 그림으로

자료의 예는 이렇다. 루트 a 아래 b와 d가 있고, b 아래 c, d 아래 e, e 아래 f·g·h가 달린 트리에서 `Find-Set(h)` 을 부른다. h → e → d → a 로 올라갔으므로 **h와 e의 부모가 a로 바뀐다** (d는 이미 부모가 a다).

**f와 g는 그대로다.** 경로에 없었기 때문이다. 경로 압축은 **탐색에 사용된 경로만** 정리한다.

> **[그림]** Find-Set(h) 전후 트리. 경로 위의 e와 h가 루트 a를 직접 가리키게 갱신되고, 경로 밖의 f, g는 e 밑에 그대로 남는다.

### 6.3 코드

```python
# 최적화 전
def find_set(x):
    if x == parent[x]:
        return x
    return find_set(parent[x])


# 최적화 후
def find_set_pc(x):
    """경로 압축이 적용된 find_set"""
    if parent[x] == x:
        return x

    parent[x] = find_set_pc(parent[x])
    return parent[x]
```

바뀐 건 **한 줄**이다. 재귀 호출의 결과를 그냥 반환하지 않고 `parent[x]` 에 **먼저 써 넣은 뒤** 반환한다.

재귀가 되돌아오는 순서를 따라가면 이렇게 된다.

```text
find(h) → find(e) → find(d) → find(a) = a  (여기서 되돌아오기 시작)
                     parent[d] = a,  return a
          parent[e] = a,  return a
parent[h] = a,  return a
```

**되돌아오는 길에** 경로 위 노드가 차례로 루트에 직결된다. 이게 재귀형으로 쓰면 자연스럽게 나오는 이유다.

### 6.4 반복형 — 재귀 한계를 피하려면

재귀는 깊이 제한이 있다 (10절). 반복문으로는 **두 번 훑기**로 같은 일을 한다.

```python
def find_iter(parent, x):
    root = x
    while parent[root] != root:      # 1차: 루트를 찾는다
        root = parent[root]
    while parent[x] != root:         # 2차: 경로를 루트에 직결한다
        parent[x], x = root, parent[x]
    return root
```

두 번째 while의 `parent[x], x = root, parent[x]` 는 **오른쪽이 먼저 전부 평가된다.** `parent[x]` 를 덮어쓰기 전에 원래 부모를 x에 담아 둔다. 순서를 바꿔 쓰면 자기 자신으로 가는 무한 루프가 된다.

깊이가 아무리 깊어도 안전하고, 재귀 프레임 비용도 없다. **실전에서는 이 버전을 쓴다.**

한 번만 훑는 변형(**경로 절반**, path halving)도 있다. `parent[x] = parent[parent[x]]` 로 한 칸씩 건너뛰며 올라가면 경로가 매번 절반이 된다. 완전 압축은 아니지만 점근 복잡도는 같고 코드가 더 짧다.

---

## 7. 최적화 2 — 랭크를 이용한 Union (Union by Rank)

### 7.1 아이디어

> 각 노드는 자신을 루트로 하는 subtree의 높이를 rank로 저장한다. 두 집합을 합칠 때 rank가 낮은 집합을 rank가 높은 집합에 붙인다.

**낮은 나무를 높은 나무 밑에 붙이면 전체 높이가 안 늘어난다.** 반대로 하면 높이가 1 늘어난다.

4절에서 본 문제 — "인자 순서가 트리 모양을 결정한다" — 를 원천 차단한다. 누가 첫 번째 인자든 **랭크가 판단**한다.

### 7.2 두 경우

**랭크가 다를 때** — 낮은 쪽을 높은 쪽에 붙인다. **루트 랭크는 그대로.**

```text
트리 A: a(rank 2) ← b(1), d(0);  b ← c(0)
트리 B: e(rank 1) ← f(0), g(0)

합친 결과: a(rank 2 그대로) ← b(1), d(0), e(1)
```

높이 2짜리 나무 밑에 높이 1짜리를 매달아도 전체 높이는 2다. 안 늘어난다.

**랭크가 같을 때** — 아무 쪽이나 붙이고, **붙인 쪽 루트의 랭크를 1 올린다.**

```text
트리 A: a(rank 2) ← b(1), d(0);  b ← c(0)
트리 B: e(rank 2) ← f(1), g(0);  f ← h(0)

합친 결과: a(rank 3) ← b(1), d(0), e(2)
```

높이 2짜리 둘을 붙이면 한쪽이 다른 쪽 밑으로 들어가면서 높이가 3이 된다. **랭크가 오르는 건 이 경우뿐이다.**

> **[그림]** 랭크가 다를 때(2와 1 → 루트 랭크 2 유지)와 같을 때(2와 2 → 루트 랭크 3) 두 경우를 나란히. 랭크가 증가하는 자리를 강조.

### 7.3 코드

```python
def union_by_rank(x, y):
    """랭크를 이용한 최적화된 union"""
    root_x = find_set(x)
    root_y = find_set(y)

    if root_x != root_y:
        if rank[root_x] > rank[root_y]:
            parent[root_y] = root_x
        elif rank[root_x] < rank[root_y]:
            parent[root_x] = root_y
        else:
            parent[root_y] = root_x
            rank[root_x] += 1


parent, rank = make_set(6)
```

세 갈래 분기가 앞의 두 경우 그대로다.

- `rank[root_x] > rank[root_y]` → y를 x 밑에
- `rank[root_x] < rank[root_y]` → x를 y 밑에 (**인자 순서와 반대로 붙는다**)
- 같으면 → 아무 쪽이나 (여기서는 y를 x 밑에) 붙이고 `rank[root_x] += 1`

가운데 분기가 핵심이다. **`union(x, y)` 인데 x가 y 밑으로 들어간다.** "인자 순서가 트리 모양을 결정하지 못하게 만든다"가 코드로 드러난 자리다.

### 7.4 랭크는 높이의 "상한"이지 정확한 높이가 아니다

여기가 헷갈리기 쉬운 지점이다.

경로 압축을 같이 켜면 **실제 트리 높이는 줄어드는데 `rank` 값은 줄지 않는다.** 압축은 `parent` 만 건드리고 `rank` 는 손대지 않기 때문이다.

그래서 `rank[r]` 은 "r을 루트로 하는 트리의 정확한 높이"가 아니라 **"그 높이의 상한"** 이다. 실제 높이는 그보다 작거나 같다.

**그런데도 두 최적화를 같이 쓴다.** 랭크가 하는 판단은 "어느 쪽이 더 큰가"라는 **비교**뿐이라, 두 값이 나란히 조금씩 과대평가돼 있어도 비교 결과는 대체로 유지된다. 설령 어긋나도 정확성 문제가 아니라 트리 모양이 조금 나빠질 뿐이다. 둘을 같이 쓴 유니온 파인드의 연산당 상각 비용은 $O(\alpha(N))$ 로 증명돼 있다 ($\alpha$ 는 아커만 함수의 역함수로, 현실적인 N에서는 4 이하).

랭크를 정확한 높이로 유지하려 들면 압축할 때마다 조상들의 랭크를 다시 계산해야 하고, 그 비용이 압축의 이득을 다 까먹는다. **부정확한 채로 두는 게 이득**이다.

### 7.5 union by size — 랭크 대신 크기

랭크 대신 **원소 개수**를 기준으로 붙이는 변형이 있다.

```python
size = [1] * (n + 1)

def union_by_size(x, y):
    rx, ry = find_set(x), find_set(y)
    if rx == ry:
        return
    if size[rx] < size[ry]:
        rx, ry = ry, rx
    parent[ry] = rx
    size[rx] += size[ry]
```

| | union by rank | union by size |
|---|---|---|
| 기준 | 서브트리 높이의 상한 | 서브트리 원소 개수 |
| 갱신 | 랭크가 같을 때만 +1 | 합칠 때마다 더한다 |
| 값의 의미 | 압축 후 부정확해진다 | **항상 정확하다** |
| 덤 | 없음 | **집합 크기를 공짜로 알 수 있다** |

점근 복잡도는 동일하다. 실무에서는 **union by size 를 더 자주 쓴다.** "각 그룹에 몇 명 있는가"를 묻는 문제가 흔한데 size 배열이 그 답을 이미 들고 있다. 크기가 정확한 이유는 간단하다 — 경로 압축은 부모 링크만 짧게 만들 뿐 **어느 원소가 어느 루트에 속하는지**는 바꾸지 않는다.

---

## 8. 최종 완성 코드 — 그대로 쓰면 NameError

### 8.1 자료의 최종 코드

`make_set` 은 앞과 같고, 나머지 둘은 이렇다.

```python
def find_set(x):
    """경로 압축이 적용된 find_set"""
    if parent[x] != x:
        parent[x] = find_set(parent[x])
    return parent[x]


def union(x, y):
    """랭크 기반으로 최적화된 union"""
    root_x = find_set(x)
    root_y = find_set(y)

    if root_x != root_y:
        if rank[root_x] > rank[root_y]:
            parent[root_y] = root_x
        elif rank[root_x] < rank[root_y]:
            parent[root_x] = root_y
        else:
            parent[root_y] = root_x
            rank[root_x] += 1
```

### 8.2 무엇이 빠졌는가

**`parent, rank = make_set(n)` 이 없다.**

- `make_set` 은 **지역 변수** `parent`, `rank` 를 만들어 **반환**한다
- `find_set` 과 `union` 은 **전역** `parent`, `rank` 를 읽는다
- 그 전역 이름이 어디서도 만들어지지 않는다

그대로 붙여 넣고 `union(1, 2)` 를 부르면 이렇게 된다.

```text
NameError: name 'parent' is not defined
```

앞의 랭크 최적화 슬라이드에는 마지막 줄에 `parent, rank = make_set(6)` 이 있었다. 최종 코드 슬라이드에서 그 한 줄이 빠졌다.

### 8.3 정본 — 클래스로 감싼다

전역 이름에 묶이는 구조 자체가 문제다. 클래스로 감싸면 바인딩 누락도, "한 프로그램에서 두 개 못 쓴다"도 같이 해결된다.

```python
class DisjointSet:
    """서로소 집합 — 경로 압축 + union by rank"""

    def __init__(self, n):
        # 정점 번호를 1..n 으로 쓴다면 range(n + 1)
        self.parent = list(range(n))
        self.rank = [0] * n
        self.count = n              # 남아 있는 집합의 개수

    def find(self, x):
        """경로 압축 — 반복형이라 깊이 제한이 없다"""
        root = x
        while self.parent[root] != root:
            root = self.parent[root]
        while self.parent[x] != root:
            self.parent[x], x = root, self.parent[x]
        return root

    def union(self, x, y):
        """합쳤으면 True, 이미 같은 집합이었으면 False"""
        rx, ry = self.find(x), self.find(y)
        if rx == ry:
            return False
        if self.rank[rx] < self.rank[ry]:
            rx, ry = ry, rx
        self.parent[ry] = rx
        if self.rank[rx] == self.rank[ry]:
            self.rank[rx] += 1
        self.count -= 1
        return True

    def connected(self, x, y):
        return self.find(x) == self.find(y)
```

원본 대비 바꾼 것과 그 이유.

| 바꾼 것 | 이유 |
|---|---|
| 전역 → 인스턴스 속성 | 바인딩 누락이 구조적으로 불가능해진다. 여러 개를 동시에 쓸 수 있다 |
| 재귀 find → 반복 find | 깊이 제한이 사라진다 (10절) |
| `if rank[rx] < rank[ry]: rx, ry = ry, rx` | 3갈래 분기가 2줄로 준다. 항상 ry를 rx 밑에 붙이는 형태로 통일 |
| `union` 이 bool 반환 | **사이클 판정**에 그대로 쓴다 (12절) |
| `count` 필드 | 연결 요소 개수를 O(1)로 답한다 |
| `range(n)` (0-based) | 규약을 하나로 고정. 1-based 가 필요하면 `range(n+1)` 로 바꾸고 주석을 단다 |

---

## 9. 검증 — 무작위 입력이 최악 입력보다 나빴다

### 9.1 "무작위면 알아서 얕아지겠지"

5절의 실험은 **일부러 사슬을 만드는 최악 입력**이었다. "현실 입력은 저러지 않으니 최적화 없이도 괜찮지 않을까" 하는 생각이 자연스럽다.

재 봤다. 원소 100,000개에 **무작위** union 200,000회, 그 다음 **무작위** 연결 질의 200,000회.

| 조합 | union(ms) | 질의(ms) | 합계(ms) | 최종 높이 |
|---|---|---|---|---|
| 최적화 없음 | 135,389.2 | 354,205.5 | **489,594.6** | **16,936** |
| 경로 압축만 | 153.3 | 58.2 | 211.5 | 3 |
| 랭크만 | 119.9 | 76.9 | 196.9 | 7 |
| 둘 다 | 94.9 | 45.3 | **140.2** | **2** |

**489,594.6 ms = 8분 9.6초.** 둘 다 켠 140.2 ms 의 **3,492배**다.

트리 높이는 **16,936 대 2.**

> **[그림]** 무작위 입력 4조합의 합계 시간을 로그 스케일 막대로. 최적화 없음이 489,594.6 ms 로 혼자 튀고, 나머지 셋은 140~212 ms 구간에 몰려 있다. 최종 트리 높이는 16,936 / 3 / 7 / 2.

### 9.2 왜 이렇게 되나

이게 이번 자료에서 가장 반직관적인 결과다. **무작위 입력이 최악 입력보다 나빴다.**

주의할 것은 두 실험의 조건이 다르다는 점이다. 5절은 원소 20,000개에 union 20,000회 + find 20,000회였고, 여기는 원소 100,000개에 union 200,000회 + 질의 200,000회다. 그러니 "3,461 ms 대 489,594 ms" 를 곧바로 비교하면 안 된다.

그래도 **"무작위 union 은 알아서 얕은 트리를 만든다"** 는 통념이 깨지는 건 분명하다. 높이가 **16,936** 까지 자랐다. 원소가 100,000개니까 그 6분의 1 깊이다.

이유는 **union 자체가 find 를 두 번 부르기 때문**이다.

```python
def union(x, y):
    root_x = find_set(x)      # ← 여기서 O(높이)
    root_y = find_set(y)      # ← 여기서 또 O(높이)
    ...
```

- 최적화가 없으면 트리가 자란다
- 트리가 자라면 find 가 느려진다
- find 가 느려지면 **union 이 느려진다**
- union 이 느려지는 동안에도 트리는 계속 자란다

**되먹임 고리**다. 표에서 union 시간만 봐도 135,389.2 ms 대 94.9 ms 로 1,400배 넘게 벌어진다. "질의를 많이 해서 느린 것"이 아니라 **합치는 것 자체가 느려진다.**

### 9.3 최적화 하나만 켜도 되는가

| 조합 | 합계(ms) | 최종 높이 |
|---|---|---|
| 경로 압축만 | 211.5 | 3 |
| 랭크만 | 196.9 | 7 |
| 둘 다 | **140.2** | **2** |

어느 하나만 켜도 3,492배 중 대부분이 사라지고, 둘 다 켜면 하나만 켠 것보다 다시 1.4 ~ 1.5배 빨라진다. 5절(최악 입력)에서는 "둘 다"가 "랭크만"보다 아주 살짝 느렸는데 여기서는 반대다 — **입력에 따라 뒤집히니 둘 다 켜는 게 안전한 기본값**이다.

---

## 10. 검증 — 재귀 find_set 은 어디서 죽는가

### 10.1 자료의 코드는 재귀형이다

```python
def find_set(x):
    if x == parent[x]:
        return x
    return find_set(parent[x])
```

트리 깊이 = 재귀 깊이다. 사슬이 깊어지면 **느려지기 전에 먼저 죽는다.**

### 10.2 어디서 죽는지 이분 탐색으로 찾았다

완전한 사슬을 만들어 두고, `sys.setrecursionlimit` 을 바꿔 가며 성공하는 최대 깊이를 찾았다.

```text
setrecursionlimit(1,000)   → 깊이 998 까지만 성공, 999 부터 RecursionError
setrecursionlimit(10,000)  → 깊이 9,998 까지만 성공, 9,999 부터 RecursionError
```

### 10.3 왜 1,000이 아니라 998인가

**한계값 = 사용 가능한 깊이가 아니다.** 두 칸이 이미 쓰여 있다.

- 모듈 최상위 프레임 하나
- `find_set` 을 부르는 바깥 함수 프레임 하나

그 위에서 재귀가 시작되니 실제 여유는 998이다. 10,000으로 올려도 정확히 같은 두 칸이 빠져 9,998이 된다.

즉 **"한계 - 2"** 가 실제 한도다. 호출 스택이 더 깊은 곳에서 부르면 더 줄어든다. 그래서 "한계를 N으로 잡았으니 깊이 N까지 된다"고 계산하면 경계에서 터진다.

### 10.4 대응 — 세 가지

**(가) 반복형으로 바꾼다 (권장).** 6.4절의 `find_iter` 를 쓴다. 깊이 제한이 사라지고 프레임 비용도 없다.

**(나) 한계를 올린다.** `sys.setrecursionlimit(300000)` 은 동작하지만, 파이썬 재귀 한계는 실제 C 스택 크기와 무관해서 너무 올리면 `RecursionError` 대신 **세그멘테이션 폴트**로 죽는다. `RecursionError` 는 잡을 수 있지만 세그폴트는 못 잡는다.

**(다) 애초에 깊어지지 않게 한다.** 랭크나 크기 기준 union을 쓰면 트리 높이가 log N 이하로 유지된다. N = 1,000,000 이어도 높이는 20 근처다. 이게 최적화의 숨은 효과다 — 빨라지는 것만이 아니라 **터지지 않게 된다.**

---

## 11. 참고 — 연결리스트 표현

### 11.1 자료의 설명

- 같은 집합의 원소들은 하나의 연결리스트로 관리한다
- 연결리스트의 **맨 앞의 원소를 집합의 대표 원소**로 삼는다
- 각 원소는 **집합의 대표원소를 가리키는 링크**를 갖는다

그림에는 각 집합마다 `rep`(대표) 와 `tail`(꼬리) 포인터가 그려져 있다.

```text
{rep=a, tail=e}   a → d → e        (d, e 의 rep 링크가 a 를 가리킨다)
{rep=b, tail=f}   b → f
{rep=c, tail=c}   c

Union(a, b) 후
{rep=a, tail=f}   a → d → e → b → f    (b, f 의 rep 가 a 로 갱신된다)
```

`Find-Set(e)` 은 e의 rep 링크를 한 번 읽으면 끝난다 → **O(1)**. 트리 표현보다 find 가 빠르다.

### 11.2 코드

노드는 `data`, `rep`(대표자 링크), `next` 세 칸을 갖고, `make_set` 은 노드를 하나 만들어 딕셔너리에 넣는다. `find_set` 은 `sets_map[data].rep` 을 그대로 돌려주면 끝이다. 문제는 `union` 이다.

```python
def union(sets_map, data1, data2):
    """두 집합을 합치기"""
    rep_node1 = find_set(sets_map, data1)
    rep_node2 = find_set(sets_map, data2)

    if rep_node1 is None or rep_node2 is None or rep_node1 == rep_node2:
        return

    tail_node1 = rep_node1
    while tail_node1.next is not None:      # ① tail 을 매번 다시 찾는다
        tail_node1 = tail_node1.next

    tail_node1.next = rep_node2

    current_node = rep_node2
    while current_node is not None:         # ② rep 를 전원 갱신한다
        current_node.rep = rep_node1
        current_node = current_node.next
```

**걸리는 점 두 가지**

**① 그림의 tail 포인터가 코드에 없다.** 그림에서는 각 집합이 tail을 들고 있다고 해 놓고, 코드는 `while` 로 매번 끝까지 훑는다. O(리스트 길이)다. tail을 유지하면 O(1)로 끝날 일이다.

**② 작은 집합을 큰 집합에 붙이는 최적화(weighted union)가 없다.** `rep_node2` 쪽 원소 전부의 rep 링크를 갱신하는데, 어느 쪽이 작은지 안 본다. 큰 집합을 작은 집합에 붙이면 갱신 대상이 최대가 된다.

두 문제가 겹치면 union 한 번이 O(N)이 되고, N번 하면 **O(N²)** 이다.

### 11.3 실측

원소 N개를 하나의 집합으로 합치는 union을 N-1회 돌렸다.

| N | 연결리스트(ms) | 배열 + 경로 압축 + 랭크(ms) | 배수 |
|---|---|---|---|
| 1,000 | 4.6 | 0.2 | 18배 |
| 2,000 | 18.1 | 0.5 | 37배 |
| 4,000 | 69.1 | 0.9 | 79배 |
| 8,000 | 263.6 | 1.8 | **148배** |

**N이 2배가 될 때 연결리스트는 4배씩 늘어난다.**

```text
4.6  →  18.1   (3.9배)
18.1 →  69.1   (3.8배)
69.1 → 263.6   (3.8배)
```

교과서에서 "O(N²)" 이라고 적는 것의 실물이다. 배열 표현은 같은 구간에서 0.2 → 1.8 로 **선형에 가깝게** 늘어난다.

> **[그림]** N = 1,000 / 2,000 / 4,000 / 8,000 에서 연결리스트 표현과 배열 + 경로 압축 + 랭크의 시간을 로그 스케일 막대로 비교. 배수가 18 → 37 → 79 → 148 로 벌어진다.

### 11.4 그래도 배울 점은 있다

연결리스트 표현이 못 쓸 물건인 건 아니다. **트레이드오프가 반대**일 뿐이다.

| | 연결리스트 표현 | 트리 표현 |
|---|---|---|
| Find-Set | **O(1)** (rep 링크 한 번) | O(α(N)) — 사실상 상수 |
| Union | O(작은 쪽 크기) | **O(α(N))** |
| 집합 원소 나열 | **O(집합 크기)** — 쉽다 | 어렵다 (자식 정보가 없다) |
| 메모리 | 노드당 포인터 2개 | 정수 1개 |

**find 가 압도적으로 많고 union 이 거의 없다면** 연결리스트가 유리할 수 있고, 집합 원소를 실제로 나열해야 하는 경우도 그렇다 — 트리 표현은 자식 정보를 버렸기 때문에 나열하려면 전체를 훑어야 한다. 게다가 weighted union (작은 쪽을 큰 쪽에 붙이기)을 넣으면 연결리스트도 전체 O(N log N) 까지 개선된다. **자료의 코드에 그게 빠져 있는 것**이 문제지 표현 자체가 틀린 건 아니다.

---

## 12. 응용 — 사이클 판정

### 12.1 간선을 하나씩 넣으면서 판정한다

무방향 그래프에 사이클이 있는지 보려면, 간선 `(u, v)` 를 하나씩 넣으면서 **넣기 전에 이미 같은 집합인지** 확인하면 된다.

```python
def has_cycle(n, edges):
    ds = DisjointSet(n)
    for u, v in edges:
        if not ds.union(u, v):     # union 이 False → 이미 같은 집합
            return True            # 이 간선이 사이클을 닫는다
    return False
```

`union` 이 bool을 반환하게 만들어 둔 게 여기서 쓰인다. `False` 는 "합칠 게 없었다", 곧 **u와 v가 이미 연결돼 있었다**는 뜻이고, 그 상태에서 간선을 하나 더 놓으면 사이클이 생긴다.

### 12.2 DFS 방식과의 대비

[DFS와 BFS 완전정리](https://it-study-2002.tistory.com/entry/DFS와-BFS-완전정리) 편에서 DFS로 사이클을 판정하는 방법을 정리했다. 방문 배열을 두고 내려가다가 **부모가 아닌 이미 방문한 정점**을 만나면 사이클이다.

두 방식은 성격이 다르다.

| | DFS 방식 | 유니온 파인드 방식 |
|---|---|---|
| 필요한 것 | **그래프 전체**가 먼저 있어야 한다 | 간선을 **하나씩** 받아 처리한다 |
| 처리 시점 | 오프라인 (전부 모아 놓고 한 번에) | **온라인** (도착하는 대로) |
| 찾아 주는 것 | 사이클의 **경로**를 복원할 수 있다 | 사이클 **존재 여부**만 |
| 방향 그래프 | 재귀 스택 상태로 판정 가능 | **곤란하다** (무방향 전용) |
| 비용 | O(V + E) | O(E · α(V)) |

**"간선이 스트리밍으로 들어오는가"** 가 갈림길이다. "i번째 간선을 추가한 순간 처음으로 사이클이 생긴다면 i는?" 같은 문제는 유니온 파인드로 자연스럽게 풀리고 DFS로는 매번 다시 돌려야 한다. 반대로 사이클의 **실제 경로**를 출력해야 하면 DFS 쪽이다. 그리고 방향 그래프에서는 유니온 파인드를 그대로 쓸 수 없다 — `union` 은 대칭 관계를 다루는데 방향 간선은 대칭이 아니다.

### 12.3 연결 요소 개수

`count` 필드를 두면 그대로 나온다.

```python
ds = DisjointSet(n)
for u, v in edges:
    ds.union(u, v)
print(ds.count)          # 연결 요소 개수
```

초기값이 n(전부 홑원소)이고 실제로 합쳐질 때만 1씩 줄인다. 정점 V개, 간선 E개인 무방향 그래프가 **트리**인지 판정하려면 `E == V - 1` 이면서 `count == 1` 이면 된다.

---

## 13. 전체 복잡도 정리

| 연산 | 최적화 없음 | 경로 압축만 | 랭크만 | 둘 다 |
|---|---|---|---|---|
| Make-Set | O(1) | O(1) | O(1) | O(1) |
| Find-Set | O(N) | 상각 O(log N) | O(log N) | **상각 O(α(N))** |
| Union | O(N) | 상각 O(log N) | O(log N) | **상각 O(α(N))** |

$\alpha(N)$ 는 아커만 함수의 역함수로 현실적인 N에서는 4 이하다. "상각(amortized)"은 연산 하나하나가 아니라 **전체 평균**이라는 뜻으로, 경로 압축은 한 번 크게 일하고 그 뒤로 계속 싸게 먹힌다. 공간은 `parent` 배열 O(N), 랭크나 크기를 쓰면 O(N) 하나 더.

---

## 14. 이 자료구조가 어디로 이어지는가

- **최소 신장 트리** — 간선을 가중치 순으로 넣되 사이클을 만드는 간선은 버린다. 12절의 사이클 판정이 그대로 쓰인다. 유니온 파인드가 없으면 매 간선마다 연결 여부를 다시 탐색해야 해서 실용적이지 않다
- **그래프 연결 요소 관리** — 간선이 추가되는 동안 "이 둘이 연결돼 있는가"를 계속 답해야 하는 문제. 12.3절의 `count` 가 그 자리다
- **격자 문제** — `(r, c)` 를 `r * W + c` 로 평탄화하면 그대로 1차원 유니온 파인드가 된다
- **동치 관계 관리** — "a와 b가 같다"는 정보가 순서 없이 들어올 때 모순을 판정하는 문제. 같다는 관계의 반사·대칭·추이 세 성질이 정확히 서로소 집합의 구조다

---

## 15. 체크리스트

- [ ] `Find-Set` 은 **루트를 돌려준다**. 원소 자체가 아니라 대표자다
- [ ] `Union(x, y)` 은 x와 y가 아니라 **x와 y의 루트**를 붙인다
- [ ] `parent[find(y)] = find(x)` 인지 그 반대인지를 **주석에 못박는다.** 인자 순서가 트리 모양을 바꾼다
- [ ] 랭크나 크기 기준 없이 union을 쓰면 **입력이 트리 모양을 결정한다**
- [ ] 경로 압축은 **find 를 할 때만** 일어난다. union 직후의 높이를 보고 "압축이 안 된다"고 판단하면 안 된다
- [ ] 재귀 `find_set` 은 기본 한계에서 **깊이 998** 까지다. 반복형을 쓴다
- [ ] `make_set(n)` 의 길이가 n인지 n+1인지 확인한다. 0-based 인지 1-based 인지 한 프로젝트 안에서 **하나로 고정**한다
- [ ] `union` 이 **bool 을 반환**하게 만들면 사이클 판정이 한 줄이 된다
- [ ] 집합 크기가 필요하면 랭크 대신 **size** 를 쓴다. 공짜로 따라온다
- [ ] 최적화 둘 다 켜는 것이 기본값이다. **입력에 따라 어느 하나가 유리한지 뒤집힌다**

---

## 16. 실행으로 확인한 것 (요약)

| # | 확인한 것 | 결과 |
|---|---|---|
| 1 | 자료 union 코드 + 자료 호출 순서 | 사슬이 아니라 **높이 1 별**, `Find-Set(b)` 간선 1개 |
| 2 | 같은 코드 + 인자 순서 뒤집기 | **높이 5 사슬, 루트 f**, `Find-Set(b)` 간선 4개 → 자료 그림 그대로 |
| 3 | 최악 입력 20,000개 · 전원 find | 최적화 없음 **3,461.4 ms** / 경로 압축 2.8 / 랭크 1.8 / 둘 다 1.9 |
| 4 | 최악 입력 · union 직후 높이 | 없음 19,999 / 압축만 19,999 / **랭크만 1** / 둘 다 1 |
| 5 | 최악 입력 · 전원 find 뒤 높이 | 없음 19,999 / **압축만 1** / 랭크만 1 / 둘 다 1 |
| 6 | 재귀 find_set 한계 | 한계 1,000 → **깊이 998** / 한계 10,000 → **깊이 9,998** |
| 7 | 무작위 100,000개 · union 20만 · 질의 20만 | 없음 **489,594.6 ms** / 압축만 211.5 / 랭크만 196.9 / **둘 다 140.2** |
| 8 | 무작위 입력 최종 트리 높이 | 없음 **16,936** / 압축만 3 / 랭크만 7 / **둘 다 2** |
| 9 | 무작위 입력 최대 배수 | **3,492배** |
| 10 | 연결리스트 표현 N=1,000→8,000 | 4.6 → 18.1 → 69.1 → 263.6 ms (**N 2배마다 약 4배**) |
| 11 | 연결리스트 대 배열, N=8,000 | **148배** 차이 |
| 12 | 자료의 최종 완성 코드 | `parent, rank = make_set(n)` 누락 → **NameError** |
