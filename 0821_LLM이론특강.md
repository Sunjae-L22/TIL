# LLM 특강 — Post-training·RLHF, IR·RAG, 에이전트·MCP, 플래닝·LangChain

> 출처: SSAFY `자료/AI/4_1_LangChain서비스개발.pdf` (218p, 이미지 전용) · 정리일: 2026-08-21
> 강사: 연세대학교 인공지능학과 여진영 교수 (Language & AGI Lab)
> ⚠️ **파일명과 내용이 다르다.** 파일명은 "LangChain서비스개발"이지만 실제 내용은 **4개 차시로 구성된 LLM 이론 특강**이고, LangChain은 4차시 말미(p.191~198)에 소개 수준으로만 등장한다. p.3의 목차(Pre-training/Instruction-tuning/RLHF)는 1차시 것만 적힌 것이다. 3_1_1/3_1_2 때와 같은 파일명-내용 불일치 사례.

## 자료의 정체와 구조

```text
1차시 (p.1~62)    : LLM Post-training — Instruction-tuning, RLHF, DPO, RLVR
2차시 (p.63~122)  : Retrieval-augmented LM — 정보 검색(IR) 이론과 RAG의 과제
3차시 (p.123~162) : LLMs with Tool Usage — LLM Agent 개념, 툴 러닝 계보, MCP
4차시 (p.163~218) : AI Agents & Langchain — 환경 표현, 플래닝, LangChain, 실습 미리보기
```

- 같은 날 정리한 **`07_Langchain` 코드 실습(0821 TIL·포스트 3편)의 이론 배경편**에 해당한다. 실습이 "어떻게 만드는가"라면 이 특강은 "왜 그렇게 만들며, 그 부품들은 어떻게 학습되는가"다.
- 기존 0811 포스트(텍스트 파운데이션 모델)가 개요 수준으로 다룬 정렬(SFT·RLHF)을 이 특강은 **수식·실험 레벨**로 다루고, DPO·RLVR·IR 이론·에이전트 이론은 기존 정리 어디에도 없던 **완전 신규**다. (맨 끝 "기존 정리와의 관계" 절 참고)

## 한눈에 보기

- Pre-training(다음 토큰 예측)만으로 부족한 이유는? GPT-3의 "달 착륙 설명" 실패 예가 보여주는 것은?
- Instruction-tuning은 단일 태스크 파인튜닝과 무엇이 다른가? Flan-T5·Super-NaturalInstructions(1.6K+ 태스크)의 "unseen 태스크 평가"란?
- MMLU(57과목)·K-MMLU는 무엇을 재는 벤치마크인가?
- Self-Instruct → Alpaca(52K) 파이프라인은 어떻게 도는가? LIMA(1K)의 교훈("Less is More")은?
- Instruction-tuning의 3가지 한계는 무엇이고, 왜 Human Preference(RLHF)가 필요한가?
- InstructGPT의 3단계(SFT → Reward Model → PPO)를 그릴 수 있는가?
- Reward Model을 절대 점수가 아닌 **pairwise 비교**로 학습하는 이유와 Bradley-Terry 손실 $J_{RM}(\phi)=-\mathbb{E}[\log\sigma(RM_\phi(x,y^w)-RM_\phi(x,y^l))]$의 의미는?
- Reward hacking과 RM 과최적화(over-optimization) 그래프가 경고하는 것은?
- DPO는 RLHF에서 무엇을 제거했나? RLVR(검증 가능한 보상)은 무엇이고 DeepSeek-R1과 어떻게 이어지나?
- Retrieval-augmented LM의 4구성요소(Datastore/Query/Index/LM)와 RAG를 쓰는 4가지 이유는?
- Sparse Retriever(TF-IDF: $w_{x,y}=\mathrm{tf}_{x,y}\times\log(N/\mathrm{df}_x)$)와 Dense Retriever의 차이는? Bi-encoder 대조 학습과 Cross-encoder는 언제 갈라 쓰나?
- RAG의 4대 과제(컨텍스트 길이 / 검색 노이즈→환각 / 지식 충돌 / 정보 통합)와 RGB 벤치마크의 4능력은?
- 에이전트의 정의(AIMA)와 LLM-first vs Agent-first 관점은? o1 같은 추론 모델은 왜 "에이전트"가 아닌가?
- 툴 러닝 계보 — WebGPT → Toolformer(self-supervised 필터링) → ToolLLM(16,464 API) → RL·Web-Shepherd — 각 단계가 해결한 것은?
- MCP의 2계층(Data Layer=JSON-RPC / Transport Layer)과, MCP 이전의 벤더별 툴 호출 규격 문제는?
- 에이전트의 환경 표현(텍스트 ALFWorld / 이미지 Touchdown / Accessibility Tree)과 탐색 연구(SteP·AWM·curiosity·BAGEL)는?
- Local vs Global planning의 구분과 ReACT / Plan-and-Solve / Reflexion / CoAct의 위치는?

---

## [1차시] LLM Post-training — Instruction-tuning과 RLHF (p.1~62)

### 표지·강사 소개·목차 (p.1-6)

- **p.1 (표지)**: "Post-training (Instruction-tuning, RLHF)" — SSAFY(Samsung SW AI Academy For Youth) 특강 표지.
- **p.2 (교수님 소개)**: 연세대학교 여진영 교수.
  - 주요 경력: 20년~현재 연세대 인공지능학과 교수 / 20년~현재 Language & AGI Lab Director / 22~24년 Market Designers Chief AI Officer / 18~20년 SKT Brain Research Scientist / 15~16년 Adobe Research Data Scientist Intern.
  - 수상: ACL 2024 최우수논문상(Outstanding Paper), 한국 KCC 2023 최우수논문상, DSTC8 대화모델 챌린지 세계 2위(2019).
  - 하단 그림: "Large Language Model-Brained GUI Agents: A Survey"(Microsoft) 타임라인 — WoB, WGE, Qweb, DOM-Q-NET, FLIN, WebGPT, WebShop(<2023) → DroidBot-GPT, WebArena, WebAgent, AutoDroid, AppAgent(2023) → Auto-GUI, WebGUM, LASER, Zero-shot Agent, OpenAgents, MM-Navigator, WebVoyager, Mobile-Agent, SeeAct, CoCo-Agent, DUAL-VCR, UFO, OS-Copilot, Cradle, AutoWebGLM, MMAC-Copilot(2024) → GUI Narrator, Mobile-Experts, Agent-E, Search-Agent, Agent Q, Openwebagent, WebPilot, NaviQAte, Steward, Hybrid Agent, **WMA(국내 대학 유일)**, AgentOccam, NNetnav, MobA, Agent S, AutoGLM, LiMAC, TinyClick, OSCAR(2024 후반~2025+).
- **p.3 (CONTENTS)**: 1. Pre-training vs Post-training / 2. Instruction-tuning / 3. Reinforcement Learning from Human Feedback (RLHF) / 4. What's Next?
- **p.4 (학습 목표)**:
  - 거대 언어모델의 학습 패러다임에 대한 이해를 한다.
  - Instruction-tuning에 대해 설명할 수 있다.
  - RLHF의 개념을 이해하고 instruction-tuning과의 차이를 이해한다.
  - RLHF 이후의 post-training 접근들을 이해한다.
- **p.5**: 절 표지 "0. 학습 시작(오버뷰)".
- **p.6 (0. 학습 시작)**: 네 가지 질문 오버뷰.
  - 거대언어모델의 학습 패러다임이 어떻게 되는가? → Pre-training(사전 학습)과 post-training(사후 학습)의 개념과 차이.
  - 언어모델이 어떻게 사용자의 질문에 응답을 할 수 있게 되었는가 → Instruction-tuning 개념.
  - 언어모델이 어떻게 사용자가 선호하는 답변을 내도록 학습이 되었는가 → Instruction-tuning의 한계와 RLHF 개념.
  - RLHF 이후에 post-training 접근들은 어떤 게 있는가? → RLHF의 한계점과 이후 접근들 소개 (**DPO, RLVR**).

### 1. Pre-training vs Post-training (p.7-11)

- **p.7**: 절 표지 "1. Pre-training vs Post-training".
- **p.8 (학습 패러다임 2단계)**:
  - 최근 거대 언어 모델 학습은 크게 **두 가지 단계**로 나뉜다.
  - **Pre-training (사전 학습)**: 방대한 텍스트 데이터를 통해 **언어와 지식을 배우는 단계**.
  - **Post-training (사후 학습)**: 사람이 원하는 방식으로 **대화하고, 안전하고, 유용하게 만드는 단계**.
- **p.9 (Pre-training)**:
  - 방대한 텍스트 데이터를 이용한 **Self-supervised learning**을 통해 언어 패턴, 지식 등을 배운다.
  - 학습 목표: **다음 단어 예측 (Next Token Prediction)**.
  - 예시: "내일은 비가 ___" → "온다"의 확률을 가장 높게 만들도록 파라미터 업데이트.
  - 그림: Decoder(Transformer, LSTM, …)에 "Iroh goes to make tasty tea"를 넣고 각 위치에서 다음 토큰(goes/to/make/tasty/tea/[END])을 예측하는 Next Token Prediction 다이어그램 (CS224n 자료 스타일).
- **p.10 (Pre-training 후 응답 예시)**:
  - 다음 단어 예측에는 강점이 있지만 **질문에 대한 대답을 하지는 않음**.
  - 예: PROMPT "달 착륙을 6살 어린이에게 몇 문장으로 설명해줘." → GPT-3 COMPLETION: "중력 이론을 6살 어린이에게 설명해줘. / 상대성이론을 6살 어린이에게 몇 문장으로 설명해줘. / 빅뱅 이론을 …. / 진화론을 …." (질문을 흉내낸 나열만 계속 — InstructGPT 논문의 유명 예시 한국어판).
- **p.11 (Post-training)**:
  - **유저의 의도를 파악하고** 원하는 답변을 모델이 응답하도록 사후 학습을 진행한다.
  - 대표 기법: **Instruction-tuning, RLHF(Reinforcement Learning from Human Feedback), DPO(Direct Preference Optimization), RLVR(Reinforcement Learning with Verifiable Reward)**.
  - 같은 프롬프트에 대해 Human(사후 학습된 모델이 목표로 하는) COMPLETION 예: "거대한 로켓이 지구에서 우주비행사들을 태우고 달로 날아갔어요. …달 돌을 가져왔어요." (질문에 실제로 답함).

### 2. Instruction-tuning — 개념 (p.12-16)

- **p.12**: 절 표지 "2. Instruction-tuning".
- **p.13 (사전 학습 후 언어모델의 한계)**:
  - 사전 학습 후 거대 언어 모델은 **유저의 의도와 일치하지 않음** (not aligned with user intent).
  - 이를 해결하기 위해 **파인 튜닝** 진행.
  - 그림(CS224n): Step 1 Pretrain (on language modeling) — "Lots of text; learn general things!" / Step 2 Finetune (on your task) — "Not many labels; adapt to the task!" (감성분류 예: "… the movie was …" → ☺/☹).
- **p.14 (파인 튜닝이란?)**:
  - 이미 **사전 학습**된 기존 모델에 특정 작업이나 도메인에 맞게 **추가로 학습시키는 과정**.
  - 그림 흐름: Massive Data →(Transfer Learning)→ Base LLM →(Finetuning + Train Data)→ Finetuned Model ↔ User(Query/Answer) → Validation & Monitoring →(Re-Finetuning)→ Train Data로 피드백 루프.
- **p.15 (Instruction-tuning이란? 1)**:
  - 언어모델이 **사람이 내린 지시문(instruction)을 따르도록 학습**하는 단계.
  - **정답 레이블**이 요구되며, **다양한 태스크**를 풀 수 있도록 적응하는 것에 중점을 둠.
  - 그림: p.13과 같은 2단계 그림이지만 Step 2가 "Finetune (on **many tasks**)" / "~~Not~~ many labels; adapt to the tasks!"로 바뀜 — 단일 태스크 파인튜닝과의 차이 강조.
- **p.16 (Instruction-tuning이란? 2 — Flan-T5 예)**:
  - **다양한 태스크**에서 (지시문, 응답) 쌍을 모아 언어모델을 파인튜닝한다.
  - 학습 예: "Please answer the following question. What is the boiling point of Nitrogen?" → "-320.4F" / "Answer the following question by reasoning step-by-step. The cafeteria had 23 apples…" → "…23-20=3. …3+6=9." (CoT 형식 데이터 포함).
  - **새로운(모델이 학습하지 않은) 태스크에서 평가**를 진행한다. 예: "Can Geoffrey Hinton have a conversation with George Washington? Give the rationale before answering." → "Geoffrey Hinton is a British-Canadian computer scientist born in 1947. George Washington died in 1799. Thus, they could not have had a conversation together. So the answer is 'no'."
  - 출처 표기: Flan-T5; Chung et al., 2022.

### Instruction-tuning — 데이터 스케일과 평가 벤치마크 (p.17-22)

- **p.17 (더 많은 태스크를 가진 데이터 학습)**:
  - 대부분의 경우와 마찬가지로, **데이터와 모델의 크기**가 핵심이다.
  - **Super-NaturalInstructions** 데이터셋은 **1.6K+ 태스크, 3M+ 예시**로 구성. (슬라이드 표기는 "Super-NatrualInstructions" — 오타)
  - 질문 제기: 이런 데이터로 학습한 거대 언어모델은 어떻게 평가해야 하는가?
  - 그림: Super-NaturalInstructions 태스크 버블맵 — Translation, Question Answering, Program Execution, Sentiment Analysis, Question Generation, Text Matching, Toxic Language Detection, Named Entity Recognition, Text Categorization, Commonsense Classification, Information Extraction, Textual Entailment 등 수십 개 태스크 유형.
- **p.18 (평가 벤치마크: MMLU)**:
  - **MMLU (Massive Multitask Language Understanding)**: **57개의 지식을 요구하는 태스크**에서 언어모델 성능을 평가하는 벤치마크.
  - 그림: 과목별 GPT-3(파랑) vs UnifiedQA(초록) vs Random(빨간 선) 정확도 막대 그래프 — Abstract Algebra, Anatomy, Astronomy, Business Ethics, Clinical Knowledge, College Biology/Chemistry/CompSci/Mathematics/Medicine/Physics, Computer Security, Conceptual Physics, Econometrics, Electrical Engineering, Elementary Mathematics, Formal Logic, Global Facts, High School Biology/Chemistry/CompSci/European History 등.
- **p.19 (MMLU 예시, 한국어 번역판)**:
  - 천문학 문제: "Type-Ia 초신성에 대해 옳은 것은?" A. 쌍성계(binary systems)에서 발생 / B. 젊은 은하에서 발생 / C. 감마선 폭발을 냄 / D. 많은 양의 X선을 냄.
  - 고등학교 생물 문제: "기린 개체군에서 환경 변화로 키가 큰 개체들이 더 유리해져 생존·번식에 성공. 어떤 선택 예시인가?" A. directional selection(방향성 선택) / B. stabilizing selection(안정화 선택) / C. sexual selection(성선택) / D. disruptive selection(분단 선택).
- **p.20-21 (K-MMLU; Son et al., 2024)**:
  - 한국어 지식 벤치마크 K-MMLU. 표 구조: Required Type of Korean Knowledge를 **Cultural / Regional / Legal / Other** 4열로 나누고, 카테고리(STEM / Applied Science / HUMSS / Other)별 예시 문항 제시.
  - p.20 예시(STEM/Applied Science): Civil-Engineering "우리나라 대도시지역의 주요 문제라고 볼 수 없는 것은?", Ecology "한반도의 생태축에 속하지 않는 것은?", Civil-Engineering(Legal) "주택건설기준 등에 관한 규정상 도로 및 주차장의 경계선으로부터 공동주택 외벽까지 최소 이격 거리?", Biology(Other) "생물자원센터(KCTC) 홈페이지 균주 검색 결과에 포함되지 않는 것은?", Geomatics "지적제도사에서 토지대장을 양전도장(量田都帳)이라 명칭하였던 시대는?", Maritime-Engineering "동해안에서 남서풍이 오래 불면 어떤 현상이 일어나는가?", Energy-Management "'개발행위허가'만으로 태양광 발전소를 건설할 수 있는 관리지역의 면적제한 기준은 최대 몇 m² 미만인가?", Gas-Technology "대구 도시가스 폭발사고의 주원인은?"
  - p.21 예시(HUMSS/Other): Korean-History "'대한국'에 대한 설명으로 옳지 않은 것은?", Accounting "K-IFRS하에서 금융자산으로 분류되지 않는 것은?", Management "최근 우리나라 소매 경영 환경 변화로 가장 옳지 않은 것은?", Food-Processing "약주나 탁주를 제조하는 제국방법이 아닌 것은?", Agricultural-Sciences "배추 F1품종 종자생산이 남해안 도서 지방에 집중된 이유", "국가품종목록 등재신청시 등재 절차", Health "한국간호사 윤리강령 항목".
- **p.22 (MMLU에서의 발전)**:
  - 지식 중심 벤치마크에서 빠르고 눈에 띄는 성과를 거두고 있다.
  - 그림(Papers with Code 리더보드 추이): GPT-2(~33%, '19) → UnifiedQA(~49) → GPT-3(~54, '20) → Gopher (few-shot, k=5)(~60, '21말) → Chinchilla (few-shot, k=5)(~68, '22) → Flan-PaLM(~75, '22말) → GPT-4(~86.4, '23) → Gemini Ultra (CoT-SC@32)(~90, '24 초).

### Instruction-tuned 최신 모델 성능 (p.23-24)

- **p.23 (최근 거대 언어 모델들)**:
  - 좌측(Google 발표 그림): Gemini Ultra MMLU **90.0% (CoT@32)** vs Human expert 89.8%(MMLU) vs 이전 SOTA GPT-4 **86.4% (5-shot, reported)**. 주석: 이전 SOTA 모델들의 평가는 서로 다른 프롬프팅 기법 사용.
  - 우측(Claude 3 benchmarks 표, 열: Claude 3 Opus / Sonnet / Haiku / GPT-4 / GPT-3.5 / Gemini 1.0 Ultra / Gemini 1.0 Pro):
    - MMLU(5-shot): 86.8 / 79.0 / 75.2 / 86.4 / 70.0 / 83.7 / 71.8
    - GPQA Diamond(0-shot CoT): 50.4 / 40.4 / 33.3 / 35.7 / 28.1 / – / –
    - GSM8K: 95.0 / 92.3 / 88.9 / 92.0 / 57.1 / 94.4(Maj1@32) / 86.5(Maj1@32)
    - MATH(4-shot): 60.1 / 43.1 / 38.9 / 52.9 / 34.1 / 53.2 / 32.6
    - MGSM(0-shot): 90.7 / 83.5 / 75.1 / 74.5(8-shot) / – / 79.0 / 63.5
    - HumanEval(0-shot): 84.9 / 73.0 / 75.9 / 67.0 / 48.1 / 74.4 / 67.7
    - DROP F1(3-shot): 83.1 / 78.9 / 78.4 / 80.9 / 64.1 / 82.4 / 74.1
    - BIG-Bench-Hard(3-shot CoT): 86.8 / 82.9 / 73.7 / 83.1 / 66.6 / 83.6 / 75.0
- **p.24 (Meta Llama 3 Instruct model performance)**:
  - 8B vs Gemma 7B-It(Measured) vs Mistral 7B Instruct(Measured): MMLU(5-shot) 68.4/53.3/58.4, GPQA(0-shot) 34.2/21.4/26.3, HumanEval(0-shot) 62.2/30.5/36.6, GSM-8K(8-shot CoT) 79.6/30.6/39.9, MATH(4-shot CoT) 30.0/12.2/11.0.
  - 70B vs Gemini Pro 1.5(Published) vs Claude 3 Sonnet(Published): MMLU 82.0/81.9/79.0, GPQA 39.5/41.5(CoT)/38.5(CoT), HumanEval 81.7/71.9/73.0, GSM-8K 93.0/91.7(11-shot)/92.3(0-shot), MATH 50.4/58.5(Minerva prompt)/40.5.

### Instruction-tuning 전/후 응답 비교 — 한국어 대명사 해석 예 (p.25-26)

- **p.25 (Fine Tuning 이전)**:
  - 모델 입력(대명사 해석 문제): 질문 "다음 문장에서 대명사가 누구를 지칭하는지 설명하거나, 애매하다면 '애매하다'라고 답하세요." / 문장 "기자와 셰프가 그들의 좋아하는 요리에 대해 논의할 것입니다." / 보기 (A) 기자의 좋아하는 요리를 논의한다 (B) 셰프의 좋아하는 요리를 논의한다 (C) 애매하다 / 답변: "단계별로 생각해 봅시다."
  - 지시 기반 Fine Tuning **이전** 모델 출력: "기자와 셰프가 기자의 좋아하는 요리를 논의할 것입니다. / …셰프의… / …기자와 셰프의…" — ✗ 질문에 답하지 못함(문장 변형만 나열).
- **p.26 (Fine Tuning 이후)**:
  - 같은 입력에 대해 지시 기반 Fine Tuning **이후**: "'기자와 셰프가 그들의 좋아하는 요리에 대해 논의할 것입니다.'라는 문장은 누가 좋아하는 요리인지를 명확히 나타내지 않기 때문에, **정답은 (C)입니다.**" — 근거 제시 + 정답 선택.

### Instruction-tuning에서 배운 것: Self-Instruct·Alpaca와 LIMA (p.27-28)

- **p.27 (이로부터 우리는 무엇을 배웠는가? 1 — 데이터 자동 생성)**:
  - 지시문(instructions), 입력(inputs), 출력(outputs)을 **언어모델로부터 생성** [Wang et al., 2022 — Self-Instruct].
  - **Alpaca**: LLaMA 7B 모델을 **52K개의 instruction-following 데이터**로 파인 튜닝.
  - 파이프라인 그림: 175 Self-Instruct seed tasks → Text-davinci-003으로 Modified Self-instruct Instruction Generation → 52K Instruction-following examples → Meta LLaMA 7B에 Supervised Finetuning → **Alpaca 7B**.
  - 데이터 예: 기존 태스크 "새해 결심으로 가능한 목록을 브레인스토밍하시오." → 출력 "체중 감량하기/운동 더 하기/더 건강하게 먹기" ; 생성된 태스크 "회의실을 디자인하기 위한 창의적인 아이디어를 브레인스토밍하시오." → "…가변형 벽과 가구 같은 유연한 요소를 활용하는 아이디어 포함…".
- **p.28 (이로부터 우리는 무엇을 배웠는가? 2 — 소량 고품질 데이터)**:
  - Instruction-tuning에는 Pre-training에 비해 많은 데이터가 필요하지 않음 (e.g., **"LIMA: Less is More for Alignment"**, Zhou et al., 2023).
  - LIMA(1K 수준 고품질 예시로 학습)와의 인간 선호 비교 그래프(LIMA wins/Tie/LIMA Loses): vs Alpaca 65B 53/21/26%, vs DaVinci003 44/21/35%, vs BARD(April) 33/25/42%, vs Claude(April) 24/22/54%, vs GPT-4(April) 18/25/57%.
  - LIMA 학습 데이터 구성 표(출처 → 예시 개수): STEM 관련 Q&A 200 / STEM 이외 주제 Q&A 200 / 생활·팁·방법론 How-to 200 / Reddit r/WritingPrompts 글쓰기 프롬프트·댓글 150 / 자연어 처리 태스크 설명 데이터셋(명령 기반 학습용) 50 / 논문 저자들이 직접 작성한 학습 태스크 예시 200. (합계 1,000)

### 3. RLHF — Instruction-tuning의 한계와 Human Preference (p.29-34)

- **p.29**: 절 표지 "3. Reinforcemen Learning from Human Feedback (RLHF)" (슬라이드 원문 오타: Reinforcemen → Reinforcement).
- **p.30 (Instruction-tuning의 한계 1)**:
  - 명확한 한계: "정답 데이터를 수집하는 것이 비싸다". 그 외 문제들:
  - **문제 1**: 개방형/창의적 생성 태스크에는 정답이 존재하지 않는다. Ex) "개와 개가 기르는 메뚜기에 대한 이야기를 만들어줘".
  - **문제 2**: 언어 모델링은 모든 토큰 레벨의 오류를 동일하게 취급하지만, 어떤 오류는 다른 오류보다 심하게 작용한다.
  - **문제 3**: 사람이 만든 답변(정답 레이블)이 최적이 아닐 수 있다.
- **p.31 (Instruction-tuning의 한계 2)**:
  - Instruction-tuning을 하더라도, 언어모델의 목표(LM objective)와 "**인간의 선호를 만족시키는 것**" 사이에는 여전히 불일치가 존재한다.
  - 그렇다면 인간의 선호(Human Preference)를 만족시키려면 어떻게 해야 할까?
  - 그림: LM이 "Avatar is a fantasy TV show"를 생성하는 예 — "fantasy" 자리에 "adventure", "show" 자리에 "musical"(취소선) 등 토큰 대체 오류의 심각도가 서로 다름을 보여줌 (문제 2의 시각화).
- **p.32 (인간의 선호도란? 1)**:
  - 단답형 질문: Q "상대성 이론을 발표한 과학자는 누구인가?" → A "알베르트 아인슈타인" (정답이 명확).
  - 주관식 질문: Q "아인슈타인이 오늘날 살아있다면, 현대 과학기술에 어떤 기여를 했을까?" (정답이 하나가 아님 → 선호 문제).
- **p.33-34 (같은 주관식 질문에 대한 4개 모델 응답 비교)**:
  - p.33 Claude Sonnet4: 양자컴퓨팅·양자정보학 혁신 기여, "으스스한 원거리 작용"(양자얽힘) 탐구, 양자암호학·양자통신, 중력파 검출·블랙홀 물리학, 끈이론·루프 양자중력, AI·머신러닝의 수학적 기초 등.
  - p.33 Copliot(원문 표기; Copilot의 오타): 양자 컴퓨팅과 인공지능의 이론적 기반 재정립, 상대성 이론은 GPS·우주 탐사에 필수, 다중우주·암흑물질 통찰, 과학 윤리·인간성의 균형 강조.
  - p.34 Gemini 2.5 flash: 통일장 이론 완성 시도(중력·전자기력·핵력 등 네 가지 기본 힘 통합, '모든 것의 이론'), 양자역학과의 조화(양자 중력 이론 연구) 등.
  - p.34 GPT-5: 양자정보과학·인공지능·우주론 관여, 블랙홀·중력파 연구, 양자컴퓨팅 근본적 이해, 데이터+수학적 모델링으로 AI 이론적 토대, 기후 변화·에너지 문제에 물리학적 통찰, 새로운 패러다임을 여는 사상가.
  - 요지: 모두 그럴듯하고 스타일이 다름 → 어느 답이 "더 좋은가"는 선호(preference)의 문제.

### RLHF — InstructGPT와 보상 최적화의 정식화 (p.35-39)

- **p.35 (InstructGPT, 2022)**: 논문 표지 "Training language models to follow instructions with human feedback" — Long Ouyang, Jeff Wu, Xu Jiang, Diogo Almeida, Carroll L. Wainwright, Pamela Mishkin, Chong Zhang, Sandhini Agarwal, Katarina Slama, Alex Ray, John Schulman, Jacob Hilton, Fraser Kelton, Luke Miller, Maddie Simens, Amanda Askell, Peter Welinder, Paul Christiano, Jan Leike, Ryan Lowe (OpenAI).
- **p.36 (Intuition: Human-in-the-loop ML)**: 순환 다이어그램 — Human Annotator → Labeled Data → Training Model → Selecting Confusing Data → Human Annotator (반복). 사람이 라벨을 만들고 모델이 헷갈려하는 데이터를 다시 사람이 라벨링하는 루프.
- **p.37 (Optimizing for human preferences — 정식화)**:
  - 요약 태스크를 예로: 지시문 $x$와 언어모델 응답 $y$에 대해 인간이 준 보상 $R(x,y) \in \mathbb{R}$을 얻을 수 있다고 상상 (점수가 높을수록 좋음).
  - 예: 지진 기사 $x$에 대해 요약 $y_1$("샌프란시스코에서 지진이 발생했습니다. 재산 피해는 경미했으며, 부상자는 없었습니다.") → $R(x,y_1)=8.0$; $y_2$("베이 지역은 날씨가 좋지만 지진과 산불에 취약합니다.") → $R(x,y_2)=1.2$.
  - 목표: 언어모델 응답의 **기대 보상(expected reward) 최대화**:
    $$\mathbb{E}_{\hat{y}\sim p_\theta(y|x)}\left[R(x,\hat{y})\right]$$
- **p.38 (RLHF 파이프라인 큰 그림 — InstructGPT 3단계)**:
  - 1단계 = Instruction-tuning, 2·3단계 = 보상 최대화.
  - **Step 1** 시범 데이터 수집 및 감독 학습: 데이터셋에서 프롬프트 샘플링("Explain the moon landing to a 6 year old") → 사람 주석자가 이상적인 답변 작성("Some people went to the moon…") → 이 정답 데이터로 GPT-3 같은 기본 모델을 미세 조정(fine-tuning, SFT).
  - **Step 2** 비교 데이터 수집 및 보상 모델 학습: 동일 질문에 모델이 여러 답변(A,B,C,D) 생성 → 사람 주석자가 최선~최악 순위 매김(D>C>A=B) → 순위 데이터로 각 답변에 점수를 매기는 **보상 모델(RM)** 훈련.
  - **Step 3** 강화학습(PPO)으로 정책 최적화: 새로운 프롬프트("Write a story about frogs") → 정책 모델(Policy Model)이 답변 생성("Once upon a time…") → 보상 모델이 점수 $r_k$ 계산 → **PPO(Proximal Policy Optimization)**로 정책 업데이트 (반복 루프).
- **p.39 (RLHF 큰 그림, 단순화 도식)**: 입력 → LLM → 출력 A/B/C → 사람 평가 → 데이터 비교(B > A > C) → 이 (Input, Output) 비교 데이터로 Reward Model 학습 → Reward Model이 보상 산출 → 강화 학습으로 LLM 업데이트 → 다시 출력 생성 (순환).

### Reward Model 학습 — 비교 기반 선호 모델링과 Bradley-Terry (p.40-42)

- **p.40-41 (Human preference를 어떻게 모델링할 것인가?)**:
  - 문제: 인간의 판단(절대 점수)은 일관성이 떨어지고, 기준이 어긋날 수 있다.
  - 해결: 직접 점수를 매기지 않고 **응답을 비교(pairwise comparison)하는 방식** 활용.
  - 해당 비교 데이터로 **리워드 모델(Reward Model)**을 학습.
  - p.41 예: 지진 기사 요약 3개 — $y_1$(원문 발췌식 요약) > $y_3$(핵심 요약) > $y_2$(엉뚱한 요약) 순으로 슬라이드 상단 비교 표기가 있으나, 하단 텍스트는 "사람이 보기에는 $y_2 > y_1 > y_3$"라고 씀 (아래 '오류 목록' 참조 — 그림에서 문서 배치는 y1, y3, y2 순이고 '>' 기호가 사이에 있음. 즉 그림은 y1 > y3 > y2를 시사, 텍스트는 y2 > y1 > y3).
  - 비교 결과가 리워드 모델 학습의 데이터가 됨.
- **p.42 (Reward Model 학습 수식 — Bradley-Terry [1952] paired comparison model)**:
  - Reward Model $RM_\phi(x, y)$: 프롬프트+응답 토큰("The Bay Area … wildfires")을 입력받아 스칼라 점수(예: 1.2) 출력.
  - 학습 목적함수:
    $$J_{RM}(\phi) = -\mathbb{E}_{(x, y^w, y^l)\sim D}\left[\log \sigma\left(RM_\phi(x, y^w) - RM_\phi(x, y^l)\right)\right]$$
    여기서 $y^w$ = "winning" sample, $y^l$ = "losing" sample; $y^w$가 $y^l$보다 높은 점수를 받아야 한다.
  - 예시(Ex): Human preference $y_2 > y_1 > y_3$ → Pairwise Labels: $y_2 > y_1$, $y_1 > y_3$, $y_2 > y_3$.
  - 손실 전개:
    $$Loss = -\sum \log\sigma\left(RM(x,y^+) - RM(x,y^-)\right) = -\left(\log\sigma(RM(x,y_2)-RM(x,y_1))\right) - \left(\log\sigma(RM(x,y_1)-RM(x,y_3))\right) - \left(\log\sigma(RM(x,y_2)-RM(x,y_3))\right)$$

### 왜 강화학습인가 + RLHF의 효과 (p.43-44)

- **p.43 (어떻게 최적화를 할 것인가? … 강화학습)**:
  - 강화학습(Reinforcement Learning, RL)은 오랫동안 이러한/관련된 문제(보상 최대화)를 연구해 온 분야.
  - 2013년 전후로 RL이 딥러닝·게임 플레이에 적용되며 다시 급증 (그림: Nature "Learning Curve" 표지 — DQN Atari; AlphaGo 로고).
  - 언어모델에 RL을 적용하려는 관심은 비교적 최근인 **2019년부터** [Ziegler et al., 2019; Stiennon et al., 2020; Ouyang et al., 2022].
    - RL을 언어모델에 적용하는 것은 어렵게 여겨짐 (현재도 그럼).
    - 언어모델에 적용될 수 있는 RL 알고리즘이 최근에 나옴 (e.g., **PPO** [Schulman et al., 2017]).
- **p.44 (RLHF는 pre-training과 instruction-tuning보다 더 나은 성능 향상을 제공한다)**:
  - 그림(Learning to summarize from human feedback, Stiennon et al., 2020): 모델 크기(1.3B/2.7B/6.7B/12.9B) 대비 "Fraction preferred to ref"(참조 요약 대비 인간 선호 비율).
    - $p^{RL}(y|x)$ Human feedback(RLHF): 0.6→0.7 (reference 0.5 선을 크게 상회)
    - Reference summaries: 0.5 (점선)
    - $p^{IFT}(y|x)$ Supervised learning: ~0.39→0.43
    - $p^{PT}(y|x)$ Pretrain only: ~0.22→0.36
  - 즉 RLHF 모델만이 사람이 쓴 참조 요약보다 선호되는 수준(>0.5)에 도달.


<!-- p.44/45 경계: 이하 RLHF 절이 이어진다 -->

### 3. RLHF — 리워드 모델 검증 (p.45)

- 주의점: **리워드 모델이 잘 작동하는지 먼저 잘 확인해야 한다**
- 그래프 (InstructGPT/OpenAI 리워드 모델 스케일링 그래프로 보임):
  - x축: Model size (10^8 ~ 10^10, 로그 스케일), y축: Validation accuracy (0.60~0.80)
  - 데이터 규모별 곡선 4개: 8k / 16k / 32k / 64k (클수록 위쪽)
  - 상단 기준선 2개: `Ensemble of humans` (~0.83, 일점쇄선) > `Human baseline` (~0.77, 점선)
  - 64k 곡선의 최대 모델에서 Validation accuracy ≈ 0.75
- 슬라이드 결론(녹색 주석): "충분히 많은 데이터로 학습한 큰 크기의 리워드 모델은 **단일 인간의 평가 수준에 근접**한다"

### 3. RLHF — ChatGPT 사례 (p.46)

- **ChatGPT = Instruction-tuning + RLHF**
- ChatGPT는 RLHF를 통한 발전에 힘입어 거대언어모델 중 **처음으로 상업적으로 활용**됨
- OpenAI 블로그 "ChatGPT: Optimizing Language Models for Dialogue" 발췌:
  - InstructGPT와 같은 방법(RLHF)을 사용하되 데이터 수집 방식만 약간 다름
  - 초기 모델은 supervised fine-tuning: 인간 AI 트레이너가 **유저와 AI 양쪽 역할을 모두 연기**한 대화 데이터 사용
  - 트레이너에게 모델이 작성한 제안(model-written suggestions)을 제공해 응답 작성을 도움
  - 이 대화 데이터셋을 InstructGPT 데이터셋(대화 형식으로 변환)과 혼합
- Note: OpenAI(및 유사 기업)는 ChatGPT 학습 세부사항(데이터, 학습 파라미터, 모델 크기)을 경쟁 우위를 위해 비공개
- 출처: https://openai.com/index/chatgpt/

### 4. What's Next? — 섹션 표지 (p.47)

### 4. What's Next? — 리워드 모델링의 한계점 (p.48~50)

- **인간의 선호도는 일관성이 부족함**
  - **리워드 해킹(Reward hacking)**: 강화학습에서 자주 발생하는 문제 (p.48)
    - 예시 그림: OpenAI "Faulty reward functions" — 보트 레이싱 게임(CoastRunners)에서 점수(SCORE 10500)만 최대화하며 코스를 돌지 않고 아이템만 먹는 에이전트
    - 출처: https://openai.com/blog/faulty-reward-functions/
  - 챗봇들은 정답 여부와 관계없이 **생산적이고 도움이 되어 보이는** 정답을 생성하게 된다 (p.49)
  - 이러한 결과들은 **환각(Hallucination)** 문제를 발생시킨다
    - 사례 1: "Google shares drop $100 billion after its new AI chatbot makes a mistake" (NPR, 2023-02-09) — Bard 오답 사건
    - 사례 2: "Bing AI hallucinates the Super Bowl" — "The most recent Super Bowl was Super Bowl LVI, Eagles, who defeated the Kansas City Chiefs by 31-24" (실제로는 열리지도 않은/잘못된 결과를 단정)
- **인간의 선호도를 학습한 리워드 모델은 더 일관성이 부족함!** (p.50)
  - 그래프: "Reward model over-optimization" (InstructGPT 계열 실험)
    - x축: **KL from supervised baseline** (0, 2, 5, 10, 25, 75, 250)
    - y축: Fraction preferred to ref (0.2~1.0)
    - 점선 `RM prediction`: KL이 커질수록 계속 상승해 1.0 근접
    - 실선 `Actual preference`: KL≈10 부근에서 정점(~0.47) 후 **하락**, KL=250에서 급락
    - 함의: 리워드 모델 점수를 과도하게 최적화하면(정책이 기준 모델에서 멀어지면) RM 예측과 실제 인간 선호가 괴리 → **과최적화(over-optimization)**

### 4. DPO (p.51)

- 슬로건: **"RLHF에서 'RL'을 제거하자"**
- **Direct Preference Optimization (DPO)**; Rafailov et al., 2023
- 비교 다이어그램 (DPO 논문 Figure 1):
  - **RLHF**: preference data ($y_w \succ y_l$, x: "write me a poem about the history of jazz") → maximum likelihood로 **reward model** 학습 → label rewards ↔ sample completions 반복하며 **reinforcement learning**으로 LM policy 최적화
  - **DPO**: preference data → **maximum likelihood**로 **final LM**을 직접 학습 (리워드 모델·RL 단계 없음)

### 4. RLVR (p.52)

- 아이디어: RLHF에서 **수학 문제처럼 답이 분명한 문제들은 정답 여부로 리워드를 주자**
- **Reinforcement Learning with Verifiable Reward (RLVR)**; Lambert et al., 2024 (Tülu 3), DeepSeek-AI, 2025
- 대표적인 성공 사례: **DeepSeek-R1**
- 다이어그램:
  - Training data → 프롬프트 $s_i$ → **Policy** $\pi_\theta(\cdot)$ → completions $a_i$
  - **Verifiable Reward**: $r = \begin{cases} \gamma & \text{if correct} \\ 0 & \text{otherwise} \end{cases}$ → scalar reward $r_i$가 Policy로 피드백
  - Policy Update: $\theta_{t+1} = \theta_t + \alpha \nabla_\theta J(\pi_\theta)$

### 확인문제 1 — Pre-training vs Post-training 분류 (p.53~54)

- 문항: 각 설명이 Pre-training인지 Post-training인지 고르기
  - A. 방대한 인터넷 텍스트 데이터로 언어 패턴과 지식 학습
  - B. 사람의 의도를 파악하고 원하는 방식으로 대화하도록 학습
  - C. Next Token Prediction 방식으로 학습
  - D. Instruction-tuning, RLHF, DPO, RLVR 등의 기법 활용
  - E. 정확한 답변보다 문장 연결성(언어 패턴)에 집중
- **정답: Pre-training: A, C, E | Post-training: B, D**
- 해설 요지:
  - A: 인터넷 텍스트로 Self-supervised Learning → 언어 구조·지식 학습 단계
  - B: 사용자의 의도(Instruction) 이해·대화형 응답 학습 단계
  - C: 문맥 기반 다음 단어 예측(Next Token Prediction)
  - D: RLHF, DPO, RLVR 등은 사람의 피드백을 반영해 모델을 정교하게 다듬는 과정
  - E: 문맥적 자연스러움엔 강하지만 질문 의도 파악 능력은 부족한 시기

### 확인문제 2 — Instruction-tuning (p.55~56)

- 문항: Instruction-tuning 설명으로 옳지 **않은** 것?
  - ① 사전학습된 LM이 사람의 지시문을 따르도록 추가 학습시키는 과정
  - ② Fine-tuning의 한 종류로, 특정 작업(Task)/도메인에 맞게 조정
  - ③ 주로 Self-supervised learning을 통해 대규모 비라벨 데이터로 학습된다
  - ④ 다양한 작업(지시문-응답 쌍)으로 새로운 태스크에도 일반화
  - ⑤ Alpaca, Flan-T5 등 모델 학습에 사용된 대표적 방법
- **정답: ③** — Instruction-tuning은 지도학습(Supervised fine-tuning) 형태로, 사람이 작성한 지시문-응답 쌍(라벨이 명확한 데이터)을 사용. 비라벨(Self-supervised)이 아님.

### 확인문제 3 — RLHF (p.57~58)

- 문항: RLHF 설명으로 옳지 **않은** 것?
  - ① 사람의 피드백 기반으로 보상(reward)을 학습
  - ② 보상 모델(Reward Model)은 사람의 선호를 점수 형태로 근사
  - ③ 강화학습(PPO 등)을 통해 모델이 보상을 극대화하도록 학습
  - ④ 항상 객관적인 보상을 사용할 수 있으며, 환각 문제는 발생하지 않는다
  - ⑤ ChatGPT 같은 대화형 모델 학습에 활용됨
- **정답: ④** — 사람의 판단은 일관되지 않고 주관적이라 객관적 보상이 어려움 → Reward hacking·Hallucination 발생 가능

### 확인문제 4(슬라이드 번호는 "3") — DPO (p.59~60)

- 문항: DPO의 특징으로 옳은 것?
  - ① RLHF와 달리 별도의 보상 모델을 학습해야 한다 (X — 반대)
  - ② 강화학습(RL) 단계를 제거하여 학습을 단순화하였다
  - ③ 사람의 피드백을 전혀 사용하지 않는다 (X — 선호 데이터 사용)
  - ④ 보상 대신 자동 채점기(Verifier)로 보상 계산 (X — 그것은 RLVR)
  - ⑤ 계산량이 많고 학습 안정성이 낮은 편이다 (X)
- **정답: ②** — DPO는 RL 단계를 제거하고 사람이 비교한 선호 데이터(preference data)로 언어모델의 확률 분포를 직접 조정. 보상 모델·PPO 없이 RLHF 수준 성능 달성.
- ※ 표기 문제: 이 문제 번호가 앞 문제와 같은 "3"으로 중복되어 있음 (4가 맞음)

### 강의 정리 (p.61~62)

- **Pre-training**: 인터넷 텍스트 등 대규모 비라벨 데이터로 언어 구조·지식 학습
- **Post-training**: 사전학습 모델을 사람 의도에 맞게 조정. 대표 방식: Instruction-tuning(지시문 학습), RLHF(인간 피드백 기반 강화학습)
- **Instruction-tuning**: Fine-tuning의 한 종류. 지시문-응답 쌍, 지도학습 기반 → 명확한 태스크 수행 능력 습득. 데이터셋 예시: **Super-NaturalInstructions, Alpaca, Flan-T5**
- **RLHF**: 인간 피드백으로 '사람이 선호하는 답변' 학습. 한계점: Reward Hacking, Hallucination
- **RLHF 이후의 진화**:
  1. **DPO**: RL 제거, 선호 데이터를 직접 확률 최적화
  2. **RLVR**: 인간 피드백 대신 정답 검증이 가능한 문제에서 자동으로 보상 계산


---

## [2차시] Retrieval-augmented LM — 정보 검색과 RAG 이론 (p.63~122)

### 표지·목차·학습목표 (p.63~65)

- 제목: **Retrieval-augmented Language Models (Information Retrieval, RAG)**
- CONTENTS:
  1. Basic Concepts of Retrieval-augmented LM
  2. Information Retrieval
  3. Retrieval-augmented LM
- 학습 목표:
  - 검색증강 언어모델(Retrieval-augmented LM)의 용어 이해
  - 정보 검색(Information Retrieval)의 검색 메서드들 이해
  - 검색증강 언어모델 이해

### 0. 학습 시작(오버뷰) (p.66~67)

- Retrieval-augmented LM이 무엇일까? → 용어 이해
- Information Retrieval이 무엇일까? → 정보 검색의 활용 + 검색기(Retriever) 이해
- Retrieval-augmented LM은 언제 사용해야 할까? → 필요한 이유와 도전과제 이해

### 1. Basic Concepts (p.68~75)

#### Retrieval-augmented LM 정의 (p.69)
- **추론 시(test time) 외부 데이터 저장소를 불러와 활용하는 언어모델**
- 그림: Training time에는 LM이 코퍼스로 학습("The capital city of Ontario is **Toronto**")
  - Test time 비교: Typical LM은 파라미터 기억만으로 빈칸을 채움 vs Retrieval-based LM은 **Datastore!**를 참조해 답함

#### 구성요소 4가지 (p.70): **Datastore, Query, Index, Language Model**
- 공통 다이어그램: Datastore → Index ← Query, LM(Input) + 검색된 텍스트 조각 결합

#### Datastore (p.71)
- **가공되지 않은 대규모 텍스트 코퍼스**
- 최소 수십억~수조 단위의 토큰으로 구성
- 라벨링된 데이터셋이 아님
- 지식베이스(Knowledge base) 같은 **구조화된 데이터가 아님**

#### Query (p.72)
- 검색 질의 / Retrieval input
- **언어모델의 입력(input)과 같아야 하는 것은 아니다** (LM 질의 ≠ 검색 쿼리 가능)

#### Index (p.73)
- 문서나 단락 같은 검색 가능한 항목들을 체계적으로 정리해 더 쉽게 찾을 수 있도록 하는 것
- 각 IR 메서드는 인덱싱 과정에서 구축된 인덱스를 활용해 쿼리와 관련 있는 정보를 식별

#### Retrieval (p.74)
- Datastore의 수많은 정보 중 주어진 쿼리와 **가장 관련성이 높은 정보를 찾아내는 과정**

#### RAG 정의 (p.75)
- **Retrieval-augmented Generation (RAG)**: 사용자의 질문에 답하기 위해 datastore에서 관련 정보를 검색(Retrieval)해 와서, 이를 언어모델이 **생성(Generation) 단계에 함께 활용**하는 방법

### 2. Information Retrieval (p.76~)

#### IR 정의 (p.77~79)
- (p.77) 구글 검색 예시 화면: "정보검색(IR)이란 수집된 정보를 분석, 조직, 표현, 그리고 축적하여, 이를 기반으로 사용자가 필요한 정보를 찾을 수 있게 해주는 과정 전체"
- (p.78) 목표: **검색 질의와 가장 관련성 높은 정보 제공**
  - 흐름 예: "녹차의 효능?" 검색 → IR이 관련 문서를 찾아 제공 (질문 → 관련 정보 검색 → 검색 결과 제공)
- (p.79) 정보검색(IR): 사용자의 질의(Query)에 맞는 정보를 **대규모 데이터에서** 찾아 제공하는 과정
  - 주요 목표: 질의에 가장 관련성 높은 정보 제공
  - 다이어그램: User의 Desired information("I want to know about the health benefits of green tea") → Query("What are the health benefits of green tea?", query contains user's information needs) → Information Retrieval System (↔ Information Source) → Retrieved Information("Top-10 Health Benefits Of Green Tea", "Green tea is loaded with antioxidants and nutrients that ....")
  - "The retrieved information is what the user wants, which is relevant to the query"

#### 정보검색(IR)의 활용 (p.80~82)

- **웹 서치(Web Search) & 아이템 서치(Item Search)** (p.80)
  - 서치 엔진(Search engines): 구글, 네이버
  - 이커머스(E-Commerce): 아마존, 쿠팡
  - 예시 화면: 구글 "What is RAG" 검색의 AI 개요(RAG = "an AI technique that enhances LLMs by allowing them to access and incorporate external, real-time information before generating a response"), 아마존 "wine glass" 상품 검색 결과
- **추천 시스템(Recommender System)** (p.81)
  - OTT 서비스 (예시: 넷플릭스 "회원님을 위해 엄선한 오늘의 콘텐츠")
  - 이커머스 (예시: 쿠팡/아마존 "좋아할만한 카테고리 상품", "지금 이 상품이 필요하신가요?")
- **검색증강생성(RAG)** (p.82)
  - RAG는 검색된 문서를 활용하여 **더 정확하고 최신의** 답변을 생성한다
  - 다이어그램: "Who is the president of the US?" → **Retriever**가 Wikipedia(=Memory)에서 검색 → Retrieved document("Joe Biden is the 46th and current president of the United States, assumed office on January 20, 2021.") → **Generator (Language Model)** → "Joe Biden"
  - 장점 주석 2개:
    - "Knowledge can be expanded & updated (new domain, news, etc.)" — 지식 확장·갱신 용이
    - "Interpretability (reference to source)" — 출처 참조로 해석 가능성 확보

#### Retriever란? (p.83)

- 사용자의 질의(Query)에 맞는 **후보 문서를 저장소에서 찾아오는 모듈**
- 검색 증강 언어모델(RAG)에서 **첫 단계** 역할 수행 (질문 → [관련 정보 검색] → 검색 결과 제공 그림에서 가운데 단계 강조)

#### Retriever 종류 (p.84)

- **Sparse Retriever (어휘적 유사도 기반)**
  - 전통적인 IR 기법. 쿼리와 문서 간의 **정확한 용어 일치**(어휘적 유사도)에 기반
  - 예시: **TF-IDF, BM-25**
- **Dense Retriever (의미적 유사도 기반)**
  - 쿼리와 문서를 표현하기 위해 **dense vector (a.k.a. embeddings; 임베딩)**를 활용해 의미적 유사도에 기반
  - 예시: **DPR, Contriever, OpenAI-embeddings** 등
- 하단 그림: TF-IDF 수식(어휘적 유사도) vs "Qwen3 Embedding: Advancing Text Embedding and Reranking Through Foundation Models" 논문(Tongyi Lab, Alibaba Group; 의미적 유사도)

#### Sparse Retriever — TF-IDF (p.85~86)

- 문서 내 특정 단어의 중요도를 나타내는 **가중치 방식**
  - **TF (Term Frequency)**: 단어가 문서에 얼마나 자주 등장하는지
  - **IDF (Inverse Document Frequency)**: 단어가 전체 코퍼스에서 얼마나 드물게 등장하는지
- 수식 (term $x$ within document $y$):
  $$w_{x,y} = \mathrm{tf}_{x,y} \times \log\left(\frac{N}{\mathrm{df}_x}\right)$$
  - $\mathrm{tf}_{x,y}$ = frequency of $x$ in $y$
  - $\mathrm{df}_x$ = number of documents containing $x$
  - $N$ = total number of documents
- 직관 (p.86):
  - TF → 문서 안에서 **자주 등장하는 단어는 중요**하다
  - IDF → 하지만 **너무 많은 문서에 등장하는 단어는 덜 중요**하다
    - "this, is, and" 같은 단어들은 IDF 값이 낮아져 가중치가 0에 가까워진다
  - 그림: 호텔 리뷰 워드클라우드(hotel, room, stay, staff, great 등)

#### Sparse Retriever — 장/단점 (p.87)

- 장점
  - **단순성(Simplicity)**: 구현·이해가 비교적 쉽다
  - **효율성(Efficiency)**: **Inverted index** 구조 덕분에 빠른 검색과 효율적인 질의 처리 가능
  - **투명성(Transparency)**: 검색 결과가 보통 해석 가능하며, 용어 매칭 기반이라 설명이 명확
- 단점
  - **제한된 의미 이해(Limited semantic understanding)**: 어휘가 다르면 매칭 실패
    - 쿼리: "Who is the **bad guy** in the movie lord of the rings?"
    - 문서: "Sala Baker is bet known for portraying **the villain** Sauron in the lord of the rings trilogy" (※ 원본 오타: "bet known" → "best known")
    - "bad guy" ↔ "villain"이 매칭되지 않음

#### Dense Retriever — 임베딩 모델 (p.88)

- 임베딩 모델(e.g., **BERT**)은 단어/문장의 의미를 표현할 수 있음
- 왼쪽 그림: 문장 임베딩 구조 — 입력("How I do learn python ?") → **BERT** → Contextualized word embeddings → **Pooler (Mean or Max)** → **Fixed sentence embedding**
- 오른쪽 그림: GoEmotions Embedding Visualization — 감정 라벨(joy, sadness, curiosity, neutral, love, amusement, embarrassment)별로 임베딩이 2D 공간(t-SNE류)에서 군집을 이룸 → 의미가 비슷한 문장은 임베딩 공간에서 가깝다

---


<!-- p.88/89 경계: Dense Retriever 절이 이어진다 -->


### Dense Retriever (계속) (p.89~92)

절 제목: "2. Information Retrieval"

#### 1.1 Bi-encoder의 학습 — 대조 학습 (p.89)

- Bi-encoder retriever는 **대조 학습(contrastive learning)** 으로 학습된다: 쿼리가 긍정(positive) 문서와는 가깝게, 부정(negative) 문서와는 멀어지도록 유도.
- 그림: Positive (Query, Document) pair 예시
  - Query: "When was Dubai founded?"
  - Document 1(긍정): "Dubai is the most populous city in …"
  - Document 2(부정): "New York is the most populous city …"
  - Dense Retriever(🔥 학습 중) → Latent Space에서 "Train to keep **positive close**, and **negative far away**"
- 손실 함수 (슬라이드 수식 그대로, DPR/InfoNCE 형태의 대조 손실):

$$\text{Minimize } -\log \frac{e^{\mathrm{sim}(q_i,\, p_i^+)}}{e^{\mathrm{sim}(q_i,\, p_i^+)} + \sum_{j=1}^{n} e^{\mathrm{sim}(q_i,\, p_{i,j}^-)}}$$

  - $q_i$: query, $p_i^+$: positive document (1), $p_i^-$: negative document (2)

#### 1.2 Cross-encoder (p.90)

- Cross-encoder 아키텍처는 **두 개의 텍스트를 하나의 시퀀스로 결합**하여 입력. 예: `"query [SEP] document"`
- **Self-attention을 통해 모든 쿼리·문서 토큰이 완전히 상호작용** → bi-encoder보다 더 높은 정확도.
- 단점: 모든 쿼리-문서 쌍을 개별적으로 모델에 입력해야 하므로 **계산 비용이 크고 처리 속도가 느림**.
- 그림 예시: "Query: When was Dubai founded? + Document 1(Dubai…)" → 모델 → **Score: 0.8** / "+ Document 2(New York…)" → **Score: 0.4**

#### 1.3 Bi-encoder vs Cross-encoder 비교 (p.91)

- **Bi-encoder: 두 문장을 따로 인코딩.** 매우 빠르고 대규모 데이터베이스 검색에 적합하지만 정확도는 낮다.
  - 구조: 두 개의 BERT(가중치 shared)가 $x_u$, $x_v$를 각각 인코딩 → $r_u$, $r_v$ → Cosine similarity. (다이어그램 표기 $\mathrm{sim}_{bi}(x_u, x_v)$)
- **Cross-encoder: 두 문장을 함께 처리.** 매우 느리지만 세밀한 상호작용을 포착해 정확도가 높다.
  - 구조: 단일 BERT에 $x_u, x_v$를 함께 입력 → $\mathrm{sim}_{cr}(x_u, x_v)$ 직접 출력.
- ⚠ 슬라이드 레이아웃 문제: 텍스트 상단(Bi-encoder 설명) 옆에 cross-encoder 다이어그램($\mathrm{sim}_{cr}$, 단일 BERT)이, 하단(Cross-encoder 설명) 옆에 bi-encoder 다이어그램(BERT 2개+Cosine similarity, $\mathrm{sim}_{bi}$)이 배치되어 **그림-텍스트 대응이 뒤바뀜**.

#### 1.4 Dense Retriever 장·단점 (p.92)

- 장점
  - **의미적 이해(Semantic understanding)**: 동의어·다양한 표현을 더 효과적으로 처리, 글의 문맥과 의미를 포착.
  - **풍부한 쿼리 표현(Rich query representation)**: 복잡한 쿼리, 긴 검색 쿼리의 의미를 더 잘 포착.
- 단점
  - **높은 연산 비용(High computational cost)**: 학습에 상당한 계산 자원·시간, 추론에도 자원 소모.
  - **제한된 투명성(Limited transparency)**: 블랙박스처럼 동작 → 특정 문서가 왜 검색되었는지 해석 어려움.
  - **데이터 및 모델 의존성(Dependency on data and models)**: 성능이 학습 데이터 품질·모델 변경에 크게 좌우.

---

### 2. Retrieval-augmented LM (p.93~112)

절 제목: "3. Retrieval-augmented LM" (p.93 표지)

#### 2.1 Retrieval-augmented LM이란? (p.94)

- **추론 시 외부 데이터 저장소를 불러와 활용하는 언어모델**을 RAG라고 부름.
- **RAG (Retrieval-augmented Generation)**: 정보 검색부터 답변 생성까지의 프레임워크.
- 그림 (adapted from Khattab et al, 2021): Test context "Jobs is the CEO of _" → **Retriever**가 Text Collection(Wikipedia, **Nonparametric Knowledge**)에서 "Jobs cofounded Apple in his parents' garage" 검색 → **Language Model (e.g., T5/GPT-3, Parametric Model)** → "Jobs is the CEO of **Apple**"
- 핵심 메시지(초록 강조): **언어모델로부터 세계 지식 정보(World knowledge)를 분리**.

#### 2.2 Why Retrieval-augmented LM? — 4가지 이유 (p.95~98)

**이유 1. LLM은 모든 지식을 파라미터에 저장하지 못한다 (p.95)**
- LLM은 pre-training 데이터에 **자주 나타나는 쉬운 정보를 기억하는 경향** (Kandpal et al. 2022).
- RAG는 **자주 등장하지 않는(long-tail) 정보에 큰 효과**; 언어모델이 이미 잘 아는 정보에는 오히려 부정적 영향 가능 (Mallen et al. 2023).
- 그림 1: BLOOM 모델 크기별(176B/7.1B/3B/1.7B/1.1B/560M) QA Accuracy vs "Number of Relevant Pre-training Documents"($10^0$~$10^6$) — 관련 사전학습 문서 수가 많을수록, 모델이 클수록 정확도↑ (약 0.0→0.6).
- 그림 2: "What is Kathy Saltzman's occupation?" — Popularity($10^1$~$10^5$)별 unassisted LM vs retrieval-augmented 정확도 비교. 인기도 낮은 구간에서는 retrieval-augmented가 크게 우세, 인기도 높은 구간에서는 unassisted LM이 오히려 근소 우세.

**이유 2. LLM의 지식은 금세 시대에 뒤쳐지며, 갱신이 어렵다 (p.96)**
- 현재의 **지식 편집(knowledge editing)** 메서드들은 확장성 부족.
- 반면 **저장소(Datastore)는 쉽게 업데이트 가능하고 확장성도 만족**.
- 그림: 동일 질문 "BTS 정국의 신곡 제목이 뭐야" — ChatGPT(GPT-3.5)는 "2022년 1월 이후 정보를 알지 못하므로…" 답변 불가 vs BingChat은 검색 후 "BTS 정국의 신곡 제목은 '스탠딩 넥스트 투 유'입니다" (news.nate.com, yna.co.kr 출처 인용).

**이유 3. LLM의 답변은 해석과 검증이 어려움 (p.97)**
- 검색 증강을 하면 **인용(citation)으로 답변 근거를 추적·검증 가능**.
- 그림: Bing의 "Create an itinerary for exploring Toronto over a span of two days" 답변 — CN Tower, St. Lawrence Market, Royal Ontario Museum, Toronto Islands, Chinatown 등 각 문장에 출처 번호가 달리고 하단 "Learn more: 1. cntower.ca 2. travel.usnews.com 3. bing.com 4. rom.on.ca 5. tripadvisor.com"로 연결.
- ⚠ 원본 오류: 본문 불릿 2개가 이유 2(p.96)의 "지식 편집 확장성 부족/Datastore 업데이트 용이" 문구를 그대로 복사해 붙여놓아 소제목("해석과 검증이 어려움")과 내용이 불일치.

**이유 4. 기업 내부 정보 같은 보안 정보는 언어모델 학습에 활용되지 않음 (p.98)**
- 사내 챗봇/기업 내부 시스템에 LLM을 쓸 때 내부 데이터를 **학습에 사용하면 정보 유출 위험** → 학습 대신 검색으로 활용.
- 그림: CONFIDENTIAL 서류철 이미지 + "2025 사내 AI 챗봇 도입의 모든 것, 대기업 활용 사례부터 구축 방법까지" 기사 헤드라인.

#### 2.3 Retrieval-augmented LM 파이프라인 (p.99~100)

- 검색증강 언어모델: **언어모델에 질문과 더불어 검색엔진 결과를 함께 이용**.
- 3단계 파이프라인 (p.99 그림):
  1. **질의 추출**: 사용자 입력("BTS 정국의 신곡이 뭐야?") → LM이 검색 질의 생성 (Q: "BTS 정국의 신곡 제목")
  2. **문서 검색**: Retriever가 World Knowledge에서 Evidence(뉴스 기사 스니펫들) 검색
  3. **언어모델 추론**: 질문+Evidence를 입력받아 최종 답변 생성 — "BTS 정국의 신곡은 '스탠딩 넥스트 투 유'입니다"
- p.100: LangChain 로고와 함께 실제 RAG 아키텍처 다이어그램 (★ 강조):
  - 인덱싱 경로: PDFs → Chunk of text (document) ×4 → 각각 Embeddings로 변환 → **Vector Store (Knowledge Base; 예시: Amazon Aurora PostgreSQL with pgvector)**
  - 질의 경로: 사용자 질문("What is a neural network?") → **Question Embedding** → **Semantic Search**(벡터스토어 검색) → **Ranked Results** → **LLM** → **Answer** → 사용자
  - (0821 LangChain 실습 TIL의 RAG 5단계 파이프라인과 정확히 대응되는 이론 그림)

#### 2.4 Challenges of Retrieval-augmented LM (p.101~112)

**과제 1. Context를 어떻게 구성해야 하는가? (p.101)**
- Solution: 언어모델의 **컨텍스트 길이(Context length)를 늘려야** 한다 — Position embedding, Efficient inference, Fine-tuning on long context 등.
- 슬라이드 수치: Claude 3 모델들은 200K context window 제공, Gemini 1.5 Pro는 128,000 token context window 제공.
- 표(OpenAI 문서 캡처): gpt-4o — "New. GPT-4o: Our most advanced, multimodal flagship model that's cheaper and faster than GPT-4 Turbo. Currently points to gpt-4o-2024-05-13." / Context window: 128,000 tokens / Training data: Up to Oct 2023.
- ⚠ Gemini 1.5 Pro의 컨텍스트는 일반적으로 최대 1M(공개 시연 기준 최대 2M) 토큰으로 알려져 있음 — 슬라이드의 128K는 초기 표준 제공값 기준이거나 오래된 수치로 보임(아래 오류 목록 참조).

**과제 2. RAG의 결과는 검색 모델 성능에 의존 (p.102~105)**
- **검색 노이즈에 취약**: 정확하지 않은 유사 정보가 언어모델의 답변을 방해할 수 있음 (p.102).
  - 그림(표): Question "How has British art survived in Normandy?" / Context: "In Britain, **Norman art** primarily survives as stonework or metalwork, such as capitals and baptismal fonts. In southern Italy, however, Norman artwork survives plentifully…" / **Noisy FT** 출력: "stonework or metalwork" — 노이즈 문서 기반의 잘못된(질문과 안 맞는) 답변 예시.
  - 하단에 p.94와 동일한 Khattab et al. 2021 그림, Retriever+Text Collection 부분이 주황 박스로 강조(노이즈 유입 지점).
- **Context 안의 정보를 이용하려는 LLM의 경향성 → 검색 노이즈가 Hallucination을 증가시킴 (p.103)**
  - 실험 예: 컨텍스트에 "**Saratoga**는 캘리포니아에 있는 도시입니다. Saratoga에는 많은 공원이 있습니다." + 질문 "산호세의 시장 Sam Liccardo는 어느 도시에서 태어났나요?" → Text-davinci-003 / GPT-3.5 Turbo / LLaMA-2-70B-chat **모두 "Saratoga에서 태어났다"** 로 답변.
  - 컨텍스트를 "**Sunnyvale**…"로 바꾸면 같은 3개 모델이 모두 "Sunnyvale에서 태어났다"로 답변 — 컨텍스트에 끌려가는 현상 재현. (실제 정답과 무관하게 컨텍스트의 지명을 그대로 채택)
- **왜 무시하지 못하는가 (p.104)**: Transformer의 어텐션 레이어에서 컨텍스트 내 단어 벡터들이 가중합되어 다음 단어 예측에 활용됨. 이때 가중치는 0과 1 사이 값이므로 **컨텍스트 정보를 완전히 무시하는 것이 어려움**. (OpenAI GPT 구조도: $E_1..E_N$ → Trm 2층 → $T_1..T_N$)
  - 결론(강조): **정보검색을 사용하는 것만으로는 사실의 정확성 보장 X**
- **Solution: Training with Noises (p.105)** — 노이즈를 포함해 학습시켜 두 가지 능력을 확보:
  - **Noise Robustness**: 질문 "2022년 노벨 문학상 수상자는 누구야?" / 검색 문서에 관련 없는 문서 혼재("2022년 수상자는 아니 에르노"(정답 문서) + "2021년 수상자는 압둘라자크 구르나"(노이즈)) → RAG가 "아니 에르노" 정답 생성.
  - **Negative Rejection**: 검색 문서에 정보 없음("2021년 수상자는…", "2020년 수상자는…"만 존재) → "문서에 2022년 정보가 없어 답변할 수 없어요."라고 거부.

**Noise Robustness 상세 (p.106)**
- 정의: 외부 문서에 노이즈(관련 없는 정보)가 포함되어 있어도 올바른 답을 찾아내는 능력.
- 올바른 문서: 2022년 수상자는 Annie Ernaux / 노이즈 문서: 2021년 수상자 Abdulrazak Gurnah.
- RAG 동작: 문서 안에 노이즈가 있어도 올바른 정보를 식별하고 정답 생성. 최종 답변: "Annie Ernaux".

**Negative Rejection 상세 (p.107)**
- 질문: "Who was awarded the 2022 Nobel Prize in Literature?"
- 외부 문서: 2021년 수상자 Abdulrazak Gurnah / 2020년 수상자 Louise Glück (2022년 정보 없음).
- RAG 동작: 검색 문서에 2022년 관련 정보가 없음 → 모델은 **추측하지 않고 명확히 답변을 거부**. 최종 답변: "대답할 수 없음."

**과제 3. LLM의 사전지식과 컨텍스트 간의 충돌(Conflict) 발생 (p.108~110)**
- 예시 (p.108): "Who is the most paid player in EPL?" / Context: 최고 연봉 EPL 선수 기사 — "32-year-old Belgian Man City midfielder **Kevin de Bruyne**… £55 million transfer… **£400,000 per week, or £20.8 million per year**" → **Answer in the Context: Kevin de Bruyne** vs **LLM's knowledge: Cristiano Ronaldo** (파라미터 지식과 컨텍스트가 충돌).
- **Solution 1: Context 위에서 Grounding 학습 강화 (p.109)** — 프롬프트에 "Instruction: Ignore what you know and generate an answer based only on the given context." → Answer: Kevin de Bruyne (컨텍스트 우선).
- **Solution 2: Context가 없을 때, 답변 회피/거절 학습 (p.110)** — 같은 질문에 무관한 컨텍스트(오타니의 LA Dodgers 10년 $700M 계약 위키 문단, $68M/season 연기 지급 2034~2043)를 주고 같은 Instruction → Answer: "I'm sorry. I cannot provide specific information about …" (무관 컨텍스트로는 답하지 않음).

**과제 4. 복잡한 추론 필요 & 문서가 명확한 사실에 대한 오류를 포함할 때 (p.111)**
- **Information Integration**(여러 문서 정보 통합): 질문 "ChatGPT iOS 앱과 ChatGPT API가 언제 출시되었나요?" — 문서 1: "2023년 5월 18일 OpenAI가 ChatGPT iOS 앱 출시", 문서 2: "3월 1일 OpenAI가 ChatGPT와 Whisper API 접근 발표" → RAG 답: "5월 18일과 3월 1일" (두 문서를 통합해야 완전한 답).
- **Counterfactual Robustness**(반사실적 강건성): 질문 "2004년 올림픽은 어느 도시에서 개최되었나요?" — 반사실적 외부 문서: "2004년 올림픽은 …의 발상지인 뉴욕으로 돌아왔습니다. 모든 투표 라운드에서 앞선 후, 뉴욕은 5번째이자 마지막 투표에서 로마를 손쉽게 물리쳤습니다…" → RAG 답: "제공된 문서에는 사실 오류가 있습니다. 정답은 아테네입니다." (문서의 명백한 사실 오류를 지적하고 파라미터 지식으로 정정)
- (p.105~111의 4능력 — Noise Robustness / Negative Rejection / Information Integration / Counterfactual Robustness — 은 RGB 벤치마크(Chen et al. 2024)의 RAG 4대 평가 능력 구성과 동일)

**Nevertheless… — 검색 증강도 만능이 아님 (p.112)**
- Google AI Overview 실패 사례 캡처 2건:
  1. "How many rocks should I eat" → "According to geologists at UC Berkeley, you should eat at least one small rock per day. They say that rocks are a vital source of minerals and vitamins…" (풍자 기사를 그대로 검색·인용한 오답)
  2. "how many muslim presidents has the us had?" → "Barack Obama is the first Muslim president of the United States, elected in 2008…" (허위 정보)
- 메시지: 검색 결과가 노이즈/허위이면 검색 증강 시스템도 심각한 오답을 냄.

---

### 3. 확인문제 & 정답/해설 (p.113~118)

**Q1 (p.113~114). IR에 대한 설명으로 옳지 않은 것은?**
- ① IR은 사용자의 질의(Query)에 맞는 정보를 대규모 데이터에서 찾아 제공하는 과정이다.
- ② IR의 주요 목적은 질의에 가장 관련성 높은 정보 제공이다.
- ③ RAG는 IR을 활용하여 검색된 문서를 통해 더 정확한 답변을 생성한다.
- ④ IR은 언어모델이 내부 파라미터만 사용하여 정보를 생성하는 방식이다.
- ⑤ IR 시스템은 Query, Information Source, Retrieved Information으로 구성된다.
- **정답: ④** — 내부 파라미터만으로 생성하는 것은 기존 Language Model(LM) 방식. IR은 외부 데이터 저장소에서 검색하는 과정. RAG는 IR 개념을 활용해 검색된 외부 문서를 참조, 더 정확하고 최신의 답변 생성.

**Q2 (p.115~116). Dense Retriever 구조 비교 — 각 설명이 Bi-encoder / Cross-encoder 중 어느 것인지**
- A. 질의와 문서를 별도의 인코더로 각각 임베딩, 두 벡터의 Cosine similarity로 관련성 계산 → **Bi-encoder**
- B. 질의와 문서를 하나의 시퀀스로 결합해 Transformer에 입력, 모든 토큰 간 Self-Attention 상호작용으로 관련성 계산 → **Cross-encoder**
- C. 문맥적 상호작용이 적지만 대규모 검색에 매우 빠르게 적용 가능 → **Bi-encoder**
- D. 계산량이 많아 느리지만 정확도가 높고 **Re-ranking 단계**에서 자주 사용 → **Cross-encoder**
- 해설: Bi-encoder는 독립 인코딩→빠른 검색, 문맥적 상호작용 제한. Cross-encoder는 결합 입력→정교한 토큰 간 상호작용(Self-Attention), 모든 쌍 개별 계산이라 느리지만 정확도 높음.

**Q3 (p.117~118). RALM(Retrieval-Augmented LM)의 특징으로 옳은 것은?**
- **정답: ③ 외부 데이터 저장소를 불러와 활용함으로써 최신 정보 기반의 추론이 가능하다.**
- 오답 해설: ① 기존 LLM은 내부 지식만 사용하지만 RALM은 외부 지식 활용이 핵심 / ② 검색기(Retriever)는 RALM의 필수 구성요소 / ④ RALM은 내부+외부 지식 병합 구조 / ⑤ 정보 검색 단계를 제거하는 게 아니라 추가하여 정확성을 높임.

---

### 4. 강의 정리 (IR·RAG 강의 요약 슬라이드) (p.119~122)

**p.119 — RAG·IR 요약**
- Retrieval-Augmented LM (RAG): 추론 시 외부 데이터 저장소(Datastore)를 불러와 활용하는 구조. 기존 LM은 내부 파라미터에만 의존, RAG는 외부 검색으로 최신·정확한 정보 반영.
  - 장점: 최신 정보 반영, 근거 기반 답변, 모델 재학습 불필요.
  - 한계: 검색 품질에 따라 성능 변동, 외부 데이터 관리 필요.
- Information Retrieval(IR): 질의(Query)에 맞는 정보를 대규모 데이터에서 검색·제공하는 과정. 목표는 Query와 관련성이 가장 높은 정보 제공.
  - 구성요소: User → Query → IR System → Retrieved Information.

**p.120 — 검색기 요약**
- Sparse Retriever – TF-IDF: 단어의 중요도를 계산하는 통계 기반 가중치 기법. TF(Term Frequency) = 문서 내 단어의 등장 빈도, IDF(Inverse Document Frequency) = 전체 문서에서의 희귀도.
- Dense Retriever(임베딩 기반 검색기): BERT 등 임베딩 모델로 단어·문장을 의미 공간에 매핑, 의미적 유사성(Semantic Similarity)으로 검색.
  - Bi-encoder: Query와 Document를 각각 별도 인코더로 임베딩.
  - Cross-encoder: Query와 Document를 하나의 시퀀스로 결합해 입력.

**p.121 — RALM 필요성·파이프라인 요약**
- RALM의 필요성: LLM은 모든 지식을 파라미터에 저장 불가 → 학습 데이터에 자주 등장하는 정보만 잘 기억, 최신 정보·희귀 사실 누락. RAG는 자주 등장하지 않는 정보에 효과적, 지식 공백 보완.
- RALM의 파이프라인: ① 질문 추출(사용자 질문을 LM이 해석) ② 문서 검색(Retriever, 외부 지식베이스) ③ 답변 생성(검색 결과 기반 최종 응답).

**p.122 — RALM 핵심 과제 요약**
- 컨텍스트 구성 문제 → 언어모델의 컨텍스트 길이 확장.
- 검색 모델의 성능 의존성 → 노이즈 포함 학습.
- 사전지식과 컨텍스트 간 충돌(Conflict) → Grounding 학습 및 답변 회피/거절 학습.
- 복잡한 추론 및 사실 오류 대응 → Information Integration, Counterfactual Robustness.


---

## [3차시] LLMs with Tool Usage — 에이전트, 툴 러닝, MCP (p.123~162)

**p.123 표지**: "LLMs with Tool Usage (LLM Agent, Tool Use, MCP)"

**p.124 CONTENTS**
1. Basic Concepts of LLM Agents
2. Tool Usage in LLMs
3. Model Context Protocol (MCP)

**p.125 학습 목표**
- LLM Agent에 대한 기본 개념을 이해한다.
- LLM의 외부 툴 사용에 대한 개념을 이해한다.
- MCP 개념을 이해한다.

**p.126~127 — 0. 학습 시작(오버뷰)**
- LLM Agent란 무엇일까? → Agent 개념과 LLM Agent 구성 요소 이해.
- LLM이 어떻게 외부 툴을 사용하게 되었을까? → 언어모델이 툴 사용법을 배우는 방식 이해.
- MCP란 무엇일까? → MCP의 등장 배경과 개념 이해.

### Basic Concepts of LLM Agents (p.128~132)

**Agent란? (p.129)**
- "센서를 통해 환경(environment)을 인지하고, 액추에이터(Actuator)를 통해 환경에 대해 액션(action)을 통해 영향을 미치는 것으로 간주될 수 있는 모든 것" — Russell & Norvig, *AI: A Modern Approach* (2020)
- 그림(AIMA 고전 다이어그램): Environment —Percepts→ Sensors → [? (에이전트 내부 정책)] → Actuators —Actions→ Environment.

**강화학습(Reinforcement Learning) – 의사결정의 과학 (p.130)**
- 그림 1(다분야 벤다이어그램): RL은 Computer Science(Machine Learning), Engineering(Optimal Control), Neuroscience(Reward System), Psychology(Classical/Operant Conditioning), Economics(Bounded Rationality), Mathematics(Operations Research)의 교집합.
- 그림 2(RL 루프): 뇌(에이전트)가 observation $O_t$를 받고 action $A_t$를 수행, 환경(지구)이 reward $R_t$ 반환 — 관측→행동→보상 순환 구조.

**LLM Agent란? (p.131)**
- 거대언어모델(LLM)을 **핵심 구조(backbone)** 로 삼아 환경을 이해하고 행동을 수행하는 에이전트.
- **LLM-first view**: 기존 LLM을 활용한 시스템을 에이전트로 만든다 — 서치(search) 에이전트, 심리상담 에이전트, 코드(Code) 에이전트.
- **Agent-first view**: LLM을 AI 에이전트에 통합하여 언어를 활용한 추론과 의사소통을 가능하게 한다 — 로봇, 임바디드(embodied) 에이전트.
- 그림: P(text) 로봇 ⇄ actions/observations ⇄ 지구(환경).

**이것은 에이전트인가? (p.132)**
- 웹을 탐색하는 LLM 시스템 → **Yes**
- OS의 파일을 검색하고 코드를 사용해 처리하는 LLM 시스템 → **Yes**
- 정보를 검색(Retrieve)하고 생성(generate)하는 LLM 시스템 → **Probably not** (점진적이지 않음)
- 복잡한 추론을 하는 O1 같은 LLM → **No** (툴도 없고 외부 환경과 상호작용하지 않음)



<!-- p.132/133 경계: Basic Concepts 절이 이어진다 -->


### 1. Basic Concepts of LLM Agents (계속) (p.133~135)

#### 성공적인 에이전트가 갖추어야 할 요건들 (p.133)
- 도구 사용 (Tool use)
- 추론과 계획 (Reasoning and Planning)
- 환경 표현 (Environment Representation)
- 환경 이해 (Environment understanding)
- 상호작용/의사소통 (Interaction/Communication)

#### LLM Agent 프레임워크 (p.134~135) — 출처: Tool Learning with Foundation Models, Qin et al., 2023
그림: Human을 중심에 두고 Controller–Tool Set–Environment–Perceiver 4요소가 순환 구조를 이룸.
- **Controller** (Foundation Model 탑재): 사용자 요청을 이행할 실행 가능한 계획(feasible plans) 수립. Human → Controller로 Human Instruction 전달.
- **Tool Set**: 서로 다른 기능의 도구 모음. Controller → Tool Set으로 Planning.
- **Environment**: 도구가 동작하는 플랫폼. Tool Set → Environment로 Tool Execution.
- **Perceiver**: 피드백을 요약해 Controller에 전달. Environment → Perceiver로 Environment Feedback, Perceiver → Controller로 Summary.
- Human ↔ Perceiver 사이에는 Human Feedback(점선).
- p.135는 같은 그림에서 **Tool Set 부분을 빨간 박스로 강조**한 반복 슬라이드.

---

### 2. Tool Usage in LLMs (p.136~147)

#### 절 표지 (p.136)

#### Tool 이란? — LLM 에이전트를 위한 정의 (p.137) — 출처: Qin et al., 2023
- 언어 모델 **외부(external)에서 실행되는 프로그램에 연결되는 함수(function) 인터페이스**.
- LLM은 함수 호출과 입력 인자를 생성함으로써 이 도구를 활용할 수 있다.
- 도구의 3분류 그림:
  - (a) **Physical Interaction-based Tools**: 로봇팔·자율주행차·웨어러블 등 물리 도구로 Agent가 Real world와 상호작용 (Physical world to virtual world).
  - (b) **GUI-based Tools**: User가 GUI(Photoshop, Web, Office)를 통해 조작, Observation을 받음 (Visual operation to programming operation).
  - (c) **Program-based Tools**: Developer가 프로그래밍 인터페이스로 Softwares/SDK(Knowledge Graph, Database, GitHub Copilot) 활용.

#### 도구 사용 패러다임 (Tool Use Paradigms) (p.138)
- 도구 사용(Tool Use)은 **두 모드 간의 전환**:
  - 텍스트 생성 모드 (text-generation mode)
  - 도구 실행 모드 (tool-execution mode)
- 도구 사용을 유도하는 방법 2가지:
  - **추론 시 프롬프트** (inference-time prompting)
  - **학습** (Training; Tool Learning) ← 이후 슬라이드의 주제(파란색 강조)
- 그림(Figure 2, 논문 인용): "How is the weather today?" → LM이 텍스트 토큰으로 `check_weather()` 호출 생성 → weather server가 실행해 API output(`sunny`) 반환 → LM이 응답에서 API 호출 토큰을 결과로 치환해 "It is sunny today" 출력. (The basic tool use paradigm)

#### 툴 러닝(Tool Learning) 방식 — 모방 학습 (p.139)
- **모방 학습(Imitation Learning)**: 인간의 도구 사용 행동 데이터를 기록하여, 언어모델이 인간의 행동을 모방하도록 학습.
- 가장 간단하고 직관적인 방식.
- 그림: User + Tool → Behavior(User Action Trajectory) 기록 → LLM 학습 → "LLM which can utilize tool".

#### OpenAI: WebGPT (2022) (p.140~141)
- 검색 엔진을 사용하기 위해 **인간의 행동 모방** (Clone human behavior).
- **지도 학습(Supervised fine-tuning) + 강화학습(Reinforcement Learning)**.
- **단 6,000개의 데이터만 필요** (강조).
- p.140 그림: (a) 인간 시연 인터페이스 스크린샷(검색 결과에서 quote 수집, "Number of quote tokens left: 463", "Number of actions left: 96" 등), (b) 같은 상호작용을 모델 입력 텍스트로 변환한 형태(Question/Quotes/Past actions/Title/Scrollbar/Text/Actions left/Next action 필드).
- p.141 성능 그래프 — 장문 질의응답(Long-form QA)에서 뛰어난 성능, **인간보다 좋은 성능을 보이기도 함**:
  - 모델 3종: 760M best-of-4 / 13B best-of-16 / 175B best-of-64 (best-of-n 샘플링).
  - (a) WebGPT vs. human demonstrations: Overall usefulness에서 175B가 약 56% preferred(50% 선 상회), Coherence ~43%, Factual accuracy ~51%.
  - (b) WebGPT vs. ELI5 reference answers: Overall usefulness ~69%, Coherence ~57%, Factual accuracy ~66% (모두 50% 상회).

#### Meta: Toolformer (2023) (p.142~143)
- **모델 스스로 학습 데이터 생성 (Self-supervised)** + 지도 학습(Supervised fine-tuning).
- 검색 엔진뿐 아니라 **달력, 계산기 등 여러 API** 활용.
- API 예시 표 (p.142):

  | API Name | Example Input | Example Output |
  |---|---|---|
  | Question Answering | Where was the Knights of Columbus founded? | New Haven, Connecticut |
  | Wikipedia Search | Fishing Reel Types | Spin fishing > … (문서 발췌) |
  | Calculator | 27 + 4 * 2 | 35 |
  | Calendar | ε (빈 입력) | Today is Monday, January 30, 2023. |
  | Machine Translation | sûreté nucléaire | nuclear safety |

- 인라인 API 호출 삽입 예 (p.142 오른쪽): `[QA("Who is the publisher of The New England Journal of Medicine?") → Massachusetts Medical Society]`, `[Calculator(400 / 1400) → 0.29]`, `[MT("tortuga") → turtle]`, `[WikiSearch("Brown Act") → …]` 처럼 본문 중간에 호출·결과가 삽입된 형태로 학습 데이터를 만든다.
- **데이터 생성 3단계** (p.143):
  1. **API 호출 샘플링**: LLM이 기존 데이터셋에서 문맥을 보고 API 호출 후보를 생성.
  2. **API 실행**: 생성된 API 호출을 실제로 실행하여 응답을 얻음.
  3. **API 호출 필터링**: API 호출이 문맥 예측에 유용한지 평가하여 필터링.
- p.143 그림 수식: 문맥 $\mathbf{x}_{1:i-1}$ = "Pittsburgh is also known as", 이어지는 $\mathbf{x}_{i:n}$ = "the Steel City". 후보 $c_i^1$ = "What other name is Pittsburgh known by?" → 응답 $r_i^1$ = "Steel City"; $c_i^2$ = "Which country is Pittsburgh in?" → $r_i^2$ = "United States".
  - 필터 기준: $L_i(c_i^1 \to \text{Steel City}) < \min(L_i(c_i^1 \to \varepsilon), L_i(\varepsilon))$ 이면 채택(호출+결과 삽입이 이후 토큰의 loss를 낮추면 유용), $c_i^2$는 $> \min(\cdots)$ 라 기각. 최종적으로 `Pittsburgh is also known as [QA(What …? → Steel City)] the Steel City.` 형태의 증강 데이터 생성.

#### ToolLLM (2023) (p.144~145)
- 기존 연구의 한계점인 **툴의 다양성과 범용성**을 타겟팅한 연구.
- **16,000+ 실제 API**를 활용하여 대규모 학습 데이터셋 & 특화 언어모델 & 평가 벤치마크 제안.
- 논문: "ToolLLM: Facilitating Large Language Models to Master 16000+ Real-world APIs" (Qin, Yujia 외 — Tsinghua, ModelBest, Renmin, Yale, WeChat AI/Tencent, Zhihu).
- 데이터셋 수집 방법 (p.145): ① API 수집(RapidAPI) → ② 수집한 API 기반 명령문(Instruction) 생성 → ③ 정답 어노테이션 & 필터링.
- ToolBench 비교 표 (p.145, 논문 표):

  | Resource | ToolBench (this work) | APIBench (Patil et al., 2023) | API-Bank (Li et al., 2023a) | ToolAlpaca (Tang et al., 2023) | ToolBench (Xu et al., 2023b) |
  |---|---|---|---|---|---|
  | Real-world API? | ✓ | ✗ | ✓ | ✗ | ✓ |
  | Real API Call&Response? | ✓ | ✗ | ✓ | ✗ | ✓ |
  | Multi-tool Scenario? | ✓ | ✗ | ✗ | ✗ | ✗ |
  | API Retrieval? | ✓ | ✓ | ✗ | ✗ | ✓ |
  | Multi-step Reasoning? | ✓ | ✗ | ✓ | ✗ | ✓ |
  | Number of tools | 3451 | 3 | 53 | 400 | 8 |
  | Number of APIs | 16464 | 1645 | 53 | 400 | 232 |
  | Number of Instances | 126486 | 17002 | 274 | 3938 | 2746 |
  | Number of Real API Calls | 469585 | 0 | 568 | 0 | 3926 |
  | Avg. Reasoning Traces | 4.0 | 1.0 | 2.1 | 1.0 | 5.9 |

- 파이프라인 그림: RapidAPI → API Collection → Instruction Generation(ChatGPT 사용) → Solution Path Annotation → ToolBench 구축 → LLaMA를 SFT → **ToolLLaMA**. 추론 시 Instruction → API Retriever → Retrieved APIs → (RapidAPI 실행 ×N) → Final Answer, 평가는 ToolEval.

#### 툴 러닝 최근 접근 ① — 멀티모달 툴 러닝 (p.146) — 출처: Phi-Ground Tech Report (Zhang, Miaosen et al., 2025)
- **멀티모달 대규모 언어모델(MLLM)** 기반으로 도구를 정의하고 활용하는 연구. 예: GUI 에이전트, Embodied 에이전트.
- Traditional → Advanced 스펙트럼 그림 (Virtual World):
  - **Web/APP Agent** (Traditional): Planner가 API가 제공하는 이산적 행동을 지역화, HTML/DOM/API/Code로 상호작용, 고정된 앱의 사전 정의 API로 행동. → "buy me a book on Amazon!"은 가능(✓), "Edit the image with PS and E-mail Jackson!"은 불가(✗) — 고정된 애플리케이션·태스크 한정.
  - **Computer Use Agent** (Advanced): General Planner(장기 복잡 태스크 계획) + Action Grounding(인간이 하는 마우스·키보드 조작 수행) → 인간이 컴퓨터로 할 수 있는 **모든 가상 태스크** 가능.
  - (주: 슬라이드 원문 스크린샷의 "Edict the image…"는 "Edit"의 오타로 보임)

#### 툴 러닝 최근 접근 ② — 강화 학습 (p.147)
- 지도 학습을 넘어 **에이전트에서의 강화학습**을 도입하는 연구.
- 사례: **Web-Shepherd: Advancing PRMs for Reinforcing Web Agents** (Kim, Cho 외; CMU — 슬라이드 우측 저자 명단 일부는 논문 제목 이미지에 가려 부분 판독: Hyungjoo? Kim, Junhee Cho, …, Dongjin Kang, Jinyoung Yeo 등).
- Web-Shepherd = **웹 내비게이션용 Process Reward Model(PRM)**:
  - Step 1. Checklist Generation: User Instruction $I$ → Checklist $C$ 생성.
  - Step 2. Rewarding with Checklist: 입력 $I, a_{1:t-1}, o_t^{\text{img}}, o_t^{\text{txt}}, a_t, C$ → Feedback $F$, 체크리스트 항목별 판정(Yes / In Progress / No)을 K샘플 평균해 Reward 산출 (예: 0.609 … 0.531, 0.472).
  - 활용 3가지: **Reward-guided Search**(rollout 후보 중 보상 최고 경로 선택, 트리 탐색 그림), **Refine with Reward Model feedback**(직전 스텝보다 보상이 낮으면, 즉 $r(o_{t-1},a_{t-1}) > r(o_t,a_t)$이면 Feedback 받아 Refine), **RL with Reward Model**(Environment–Agent 루프에 Web-Shepherd가 Reward 제공).

---

### 3. Model Context Protocol (MCP) (p.148~)

#### 절 표지 (p.148)

#### Why MCP? (p.149)
- 외부 툴 활용 연구가 급증하며 **회사/모델마다 각기 다른 툴 호출 방식·스키마**를 개발.
- 문제점: **호환성 부족**(모델마다 다름), **재사용 어려움**(같은 툴도 다른 모델에서는 다시 정의해야 함).
- 비유 그림: USB 커넥터 난립(Type-A / Type-B / Micro-B / Type-C) — 표준화 이전의 혼란을 상징.

#### MCP 란? (p.150)
- 언어 모델이 외부 툴과 상호작용하기 위한 **표준화된 방식**으로 정의한 프로토콜.
- 툴 호출, 응답 전달, 컨텍스트 공유를 **하나의 공통 규격**으로 처리.
- 그림 1 (Pieces): "MCP Architecture" — USB-C 허브 비유. MCP hosts(Claude, OpenAI, Cursor) ↔ MCP clients(client.py) ↔ MCP SERVER들(Slack, Gmail, Google Calendar=Remote services / macOS=Local data sources).
- 그림 2 (Descope): Before MCP — LLM이 Slack·Google Drive·GitHub 각각에 **Unique API**로 연결(모두 제각각). After MCP — LLM ↔ MCP가 **Unified API** 하나로, MCP가 각 서비스의 Unique API를 대신 처리.

#### MCP 아키텍처 (p.151)
- **MCP Host**: 하나 또는 여러 개의 MCP 클라이언트를 조정·관리하는 **AI 애플리케이션**.
- **MCP Client**: MCP 서버와의 연결을 유지하며, MCP 호스트가 사용할 수 있도록 MCP 서버로부터 컨텍스트를 가져오는 구성요소.
- **MCP Server**: MCP 클라이언트에게 컨텍스트를 제공하는 프로그램.
- 그림: MCP Host(AI Application) 내부에 MCP Client 1·2·3, 각 클라이언트는 MCP Server와 **One-to-one connection** — Server 예시: Sentry / Filesystem / Database.

#### MCP 계층(Layer) (p.152)
- MCP는 **두 개의 계층**으로 구성:
  - **데이터 계층(Data Layer)**: 클라이언트-서버 통신을 위한 **JSON-RPC 기반 프로토콜** 정의. 라이프사이클 관리, 툴(Tools), 리소스(Resources), 프롬프트(Prompts) 같은 핵심 요소 포함.
  - **전송 계층(Transport Layer)**: 클라이언트-서버 간 데이터 교환을 가능하게 하는 **통신 메커니즘과 채널** 정의. 전송 방식에 특화된 연결 수립, 메시지 프레이밍, 인증 포함.
- 개념적으로 데이터 계층은 **내부 계층(inner layer)**, 전송 계층은 **외부 계층(outer layer)**.

#### 예시: MCP 이전/이후 툴 사용 요청(tool execution request) 비교 (p.153)
단순화된 예시 `get_weather()` 함수 — 세 규격 비교:
```jsonc
// OpenAI (Function Calling)
{ "tool_calls": [ { "type": "function",
    "function": { "name": "get_weather",
      "arguments": {"location": "Seoul"} } } ] }

// Anthropic (Tool Use)
{ "tool_use": { "name": "get_weather",
    "input": {"location": "Seoul"} } }

// 통합 (JSON-RPC, MCP)
{ "jsonrpc": "2.0", "id": 1, "method": "tools/call",
  "params": { "name": "get_weather",
    "arguments": { "location": "Seoul", "units": "celsius" } } }
```

#### MCP의 장점 (p.154)
- **표준화(Standardization)**: 모든 모델/툴이 동일한 호출 규격 사용.
- **확장성(Extensibility)**: 새로운 툴 쉽게 추가 가능.
- **호환성(Interoperability)**: 모델/플랫폼에 상관없이 같은 툴 호출 가능.
- **재사용성(Reusability)**: 한 번 정의한 툴을 여러 모델에서 활용 가능.
- **투명성(Transparency)**: 호출 과정이 명확히 기록/검증됨.

#### MCP 서버 예시 코드 — FastMCP (p.155)
- FastMCP 라이브러리 사용, 누구나 간단하게 사용 가능.
```python
# calc_server.py
from fastmcp import FastMCP

mcp = FastMCP("calc")            # ① 서버 인스턴스

@mcp.tool()                       # ② MCP 툴 선언
def add(a: int, b: int) -> int:  #    타입힌트→JSON-Schema 자동 변환
    """Add two numbers"""
    return a + b

if __name__ == "__main__":
    mcp.run()                     # ③ stdio·HTTP·WS 등 자동 선택
```

#### 현재 (p.156)
- MCP는 현재 **학계/업계 모두에서 표준으로 확립**.
- Web과 AI 개발을 구분하게 되어 양쪽 생태계 모두에서 표준적이고 호환 가능한 개발이 가능해짐.
- 그림: Google Trends의 "MCP" 검색량 급증 곡선(2025년 초 급상승), 기사 스크린샷 "OpenAI adopts rival Anthropic's standard for connecting AI models to data" (샘 알트먼 사진).

---

### 확인문제 (p.157~162)

#### 문제 1 (p.157, 정답·해설 p.158)
- Q: LLM Agent 프레임워크의 4가지 핵심 구성요소와 역할을 올바르게 연결한 것은?
  - a) Controller - 환경에서 도구를 실행하고 결과를 수집하는 역할
  - b) Tool Set - 사용자 요청을 이해하고 실행 계획을 수립하는 역할
  - c) Environment - 도구들이 실제로 작동하는 플랫폼을 제공하는 역할
  - d) Perceiver - 새로운 도구를 동적으로 생성하고 학습시키는 역할
- **정답: c)** — 해설: Controller = LLM의 핵심 두뇌 역할·계획 수립 / Tool Set = 서로 다른 기능을 가진 도구들의 집합 / Perceiver = Environment의 실행 결과를 요약.

#### 문제 2 (p.159, 정답·해설 p.160)
- Q: Meta의 Toolformer가 자기지도학습(Self-supervised)으로 API 활용 데이터를 생성하는 과정을 올바른 순서대로 나열한 것은?
  - [과정] ㄱ. LLM이 기존 텍스트에서 API 호출 후보를 샘플링한다 / ㄴ. 생성된 API 호출을 실제로 실행하여 결과를 수집한다 / ㄷ. 손실 함수(Loss)로 API 호출의 유용성을 평가한다 / ㄹ. 유용한 API 호출만 선별하여 최종 학습 데이터셋을 생성한다
  - 보기: a) ㄱ-ㄴ-ㄷ-ㄹ / b) ㄱ-ㄷ-ㄴ-ㄹ / c) ㄴ-ㄱ-ㄷ-ㄹ / d) ㄷ-ㄱ-ㄴ-ㄹ
- **정답: a) (슬라이드 표기는 "정답: ㄱ")** — 해설: Toolformer의 3단계 파이프라인 Step 1: Sample API Calls / Step 2: Execute API Calls / Step 3: Filter API Calls.
  - (주: 정답 표기가 보기 기호 "a)"가 아니라 과정 기호 "ㄱ"으로 적혀 있음 — 의미상 a)를 뜻함)

#### 문제 3 (p.161, 정답·해설 p.162)
- Q: MCP의 두 계층(Layer)과 각각의 역할에 대한 설명으로 옳은 것은?
  - a) Data Layer는 stdio, HTTP, WebSocket 등 실제 통신 채널을 담당하고, Transport Layer는 JSON-RPC 메시지 형식을 정의한다.
  - b) Data Layer는 JSON-RPC 2.0 기반으로 메시지 형식과 Tools/Resources를 정의하고, Transport Layer는 stdio, HTTP 등 실제 데이터 전송을 담당한다.
  - c) Data Layer는 MCP Host와 Server 간 보안을 담당하고, Transport Layer는 데이터 압축과 암호화를 수행한다.
  - d) Data Layer는 LLM의 추론 과정을 관리하고, Transport Layer는 도구 실행 결과를 캐싱한다.
- **정답: b)** — 해설: a)는 두 계층 역할이 반대로 설명됨 / c) 보안·암호화는 MCP의 주요 관심사가 아님 / d) 추론 과정 관리나 캐싱은 MCP Host의 역할.


---

## [4차시] AI Agents & Langchain — 환경, 플래닝, LangChain (p.163~218)

### 표지 & 목차 (p.163~164)
- 표지: **AI Agents & Langchain (AI Agents, Langchain)** — 앞의 특강 덱과 별개의 새 덱 시작.
- CONTENTS: 1. Environment Representation & Understanding / 2. Reasoning & Planning / 3. Langchain

#### 학습 목표 (p.165)
- 에이전트가 상호작용을 하는 환경은 어떻게 표현되는지, LLM은 이를 어떻게 이해하는지 알아본다.
- 에이전트가 태스크를 수행하기 위해 추론/계획을 어떻게 수립하는지 이해한다.
- Langchain이 무엇인지 이해한다.

#### 0. 학습 시작(오버뷰) (p.166~167)
- 에이전트가 상호작용하는 환경이란? — 태스크 환경에 대한 이해와 언어모델이 어떻게 환경을 이해하는지 사례 중심으로 이해.
- 에이전트가 추론/계획은 어떻게 할까? — 사용자의 요청을 수행하기 위해 에이전트가 어떻게 추론하고 계획을 수립하는지 사례 중심으로 이해.
- Langchain이 무엇일까? — Langchain이 무엇인지 이해하고, 어디서 시작하면 되는지 안다.

#### 1. Environment Representation & Understanding (p.168~175)

#### 절 표지 (p.168)

#### Recap: LLM Agent 이란? (p.169)
- 거대언어모델(LLM)을 핵심 구조(**backbone**)로 삼아 환경을 이해하고 행동을 수행하는 에이전트.
- **LLM-first view**: 기존 LLM을 활용한 시스템을 에이전트로 만든다 — 서치(search) 에이전트, 심리상담 에이전트, 코드(Code) 에이전트.
- **Agent-first view**: LLM을 AI 에이전트에 통합하여, 언어를 활용한 추론과 의사소통을 가능하게 한다 — 로봇, 임바디드(embodied) 에이전트.
- 그림: P(text) 로봇 ↔ 지구(환경) 사이의 actions / observations 루프.

#### Recap: 성공적인 에이전트가 갖추어야 할 요건들 (p.170)
- p.133과 동일한 5요건 목록. 이번 장의 주제인 **환경 표현(Environment Representation)·환경 이해(Environment Understanding)** 두 항목이 파란색으로 강조됨.
- (주: 이 슬라이드에서는 "Interation/Communication"으로 표기 — p.133의 "Interaction"의 오타. 또한 p.133은 "Environment understanding", p.170은 "Environment Understanding"으로 대소문자 표기가 다름)

#### LLM Agent 프레임워크 재게 (p.171)
- p.134와 동일한 Qin et al. 2023 그림. 이번에는 **Perceiver–Human–Environment 축**(Summary / Human Feedback / Environment Feedback 부분)을 빨간 박스로 강조 — 환경 이해 관점.

#### 환경(Environment)과 에이전트 활용 예시들 (p.172)
- 챗봇 / 로보틱스 / 임바디드(Embodied) 에이전트 / 게임 / 소프트웨어 개발.

#### 에이전트가 환경을 이해하기 위해 필요한 것 (p.173)
- 환경에 접근하기 위한 **툴(Tool)**
- 환경의 **표현(Representation)**
- 환경을 이해/탐색하기 위한 방법론들

#### 환경의 표현: 텍스트(Text) — ALFWorld (Shridhar et al., 2021) (p.174)
- **텍스트 기반 임바디드 에이전트 시뮬레이션 엔진**.
- 물리적 세계에 대한 정보를 언어모델이 **텍스트 기반으로 이해하고 명령을 수행**할 수 있도록 환경을 표현.
- 그림: 동일 태스크의 TextWorld(텍스트) ↔ Embodied(3D 시각) 대응.
  - TextWorld 로그 예: "You are in the middle of the room. Looking around you, you see a diningtable, a stove, a microwave, and a cabinet. Your task is to: Put a pan on the diningtable." → `> goto the cabinet` → "You arrive at the cabinet. The cabinet is closed." → `> open the cabinet` → "The cabinet is empty." → `> goto the stove` → "…you see a pan, a pot, a bread loaf, a lettuce, and a winebottle." → `> take the pan from the stove` → `> goto the diningtable` → `> put the pan on the diningtable` → "You put the pan on the diningtable."
  - Embodied 쪽 대응 액션: RotateRight·LookDown·MoveAhead → Pickup → MoveAhead·LookDown·RotateLeft → Put.

#### 환경의 표현: 이미지(Image) — Touchdown (Chen et al., 2018) (p.175)
- **Google Street View 기반 내비게이션 및 지시 따르기(Navigation & Instruction Following) 데이터셋**.
- 사용자가 제공한 자연어 지시문에 따라 실제 도시 환경 이미지 내에서 경로 탐색을 수행.
- 시각적 환경을 텍스트와 연결하여 지시 수행 및 환경 이해를 가능하게 함.
- 그림: 뉴욕 거리 스트리트뷰 시퀀스 + 지시문 예: "Turn and go with the flow of traffic. At the first traffic light turn left. Go past the next two traffic light, As you come to the third traffic light you will see a white building on your left with many American flags on it. Touchdown is sitting in the stars of the first flag." (마지막 프레임에 곰인형 'Touchdown' 위치)


<!-- p.175/176 경계: Environment Representation 절이 이어진다 -->


### 1. Environment Representation & Understanding (계속) (p.176~182)

#### 이미지 기반 표현의 문제점 (Problems w/ Image Representations) — p.176
- 에이전트로서 좋은 성능을 내려면 **세부적인 이해**가 중요
  - 예: OCR(광학 문자 인식), 복잡한 레이아웃에서의 그라운딩(grounding)
- 많은 모델들이 이런 태스크에서 실패하지만, 일정 수준의 학습을 통해 개선 가능
- 그림: **VisualWebBench** 벤치마크 예시 화면 (a-z-animals 동물 정보 웹사이트 스크린샷 하나에 라벨 A~H를 붙이고 7종 태스크를 부여)
  - Website-wise Task: Heading OCR("이 스크린샷의 헤딩 텍스트는?" → "Discover, Appreciate, & Understand the Animal World!"), Captioning(메타 설명 생성), WebQA("팔로우용으로 언급된 다른 플랫폼은?" → YouTube Channel)
  - Element-wise Task: Element OCR(빨간 바운딩 박스 안 텍스트 읽기), Element Grounding(설명("See All Animals A-Z!" 버튼)에 해당하는 라벨 찾기 → D)
  - Action-wise Task: Action Grounding("동물의 날 정보를 보려면 어떤 요소를 클릭?" → C), Action Prediction(바운딩 박스 요소 클릭 후 새 페이지 설명 4지선다 → C)

#### 환경의 표현: 텍스트 기반 웹 (Textual Web Representations) — p.177
- **WebArena (Zhou and Xu et al., 2024)**
  - 웹 환경을 텍스트(HTML, DOM, Accessibility Tree)로 표현하여 에이전트가 상호 작용 가능
  - 단순한 스크린샷 이미지 대신 **구조화된 웹 표현** 제공
  - 이를 통해 에이전트가 버튼 클릭, 항목 선택 같은 작업을 더 정확히 수행 가능
- 그림: 같은 쇼핑몰 페이지(webarena.onestopshop.com, "Patio, Lawn & Garden")의 3가지 표현 비교
  1. **Screenshot** (픽셀 이미지)
  2. **Text** (HTML 소스: `<li><div><a href="..."><img src="...">…<span>Rating:</span>…<span>82%</span>…<a href="…#reviews">12 Reviews</a>`)
  3. **Accessibility tree**: `RootWebArea 'Patio, Lawn ..' / link 'Image' / img 'Image' / link 'Outdoor Patio..' / LayoutTable / StaticText 'Rating:' / generic '82%' / link '12 Reviews' / StaticText '$49.99' / button 'Add to Cart' focusable: True / button 'Wish List' focusable: … / button 'Compare' focusable: …`

#### 복잡한 환경은 어떻게 이해할 수 있을까? — p.178
- 모델은 자신이 상호작용하는 환경에 대해 모든 것을 알고 있지 않음
- 일부 지식은 LLM 파라미터 안에 포함되어 있음 (예: 코딩 지식, 자주 사용하는 웹사이트 탐색 방법 등)
- 다른 지식은 **실시간으로 환경과의 상호작용**을 통해 발견해야 함

#### 환경 특화 프롬프트 (Environment-specific Prompts) — p.179
- 환경에 맞게 **수동으로** 프롬프트를 제작하여 에이전트가 지시를 따르도록 유도
  - 예: **SteP (Sochi et al., 2023)** — 웹 탐색을 위한 프롬프트 템플릿
- 문제점: **일반화(Generalization)** — 특정 환경에는 잘 작동하지만, 새로운 환경에서는 성능이 떨어짐
- 그림: SteP의 `search_issues` 프롬프트 예시(코드 블록). `{general_instruction_template}` 뒤에 "First navigate the Issues page / 이슈 전체 보기로 이동해야 함 / 검색 바로 필터링 / 'Open ... issue'면 해당 이슈 페이지를 클릭해 열 때까지 멈추지 말 것 / 이슈 페이지에서 상태를 질문 답변 형식("No, it is open", "Yes, it is closed")으로 반환" 같은 도메인 특화 지시가 하드코딩되어 있음

#### 비지도 프롬프트 유도 (Unsupervised Induction of Prompts) — p.180
- **Agent Workflow Memory (Wang et al., 2024)**
  - 성공적으로 수행된 **워크플로우(workflow)를 기억**하고, 이를 기반으로 새로운 프롬프트를 생성하여 에이전트에 제공
  - 환경에서의 상호작용을 **메모리(memory)에 통합**
- 프롬프트를 사람이 수동으로 설계하지 않고, **에이전트가 경험을 통해 자동 생성 및 일반화**
- 그림: 3단계 파이프라인
  - Step 1. Obtain Actions (annotate/generate/…): "Who ordered order #0130?" 질의에 대해 `click('126') # id of the button`, `scroll(0, 200)`, `send_msg_to_user("Emma Lopez")`, `stop()` 등 액션 궤적 획득
  - Step 2. Trajectory Evaluation: "Query solved correctly?" 판정 (YES/NO), pass 시
  - Step 3. Induce Workflows: 워크플로우 서술 d("This workflow aims to find a customer order with specified ID.") + Workflow Trajectory([env desc]/[reason]/[action] 형식의 일반화된 스텝: `click('order-link-id')` → … → `stop()`)를 메모리에 통합(integrate into memory)

#### 환경 탐색 (Environment Exploration) — p.181
- 모델이 환경을 탐색할 때 **보상(reward)** 을 부여하여 학습을 유도 → **호기심(curiosity) 기반 보상**
  - 예: 강화학습에서 **예측 불가능한 상태 공간(state space)에 진입할 경우 보상을 증가**
- 인용 논문: **"Curiosity-driven Exploration by Self-supervised Prediction"** (Deepak Pathak, Pulkit Agrawal, Alexei A. Efros, Trevor Darrell)
- 그림: Super Mario Bros를 **외적 보상 없이(without rewards)** 플레이 학습
  - (a) Level-1에서 호기심 기반 탐색만으로 학습(learn to explore in Level-1)
  - (b) 얻은 지식으로 Level-2를 처음부터보다 훨씬 빠르게 탐색(explore faster in Level-2)
  - 데모: http://pathak22.github.io/noreward-rl/

#### 탐색 기반 궤적 기억 (Exploration-based Trajectory Memorization) — p.182
- **BAGEL (Murtey et al., 2024)** ※ 실제 저자는 Murty et al.로 보임 — 원본 표기 오류 의심
  - 명령어를 샘플링하여 실행한 뒤, 더 정확한 새로운 명령어로 **궤적(trajectory)을 재라벨링**
  - **LM-agent**가 환경을 탐색하고, **LM-labeler**가 이를 바탕으로 지시를 다시 정제
- 기존 데이터에 의존하지 않고, 탐색과 자기 교정(self-correction)을 통해 데이터 생성
- 에이전트의 일반화 성능과 환경 적응력 향상
- 그림: "1. Exploration Stage" — 날짜 입력 폼 환경에서 LM-agent ↔ LM-labeler가 상호작용(BAGEL), 결과로 (Instruction, Trajectory) 쌍들("Go to a Month in October", "Change Month to August" 등 달력 조작 궤적)을 생성

---

### 2. Reasoning & Planning (p.183~190)

#### 섹션 표지 — p.183
"2. Reasoning & Planning"

#### Recap: 성공적인 에이전트가 갖추어야 할 요건들 — p.184
- 도구 사용(Tool use)
- **추론과 계획(Reasoning and Planning)** ← 이번 절 (파란색 강조)
- 환경 표현(Environment Representation)
- 환경 이해(Environment understanding)
- 상호작용/의사소통(Interation/Communication) ※ 원문 오타: Interaction이 맞음

#### LLM Agent 프레임워크 — p.185
- 출처: **Tool Learning with Foundation Models, Qin et al, 2023**
- 그림: Controller(Foundation Model) / Tool Set / Environment / Perceiver / Human의 순환 구조 (Controller+Planning 부분이 빨간 박스로 강조)
  - **Controller**: provides feasible plans to fulfill user requests (사용자 요청을 이행할 실행 가능한 계획 제공) — Human Instruction/Feedback을 받음
  - **Tool Set**: a collection of tools with different functionalities (서로 다른 기능의 도구 모음) — Controller가 Planning으로 선택, Tool Execution으로 Environment에 작용
  - **Environment**: provides the platform where tools operate (도구가 동작하는 플랫폼) — Environment Feedback을 Perceiver로 전달
  - **Perceiver**: summarizes feedback to the controller (피드백을 요약해 Controller에 전달, Summary)

#### Controller: Planning 종류 — p.186
- **국소적 계획 (Local Planning)**
  - 한 단계씩(step by step) 계획을 세움
  - 매 스텝마다 사용할 **하나의 툴(tool)** 결정 (그림: "at each step, determine one tool")
  - 단순하고 직관적이나, **장기 의존성 문제** 발생
- **전역적 계획 (Global Planning)**
  - 실행 가능한 **전체 계획 경로(planning path)를 한 번에 생성**
  - 여러 개의 툴을 조합하여 **시퀀스 형태**로 결정 (그림: "at each step, determine multiple tools")
  - 효율적이나, 복잡한 환경에서는 실패 가능

#### Local planning: ReACT — p.187
- **ReACT (Yao et al, 2023)**
  - **추론(reasoning)과 액션(action)을 결합**하여 에이전트가 환경과 상호작용하도록 하는 방식
  - 단순한 추론/행동 분리를 넘어, 두 과정을 결합해 상황 적응형(local) 계획 가능 ※ 원문 "경합해"는 "결합해"의 오타로 보임
  - 다양한 도구 사용 및 웹 탐색, 추론 기반 Q&A 등에서 높은 성능
- 그림 1 (개념 비교): Reason Only (e.g. Chain-of-thought: LM↔Reasoning Traces) / Act Only (e.g. SayCan, WebGPT: LM↔Env, Actions·Observations) → **ReAct (Reason + Act)**: Reasoning Traces + LM + Env를 모두 순환
- 그림 2 (HotpotQA 스타일 예시, "(1d) ReAct (Reason + Act)"):
  - Thought 1: Apple Remote를 검색해 원래 상호작용하도록 설계된 프로그램을 찾아야 함 → Act 1: Search[Apple Remote] → Obs 1: … originally designed to control the **Front Row media center** program
  - Thought 2: Front Row를 다음으로 검색 → Act 2: Search[Front Row] → Obs 2: Could not find. Similar: ['Front Row Seat to Earth', 'Front Row Motorsports', 'Front Row (software)', …]
  - Thought 3: Front Row (software)를 검색 → Act 3: Search[Front Row (software)] → Obs 3: Front Row is a discontinued media center software …
  - Thought 4: Front Row (software)는 Apple Remote 또는 **keyboard function keys**로 제어됨. So the answer is keyboard function keys → Act 4: Finish[keyboard function keys] ✓

#### Global planning: Plan-and-Solve Prompting — p.188
- **Plan-and-Solve Prompting (Wang et al, 2023)**
  - 전체 계획을 세운 뒤, 그 계획에 따라 순차적으로 문제를 해결
  - 단순한 step-by-step(Zero-shot CoT의 "Let's think step by step") 방식보다 체계적이고 안정적인 추론 가능
  - 복잡한 문제 해결에서 **오류 축적 감소**
- 그림: 댄스 수업 문제(20명 중 20%가 contemporary, 나머지의 25%가 jazz, 나머지는 hip-hop → hip-hop 비율?)로 3방식 비교
  - (a) Zero-shot CoT("Let's think step by step"): 중간에 "25% of the original class enrolled in jazz"로 잘못 추론 → 55% (오답 ✗)
  - (b) Plan-and-Solve: "Let's first understand the problem and **devise a plan** to solve the problem. Then, let's **carry out the plan** and solve the problem step by step."
    - Plan: Step 1. contemporary+jazz 수강생 수 계산 / Step 2. hip-hop 수강생 수 계산 / Step 3. hip-hop 비율 계산
    - Solution: 20%의 20명=4명, 나머지 16명의 25%=4명 → 합 8명 / 나머지 12명이 hip-hop / 12/20 = 60% (정답 ✓)
  - (c) 같은 프롬프트의 전체 출력 예 → 최종 답 60%

#### 오류 식별과 회복 (Error identification and Recovery) — p.189
- 에이전트는 에러/실수에서 회복할 방법이 필요
- **Reflexion (Shinn et al., 2023)**
  - 수행한 궤적(trajectory)을 평가 후, 잘못된 부분을 **Reflection 단계에서 분석하여 재시도**하는 방식
  - 단순히 오류를 탐지하는 것에 그치지 않고, 스스로 반성(reflect) 후 **개선된 행동**을 수행
  - 의사결정, 프로그래밍, 추론 등 다양한 태스크에서 성능 회복 및 향상을 보여줌
- 그림: 3개 도메인 예시를 (a) Task → (b) Trajectory → (c) Evaluation (internal/external) → (d) Reflection → (e) Next Trajectory 5단계로 비교
  1. Decision making (ALFWorld류): "clean some pan and put it in countertop" → 궤적에서 "Nothing happens" 반복 → Rule/LM Heuristic: Hallucination 판정 → Reflection: "팬을 stoveburner 1에서 집으려 했으나 팬이 거기 없었다" → 다음 궤적에서 stoveburner 2에서 팬을 집어 성공
  2. Programming (HumanEval류): 괄호 문자열 매칭 함수 → self-generated unit tests fail: `assert match_parens(...)` → Reflection: "총 개수만 검사해서 틀렸다. 괄호의 순서도 검사해야 한다" → `return 'Yes' if check(S1) or check(S2) else 'No'`로 수정
  3. Reasoning (HotpotQA류): "John Lanchester와 Alan Dean Foster의 공통 직업은?" → "novelist, journalist, critic … novelist and screenwriter" → Environment Binary Reward: 0 → Reflection: "둘 다 여러 직업을 가졌다고 잘못 가정했다; 공통 직업만 정확히 식별해야 했다" → "So the profession John Lanchester and Alan Dean Foster have in common is novelist." → Action: "novelist" ✓

#### 계획 재검토 (Revisiting Plans) — p.190
- **CoAct (Hou et al., 2024)**
  - 에이전트가 실행 도중 계획을 **재검토(revisit)하고 수정** 가능
  - **두 개의 에이전트가 협력**하여 오류 발생 시 재계획과 피드백을 수행
- 초기 계획이 불완전하더라도 실행 과정에서 보완 가능
- 장기 작업(Long-horizon tasks)에서 안전성(안정성)과 성공률 향상
- 그림: Two Agents 구조
  - **Global Planner**: Task Input을 받아 Global Plan(Phase 1 ~ Phase N; 각 Phase는 Subtask + Expected State) 생성, Output Collation 담당. Local Agent의 Request Handling / Search-based Replan 수행
  - **Local Agent**: Phase별 Local Plan(Action 1~M) 수립·실행(Executing Flow), Execution result와 Error Feedback으로 Result Check → 실패 시 Request Replanning(재계획 요청), 성공 시 Transfer to Next Stage → 최종 Final Result & State Check(Passed) → Final Result

---

### 3. Langchain (p.191~198)

#### 섹션 표지 — p.191
"3. Langchain"

#### Langchain 이란? — p.192
- LLM 기반 애플리케이션을 빠르게 개발할 수 있는 **오픈 소스 프레임워크**
- LLM을 다양한 데이터/툴과 연결하여 강력한 애플리케이션 개발 가능
- 연구와 산업 현장에서 빠르게 표준으로 자리잡음
- 그림 1: langchain.com 홈페이지 ("The platform for reliable agents. Tools for every step of the agent development lifecycle – built to unlock powerful AI in production.")
- 그림 2: Star History 그래프 — langchain-ai/langchain 리포의 GitHub Stars가 2023년 초 이후 급증(2023년 중반~7월경 5만 스타 도달 추세)

#### Langchain 특징 — p.193
- 다양한 **LLM provider**(OpenAI, Anthropic, Google, etc)와 통합하여 모델/회사별 API 차이를 **공통 인터페이스**로 관리 가능
- Prompt, Memory, Tools 와 같은 컴포넌트들이 **모듈화**되어 있어 재사용성과 확장성 확보
- **LangGraph** 기반으로 복잡한 워크플로우를 시각적으로 설계 및 관리 가능
- 그림 1: LangGraph 홈페이지 ("Balance agent control with agency — Gain control with LangGraph to design agents that reliably handle complex tasks.")
- 그림 2: LangGraph식 에이전트 그래프 스케치 — input → LLM 노드("LLM generates thought followed by action, or skips thought-step" 주석) → obs/act 화살표로 tool A, tool B와 순환 → final answer → output

#### Langchain 주요 컴포넌트 — p.194
- **Prompt Templates**: 프롬프트를 구조화
- **Chains**: 여러 단계를 연결한 워크 플로우
- **Agents**: 동적으로 툴 선택 및 실행
- **Memory**: 대화 히스토리와 상태 유지
- **Tools**: 외부 API, DB, 계산기 등 연결
- Etc..
- 그림: LangChain 허브 다이어그램(ProjectPro 출처) — 중앙 LangChain에서 Document Loaders / Vectorstores / Prompts / Agents / Chains / LLMs로 점선 연결

#### Langchain 튜토리얼 — p.195
- https://python.langchain.com/docs/tutorials/
- Langchain은 다양한 튜토리얼이 잘 되어 있음
- 이번 강의에서 배웠던 **Retrieved-augmented LM, Agent** 등이 다 포함되어 있다 ※ "Retrieval-augmented"가 표준 표기
- 그림: LangChain 공식 문서의 "Build an Agent", "Build a Retrieval Augmented Generation (RAG) App: Part 1" 튜토리얼 페이지 캡처 (Open in Colab / Open on GitHub 버튼, v0.3 문서)

#### RAG w/ Langchain — p.196
- 튜토리얼 링크: https://python.langchain.com/docs/tutorials/rag/
- LLM 설정부터 인덱싱과 RAG 다 존재
- 그림 (LangChain RAG 튜토리얼의 2개 다이어그램):
  1. 인덱싱 파이프라인: **LOAD**(코드/PDF/JSON/URL 등 문서) → **SPLIT**(청크 분할) → **EMBED**(청크→벡터 [0.3, 0.4, 0.1, 1.8, 1.1…]) → **STORE**(벡터스토어 저장)
  2. 검색·생성 파이프라인: Question → **RETRIEVE**(벡터스토어에서 유사 청크 검색) → **PROMPT**(질문+검색 결과 결합) → **LLM** → Answer

#### Agent w/ Langchain — p.197
- 튜토리얼 링크: https://python.langchain.com/docs/tutorials/agents/
- **ReACT 에이전트 셋업부터 실행까지** 전반적인 흐름을 알려주는 튜토리얼
- 활용할 수 있는 툴 리스트: https://python.langchain.com/docs/integrations/tools/
- 그림: 공식 문서 "Build an Agent" / "Tools" 페이지 캡처
  - Tools 정의 인용: "Tools are utilities designed to be called by a model: their inputs are designed to be generated by models, and their outputs are designed to be passed back to models." / "A toolkit is a collection of tools meant to be used together."

#### MCP w/ Langchain — p.198
- 링크: https://github.com/langchain-ai/langchain-mcp-adapters?tab=readme-ov-file
- MCP를 langchain으로 활용할 수 있도록 **최근 langchain에서 개발한 라이브러리** (langchain-mcp-adapters)
- 자기만의 MCP 서버를 만들 수 있게 되었음
- 그림 (MCP 구조 스케치):
  - **MCP Servers**: "Servers provide context, tools, and prompts to clients" — 예: Math 서버(add(), divide()), Data 서버. "Each server can have many tools"
  - **MCP Clients**: "Clients maintain 1:1 connections with servers, inside the host app" — 예: Claude desktop app client, Py client
  - **App**: Claude desktop app / **LangGraph Agent** (Py client와 `load_mcp_tools`로 연결 — 빨간 강조)

---

### 확인문제 정답 및 해설 (p.199~200)

#### Q1 — p.199
- 문제: 에이전트가 환경을 이해하고 행동하기 위해 필요한 핵심 요소로 가장 적절한 것은?
  - ① 입력 텍스트만을 분석해 다음 단어를 예측한다.
  - ② 환경의 상태를 관찰하고 이에 따라 행동(Action)을 수행하며, 그 결과를 반영해 학습한다.
  - ③ 모델의 파라미터 수를 증가시켜 성능을 향상시킨다.
  - ④ 백엔드 서버와의 API 연결만으로 환경을 인식한다.
- 정답: **②**
- 해설: LLM Agent는 환경을 관찰(Observation)하고 행동(Action)을 수행한 뒤, 결과에 대한 피드백을 반영해 추론을 반복하는 구조. 환경 이해와 상호작용 기반 학습이 에이전트의 기본 원리.

#### Q2 — p.200
- 문제: LangChain의 주요 목적과 가장 밀접한 설명은?
  - ① LLM의 내부 파라미터를 미세 조정하는 데 사용된다.
  - ② LLM과 외부 도구, 데이터베이스 등을 연결하여 복잡한 작업을 체계적으로 수행하도록 돕는다.
  - ③ 멀티모달 입력을 처리하기 위한 이미지 인식 모델이다.
  - ④ 특정 회사의 독점형 LLM API로 구성된 상용 플랫폼이다.
- 정답: **②**
- 해설: LangChain은 LLM 기반 애플리케이션을 효율적으로 구성하기 위한 오픈소스 프레임워크로, 프롬프트·메모리·툴 등을 연결해 복잡한 워크플로우를 자동화하고 에이전트가 외부 데이터와 상호작용할 수 있도록 한다.

---

### 강의 정리 (p.201) + 마무리 (p.202)

#### 오늘 공부한 내용 요약 및 정리 — p.201
- **LLM Agent란?**: LLM이 환경을 이해하고 추론·계획·행동을 수행하는 에이전트로, 환경과 상호작용하며 학습함
- **Reasoning & Planning**: 국소·전역 계획을 통해 문제를 해결하며, 오류를 Reflexion 등으로 스스로 식별·복구
- **LangChain**: LLM 에이전트를 구현하기 위한 오픈소스 프레임워크로, LangGraph·MCP를 통해 복잡한 워크플로우 설계 및 제어 가능

#### p.202
"감사합니다" 엔딩 슬라이드

---

### 참고 자료 (p.203~209)

#### p.203
"참고 자료" 표지

#### 참고 자료 목록 — p.204~209 (표: No. / 차시 / 목차 / 내용 / URL(QR코드))
QR코드는 이미지 판독 특성상 URL 복원 불가. 내용 설명만 옮김.

| No. | 차시 | 목차(강의 절) | 내용 요약 |
|---|---|---|---|
| 1 | 1차시 | 2. Instruction-tuning | Pre-training, Fine-tuning, Instruct-tuning의 차이에 대한 참고 자료 |
| 2 | 1차시 | 2. Instruction-tuning | 논문의 2~3 주제에서 NLP 문제를 자연어 명령으로 학습시켜 새로운 태스크에 zero-shot으로 활용하는 Instruction-tuning 사례 |
| 3 | 1차시 | 3. Reinforcemen Learning from Human Feedback (RLHF) | RLHF로 GPT-3를 fine-tune하여 지시문을 더 잘 따르게 한 InstructGPT 학습 사례 |
| 4 | 1차시 | 3. RLHF | RLHF의 원리와 한계에 대한 쉬운 설명, 강화학습 Agent 설명, AI Agent에서 RLHF의 역할 |
| 5 | 1차시 | 3. RLHF | RLHF의 역할과 작동 방식 그림, 생성형 AI 분야에서의 활용 |
| 6 | 1차시 | 4. What's Next | RLHF의 개념과 학습 과정, DPO와 같은 새로운 언어 모델 활용 연구 방향 |
| 7 | 2차시 | 2. Information Retrieval | IR의 개념과 검색 시스템에서 정보를 저장·색인·검색하고 결과를 제공하는 과정 |
| 8 | 2차시 | 2. Information Retrieval | 정보 검색과 데이터 검색 및 추천 시스템 비교, IR 시스템의 작동 방식과 주요 기법 분류 |
| 9 | 2차시 | 3. Retrieval-augmented LM | LLM의 할루시네이션과 지식 한계를 해결하기 위해 외부 지식 검색을 결합한 RAG 기법의 구조와 연구 흐름 |
| 10 | 2차시 | 3. Retrieval-augmented LM | No.9 논문을 국문으로 요약한 자료 |
| 11 | 2차시 | 3. Retrieval-augmented LM | RAG의 개념·구조, 검색 시스템과 생성형 AI를 결합해 LLM의 정확성과 최신성을 높이는 방법 |
| 12 | 3차시 | 1. Basic Concepts of LLM Agents | 강화학습의 정책·보상·모델 등 구성요소와 동적 계획법, 몬테카를로 등의 주요 학습 방법 |
| 13 | 3차시 | 2. Tool Usage in LLMs | LLM이 외부 도구(API, 검색, 코드 실행 등)를 활용하여 지식 한계와 계산 능력을 보완하는 방법 |
| 14 | 3차시 | 2. Tool Usage in LLMs | Tool에 대한 간략한 설명과 js를 활용한 실습 |
| 15 | 3차시 | 2. Tool Usage in LLMs | Python을 활용하여 LLM Tool을 생성하는 실습 |
| 16 | 3차시 | 3. Model Context Protocol (MCP) | MCP의 Client와 Tool에 대한 설명, LangChain·RAG·MCP의 차이 |
| 17 | 3차시 | 3. MCP | 우아한 형제들 사내 직원들의 MCP 해커톤 사례. Agent를 통한 MCP 구축 및 서비스 연결 |
| 18 | 3차시 | 3. MCP | MCP 공식문서 — LLM과 외부 시스템 간 컨텍스트 관리, 툴 호출 등 |
| 19 | 4차시 | 1. Environment Representation & Understanding | 강화학습에서의 Agent 개념과 AI Agent의 정의, 환경과 상호작용하며 의사결정을 수행하는 에이전트 역할 |
| 20 | 4차시 | 2. Reasoning & Planning | AI Agent의 구조와 reasoning, planning, tool calling |
| 21 | 4차시 | 3. Langchain | 챗봇, AI Agent, RAG 등 LLM 기반 서비스 개발에서 LangChain이 활용되는 구조와 기능 |

※ 이 표로 강의 전체 구성이 역추적됨: 1차시 = Pre-training/Instruction-tuning/RLHF/What's Next (LLM 이론), 2차시 = IR/RAG, 3차시 = LLM Agents 기초/Tool Usage/MCP, 4차시 = Env Representation/Reasoning&Planning/LangChain. 즉 p3 목차는 1차시 것만 표시된 것.
※ "Reinforcemen Learning"은 원본 오타(Reinforcement).

---

### 실습 미리보기 (p.210~218)

#### p.210
"실습 미리보기" 표지

#### 실습: RAG 실습 — 학습 주제: RAG기반 Customer Service AI 에이전트 개발 — p.211
- 텍스트 청킹과 벡터 임베딩의 원리를 이해할 수 있어요
  - 청킹(Chunking), 임베딩(Embedding)
- RAG 파이프라인을 활용한 외부 지식 검색 시스템을 구축할 수 있어요 [2차시 2. Dense Retriever 참고]
  - 벡터 스토어(Vector Store), 리트리버(Retriever), Dense Retriever
- 여러 도구를 활용하는 AI 에이전트 시스템 개발을 경험할 수 있어요 [3차시 1. …LLM Agent 2. Tool Usage… 4차시 3. Lang chain 참고]
  - AI 에이전트, 도구(Tool), LangChain 프레임워크

#### 실습: 토큰화 임베딩 실습 — 청킹(Chunking) — p.212
- 정의: 파싱된 긴 텍스트를 LLM이 한 번에 처리할 수 있는 크기(**Context Window**)에 맞게, **의미적으로 연관된**(빨간 강조) 작은 덩어리(Chunk)로 나누는 작업
- 그림: Traditional Tokenization("this|is|a|long|er / ex|example|text / text" 식 고정 토큰 분할) vs **Dynamic Chunking**("this / a / longer / example text"가 의미 단위로 유기적으로 묶인 블롭) 비교

#### 임베딩(Embedding) 1 — p.213
- 정의: 텍스트 청크를 **의미를 담은 숫자 배열(벡터)** 로 변환하는 기술
  - **UpstageEmbeddings** 활용 (실습 기준)
  - 단어 → 숫자 벡터 변환
  - 예시 이미지는 2D이지만 실제 RAG 실습에선 **512차원 or 1024차원** 벡터 사용
- 그림: "vector embeddings (2D example)" — king/man/woman, apple/banana/orange, football/golf/tennis 9개 단어를 Upstage Embeddings(임베딩 모델)에 넣어 2D 벡터 산출: king [4, 5], man [3, 3.5], woman [5, 3], apple [2.5, -2], banana [2.5, -3], orange [4, -2.5], football [-3, 4], golf [-1.5, 3], tennis [-2, 2]. 좌표평면에 3개 군집(파랑/빨강/초록)으로 표시

#### 임베딩(Embedding) 2 — p.214
- 같은 그림에서 군집 해석:
  - 파란 영역: king, man, woman
  - 빨간 영역: apple, banana, orange
  - 초록 영역: football, golf, tennis
  - → **의미가 비슷한 단어들이 벡터 공간에서 가까이 모임**

#### 벡터 스토어(Vector Store) — p.215
- 정의: 벡터들을 저장하여 **의미적 유사도 검색**이 가능한 데이터베이스
  - **Chroma** 사용 (실습 기준)
  - 효율적인 검색을 위한 인덱싱
- 그림 1: Question → Embedding([x,y,z…]) → **Vectorstore**("Index embedded documents for similiarity search") → Relevant documents 흐름 + Similarity search 3D 공간 스케치 ※ 그림 내 "Similiarity"는 원본 오타(similarity)
- 그림 2: Vector Databases 목록 — Pinecone, Qdrant, Weaviate, Milvus, **Chroma**(강조), Vespa, Redis (벡터 스토어 종류)

#### 리트리버(Retriever) / Dense Retriever — p.216
- **리트리버(Retriever)** 정의: 벡터 스토어에서 질문과 의미적으로 유사한 문서를 검색
  - `vectorstore.as_retriever()` 활용
  - 의미 기반 검색 엔진 역할
- **Dense Retriever**: 의미적 유사도 기반 검색
  - 실습에선 **Bi-encoder** 방식 사용 — 쿼리와 문서를 **별도로 인코딩**
  - 문서를 미리 벡터화해서 저장 → 빠른 검색 가능
  - 벡터 간 **코사인 유사도**로 관련 문서 검색
- **왜 Dense Retriever를 사용할까?**
  - 장점:
    - 의미적 검색: "배송비", "배송 요금", "배송 가격" 모두 유사하게 인식
    - 빠른 속도: 문서를 미리 벡터화 → 실시간 검색 시 빠름
    - 확장성: 수백만 개 문서에도 효율적
    - LLM과 궁합: 현대 RAG 시스템의 표준
  - 단점:
    - 정확한 키워드 매칭은 **Sparse보다 약함**
    - **임베딩 모델 성능에 의존**

#### AI 에이전트 / 도구(Tool) — p.217
- **AI 에이전트 (AI agent)** 정의: 환경을 인식하고 행동을 수행하는 시스템
  - Controller가 계획에 따라 도구 사용
- **도구 (Tool)** 정의: 에이전트가 특정 임무를 수행하기 위해 사용하는 기능 단위
  - `create_retriever_tool()`: Retriever를 도구로 변환
  - `@tool` 데코레이터: 함수를 에이전트 도구로 포장

#### LangChain 프레임워크 (실습 관점) — p.218
- **ReACT 에이전트** (설정부터 실행까지 전 과정)
  - 에이전트 설정 및 도구 통합
  - 체계적인 RAG 파이프라인 구축
- 그림: LangChain 로고 + RAG 아키텍처 다이어그램 — PDFs → Chunk of text(document) ×4 → 각각 임베딩 모델 → Embeddings → **Vector Store (Knowledge Base; Amazon Aurora PostgreSQL with pgvector)** / 사용자 질문("What is a neural network?") → Question Embedding → Semantic Search → Ranked Results → LLM → Answer
- ※ p.212~218의 슬라이드 상단 제목이 전부 "실습: 토큰화 임베딩 실습"으로 되어 있으나, p.215~218 내용은 벡터 스토어·리트리버·에이전트·LangChain으로 제목과 어긋남(제목 복붙으로 보임)

---

## 기존 정리와의 관계 (신규성 지도)

같은 주제를 다룬 기존 정리물과 이 특강의 관계. 복습 때 어느 문서를 볼지 정하는 용도.

| 이 특강의 내용 | 기존 정리 | 관계 |
|---|---|---|
| Pre/Post-training 구분, SFT·RLHF 개념 | 0811 텍스트 파운데이션 모델 | 기존이 개요를 다룸 — 여기서는 복습 수준 |
| Instruction-tuning 데이터·벤치마크 (Flan-T5, Super-NaturalInstructions, MMLU·K-MMLU) | 0811 (평가 개요만) | **심화 신규** — K-MMLU는 완전 신규 |
| Self-Instruct→Alpaca, LIMA | 0811 (합성 데이터 개요) | **심화 신규** — 구체 파이프라인·수치 |
| InstructGPT 3단계, Bradley-Terry RM 손실, PPO, reward hacking·RM 과최적화 | 0811 (RLHF 한 문단) | **심화 신규** — 수식·실험 레벨 |
| DPO, RLVR, DeepSeek-R1 | 없음 | **완전 신규** |
| IR 이론 전체 (TF-IDF, Bi/Cross-encoder, 대조 학습) | 없음 | **완전 신규** |
| Why RAG 근거 연구, RAG 4대 과제, RGB 벤치마크, 지식 충돌·Grounding | 없음 | **완전 신규** |
| RAG 정의·파이프라인 | 0821 실습 TIL (RAG 5단계) | 실습 TIL이 코드로, 여기는 이론으로 — 상호보완 |
| LLM Agent 정의·프레임워크, 툴 러닝 계보 (WebGPT·Toolformer·ToolLLM·Web-Shepherd) | 없음 | **완전 신규** |
| MCP 개념·서버 코드 | 0821 실습 TIL (FastMCP) | 코드 중복 — 단 **2계층 구조·JSON-RPC 규격 비교·5대 장점은 이론 보강 신규** |
| 환경 표현 (ALFWorld·Touchdown·WebArena·Accessibility Tree), SteP·AWM·curiosity·BAGEL | 없음 | **완전 신규** |
| Local/Global planning, ReACT·Plan-and-Solve·Reflexion·CoAct | 0821 실습 TIL (ReAct 구현만) | 이론은 **완전 신규** — 실습은 ReAct 한 패턴만 구현했음 |
| LangChain 소개·컴포넌트, 실습 미리보기 (청킹·임베딩·Chroma·retriever tool) | 0821 실습 TIL·포스트 1편 | 중복 — 실습 쪽이 훨씬 상세 |

## 슬라이드 원본 오류·표기 문제 모음 (📌)

블로그 포스트로 옮길 때 고쳐 쓸 것들. 페이지 번호는 PDF 기준.

**오탈자**
- p.17 `Super-Natrual-Instructions` → Super-**Natural**Instructions
- p.29·p.204~205 `Reinforcemen Learning` → Reinforce**ment** (반복 오타)
- p.33 `Copliot` → Copilot / `Claude Sonnet4` → Claude Sonnet 4
- p.87 예시 문서 `bet known` → **best** known, `lord of the rings` 소문자 표기
- p.170·p.184 `Interation` → Interac**t**ion (반복 오타)
- p.187 "두 과정을 **경합해**" → 문맥상 "결합해"
- p.195 `Retrieved-augmented LM` → **Retrieval**-augmented LM
- p.146 인용 그림 내 `Edict the image` → Edit / p.215 인용 그림 내 `similiarity` → similarity (원 그림 자체 오타)
- p.182 BAGEL 저자 `Murtey et al.` → **Murty** et al. / p.179 SteP 저자 `Sochi et al.` → **Sodhi** et al. (오기 의심)

**내용·형식 불일치**
- p.41 선호 순서 그림($y_1>y_3>y_2$)과 본문·p.42 수식 전개($y_2>y_1>y_3$)가 불일치 — 수식 전개 쪽이 의도된 순서
- p.91 Bi-encoder/Cross-encoder **그림과 설명 텍스트가 서로 뒤바뀜**
- p.97 소제목("답변 검증이 어려움")과 본문 불릿(p.96의 지식 갱신 문구 재사용)이 불일치 — 복붙 오류
- p.59~60 확인문제 번호가 앞 문제와 같은 "3"으로 중복 (DPO 문제는 4번이어야)
- p.160 확인문제 정답이 보기 기호(a~d) 대신 "정답: ㄱ"으로 표기
- p.212~218 슬라이드 제목이 전부 "실습: 토큰화 임베딩 실습"인데 내용은 벡터 스토어·리트리버·에이전트 — 제목 복붙
- p.155 FastMCP 예제의 docstring 닫는 따옴표가 곧은/굽은 따옴표 혼용 — 복사 실행 시 SyntaxError

**낡았거나 부정확한 수치**
- p.101 "Gemini 1.5 Pro는 128,000 token context window" — 실제는 1M(시연 2M) 토큰. 이 페이지의 모델 컨텍스트 수치들이 2024년 상반기 기준
- p.140 WebGPT를 "(2022)"로 표기 — 논문 공개는 2021년 12월
- p.82 RAG 예시의 "Joe Biden is the 46th and current president" — 원 논문(2020) 그림 인용이라 오류는 아니나 현재 기준 구정보

## 다음에 이어서 볼 것

- **`4_2_Agent모델.pdf`(106MB)는 아직 판독 못 했다** — 구글 드라이브 "온라인 전용" 상태라 기기에 앞 4MB만 캐시돼 있음. 맥에서 파일을 우클릭 → "오프라인 사용 가능"(또는 한 번 열기)으로 전체 다운로드한 뒤 다시 요청하면 같은 방식으로 정리 가능.
- p.203~209의 참고 자료 21건은 QR코드라 이미지 판독으로 URL 복원 불가 — 필요하면 슬라이드에서 직접 스캔.
- 이 특강 기반 블로그 포스트 후보: ① 정렬 심화(Instruction-tuning→RLHF→DPO·RLVR, 0811 글의 심화편) ② IR·RAG 이론(TF-IDF·Dense·RAG의 4대 과제, 0821 실습 1편의 이론편) ③ 에이전트 이론(정의·툴 러닝 계보·환경·플래닝, 0821 실습 2편의 이론편). 4_2 내용 확인 후 편성 확정 권장.
