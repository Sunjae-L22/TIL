# 파이썬 클래스와 객체지향 — 객체·상속·예외 처리·모듈

> 출처: `1_알고리즘기본1.pdf` (총 137페이지) · 정리일: 2026-09-01
> **파일명은 「알고리즘 기본 1」이지만 실제 내용은 파이썬 클래스와 객체지향(Classes)이다.** 알고리즘·복잡도·리스트는 다음 날 자료에 있다 → [0901 알고리즘 기본과 리스트](0901_알고리즘기본과리스트.md)
> 페이지 표기는 PDF 물리 페이지 기준 (슬라이드 인쇄 번호와 일치).
> 판독 방식: **전 페이지 이미지 직접 판독** (OCR 미사용). 판독 실패한 페이지는 **없음**.
> 원본 오류 4건은 본문에서 고쳐 쓰고 `> 📌` 로 밝혔다.
> 관련: [0804 Python 핵심문법](0804_Python_핵심문법.md) · [0826 파이썬 스코프와 global](0826_파이썬스코프와global.md)

## 한눈에 보기

- **객체는 왜 "속성 + 행동"인가** — `'hello'.upper()` 를 이미 쓰고 있었다는 사실에서 시작한다. 클래스는 설계도, 인스턴스는 그 설계도로 찍어낸 실체다.
- **`self` 는 마법이 아니다** — `'hello'.upper()` 는 사실 `str.upper('hello')` 다. 인스턴스가 첫 번째 인자 자리로 들어가는 것뿐이고, 이걸 알면 메서드 3종(인스턴스/클래스/정적)의 차이가 한 번에 정리된다.
- **다중 상속에서 어느 부모가 이기는가** — 다이아몬드 문제와 MRO. `super()` 는 "부모 전부"가 아니라 **MRO상 바로 다음 하나**를 가리킨다.
- **에러와 예외는 다르다** — 문법 에러는 실행 전에 걸리고, 예외는 실행 중에 터진다. `except Exception` 으로 `KeyboardInterrupt` 가 안 잡히는 이유는 상속 계층에 있다.
- **모듈 → 패키지 → 라이브러리** — `import *` 를 쓰면 안 되는 이유, `as` 별칭, 사용자 정의 모듈까지.

## 목차

1. [객체와 클래스의 기본 개념](#객체와-클래스의-기본-개념) (p.6-17)
2. [클래스 정의와 구성요소](#클래스-정의와-구성요소) (p.21-31)
3. [메서드의 세 가지 종류](#메서드의-세-가지-종류) (p.35-49)
4. [상속 (Inheritance)](#상속-inheritance) (p.53-65)
5. [클래스 참고](#클래스-참고) (p.69-78)
6. [에러와 예외](#에러와-예외) (p.83-98)
7. [예외 처리](#예외-처리) (p.101-115)
8. [모듈](#모듈) (p.118-125)
9. [파이썬 표준 라이브러리와 패키지](#파이썬-표준-라이브러리와-패키지) (p.128-137)


---

## 객체와 클래스의 기본 개념

### 클래스와 객체의 정의 (p.6-7)

- **클래스(Class)**: 파이썬에서 **타입을 표현하는 방법**
  - 객체를 생성하기 위한 **설계도**
  - **데이터와 기능을 함께 묶는** 방법을 제공
- **객체(Object)**: 클래스에서 정의한 것을 토대로 **메모리에 할당된 것**
  - **'속성'과 '행동'으로 구성된 모든 것**

### 객체 = 속성 + 행동 (p.8-9)

가수를 객체로 본 예시. 왼쪽은 속성, 오른쪽은 행동이다.

| 속성(정보) → 변수 | 행동(동작) → 메서드 |
| --- | --- |
| 직업: 가수 | 랩() |
| 생년월일: 1993년 5월 16일 | 댄스() |
| 국적: 대한민국 | 소몰이() |

- 속성(정보)은 코드에서 **변수**로 표현된다.
- 행동(동작)은 코드에서 **메서드**로 표현된다.

### 클래스와 인스턴스 (p.10-11)

- 클래스로 만든 객체를 **인스턴스(instance)** 라고 부른다.
- `가수(클래스)` → `아이유`, `기타 치는 가수`, `다른 가수` ... (여러 객체)

문장으로 정리하면:

- 아이유는 객체다 (O)
- 아이유는 인스턴스다 (△) - 무엇의 인스턴스인지가 빠져 어색하다
- 아이유는 가수의 인스턴스다 (O)

> **주의** - "인스턴스"는 항상 **"어떤 클래스의"** 인스턴스인지와 짝지어 말해야 한다. 그냥 "인스턴스다"라고만 하면 반쪽짜리 표현이다.

정리하면 `클래스(가수)와 객체(아이유)` → `타입(list)`의 관계와 같다.
즉 **클래스를 만든다 == 타입을 만든다**.

### 이미 쓰고 있던 클래스와 객체 (p.12-16)

파이썬 내장 타입도 전부 클래스이고, 우리가 쓰던 값은 전부 그 클래스의 인스턴스다.

```python
name = 'Alice'

print(type(name))  # <class 'str'>
```

- 변수 `name`의 타입은 `str` 클래스다.
- 변수 `name`은 **`str` 클래스의 인스턴스**다.
- `''`, `'hello'`, `'파이썬'` → 모두 **문자열 타입(클래스)의 객체(인스턴스)**

메서드 호출 문법도 같은 구조로 읽힌다.

```python
"hello".upper()
```

- `문자열.대문자로()` → `객체.행동()` → `인스턴스.메서드()`

```python
[1, 2, 3].sort()
```

- `리스트.정렬해()` → `객체.행동()` → `인스턴스.메서드()`

**하나의 객체(object)는 특정 타입의 인스턴스(instance)이다.**

- `123`, `900`, `5`는 모두 `int`의 인스턴스
- `'hello'`, `'bye'`는 모두 `string`의 인스턴스
- `[232, 89, 1]`, `[]`은 모두 `list`의 인스턴스

### 객체 정리: 타입 / 속성 / 조작법 (p.17)

객체를 볼 때 던져야 할 세 가지 질문.

- **타입(type)**: 어떤 연산자(operator)와 조작(method)이 가능한가?
- **속성(attribute)**: 어떤 상태(데이터)를 가지는가?
- **조작법(method)**: 어떤 행위(함수)를 할 수 있는가?

> **객체(Object) = 속성(Attribute) + 기능(Method)**

## 클래스 정의와 구성요소

### 클래스 정의 문법 (p.21-22)

- 클래스는 **파이썬에서 타입을 표현하는 방법**이자, 객체를 생성하기 위한 설계도이며, 데이터와 기능을 함께 묶는 수단이다.
- `class` 키워드로 정의한다.
- 클래스 이름은 **파스칼 케이스(Pascal Case)** 방식으로 작성한다.

```python
class MyClass:
    pass
```

### 인스턴스 생성 및 활용 (p.23)

클래스를 정의한 뒤 `클래스이름(인자)` 형태로 호출하면 인스턴스가 만들어진다.

```python
# 클래스 정의
class Person:
    blood_color = 'red'

    def __init__(self, name):
        self.name = name

    def singing(self):
        return f'{self.name}가 노래합니다.'
```

```python
# 인스턴스 생성
singer1 = Person('iu')

# 메서드 호출
print(singer1.singing())

# 속성(변수) 접근
print(singer1.blood_color)
```

- 인스턴스 생성 → `변수 = 클래스명(인자)`
- 메서드 호출 → `인스턴스.메서드()`
- 속성 접근 → `인스턴스.변수`

### 생성자 함수 __init__ (p.25)

- 객체를 생성할 때 **자동으로 호출되는 특별한 메서드**
- `__init__` 메서드로 정의되며, **객체 초기화**를 담당
- 생성자 함수를 통해 인스턴스를 생성하고 필요한 초기값을 설정한다.

```python
class Person:
    blood_color = 'red'

    def __init__(self, name):   # 생성자 함수
        self.name = name

    def singing(self):
        return f'{self.name}가 노래합니다.'


# 인스턴스 생성
singer1 = Person('iu')
```

`Person('iu')`를 호출하는 순간 `__init__`이 자동 실행되어 `self.name`이 `'iu'`로 초기화된다.

### 인스턴스 변수 (p.26)

- **인스턴스마다 별도로 유지되는 변수**
- 인스턴스마다 독립적인 값을 가지며, **인스턴스가 생성될 때마다 초기화**된다.
- 위 예시에서 `self.name = name` 부분이 인스턴스 변수다.

### 클래스 변수 (p.27)

- **클래스 내부에 선언된 변수**
- 클래스로 생성된 **모든 인스턴스들이 공유하는 변수**
- 위 예시에서 `blood_color = 'red'` 부분이 클래스 변수다.

### 인스턴스 메서드 (p.28)

- **각 인스턴스에서 호출할 수 있는 메서드**
- 인스턴스 변수에 접근하고 수정하는 등의 작업을 수행한다.
- 위 예시에서 `def singing(self):` 부분이 인스턴스 메서드다.

### 클래스 변수 활용: 인스턴스 개수 세기 (p.30)

"가수가 몇 명인지 확인하고 싶다면?" → 인스턴스가 생성될 때마다 클래스 변수가 늘어나도록 설정할 수 있다.

```python
class Person:
    count = 0

    def __init__(self, name):
        self.name = name
        Person.count += 1


person1 = Person('iu')
person2 = Person('BTS')
print(Person.count)  # 2
```

### 클래스 변수와 인스턴스 변수의 충돌 (p.31)

`class.class_variable` 로 클래스 변수를 참조한다.

```python
class Circle:
    pi = 3.14

    def __init__(self, r):
        self.r = r

c1 = Circle(5)
c2 = Circle(10)
```

```python
print(Circle.pi)  # 3.14
print(c1.pi)  # 3.14
print(c2.pi)  # 3.14
```

```python
Circle.pi = 5  # 클래스 변수 변경
print(Circle.pi)  # 5
print(c1.pi)  # 5
print(c2.pi)  # 5
```

```python
c2.pi = 5  # 인스턴스 변수 변경
print(Circle.pi)  # 3.14 (클래스 변수)
print(c1.pi)  # 3.14 (클래스 변수)
print(c2.pi)  # 5 (새로운 인스턴스 변수가 생성됨)
```

> **주의** - `인스턴스.클래스변수 = 값` 은 클래스 변수를 바꾸는 것이 아니라 **그 인스턴스에만 붙는 새로운 인스턴스 변수를 만든다.** 클래스 변수를 실제로 바꾸려면 반드시 `클래스명.변수 = 값` 으로 접근해야 한다. 반대로 클래스 변수를 바꾸면 아직 자기 값을 갖지 않은 모든 인스턴스가 함께 영향을 받는다.

## 메서드의 세 가지 종류

### 메서드 종류 개요 (p.35)

파이썬의 메서드는 세 가지로 나뉜다.

1. 인스턴스 메서드
2. 클래스 메서드
3. 정적 메서드

세 종류의 차이를 정리하면 다음과 같다.

```text
Python Methods
├─ Instance Methods
│   - Most Common
│   - Must have self parameter
│   - No decorator needed
│   - Can be accessed through object (instance of Class)
├─ Class Methods (@)
│   - Doesn't need self parameter
│   - Need cls as parameter
│   - Need decorator @classmethod
│   - Can be accessed directly through the class. Don't need instance of class.
└─ Static Methods (@)
    - Doesn't need self parameter
    - Doesn't need self or cls as parameter
    - Need decorator @staticmethod
    - Can only access variables passed as argument.
    - Static method cannot be accessed through class or it's instance.
```

> 📌 원본 p.35의 Static Methods 마지막 줄 "Static method cannot be accessed through class or it's instance"는 오해의 소지가 있다. 정적 메서드는 `클래스명.메서드()` 와 `인스턴스.메서드()` 둘 다로 **호출 가능**하다. 실제로 접근할 수 없는 것은 **클래스 변수와 인스턴스 변수**이며, 인자로 전달받은 값만 다룰 수 있다는 뜻이다.

### 인스턴스 메서드 (p.37)

- 클래스로부터 생성된 **각 인스턴스에서 호출할 수 있는 메서드**
  - 인스턴스의 상태를 조작하거나 동작을 수행
- 반드시 **첫 번째 매개변수로 인스턴스 자신(`self`)을 전달받음**

```python
class MyClass:

    def instance_method(self, arg1, ...):
        pass
```

> **주의** - `self`는 매개변수 이름일 뿐이며 다른 이름으로 설정 가능하다. 하지만 **다른 이름을 사용하지 않을 것을 강력히 권장**한다.

### self 동작 원리 (p.38-39)

`upper` 메서드를 사용해 문자열 `'hello'`를 대문자로 변경하기.

```python
'hello'.upper()
```

하지만 실제 파이썬 내부 동작은 다음과 같이 진행된다.

```python
str.upper('hello')
```

- `str` 클래스가 `upper` 메서드를 호출했고, 그 첫 번째 인자로 문자열 인스턴스가 들어간 것이다.
- 이것이 **인스턴스 메서드의 첫 번째 매개변수가 반드시 인스턴스 자기 자신인 이유**다.

정리하면 `'hello'.upper()` 는 `str.upper('hello')` 를 **객체 지향 방식의 메서드로 호출하는 표현(단축형 호출)** 이다.

- `'hello'`라는 문자열 객체가 단순히 어딘가의 함수로 들어가는 인자로 활용되는 것이 아니라, **객체 스스로 메서드를 호출하여 코드를 동작하는 객체 지향적인 표현**인 것이다.

호출 흐름으로 보면: `인스턴스.메서드(인자)` → `클래스.메서드(인스턴스, 인자)`

### 생성자 메서드 (p.40)

- 인스턴스 객체가 생성될 때 자동으로 호출되는 메서드
- **인스턴스 변수들의 초기값을 설정**한다.

```python
class Person:
    def __init__(self, name):
        self.name = name
        print('인스턴스가 생성되었습니다.')

    def greeting(self):
        print(f'안녕하세요. {self.name}입니다.')


person1 = Person('지민')  # 인스턴스가 생성되었습니다.
person1.greeting()  # 안녕하세요. 지민입니다.
```

### 클래스 메서드 (p.42-43)

- **클래스가 호출하는 메서드**
  - 클래스 변수를 조작하거나 클래스 레벨의 동작을 수행
- **`@classmethod` 데코레이터**를 사용하여 정의
- 호출 시, **첫 번째 인자로 해당 메서드를 호출하는 클래스(`cls`)가 전달됨**

```python
class MyClass:

    @classmethod
    def class_method(cls, arg1, ...):
        pass
```

> **주의** - `cls`도 매개변수 이름일 뿐이며 다른 이름으로 설정 가능하지만, 다른 이름을 사용하지 않을 것을 강력히 권장한다.

인자 `cls`에는 `Person` 클래스가 전달된다.

```python
class Person:
    count = 0

    def __init__(self, name):
        self.name = name
        Person.count += 1

    @classmethod
    def number_of_population(cls):
        print(f'인구수는 {cls.count}입니다.')
```

```python
person1 = Person('iu')
person2 = Person('BTS')

# 인구수는 2입니다.
Person.number_of_population()
```

### 정적 메서드 (p.45-46)

- **클래스와 인스턴스와 상관없이 독립적으로 동작하는 메서드**
  - 주로 클래스와 관련이 있지만 **인스턴스와 상호작용이 필요하지 않은 경우**에 사용
- **`@staticmethod` 데코레이터**를 사용하여 정의
- 호출 시 **필수적으로 작성해야 할 매개변수가 없음** (`self`도 `cls`도 받지 않는다)

```python
class MyClass:

    @staticmethod
    def static_method(arg1, ...):
        pass
```

단순히 문자열을 조작하는 기능을 제공하는 정적 메서드 예시.

```python
class StringUtils:
    @staticmethod
    def reverse_string(string):
        return string[::-1]

    @staticmethod
    def capitalize_string(string):
        return string.capitalize()
```

```python
text = 'hello, world'

reversed_text = StringUtils.reverse_string(text)
print(reversed_text)  # dlrow ,olleh

capitalized_text = StringUtils.capitalize_string(text)
print(capitalized_text)  # Hello, world
```

### 인스턴스와 클래스 간의 이름 공간 (p.48-49)

- 클래스를 정의하면, 클래스와 해당하는 **이름 공간(namespace)** 이 생성된다.
- 인스턴스를 만들면, 인스턴스 객체가 생성되고 **독립적인 이름 공간**이 생성된다.
- 인스턴스에서 특정 속성에 접근하면, **인스턴스 → 클래스 순으로 탐색**한다.

다음 코드를 실행했을 때의 메모리 구조.

```python
class Person:
    blood_color = 'red'

    def __init__(self, name):
        self.name = name


p1 = Person('iu')
p2 = Person('BTS')
```

```text
Global frame
  Person ─────→ [Person class]  ← 클래스 이름 공간
                  __init__    : function __init__(self, name)
                  blood_color : "red"
  p1 ─────────→ [Person instance]  ← 인스턴스별 이름 공간
                  name : "iu"
  p2 ─────────→ [Person instance]  ← 인스턴스별 이름 공간
                  name : "BTS"
```

- `p1.name` → 인스턴스 이름 공간에서 바로 찾음
- `p1.blood_color` → 인스턴스 이름 공간에 없으므로 클래스 이름 공간까지 올라가서 찾음

> **주의** - 탐색은 **인스턴스 → 클래스 한 방향으로만** 일어난다. 클래스에서 인스턴스의 이름 공간을 들여다볼 수는 없다. 앞서 본 `c2.pi = 5` 가 클래스 변수를 바꾸지 않고 인스턴스 이름 공간에 새 이름을 만들어 버리는 이유가 바로 이 구조 때문이다.

## 상속 (Inheritance)

### 상속 없이 구현하면 생기는 문제 (p.53-54)

하나의 클래스로 모든 대상을 표현하려 하면, 대상마다 다른 정보를 담을 자리가 없다.

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def talk(self):
        print(f'반갑습니다. {self.name}입니다.')


s1 = Person('김학생', 23)
s1.talk()  # 반갑습니다. 김학생입니다.

p1 = Person('박교수', 59)
p1.talk()  # 반갑습니다. 박교수입니다.
```

`Person` 하나로는 학생의 학점(gpa), 교수의 학과(department) 같은 고유 정보를 표현하기 어렵다. 그렇다고 클래스를 따로 쪼개면 이번엔 **공통 메서드가 중복 정의**된다.

```python
class Professor:
    def __init__(self, name, age, department):
        self.name = name
        self.age = age
        self.department = department

    def talk(self):  # 중복
        print(f'반갑습니다. {self.name}입니다.')


class Student:
    def __init__(self, name, age, gpa):
        self.name = name
        self.age = age
        self.gpa = gpa

    def talk(self):  # 중복
        print(f'반갑습니다. {self.name}입니다.')
```

> **주의** — 클래스 분리 자체가 목적이 아니다. 분리하되 공통 부분을 한 곳에만 두는 것이 상속의 동기다.

### 상속을 사용한 계층 구조 (p.55)

공통 속성/메서드는 부모(`Person`)에 두고, 자식 클래스가 이를 물려받는다.

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def talk(self):  # 메서드 재사용
        print(f'반갑습니다. {self.name}입니다.')


class Professor(Person):
    def __init__(self, name, age, department):
        self.name = name
        self.age = age
        self.department = department


class Student(Person):
    def __init__(self, name, age, gpa):
        self.name = name
        self.age = age
        self.gpa = gpa


p1 = Professor('박교수', 49, '컴퓨터공학과')
s1 = Student('김학생', 20, 3.5)

# 부모 Person 클래스의 talk 메서드를 활용
p1.talk()  # 반갑습니다. 박교수입니다.

# 부모 Person 클래스의 talk 메서드를 활용
s1.talk()  # 반갑습니다. 김학생입니다.
```

클래스 다이어그램으로 보면 아래와 같은 계층이다.

```text
Person  (name, age / talk())
   ↑                ↑
Student          Professor
 (gpa)          (department)
```

`talk()`는 `Person`에만 존재하지만, 두 자식 인스턴스 모두 호출할 수 있다. 중복 정의가 사라졌다.

### 다중 상속 (p.56-58)

- 둘 이상의 상위 클래스로부터 여러 행동이나 특징을 상속받을 수 있는 것
- 상속받은 모든 클래스의 요소를 활용 가능함
- 중복된 속성이나 메서드가 있는 경우 **상속 순서에 의해 결정**됨

```python
class Person:
    def __init__(self, name):
        self.name = name

    def greeting(self):
        return f'안녕, {self.name}'


class Mom(Person):
    gene = 'XX'

    def swim(self):
        return '엄마가 수영'


class Dad(Person):
    gene = 'XY'

    def walk(self):
        return '아빠가 걷기'


class FirstChild(Dad, Mom):
    def swim(self):
        return '첫째가 수영'

    def cry(self):
        return '첫째가 응애'


baby1 = FirstChild('아가')
print(baby1.cry())   # 첫째가 응애
print(baby1.swim())  # 첫째가 수영
print(baby1.walk())  # 아빠가 걷기
print(baby1.gene)    # XY
```

> **주의** — `baby1.gene`이 `'XY'`인 것이 핵심이다. `FirstChild(Dad, Mom)`에서 `Dad`가 먼저 적혀 있으므로 `Dad.gene`이 이긴다. 괄호 안 순서를 `(Mom, Dad)`로 바꾸면 결과가 `'XX'`로 바뀐다. `swim()`은 자식이 재정의했으므로 `Mom.swim`이 아니라 `FirstChild.swim`이 호출된다.

### 다이아몬드 문제와 MRO (p.59-60)

**다이아몬드 문제(The diamond problem)**: 두 클래스 B와 C가 A에서 상속되고, 클래스 D가 B와 C 모두에서 상속될 때 발생하는 모호함.

```text
      A
     ↗ ↖
    B    C
     ↖ ↗
      D
```

- B와 C가 재정의한 메서드가 A에 있고 D가 이를 재정의하지 않은 경우라면
- D는 B의 메서드 중 어떤 버전을 상속하는가? 아니면 C의 메서드 버전을 상속하는가?

파이썬은 이를 **MRO(Method Resolution Order)** 로 해결한다.

- MRO 알고리즘을 사용하여 클래스 목록을 생성
- 부모 클래스로부터 상속된 속성들의 검색을 **C3 선형화 규칙**에 맞춰 진행
- 계층 구조에서 겹치는 같은 클래스를 두 번 검색하지 않음
- 속성이 D에서 발견되지 않으면 B에서 찾고, 거기에서도 발견되지 않으면 C에서 찾는 순으로 진행됨

```python
class D(B, C):
    pass
```

즉 탐색 순서는 `D → B → C → A → object`.

### MRO가 필요한 이유 (p.64)

- 부모 클래스들이 여러 번 액세스 되지 않도록,
- 각 클래스에서 지정된 왼쪽에서 오른쪽으로 가는 순서를 보존하고,
- 각 부모를 오직 한 번만 호출하고,
- 부모들의 우선순위에 영향을 주지 않으면서 서브 클래스를 만드는 단조적인 구조 형성

그 결과,

- 프로그래밍 언어의 신뢰성 있고 확장성 있는 클래스를 설계할 수 있도록 도움
- 클래스 간의 메서드 호출 순서가 예측 가능하게 유지되며, 코드의 재사용성과 유지보수성이 향상

### super() (p.61-63)

부모 클래스(또는 상위 클래스)의 메서드를 호출하기 위해 사용하는 내장 함수.

- 다중 상속 시 MRO(메서드 결정 순서)를 기반으로 현재 클래스가 상속하는 모든 부모 클래스 중 다음에 호출될 메서드를 결정하여 자동으로 호출

**단일 상속에서의 사용 (사용 전 / 사용 후)**

```python
# 사용 전 - 부모의 초기화 코드를 자식이 그대로 베껴 씀
class Person:
    def __init__(self, name, age, number, email):
        self.name = name
        self.age = age
        self.number = number
        self.email = email


class Student(Person):
    def __init__(self, name, age, number, email, student_id):
        self.name = name
        self.age = age
        self.number = number
        self.email = email
        self.student_id = student_id
```

```python
# 사용 후 - 공통 초기화는 부모에게 위임
class Person:
    def __init__(self, name, age, number, email):
        self.name = name
        self.age = age
        self.number = number
        self.email = email


class Student(Person):
    def __init__(self, name, age, number, email, student_id):
        # Person의 init 메서드 호출
        super().__init__(name, age, number, email)
        self.student_id = student_id
```

**다중 상속에서의 사용**

```python
class ParentA:
    def __init__(self):
        self.value_a = 'ParentA'

    def show_value(self):
        print(f'Value from ParentA: {self.value_a}')


class ParentB:
    def __init__(self):
        self.value_b = 'ParentB'

    def show_value(self):
        print(f'Value from ParentB: {self.value_b}')


class Child(ParentA, ParentB):
    def __init__(self):
        super().__init__()  # ParentA 클래스의 __init__ 메서드 호출
        self.value_c = 'Child'

    def show_value(self):
        super().show_value()  # ParentA 클래스의 show_value 메서드 호출
        print(f'Value from Child: {self.value_c}')
```

```python
child = Child()
child.show_value()

"""
Value from ParentA: ParentA
Value from Child: Child
"""
```

> **주의** — `Child(ParentA, ParentB)`에서 `super()`는 MRO상 **다음 하나**(`ParentA`)만 가리킨다. `ParentB.__init__`은 실행되지 않으므로 `self.value_b`는 아예 만들어지지 않고, 출력에도 `Value from ParentB`가 없다. `super()`가 "모든 부모"를 부른다고 오해하기 쉬운 지점.

### super의 2가지 사용 사례 (p.65)

1. **단일 상속 구조**
   - 명시적으로 이름을 지정하지 않고 부모 클래스를 참조할 수 있으므로, 코드를 더 유지 관리하기 쉽게 만들 수 있음
   - 클래스 이름이 변경되거나 부모 클래스가 교체되어도 `super()`를 사용하면 코드 수정이 더 적게 필요
2. **다중 상속 구조**
   - MRO를 따른 메서드 호출
   - 복잡한 다중 상속 구조에서 발생할 수 있는 문제를 방지

## 클래스 참고

### 누가 어떤 메서드를 사용해야 할까 (p.69-70)

- 클래스가 사용해야 할 것
  - 클래스 메서드
  - 스태틱 메서드
- 인스턴스가 사용해야 할 것
  - 인스턴스 메서드

세 종류를 모두 가진 클래스로 직접 호출해 보며 차이를 확인한다.

```python
class MyClass:

    def instance_method(self):
        return 'instance method', self

    @classmethod
    def class_method(cls):
        return 'class method', cls

    @staticmethod
    def static_method():
        return 'static method'
```

### 클래스가 할 수 있는 것 vs 인스턴스가 할 수 있는 것 (p.71-73)

**클래스는 모든 메서드를 호출 할 수 있음.** 하지만 클래스는 클래스 메서드와 스태틱 메서드만 사용하도록 한다.

```python
instance = MyClass()

print(MyClass.instance_method(instance))  # ('instance method', <__main__.MyClass object at0x…028F10>)
print(MyClass.class_method())   # ('class method', <class '__main__.MyClass'>)
print(MyClass.static_method())  # static method
```

**인스턴스는 모든 메서드를 호출 할 수 있음.** 하지만 인스턴스는 인스턴스 메서드만 사용하도록 한다.

```python
instance = MyClass()

print(instance.instance_method())  # ('instance method', <__main__.MyClass object at 0x0000…84EAF10>)
print(instance.class_method())   # ('class method', <class '__main__.MyClass'>)
print(instance.static_method())  # static method
```

> **주의** — `할 수 있다 != 써도 된다`. 각자의 메서드는 객체지향 패러다임에 따라 명확한 목적에 따라 설계된 것이기 때문에 클래스와 인스턴스 각각 올바른 메서드만 사용한다.

클래스로 인스턴스 메서드를 부르려면 `MyClass.instance_method(instance)`처럼 인스턴스를 직접 넘겨야 한다. 인스턴스로 부를 때(`instance.instance_method()`)는 `self`가 자동으로 전달된다는 차이가 여기서 드러난다.

### 매직 메서드 (magic method) (p.75-76)

- 인스턴스 메서드
- 특정 상황에 자동으로 호출되는 메서드
- Double underscore(`__`)가 있는 메서드는 특수한 동작을 위해 만들어진 메서드
- 스페셜 메서드 혹은 매직 메서드라고 불림
- 예시: `__str__(self)`, `__len__(self)`, `__lt__(self, other)`, `__le__(self, other)`, `__eq__(self, other)`, `__gt__(self, other)`, `__ge__(self, other)`, `__ne__(self, other)`

> 📌 원본 p.75에는 `__len__(self)__` 로 되어 있으나 `__len__(self)` 가 맞다.

비교 연산자 매직 메서드는 이름과 연산자가 1:1로 대응한다.

| 매직 메서드 | 대응 연산자 |
| --- | --- |
| `__lt__` | `<` |
| `__le__` | `<=` |
| `__eq__` | `==` |
| `__ne__` | `!=` |
| `__gt__` | `>` |
| `__ge__` | `>=` |

**`__str__(self)`**: 내장함수 `print`에 의해 호출되어 객체 출력을 문자열 표현으로 변경.

```python
class Circle:
    def __init__(self, r):
        self.r = r

    def __str__(self):
        return f'원의 반지름: {self.r}'
```

```python
c1 = Circle(10)
c2 = Circle(1)

print(c1)  # 원의 반지름: 10
print(c2)  # 원의 반지름: 1
```

`__str__`을 정의하지 않으면 `print(c1)`은 `<__main__.Circle object at 0x...>` 같은 기본 표현이 나온다.

### 데코레이터 (Decorator) (p.78)

다른 함수의 코드를 유지한 채로 수정하거나 확장하기 위해 사용되는 함수.

```python
# 데코레이터 정의
def my_decorator(func):
    def wrapper():
        # 함수 실행 전에 수행할 작업
        print('함수 실행 전')
        # 원본 함수 호출
        result = func()
        # 함수 실행 후에 수행할 작업
        print('함수 실행 후')
        return result
    return wrapper
```

```python
# 데코레이터 사용
@my_decorator
def my_function():
    print('원본 함수 실행')

my_function()

"""
함수 실행 전
원본 함수 실행
함수 실행 후
"""
```

호출 흐름은 `my_function()` → `wrapper()` → (전처리) → `func()` = 원본 `my_function` → (후처리) 순이다. 앞서 본 `@classmethod`, `@staticmethod`도 같은 데코레이터 문법이다.

## 에러와 예외

### 파이썬의 에러 유형 (p.83)

| 구분 | 이름 | 설명 |
| --- | --- | --- |
| 문법 에러 | Syntax Error | 프로그램의 구문이 올바르지 않은 경우 발생 (오타, 괄호 및 콜론 누락 등의 문법적 오류) |
| 예외 | Exception | 프로그램 실행 중에 감지되는 에러 |

문법 에러는 실행 자체가 시작되지 못하고, 예외는 실행 도중에 터진다는 것이 결정적 차이다.

### 문법 에러 예시 (p.84-85)

- Invalid syntax (문법 오류)

```python
while # SyntaxError: invalid syntax
```

- assign to literal (잘못된 할당)

```python
5=3 # SyntaxError: cannot assign to literal
```

- EOL (End of Line)

```python
print('hello
# SyntaxError: EOL while scanning string literal
```

- EOF (End of File)

```python
print(
# SyntaxError: unexpected EOF while parsing
```

> **주의** — EOL은 따옴표가 줄 끝까지 닫히지 않은 경우, EOF는 괄호가 파일 끝까지 닫히지 않은 경우다. 둘 다 "닫지 않았다"는 같은 실수지만 파서가 어디까지 읽다 포기했는지가 다르다.

### 예외와 내장 예외 (p.87-88)

**예외(Exception)**: 프로그램 실행 중에 감지되는 에러.

**내장 예외(Built-in Exceptions)**: 예외 상황을 나타내는 예외 클래스들. 파이썬에서 이미 정의되어 있으며, 특정 예외 상황에 대한 처리를 위해 사용.

참고: https://docs.python.org/ko/3/library/exceptions.html#ValueError

### 대표적인 내장 예외 (p.89-95)

**ZeroDivisionError**: 나누기 또는 모듈로 연산의 두 번째 인자가 0일 때 발생

```python
10/0 # ZeroDivisionError: division by zero
```

**NameError**: 지역 또는 전역 이름을 찾을 수 없을 때 발생

```python
print(name_error)
# NameError: name 'name_error' is not defined
```

**TypeError**: 타입 불일치 / 인자 누락 / 인자 초과 / 인자 타입 불일치의 네 갈래로 나뉜다.

```python
# 타입 불일치
'2' + 2  # TypeError: can only concatenate str (not "int") to str

# 인자 누락
sum()  # TypeError: sum() takes at least 1 positional argument (0 given)

# 인자 초과
sum(1, 2, 3)  # TypeError: sum() takes at most 2 arguments (3 given)

# 인자 타입 불일치
import random
random.sample(1, 2)
# TypeError: Population must be a sequence.
# For dicts or sets, use sorted(d).
```

**ValueError**: 연산이나 함수에 문제가 없지만 부적절한 값을 가진 인자를 받았고, 상황이 `IndexError` 처럼 더 구체적인 예외로 설명되지 않는 경우 발생

```python
int('1.5') # ValueError: invalid literal for int() with base 10: '1.5'

range(3).index(6) # ValueError: 6 is not in range
```

> 📌 원본 p.92에는 `int('1.5')`의 에러 메시지가 `... with base 10: '3.5'` 로 되어 있으나, 인자가 `'1.5'`이므로 메시지도 `'1.5'`가 맞다.

**IndexError**: 시퀀스 인덱스가 범위를 벗어날 때 발생

```python
empty_list = []
empty_list[2]
# IndexError: list index out of range
```

**KeyError**: 딕셔너리에 해당 키가 존재하지 않는 경우

```python
person = {'name': 'Alice'}
person['age']  # KeyError: 'age'
```

**ModuleNotFoundError**: 모듈을 찾을 수 없을 때 발생

```python
import hahaha  # ModuleNotFoundError: No module named 'hahaha'
```

> **주의** — `TypeError`와 `ValueError`의 경계를 헷갈리기 쉽다. 타입 자체가 맞지 않으면 `TypeError`(`'2' + 2`), 타입은 맞는데 값이 부적절하면 `ValueError`(`int('1.5')` - 인자는 정상적인 str이지만 정수로 해석할 수 없는 값). 시퀀스는 `IndexError`, 딕셔너리는 `KeyError`로 짝지어 외운다.

### 내장 예외 마무리: ImportError / KeyboardInterrupt / IndentationError (p.96-98)

파이썬이 기본 제공하는 내장 예외(built-in exception) 목록의 뒷부분이다. 각각 언제 터지는지와 실제 메시지를 함께 외워두면 트레이스백만 보고 원인을 바로 짚을 수 있다.

- **ImportError** - `import` 하려는 이름을 찾을 수 없을 때 발생

```python
from random import hahaha

# ImportError: cannot import name 'hahaha' from 'random'
```

- **KeyboardInterrupt** - 사용자가 `Control-C` 또는 `Delete`를 누를 때 발생. 무한루프에 빠진 프로그램을 강제 종료할 때 실제로 만나게 되는 예외다.

```python
while True:
    continue

'''
Traceback (most recent call last):
  File "...", line 2, in <module>
    continue
KeyboardInterrupt
'''
```

- **IndentationError** - 잘못된 들여쓰기와 관련된 문법 오류

```python
for i in range(10):
print(i)  # IndentationError: expected an indented block
```

> **주의** - `KeyboardInterrupt`는 `Exception`이 아니라 `BaseException` 바로 아래에 있다. 그래서 `except Exception:`으로는 잡히지 않는다(뒤의 상속 계층구조 참고).

## 예외 처리

### 예외 처리란 (p.101)

**예외 처리(Exception Handling)** 는 예외가 발생했을 때 프로그램이 비정상적으로 종료되지 않고, 적절하게 처리할 수 있도록 하는 방법이다. 에러를 "없애는" 것이 아니라, 에러가 났을 때의 **대응 흐름을 미리 정의해두는 것**이 핵심이다.

### 예외처리 4가지 절: try / except / else / finally (p.102, p.112)

| 절 | 언제 실행되는가 |
| --- | --- |
| `try` | 예외가 발생할 수 있는 코드를 작성 |
| `except` | 예외가 발생했을 때 실행할 코드를 작성 |
| `else` | 예외가 **발생하지 않았을 때** 실행할 코드를 작성 |
| `finally` | 예외 발생 여부와 **상관없이 항상** 실행할 코드를 작성 |

```python
try:
    x = int(input('숫자를 입력하세요: '))
    y = 10 / x
except ZeroDivisionError:
    print('0으로 나눌 수 없습니다.')
except ValueError:
    print('유효한 숫자가 아닙니다.')
else:
    print(f'결과: {y}')
finally:
    print('프로그램이 종료되었습니다.')
```

- `else` 블록은 예외가 발생하지 않았을 때 **추가 작업**을 진행하는 자리다.
- `finally` 블록은 파일 닫기, 연결 해제처럼 성공/실패와 무관하게 반드시 해야 하는 뒷정리에 쓴다.

### try-except 구조와 제어 흐름 (p.104-105)

예외가 발생하면 프로그램 흐름은 `try` 블록을 빠져나와 해당 예외에 대응하는 `except` 블록으로 이동한다.

```text
try 블록 실행
   ├─ 예외 없음 ─────────────→ (else 블록) ─→ finally 블록
   └─ 예외 발생 ─→ 예외 타입과 일치하는 except 블록 ─→ finally 블록
```

`try` 안에서 예외가 터진 **그 지점 이후의 남은 코드는 실행되지 않는다**. 예외가 난 줄부터 바로 except로 점프한다.

```python
try:
    # 예외가 발생할 수 있는 코드

except 예외:
    # 예외 처리 코드
```

실제 예시 두 가지.

```python
try:
    result = 10 / 0
except ZeroDivisionError:
    print('0으로 나눌 수 없습니다.')

# 0으로 나눌 수 없습니다.
```

```python
try:
    num = int(input('숫자입력 : '))
except ValueError:
    print('숫자가 아닙니다.')

"""
숫자입력 : a
숫자가 아닙니다.
"""
```

### 복수 예외 처리 (p.107-110)

**문제** - 100을 사용자가 입력한 값으로 나누고 출력하는 코드를 작성하시오.

```python
num = int(input('100으로 나눌 값을 입력하시오 : '))
print(100 / num)
```

먼저 발생 가능한 에러가 무엇인지 예상해보는 것이 순서다. 이 두 줄에서 나올 수 있는 예외는 두 가지다.

| 상황 | 발생 예외 |
| --- | --- |
| `int('a')` - 문자열을 int로 형변환 | `ValueError` |
| `100 / 0` - 0으로 숫자를 나눔 | `ZeroDivisionError` |

발생 가능한 에러를 **모두 명시하거나 & 별도로 작성**하는 두 가지 방식이 있다.

방식 1 - 여러 예외를 튜플로 묶어 한 번에 처리 (처리 내용이 같을 때):

```python
try:
    num = int(input('100으로 나눌 값을 입력하시오 : '))
    print(100 / num)
except (ValueError, ZeroDivisionError):
    print('제대로 입력해주세요.')
```

방식 2 - 예외별로 다른 메시지를 주고, 마지막에 나머지를 받는 `except`를 둠:

```python
try:
    num = int(input('100으로 나눌 값을 입력하시오 : '))
    print(100 / num)
except ValueError:
    print('숫자를 넣어주세요.')
except ZeroDivisionError:
    print('0으로 나눌 수 없습니다.')
except:
    print('에러가 발생하였습니다.')
```

### 내장 예외의 상속 계층구조 주의 (p.114-115)

내장 예외 클래스는 상속 계층구조를 가지기 때문에, `except` 절로 분기할 때 **반드시 하위 클래스를 먼저 확인할 수 있도록 작성해야 한다.**

아래처럼 최상위인 `BaseException`을 먼저 쓰면, 코드는 2번째 `except` 절 이후로 **도달하지 못한다.**

```python
try:
    num = int(input('100으로 나눌 값을 입력하시오 : '))
    print(100 / num)
except BaseException:
    print('숫자를 넣어주세요.')
except ZeroDivisionError:      # 이 블록에 도달하지 못함
    print('0으로 나눌 수 없습니다.')
except:                        # 이 블록에 도달하지 못함
    print('에러가 발생하였습니다.')
```

내장 예외 계층구조(공식 문서 `https://docs.python.org/ko/3/library/exceptions.html#exception-hierarchy`)를 문자로 옮기면 다음과 같다.

```text
BaseException
├── Exception
│   ├── AttributeError
│   ├── ArithmeticError
│   │   ├── FloatingPointError
│   │   ├── OverflowError
│   │   └── ZeroDivisionError
│   ├── EOFError
│   ├── NameError
│   ├── LookupError
│   │   ├── IndexError
│   │   └── KeyError
│   ├── StopIteration
│   ├── OSError
│   │   ├── FileExistsError
│   │   └── PermissionError
│   ├── TypeError
│   └── ValueError
└── KeyboardInterrupt
```

> **주의** - `except`는 위에서부터 순서대로 매칭을 시도하고, **처음 일치한 하나만** 실행한다. 상위 클래스를 위에 두면 하위 클래스 분기가 영원히 죽은 코드가 된다. 넓은 예외(`BaseException`, `Exception`, 맨 마지막의 `except:`)는 항상 **아래쪽**에 둘 것.

## 모듈

### 모듈이란 (p.118)

**모듈(Module)** 은 한 파일로 묶인 변수와 함수의 모음이다. 즉 **특정한 기능을 하는 코드가 작성된 파이썬 파일(.py)** 그 자체다. 파일 하나 = 모듈 하나라고 생각하면 된다.

### 모듈을 가져오는 방법: import / from (p.120)

`import` 문 사용 - 모듈 전체를 가져오고, 쓸 때는 `모듈명.이름`으로 접근한다.

```python
import math

print(math.sqrt(4))
```

`from` 절 사용 - 모듈 안의 특정 이름만 가져오고, 모듈명 없이 바로 쓴다.

```python
from math import sqrt

print(sqrt(4))
```

### `.` (dot) 연산자 (p.121)

`.` 연산자는 **"점의 왼쪽 객체에서 점의 오른쪽 이름을 찾아라"** 라는 의미다. 모듈이든 클래스든 인스턴스든 이 규칙은 동일하게 적용된다.

```python
# 모듈명.변수명
print(math.pi)

# 모듈명.함수명
print(math.sqrt(4))
```

### 모듈 주의사항: 이름 충돌과 `*` (p.122)

- 서로 다른 모듈이 **같은 이름의 함수를 제공할 경우 문제가 발생**한다.
- 나중에 `import`된 이름이 앞의 이름을 **덮어써 버린다**(마지막에 import된 이름으로 대체됨).

```python
from math import pi, sqrt
from my_math import sqrt
```

위 코드에서 `sqrt`는 `my_math`의 것으로 바뀌어, `math.sqrt`는 더 이상 호출할 수 없다.

```python
# 그래서 모듈 내 모든 요소를 한번에 import 하는 * 표기는 권장하지 않음

from math import *
```

> **주의** - `from 모듈 import *`는 어떤 이름이 들어왔는지 코드만 봐서는 알 수 없고, 기존 이름을 조용히 덮어쓴다. 충돌 원인을 추적하기 어려워지므로 쓰지 말 것.

### `as` 키워드로 별칭 붙이기 (p.123)

`as` 키워드를 사용하여 **별칭(alias)** 을 부여한다. 두 개 이상의 모듈에서 동일한 이름의 변수, 함수, 클래스 등을 가져올 때 발생하는 **이름 충돌을 해결**하는 방법이다.

```python
from math import sqrt
from my_math import sqrt as my_sqrt

sqrt(4)
my_sqrt(4)
```

### 사용자 정의 모듈 만들어 쓰기 (p.125)

직접 정의한 모듈을 쓰는 절차는 세 단계다.

- [ ] 1. 모듈 `my_math.py` 작성
- [ ] 2. 두 수의 합을 구하는 `add` 함수 작성
- [ ] 3. `my_math` 모듈 import 후 `add` 함수 호출

<details>
<summary>정답 코드</summary>

```python
# my_math.py
def add(x, y):
    return x + y
```

```python
# sample.py
import my_math

print(my_math.add(1, 2))
```

</details>

같은 디렉터리(`LECTURE/`)에 `my_math.py`와 `sample.py`가 나란히 있으면 별도 설정 없이 바로 import된다.

## 파이썬 표준 라이브러리와 패키지

### 파이썬 표준 라이브러리 (p.128)

**파이썬 표준 라이브러리(Python Standard Library, PSL)** 는 파이썬 언어와 함께 제공되는 다양한 **모듈과 패키지의 모음**이다. 별도 설치 없이 바로 쓸 수 있는 것들이 여기에 들어 있다. 공식 문서: `https://docs.python.org/ko/3/library/index.html`

### 패키지란 (p.129)

**패키지(Package)** 는 **연관된 모듈들을 하나의 디렉터리에 모아 놓은 것**이다. 모듈이 파일 단위라면, 패키지는 디렉터리 단위의 묶음이다.

```text
모듈(module)   = 파이썬 파일 1개 (.py)
패키지(package) = 모듈들을 담은 디렉터리
라이브러리      = 패키지/모듈의 모음
```

### 패키지 사용하기 (p.131-132)

아래와 같은 디렉터리 구조로 작성한다. 여기서 **패키지 3개**는 `my_package`, `math`, `statistics`이고, **모듈 2개**는 `my_math`, `tools`다.

```text
sample.py
my_package/
├── math/
│   └── my_math.py
└── statistics/
    └── tools.py
```

```python
# my_package/math/my_math.py
def add(x, y):
    return x + y
```

```python
# my_package/statistics/tools.py
def mod(x, y):
    return x % y
```

각 패키지의 모듈을 `import` 하여 사용한다. 패키지 경로는 `.`으로 이어 쓴다.

```python
# sample.py

from my_package.math import my_math
from my_package.statistics import tools


print(my_math.add(1, 2))  # 3
print(tools.mod(1, 2))  # 1
```

> **주의** - 이 예시의 하위 패키지 이름 `math`, `statistics`는 파이썬 표준 라이브러리와 이름이 같다. `my_package.` 접두어를 통해 접근하므로 예제는 동작하지만, 실무에서 표준 라이브러리와 같은 이름을 쓰면 import 충돌을 부르니 피하는 것이 좋다.

### 패키지 종류: PSL 내부 패키지 vs 외부 패키지 (p.133)

| 구분 | 사용 방법 |
| --- | --- |
| **PSL 내부 패키지** | 설치 없이 바로 `import`하여 사용 |
| **외부 패키지** | `pip`를 사용하여 설치 후 `import` 필요 |

### pip와 패키지 설치 (p.134-135)

**pip** 는 외부 패키지들을 설치하도록 도와주는 **파이썬의 패키지 관리 시스템**이다. 패키지 저장소는 `https://pypi.org/` 다.

최신 버전 / 특정 버전 / 최소 버전을 명시하여 설치할 수 있다.

```bash
$ pip install SomePackage
$ pip install SomePackage==1.0.5
$ pip install SomePackage>=1.0.4
```

- 버전을 안 쓰면 최신 버전, `==`는 정확히 그 버전, `>=`는 그 버전 이상을 뜻한다.

### 외부 패키지 설치 및 사용 예시: requests (p.136)

```bash
$ pip install requests
```

```python
import requests


url = 'https://random-data-api.com/api/v2/users'
response = requests.get(url).json()

print(response)
```

### 패키지 사용 목적 (p.137)

- 모듈들의 **이름공간을 구분하여 충돌을 방지**한다.
- 모듈들을 **효율적으로 관리하고 재사용**할 수 있도록 돕는 역할을 한다.

> **주의** - 앞서 본 `from math import *`의 이름 충돌 문제와 같은 맥락이다. 패키지는 결국 "이름을 어디에 소속시킬 것인가"를 관리하는 장치다.

---

## 정리 체크리스트

- [ ] 클래스와 인스턴스의 관계를 "설계도와 실체"로 설명할 수 있다
- [ ] 인스턴스 변수와 클래스 변수의 차이, 그리고 `c2.pi = 5` 가 클래스 변수를 바꾸지 못하는 이유를 말할 수 있다
- [ ] `'hello'.upper()` 가 `str.upper('hello')` 와 같다는 것을 `self` 로 설명할 수 있다
- [ ] 인스턴스 메서드 / 클래스 메서드(`@classmethod`) / 정적 메서드(`@staticmethod`) 를 언제 쓰는지 구분할 수 있다
- [ ] 이름 공간 탐색이 인스턴스 → 클래스 방향의 **단방향**이라는 것을 안다
- [ ] 다중 상속의 다이아몬드 문제와 MRO를 설명할 수 있다
- [ ] `super()` 가 MRO상 다음 클래스 하나만 가리킨다는 것을 안다
- [ ] 매직 메서드(`__str__`, `__len__`, `__eq__`)를 정의해 내장 함수 동작을 바꿀 수 있다
- [ ] 에러와 예외의 차이, `try / except / else / finally` 각 절의 실행 시점을 안다
- [ ] `except` 절을 상위 예외부터 쓰면 왜 아래가 죽는지 설명할 수 있다
- [ ] `import` / `from ... import` / `as` 의 이름 공간 차이를 안다

## 복습 문제

1. `Circle.pi` 가 `3.14` 인 상태에서 인스턴스 `c2` 에 대해 `c2.pi = 5` 를 실행하면 `Circle.pi` 와 다른 인스턴스 `c1.pi` 는 어떻게 되는가? — <details><summary>답</summary>둘 다 `3.14` 그대로다. `c2.pi = 5` 는 클래스 변수를 바꾸는 게 아니라 `c2` 에 **새 인스턴스 변수** `pi` 를 만든다. 이름 공간 탐색이 인스턴스 → 클래스 단방향이라, 이후 `c2.pi` 는 자기 인스턴스 변수 `5` 를 먼저 찾아 반환한다.</details>
2. `@classmethod` 와 `@staticmethod` 는 각각 언제 쓰는가? — <details><summary>답</summary>클래스 메서드는 첫 인자로 `cls` 를 받아 **클래스 변수를 읽거나 바꿔야 할 때** 쓴다(인스턴스 개수 카운트 등). 정적 메서드는 `self` 도 `cls` 도 받지 않으며, 논리적으로 그 클래스에 속하지만 **클래스·인스턴스 상태를 전혀 건드리지 않는 유틸리티**에 쓴다.</details>
3. `class FirstChild(Dad, Mom)` 과 `class FirstChild(Mom, Dad)` 는 무엇이 달라지는가? — <details><summary>답</summary>MRO(메서드 탐색 순서)가 달라진다. 같은 이름의 속성·메서드가 양쪽 부모에 있으면 **먼저 쓴 부모의 것**이 이긴다. 그래서 상속 목록의 순서가 결과를 바꾼다.</details>
4. `except Exception:` 으로 `KeyboardInterrupt` 를 잡을 수 있는가? — <details><summary>답</summary>없다. `KeyboardInterrupt` 는 `Exception` 이 아니라 `BaseException` 바로 아래에 있다. `Ctrl+C` 로 강제 종료하려는 의도가 광범위한 `except` 에 삼켜지지 않도록 계층이 그렇게 설계돼 있다.</details>
5. `try / except / else / finally` 에서 `else` 와 `finally` 는 각각 언제 실행되는가? — <details><summary>답</summary>`else` 는 **예외가 발생하지 않았을 때만** 실행된다. `finally` 는 예외 발생 여부와 무관하게 **항상** 마지막에 실행된다(자원 정리용).</details>
6. `from math import *` 를 쓰면 안 되는 이유는? — <details><summary>답</summary>모듈 안의 모든 이름이 현재 이름 공간에 쏟아져 들어와, 내가 정의한 함수·변수와 **이름이 충돌해도 조용히 덮어쓴다.** 어떤 이름이 어디서 왔는지도 추적할 수 없다. `import math` 로 점 표기법을 쓰거나 필요한 이름만 골라 가져온다.</details>
