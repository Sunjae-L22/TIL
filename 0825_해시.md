# 파이썬 해시 완전정리 — dict와 set은 왜 빠르고 어디서 깨지는가

> 자기주도 학습 정리 (코딩 테스트 대비) · 정리일: 2026-08-25
> 블로그 발행본: https://it-study-2002.tistory.com/entry/파이썬-해시-완전정리
> 시리즈: **01 해시** · [02 DFS와 BFS 이론](0825_DFS와BFS_이론.md) · [03 DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md)

리스트로 짠 풀이가 예제는 다 맞는데 제출만 하면 시간 초과가 뜬다. 코드를 아무리 들여다봐도 논리는 멀쩡하다. 이때 범인은 대개 `in` 한 줄이다.

이 글은 그 한 줄을 `O(N)`에서 `O(1)`로 바꾸는 도구인 해시를 정리한 것이다. "dict 쓰면 빨라진다"까지는 대부분 알지만, **왜 빨라지는지**와 **그 빠름이 어디서 깨지는지**를 모르면 엉뚱한 데 쓰거나 반대로 써야 할 때 못 쓴다. 원리 → 파이썬 구현체 → 코딩 테스트 패턴 → 쓰면 안 되는 경우 순서로 묶었다.

<br>

## 📖 이 글에서 다루는 내용

1. 리스트로는 안 되는 순간
2. 해시 테이블은 어떻게 O(1)이 되는가
3. 충돌 — O(1)이 깨지는 지점
4. "연산 1번"이라는 오해
5. hashable — 무엇이 키가 될 수 있는가
6. 파이썬 해시 4대장
7. 코딩 테스트에서 해시를 눈치채는 신호
8. Counter 연산 제대로 알기
9. set 연산으로 두 집합 비교하기
10. 해시가 답이 아닐 때

<br>

---

## 1. 리스트로는 안 되는 순간

리스트에서 `x in my_list`를 하면 파이썬은 **0번 인덱스부터 하나씩 꺼내 비교한다.** 찾는 값이 마지막에 있거나 아예 없으면 N번을 전부 훑는다. 이게 `O(N)`이다.

문제는 이 탐색이 반복문 안에 들어갈 때다. 원소 N개를 각각 리스트에서 찾으면 `O(N²)`이 된다. N이 10만이면 100억 번이고, 파이썬은 초당 대략 1천만~1억 번 수준의 연산을 처리하므로 확실히 시간 초과다.

> **[그림]** 리스트는 처음부터 순차 비교해 N번이 걸리지만 해시는 키를 계산해 한 번에 위치로 도달하는 과정 비교

해시는 접근 방식이 다르다. **찾는 게 아니라 계산한다.** 키를 넣으면 저장 위치가 바로 나오므로, 데이터가 10개든 10만 개든 도달하는 비용이 거의 같다.

```python
data = list(range(100_000))
data_set = set(data)

99_999 in data       # 리스트: 10만 번 비교
99_999 in data_set   # 집합: 해시값 계산 후 한 번에 접근
```

> ⚠️ **`in` 하나로 성능이 갈린다**
> 같은 `in` 키워드지만 대상 자료형에 따라 시간 복잡도가 완전히 다르다. 리스트·튜플·문자열은 `O(N)`, 집합·딕셔너리는 평균 `O(1)`이다. 코드 모양이 똑같아서 눈으로는 안 보인다는 게 이 함정의 무서운 점이다.

<br>

---

## 2. 해시 테이블은 어떻게 O(1)이 되는가

용어부터 정리하면 흔한 혼동 하나가 사라진다.

| 용어 | 정확한 의미 |
|---|---|
| **해시 함수** | 임의의 데이터를 고정된 크기의 정수로 바꾸는 함수 |
| **해시값** | 해시 함수가 뱉은 그 정수 |
| **해시 테이블** | 해시값을 배열 인덱스로 삼아 데이터를 저장하는 자료구조 |
| **dict / set** | 파이썬이 해시 테이블로 구현해 둔 내장 자료형 |

> 📌 흔히 "해시는 키와 값을 짝지어 저장하는 자료구조"라고 설명하지만, 정확히는 **해시는 값을 정수로 바꾸는 기법**이고 그걸 이용해 만든 것이 해시 테이블이다. `set`은 값 없이 키만 저장하므로 "키-값 짝"이라는 정의에 애초에 들어맞지 않는다.

파이썬에서는 내장 함수 `hash()`로 해시값을 직접 볼 수 있다.

```python
hash(42)        # 42
hash("apple")   # 예: -8877568...  (실행할 때마다 달라진다)
hash((1, 2))    # 튜플도 가능
```

동작 순서는 세 단계다.

> **[그림]** 키가 해시 함수를 거쳐 정수가 되고 테이블 크기로 나눈 나머지가 배열 인덱스가 되는 3단계 흐름

1. **키 → 해시값**: `hash("apple")`이 큰 정수를 만든다.
2. **해시값 → 인덱스**: 테이블 크기로 나눈 나머지를 취해 배열 범위 안의 인덱스로 접힌다.
3. **인덱스 → 저장/조회**: 배열의 그 자리에 바로 접근한다.

배열은 인덱스를 알면 즉시 접근할 수 있는 자료구조다. 그러니 **키에서 인덱스를 계산해내기만 하면 탐색이 사라진다.** 이게 해시가 빠른 이유의 전부다.

> 💡 **문자열 해시값은 실행할 때마다 바뀐다**
> 파이썬은 보안상 이유로 프로세스마다 다른 시드를 써서 문자열 해시를 뒤섞는다(해시 충돌 공격 방어). 그래서 `hash("apple")` 값을 코드에 하드코딩하거나, 실행 간에 같을 거라 가정하면 안 된다. 정수·튜플은 이 영향을 받지 않는다.

<br>

---

## 3. 충돌 — O(1)이 깨지는 지점

서로 다른 키가 같은 인덱스로 계산되는 일이 반드시 생긴다. 해시값의 경우의 수는 사실상 무한한데 배열 칸은 유한하기 때문이다. 이걸 **해시 충돌(collision)** 이라고 한다.

> **[그림]** 두 키가 같은 인덱스를 가리키는 충돌 상황과 체이닝 및 개방 주소법 두 가지 해결 방식 비교

해결 방식은 크게 둘이다.

| 방식 | 동작 | 특징 |
|---|---|---|
| **체이닝** | 같은 칸에 연결 리스트를 달아 이어 붙인다 | 구현이 단순하다. 자바 `HashMap`이 이 방식 |
| **개방 주소법** | 자리가 차 있으면 다른 빈 칸을 찾아간다 | 포인터가 없어 캐시 효율이 좋다. **CPython의 dict가 이 방식** |

여기가 핵심이다. **충돌이 나면 그만큼 추가 탐색이 붙는다.** 극단적으로 모든 키가 같은 칸에 몰리면 탐색은 결국 선형이 되어 `O(N)`으로 퇴화한다.

| 연산 | 평균 | 최악 |
|---|---|---|
| 조회 · 삽입 · 삭제 | `O(1)` | `O(N)` |

> 📌 "해시는 `O(1)`에 수렴한다"는 표현은 정확하지 않다. **평균이 `O(1)`이고 최악은 `O(N)`이다.** 다만 파이썬은 적재율이 일정 수준을 넘으면 테이블을 키워 다시 배치하고(rehashing), 탐사 순서에도 해시값 상위 비트를 섞어 쓰기 때문에 실전 코딩 테스트에서 최악에 걸릴 일은 거의 없다. 그래도 "항상 1번"이 아니라는 것은 알고 있어야 면접에서 막히지 않는다.

<br>

---

## 4. "연산 1번"이라는 오해

`O(1)`을 "연산 한 번"으로 읽는 설명을 자주 보는데, 이건 잘못이다. `O(1)`은 **데이터 개수 N에 관계없이 비용이 일정하다**는 뜻이지 비용이 1이라는 뜻이 아니다.

> **[그림]** 데이터 개수와 무관하게 일정한 상수 비용이지만 그 상수 안에 해시 계산 비용이 들어 있음을 보여주는 그래프

실제로는 조회 한 번에 최소 이만큼이 든다.

- 키의 해시값 계산
- 인덱스로 접기
- 배열 접근
- 충돌 여부 확인을 위한 키 비교(`==`)

특히 **문자열 키의 해시 계산은 문자열 길이 L에 비례해 `O(L)`이다.** 문자 전체를 훑어야 하기 때문이다. 그래서 키가 아주 긴 문자열이면 상수가 커진다.

```python
key = "a" * 1_000_000
hash(key)   # 첫 호출: 100만 글자를 전부 훑는다
hash(key)   # 두 번째부터: 캐시된 값을 즉시 반환
```

> 💡 **파이썬 문자열은 해시값을 캐싱한다**
> `str` 객체는 한 번 계산한 해시값을 내부에 저장해 두고 재사용한다. 그래서 같은 문자열 객체를 반복해서 키로 쓰면 두 번째부터는 계산 비용이 사라진다. 다만 매번 새로 만들어지는 문자열(예: 반복문 안의 슬라이싱 결과)은 매번 새로 계산된다.

정리하면 **해시가 빠른 이유는 연산이 적어서가 아니라 N이 커져도 연산이 늘지 않기 때문이다.** N이 아주 작을 때는 오히려 리스트가 더 빠를 수도 있다.

<br>

---

## 5. hashable — 무엇이 키가 될 수 있는가

코딩 테스트에서 실제로 가장 자주 만나는 에러가 이것이다.

```python
visited = {}
visited[[1, 2]] = True
# TypeError: unhashable type: 'list'
```

해시 테이블은 **키의 해시값이 절대 변하지 않는다**는 전제 위에 서 있다. 값이 바뀌면 저장할 때 계산한 인덱스와 찾을 때 계산한 인덱스가 달라져 데이터를 영영 못 찾는다. 그래서 파이썬은 **변경 가능한(mutable) 객체를 키로 받지 않는다.**

> **[그림]** 리스트 딕셔너리 집합은 키로 쓸 수 없고 정수 문자열 튜플 frozenset은 키로 쓸 수 있음을 구분한 분류표

| 키로 쓸 수 있다 (hashable) | 키로 쓸 수 없다 (unhashable) |
|---|---|
| `int`, `float`, `str`, `bool` | `list` |
| `tuple` (원소가 전부 hashable일 때) | `dict` |
| `frozenset` | `set` |
| `None`, 사용자 정의 클래스(기본값) | `bytearray` |

```python
visited = {}
visited[(1, 2)] = True          # 튜플은 불변 → 가능

seen = set()
seen.add(frozenset([1, 2]))     # set은 불가, frozenset은 가능

hash((1, [2]))
# TypeError — 튜플이라도 안에 리스트가 있으면 불가
```

> ⚠️ **좌표 문제는 튜플 키가 정석이다**
> BFS·DFS에서 방문 처리를 할 때 `visited[(x, y)]` 형태로 튜플을 키로 쓰는 패턴은 거의 필수다. 2차원 리스트를 만들 수 없는 경우(좌표 범위가 넓거나 음수)에 특히 그렇다. 마지막 줄처럼 튜플 안에 리스트가 섞이면 튜플이어도 실패한다는 점을 같이 기억해 두자.

<br>

---

## 6. 파이썬 해시 4대장

`dict`와 `set`만 알고 가면 코드가 길어진다. `collections`의 두 개를 더 챙기면 대부분의 빈도수·그룹핑 문제가 몇 줄로 끝난다.

> **[그림]** dict set Counter defaultdict 네 가지 자료구조의 용도와 특징을 나란히 비교한 카드

### dict — 키와 값을 직접 관리할 때

```python
scores = {"apple": 1, "banana": 2}
scores["cherry"] = 3

"apple" in scores        # True  ← 키를 검사한다
1 in scores              # False ← 값은 검사하지 않는다
```

> ⚠️ **`in`은 키만 본다**
> `1 in scores`가 `False`인 이유는 `in`이 키만 검사하기 때문이다. 값에서 찾으려면 `1 in scores.values()`를 써야 하는데, 이건 **`O(N)`이라 해시의 이점이 사라진다.** 값으로 찾을 일이 잦다면 애초에 키와 값을 뒤집어 저장하는 편이 낫다.

### set — 존재 여부와 중복 제거

값이 필요 없고 "있냐 없냐"만 중요할 때, 그리고 중복을 없앨 때 쓴다.

```python
nums = [1, 2, 2, 3]
unique = set(nums)       # {1, 2, 3}
len(unique)              # 3 — 중복 제거 후 개수
```

### Counter — 등장 횟수 자동 집계

```python
from collections import Counter

participant = ["leo", "kiki", "eden", "leo"]
count = Counter(participant)
# Counter({'leo': 2, 'kiki': 1, 'eden': 1})

count.most_common(1)     # [('leo', 2)] — 가장 많이 나온 것
count["없는키"]           # 0 — KeyError가 아니라 0을 반환한다
```

`most_common()`은 인자를 안 주면 전체를 빈도 내림차순으로 정렬해 반환한다. 빈도수 문제의 절반은 이 한 줄로 끝난다.

### defaultdict — 값이 누적될 때

키가 없을 때 기본값을 자동으로 만들어 준다. **그래프 인접 리스트나 그룹핑에서는 Counter보다 이쪽이 훨씬 자주 쓰인다.**

```python
from collections import defaultdict

graph = defaultdict(list)
graph[1].append(2)       # 키 1이 없어도 []를 만든 뒤 append
graph[1].append(3)
# defaultdict(<class 'list'>, {1: [2, 3]})

group = defaultdict(int)
group["a"] += 1          # 0에서 시작해 1
```

> ⚠️ **defaultdict는 조회만 해도 키가 생긴다**
> `graph[99]`처럼 읽기만 해도 빈 리스트가 만들어져 딕셔너리에 남는다. 나중에 `len(graph)`를 세거나 키를 순회할 때 예상 밖의 항목이 끼어드는 원인이 된다. 값을 만들 의도 없이 확인만 하려면 `graph.get(99)` 또는 `99 in graph`를 써야 한다.

| 상황 | 선택 |
|---|---|
| 키와 값을 직접 지정 | `dict` |
| 존재 여부 확인 · 중복 제거 | `set` |
| 등장 횟수 세기 · 최빈값 | `Counter` |
| 리스트에 누적 · 그룹으로 묶기 | `defaultdict(list)` |

<br>

---

## 7. 코딩 테스트에서 해시를 눈치채는 신호

문제를 읽다가 아래 셋 중 하나가 보이면 해시를 먼저 떠올린다.

> **[그림]** 데이터 크기가 크거나 탐색이 반복되거나 빈도수를 세는 세 가지 신호를 정리한 도식

### 신호 1 — 입력 크기가 10만 이상이다

제한 조건에 `1 ≤ N ≤ 100,000`이 보이면 `O(N²)` 풀이는 이미 탈락이다. 이중 반복문 대신 한 번 순회하며 해시에 넣고, 다시 한 번 순회하며 조회하는 `O(N)` 구조로 바꾼다.

```python
# O(N²) — 시간 초과
for x in a:
    if x in b:           # b가 리스트면 매번 O(N)
        ...

# O(N) — b를 집합으로 한 번만 변환
b_set = set(b)
for x in a:
    if x in b_set:       # O(1)
        ...
```

### 신호 2 — 같은 탐색을 반복한다

"이 값이 이미 나왔는가"를 계속 확인해야 하는 유형이다. 중복 판정, 방문 처리, 두 수의 합(투 섬)이 여기 해당한다.

```python
# 두 수의 합 — 한 번 순회로 끝난다
def two_sum(nums, target):
    seen = {}
    for i, n in enumerate(nums):
        if target - n in seen:
            return [seen[target - n], i]
        seen[n] = i
```

### 신호 3 — 개수를 세야 한다

"누가 몇 표를 받았는가", "어떤 옷을 몇 벌 가졌는가" 같은 유형이다. `Counter`를 바로 꺼낸다.

> ✅ **판단 기준 한 줄**
> 반복문 안에 `in`이나 `.index()`가 있고 그 대상이 리스트라면, 그 자리에 해시가 들어가야 한다.

<br>

---

## 8. Counter 연산 제대로 알기

`Counter`끼리는 사칙연산 비슷한 연산을 할 수 있다. 이게 강력한 이유가 있는데, 그 이유를 모르고 외우면 다른 문제에서 틀린다.

> **[그림]** Counter 뺄셈이 0 이하 항목을 자동으로 제거하는 동작과 subtract 메서드가 음수를 보존하는 차이

| 연산 | 동작 | 0 이하 처리 |
|---|---|---|
| `c1 - c2` | 개수를 뺀다 | **0 이하는 결과에서 사라진다** |
| `c1 + c2` | 개수를 더한다 | 해당 없음 |
| `c1 & c2` | 각 키의 최솟값 (교집합) | 0은 사라진다 |
| `c1 \| c2` | 각 키의 최댓값 (합집합) | 0은 사라진다 |
| `c1.subtract(c2)` | 개수를 뺀다 (제자리 수정) | **음수를 그대로 남긴다** |

핵심은 첫 줄이다. `-` 연산은 **결과가 0 이하인 항목을 조용히 버린다.** 그래서 "남은 것 하나"를 찾는 문제가 한 줄로 풀린다.

```python
from collections import Counter

participant = ["leo", "kiki", "eden", "leo"]   # leo 동명이인 2명
completion  = ["leo", "kiki", "eden"]          # leo 1명만 완주

result = Counter(participant) - Counter(completion)
# Counter({'leo': 1})  ← kiki, eden은 0이 되어 사라졌다

answer = next(iter(result))   # 'leo'
```

> 💡 **`list(result.keys())[0]`보다 `next(iter(result))`**
> `list(...)[0]`은 키 전체를 리스트로 복사한 뒤 첫 항목만 쓴다. `next(iter(...))`는 첫 항목 하나만 꺼내므로 불필요한 복사가 없다. 결과가 하나뿐인 게 보장되는 상황에서는 이쪽이 깔끔하다.

음수까지 봐야 한다면 `-` 대신 `subtract()`를 쓴다.

```python
c = Counter({"a": 1})
c.subtract({"a": 3})
# Counter({'a': -2})  ← 사라지지 않고 음수로 남는다

Counter({"a": 1}) - Counter({"a": 3})
# Counter()  ← 통째로 사라진다
```

> ⚠️ **`-`와 `subtract()`를 헷갈리면 조용히 틀린다**
> 에러가 나지 않고 항목만 사라지기 때문에 디버깅이 어렵다. "부족한 개수"를 구해야 하는 문제에서 `-`를 쓰면 답이 빈 결과로 나온다.

<br>

---

## 9. set 연산으로 두 집합 비교하기

두 리스트를 비교하라는 문제는 대부분 집합 연산 한 줄로 끝난다. 중복을 무시해도 되는 경우에 한해서다.

> **[그림]** 교집합 합집합 차집합 대칭차집합 네 가지 집합 연산을 벤 다이어그램으로 표현한 도식

| 연산자 | 메서드 | 의미 |
|---|---|---|
| `a & b` | `a.intersection(b)` | 양쪽에 모두 있는 것 |
| `a \| b` | `a.union(b)` | 어느 한쪽에라도 있는 것 |
| `a - b` | `a.difference(b)` | a에만 있는 것 |
| `a ^ b` | `a.symmetric_difference(b)` | 한쪽에만 있는 것 |

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}

a & b        # {3, 4}
a | b        # {1, 2, 3, 4, 5}
a - b        # {1, 2}
a ^ b        # {1, 2, 5}

a <= b       # False — a가 b의 부분집합인가
```

> ⚠️ **집합 연산은 중복을 지운다**
> `set`으로 바꾸는 순간 개수 정보가 사라진다. 위의 '완주하지 못한 선수'처럼 동명이인이 있는 문제에 `set(participant) - set(completion)`을 쓰면 빈 집합이 나온다. **중복 개수가 답에 영향을 주면 `set`이 아니라 `Counter`를 써야 한다.** 이 구분이 해시 문제의 가장 흔한 오답 원인이다.

<br>

---

## 10. 해시가 답이 아닐 때

해시는 만능이 아니다. 장점만 알고 쓰면 오히려 손해를 본다.

> **[그림]** 순서가 필요할 때 범위 탐색이 필요할 때 메모리가 빠듯할 때 해시 대신 써야 할 자료구조

| 상황 | 이유 | 대안 |
|---|---|---|
| **정렬된 순서가 필요하다** | 해시 테이블에는 크기 순서 개념이 없다 | `sorted()`, `heapq` |
| **범위로 검색해야 한다** | "10 이상 20 이하"를 해시로는 못 뽑는다 | 정렬 후 이분 탐색(`bisect`) |
| **최솟값·최댓값을 자주 꺼낸다** | 매번 전체 순회라 `O(N)` | `heapq` |
| **메모리가 빠듯하다** | 빈 칸을 남겨 두는 구조라 리스트보다 많이 쓴다 | 리스트, 배열 |
| **N이 아주 작다** | 해시 계산 비용이 상대적으로 크다 | 리스트로 충분 |

메모리 이야기를 조금 더 하면, 해시 테이블은 충돌을 줄이려고 **항상 실제 데이터보다 큰 배열을 유지한다.** 즉 속도를 위해 공간을 내주는 트레이드오프다. 원소 100만 개짜리 `set`은 같은 개수의 리스트보다 훨씬 많은 메모리를 쓴다. 메모리 제한이 빡빡한 문제에서는 이게 발목을 잡을 수 있다.

> 💡 **dict는 순서를 기억하지만 정렬하지는 않는다**
> Python 3.7부터 dict는 **삽입 순서**를 보장한다(3.6에서도 동작했지만 구현 세부사항이었고 3.7에서 언어 명세가 됐다). 다만 이건 "넣은 순서대로 나온다"는 뜻이지 "크기 순으로 정렬된다"는 뜻이 아니다. `set`은 순서 보장이 아예 없다.

<br>

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **왜 빠른가** | 찾는 게 아니라 키에서 위치를 계산한다. 그래서 N이 커져도 비용이 안 늘어난다 |
| **시간 복잡도** | 평균 `O(1)`, **최악 `O(N)`**. "무조건 1번"이 아니다 |
| **`O(1)`의 의미** | 연산 1번이 아니라 N에 무관하다는 뜻. 문자열 키는 길이 L만큼 계산이 든다 |
| **hashable** | 불변 객체만 키가 된다. `list`는 안 되고 `tuple`·`frozenset`은 된다 |
| **`in`의 함정** | 리스트 `in`은 `O(N)`, 집합·딕셔너리 `in`은 `O(1)`. 코드 모양은 똑같다 |
| **dict의 `in`** | 값이 아니라 **키**를 검사한다 |
| **`.get(k, d)`** | 키가 없어도 에러 없이 기본값 반환. `d[k]`만 `KeyError`를 낸다 |
| **Counter `-`** | 결과가 **0 이하면 항목이 사라진다.** 음수가 필요하면 `subtract()` |
| **set vs Counter** | 중복 개수가 답에 영향을 주면 `set`이 아니라 `Counter` |
| **defaultdict** | 조회만 해도 키가 생긴다. 확인만 할 땐 `in`이나 `.get()` |
| **안 쓸 때** | 정렬·범위 검색·최솟값 조회·메모리 제한 |

<br>

> 다음 글은 탐색 알고리즘이다. 방문 처리에 `set`과 튜플 키가 그대로 다시 등장한다 — [DFS와 BFS 완전정리](0825_DFS와BFS_이론.md) 편에서 이어진다.

<br>

## 🔗 참고 자료

- [파이썬 공식 문서 — 내장 자료형](https://docs.python.org/ko/3/library/stdtypes.html)
- [파이썬 공식 문서 — collections](https://docs.python.org/ko/3/library/collections.html)
- [파이썬 공식 문서 — hash()](https://docs.python.org/ko/3/library/functions.html#hash)
- [파이썬 시간 복잡도 정리 (Python Wiki)](https://wiki.python.org/moin/TimeComplexity)
