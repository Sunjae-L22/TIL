<!--
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
티스토리 발행 정보  (이 주석 블록은 화면에 보이지 않습니다)

제목    : [VLM] 생성·적응·합성 데이터 완전 정리 — Diffusion, PEFT, 그리고 데이터 공장이 된 모델
카테고리 : AI / 딥러닝
태그    : 파운데이션모델, Diffusion, StableDiffusion, LatentDiffusion, ControlNet, SAM, GroundingDINO, PEFT, LoRA, DreamBooth, 파인튜닝, 합성데이터, InstructPix2Pix, 지식증류, HuggingFace, SSAFY, TIL
요약    : 보는 모델(VLM) 다음은 만드는 모델과, 그걸 내 것으로 만드는 법이다.
          SAM·Grounding DINO를 파이프로 잇는 조립 패턴에서 시작해 Diffusion과 Latent Diffusion의 구조,
          같은 Stable Diffusion을 파인튜닝해 용도를 바꾸는 ControlNet·Marigold 계열,
          그리고 PEFT·DreamBooth로 모델을 개인화하고 파운데이션 모델 자체를 데이터 공장으로 쓰는 방법까지 한 줄로 꿰었다.

이미지 업로드 순서 (본문 등장 순)
 1. 01-assembly-pattern.png     2. 02-diffusion-vs-ldm.png
 3. 03-controlnet.png           4. 04-sd-fourway.png
 5. 05-nextgpt-anytoany.png     6. 06-prompt-vs-finetune.png
 7. 07-lr-curves.png            8. 08-peft-spectrum.png
 9. 09-dreambooth.png          10. 10-distillation.png
11. 11-data-factory.png        12. 12-serving-stack.png
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
-->

CLIP과 VLM까지 공부하고 나면 모델은 이미지를 보고 말할 수 있게 된다. 그런데 막상 그 모델을 내 일에 붙이려는 순간 벽이 세 번 나온다.

**첫째, 필요한 능력이 그 모델에 없다.** VLM은 "이 사진에 뭐가 있냐"에는 답하지만 "그 물체만 오려내라"는 못 한다. **둘째, 내 도메인에서 성능이 뚝 떨어진다.** 범용 모델을 위성사진 판독이나 우리 동네 관광 안내에 쓰면 생각보다 못 쓴다. **셋째, 그래서 학습을 시키려는데 데이터가 없다.** 의료 영상은 개인정보 때문에 못 모으고, 3D·모션 데이터는 장비 없이는 아예 취득이 안 된다.

이 글은 그 세 벽을 차례로 넘는 방법을 정리한 것이다. 첫 번째는 **모델을 새로 학습시키지 않고 파이프로 잇는 조립 패턴**으로, 두 번째는 **적응 학습(PEFT·DreamBooth)** 으로, 세 번째는 **파운데이션 모델을 데이터 공장으로 쓰는 발상**으로 푼다. 세 답이 각각 이 글의 세 덩어리다.

> 이 글은 [이미지 파운데이션 모델 완전 정리 — CLIP, LLaVA, 그리고 작아지는 VLM]에서 이어진다.

앞 글이 "어떻게 모델에게 눈을 달았는가"였다면, 이 글은 그 다음 이야기다. **보는 모델 다음은 만드는 모델이고, 만들 줄 알게 되면 데이터도 만들 수 있다.**

<br>

## 📖 이 글에서 다루는 내용

1. 조립 패턴 — 학습 없이 능력을 더한다 (SAM, Grounding DINO)
2. 생성 모델링과 Diffusion — 분포를 근사한다는 것
3. Latent Diffusion — 확산을 잠재 공간으로 옮기다
4. 파인튜닝으로 용도 바꾸기 — 생성 모델은 시각 사전지식이다
5. 3D·비디오·통합 모델 — 나머지 파운데이션 모델 지도
6. 적응 학습 — 프롬프팅이 닿지 못하는 곳
7. PEFT — 성능과 효율 사이에서 위치 고르기
8. DreamBooth — 사진 3~5장으로 만드는 개인 전용 모델
9. 합성 데이터 ① 지식 증류 — 선생님이 찍어주는 정답
10. 합성 데이터 ② InstructPix2Pix — 모델을 데이터 공장으로
11. 합성 데이터 ③ 시뮬레이션과, 공짜가 아닌 이유
12. What's next — 툴을 쓰는 모델과 AGI의 빈칸
13. 배포와 서빙 — 만든 것을 남이 쓰게 하기
14. 핵심 요약

<br>

---

## 1. 조립 패턴 — 학습 없이 능력을 더한다

VLM만 파운데이션 모델인 게 아니다. 컴퓨터 비전(CV)에도 방대한 데이터로 학습한 범용 모델들이 나왔고, 이들이 **분할(segmentation)·탐지(detection)·깊이 추정(depth)** 을 프롬프트 하나로 처리한다.

앞 글에서 다룬 **Set of Mark(SoM)** 를 기억하면 출발점이 잡힌다. 이미지 영역마다 번호를 찍어 주면 VLM이 못 하던 위치 지칭이 "번호 고르기"로 바뀌어 성능이 오르는 트릭이었다. 그때 넘어갔던 질문이 하나 있다. **그 번호는 누가 찍어 주는가?** 답이 이번 절의 모델들이다. SEEM, Semantic-SAM, SAM, MaskDINO 같은 분할 모델이 영역을 잘라 주고 그 위에 번호가 올라간다.

즉 SoM은 VLM 혼자 하는 재주가 아니라 **파운데이션 모델 두 개를 파이프로 잇는 조립 패턴의 첫 사례**였다. 이 절 전체가 그 패턴의 전개다.

### SAM — 무엇이든 분할한다

> **Segment Anything (SAM, 2023 / SAM2, 2024) — Meta**
> 클릭·박스·대략적 마스크·텍스트 같은 유저 입력(= segmentation prompt)을 받아, 원하는 영역의 마스크를 뽑아내는 범용 분할 모델.

- **약 1,100만 장의 이미지, 약 10억 개의 마스크**로 학습했다
- 컴퓨터 비전에서도 데이터를 충분히 밀어 넣으면 파운데이션 모델이 만들어진다는 것을 증명한 모델이다
- SAM2는 이를 **비디오와 이미지 양쪽**으로 확장한 버전이다

```text
segmentation prompt (점 / 박스 / 대략적 마스크 / "cat with black ears")
                         +
                      image
                         ↓
                     [ SAM ]
                         ↓
                    valid mask
```

여기서 눈여겨볼 것은 **프롬프트의 종류가 열려 있다**는 점이다. 클래스 목록을 미리 정하고 그 안에서만 고르는 기존 분할 모델과 달리, SAM은 "사용자가 가리킨 것"을 잘라 준다. 무엇을 자를지의 결정권이 모델 밖으로 나온 셈이다.

> 📌 **원본 강의자료와 다르게 쓴 부분**
> 슬라이드의 인용 표기가 `Kirillovet al.`로 붙어 있는데 **Kirillov et al.** 이 맞다. SAM 1저자는 Alexander Kirillov다.

### Grounding DINO — 텍스트로 찾는다

> **Grounding DINO (2023) — IDEA Research**
> 텍스트 입력으로 이미지 안의 물체를 탐지하는 모델. 미리 정해진 클래스 목록이 필요 없는 **오픈셋 탐지(open-set detection)** 다.

"판다"라고 넣으면 이미지 속 판다들에 박스가 쳐진다. 더 재미있는 건 **명사가 아닌 서술 표현도 먹는다**는 점이다. "달리는 소녀"라고 넣으면 여러 사람 중 달리는 인물만 잡는다. 클래스 이름이 아니라 문장을 이해해서 위치를 짚는 것이라, 탐지 분야에서도 파운데이션 모델이 통한다는 것을 보여준 사례가 됐다.

### Grounded SAM — 둘을 잇는다

두 모델을 나란히 놓고 보면 빈칸이 보인다. Grounding DINO는 **어디에 있는지(박스)** 는 알지만 픽셀 단위로 오려내지 못하고, SAM은 **오려내기는 잘하지만 무엇을 오려낼지** 스스로 정하지 못한다. 서로의 결핍이 서로의 출력이다.

![Grounding DINO가 텍스트로 물체 박스를 찾고 그 박스를 SAM의 박스 프롬프트로 넘겨 마스크를 만드는 Grounded SAM의 조립 구조와, 두 모델을 잇는 데 추가 학습이 전혀 필요 없다는 점을 보여주는 다이어그램](01-assembly-pattern.png)

```text
텍스트 입력 "말, 구름, 풀, 하늘, 언덕"
        ↓
[Grounding DINO] → 모든 물체를 탐지 (박스)
        ↓  박스를 그대로 프롬프트로 전달
[SAM] → 물체별 마스크 생성 (분할)
        ↓
텍스트 한 줄로 "탐지 + 분할"이 동시에 끝난다
```

**Grounded SAM(2024)** 이 하는 일이 정확히 이것이다. 새 모델을 학습시킨 게 아니라, 한쪽의 출력을 다른 쪽의 입력 규격에 맞춰 꽂았을 뿐이다. 그런데 결과물은 두 모델 어느 쪽도 혼자서는 못 하던 능력이다.

**SAMURAI(2024, Univ. Washington)** 도 같은 계열이다. SAM2에 **모션 인식 메모리(motion-aware memory)** 를 붙여 프레임 간 추적을 안정화했고, 2025년 5월 기준 비주얼 물체 트래킹 SOTA다. 비디오 편집, 물체 지우기, 이상행동 감지, CCTV 자동 분석, 스포츠 중계 같은 응용이 여기서 나온다.

> ➡️ **이 글 전체를 관통하는 첫 번째 주제.** 파운데이션 모델의 진짜 활용은 "하나를 잘 고르는 것"이 아니라 **"여러 개를 어떻게 이어 붙이는가"** 다. 이어 붙이는 데 GPU도 라벨도 들지 않는다. 뒤에 나올 InstructPix2Pix(10절)와 VISPROG(12절)가 이 발상을 극단까지 밀어붙인다.

<br>

---

## 2. 생성 모델링과 Diffusion — 분포를 근사한다는 것

이제 "보는 모델"에서 "만드는 모델"로 넘어간다. 이미지 생성 모델은 대규모 이미지로 학습되어 **텍스트 설명을 토대로 새로운 이미지를 생성**한다. 그런데 입력이 텍스트만은 아니라는 점이 응용의 출발점이다. 포즈를 주면 그 포즈의 사람을 그리고, 스케치를 주면 그 구도로 그린다.

생성 모델이 실제로 쓰이는 자리를 먼저 정리하면 이렇다.

- 실제 데이터와 유사한 이미지 생성
- **학습 데이터 생성 / 데이터 증강** ← 이 글 후반부(9~11절)의 주제다
- 이미지 화질 개선 (super-resolution)
- 이미지 편집, 스타일 변경

### 생성 모델링의 목표 — 모델 갭

원리를 한 번은 짚고 가야 나머지가 풀린다. 우리에게 있는 것은 데이터셋, 즉 **어떤 미지의 분포에서 뽑힌 샘플들**뿐이다.

```text
데이터셋   :  {x}         샘플들
                xᵢ ~ p_data(x)      미지의 실제 데이터 분포에서 뽑혔다

생성 모델  :  {x}로부터 p_data(x)를 알아내는 과정
              p_data(x)를 안다  =  x를 만들어내는 과정을 안다
                                =  새로운 x_new를 샘플링할 수 있다
```

문제는 `p_data(x)`가 어떻게 생겼는지 아무도 모른다는 것이다. 강의 표현대로 "신만 알고 있다". 그래서 **주어진 샘플로부터 근사 모델 `p_model(x)`을 학습해 `p_data(x) ≈ p_model(x)`를 만드는 것**이 생성 모델링의 목표가 된다.

여기서 피할 수 없는 한계가 나온다.

```text
        p_data  ●
                 ↘  ← 모델 갭(model gap)
                  ● p_model
                                [ 표현 가능한 모델 집합 M ]
                                   p_model ∈ M
```

내가 고른 모델 구조 `M` 안에서는 `p_data`에 정확히 도달할 수 없다. 이 거리가 **모델 갭**이고, 갭이 크면 샘플 품질이 떨어진다. 강의 슬라이드의 예시 이미지가 "테니스공인지 강아지인지 헷갈리는" 결과물인 이유가 이것이다. 그래서 남는 질문은 딱 두 개다. **"`p_model`의 구조를 어떻게 짤 것인가, 어떻게 학습시킬 것인가."**

### Diffusion (DDPM) — 되돌아오는 길을 배운다

그 두 질문에 대한 현재의 답이 **Denoising Diffusion Probabilistic Models(DDPM)**, 흔히 말하는 Diffusion 모델이다.

```text
forward  : [고양이 사진] → 노이즈 조금 → 더 → 더 → [가우시안 노이즈]
           ───────────── 노이즈를 더해가는 과정 ─────────────→

backward : [고양이 사진] ← 노이즈 제거 ← 제거 ← 제거 ← [가우시안 노이즈]
           ←──────────── 노이즈를 제거해가는 과정 ─────────────
```

- **메커니즘** — 노이즈로부터 점진적으로 노이즈를 제거하며 이미지를 구체화한다
- **학습법** — 합성 노이즈를 입히고 **그 노이즈가 무엇이었는지 맞히는 문제**를 반복해서 풀린다
- **단점** — 느리다. 수십에서 수천 스텝을 반복해야 한 장이 나온다

발상 자체가 우아하다. 복잡하고 모르는 분포 `p_data`를 직접 모델링하는 대신, **우리가 완벽히 아는 간단한 분포(정규분포)에서 출발해 되돌아오는 경로를 배운다.** 앞의 모델 갭 그림으로 말하면, 목표 지점을 직접 겨냥하지 않고 출발 지점을 아는 곳으로 옮긴 것이다.

> 💡 **왜 하필 노이즈 예측인가**
> "이미지를 그려라"는 정답이 하나가 아니라 학습 신호를 주기 어렵다. 반면 "내가 방금 더한 노이즈를 맞혀라"는 정답이 딱 하나 있고, 그 정답을 공짜로 무한히 만들어낼 수 있다. **정답 생성이 공짜인 문제로 바꿔치기한 것**이 Diffusion 학습의 핵심 트릭이다.

<br>

---

## 3. Latent Diffusion — 확산을 잠재 공간으로 옮기다

DDPM의 단점은 느리다는 것이었고, 그 원인은 명확하다. **512×512 픽셀 공간에서 수백 번 노이즈를 제거하기 때문**이다. 픽셀 하나하나가 다 연산 대상이다.

**Latent Diffusion Model(LDM)** 의 해법은 한 문장이다. **고차원 픽셀 대신 저차원 잠재(latent) 임베딩으로 압축한 뒤, 거기서 확산을 돌린다.** 2022년 **Stable Diffusion**이 이 구조로 나왔고, 연산 효율만 좋아진 게 아니라 생성 품질까지 올라갔다.

![위쪽에는 픽셀 공간에서 직접 노이즈를 제거하는 DDPM을, 아래쪽에는 인코더로 잠재 공간에 들어가 확산을 돌리고 디코더로 되돌아오는 Latent Diffusion을 대비시키고 텍스트 조건이 크로스 어텐션의 키와 값으로 주입되는 경로를 표시한 다이어그램](02-diffusion-vs-ldm.png)

```text
[LDM 구조]
 Pixel Space              Latent Space                    Conditioning
   x → E ─────────────→ z ── Diffusion Process ──→ z_T      Text
                          ↑                        │        Semantic Map
   x̃ ← D ←──────────── z ←┴─ Denoising U-Net ε_θ ←─┘        Images
                            (cross-attention의 K, V)          ↓
                            z_T → z_{T-1} → … → z            τ_θ ──→ K, V
```

정리하면 세 가지다.

| 요소 | 하는 일 |
|---|---|
| **인코더 E / 디코더 D** | 픽셀 공간 ↔ 잠재 공간을 오간다. 확산은 오직 잠재 공간에서만 돈다 |
| **U-Net ε_θ** | 잠재 표현에서 제거할 노이즈를 예측한다. 이미지가 아니라 노이즈를 뱉는다 |
| **조건 인코더 τ_θ** | 텍스트·세그멘테이션 맵·이미지 등의 조건을 인코딩해 **cross-attention의 K, V로 주입**한다 |

마지막 줄이 중요하다. 어텐션의 Q는 이미지 잠재 쪽에서 오고 K, V는 조건 쪽에서 온다. 즉 **"지금 그리고 있는 이 부분이 프롬프트의 어느 단어와 관련 있는가"** 를 매 스텝 계산하면서 그린다. 어텐션과 Q/K/V의 계산 구조 자체는 [RNN부터 ViT까지 완전 정리] 글에서 다뤘으니, 여기서는 **조건이 들어오는 문이 cross-attention이라는 것**만 기억하면 된다.

### 생성 과정을 2단계로 열어 보기

LDM으로 이미지 한 장이 나오는 과정은 크게 두 단계로 나뉜다.

| 단계 | 이름 | 하는 일 |
|---|---|---|
| 1단계 | 텍스트 특징 생성 | 프롬프트를 함축하는 임베딩을 만든다 |
| 2단계 | 이미지 특징 개선 | 그 텍스트 특징을 바탕으로 이미지 표현을 점진적으로 다듬는다 |

```text
프롬프트 "a cute and adorable bunny, with huge clear eyes,
          holding a bunch of flowers, in the style of cute pixar character"
        ↓
[1단계] Text Representation Generator
        ↓  (Guidance scale 7)
[2단계] Image Representation Refiner ←── Random noise at timestep 0 (Seed 1)
        ↓
   Refined Representation ──(Upscale)──→ 최종 이미지
```

2단계 안에서 한 스텝만 열어 보면 이렇게 생겼다.

```text
Representation of timestep 49
        ↓
     [ UNet ]  ── 제거할 노이즈를 예측 ──→ Noise
        ↓                                  ↓ (weaken)
        └──────────── ⊖ 빼기 ─────────────┘
                      ↓
        Refined Representation (timestep 50)
```

UNet은 **이미지가 아니라 제거할 노이즈를 예측**하고, 그 노이즈를 현재 표현에서 빼서 다음 표현을 만든다. 이 뺄셈이 수십 번 반복되면 한 장이 나온다.

> ⚠️ **Seed와 Guidance scale은 완전히 다른 것이다**
> **Seed**는 timestep 0의 랜덤 노이즈를 결정한다 — 출발점이 어디냐를 정한다. **Guidance scale**은 이미지 표현이 텍스트 프롬프트를 얼마나 강하게 따를지를 정한다 — 값이 크면 프롬프트를 충실히 따르지만 **과장된다(can exaggerate)**. 실습에서 "프롬프트가 안 먹힌다"며 시드를 흔드는 실수가 흔한데, 그건 guidance scale이 담당하는 일이다.

### 지금 실제로 쓰는 생성 모델들

모델 카탈로그는 반년이면 낡으므로 한 표로 압축한다.

| 모델 | 공개 | 성격 | 특징 |
|---|---|---|---|
| **Midjourney v7** | 2025.04 알파 | 폐쇄형 | 세부 묘사와 프롬프트 해석 정확도, **손과 신체 표현의 일관성**. 긴 프롬프트 이해, 텍스트 포함 이미지 |
| **Stable Diffusion 3 / 3.5** | 2024.02 / 10 | 오픈 가중치 | **Diffusion Transformer** 도입. 다중 객체 프롬프트 처리와 글자 표현 강화. 800M~8B 크기 선택 가능 |
| **FLUX.1** | 2024.08 | 일부 오픈 | **120억(12B) 파라미터 Rectified Flow Transformer**. 프롬프트 준수와 복잡한 장면, 특히 이미지 안 글자 표현이 탁월 |

FLUX.1은 라이선스가 셋으로 갈린다.

| 변형 | 성격 | 라이선스 |
|---|---|---|
| Pro | 최고 성능 | API 유료 |
| Dev | 오픈 가중치 | 연구용(비상업) |
| Schnell | 경량·고속 | 오픈소스, 상업용 허용 |

> 📌 **원본 강의자료와 다르게 쓴 부분**
> 슬라이드에는 FLUX.1이 "**12억** 파라미터"로 적혀 있으나 실제로는 **12B = 120억 파라미터**다. 영어 "12B"를 옮기는 과정에서 자릿수가 어긋난 것으로 보인다. 12억과 120억은 하드웨어 요구사항이 완전히 다르므로 그냥 넘길 오타가 아니다.

<br>

---

## 4. 파인튜닝으로 용도 바꾸기 — 생성 모델은 시각 사전지식이다

여기서 관점이 한 번 뒤집힌다. 지금까지 Stable Diffusion은 "그림 그려주는 도구"였다. 그런데 이 모델은 수억 장의 이미지를 보면서 **빛이 어떻게 떨어지고, 물체가 어떻게 가려지고, 원근이 어떻게 생기는지**를 이미 배웠다. 그 지식은 그림 그리기 말고 다른 데도 쓸 수 있다.

이 절의 네 모델은 전부 같은 발상이다. **잘 학습된 diffusion 모델을 파인튜닝해서 원래 목적이 아닌 다른 일을 시킨다.** 생성 모델을 결과물이 아니라 **시각 세계의 사전지식(visual prior)** 으로 쓰는 것이다.

### ControlNet — 구조는 조건이, 내용은 텍스트가

텍스트만으로는 구도를 제어할 수 없다. "왼쪽에 서서 오른팔을 든 사람"이라고 아무리 써도 원하는 포즈가 나오지 않는다. **ControlNet(2023)** 은 텍스트와 별개로 **구조를 지정하는 조건 신호**를 하나 더 받는다.

![원본 UNet 블록은 얼려 두고 복사한 블록에만 조건 신호를 넣어 학습시키며 양쪽 끝의 제로 컨볼루션으로 원본 경로에 합류시키는 ControlNet의 구조와 지원하는 여덟 가지 조건 신호를 함께 보여주는 다이어그램](03-controlnet.png)

지원하는 조건 신호는 논문 기준 8종이다.

| 조건 신호 | 고정하는 것 |
|---|---|
| Sketch (손그림) | 대략적 구도 |
| Canny edge / HED edge | 윤곽선 |
| M-LSD line | 직선 구조 (건축물에 강함) |
| Normal map | 표면 법선 방향 |
| Depth map | 앞뒤 거리 관계 |
| ADE20k seg. | 영역별 의미 (하늘·건물·도로) |
| Human pose | 사람 골격 |

```text
[조건 신호] + [텍스트 프롬프트] → ControlNet + Diffusion → [생성 샘플]
```

핵심은 **역할 분리**다. 같은 얼굴 실루엣 엣지에 "a high-quality and extremely detailed image"만 붙여도 궁전 복도, 황금 트로피, 석상, 나무 기둥까지 전혀 다른 해석이 나온다. 같은 집 스케치에 "house"를 붙이고 베이스 모델만 SD 1.5 / Comic Diffusion / Protogen 3.4로 바꾸면 화풍이 통째로 바뀐다.

> ➡️ **구조(윤곽·포즈·깊이)는 조건 신호가 고정하고, 내용과 스타일은 텍스트와 베이스 모델이 결정한다.** ControlNet을 한 문장으로 기억한다면 이것이다.

구조적으로는 **원본 UNet은 얼려 두고 그 복사본에만 조건을 넣어 학습시킨 뒤, 앞뒤를 0으로 초기화한 컨볼루션(zero convolution)으로 원본 경로에 합류**시킨다. 학습 초기에 zero conv의 출력이 0이므로 원본 모델의 능력을 전혀 훼손하지 않은 채로 출발한다는 점이 설계의 묘수다. 원본을 건드리지 않고 옆에 얇은 것을 붙여 새 능력을 얻는 이 방식은 7절의 Adaptor·LoRA와 정확히 같은 사고방식이다.

> 📌 **원본 강의자료와 다르게 쓴 부분**
> ControlNet 논문의 학회 표기가 자료 안에서 엇갈린다. 앞쪽 참고문헌 슬라이드에는 **CVPR 2023**, ControlNet 본문 슬라이드에는 **ICCV 2023**으로 적혀 있다. 실제 게재는 **ICCV 2023**이다.

### Zero123XL — 한 장으로 다른 각도를 만든다

> **Zero123XL (컬럼비아 대학교, 2023) — 2D에서 3D로**
> 2D 이미지 한 장을 받아, 그 물체를 **다른 카메라 위치에서 봤을 때의 모습**을 생성한다.

```text
Input (신발 사진 1장) + 카메라 변화량 (Down 30°, Left 90°)
        ↓
   [ Zero123XL ]
        ↓
Synthesized (그 각도에서 본 신발)  →  여러 각도를 모으면 3D 메시 복원
```

각도를 바꿔가며 여러 장을 뽑아 모으면 물체의 3D 전체 형상까지 재현된다. 3D 모델링, VR·AR 콘텐츠 제작이 응용처다. 여기서 놀라운 지점은 **사진에 안 찍힌 뒷면을 모델이 "그럴듯하게 지어낸다"** 는 것이다. 그게 가능한 이유가 바로 diffusion 모델이 학습해둔 시각 사전지식이다.

> 📌 **원본 강의자료와 다르게 쓴 부분**
> 슬라이드에는 "**콜롬비아** 대학"으로 적혀 있으나, Zero-1-to-3 논문의 소속은 **컬럼비아 대학교(Columbia University)** 다. "콜롬비아"는 남미 국가명이다.

### Marigold — 사진 한 장에서 깊이를 잰다

> **Marigold (2024)**
> 이미지 생성 diffusion 모델을 **합성 데이터에 파인튜닝**해 **단안 깊이 추정(monocular depth estimation)** 을 시킨다.

사진 한 장 → 깊이 맵 → 포인트 클라우드 → 3D 장면 복원까지 이어진다. 인상적인 건 **실사진뿐 아니라 회화에서도 그럴듯한 깊이가 나온다**는 점이다. 미켈란젤로의 피에타나 고전 회화를 넣어도 앞뒤 관계를 잡아낸다. 생성 모델이 학습한 시각 사전지식이 학습 도메인을 넘어 작동한다는 직접적인 증거다.

그리고 여기서 **합성 데이터가 왜 필요한지**가 처음 드러난다. 실제 사진에 정확한 픽셀별 깊이 정답을 라벨링하는 건 사실상 불가능하다. 그런데 렌더링한 합성 장면은 **깊이 정답이 공짜로 딸려 나온다.** 9~11절의 주제가 여기서 예고된다.

### JointDiT — RGB와 깊이를 함께 만든다

> **JointDiT (Microsoft, POSTECH, 2025.05)**
> 이미지와 3D 깊이 맵을 **하나의 확산 과정에서 동시에 모델링**한다.

Marigold가 "이미지 → 깊이"의 단방향이라면 JointDiT는 양쪽을 함께 다룬다. 그래서 한 모델에서 세 방향이 나온다 — **① 이미지와 깊이를 동시 생성 ② 이미지를 주면 깊이 추정 ③ 깊이를 주면 이미지 생성.** 물리적으로 더 그럴듯한 장면이 나오는 것도 이 덕분이다.

### 네 갈래를 한 장으로

![같은 Stable Diffusion을 출발점으로 삼아 ControlNet은 조건 제어, Zero123XL은 시점 변경, Marigold는 깊이 추정, JointDiT는 이미지와 깊이의 동시 생성으로 각각 용도를 바꾸는 네 갈래 파인튜닝 지도 다이어그램](04-sd-fourway.png)

| 모델 | 무엇을 바꿨나 | 새로 얻은 능력 |
|---|---|---|
| **ControlNet** | 조건 입력 경로를 추가 | 구도·포즈·깊이를 지정한 생성 |
| **Zero123XL** | 카메라 변화량을 조건으로 | 시점 변경, 단일 이미지 3D 복원 |
| **Marigold** | 출력을 깊이 맵으로 | 단안 깊이 추정 |
| **JointDiT** | RGB와 Depth를 함께 확산 | 양방향 생성·추정 |

> 💡 **면접·시험에서 자주 갈리는 구분**
> **ControlNet은 "무엇을 조건으로 받을지"를 바꾸는 기법**이고, 뒤에 나올 **DreamBooth는 "무엇을 그릴 수 있는지"를 바꾸는 기법**이다. 전자는 구조 제어, 후자는 피사체 개인화다. 둘 다 Stable Diffusion을 손대지만 목적이 완전히 다르다.

<br>

---

## 5. 3D·비디오·통합 모델 — 나머지 파운데이션 모델 지도

여기서부터는 카탈로그다. 개별 스펙보다 **각 계열이 어떤 결핍을 메우려고 나왔는지**만 잡으면 충분하다.

### 3D — 무엇이든 깊이를 잰다

SAM이 "무엇이든 분할한다"였다면, 이 계열은 "무엇이든 깊이를 재고 자세를 잡는다"다.

| 모델 | 소속/연도 | 하는 일 | 눈여겨볼 점 |
|---|---|---|---|
| **Depth Anything v2** | HKU, TikTok / 2024 | 범용 깊이맵 예측 | 라벨 데이터 150만 장 + **라벨 없는 데이터 6,200만 장** |
| **Sapiens** | Meta / 2024 | 사람 중심 태스크 4종 | 3,000만 장 학습. 하나의 백본으로 자세·부위분할·깊이·법선 동시 출력 |
| **MegaSaM** | Google DeepMind / 2024.12 | 단안 동영상에서 카메라 포즈와 깊이 추정 | 카메라도 피사체도 움직이는 동적 장면 |
| **CUT3R** | UC Berkeley, DeepMind / 2025.01 | 통합 3D 표현 복원 | **찍히지 않은 각도까지 추론**, 순서 없는 사진 모음도 처리 |

> ⚠️ **Depth Anything v2의 숫자를 더하면 안 된다**
> 150만 개는 **라벨된** 데이터, 6,200만 개는 **라벨 없는** 데이터다. 둘을 합쳐 "6,350만 장을 지도학습했다"고 읽으면 틀린다. 라벨 없는 대량 데이터를 **의사 라벨(pseudo-label)** 로 끌어들여 스케일을 키운 것이 이 모델의 핵심이고, 바로 9절의 지식 증류와 같은 계열의 발상이다.

Sapiens가 처리하는 사람 중심 태스크 4종은 이렇다.

| 태스크 | 하는 일 |
|---|---|
| 2D Pose Estimation | 관절 위치 예측 |
| Body-part Segmentation | 신체 부위 구분 |
| Depth Estimation | 카메라와의 거리 예측 |
| Surface Normal Prediction | 3D 모델링용 표면 법선 방향 예측 |

### 비디오 — 시간 축이 붙는다

| 모델 | 성격 | 특징 |
|---|---|---|
| **Sora** (OpenAI, 2024.12) | 폐쇄형 | 최대 **1080p·20초**. ChatGPT와 통합된 대화형 편집. 물리 이해를 보여 **월드 모델** 가능성 제기 |
| **Veo 3** (Google) | 폐쇄형 | 영상과 **싱크된 소리를 함께 생성** — 기존 비디오 생성 모델은 전부 무음이었다 |
| **Modify Video** (Luma Labs) | 폐쇄형 | 실사 영상의 인물·배경·스타일을 통째로 교체하는 **편집** 모델 |
| **Wan 2.2** (2025.07) | 오픈소스 | **X-to-Video** — text/image/sound 무엇이든 입력. 1280×720, 24 FPS |

**월드 모델(world model)** 은 현실 세계의 규칙(물리·환경)을 이해하고 시뮬레이션할 수 있는 모델을 말한다. 앞으로 일어날 일을 예측하고 계획하는 데 쓴다. Sora의 기술 보고서 제목이 아예 *Video generation models as world simulators* 인 것이 이 주장의 근거다.

> 💡 **실무에서 중요한 건 단일 모델 성능이 아니라 스택 조합이다**
> HeyGen의 Avatar IV가 좋은 예다. `{텍스트 스크립트, 목소리 샘플, 사진 한 장}` → `{말하는 영상}`인데, 내부는 `NotebookLM으로 스크립트·음성 생성 → Avatar IV로 영상 생성`이라는 조합이다. 코카콜라 광고가 이미 이런 방식으로 제작되는 단계에 왔다. 1절의 조립 패턴이 서비스 레벨에서 반복된다.

### 통합 — 무엇이든 넣고 무엇이든 뽑는다

대규모 언어모델에 **영상과 소리** 입력이 붙으면서 멀티모달 언어모델로 확장되고 있다. 비디오를 넣는 방식은 크게 둘이다.

| 방식 | 대표 모델 | 설명 |
|---|---|---|
| ImageBind 기반 | OneLLM (2024) | 여러 모달리티를 하나의 공통 임베딩 공간에 정렬해 넣는다 |
| 프레임 단위 | VideoLLaMA2 (2024) | 비디오를 프레임으로 쪼개 이미지 시퀀스로 처리한다 |

이미지와 오디오를 함께 주고 "주어진 오디오에 따라 이미지에서 소리 나는 곳을 찾아 설명하라"고 하면, 모델이 `① hair dryer ② woman ③ hand ④ cat`처럼 **소리의 근원을 이미지 영역에 대응(grounding)** 시켜 답한다.

그리고 이 계열의 끝에 **NExT-GPT(2023)** 가 있다. 임의의 모달리티를 받아 임의의 모달리티로 내보내는 **any-to-any 통합 파운데이션 모델**이다.

![텍스트 이미지 오디오 비디오 인코더와 LLM과 각 모달리티의 디퓨전 모델은 모두 얼려두고 입력 프로젝션과 출력 프로젝션 층만 학습시켜 any-to-any 능력을 얻는 NExT-GPT의 파이프라인 다이어그램](05-nextgpt-anytoany.png)

```text
[입력측]
  Text  ──────────────────────────────────────────┐
  Image → Image Encoder(고정) → Image Input Proj. ─┤
  Audio → Audio Encoder(고정) → Audio Input Proj. ─┼→ [ LLM (고정) ]
  Video → Video Encoder(고정) → Video Input Proj. ─┘

[출력측]
                     ┌────────────────────────────────────→ Text
                     ├→ Image Output Proj. → Image Diffusion(고정) → Image
  [ LLM (고정) ] ────┼→ Audio Output Proj. → Audio Diffusion(고정) → Audio
                     └→ Video Output Proj. → Video Diffusion(고정) → Video
```

다섯 구간으로 나뉜다.

1. **Multimodal Input Encoding** — 모달리티별 인코더로 특징 추출
2. **LLM-centric Alignment** — 입력 프로젝션으로 LLM이 알아듣는 공간에 정렬
3. **LLM-based Semantic Understanding** — LLM이 의미를 이해하고 추론
4. **Instruction-following Alignment** — 출력 프로젝션으로 생성 모델이 알아듣게 정렬
5. **Multimodal Output Generation** — 모달리티별 diffusion 모델이 결과물 생성

> ➡️ **인코더도, LLM도, diffusion도 전부 얼어 있다. 학습되는 것은 입출력 프로젝션 층뿐이다.**
> 앞 글에서 본 LLaVA의 구조(인코더 + 프로젝션 + LLM)를 양쪽으로 확장한 것이고, "무엇을 얼리고 무엇을 학습시키는가"라는 질문의 답이 여기서는 **"거의 다 얼리고 얇은 어댑터만"** 이다. 이 발상이 다음 절 PEFT의 본론이다.

강사가 남긴 조언 하나 — 프론티어 모델은 계속 갱신되므로 **본인만의 모델 리스트를 만들어 두는 것이 경쟁력**이 된다. 강의가 제시한 출발점 목록은 이렇다.

| 용도 | 모델 |
|---|---|
| Image | DINOv3 |
| Image & Text | CLIP, BLIP, GLIPv2 |
| Language-to-policy | RT1, RT2 |
| Speech recognition | wav2vec, Whisper |
| Audio & Text | CLAP |
| Multi-modal embedding | ImageBind |
| Multi-modal LLM | InternVL, Qwen3-VL |
| Object 3D | Zero123XL |
| Text & Satellite | RemoteCLIP |

<br>

---

## 6. 적응 학습 — 프롬프팅이 닿지 못하는 곳

여기까지가 첫 번째 덩어리다. 이제 두 번째 질문으로 넘어간다. **이 좋은 모델들을 왜 그대로 못 쓰는가?**

강의가 던지는 현실 문제는 둘이다.

1. **범용 모델은 내 도메인에서 성능이 떨어진다.** 이미지 파운데이션 모델을 "우리 동네 관광 가이드"로 쓰면 아는 게 없고, 위성사진 판독에 쓰면 세밀한 구조를 구분하지 못한다. 그렇다고 대규모 재학습을 해야 하는가?
2. **애초에 데이터가 없다.** 의료 영상은 개인정보 문제로 확보가 어렵고, 모션·3D 데이터는 고가 장비 없이 취득이 불가능하다.

이 두 질문의 답이 각각 **적응 학습(adaptation)** 과 **합성 데이터(synthetic data)** 다. 6~8절이 앞의 답, 9~11절이 뒤의 답이다.

### 미세조정이 프롬프팅보다 나은 네 가지

파운데이션 모델은 방대한 데이터로 학습돼 범용 문제에 바로 쓸 수 있지만, **최신 정보나 특정 도메인에는 최적화돼 있지 않아 즉시 활용이 어려운 경우가 반드시 있다.** 그때 쓰는 것이 미세조정(fine-tuning) — 이미 학습된 모델을 추가 학습으로 조금만 튜닝하는 것이다.

```text
[Pretraining]  Large data → 학습 → Base Model
[Fine-tuning]  Small data → Base Model에 추가 학습 → Fine-tuned Model

파운데이션 모델 + Fine-tuning = 실용적인 개인화 파운데이션 모델
```

프롬프팅 대신 미세조정을 택하는 이유는 네 가지다.

1. 프롬프팅보다 **결과물 품질이 좋다**
2. 프롬프트에 넣는 예제보다 **훨씬 많은 예제로 학습**할 수 있다
3. 프롬프트 길이가 줄어 **토큰이 절약된다**
4. 응답 **지연시간(latency)이 짧아진다**

3번과 4번은 같은 말처럼 보이지만 비용 구조가 다르다. 실제로 얼마나 줄어드는지 같은 번역 작업으로 비교하면 선명하다.

![프롬프팅 방식에서는 태스크 설명과 세 개의 예시와 질문까지 다섯 줄이 매 호출마다 들어가지만 미세조정 후에는 태스크 설명과 질문 두 줄만 남아 예시 세 줄만큼의 토큰과 지연시간이 절약됨을 대비한 다이어그램](06-prompt-vs-finetune.png)

**(a) 프롬프팅 — 사전학습 모델은 고정**

```text
1  한글을 영어로 번역해주세요     ← 태스크 설명
2  강아지 ⇒ dog                 ┐
3  고양이 ⇒ cat                 ├ 예시 (few-shot)
4  토끼   ⇒ rabbit              ┘
5  소     ⇒ ?                   ← 실제 질문
   → [사전학습 모델 (고정)] → "소 ⇒ cow"
```

**(b) 미세조정 — 모델 자체를 학습**

```text
1  한글을 영어로 번역해주세요     ← 태스크 설명
2  소 ⇒ ?                       ← 실제 질문
   → [미세조정된 모델] → "소는 영어로 "Cow" 입니다."
```

**예시 3줄이 통째로 사라졌다.** 매 호출마다 그만큼의 입력 토큰과 지연시간을 아낀다. 학습 시점에 이미 프롬프트에 넣을 수 있는 것보다 훨씬 많은 예제를 흡수했기 때문에 가능한 일이다. 호출이 하루 백만 번이면 이 3줄이 곧 인프라 비용이다.

프롬프트를 어떻게 구성하는가(역할 지정, 예시 제시, 출력 형식 명시)와 RLHF 같은 정렬 학습은 [텍스트 파운데이션 모델 정리 글]에서 다뤘다. 이 글은 그 프롬프트로도 안 되는 지점에서 출발한다.

### 미세조정의 학습률 — 크게 잡으면 안 된다

미세조정도 결국 경사하강법이다. 손실함수의 기울기(gradient)를 구해 **작은 비율만큼** 모델에 반영하고, 그 비율이 **학습률(learning rate)** 이다. 적절한 값은 모델과 데이터마다 달라 실험으로 찾는다. 강의가 제시한 실전 값은 **5e-3(0.005배)**, **3e-4(0.0003배)** 정도다.

![학습률이 아주 높으면 손실이 발산하고 높으면 높은 값에서 정체되며 적절하면 낮은 값으로 수렴하고 낮으면 에폭 안에 수렴하지 못하는 네 가지 손실 곡선 패턴과, 지형도 위에서 너무 낮은 학습률은 지역 최솟값에 갇히고 너무 높은 학습률은 전역 최솟값을 뛰어넘는 모습을 함께 보여주는 다이어그램](07-lr-curves.png)

| 상황 | 손실 곡선의 증상 | 참고 값 |
|---|---|---|
| very high | 위로 치솟으며 발산한다 | 1e-1 |
| high | 빠르게 떨어지다 높은 값에서 정체된다 | — |
| **good** | 빠르게 떨어져 낮은 값으로 수렴한다 | 5e-3, 3e-4 |
| low | 아주 천천히 떨어져 에폭 안에 수렴하지 못한다 | 1e-8 |

지형도로 보면 원인이 더 분명하다. 너무 낮으면 `출발 → 첫 골짜기(지역 최솟값)에서 정지`이고, 너무 높으면 `출발 → 전역 최솟값을 뛰어넘어 반대편 언덕에 착지`다.

> ⚠️ **미세조정에서는 학습률을 보수적으로, 작게 시작한다**
> 이게 사전학습과 결정적으로 다른 지점이다. 사전학습은 아무데서나 출발하지만 **미세조정은 이미 좋은 지점에 서 있다.** 여기서 큰 학습률을 쓰면 그 좋은 지점을 걷어차 버린다. 강의는 이걸 "앉아!"라고 가르쳤더니 큰 학습률 때문에 완전히 다른 개가 되어버린 그림으로 비유한다. 학습률 스케줄과 곡선 진단 전반은 [RNN부터 ViT까지 완전 정리] 글에 정리해 뒀다.

<br>

---

## 7. PEFT — 성능과 효율 사이에서 위치 고르기

미세조정이 좋다는 건 알겠는데, 문제는 **모델이 너무 커졌다**는 것이다.

| 모델 | 연도 | 파라미터 수 |
|---|---|---|
| AlexNet | 2012 | 60M |
| VGG-19 | 2014 | 144M |
| AmoebaNet-A | 2018 | 469M |
| ResNeXt-101 | 2018 | 829M |
| BiT-M | 2019 | 928M |
| ViT-G/14 | 2021 | 1,843M |
| ViT-MoE-15B | 2021 | 14,700M |

9년 사이 245배다. 오픈소스 파운데이션 모델을 출발점으로 삼는 접근이 일반화됐지만, **전체 파라미터를 미세조정하는 것은 여전히 감당하기 어려운 비용**이다. 태스크가 열 개면 모델 사본도 열 개다.

> 📌 **원본 강의자료와 다르게 쓴 부분**
> 그래프의 마지막 막대 레이블이 `ViT-MoE-15E`로 되어 있으나 `ViT-MoE-15B`(15 Billion)가 맞다. 같은 막대의 값 14,700M ≈ 14.7B가 이를 뒷받침한다.

그래서 나온 것이 **PEFT(Parameter-Efficient Fine-Tuning)**, 즉 **파라미터를 조금만 건드리는 미세조정**이다. 크게 두 갈래로 갈린다 — **프롬프트 튜닝**과 **Adaptor 모듈 추가 학습(대표가 LoRA)**.

```text
프롬프트 → [사전학습 파운데이션 모델 (고정)] → 예측 결과
                      ↑
              LoRA (추가 파라미터만 학습)
```

### 세 방식의 위치

![모델 전체 튜닝은 성능이 높고 효율이 낮으며 프롬프트 디자인은 효율이 높고 성능이 낮은 양 극단에 놓이고 프롬프트 튜닝과 Adaptor가 그 중간에서 양쪽을 취하는 위치임을 성능과 효율 두 축 위에 배치해 보여주는 PEFT 스펙트럼 다이어그램](08-peft-spectrum.png)

| | 모델 튜닝 (미세조정) | **프롬프트 튜닝** | 프롬프트 디자인 |
|---|---|---|---|
| 파운데이션 모델 | 추가 학습 (전체) | 고정 | 고정 |
| 입력 | 입력 텍스트 | 학습 가능한 프롬프트 + 입력 텍스트 | 사람이 만든 프롬프트 + 입력 텍스트 |
| 위치 | 성능 최고 · 효율 최저 | 양쪽을 취함 | 효율 최고 · 성능 최저 |

**프롬프트 디자인(prompt design)** 은 모델이 원하는 결과를 내도록 **입력 텍스트를 변형**하는 방법이다. 추가 학습이 전혀 없다는 게 최대 장점이고, 사람이 직접 설계해야 하며 성능 향상이 제한적이라는 게 단점이다. Text-to-Image에서도 똑같이 작동한다.

| 프롬프트 | 결과 |
|---|---|
| "강아지 사진을 생성해줘" | 평범한 강아지 사진 |
| "**캐논 EOS-1D로 찍은 듯한** 강아지 사진을 생성해줘" | 화질·조명·심도가 개선된 사진 |

같은 모델인데 입력 문장만 바꿔 품질이 달라진다.

**프롬프트 튜닝(prompt tuning, 2021)** 은 여기서 한 걸음 나간다. **학습 가능한 가상 토큰(virtual token)** 을 입력에 붙이고, 역전파로 **오직 그 가상 토큰의 임베딩만 학습**한다. 모델은 그대로 얼려 둔다.

```text
[사전학습 파운데이션 모델 (고정)]
        ↑                 ↑
 학습 가능한 프롬프트      입력 텍스트
   (가상 토큰 임베딩)
```

| 장점 | 단점 |
|---|---|
| 사람의 설계 없이 **스스로 프롬프트를 학습**한다 | **학습된 프롬프트는 해석 불가능**하다 (그냥 숫자 열이다) |
| 모델을 고정하므로 **지식 손실이 없다** (일반 미세조정은 지식 손실 발생) | |
| 적은 비용으로 새 데이터셋에 적응시킬 수 있다 | |

> ➡️ **프롬프트 튜닝이 강조되는 이유는 성능 구간과 효율 구간이 겹치는 지점에 있기 때문이다.** 모델을 안 건드리므로(효율·지식 보존) 프롬프트 디자인의 장점을 갖고, 프롬프트를 사람이 아니라 경사하강법이 최적화하므로(성능) 모델 튜닝의 장점도 갖는다.

### Adaptor — 옆에 얇은 것을 붙인다

**Adaptor**는 **학습 가능한 작은 모듈을 레이어마다 삽입하고 그 모듈만 학습**하는 기법이다.

```text
[기존 모델 블록]                 [학습 가능 파라미터가 추가된 블록]
      ↑                                   ↑
   Layer 4                     [Adaptor (학습)][Layer 4 (고정)]
      ⊕ ←─┐        모듈 추가            ⊕ ←─┐
   Layer 3 │ skip     ⇒        [Adaptor (학습)][Layer 3 (고정)] │ skip
      ↑    │                            ↑        │
   Layer 2 │                   [Adaptor (학습)][Layer 2 (고정)] │
      ⊕ ←─┤                            ⊕ ←─┤
   Layer 1 │                   [Adaptor (학습)][Layer 1 (고정)] │
      ↑────┘                            ↑────────┘
```

원래 레이어는 그대로 두고 옆에 얇은 모듈만 붙이므로, 학습할 파라미터 수가 전체의 극히 일부로 줄어든다. **LoRA(Low-Rank Adaptation)** 가 이 계열의 대표다.

> 💡 **4절의 ControlNet과 같은 구조라는 걸 눈치챘다면 정확하다**
> ControlNet도 원본 UNet은 얼리고 복사본에 조건을 넣어 zero conv로 합류시켰다. NExT-GPT도 인코더·LLM·diffusion을 얼리고 프로젝션만 학습했다. **"거대한 원본은 그대로 두고, 얇은 것을 옆에 붙여 새 능력을 얻는다"** — 이 패턴이 도메인을 가리지 않고 반복된다. 저장 비용 관점에서도 유리하다. 태스크 열 개면 거대 모델 열 개가 아니라 어댑터 열 개만 있으면 된다.

<br>

---

## 8. DreamBooth — 사진 3~5장으로 만드는 개인 전용 모델

적응 학습이 도메인 단위로 내려오면 **도메인 특화**이고, 개인 단위까지 내려오면 **개인화**다. 이미지 생성 모델의 개인화 대표 사례가 **DreamBooth(CVPR 2023)** 다.

목표가 구체적이다. **내 강아지 사진 몇 장으로, 그 강아지를 어떤 상황에도 등장시키는 개인 전용 생성 모델을 만든다.**

![내 피사체 사진 서너 장과 클래스 이름을 함께 넣어 고유 식별자 토큰과 모델을 함께 미세조정하고, 추론 시에는 그 식별자를 포함한 프롬프트로 같은 피사체를 다른 상황에 등장시키는 DreamBooth의 학습과 추론 흐름 다이어그램](09-dreambooth.png)

```text
[Fine-Tuning]
 입력: 내 피사체 이미지 3~5장
     + 피사체의 클래스 이름 ("dog")
     + 사전학습된 Text-to-Image 모델
   → DreamBooth →
 출력: 고유 식별자 "[V]"
     + 개인화된 Text-to-Image 모델

[Inference]
 "A [V] dog in the beach"                 → 해변의 그 강아지
 "A [V] dog walking on a colorful carpet" → 카펫 위의 그 강아지
```

핵심은 **학습 가능한 토큰(고유 식별자)과 모델을 함께 미세조정**한다는 것이다.

> ⚠️ **입력 이미지는 3~5장이면 된다**
> 개인화에 수천 장이 필요하지 않다는 것이 DreamBooth의 임팩트다. `[V]`는 사전에 의미가 거의 없는 **희귀 토큰**을 골라 "내 피사체"라는 뜻을 새로 심어 넣은 것이다. 흔한 단어를 쓰면 원래 의미와 충돌한다. 그리고 `dog`라는 **클래스명을 함께 주는 이유**는, 모델이 원래 알고 있던 "개"라는 개념을 잃지 않게 하기 위해서다(사전 보존, prior preservation). 이게 없으면 3~5장에 과적합돼 어떤 프롬프트를 넣어도 그 강아지의 특정 포즈만 나오는 사고가 난다.

한 번 개인화하면 프롬프트만 바꿔 여러 축으로 변형할 수 있다.

| 변형 축 | 프롬프트 형태 | 예시 |
|---|---|---|
| 장소·상황 재배치 | "in the Acropolis", "in a bucket", "swimming", "getting a haircut" | 같은 강아지가 각 상황에 등장 |
| 화풍 변환 | 화가 이름 지정 | Van Gogh, Michelangelo, Rembrandt, Vermeer, Renoir, da Vinci |
| 색상 변경 | `"A [color] [V] car"` | purple, red, yellow, blue, pink |
| 종 하이브리드 | `"A cross of a [V] dog and a [target species]"` | Bear, Panda, Koala, Lion, Hippo |

> ➡️ **핵심 성과는 "피사체의 정체성(identity)은 유지하면서 나머지 속성만 바꾼다"** 는 것이다. 배경도 화풍도 심지어 종까지 바꾸는데 "내 강아지"라는 건 알아볼 수 있다.

<br>

---

## 9. 합성 데이터 ① 지식 증류 — 선생님이 찍어주는 정답

이제 세 번째 덩어리, **데이터가 없을 때** 다. 현실 데이터가 부족하거나 취득이 불가능할 때 쓰는 방법이 **모델이 만들어낸 데이터로 다른 모델을 학습시키는 것**이다.

첫 번째 방식은 **지식 증류(Knowledge Distillation)** 다. 출발 질문은 이렇다. **사전학습된 고성능 모델의 지식을 작은 모델에 압축해서, 빠르고 효율적으로 만들 수 없을까?**

여기에 중요한 배경이 하나 있다. 지금까지 본 미세조정 같은 **전이학습은 사전학습 모델과 타깃 모델의 구조가 같다는 것을 전제**한다. 지식 증류는 그 제약을 푼다. **구조가 달라도 된다.**

> **지식 증류 (Knowledge Distillation)**
> 높은 성능의 무거운 모델(**Teacher**)을 모방하도록 가벼운 모델(**Student**)을 학습시키는 방법. 뒤집어 보면, 작은 모델 혼자서는 배우기 어려운 데이터 특징을 무거운 모델의 도움을 받아 배우게 하는 기법이다.

![소규모 목표 데이터를 얼린 Teacher 모델과 학습 중인 Student 모델에 함께 넣고 Teacher의 소프트 라벨과 Student의 출력이 가까워지도록 하는 증류 손실과 실제 정답과 맞추는 손실 두 갈래로 학습 신호가 흐르는 지식 증류 구조 다이어그램](10-distillation.png)

```text
                   목표 데이터 (소규모)
                   ↙                ↘
   [Teacher: 사전학습 고성능 모델 (고정)]   [Student: 타깃 모델 (학습)]
                ↓                            ↓
        Teacher 출력                    Student 출력
        (soft label = 가짜 정답)
                └──── Distillation Loss ────┘
                             +
                    Ground Truth와의 일반 손실
```

학습 방식은 **선생님이 예측한 soft label과 학생의 예측이 가까워지도록** 유도하는 것이다. **Soft label**은 0과 1 사이의 모델 예측값을 **가짜 라벨(정답)** 로 쓰는 것을 말한다. 학습 신호는 두 갈래로 들어간다 — Teacher의 예측과 맞추는 **증류 손실** + 실제 정답과 맞추는 **일반 손실**.

> 💡 **왜 soft label이 하드 라벨보다 정보가 많은가**
> 정답이 "고양이"라는 하드 라벨은 `[0, 1, 0, 0]`이다. 반면 Teacher의 soft label은 `[0.05, 0.80, 0.13, 0.02]` 같은 형태로 **"고양이지만 개와도 조금 닮았고 자동차와는 전혀 다르다"** 는 클래스 간 관계까지 담고 있다. Teacher가 데이터에서 배운 이 미묘한 구조가 Student에게 전달되는 것이 증류의 핵심이다. 앞 글에서 본 DeiT(CNN 선생님 → ViT 학생)가 정확히 이 기법이었다.

이게 "합성 데이터 활용법"으로 분류되는 이유도 여기 있다. **Teacher가 뽑아낸 soft label 자체가 사람이 만들지 않은 합성 정답**이다. 소규모 목표 데이터만 있으면 Teacher가 무한히 라벨을 찍어 준다. 5절에서 본 Depth Anything v2가 6,200만 장의 라벨 없는 데이터를 끌어들인 방식도 이것과 같은 계열이다.

> 📌 **원본 강의자료와 다르게 쓴 부분**
> 각주가 `Hinton et al., "Distilling the Knowledge in a Neural Network", NIPS 2024 Deep Learning Workshop`으로 되어 있으나 **NIPS 2014**가 맞다. 지식 증류는 2014년 논문이고, 딥러닝 초기 기법이라는 사실이 계보 이해에 중요하다.

<br>

---

## 10. 합성 데이터 ② InstructPix2Pix — 모델을 데이터 공장으로

이 글에서 가장 인상적인 사례다. 발상을 한 문장으로 요약하면 이렇다. **학습 데이터가 세상에 없으면, 파운데이션 모델로 만들어 버린다.**

**InstructPix2Pix(CVPR 2023)** 는 **명령(instruction)에 따라 이미지를 편집하는 모델**이다. "Swap sunflowers with roses", "Add fireworks to the sky", "What would it look like if it were snowing?", "Turn it into a still from a western" 같은 지시문 하나면 된다. 기존 방법이 입력 이미지와 출력 이미지의 **상세 설명을 둘 다** 요구했던 것과 대조된다.

### 문제 — 그런 데이터셋은 세상에 없다

이걸 지도학습으로 만들려면 `{지시사항, 편집 전 이미지, 편집 후 이미지}` 3종 세트가 필요하다. 그런데 세상에 있는 범용 데이터셋은 `{이미지, 캡션}` 형태뿐이다. 같은 장면에서 딱 한 부분만 바뀐 이미지 쌍을, 지시문까지 붙여서, 수십만 개 모은 데이터셋은 존재하지 않는다.

그래서 연구진은 **기존 파운데이션 모델들을 조합해 데이터셋 자체를 합성해 버린다.**

![사람이 쓴 700개 편집 예시로 GPT를 미세조정하고 그 GPT로 45만 개의 캡션 지시 편집캡션 3종 텍스트를 생성한 뒤 Stable Diffusion과 Prompt2Prompt로 이미지 쌍을 만들어 최종 InstructPix2Pix 모델을 학습시키고 실사진에 적용하는 데이터 공장 파이프라인 다이어그램](11-data-factory.png)

### 단계 1 — 사람이 씨앗 700개를 만든다

먼저 사람이 직접 작은 데이터셋을 쓴다. **단 700개다.**

| Input LAION caption | Edit instruction | Edited caption |
|---|---|---|
| Yefim Volkov, Misty Morning | make it afternoon | Yefim Volkov, Misty Afternoon |
| girl with horse at sunset | change the background to a city | girl with horse at sunset in front of city |
| painting-of-forest-and-pond | Without the water. | painting-of-forest |

이 700개로 **GPT를 미세조정**한다.

### 단계 2 — GPT가 45만 개를 찍어낸다

미세조정된 GPT는 **창의적이면서도 그럴듯한 지시문과 캡션을 생성**한다. 결과가 **45만 개 이상의 텍스트 3종 세트**다.

| Input LAION caption | Edit instruction | Edited caption |
|---|---|---|
| Alex Hill, Original oil painting on canvas, Moonlight Bay | in the style of a coloring book | Alex Hill, Original coloring book illustration, Moonlight Bay |
| Kate Hudson arriving at the Golden Globes 2015 | make her look like a zombie | Zombie Kate Hudson arriving at the Golden Globes 2015 |

```text
(a) Generate text edits:
  Input Caption:  "photograph of a girl riding a horse"
    → [GPT-3 (fine-tuned)] →
  Instruction:    "have her ride a dragon"
  Edited Caption: "photograph of a girl riding a dragon"
```

### 단계 3 — 텍스트 쌍에서 이미지 쌍을 만든다

이제 캡션 쌍을 이미지 쌍으로 바꾼다. 여기가 가장 까다로운 지점이다.

```text
(b) Generate paired images:
  Input Caption:  "photograph of a girl riding a horse"
  Edited Caption: "photograph of a girl riding a dragon"
    → [Stable Diffusion + Prompt2Prompt] →
  (편집 전 이미지, 편집 후 이미지) 쌍
```

> ⚠️ **Prompt2Prompt가 빠지면 이 파이프라인은 무너진다**
> 캡션 두 개를 Stable Diffusion에 **따로따로** 넣으면 구도·인물·배경이 전부 다른 두 장이 나온다. 그건 "편집 쌍"이 아니라 그냥 무관한 두 장이다. **Prompt2Prompt는 두 생성 과정의 어텐션 맵을 공유해 바꾸라고 한 부분만 바뀌고 나머지는 유지되게** 만든다. 논문의 비교 그림이 정확히 이 차이를 보여준다 — Prompt2Prompt 없이는 말과 용의 자세·배경이 제각각이고, 있으면 같은 장면에서 말만 용으로 바뀐다.

### 단계 4 — 합성 데이터로 학습하고, 실사진에 쓴다

```text
사람이 쓴 700개 편집 예시
   → GPT 미세조정
   → 45만 개 {입력 캡션, 편집 지시, 편집된 캡션} 생성
   → Stable Diffusion + Prompt2Prompt로 {편집 전, 편집 후} 이미지 쌍 생성
   → 완성된 합성 데이터셋으로 InstructPix2Pix 학습
   → 실사진에 "turn her into a snake lady" 같은 명령만으로 편집
```

학습은 전부 합성 데이터로 하지만, **추론은 실제 사진에 사람이 직접 쓴 지시문으로 한다.** 합성 데이터로 배운 능력이 실제 도메인으로 일반화된다는 것이 이 연구의 핵심 주장이다. 결과적으로 진주 귀걸이를 한 소녀에게 `"Apply face paint"`, `"She should look 100 years old"`, `"What if she were in an anime?"`, `"Make her James Bond"` 같은 명령이 전부 하나의 모델에서 처리된다.

> ➡️ **GPT-3와 Stable Diffusion은 "학습 대상"이 아니라 데이터 공장이다.** 최종적으로 학습되는 것은 InstructPix2Pix 한 모델뿐이다. 이 역할 구분이 헷갈리기 쉽다. 사람이 만든 데이터는 700개인데, 모델 두 개를 거쳐 45만 개짜리 지도학습 데이터셋이 나왔다.

### LLaVA도 같은 발상이다

앞 글에서 다룬 **LLaVA**의 학습 데이터도 이렇게 만들어졌다. 시각 설명 데이터(visual instruction data)를 GPT로 생성한 것이다. 재미있는 건 **이미지를 GPT에 직접 넣지 않는다**는 점이다. 기존 데이터셋의 **캡션과 바운딩 박스를 텍스트로 변환해** GPT에 넣고 문제-정답 쌍을 만든다.

| 넣는 것 (컨텍스트) | 내용 |
|---|---|
| Type 1: Captions | 이미지에 달린 캡션 문장들 |
| Type 2: Boxes | `person: [0.681, 0.242, 0.774, 0.694], backpack: [0.384, 0.696, ...]` 형태의 객체 좌표 |

| 나오는 것 (응답) | 설명 |
|---|---|
| Type 1: conversation | 짧은 QA. "이 사진의 차량 종류는?" → "검은색 SUV입니다" |
| Type 2: detailed description | 장면 전체를 문단으로 상세 묘사 |
| Type 3: complex reasoning | 추론이 필요한 QA. "이 사람들이 겪는 어려움은?" → 짐이 많아 SUV에 다 못 싣는 상황이라는 추론 |

당시 GPT는 이미지를 못 봤다. 그런데 **캡션과 좌표만으로 충분히 그럴듯한 시각 QA를 만들어낼 수 있었고**, 그 데이터로 학습한 LLaVA는 실제로 이미지를 보고 답하게 됐다. 실사용 예를 보면 원본 모나리자에는 "다빈치의 모나리자, 루브르에 전시"라고 답하지만, 개 얼굴로 합성된 패러디 이미지에는 "작가는 명확하지 않지만 다빈치의 모나리자를 참고한 것으로 보인다"고 답을 조정한다. **암기가 아니라 시각 정보에 근거해 답하고 있다는 증거다.**

<br>

---

## 11. 합성 데이터 ③ 시뮬레이션과, 공짜가 아닌 이유

생성 모델을 안 쓰고 **알고리즘과 시뮬레이션으로 직접 만드는** 더 단순한 계열도 있다.

> **합성 데이터 (synthetic data)**
> 실제 데이터를 모방하거나 새로 생성한 인공 데이터. 가상의 이미지·텍스트·소리 등을 알고리즘으로 만든다. 실제 데이터 수집이 어려울 때 대체하고, 데이터 부족 문제를 해결해 모델 성능을 끌어올리는 데 쓴다.

대표 기법이 **copy-paste 증강**이다. 한 이미지에서 분할해낸 객체를 잘라 다른 배경에 붙여 물체 탐지용 데이터를 만든다. 축구 선수를 기린과 타조가 있는 초원 이미지에 붙여넣는 식이다. 오토바이 탄 사람이나 야구 선수를 피자 이미지 위에 합성하는 극단적 조합도 그대로 학습 데이터가 된다. 말이 안 되는 장면이지만, 탐지 모델 입장에서는 "배경이 뭐든 이 형태는 사람"이라는 신호를 강화해 준다.

**진짜 가치는 취득 자체가 불가능한 데이터에서 나온다.** 대표 예가 **모션 증폭(motion magnification)** 이다. 심박에 따른 피부의 미세한 색 변화, 건물의 육안으로 안 보이는 진동 같은 것을 증폭해서 보이게 만드는 태스크인데, 여기서 필요한 "증폭 전/후 정답 쌍"은 **현실에서 촬영이 원천적으로 불가능하다.** 증폭된 현실이라는 게 없기 때문이다. 그래서 합성 데이터로 학습한다. 4절 Marigold의 깊이 정답이 렌더링에서 공짜로 나온 것과 정확히 같은 논리다.

> 📌 **원본 강의자료와 다르게 쓴 부분**
> 이 페이지의 각주가 *Learning-based Motion Magnification* (ECCV 2018)로 달려 있는데, 함께 실린 copy-paste 증강 그림(축구선수 ↔ 기린·타조)은 그 논문의 것이 아니다. **Ghiasi et al., *Simple Copy-Paste is a Strong Data Augmentation Method for Instance Segmentation*, CVPR 2021** 계열의 그림이다. 모션 증폭 각주는 뒤 페이지에만 해당한다.

### 합성 데이터는 공짜가 아니다

여기까지 읽으면 합성 데이터가 만능처럼 보이지만, 대표적인 실패 모드가 둘 있다.

**1) 라벨 품질의 불일치(Label Inconsistency)**

합성 데이터를 만들 때 쓰는 VLM·LLM 기반 라벨러가 **항상 일관된 기준으로 라벨을 생성하지는 않는다.** 같은 객체라도 프롬프트 방식·모델 버전·컨텍스트에 따라 표현이 달라진다.

```text
같은 기타 사진에 대해:
  "electric guitar"  /  "guitar player"  /  "acoustic instrument"
  → 셋 다 틀린 말은 아니지만, 학습 데이터로는 서로 다른 클래스가 된다
```

개선 방안은 **라벨 정규화, 클래스 사전 고정, 교차 모델 검증, 휴먼 검수**다.

**2) 할루시네이션(Hallucination)**

생성 모델이 실제로 존재하지 않는 객체·특징·관계를 만들어내는 현상이다. 손가락이 6개인 이미지, 물리적으로 불가능한 그림자가 지속적으로 합성된다. 개선 방안은 **정교한 프롬프트, 후처리 Refiner, 사실성 검증 모델, 실데이터 혼합**이다.

> ⚠️ **본질은 "틀린 데이터가 섞인다"가 아니라 "모델이 그 오류를 정답으로 학습한다"는 것이다**
> 합성 데이터의 오류는 단순히 정확도를 떨어뜨리는 데서 끝나지 않는다. 모델이 그것을 정답으로 배워 **오류와 편향이 증폭된다.** 손가락 6개가 계속 나오는 모델로 데이터를 더 만들면 손가락 6개가 정답이 된다. 그래서 두 문제 모두 개선 방안의 마지막이 항상 **"휴먼 검수"와 "실데이터 혼합"** 이다. 자동화의 마지막 한 칸은 사람이 채워야 한다.

<br>

---

## 12. What's next — 툴을 쓰는 모델과 AGI의 빈칸

지금까지 배운 파운데이션 모델들이 어디로 가고 있는가에 대한 전망 파트다.

### 인간 지능의 구조와 빈칸

강의는 인간의 인지를 이렇게 놓는다.

```text
Human's Intelligence (cognition) = perception ∪ higher cognitive processes

cognition 안에는  perception(지각) · imagination(상상) · higher cognitive functions(고차 인지)
```

현재 AI 시스템은 여기에 이렇게 대응된다.

```text
World      →  Perception              →  Cognition  →  Interpretation
(실제 장면)   Multi-modal interface       LLM          "A man standing next to
              (이미지·음성 → 토큰)                       the pavilion with a gray roof!"
```

강의는 같은 그림을 세 번 보여주면서 한 칸씩 채운다.

1. **기본형** — 지각은 멀티모달 인터페이스가, 고차 인지는 LLM이 담당한다
2. **상상 추가** — LLM 쪽에 `imagination!` 말풍선이 붙는다. 인지가 해석만 하는 게 아니라 **상상(생성)** 도 해야 한다는 것
3. **루프 닫기** — 그 상상의 화살표가 **다시 World로 되돌아간다**

```text
        ┌──────────────── imagination ────────────────┐
        ↓                                             │
World  →  Multi-modal interface  →  LLM  →  Interpretation
(행동)          (지각)             (인지·상상)
```

> ➡️ **핵심은 마지막 화살표의 방향이다.** `LLM → World`로 되돌아가는 피드백이 생겨야 지각-인지-행동이 닫힌 고리가 되고, 그것이 AGI와 Embodied AI로 가는 그림이다. 그림만 보면 "같은 슬라이드 3장"으로 넘기기 쉽지만 셋의 차이가 곧 로드맵이다.

### 월드 모델 — 생성 모델이 물리를 이해한다면

**Sora**의 응용 사례로 **비디오 생성 기반 3D 장면 복원**이 제시된다. 생성된 비디오에서 3D 구조를 복원할 수 있다는 것은, 모델 내부에 3차원 공간과 물리에 대한 표현이 이미 잡혀 있다는 뜻이다. 슬라이드의 질문이 그대로 요지다 — **"설마 물리현상을 이해?"**

이게 사실이라면 Sora는 픽셀 생성기가 아니라 **월드 시뮬레이터**이고, 로봇 제어 같은 **Embodied AI의 핵심 컴포넌트**가 된다. 계층적 계획을 위한 **Compositional Foundation Models** 구조가 그 그림이다.

| 담당 계층 | 모델 | 학습 데이터 |
|---|---|---|
| Task Planning (무엇을 할지) | Large Language Model | Internet Text |
| Visual Planning (어떻게 보일지) | Video Diffusion | Internet Video |
| Action Planning (어떻게 움직일지) | Action Model | Egocentric Images |

세 모델이 **반복 정제(iterative refinement)** 로 서로를 보정하며 하나의 계획을 만든다. NVIDIA의 `DreamZero`가 제시하는 **World Action Model(WAM)** 은 이걸 한 모델로 합친다 — `Videos + Language + Proprio(고유수용 감각)` → WAM → `Future Videos + Continuous actions`. 미래 영상 예측과 연속 행동 출력을 동시에 담당한다.

### 툴을 쓰는 모델 — 학습 없이 능력을 늘린다

먼저 **RAG(Retrieval Augmented Generation)** 부터. LLM에 검색을 붙이는 구조다.

| 장점 | 내용 |
|---|---|
| 유연성 | **추가 학습 없이** 최신 정보 제공 |
| 개인화 | 검색과 언어모델의 합성으로 맞춤 답변 |
| 정확성 | 검증을 통한 환각 감소 |

```text
User ──질문──→ [Retriever]
                   │  검색 쿼리 ↓   ↑ 검색된 텍스트
                   │          [문서 저장소]
                   └──Full Prompt──→ [LLM] ──응답──→ User
```

핵심은 질문을 그대로 LLM에 넣지 않고, **검색된 텍스트를 붙인 Full Prompt를 만들어 넣는다**는 점이다.

여기서 한 걸음 더 나가면 **툴 증강 언어모델(Tool-augmented LLM), 즉 에이전트**다. 언어모델이 외부 툴(API)을 호출해 자기 능력 밖의 일을 수행한다.

```text
사용자 ──프롬프트──→ [LLM Agent] ──액션──→ 실제 세계(World)
      ←──응답─────                ←──관측──
                         ↑
              Textual / Visual / Audio / Camera /
              Location / Device / Shopping / Game APIs
```

대표 사례가 **VISPROG(Visual Programming, AI2, CVPR 2023)** 다. **언어모델의 추가 학습 없이** 주어진 툴만 써서 사용자가 텍스트로 요청한 영상 처리를 수행한다.

```text
자연어 지시문 ─────────┐
                       ├→ [프로그램 생성기 (GPT-3)]
in-context 예제 쌍 ────┘            │
(Instruction/Program)                ↓
                          생성된 고수준 프로그램
                                     │
                    입력 이미지 ─────┤
                                     ↓
              [프로그램 인터프리터 (Python + 모듈 라이브러리)]
                                     │
                          ┌──────────┴──────────┐
                      Prediction          Visual Rationale
```

GPT-3가 in-context 예제만 보고 프로그램을 짜면, 실제 픽셀 조작은 파이썬 모듈들이 한다.

```python
# Instruction: Replace the BMW with an Audi and cloudy sky with clear sky
# → GPT-3가 생성한 프로그램:
OBJ0=Seg(image=IMAGE)
OBJ1=Select(image=IMAGE, object=OBJ0, query='BMW')
IMAGE0=Replace(image=IMAGE, object=OBJ1, prompt='Audi')
OBJ1=Seg(image=IMAGE0)
OBJ2=Select(image=IMAGE0, object=OBJ1, query='cloudy sky')
IMAGE1=Replace(image=IMAGE0, object=OBJ2, prompt='clear sky')
RESULT=IMAGE1
```

질의응답도 같은 방식으로 푼다. "사진에 넥타이와 안경이 둘 다 있는가?"를 프로그램으로 분해한다.

```python
# Question: Are there both ties and glasses in the picture?
BOX0=Loc(image=IMAGE, object='ties')
ANSWER0=Count(box=BOX0)
BOX1=Loc(image=IMAGE, object='glasses')
ANSWER1=Count(box=BOX1)
ANSWER2=Eval("'yes' if {ANSWER0} > 0 and {ANSWER1} > 0 else 'no'")
RESULT=ANSWER2
```

여기서 각 모듈이 무엇으로 구현됐는지를 보면 이 논문의 진짜 메시지가 보인다.

| 분류 | 모듈 → 구현 |
|---|---|
| Image Understanding | `Loc`→OWL-ViT, `FaceDet`→DSFD, `Seg`→MaskFormer, `Select`/`Classify`→CLIP-ViT, `Vqa`→ViLT |
| Image Manipulation | `Replace`→Stable Diffusion, `ColorPop`/`BgBlur`→PIL + cv2.grabCut, `Crop` 계열→`PIL.crop()` |
| Knowledge Retrieval | `List`→GPT-3 |
| Arithmetic & Logical | `Eval`→`eval()`, `Count`→`len()` |

**무거운 모듈은 `Loc`, `Seg`, `Vqa`, `Replace` 넷뿐이고 나머지는 PIL·OpenCV 한 줄**이다. 툴 라이브러리 대부분이 기존 자산의 얇은 래퍼다.

> ➡️ **학습(gradient update)이 단 한 번도 없다.** 1절의 Grounded SAM이 모델 두 개를 이었다면, VISPROG는 **LLM이 그 조립을 매번 즉석에서 설계**한다. 조립 패턴의 최종 형태다.

같은 계열의 사례가 이어진다. **Claude Computer Use**는 텍스트 지시로 컴퓨터를 사람처럼 조작하는 서비스이고(데모에도 보안 경고가 붙어 있다 — 악성 웹 콘텐츠가 모델 행동을 가로챌 수 있으므로 민감한 계정 접근 권한을 주면 안 된다), 로봇 조작용 코딩 에이전트 **CaP-X**는 여러 코딩 에이전트를 병렬로 돌려 후보 코드를 만들고 앙상블한 뒤 실행 결과와 시각적 변화를 피드백으로 되돌리는 폐루프를 만든다.

**Agent Laboratory**는 이걸 연구 자동화로 확장한다.

| Phase | 담당 에이전트 | 사용 툴 |
|---|---|---|
| Literature Review | Postdoc + PhD Student | arXiv |
| Plan Formulation | Postdoc + PhD Student | — |
| Data Preparation | SW Engineer + ML Engineer | Hugging Face |
| Running Experiments | ML Engineer (mle-solver) | Python |
| Report Writing | PhD Student (paper-solver) | LaTeX |
| Report Refinement | Reviewers + PhD Student | — |

> 💡 **mle-solver와 paper-solver는 같은 구조다**
> mle-solver는 이전 코드를 수정해 새 코드를 만들고 실행 결과를 **reward model**로 채점해 상위만 남긴다. paper-solver는 이전 리포트를 수정해 새 리포트를 만들고 Professor 에이전트의 **report score**로 상위만 남긴다. **"생성 → 채점 → 상위 선별 → 재생성"이라는 동일한 진화적 탐색**이고, 담당 에이전트와 채점자만 다르다. 에이전트 시스템을 설계할 때 반복해서 쓸 수 있는 뼈대다.

<br>

---

## 13. 배포와 서빙 — 만든 것을 남이 쓰게 하기

마지막은 실무 단계다. 모델을 잘 만들었어도 남이 쓸 수 없으면 아무 일도 일어나지 않는다.

### 허깅페이스와 서빙 구조

**허깅페이스(Hugging Face)** 는 AI 오픈소스 모델과 데이터셋을 공유하는 플랫폼이다. 사전학습 모델 **가중치**와 **데이터셋**을 제공하고, NLP·CV·음성 전 분야를 커버한다. 플랫폼이 제공하는 축은 다섯이다 — `Model Architectures`, `Pre-trained models`, `Tokenization`, `Training pipelines`, `Datasets`.

> **모델 서빙 (model serving)**
> 사용자에게 모델의 예측 결과를 전달하는 절차. **배포**(학습된 모델을 서비스 가능한 상태로 변환해 설치·실행 유지), **API 제공**(REST API, gRPC 등), 그리고 확장·라우팅·모니터링 같은 운영 보조 기능으로 구성된다.

![사용자 인터페이스와 오케스트레이션 서버와 모델 서버로 이루어진 3계층 서빙 구조를 보여주고, Hugging Face Inference API가 오른쪽의 모델 서버 계층을 통째로 대신 호스팅해 주는 관계를 겹쳐 표시한 다이어그램](12-serving-stack.png)

```text
[사용자 인터페이스 (UI)]        [Server]                 [Model Server]
   ChatGPT 같은 화면      오케스트레이션 Layer              모델 서빙
        │                    (LangChain)          (ONNX Runtime / FastAPI /
        │ ── 요청 ──→            │ ── HTTP REST ──→   TensorFlow Serving)
        │   HTTP REST API        │                          │ 추론
        │ ←── 응답 핸들링 ──      │ ←────────────────────────┘
   UI 렌더링
```

요청은 `UI → Server → Model Server` 순으로 흐르고 응답은 반대로 돌아온다. 우리가 다루는 부분은 오른쪽 끝의 **Model Server**다.

### Hugging Face Inference API

**별도 서버 구축 없이 REST API만으로 모델을 바로 쓰는 서비스**다. 앞 그림에서 **Model Server 부분을 허깅페이스가 대신 호스팅**해 주는 형태라고 보면 정확하다.

- 연구·테스트 목적이라 **요청 수 제한**이 있다 (무료·유료 버전)
- 게시된 모델 수는 캡처 시점 기준 **1,335,561개**. 태스크별(Multimodal / Computer Vision / NLP) 필터링이 된다
- **모든 게시 모델이 배포를 지원하지는 않는다.** 모델 카드의 `Deploy` 버튼에서 선택지를 확인해야 한다

| Deploy 선택지 | 용도 |
|---|---|
| Inference API (serverless) | 프로토타이핑용 서버리스 추론 |
| HF Inference Endpoints | 프로덕션용 전용 엔드포인트 |
| Amazon SageMaker | AWS 통합 배포 |

Python 클라이언트 사용법은 짧다.

```python
from huggingface_hub import InferenceClient

# provider와 api_key로 클라이언트를 만든다
client = InferenceClient(
    provider="hf-inference",
    api_key="hf_xxxxxxxxxxxxxxxxxxxxxxxx"   # 실제로는 환경변수로 주입한다
)

messages = [
    {"role": "user", "content": "What is the capital of France?"}
]

# 모델 ID를 지정해 추론을 요청한다
completion = client.chat.completions.create(
    model="deepseek-ai/DeepSeek-R1-Distill-Qwen-32B",
    messages=messages,
    max_tokens=500
)

print(completion.choices[0].message)
```

> ⚠️ **api_key를 코드에 그대로 적으면 안 된다**
> `api_key`는 사용자별 고유 액세스 키, 즉 비밀번호다. 허깅페이스 홈페이지에서 **Access Token**을 발급받아 **환경변수 등으로 주입**해야 한다. 슬라이드에서도 `hf_xxxx...`로 마스킹돼 있다. 이걸 그대로 깃허브에 올리면 토큰이 즉시 탈취된다.

### 온디바이스 서빙

**MediaPipe**, **MLC LLM** 같은 모바일 서빙 프레임워크로 안드로이드에서 LLM을 직접 돌릴 수 있다. 장점은 두 가지 — **개인정보 보안**과 **오프라인 사용**이다. 앞 글에서 sVLM(작은 VLM)을 다룬 이유가 여기서 만난다. 작게 만들어야 이 자리에 올릴 수 있다.

| 모델 | 양자화 포맷 |
|---|---|
| Qwen2-1.5B-Instruct | q4f16_1-MLC |
| Llama-3.1-8B-Instruct | q3f16_1-MLC |
| Phi-3-mini-4k-instruct | q4f16_1-MLC |
| gemma-2-2b-it | q4f16_1-MLC |
| Mistral-7B-Instruct-v0.3 | q4f16_1-MLC |

> 💡 **모델 이름 뒤의 q4f16_1은 양자화 설정이다**
> `q4`/`q3`는 **가중치 비트 수**, `f16`은 **연산 정밀도**를 뜻한다. 목록에서 8B짜리 Llama-3.1만 `q3`로 더 강하게 압축한 것은 모바일 메모리에 맞추기 위해서다. 같은 프레임워크에서도 모델 크기에 따라 압축 강도를 다르게 잡는다는 게 실무 감각이다. LLM만이 아니라 얼굴 인식용 **MobileFace** 같은 모델도 온디바이스로 돌아간다. 멀티플랫폼 호환을 위한 **ONNX**를 함께 공부해 두면 좋다.

### Gradio — 데모 페이지 만들기

**Gradio**는 머신러닝 모델을 웹 인터페이스로 쉽게 배포하게 해주는 오픈소스 라이브러리다. 코딩을 모르는 사용자도 브라우저로 모델과 상호작용할 수 있고, **허깅페이스와 통합**돼 있어 Spaces에 그대로 올리면 라이브 데모가 된다. 이미지·텍스트·오디오 등 다양한 입출력을 지원한다. `Whisper JAX`(마이크 녹음 → 전사), `Kolors Virtual Try-On`(인물 사진 + 의류 사진 → 가상 피팅) 같은 실제 데모가 전부 Gradio다.

```python
import gradio as gr

# Gradio 인터페이스 생성 (Gradio 4.x 이상 기준)
interface = gr.Interface(
    fn=predict_image,                      # 예측에 사용할 함수
    inputs=gr.Image(image_mode='L'),       # 입력 설정 (흑백 이미지)
    outputs="label",                       # 출력 설정 (라벨)
    live=True                              # 입력이 바뀔 때마다 실시간 예측
)

# 인터페이스 실행
interface.launch()
```

> ⚠️ **강의자료의 Gradio 코드는 그대로 복붙하면 실행되지 않는다**
> 슬라이드 코드는 `gr.inputs.Image(shape=(28, 28), image_mode='L', invert_colors=True)` 형태인데, 이건 **Gradio 3.x 이하의 옛 API**다. 4.x 이상에서는 `gr.inputs` 네임스페이스 자체가 사라지고 `gr.Image(...)`로 바뀌었으며 **`shape`와 `invert_colors` 인자는 제거**됐다. 위 코드는 최신 API로 고쳐 쓴 것이다. 리사이즈와 색 반전이 필요하면 `predict_image` 함수 안에서 직접 처리하면 된다. 튜토리얼 코드가 안 돌아갈 때는 라이브러리 메이저 버전부터 확인하는 습관이 필요하다.

### 다음 실습으로 이어지는 세 단계

이 글의 내용이 실습에서 어떻게 한 줄로 이어지는지가 마지막 그림이다.

```text
[생성] Stable Diffusion으로 합성 이미지 생성
          ↓
[평가] CLIP으로 "프롬프트 ↔ 생성 이미지" 일치도를 자동 채점
       (= 11절의 할루시네이션·라벨 불일치를 걸러내는 단계)
          ↓
[확장] 걸러낸 합성 데이터로 전이 학습 → 최종 태스크 모델
```

**CLIP이 여기서 평가자로 쓰인다는 점이 흥미롭다.** 앞 글에서 본 대로 CLIP은 이미지를 분류하는 게 아니라 **이미지와 텍스트의 짝을 맞추는** 모델이다. 그 성질을 그대로 뒤집으면 "생성 프롬프트와 생성 이미지가 얼마나 일치하는가"를 점수로 쓸 수 있다. 학습 때 본 적 없는 클래스도 캡션만 만들면 다룰 수 있는 제로샷 성질 덕분이다.

전이 학습 단계에서 쓰는 두 기법도 구분해 두면 좋다.

| 기법 | 갱신 대상 | 언제 쓰나 |
|---|---|---|
| **Linear Probing** | 출력층만 학습 | 데이터가 아주 적고, 사전학습 표현이 이미 충분히 좋을 때 |
| **Fine-tuning** | 일부 또는 전체 가중치 재학습 | 데이터가 어느 정도 있고, 도메인 차이가 클 때 |

> 📌 **원본 강의자료와 다르게 쓴 부분**
> 실습 미리보기 슬라이드에 `Station Diffusion`으로 적힌 곳이 있으나 **Stable Diffusion**이 맞다. 바로 다음 슬라이드에서는 올바르게 표기돼 있다.

<br>

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **조립 패턴** | Grounding DINO(박스) → SAM(마스크) = Grounded SAM. **추가 학습 없이 파이프로 이어 새 능력을 얻는다.** SoM의 마크를 찍어주는 것도 이 모델들이다 |
| **Diffusion** | 복잡한 `p_data`를 직접 모델링하는 대신, 아는 분포(정규분포)에서 되돌아오는 경로를 배운다. **정답이 공짜인 "노이즈 맞히기"로 문제를 바꿔치기**한 것 |
| **Latent Diffusion** | DDPM과의 차이는 **확산이 도는 공간**. 픽셀 대신 잠재 공간에서 돌아 훨씬 싸다. 조건은 **cross-attention의 K, V**로 주입 |
| **Seed vs Guidance** | Seed는 출발 노이즈를, Guidance scale은 프롬프트를 얼마나 따를지를 정한다. 완전히 다른 것 |
| **생성 모델 = 시각 사전지식** | ControlNet(조건 제어)·Zero123XL(시점)·Marigold(깊이)·JointDiT(동시 생성)는 **같은 SD를 파인튜닝해 용도만 바꾼 것** |
| **ControlNet vs DreamBooth** | ControlNet은 "무엇을 조건으로 받을지", DreamBooth는 "무엇을 그릴 수 있는지"를 바꾼다 |
| **얇게 붙이기** | ControlNet의 zero conv, NExT-GPT의 프로젝션, Adaptor·LoRA — 전부 **원본은 얼리고 얇은 것을 옆에 붙인다** |
| **미세조정 > 프롬프팅** | 품질·예제 수·**토큰 절약**·지연시간 네 가지. few-shot 예시 3줄이 매 호출마다 사라지는 게 곧 비용이다 |
| **미세조정 학습률** | 이미 좋은 지점에 서 있으므로 **작게, 보수적으로** 시작한다. 크게 잡으면 그 지점을 걷어찬다 |
| **PEFT 3방식** | 모델 튜닝(성능↑ 효율↓) — **프롬프트 튜닝·Adaptor(양쪽을 취함)** — 프롬프트 디자인(효율↑ 성능↓) |
| **DreamBooth** | 사진 **3~5장** + 희귀 토큰 `[V]` + 클래스명(사전 보존). **정체성은 유지하고 나머지 속성만** 바꾼다 |
| **지식 증류** | Teacher의 **soft label 자체가 합성 정답**. 하드 라벨과 달리 클래스 간 관계 정보를 담는다. 구조가 달라도 된다 |
| **데이터 공장** | 사람 700개 → GPT 미세조정 → 45만 개 텍스트 → **SD + Prompt2Prompt**로 이미지 쌍 → InstructPix2Pix 학습. Prompt2Prompt 없으면 편집 쌍이 안 된다 |
| **합성 데이터의 위험** | 라벨 불일치와 할루시네이션이 **정답으로 학습되어 편향이 증폭**된다. 답은 늘 휴먼 검수 + 실데이터 혼합 |
| **학습 0회의 끝** | VISPROG는 GPT-3가 in-context 예제만 보고 프로그램을 짜고 파이썬 모듈이 실행한다. 조립 패턴의 최종형 |
| **서빙** | `UI → 오케스트레이션 Server → Model Server`. HF Inference API는 오른쪽 끝을 대신 호스팅해 주는 것 |

<br>

## 🔗 참고 자료

- [Kirillov et al., 2023 — Segment Anything (SAM)](https://arxiv.org/abs/2304.02643)
- [Liu et al., 2023 — Grounding DINO](https://arxiv.org/abs/2303.05499)
- [Rombach et al., CVPR 2022 — High-Resolution Image Synthesis with Latent Diffusion Models](https://arxiv.org/abs/2112.10752)
- [Ho et al., 2020 — Denoising Diffusion Probabilistic Models (DDPM)](https://arxiv.org/abs/2006.11239)
- [Zhang et al., ICCV 2023 — Adding Conditional Control to Text-to-Image Diffusion Models (ControlNet)](https://arxiv.org/abs/2302.05543)
- [Liu et al., ICCV 2023 — Zero-1-to-3: Zero-shot One Image to 3D Object](https://arxiv.org/abs/2303.11328)
- [Ke et al., 2023 — Marigold: Repurposing Diffusion-Based Image Generators for Monocular Depth Estimation](https://arxiv.org/abs/2312.02145)
- [Wu et al., 2023 — NExT-GPT: Any-to-Any Multimodal LLM](https://arxiv.org/abs/2309.05519)
- [Lester et al., EMNLP 2021 — The Power of Scale for Parameter-Efficient Prompt Tuning](https://arxiv.org/abs/2104.08691)
- [Hu et al., 2021 — LoRA: Low-Rank Adaptation of Large Language Models](https://arxiv.org/abs/2106.09685)
- [Ruiz et al., CVPR 2023 — DreamBooth](https://arxiv.org/abs/2208.12242)
- [Hinton et al., NIPS 2014 Workshop — Distilling the Knowledge in a Neural Network](https://arxiv.org/abs/1503.02531)
- [Brooks et al., CVPR 2023 — InstructPix2Pix](https://arxiv.org/abs/2211.09800)
- [Hertz et al., 2022 — Prompt-to-Prompt Image Editing with Cross Attention Control](https://arxiv.org/abs/2208.01626)
- [Liu et al., NeurIPS 2023 — Visual Instruction Tuning (LLaVA)](https://arxiv.org/abs/2304.08485)
- [Gupta & Kembhavi, CVPR 2023 — Visual Programming (VISPROG)](https://arxiv.org/abs/2211.11559)
- [Hugging Face — Inference API 문서](https://huggingface.co/docs/api-inference/index)
- [Gradio 공식 문서](https://www.gradio.app/docs)
