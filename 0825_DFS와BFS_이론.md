# DFS와 BFS 완전정리 — 원리와 최단 거리 보장의 근거

> 자기주도 학습 정리 (코딩 테스트 대비) · 정리일: 2026-08-25
> 블로그 발행본: https://it-study-2002.tistory.com/entry/DFS와-BFS-완전정리
> 시리즈: [01 해시](0825_해시.md) · **02 DFS와 BFS 이론** · [03 DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md)

DFS와 BFS는 코드를 외우기는 쉬운 알고리즘이다. 열 줄이 안 되고, 검색하면 템플릿이 쏟아진다. 그런데 막상 문제에서 "최단 거리를 구하라"를 만나면 손이 멈춘다. **DFS로 풀어도 되나? 왜 안 되지?** 이 질문에 답을 못 하면 템플릿을 아무리 외워도 매번 찍게 된다.

이 글은 그 답을 만들기 위한 정리다. 두 알고리즘이 각각 **스택과 큐라는 자료구조 위에서 어떤 순서로 돌아가는지**, 그 순서의 차이가 왜 최단 거리 보장으로 이어지는지, 그리고 그 보장이 어디서 깨지는지까지를 다룬다. 그래프 표현 → DFS 원리 → BFS 원리 → 방문 처리 → 최단 거리 → 복잡도 순서로 묶었다.

> 방문 처리에 쓰는 set과 튜플 키는 [파이썬 해시 완전정리](0825_해시.md) 편에서 다뤘다.

<br>

## 📖 이 글에서 다루는 내용

1. 그래프라고 말해 주지 않는 문제들
2. 그래프를 코드로 옮기는 세 가지 방법
3. DFS — 끝까지 파고들었다가 되돌아온다
4. BFS — 가까운 곳부터 층으로 훑는다
5. 방문 처리를 어디서 하는가
6. BFS는 왜 최단 거리를 보장하는가
7. 시간 복잡도와 공간 복잡도
8. 재귀 DFS의 함정
9. DFS와 BFS 중 무엇을 쓸 것인가
10. 흔한 실수 모음

<br>

---

## 1. 그래프라고 말해 주지 않는 문제들

문제 지문에 "그래프"라는 단어가 나오는 경우는 생각보다 드물다. 대신 이렇게 나온다.

- "N×M 지도에서 상하좌우로 이동할 수 있다"
- "친구의 친구까지 몇 단계 만에 닿는가"
- "물통 A와 B로 만들 수 있는 물의 양"

셋 다 겉모습은 전혀 다른데, 전부 같은 구조다. **어떤 상태가 있고, 그 상태에서 갈 수 있는 다음 상태가 정해져 있다.** 이게 그래프다.

> **[그림]** 격자 지도와 인물 관계망과 상태 전이가 모두 정점과 간선으로 환원되는 세 가지 사례를 나란히 보여주는 그림

용어를 먼저 못 박아 두면 뒤가 편하다.

| 용어 | 의미 | 격자 문제에서는 |
|---|---|---|
| **정점(vertex, node)** | 하나의 상태·지점 | 칸 하나 `(r, c)` |
| **간선(edge)** | 두 정점이 이어져 있다는 사실 | 상하좌우로 붙어 있음 |
| **차수(degree)** | 한 정점에 붙은 간선 수 | 최대 4 (모서리면 2~3) |
| **V, E** | 정점 개수, 간선 개수 | `V = N×M`, `E ≤ 2NM` |
| **무방향 / 방향** | 간선이 양방향인가 한쪽인가 | 보통 양방향 |

**탐색(traversal)** 은 이 그래프의 모든 정점을 빠짐없이, 그리고 중복 없이 한 번씩 훑는 일이다. 빠짐없이는 쉽다. 어려운 건 중복 없이다. 그래프에는 사이클이 있어서 아무 생각 없이 이웃을 따라가면 무한히 맴돈다. 그래서 **탐색 알고리즘의 절반은 "어디로 갈 것인가"이고 나머지 절반은 "어디를 이미 갔는가"다.**

앞의 것을 담당하는 게 자료구조(스택이냐 큐냐)이고, 뒤의 것을 담당하는 게 방문 배열이다. DFS와 BFS의 차이는 오직 **앞의 절반**에서만 갈린다.

> 💡 **"상태"를 정점으로 잡는 순간 범위가 넓어진다**
> 정점이 꼭 지도 위의 칸일 필요는 없다. `(현재 위치, 남은 열쇠 개수)`처럼 여러 값을 묶은 튜플도 정점이 될 수 있다. 이렇게 잡으면 언뜻 그래프로 안 보이는 문제도 전부 탐색 문제가 된다. 이런 상태 공간 설계는 다음 글에서 따로 다룬다.

<br>

---

## 2. 그래프를 코드로 옮기는 세 가지 방법

머릿속의 그림을 코드로 바꾸는 방법은 크게 셋이다. 어떤 걸 고르느냐에 따라 메모리와 순회 비용이 달라진다.

> **[그림]** 인접 행렬과 인접 리스트와 격자 자체 세 가지 그래프 표현 방식의 저장 형태와 비용을 비교한 그림

이 글 전체에서 쓸 예제 그래프를 하나 정해 둔다. 정점 6개, 간선 6개짜리 무방향 그래프다.

```text
간선: (1,2) (1,3) (2,4) (2,5) (3,6) (5,6)

        1
       / \
      2   3
     / \   \
    4   5 — 6
```

### 방법 1 — 인접 행렬

`V × V` 크기의 2차원 배열을 만들고, 간선이 있으면 1을 넣는다.

```python
N = 6
matrix = [[0] * (N + 1) for _ in range(N + 1)]   # 1번부터 쓰려고 +1

edges = [(1, 2), (1, 3), (2, 4), (2, 5), (3, 6), (5, 6)]
for a, b in edges:
    matrix[a][b] = 1
    matrix[b][a] = 1        # 무방향이면 양쪽 다 채운다

matrix[1]      # [0, 0, 1, 1, 0, 0, 0]  ← 1번은 2, 3과 이어져 있다
matrix[1][2]   # 1  ← "1과 2가 이어져 있나?"를 즉시 판정
```

장점은 **두 정점이 이어져 있는지를 `O(1)`에 판정**한다는 것이다. 단점은 메모리다. `V²`칸을 무조건 잡으므로 정점이 1만 개면 1억 칸이고, 이건 파이썬 리스트로는 감당이 안 된다.

그리고 결정적인 단점이 하나 더 있다. **한 정점의 이웃을 순회하려면 그 행 전체를 훑어야 한다.** 이웃이 2개뿐이어도 V칸을 다 본다.

```python
for nxt in range(1, N + 1):
    if matrix[1][nxt]:      # 이웃 2개를 찾으려고 6번 확인
        print(nxt)
```

### 방법 2 — 인접 리스트

각 정점마다 이웃만 리스트로 들고 있는다. **코딩 테스트에서 기본값으로 삼아야 할 방식이다.**

```python
from collections import defaultdict

graph = defaultdict(list)
for a, b in edges:
    graph[a].append(b)
    graph[b].append(a)      # 무방향이면 양쪽에 넣는다

dict(graph)
# {1: [2, 3], 2: [1, 4, 5], 3: [1, 6], 4: [2], 5: [2, 6], 6: [3, 5]}
```

`defaultdict(list)`를 쓰면 키가 없을 때 빈 리스트를 자동으로 만들어 주므로 `if a not in graph: graph[a] = []` 같은 줄이 사라진다. 정점 번호가 1부터 N까지로 촘촘하면 `graph = [[] for _ in range(N + 1)]`로 리스트를 써도 된다. 정점이 문자열이거나 좌표 튜플이면 `defaultdict` 쪽이 편하다.

메모리는 간선 수에 비례하는 `O(V + E)`, 이웃 순회는 **실제 이웃 개수만큼만** 돈다. 대신 "1과 4가 이어져 있나?"를 물으면 `graph[1]`을 훑어야 하므로 그 정점의 차수만큼 시간이 든다.

### 방법 3 — 격자 그 자체

격자 문제는 그래프를 따로 만들지 않는다. **좌표 계산이 곧 간선이다.**

```python
board = [
    [0, 0, 1],
    [1, 0, 1],
    [0, 0, 0],
]
R, C = len(board), len(board[0])

# (r, c)의 이웃 = 상하좌우 네 방향 중 격자 안에 있는 칸
r, c = 1, 1
for dr, dc in ((-1, 0), (1, 0), (0, -1), (0, 1)):
    nr, nc = r + dr, c + dc
    if 0 <= nr < R and 0 <= nc < C:
        print((nr, nc), board[nr][nc])
```

인접 리스트를 만드는 순간 메모리가 4배로 늘 뿐 아무 이득이 없다. 격자는 **필요할 때 즉석에서 이웃을 계산**하는 게 정석이다.

### 셋 중 무엇을 고를 것인가

| 기준 | 인접 행렬 | 인접 리스트 | 격자 자체 |
|---|---|---|---|
| **메모리** | `O(V²)` | `O(V + E)` | `O(V)` (입력 그대로) |
| **간선 존재 확인** | `O(1)` | `O(차수)` | `O(1)` (좌표 계산) |
| **이웃 순회** | `O(V)` — 없는 것도 다 본다 | `O(차수)` | `O(4)` 또는 `O(8)` |
| **전체 탐색 비용** | `O(V²)` | `O(V + E)` | `O(N×M)` |
| **쓸 때** | V가 작고(≤ 1000) 간선이 촘촘할 때 | 대부분의 경우 | 지도·미로 문제 |

> ✅ **판단 한 줄**
> 정점이 1000개를 넘어가면 인접 행렬은 후보에서 빠진다. 격자면 격자 그대로, 나머지는 `defaultdict(list)` 인접 리스트다.

> ⚠️ **무방향 그래프인데 한쪽만 넣는 실수**
> `graph[a].append(b)`만 쓰고 `graph[b].append(a)`를 빼먹으면 방향 그래프가 되어 버린다. 예제 입력은 시작점이 앞쪽 번호라 우연히 통과하고, 제출하면 틀린다. 간선을 넣는 두 줄은 항상 한 세트로 붙여 쓰는 습관을 들이는 게 낫다.

<br>

---

## 3. DFS — 끝까지 파고들었다가 되돌아온다

DFS(Depth First Search, 깊이 우선 탐색)는 **갈 수 있는 데까지 간 뒤, 막히면 갈림길로 되돌아와 다른 길을 시도**하는 방식이다. 미로에서 한 손을 벽에 대고 걷는 것과 비슷하다.

이 동작을 가능하게 하는 자료구조가 **스택**이다. "지금 어디까지 왔는지"를 쌓아 두었다가, 막히면 하나씩 꺼내 되돌아가기 때문이다. 마지막에 들어온 것이 먼저 나오는 LIFO 구조가 "가장 최근 갈림길로 되돌아간다"와 정확히 대응된다.

> **[그림]** 예제 그래프에서 DFS가 진행되는 동안 스택에 정점이 쌓였다가 되돌아오면서 빠지는 과정을 단계별로 보여주는 그림

### 재귀로 구현하기

파이썬에서 가장 짧고 많이 쓰는 형태다. 함수 호출 스택이 곧 탐색 스택 역할을 한다.

```python
visited = set()
order = []

def dfs(node):
    visited.add(node)          # 도착하자마자 방문 처리
    order.append(node)
    for nxt in graph[node]:
        if nxt not in visited:
            dfs(nxt)           # 이웃으로 한 칸 더 들어간다

dfs(1)
order        # [1, 2, 4, 5, 6, 3]
```

읽는 방법은 이렇다. `dfs(1)`이 `graph[1] = [2, 3]`의 첫 이웃 2로 들어간다. `dfs(2)`는 4로 들어가고, 4는 이웃이 2뿐인데 이미 방문했으므로 **거기서 함수가 끝나고 `dfs(2)`로 되돌아온다.** 이 "되돌아옴"이 곧 스택에서 하나 꺼내는 동작이다.

### 명시적 스택으로 구현하기

재귀 없이 리스트를 스택으로 써서 같은 일을 한다.

```python
def dfs_stack(start):
    visited, order = set(), []
    stack = [start]

    while stack:
        node = stack.pop()         # 마지막에 넣은 것부터 꺼낸다
        if node in visited:
            continue               # 중복으로 쌓인 것은 버린다
        visited.add(node)
        order.append(node)

        for nxt in graph[node]:
            if nxt not in visited:
                stack.append(nxt)

    return order

dfs_stack(1)     # [1, 3, 6, 5, 2, 4]
```

### 두 구현의 방문 순서가 다르다

같은 그래프, 같은 시작점인데 결과가 다르게 나왔다.

```text
재귀 DFS      : 1 → 2 → 4 → 5 → 6 → 3
명시적 스택 DFS : 1 → 3 → 6 → 5 → 2 → 4
```

> **[그림]** 재귀 DFS와 스택 DFS가 같은 그래프에서 서로 다른 방문 순서를 만드는 이유를 이웃 처리 방향으로 설명한 그림

이유는 단순하다. `graph[1] = [2, 3]`일 때 **재귀는 2를 먼저 호출**하지만, **스택은 2와 3을 차례로 넣은 뒤 마지막에 넣은 3을 먼저 꺼낸다.** 스택이 순서를 뒤집는 것이다.

재귀와 같은 순서를 원하면 이웃을 거꾸로 넣으면 된다.

```python
for nxt in reversed(graph[node]):
    if nxt not in visited:
        stack.append(nxt)
# 결과: [1, 2, 4, 5, 6, 3] — 재귀와 동일
```

> ⚠️ **"둘 다 DFS인데 답이 다른데요"**
> 정점을 전부 방문한다는 사실은 두 구현 모두 동일하고, 그래서 연결 요소 개수 세기 같은 문제에서는 어느 쪽을 써도 답이 같다. 하지만 **방문 순서 자체가 정답인 문제**(문제에서 "번호가 작은 것부터 방문한다"를 명시하는 경우가 많다)에서는 이 차이가 곧 오답이다. 순서를 요구하는 문제라면 재귀를 쓰거나, 스택에 `reversed()`를 붙인다.

> 💡 **DFS가 잘 맞는 문제**
> "경로가 존재하는가", "이 덩어리의 크기는 얼마인가", "모든 조합을 다 시도해 봐야 한다" 같은 유형이다. 반대로 **최단 거리에는 맞지 않는다.** 먼저 도착한 경로가 짧다는 보장이 전혀 없기 때문이다. 이 이야기는 6번에서 이어진다.

<br>

---

## 4. BFS — 가까운 곳부터 층으로 훑는다

BFS(Breadth First Search, 너비 우선 탐색)는 정반대다. **시작점에서 1칸 거리인 곳을 전부 본 다음, 2칸 거리인 곳을 전부 보고, 그다음 3칸**으로 나아간다. 물에 돌을 던졌을 때 파문이 번지는 모양이다.

이 동작을 만드는 자료구조가 **큐**다. 먼저 들어온 것이 먼저 나오는 FIFO 구조라, 먼저 발견된(= 더 가까운) 정점이 먼저 처리된다.

> **[그림]** 예제 그래프에서 BFS가 거리 0 층과 1 층과 2 층을 차례로 처리하며 큐가 채워지고 비워지는 과정을 보여주는 그림

```python
from collections import deque

def bfs(start):
    visited = {start}          # 시작점은 미리 방문 처리
    q = deque([start])
    order = []

    while q:
        node = q.popleft()     # 앞에서 꺼낸다
        order.append(node)

        for nxt in graph[node]:
            if nxt not in visited:
                visited.add(nxt)   # 큐에 넣을 때 방문 처리 (중요)
                q.append(nxt)      # 뒤에 붙인다

    return order

bfs(1)      # [1, 2, 3, 4, 5, 6]
```

`1`을 꺼내면서 이웃 `2, 3`을 큐에 넣는다. 큐는 `[2, 3]`. `2`를 꺼내면서 `4, 5`를 넣으면 `[3, 4, 5]`. `3`을 꺼내면서 `6`을 넣으면 `[4, 5, 6]`. **1층을 다 처리한 뒤에야 2층이 나온다는 점이 눈으로 보인다.**

### 큐는 반드시 deque로 만든다

파이썬 리스트로도 큐를 흉내 낼 수는 있다. `append()`로 넣고 `pop(0)`으로 꺼내면 된다. **그런데 이걸 쓰면 시간 초과가 난다.**

> **[그림]** 리스트의 pop(0)이 남은 원소를 전부 한 칸씩 당겨 O(N)이 되는 것과 deque가 양끝 포인터만 옮겨 O(1)이 되는 차이를 나타낸 그림

리스트는 메모리에 원소가 연속으로 놓인 구조다. 0번을 빼내면 **나머지 원소 전부를 한 칸씩 앞으로 당겨야** 인덱스가 맞는다. 그래서 `pop(0)`은 `O(N)`이다. 반면 `pop()`(맨 뒤)은 당길 게 없어서 `O(1)`이다.

`deque`는 양쪽 끝에서의 삽입·삭제를 위해 만들어진 구조라 `popleft()`가 `O(1)`이다.

| 연산 | `list` | `deque` |
|---|---|---|
| 뒤에 추가 `append(x)` | `O(1)` | `O(1)` |
| 뒤에서 제거 `pop()` | `O(1)` | `O(1)` |
| **앞에서 제거** | `pop(0)` → **`O(N)`** | `popleft()` → **`O(1)`** |
| 앞에 추가 | `insert(0, x)` → `O(N)` | `appendleft(x)` → `O(1)` |
| 인덱스 접근 `a[i]` | `O(1)` | `O(N)` — 중간 접근은 느리다 |

원소 10만 개를 앞에서부터 전부 꺼내는 데 걸리는 시간을 실제로 재 보면 차이가 분명하다.

```python
import time
from collections import deque

lst = list(range(100_000))
t = time.perf_counter()
while lst:
    lst.pop(0)
print(round(time.perf_counter() - t, 3), "초")   # 0.749 초

dq = deque(range(100_000))
t = time.perf_counter()
while dq:
    dq.popleft()
print(round(time.perf_counter() - t, 4), "초")   # 0.0052 초
```

**약 140배 차이**다. BFS는 큐에서 꺼내는 동작이 정점 수만큼 반복되므로, 리스트를 쓰면 전체가 `O(V²)`로 퇴화한다. 정점 10만 개짜리 문제에서는 그대로 시간 초과다.

> ⚠️ **`deque`는 중간 인덱스 접근이 느리다**
> 표의 마지막 줄이 그것이다. `deque`는 블록 단위 연결 구조라 `dq[500]` 같은 접근이 `O(N)`이다. BFS에서는 양끝만 쓰므로 문제가 없지만, 중간을 자주 읽어야 하는 자료라면 `deque`가 정답이 아니다. **양끝만 건드릴 때 쓰는 도구**로 기억하는 게 정확하다.

> 💡 **`deque([start])`와 `deque(start)`**
> `deque(start)`처럼 정수를 그대로 넘기면 `TypeError`가 난다. `deque`는 반복 가능한 객체를 받아 원소를 하나씩 넣기 때문이다. 반드시 리스트로 감싸 `deque([start])`로 쓰거나, 빈 큐를 만든 뒤 `q.append(start)`로 넣는다.

<br>

---

## 5. 방문 처리를 어디서 하는가

여기가 BFS에서 가장 많이 나오는 버그다. 코드가 돌아가고 답도 맞게 나오는데, 큰 입력에서만 느려지거나 메모리가 터진다. **에러가 안 나기 때문에 찾기가 어렵다.**

방문 처리를 넣을 수 있는 자리는 두 곳이다.

```python
# (A) 큐에 넣을 때 표시  ← 올바른 방식
for nxt in graph[node]:
    if nxt not in visited:
        visited.add(nxt)
        q.append(nxt)

# (B) 큐에서 꺼낼 때 표시  ← 중복이 쌓인다
node = q.popleft()
if node in visited:
    continue
visited.add(node)
for nxt in graph[node]:
    if nxt not in visited:
        q.append(nxt)
```

> **[그림]** 큐에 넣을 때 방문 표시하면 정점이 한 번만 들어가지만 꺼낼 때 표시하면 같은 정점이 여러 번 쌓이는 차이를 보여주는 그림

### 왜 중복이 쌓이는가

(B)에서는 정점이 큐에 **들어가 있는 동안에는 아직 방문 표시가 안 되어 있다.** 그래서 그사이에 다른 정점이 같은 이웃을 발견하면 "아직 방문 안 했네" 하고 또 넣는다.

예제 그래프에서 5와 3이 각각 6을 이웃으로 갖고 있다. (B)로 돌리면 6이 큐에 두 번 들어간다. 정점 6개짜리 그래프에서 1번 더 들어가는 정도라 티가 안 난다.

문제는 간선이 많아질 때다. **정점 100개가 서로 전부 연결된 완전 그래프**에서 큐에 들어간 횟수를 세어 보면 이렇다.

```python
from collections import defaultdict, deque

graph = defaultdict(list)
for a in range(1, 101):
    for b in range(1, 101):
        if a != b:
            graph[a].append(b)

def bfs_push_mark(start):          # (A) 넣을 때 표시
    visited = {start}
    q = deque([start])
    pushed = 1
    while q:
        node = q.popleft()
        for nxt in graph[node]:
            if nxt not in visited:
                visited.add(nxt)
                q.append(nxt)
                pushed += 1
    return pushed

def bfs_pop_mark(start):           # (B) 꺼낼 때 표시
    visited = set()
    q = deque([start])
    pushed = 1
    while q:
        node = q.popleft()
        if node in visited:
            continue
        visited.add(node)
        for nxt in graph[node]:
            if nxt not in visited:
                q.append(nxt)
                pushed += 1
    return pushed

bfs_push_mark(1)    # 100
bfs_pop_mark(1)     # 4951
```

정점 100개짜리 그래프에서 큐에 **4,951번** 들어갔다. 정확히 필요한 횟수의 50배다. 정점이 늘어나면 이 비율이 그대로 커진다. 큐 길이가 정점 수를 한참 넘어가므로 **메모리 초과**로도 이어지고, 꺼내서 버리는 동작이 반복되므로 **시간 초과**로도 이어진다.

| 방식 | 큐에 들어가는 횟수 | 결과 |
|---|---|---|
| **(A) 넣을 때 표시** | 정점당 정확히 1번 | 큐 길이 ≤ V |
| (B) 꺼낼 때 표시 | 정점을 가리키는 간선 수만큼 | 큐 길이가 E까지 부풀 수 있다 |

> ✅ **BFS에서 `visited` 갱신과 `q.append()`는 항상 붙어 있어야 한다**
> 두 줄을 한 덩어리로 외운다. 하나를 쓰고 다른 하나를 안 쓰는 코드는 거의 항상 버그다.

### DFS는 왜 꺼낼 때 표시해도 되는가

앞의 `dfs_stack()`에는 `if node in visited: continue`가 들어 있었다. 즉 꺼낼 때 확인하는 방식이다. DFS는 **방문 순서만 맞으면 되고 거리를 재지 않으므로** 중복이 쌓여도 정답이 틀리지는 않는다. 다만 성능 문제는 똑같이 생기므로, 스택에 넣기 전에도 `if nxt not in visited`로 한 번 걸러 주는 게 좋다. 위 코드가 그렇게 되어 있다.

BFS에서는 성능뿐 아니라 **정답 자체가 걸린다.** 다음 절이 그 이유다.

> ⚠️ **DFS 재귀에서 방문 처리를 함수 밖에서 하면 안 된다**
> `dfs(nxt)`를 호출하기 직전에 부모가 `visited.add(nxt)`를 해도 동작은 하지만, 시작점만 빠뜨리기 쉽다. **함수에 들어가자마자 자기 자신을 표시**하는 형태가 실수가 적다.

<br>

---

## 6. BFS는 왜 최단 거리를 보장하는가

DFS는 최단 거리를 못 구하는데 BFS는 구한다. 이건 우연이 아니라 큐라는 자료구조에서 필연적으로 따라 나오는 성질이다.

### 층 단위로 훑는다는 것

BFS가 정점을 큐에서 꺼내는 순서를 보면, **거리 0인 정점 → 거리 1인 정점들 → 거리 2인 정점들** 순으로 완벽하게 정렬되어 있다.

> **[그림]** BFS가 시작점에서 거리가 같은 정점들을 하나의 층으로 묶어 층 순서대로 처리하기 때문에 처음 도달한 거리가 최단 거리가 됨을 보여주는 그림

근거는 두 문장이다.

1. **큐는 순서를 뒤집지 않는다.** 거리 `d`인 정점들이 큐에 먼저 들어갔다면, 그것들은 거리 `d+1`인 정점들보다 반드시 먼저 꺼내진다.
2. **거리 `d`인 정점을 처리하면서 넣는 이웃은 거리 `d+1`이다.** 그 이웃이 이미 방문되었다면 넣지 않으므로, 큐에는 항상 "가장 앞쪽 층"과 "그다음 층"만 섞여 있다.

이 둘이 맞물리면 큐 안의 거리 값은 항상 오름차순을 유지한다. 그러면 결론이 따라 나온다. **어떤 정점에 처음 도달했을 때의 거리가 그 정점까지의 최단 거리다.** 더 짧은 경로가 있었다면 그 경로는 더 앞쪽 층에 속하므로 이미 먼저 도달했을 것이기 때문이다.

거리를 실제로 기록하려면 방문 배열을 거리 배열로 바꾸면 된다.

```python
from collections import deque

def bfs_dist(start):
    dist = {start: 0}                     # 방문 표시 + 거리를 한 번에
    q = deque([start])

    while q:
        node = q.popleft()
        for nxt in graph[node]:
            if nxt not in dist:           # 아직 도달 안 한 정점만
                dist[nxt] = dist[node] + 1
                q.append(nxt)

    return dist

bfs_dist(1)
# {1: 0, 2: 1, 3: 1, 4: 2, 5: 2, 6: 2}
```

`visited`와 `dist`를 따로 두지 않고 `dist`에 키가 있는지로 방문 여부를 판정한 점을 눈여겨볼 만하다. 배열 두 개를 관리하다 한쪽만 갱신하는 실수를 원천적으로 막는다. 격자 문제라면 `dist`를 `-1`로 채운 2차원 리스트로 두고 `-1`이면 미방문으로 보는 방식이 같은 역할을 한다.

여기서도 **방문 표시는 큐에 넣는 순간 해야 한다.** 꺼낼 때 표시하면 같은 정점이 서로 다른 거리 값으로 여러 번 큐에 들어가고, 나중에 꺼내진 더 큰 값이 앞의 값을 덮어써서 **거리가 틀어진다.** 5번의 버그가 여기서는 성능이 아니라 오답으로 나타난다.

### DFS로는 왜 안 되는가

DFS는 한 방향으로 끝까지 파고든다. 목표 정점에 도달했을 때 그 경로가 짧은 경로였는지 긴 경로였는지 알 방법이 없다. 예제 그래프에서 1에서 6까지 가는 경로는 두 개다.

```text
1 → 3 → 6           (2칸)
1 → 2 → 5 → 6       (3칸)
```

재귀 DFS는 `graph[1] = [2, 3]`의 첫 이웃인 2부터 들어가므로 **3칸짜리 경로로 6에 먼저 도착한다.** 여기서 멈추면 답이 3이 되어 틀린다. DFS로 최단 거리를 구하려면 모든 경로를 끝까지 다 탐색하고 최솟값을 갱신해야 하는데, 이건 경로 수가 지수적으로 늘어나므로 사실상 불가능하다.

> ✅ **한 줄 기준**
> "최소 몇 번 만에", "최단 거리", "가장 빠른"이 지문에 있으면 BFS다. DFS로 시작했다가 나중에 고치는 것보다, 이 단어를 보는 순간 큐를 꺼내는 편이 빠르다.

### 가중치가 붙으면 보장이 깨진다

BFS가 최단 거리를 보장하는 근거는 **모든 간선의 비용이 똑같이 1**이라는 전제였다. "한 칸 이동 = 거리 1"이 성립해야 층이 곧 거리가 된다.

간선마다 비용이 다르면 이 전제가 무너진다.

> **[그림]** 간선 비용이 서로 다른 그래프에서 BFS가 간선 수가 적은 경로를 최단으로 잘못 고르는 반례와 다익스트라가 필요한 이유를 보여주는 그림

```text
1 —(10)— 3          간선 하나로 바로 간다. 비용 10
1 —(1)— 2 —(1)— 3   간선 두 개를 거친다. 비용 2
```

BFS는 1에서 3으로 가는 간선을 발견하는 순간 "거리 1"로 확정하고 더 이상 3을 갱신하지 않는다. 하지만 실제 최소 비용은 2를 거치는 경로의 **2**다. **BFS는 간선의 개수를 세는 알고리즘이지 비용을 재는 알고리즘이 아니다.**

| 상황 | 알맞은 알고리즘 |
|---|---|
| 모든 간선 비용이 같다 (격자 이동, 친구 관계) | **BFS** |
| 간선 비용이 0 또는 1 두 가지뿐이다 | 0-1 BFS (다음 글) |
| 간선 비용이 제각각인 양수다 | **다익스트라** — 우선순위 큐로 "가장 싼 것부터" 꺼낸다 |
| 음수 비용이 섞여 있다 | 벨만-포드 |
| 모든 정점 쌍의 최단 거리가 필요하다 | 플로이드-워셜 |

다익스트라가 BFS의 큐를 우선순위 큐(`heapq`)로 바꾼 형태라는 점을 보면 둘의 관계가 분명해진다. BFS가 "먼저 발견된 것부터" 꺼내는 것을 "지금까지 비용이 가장 싼 것부터" 꺼내는 것으로 바꾸면 다익스트라다. 모든 비용이 1이면 두 기준이 같아지므로, **BFS는 가중치가 균일한 경우의 다익스트라**라고 볼 수 있다.

> 💡 **격자 문제가 BFS로 잘 풀리는 이유**
> 상하좌우 한 칸 이동은 전부 비용 1이다. 전제가 자동으로 만족되므로 격자 최단 경로는 거의 항상 BFS다. 반대로 "이동 시간이 칸마다 다르다"는 조건이 붙는 순간 격자여도 다익스트라로 넘어가야 한다.

<br>

---

## 7. 시간 복잡도와 공간 복잡도

DFS와 BFS의 복잡도는 **완전히 같다.** 둘 다 모든 정점을 한 번씩 방문하고, 각 정점에서 이웃을 한 번씩 확인하기 때문이다.

> **[그림]** 인접 리스트에서는 정점과 간선을 각각 한 번씩만 보아 O(V+E)가 되고 인접 행렬에서는 행 전체를 훑어 O(V제곱)이 되는 차이를 나타낸 그림

### 인접 리스트일 때 — `O(V + E)`

- **정점**: 각 정점을 큐/스택에 한 번 넣고 한 번 꺼낸다 → `O(V)`
- **간선**: 각 정점에서 자기 이웃 리스트를 한 번 훑는다. 모든 정점의 차수 합은 `2E`(무방향)이므로 → `O(E)`

합치면 `O(V + E)`다. 이보다 빠를 수는 없다. 모든 정점을 방문해야 하는 이상 최소 `V`번은 봐야 하기 때문이다.

### 인접 행렬일 때 — `O(V²)`

정점 하나의 이웃을 찾으려고 그 행의 V칸을 전부 확인한다. 정점이 V개이므로 `V × V`다. 실제 간선이 몇 개든 상관없이 `O(V²)`가 된다.

| 표현 방식 | 시간 복잡도 | 공간 복잡도 |
|---|---|---|
| 인접 리스트 | `O(V + E)` | `O(V + E)` |
| 인접 행렬 | `O(V²)` | `O(V²)` |
| 격자 `N×M` | `O(N×M)` | `O(N×M)` |

간선이 아주 촘촘한 그래프(`E ≈ V²`)에서는 두 방식의 시간 복잡도가 같아진다. 하지만 대부분의 문제는 간선이 성긴 편이라 인접 리스트가 유리하다.

### 공간은 어디에 쓰이는가

| 항목 | DFS | BFS |
|---|---|---|
| 그래프 저장 | `O(V + E)` | `O(V + E)` |
| 방문 배열 | `O(V)` | `O(V)` |
| **스택 / 큐 최대 크기** | 최장 경로 길이 (최악 `O(V)`) | 한 층의 최대 크기 (최악 `O(V)`) |

최악의 경우 둘 다 `O(V)`지만 **모양이 다르다.** DFS는 그래프가 한 줄로 길게 늘어질 때(경로 그래프) 스택이 깊어지고, BFS는 그래프가 넓게 퍼질 때(별 모양) 큐가 커진다.

실전에서는 이 차이가 이렇게 나타난다. `1000 × 1000` 격자를 생각해 보자.

- **DFS**: 최악의 경우 100만 칸을 한 줄로 훑는다 → 재귀 깊이 100만. **파이썬에서는 그대로 터진다.**
- **BFS**: 큐에 들어가는 건 한 층뿐이라 대략 대각선 길이 수준. 훨씬 안전하다.

> ✅ **격자가 크면 BFS가 기본값이다**
> 최단 거리를 안 구해도 그렇다. DFS는 재귀 깊이 문제가 있어서 큰 격자에서 위험하다. 다음 절이 그 이야기다.

<br>

---

## 8. 재귀 DFS의 함정

재귀 DFS는 짧고 읽기 좋지만, 파이썬에서는 지뢰가 하나 깔려 있다.

### 기본 재귀 한계는 1000이다

```python
import sys
sys.getrecursionlimit()      # 1000
```

이 1000은 "재귀 함수를 999번 호출할 수 있다"는 뜻이 아니다. **파이썬 인터프리터 전체의 호출 스택 깊이 상한**이다. 이미 여러 함수 안에 들어와 있으면 그만큼 여유가 줄어든다.

한계를 넘으면 이렇게 끝난다.

```python
def deep(n):
    return 0 if n == 0 else deep(n - 1) + 1

deep(2000)
# RecursionError: maximum recursion depth exceeded
```

문제는 DFS의 최대 재귀 깊이가 **정점 개수만큼 될 수 있다**는 것이다. 정점 1000개짜리 그래프가 한 줄로 이어져 있으면 그대로 한계에 걸린다. `100 × 100` 격자만 되어도 최악 깊이가 1만이다.

### 1차 대응 — 한계를 올린다

```python
import sys
sys.setrecursionlimit(10 ** 6)      # 보통 문제 첫 줄에 박아 둔다
```

대부분의 문제는 이 한 줄로 해결된다. 다만 이건 **파이썬이 스스로 걸어 둔 안전장치를 푸는 것**이지 실제 스택 메모리를 늘리는 게 아니다. 값을 아주 크게 잡고 실제로 그만큼 깊이 들어가면 운영체제의 스택 한계에 부딪혀 **프로세스가 통째로 죽는다.** 채점 결과로는 `RecursionError`가 아니라 런타임 에러나 메모리 초과로 뜨기 때문에 원인을 찾기 어렵다.

### 2차 대응 — 반복문 스택으로 바꾼다

깊이가 정말 깊을 게 예상되면(격자 크기가 크거나 정점이 10만 단위) 처음부터 명시적 스택으로 쓴다.

```python
def dfs_stack(start):
    visited = {start}
    stack = [start]

    while stack:
        node = stack.pop()
        for nxt in graph[node]:
            if nxt not in visited:
                visited.add(nxt)
                stack.append(nxt)

    return visited
```

리스트는 힙 메모리에 잡히므로 호출 스택 한계와 무관하다. 깊이가 100만이어도 안전하다.

| 대응 | 언제 | 한계 |
|---|---|---|
| 그대로 재귀 | 깊이가 확실히 1000 미만 | — |
| `setrecursionlimit` | 깊이 수만 수준 | 너무 크면 프로세스가 죽는다 |
| **반복문 스택** | 깊이 예측이 안 되거나 아주 깊을 때 | 코드가 조금 길어진다 |
| **BFS로 교체** | 애초에 순서 상관없이 다 훑기만 하면 될 때 | 최단 거리도 덤으로 얻는다 |

> 💡 **"전부 훑기만 하면 되는" 문제는 BFS가 더 안전하다**
> 연결 요소 개수 세기, 덩어리 크기 구하기처럼 방문 순서가 답에 영향을 주지 않는 문제는 DFS로도 BFS로도 풀린다. 이때 파이썬에서는 **BFS를 고르는 편이 낫다.** 재귀 깊이 걱정이 없고, 코드 길이도 거의 같다.

> ⚠️ **재귀 함수 안에서 큰 리스트를 복사하지 않는다**
> `dfs(path + [node])`처럼 매 호출마다 리스트를 새로 만들면 깊이 × 길이만큼 메모리가 든다. 하나의 리스트를 공유하면서 `path.append()` / `path.pop()`으로 되돌리는 방식이 정석이다. 이 패턴은 백트래킹에서 본격적으로 쓰이므로 다음 글에서 다룬다.

<br>

---

## 9. DFS와 BFS 중 무엇을 쓸 것인가

여기까지 왔으면 기준은 이미 다 나왔다. 정리하면 이렇다.

> **[그림]** DFS와 BFS의 자료구조와 진행 방향과 최단 거리 보장 여부와 메모리 특성을 나란히 비교한 표 형태의 그림

| 기준 | DFS | BFS |
|---|---|---|
| **자료구조** | 스택 (재귀 호출 스택 포함) | 큐 (`collections.deque`) |
| **진행 방향** | 한 갈래를 끝까지 판다 | 가까운 층부터 넓게 편다 |
| **최단 거리** | **보장 못 한다** | **보장한다** (간선 비용이 모두 같을 때) |
| **메모리 모양** | 경로가 길면 깊어진다 | 층이 넓으면 커진다 |
| **파이썬 재귀 한계** | 걸린다 | 무관 |
| **구현 길이** | 재귀면 아주 짧다 | 비슷하다 |
| **시간 복잡도** | `O(V + E)` | `O(V + E)` |

### 상황별 선택

| 문제가 이렇게 물으면 | 고르는 것 |
|---|---|
| "최소 몇 번 만에", "최단 거리", "최소 이동 횟수" | **BFS** |
| "경로가 존재하는가", "도달할 수 있는가" | 둘 다 가능 (BFS가 안전) |
| "덩어리가 몇 개인가", "영역의 크기는" | 둘 다 가능 (BFS가 안전) |
| "가능한 모든 경우를 만들어라" (순열·조합) | **DFS** (백트래킹) |
| "사이클이 있는가", "위상 정렬" | **DFS** 계열 |
| 격자가 아주 크다 (`1000 × 1000` 이상) | **BFS** — 재귀 깊이 위험 |
| 트리에서 부모·자식 관계를 타고 계산 | **DFS** — 자식 결과를 모아 올리기 좋다 |

> ✅ **애매하면 BFS**
> 파이썬 한정 실무 기준이다. 재귀 깊이 사고가 없고, 최단 거리를 덤으로 얻고, 성능도 같다. DFS를 꼭 써야 하는 건 **경로 자체를 만들어 나가는 유형**(백트래킹)과 **자식의 결과가 부모에게 필요한 유형**(트리 DP) 정도다.

<br>

---

## 10. 흔한 실수 모음

에러 메시지 없이 조용히 틀리는 것들만 모았다.

| 실수 | 증상 | 고치는 법 |
|---|---|---|
| 큐를 리스트로 만들고 `pop(0)` | 큰 입력에서만 시간 초과 | `collections.deque` + `popleft()` |
| BFS 방문 처리를 꺼낼 때 | 메모리 초과, 거리 값 오염 | 큐에 **넣을 때** 표시 |
| 시작점 방문 처리를 빼먹음 | 시작점이 다시 큐에 들어간다 | `visited = {start}`로 초기화 |
| 무방향인데 간선을 한쪽만 등록 | 예제는 맞고 제출은 틀림 | 두 줄을 한 세트로 |
| 격자 범위 검사 순서가 뒤바뀜 | `IndexError` 또는 잘못된 값 | 범위 검사 먼저, 값 검사 나중 |
| 재귀 깊이 초과 | `RecursionError`, 런타임 에러 | `setrecursionlimit` 또는 반복문 스택 |
| 방문 배열과 거리 배열을 따로 관리 | 한쪽만 갱신되어 답이 틀림 | `dist` 하나로 겸용 |
| DFS로 최단 거리를 구함 | 답이 최솟값보다 크게 나옴 | BFS로 교체 |
| 가중치 그래프에 BFS | 간선 수만 세어 답이 틀림 | 다익스트라 |
| `defaultdict`를 조회만 했는데 키가 생김 | `len(graph)`가 예상과 다름 | 확인만 할 땐 `in` 또는 `.get()` |

### 격자 범위 검사 순서

마지막에서 두 번째 줄을 조금 더 보자. 이런 코드는 틀린다.

```python
# 잘못된 순서 — 범위를 벗어난 인덱스로 먼저 접근한다
if board[nr][nc] == 0 and 0 <= nr < R and 0 <= nc < C:
    ...
```

파이썬의 `and`는 왼쪽부터 평가하므로 `board[nr][nc]`가 먼저 실행된다. `nr`이 `-1`이면 파이썬은 에러 대신 **맨 뒷줄을 반환**해 버려서, 에러도 안 나고 답만 조용히 틀린다.

```python
# 올바른 순서 — 범위 검사가 먼저
if 0 <= nr < R and 0 <= nc < C and board[nr][nc] == 0:
    ...
```

`and`는 왼쪽이 거짓이면 오른쪽을 아예 평가하지 않는다(단축 평가). 이 성질에 기대는 순서다.

> ⚠️ **음수 인덱스가 에러를 안 낸다는 게 핵심이다**
> C나 자바였으면 그 자리에서 터져서 바로 알았을 버그가, 파이썬에서는 통과해 버린다. 격자 문제에서 "왜 답이 조금씩 다르지" 싶을 때 첫 번째로 의심할 곳이다.

### 시작점 처리

```python
# 잘못된 초기화 — 시작점이 표시되지 않았다
visited = set()
q = deque([start])

# 올바른 초기화
visited = {start}
q = deque([start])
```

첫 번째 코드에서는 시작점의 이웃을 처리할 때 그 이웃이 다시 시작점을 이웃으로 갖고 있으므로(무방향 그래프) 시작점이 큐에 또 들어간다. 거리를 재는 BFS라면 `dist[start]`가 2로 덮어써지면서 이후 계산이 전부 어긋난다.

<br>

---

## ✅ 핵심 요약

| 주제 | 꼭 기억할 것 |
|---|---|
| **탐색의 본질** | 어디로 갈지(스택/큐)와 어디를 갔는지(방문 배열), 두 부분으로 나뉜다 |
| **그래프 표현** | 격자는 격자 그대로, 나머지는 `defaultdict(list)`. 인접 행렬은 V ≤ 1000일 때만 |
| **DFS** | 스택. 재귀 구현과 명시적 스택 구현의 **방문 순서가 다르다** (스택이 뒤집는다) |
| **BFS** | 큐. **반드시 `collections.deque`** — `list.pop(0)`은 `O(N)`이라 전체가 `O(V²)`가 된다 |
| **방문 처리 위치** | BFS는 **큐에 넣을 때** 표시한다. 꺼낼 때 하면 같은 정점이 중복으로 쌓인다 |
| **최단 거리 보장** | 큐가 거리 순으로 층을 훑기 때문. **처음 도달한 거리가 곧 최단 거리** |
| **보장이 깨지는 곳** | 간선 비용이 다르면 BFS는 간선 수만 센다 → 다익스트라 |
| **복잡도** | 인접 리스트 `O(V + E)`, 인접 행렬 `O(V²)`. DFS와 BFS는 서로 같다 |
| **재귀 한계** | 기본 1000. `sys.setrecursionlimit(10 ** 6)` 또는 반복문 스택으로 전환 |
| **선택 기준** | "최단"이면 BFS, "모든 경우"면 DFS. 애매하면 파이썬에서는 BFS가 안전하다 |
| **격자 범위 검사** | 범위 확인을 **먼저**, 값 확인을 나중에. 음수 인덱스는 에러 없이 통과한다 |

<br>

> 원리를 잡았으면 이제 문제다. 격자 탐색·최단 경로 복원·다중 시작점·백트래킹 같은 실전 유형은 [DFS와 BFS 문제 유형](0825_DFS와BFS_문제유형.md) 편에서 이어진다.

<br>

## 🔗 참고 자료

- [파이썬 공식 문서 — collections.deque](https://docs.python.org/ko/3/library/collections.html#collections.deque)
- [파이썬 공식 문서 — sys.setrecursionlimit](https://docs.python.org/ko/3/library/sys.html#sys.setrecursionlimit)
- [파이썬 시간 복잡도 정리 (Python Wiki)](https://wiki.python.org/moin/TimeComplexity)
- [파이썬 공식 문서 — heapq](https://docs.python.org/ko/3/library/heapq.html)
