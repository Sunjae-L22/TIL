# 이미지 딥러닝 모델 (CNN과 모델 변천사)

> 출처: `3_1_2_다양한신경망모델.pdf` (총 117페이지, SSAFY · KAIST 심현정 교수) · 정리일: 2026-08-15
> 페이지 표기는 PDF 기준 (슬라이드 인쇄 번호와 동일)
> 파일명은 "다양한 신경망 모델"이지만 실제 내용은 **이미지 딥러닝 한 차시**다. RNN·GAN·트랜스포머는 나오지 않고, CNN의 구조와 CNN 기반 모델 4개(AlexNet → VGG → ResNet → MobileNet)의 변천사를 다룬다.

## 한눈에 보기

- FCN에 이미지를 넣으면 무엇이 깨지는가 → 왜 합성곱 레이어가 필요한가
- 출력 해상도·메모리·파라미터 수·FLOPs를 손으로 계산할 수 있는가 (공식 4개면 AlexNet 표 전체가 재현된다)
- 변천사의 축은 세 번 바뀐다: **깊이를 쌓는 법**(AlexNet→VGG) → **깊어도 학습되게 하는 법**(ResNet) → **가볍게 만드는 법**(MobileNet)
- 각 모델을 "구조 → 무엇을 해결했나 → 남긴 한계"의 틀로 반복해서 읽는다
- 세 쌍의 트레이드오프: 풀링 vs 스트라이드 합성곱 / 일반 잔차 vs 보틀넥 / 일반 합성곱 vs 깊이별 분리 합성곱

## 목차

1. [왜 이미지에는 CNN인가](#왜-이미지에는-cnn인가) (p.5-11)
2. [합성곱 연산의 해부](#합성곱-연산의-해부) (p.12-32)
3. [CNN을 완성하는 구조 요소](#cnn을-완성하는-구조-요소) (p.33-47)
4. [모델 변천사 개관](#모델-변천사-개관) (p.48-49)
5. [AlexNet 딥러닝의 시작점](#alexnet-딥러닝의-시작점) (p.50-76)
6. [VGGNet 깊이와 규칙](#vggnet-깊이와-규칙) (p.77-88)
7. [ResNet 깊이의 학습 가능성](#resnet-깊이의-학습-가능성) (p.89-103)
8. [MobileNet 경량화](#mobilenet-경량화) (p.104-109)
9. [복습 문제](#복습-문제) (p.110-115)

---

## 왜 이미지에는 CNN인가

### 어디에 쓰이나 (p.7-8)

산업 현장에서는 금형 표면 스크래치·식품/의약품 이물질 검출, PCB 불량 자동 판정(불완전 접합, 플럭스 잔여물, 미세 크랙) 같이 육안으로 구분하기 힘든 결함을 잡고, 일상에서는 성동구 '안심우회전시설'처럼 AI 카메라가 차량·보행자를 실시간 감지한다.

### FCN에 이미지를 넣으면 (p.11)

완전 연결층(Fully-Connected Layer)은 입력을 받아 출력으로 변환하는 신경망의 기본 모듈이다. 32×32×3 이미지(높이·너비 32픽셀, RGB 3채널, 값 0~255)를 넣으려면 **모든 화소를 나열해 1차원 벡터로 펴야(Flatten)** 한다.

```text
32x32x3 (3차원)  --Flatten-->  1x3072 (1차원)  --W(10x3072)-->  1x10 (10개 중 1개로 분류)
```

모델 상수(파라미터) $W$는 10×3072 — 입력의 **모든** 화소 값이 출력에 미치는 영향을 하나하나 학습한다. 문제는 펴는 순간 "어떤 화소가 어떤 화소 옆에 있었는지"라는 공간 구조가 사라진다는 것. 이 한계가 2절 이후 CNN의 모든 설계를 정당화한다.

---

## 합성곱 연산의 해부

### 필터와 내적 (p.12-16)

합성곱 레이어(Convolution Layer)는 **입력 이미지를 필터와 연산하여 특징 맵(feature map)을 뽑아내는 모듈**이다. FCN과 달리 3차원 구조를 그대로 보존하면서 연산한다.

- 입력 3×32×32에 필터 **3×5×5** — 필터는 항상 **입력의 깊이/채널 수와 동일한 차원**이어야 한다 (차원 주의, 시험 포인트)
- 특정 위치에서의 출력 하나 = 필터와 같은 크기의 이미지 영역 간 내적 $w^\top x + b$ ($3 \times 5 \times 5 = 75$차원 내적 + 편향). $x$는 필터가 덮는 이미지 영역, $w$는 필터의 가중치, $b$는 편향(보정값)
- 필터를 이미지 위에서 이동시키며 내적을 반복 → 모든 값을 모은 것이 **활성화 맵(Activation Map)**. 32×32 입력에 5×5 필터면 28×28이 나온다

### 예시로 감 잡기 박스필터 (p.17-25)

모든 원소가 $\frac{1}{9}$인 3×3 필터를 90 값 덩어리(+노이즈 화소)가 있는 10×10 입력 위에 슬라이드시키면, 출력에는 주변 9칸의 평균이 찍힌다. 경계는 0→10→20→…으로 부드러워지고 한가운데 섞인 노이즈(90 사이의 0 하나, 배경의 90 하나)는 뭉개진다. **필터 = 지역 패턴을 요약하는 연산**이라는 직관.

### 출력 해상도 공식 (p.26-29)

$$\text{출력 해상도} = \text{입력 해상도} - \text{필터 해상도} + 1$$

- 예: $32 - 5 + 1 = 28$, $10 - 3 + 1 = 8$ (p.28의 격자 예시)
- 입출력 해상도를 유지하고 싶다면 → **패딩(Padding)**: 정의되지 않는 출력 위치를 0 혹은 가장 가까운 값으로 채운다 (p.29)
- 뒤의 AlexNet 계산까지 일반화하면 (스트라이드 $S$, 패딩 $P$ 포함):

$$H' = \frac{H - K + 2P}{S} + 1$$

### 필터 여러 개와 배치 (p.26-32)

- 필터가 6개(6×3×5×5)면 활성화 맵도 6장 → 출력 6×28×28. 필터 수가 곧 **출력 채널 수**
- 바이어스는 필터당 1개, 6×1 벡터로 추가 (p.30)
- 이미지 $N$장을 배치로: 입력 $N \times C_{in} \times H \times W$ → 출력 $N \times C_{out} \times H' \times W'$ (p.32)

> **주의** — p.31 슬라이드 하단의 출처 표기("https://www.anthropic.com/claude")는 그림(배치 합성곱 모식도)과 무관한 잘못된 인용으로 보인다. 해당 그림 계열은 통상 미시간대 EECS 498(Justin Johnson) 강의 자료 형식이다.

---

## CNN을 완성하는 구조 요소

### 중첩과 비선형 (p.34-36)

합성곱 레이어를 여러 개 쌓아 모델 상수를 늘릴 수 있다.

```text
입력 Nx3x32x32 --Conv W1: 6x3x5x5,  b1: 6  --> Nx6x28x28   (첫 번째 히든 레이어)
              --Conv W2: 10x6x3x3, b2: 10 --> Nx10x26x26  (두 번째 히든 레이어)
              --Conv W3: 12x10x3x3, b3: 12 --> ...
```

그런데 선형 변환만 반복하면 $W_{all} = W_3 W_2 W_1$로 접혀서 **결국 하나의 선형 분류기**와 같다. 그래서 사이마다 비선형 활성화를 끼운다: **(Conv[선형] + ReLU[비선형]) 반복**이 모델링 파워를 만든다.

### 학습된 필터가 보는 것 (p.37)

학습된 필터를 시각화하면 구조별로 배우는 게 다르다: **선형 모델**은 각 카테고리의 템플릿(전형적 모습) 1장씩(plane, car, bird, …), **FCN**은 다양한 템플릿 제공, **CNN**은 **계층별로 이미지의 지역적 템플릿**(예: 엣지, 색상 등)을 학습한다.

### 수용영역 (p.38-39)

- **수용영역(Receptive Field)**: CNN이 이미지를 처리하면서 한 번에 볼 수 있는 영역의 크기 — "네트워크의 시야"
- CNN 레이어는 지역 특징 추출에 유리하게 설계됐지만, 실제 작업에는 이미지 전체 맥락도 필요 → **지역 정보 + 전체 맥락**을 함께 잡는 설계가 필요
- 확장 방법: Conv layer를 깊게 쌓기(deep stacking), Pyramid structure, Pooling, Stride, Dilated Conv 등. 일반적으로 **깊어질수록 수용영역이 넓어진다**
- 고해상도 이미지는 수용영역을 키우려면 레이어를 너무 많이 통과해야 해서 비실용적 → 실전 해법은 **입력 사이즈를 줄여서 넣는 것** (p.39)

### 풀링 (p.40-42)

풀링의 두 가지 효과:

1. **연산 효율** — 해상도를 절반으로 줄이면 연산량이 크게 준다. 예: 64채널 224×224 → 112×112면 1.85 GFLOPs → 0.46 GFLOPs, **약 4배 차이** (p.40)
2. **위치 변화의 강건성** — 저해상도 정보에 근거해 작업하므로 객체가 몇 화소 이동해도 동일한 출력 (p.41)

**맥스풀링**: 정해진 커널 사이즈(예: 2×2)로 이미지를 나눠 각 영역의 최댓값만 선택 (p.42).

```text
입력 단일 채널        2x2씩 나눠 최댓값       출력
1 1 | 2 4
5 6 | 7 8    ->      6 | 8
----+----            --+--
3 2 | 1 0            3 | 4
1 2 | 3 4
```

풀링에는 **학습할 모델 상수가 없다**(비학습 연산). 이 점이 확인문제 3에도 나온다.

### 스트라이드 합성곱 (p.43-47)

- 일반 합성곱은 필터를 1칸씩, 스트라이드 합성곱은 **스트라이드 값만큼(예: 2칸)** 이동한 후 출력을 계산
- 풀링처럼 입력 해상도를 줄인다 (연산 효율 + 위치 강건성)

| | 풀링 | 스트라이드 합성곱 |
|---|---|---|
| 학습 상수 | 없음 | **커널을 동시에 학습** |
| 정보 손실 | 해상도 저하로 손실 | 상대적으로 적음 |
| 레이어 수 | 합성곱 + 풀링 2개 | **하나의 레이어로 대체** |

고도화된 CNN에서는 풀링 대신 스트라이드 합성곱을 활용하는 경향이 있다 (p.43).

---

## 모델 변천사 개관

ImageNet 분류 에러율(%)로 본 2010~2017 (p.49). 세로축은 낮을수록 좋다.

```text
28.2   25.8   16.4    11.7    7.3     6.7      3.6     3      2.3    | 5.1
2010   2011   2012    2013    2014    2014     2015    2016   2017   | Human
Lin    Sanchez AlexNet Zeiler& VGG     GoogLeNet ResNet  Shao   SENet |
(Shallow 얕은 모델)  (8층)   Fergus  (19층)  (22층)   (152층) (152층) (152층)
```

- **2012 AlexNet에서 에러가 25.8→16.4로 급락**하며 딥러닝 시대가 열리고, 2015 ResNet(152층)이 **인간 수준(5.1%)을 넘어선다**
- 층수 흐름: 얕은 모델 → 8층 → 19/22층 → 152층. "깊이와 효율성을 동시에 높이는 방향"의 역사 (p.6)

이후 4개 절은 이 차트의 봉우리 4개를 하나씩 해부한다. 각 절은 **구조 → 무엇을 해결했나 → 한계** 순서로 읽는다.

---

## AlexNet 딥러닝의 시작점

### 구조 (p.52-56)

**5개의 합성곱 계층과 3개의 완전연결 계층으로 구성된 8계층 CNN 모델** (2012, Krizhevsky et al. 에러율 16.4%).

- 구성 요소: 5개 합성곱 레이어 + 맥스 풀링 + 3개 연결층 레이어 + ReLU 비선형 활성화
- 입력: 이미지 3×227×227 — 논문 그림에는 224로 되어 있으나 **227로 정정**되었다 (p.55)
- 출력: ImageNet 1000개 클래스에 대한 확률 분포 (FC3 → Softmax)
- 학습 시 정답은 원-핫 벡터(원소 1개만 '1'), 예측값은 각 클래스에 대한 0~1 사이 확률

> **자료 오류 주의** — p.53(그리고 VGG p.79, ResNet p.91)에는 출력이 "1×1024 벡터"로 적혀 있는데, 같은 자료의 p.67("FC3 → Softmax, **1000 클래스**")·p.73 표(FC3 출력 채널 **1000**)·p.80 그림(FC 1000)과 모순된다. ImageNet 분류 기준 **1000이 맞다.**

당시 GPU(GTX 580)는 메모리가 3GB뿐이라 **GPU 2개에 Conv/FC 레이어를 분산**(채널·뉴런을 반씩 나눠 계산)했고, 이것이 Conv2/4/5의 그룹 합성곱으로 남아 있다 (p.55, 73). 요즘 GPU(RTX 3090, A100, H100 — 24~80GB VRAM)라면 한 장으로 충분한 구조다 (p.56).

### 단계별 데이터 흐름 (p.56-67)

p.56 아키텍처 그림을 문자로 옮기면:

```text
입력 227x227x3
 → Conv1 11x11, s=4, 96개        → 55x55x96    ((227-11)/4+1 = 55)
 → MaxPool 3x3, s=2 (겹침 풀링)   → 27x27x96    ((55-3)/2+1 = 27)
 → Conv2 5x5, pad=2, 256개       → 27x27x256   ((27+2*2-5)/1+1 = 27, 패딩으로 유지)
 → MaxPool 3x3, s=2              → 13x13x256   ((27-3)/2+1 = 13)
 → Conv3 3x3, pad=1, 384개       → 13x13x384
 → Conv4 3x3, pad=1, 384개       → 13x13x384
 → Conv5 3x3, pad=1, 256개       → 13x13x256
 → MaxPool 3x3, s=2              → 6x6x256     ((13-3)/2+1 = 6)
 → Flatten                        → 9216        (6*6*256)
 → FC1 4096 → FC2 4096 → FC3 1000 → Softmax
```

패턴이 보인다: 초반에는 **큰 필터+큰 스트라이드로 해상도를 강하게 줄이고**(Conv1), 중반은 패딩으로 해상도를 유지하며 채널을 늘리고, 풀링 3번으로 227→55→27→13→6까지 압축한 뒤 FC로 넘긴다.

### 비용 계산법 세 가지 공식 (p.68-72)

Conv1을 예로 층 하나의 비용을 손으로 계산하는 방법. 이 세 공식만 알면 아래 전체 표를 재현할 수 있다.

- **① 출력 활성 메모리** — 각 활성값을 float32(4 Byte)로 저장한다고 가정: $C' \cdot H' \cdot W' = 96 \cdot 55 \cdot 55 = 290{,}400$ → ×4 Byte = **1,134 KB**
- **② 모델 상수(파라미터) 수** — 필터 웨이트 + 바이어스: $C' \cdot C \cdot K^2 + C' = 96 \cdot 3 \cdot 11 \cdot 11 + 96 = 34{,}944$ (**34.9K**)
- **③ FLOPs(연산량)** — 출력 원소 수 × 원소별 연산(곱셈·덧셈): $(H' \cdot W') \cdot (C \cdot K^2 \cdot C') \cdot 2 = (55 \cdot 55)(3 \cdot 11 \cdot 11 \cdot 96) \cdot 2 = 105{,}415{,}200$ (**105.4M**)

### AlexNet 전체 표 (p.73)

당시 Conv2/4/5에 그룹 합성곱(2 GPU 병렬)을 적용해 해당 층 연산량이 절반이다. 표는 원문 그대로:

| Layer | C입력 | H/W입력 | F(필터) | K | S | P | C출력 | H/W출력 | 메모리(KB) | 상수(k) | 연산(FLOPs) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Conv1 | 3 | 227 | 96 | 11 | 4 | 0 | 96 | 55 | 1,134 | 34.9 | 105.4M |
| Pool1 | 96 | 55 | - | 3 | 2 | 0 | 96 | 27 | 273 | 0 | 0 |
| Conv2 | 96 | 27 | 256 | 5 | 1 | 2 | 256 | 27 | 729 | 307.5 | 224M |
| Pool2 | 256 | 27 | - | 3 | 2 | 0 | 256 | 13 | 169 | 0 | 0 |
| Conv3 | 256 | 13 | 384 | 3 | 1 | 1 | 384 | 13 | 254 | 885 | 149.5M |
| Conv4 | 384 | 13 | 384 | 3 | 1 | 1 | 384 | 13 | 254 | 664.0 | 112.1M |
| Conv5 | 384 | 13 | 256 | 3 | 1 | 1 | 256 | 13 | 169 | 443.0 | 74.7M |
| Pool5 | 256 | 13 | - | 3 | 2 | 0 | 256 | 6 | 36 | 0 | 0 |
| Flatten | - | - | - | - | - | - | 9216 | - | 36 | 0 | 0 |
| FC1 | 9216 | - | - | - | - | - | 4096 | - | 16 | 37,752 | 37.7M |
| FC2 | 4096 | - | - | - | - | - | 4096 | - | 16 | 16,781 | 16.7M |
| FC3 | 4096 | - | - | - | - | - | 1000 | - | 4 | 4,097 | 4.1M |

### 표에서 읽는 세 가지 분포 인사이트 (p.74-76)

1그룹 기준 막대그래프 셋의 결론:

- **메모리 사용은 초기 레이어에 집중** — Conv1 1,134KB가 최대, 뒤로 갈수록 줄어든다 (해상도가 크니까)
- **모델 상수는 완전연결 레이어에 집중** — FC1 혼자 37,753K. 9216×4096 행렬 하나가 전체 파라미터의 절반 이상
- **FLOPs는 합성곱에서 주로 발생** — Conv2 447.9M이 최대, FC는 커봐야 37.7M

→ "무거움"의 종류가 층마다 다르다. 이 관찰이 이후 모델들의 개선 포인트가 된다 (VGG는 FC를 그대로 둬서 파라미터 폭발, ResNet은 FC를 제거, MobileNet은 Conv 연산을 분해).

### 무엇을 해결했나, 한계는 (p.111, p.117)

- **해결**: 기존 전통 알고리즘의 성능 한계 — "딥러닝이 정말 이미지에서 통할까?"를 증명. GPU 기반 대규모 CNN의 첫 성공, ReLU 도입, Dropout을 통한 과적합 억제
- **한계**: 11×11, 5×5, 3×3이 뒤섞인 임기응변식 설계(디자인 룰 부재), FC에 몰린 거대한 파라미터. 다음 두 모델이 각각 이 지점을 판다
- 응용 예: 사진 자동 분류, 반려동물 종류 분류 앱, 얼굴/사물 분류의 초기 기술 기반. 논문 총 인용수 162,207회 — 다윈 종의 기원(64,798)보다 많다 (p.54)

---

## VGGNet 깊이와 규칙

### 구조 (p.78-80)

2014년 Simonyan & Zisserman. 에러율 7.3%(VGG-19 기준 19층). VGG-16 기준 구조 특징 (p.79):

- 5개 합성곱 블록 (각 블록 = 합성곱-ReLU 반복) + 각 블록 끝 맥스 풀링
- 3개 연결층 레이어, ReLU 비선형 활성화, 최종 소프트맥스
- **총 16개의 합성곱/연결층 레이어** (합성곱 13 + FC 3)
- 입력: 3×224×224 / 출력: 1000 클래스 (p.79 원문의 "1×1024"는 앞서 언급한 자료 오류)

```text
VGG-16:  [3x3 conv 64] x2 → Pool → [3x3 conv 128] x2 → Pool → [3x3 conv 256] x3 → Pool
         → [3x3 conv 512] x3 → Pool → [3x3 conv 512] x3 → Pool → FC 4096 → FC 4096 → FC 1000 → Softmax
VGG-19:  256/512 블록에서 합성곱이 하나씩 늘어 19층
```

### 디자인 룰 세 가지 (p.82-84)

AlexNet의 임기응변식 설계를 세 줄 규칙으로 대체했다:

1. **3×3 합성곱 / 스트라이드 1 / 패딩 1**만 반복 적용
2. **맥스풀링(2×2)** 으로만 크기를 줄인다
3. **풀링 후 채널 크기 2배** (64→128→256→512)

왜 3×3만으로 충분한가 — **3×3 합성곱 두 개를 연달아 적용하면 5×5 합성곱과 동일한 리셉티브 필드**를 확보하면서 연산량은 줄어든다 (p.83):

| | Option 1: 합성곱(5×5, C→C) | Option 2: 합성곱(3×3, C→C) 두 번 |
|---|---|---|
| Params | $25C^2$ | $18C^2$ |
| FLOPs | $25C^2HW$ | $18C^2HW$ |

게다가 사이에 ReLU가 한 번 더 들어가니 비선형성도 늘어난다. "풀링 후 채널 2배" 룰은 해상도가 절반이 될 때 채널을 2배로 늘려 **레이어별 연산량을 비슷하게 유지**하는 장치다 — AlexNet은 층마다 연산량 편차가 컸다 (p.84).

### AlexNet과의 비교 거대한 모델 (p.85-87)

| 항목 | AlexNet | VGG-16 | 배율 |
|---|---|---|---|
| 메모리(활성값) | 1.9MB | 48.6MB | 약 25배 |
| 모델 상수 | 61M | 138M | 약 2.3배 |
| 연산량 | 0.7GFLOPs | 13.6GFLOPs | 약 19.4배 |

층별로 보면 VGG-16의 FC6 파라미터가 102.8M으로 단일 층 최대이고(AlexNet FC1은 37.753M), 연산은 Conv4 블록 4.60GFLOPs가 최대다. **메모리·연산의 병목은 초반 고해상도 conv, 파라미터의 병목은 FC**라는 AlexNet의 패턴이 그대로, 더 크게 반복된다.

### 무엇을 해결했나, 한계는 (p.81, 88, p.117)

- **해결**: "네트워크를 깊게 하면 얼마나 좋아지는가?" 실험 — **작고 단순한 필터를 깊게 쌓으면 성능이 오른다**는 새로운 설계 철학 제시
- 단순·규칙적 설계 덕분에 모델 해석에 이상적이고, 특징 추출기·전이학습(Transfer Learning)의 강력한 베이스라인이 됐다
- **한계**: 파라미터가 많고 연산량 요구가 매우 크다 (138M, 13.6GFLOPs). 그리고 "그냥 더 깊게"는 곧 벽에 부딪힌다 → ResNet의 등장 배경
- 응용 예: 사진 스타일별 분류 앱, 패션 이미지 기반 추천 시스템, 전이학습 기반 실무 프로젝트

---

## ResNet 깊이의 학습 가능성

### 등장 배경 깊다고 잘 되는 게 아니다 (p.90-93)

2015년 He et al. 에러율 3.6%, 152층. 배경: 배치 정규화 방식으로 10+ 레이어 학습이 가능해진 시점에서, 더 깊게 쌓아봤더니 —

- **20-layer 모델**: 학습이 안정적으로 진행되며 에러가 꾸준히 감소
- **56-layer 모델**: 더 깊음에도 불구하고 **training error가 오히려 높음** (test error도 더 높게 유지)

과적합이라면 training error는 낮아야 하는데 그것조차 높다. 즉 깊은 모델이 얕은 모델의 해를 **표현할 수 있는데도 학습으로 찾지 못하는** 것 — 이것이 **degradation problem**이다 (기울기 소실 문제와 함께 깊은 모델 학습을 막던 벽).

### 아이디어 잔차 블록 (p.94-95)

작은 모델(10+레이어)의 성능은 "최소한" 누릴 수 있도록 — 작은 레이어의 추정값(입력값)을 그대로 후속 레이어에 제공하자.

```text
       x ──┬── Conv(3x3) → ReLU → Conv(3x3) ──> F(x) ──(+)──> ReLU → 출력 F(x)+x
           │                                          │
           └── Shortcut 경로 (Identity mapping) ───────┘
```

- 입력 $x$가 두 경로로 나뉜다: 변환 경로 $F(x)$ (Conv→ReLU→Conv), 지름길 경로 $x$
- 마지막에 두 값을 더한다: $\text{Output} = F(x) + x$
- **학습이 잘 안 되면 $F(x) = 0$이 될 수 있고, 그러면 출력은 $x$** — 웨이트를 0으로 초기화하면 작은 모델과 동일한 예측. 깊게 쌓아도 **최소 성능이 보장**된다
- 레이어가 전체 변환 $H(x)$ 대신 **잔차(Residual) $F(x) = H(x) - x$만 학습**하면 되므로 학습이 쉬워진다

### 일반 잔차 블록 vs 보틀넥 잔차 블록 (p.96-97)

연산 효율을 위해 보틀넥(Bottleneck) 구조를 도입했다. 28×28×256 입력 기준 ($C = 64$, $4C = 256$):

```text
[일반 잔차 블록]                  [보틀넥 잔차 블록]
Conv(3x3, C→C)                   Conv(1x1, 4C→C)   : 64개 필터로 채널 축소  28x28x64
Conv(3x3, C→C)                   Conv(3x3, C→C)    : 64개 맵에만 3x3 적용   28x28x64
                                 Conv(1x1, C→4C)   : 256 필터로 복원        28x28x256
전체 FLOPs: 18HWC^2               전체 FLOPs: 17HWC^2
```

$18HWC^2$ vs $17HWC^2$ — **더 적은 연산으로 레이어는 2개→3개로 더 깊게**. 1×1 합성곱으로 채널을 눌렀다 펴는 이 기법은 Inception(GoogLeNet)에서 차원 축소로 활용된 것을 가져왔다 (p.91).

### 전체 구조 스템과 전역 평균 풀링 (p.98-101)

기존 모델의 효율성 레시피를 조합했다:

1. **Stem 구조**를 모델 초기에 도입 — 7×7 conv(stride 2) + 풀링으로 입구에서 해상도를 1/4로 줄이고 이후 잔차 블록 적용 (AlexNet Conv1의 교훈)
2. **여러 개의 FC 레이어 제거** (GoogLeNet에서 제안) — 전역 평균 풀링(Global Average Pooling)을 사용해 마지막 1개 FC만 적용 (VGG 파라미터 병목의 해소)

```text
Input → [Stem: 7x7 conv, /2 → Pool] → [64 블록들] → [128 블록들, /2] → [256 블록들] → [512 블록들] → Pool(전역 평균) → FC 1000 → Softmax
```

스테이지가 바뀔 때마다 채널 2배·해상도 절반 — VGG의 룰이 그대로 살아 있다. 패밀리 구성표 (p.101, 원문 그대로):

| 모델 | Block Type | Stem | S1 Blocks/Layers | S2 | S3 | S4 | FC | GFLOP | ImageNet top-5 error |
|---|---|---|---|---|---|---|---|---|---|
| ResNet-18 | Basic | 1 | 2 / 4 | 2 / 4 | 2 / 4 | 2 / 4 | 1 | 1.8 | 10.92 |
| ResNet-34 | Basic | 1 | 3 / 6 | 4 / 8 | 6 / 12 | 3 / 6 | 1 | 3.6 | 8.58 |
| ResNet-50 | Bottle | 1 | 3 / 9 | 4 / 12 | 6 / 18 | 3 / 9 | 1 | 3.8 | 7.13 |
| ResNet-101 | Bottle | 1 | 3 / 9 | 4 / 12 | 23 / 69 | 3 / 9 | 1 | 7.6 | 6.44 |
| ResNet-152 | Bottle | 1 | 3 / 9 | 8 / 24 | 36 / 108 | 3 / 9 | 1 | 11.3 | 5.94 |

- **ResNet-34와 ResNet-50의 차이는 Basic 잔차 블록 vs 보틀넥 잔차 블록 사용 여부** — 같은 블록 수(3-4-6-3)로 GFLOP는 3.6→3.8 거의 그대로인데 에러는 8.58→7.13
- VGG-16(13.6 GFLOPs, top-5 9.62)과 비교하면 ResNet-18(1.8 GFLOPs, 10.92)·34(3.6, 8.58)가 훨씬 싸고 비슷하거나 더 정확하다 (p.100, torchvision 224×224 single-crop 기준)
- **ResNet-50은 지금도 활발히 사용되는 CNN 기반 강력한 베이스라인**

### 학습 레시피 (p.102)

잘 알려진 ResNet 학습 레시피, 원문 그대로:

- 모든 Conv 레이어 적용 시 **배치정규화** 수행
- **Xavier 초기화 (He 초기화)** 적용 / 그레디언트 소실 문제 다룸
- **SGD + 모멘텀(0.9)** 사용
- 학습 비율 0.1 적용, 단 Val 오류가 정체되면 1/10로 비율 축소
- 미니 배치 사이즈는 256 사용
- 웨이트 디케이 1e-5
- **드롭아웃 미사용**

> **주의** — 원논문(He et al., CVPR 2016)의 weight decay는 1e-4다(슬라이드는 1e-5). 또 Xavier(Glorot) 초기화와 He(MSRA) 초기화는 서로 다른 방법인데 슬라이드는 괄호로 묶어 동일시했다 — ResNet이 실제로 쓴 것은 He 초기화. 시험이 아니라 실무 재현이 목적이면 논문 값을 따르자.

### 무엇을 해결했나, 성과 (p.103, p.117)

- **해결**: 깊은 CNN이 학습되지 않는 문제(기울기 소실, degradation)를 잔차 연결로 구조적으로 해결 — **100+층(50~152층) 초심층 모델 학습 가능**
- 성과: MSRA 팀이 ILSVRC & COCO 2015의 **5개 메인 트랙 전부 1위** (ImageNet Classification "Ultra-deep" 152층 / Detection 2위 대비 16%↑ / Localization 27%↑ / COCO Detection 11%↑ / Segmentation 12%↑)
- 응용 예: 자율주행 객체 인식, 의료 영상 종양 탐지, CCTV 사람/차량 인식 시스템 백본 모델
- **남긴 한계**: 성능은 잡았지만 모바일·엣지에서 돌리기엔 여전히 무겁다 → MobileNet

---

## MobileNet 경량화

### 아이디어 공간과 채널의 분리 (p.105)

2017년. 목표: **모바일/임베디드 환경에서 구동 가능**한 모델.

기존 합성곱은 공간($H \times W$)과 채널($C$)을 **동시에** 처리해 연산량이 과도하다. MobileNet의 핵심은 이걸 **두 단계로 분리**하는 것: **깊이별 합성곱(Depthwise Conv)** — 각 채널별로 독립적인 3×3 합성곱(채널 간 섞지 않음), 그다음 **화소별 합성곱(Pointwise Conv)** — 1×1 합성곱을 채널 방향으로 적용(공간은 안 보고 채널만 섞음).

### 연산량 비교 (p.106-108)

```text
[일반 합성곱 블록]                [깊이별/화소별 분리 블록]
Conv(3x3, C→C)    : 9C^2HW       Conv(3x3, C→C, groups=C)  "깊이별"  : 9CHW
Batch Norm                       Batch Norm → ReLU
ReLU                             Conv(1x1, C→C)            "화소별"  : C^2HW
                                 Batch Norm → ReLU
```

$$\text{연산량 절감 비율} = \frac{9C^2HW}{9CHW + C^2HW} = \frac{9C}{9+C} \;\Rightarrow\; 9 \;(C \to \infty)$$

- 채널이 충분히 크면 **연산량 기존 대비 9배 가량 절감**, 모델 상수도 대폭 감소
- PyTorch식으로는 `groups=C` 옵션이 깊이별 합성곱이다 (p.106 그림의 표기)

### 무엇을 해결했나, 영향 (p.109, p.117)

- **해결**: 스마트폰/IoT 환경에서 너무 무거운 모델 → 경량화. 가볍고 빠른 모델로 **모바일 디바이스에서 딥러닝 모델 실시간 실행 가능함을 보임**
- **효율형 모델 구조의 표준 제시** — 깊이별 합성곱과 보틀넥 구조는 이후 경량 모델들이 지속적으로 채택
- 응용 예: 스마트폰 실시간 얼굴 필터, 모바일 OCR(문자 인식), 키오스크/IoT 디바이스 실시간 AI 모델

---

## 복습 문제

원본 확인문제 3개(p.110-115)를 옮겼다.

**1.** CNN 기반 모델 변천사를 순서대로 나열하면? (A. MobileNet / B. VGG / C. AlexNet / D. GoogLeNet / E. ResNet) — <details><summary>답</summary>**C→B→D→E→A.** AlexNet(2012) ILSVRC 우승·딥러닝 CNN의 본격적 시작점 → VGG(2014) 3×3 작은 필터를 깊게 쌓은 단순·강력한 구조 → GoogLeNet/Inception(2014) Inception 모듈로 효율적 연산 → ResNet(2015) Residual Connection(잔차 연결)으로 기울기 소실 해결 → MobileNet(2017) Depthwise Separable Convolution으로 모바일·임베디드 최적화. 각 모델은 깊이·효율성·연결 구조·경량화 등 CNN의 한계를 단계적으로 극복하며 등장했다.</details>

**2.** CNN 모델의 특징과 가장 먼 것은? — ① 모든 화소를 1차원 벡터로 나열해 입력으로 활용 ② 기본 구조는 합성곱→활성화→풀링→완전연결층 ③ 지역적 특징을 먼저 학습, 깊어질수록 추상적 특징 학습 ④ 위치 변화·노이즈에 강건 ⑤ 깊어질수록 수용영역이 커져 넓은 맥락 이해 — <details><summary>답</summary>**①.** 1차원으로 펴서 입력하는 것은 FCN의 방식이다. CNN도 마지막 분류 단계에서 FC layer 앞에 feature map을 1차원으로 펴긴 하지만 이는 출력 단계의 보조 과정이고, CNN의 핵심은 입력의 2차원 구조를 보존한 채 합성곱·풀링으로 지역적 특징을 추출하는 것이다.</details>

**3.** CNN에서 풀링(Pooling)의 주요 목적이 아닌 것은? — ① 차원 축소 및 연산량 감소 ② 위치 변화에 대한 불변성 확보 ③ 네트워크의 파라미터 수 증가 ④ 지역적 특징을 요약하여 다음 층에 전달 — <details><summary>답</summary>**③.** 풀링 연산 자체는 학습 파라미터가 없는 비학습 연산이며, 오히려 feature map 크기를 줄여 다음 층의 연산량과 파라미터 수를 감소시킨다. ①②④는 모두 풀링의 목적이 맞다.</details>

---

## 정리 체크리스트

강의 정리(p.116)의 세 질문을 포함해 자기 점검용으로 재구성했다.

- [ ] CNN의 기본 구조(합성곱 → 활성화 → 풀링 → 완전연결층)와, FCN 대비 CNN이 보존하는 것을 말할 수 있다
- [ ] $H' = (H - K + 2P)/S + 1$과 메모리·파라미터·FLOPs 공식으로 임의 Conv 층의 비용을 계산할 수 있다
- [ ] 필터/수용영역/패딩/스트라이드 용어와, 풀링 vs 스트라이드 합성곱의 차이(학습 상수 유무)를 설명할 수 있다
- [ ] 왜 Conv 사이에 ReLU가 필요한지($W_3W_2W_1$ 접힘) 설명할 수 있다
- [ ] AlexNet의 층 구성과 "메모리는 앞 conv, 파라미터는 FC, 연산은 conv" 분포를 말할 수 있다
- [ ] VGG의 디자인 룰 3개와 3×3 두 번 = 5×5 수용영역($18C^2$ vs $25C^2$)을 설명할 수 있다
- [ ] degradation problem이 과적합과 어떻게 다른지, 잔차 블록 $F(x)+x$가 왜 최소 성능을 보장하는지 설명할 수 있다
- [ ] 보틀넥 블록에서 1×1 합성곱의 역할 두 가지(축소/복원)를 말할 수 있다
- [ ] 깊이별/화소별 합성곱의 연산량($9CHW + C^2HW$)과 절감 비율 극한(9배)을 유도할 수 있다
- [ ] AlexNet → VGG → GoogLeNet → ResNet → MobileNet 순서와 각각의 혁신 포인트를 말할 수 있다

---

> **읽은 방식 메모** — 전체 117페이지를 2200px OCR로 읽고, 수치·표·아키텍처가 있는 26개 페이지(p.31, 37, 42, 44, 49, 53, 56, 66, 70, 73, 79, 80, 83, 85-87, 91, 94, 96, 98, 100-102, 106, 108, 117)와 앞부분 p.17, 24, 28은 1600px(기존 1100px 포함) 이미지로 재확인했다. 본문 표(AlexNet p.73, ResNet p.101, 비교 수치 p.85-87)는 모두 이미지 대조를 마친 값이다. p.17-25 박스필터 격자의 개별 셀 값은 노트에 옮기지 않고 개념만 정리했다. 슬라이드의 한국어 서술문 일부는 OCR 조사·어미 오류를 문맥으로 복원했다.
